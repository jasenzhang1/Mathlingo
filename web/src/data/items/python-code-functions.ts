import type { Item, SourceRef } from "../../lib/assessment/types";

/**
 * `code`-format items for the Functions chapter — see
 * items/python-code-exercises.ts for the format and its two authoring rules
 * (the reference solution passes every test; no test passes against the
 * starter stub). Three per concept, so each lesson here can meet
 * CODE_ITEM_QUOTA — including `python-functions`, which had none.
 */

const AUTHORED: SourceRef = {
  id: "mathlingo-authored-python",
  tier: "generated",
  title: "Mathlingo authored item (Python sweep)",
};

export const pythonCodeFunctionsItems: Item[] = [
  {
    id: "python-functions--code-rectangle-area",
    conceptId: "python-functions",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function area(width, height) that RETURNS the product of the two numbers. It must return the value, not print it.",
    starterCode: "def area(width, height):\n    pass\n",
    referenceSolution: "def area(width, height):\n    return width * height\n",
    codeTests: [
      {
        id: "basic",
        description: "area(3, 4) == 12",
        run: "result = area(3, 4)",
        check: "result == 12",
      },
      {
        id: "zero",
        description: "area(0, 5) == 0",
        run: "result = area(0, 5)",
        check: "result == 0",
      },
      {
        id: "returns-not-prints",
        description: "the value comes back to the caller, so it can be used in arithmetic",
        run: "result = area(2, 3) + 1",
        check: "result == 7",
      },
    ],
    difficulty: 0.2,
    discrimination: 1.2,
    expectedSeconds: 75,
    prereqClosure: ["python-functions"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-functions--code-early-return",
    conceptId: "python-functions",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function describe(n) that returns \"zero\" when n is 0, \"negative\" when n is below 0, and \"positive\" otherwise. Use early returns rather than one nested if.",
    starterCode: "def describe(n):\n    pass\n",
    referenceSolution: "def describe(n):\n    if n == 0:\n        return \"zero\"\n    if n < 0:\n        return \"negative\"\n    return \"positive\"\n",
    codeTests: [
      {
        id: "zero",
        description: "describe(0) == \"zero\"",
        run: "result = describe(0)",
        check: "result == \"zero\"",
      },
      {
        id: "negative",
        description: "describe(-4) == \"negative\"",
        run: "result = describe(-4)",
        check: "result == \"negative\"",
      },
      {
        id: "positive",
        description: "describe(9) == \"positive\"",
        run: "result = describe(9)",
        check: "result == \"positive\"",
      },
    ],
    difficulty: 0.35,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-functions"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-functions--code-two-values",
    conceptId: "python-functions",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function halve(n) that returns two values: the whole number of times 2 goes into n, and the remainder. halve(7) gives (3, 1).",
    starterCode: "def halve(n):\n    pass\n",
    referenceSolution: "def halve(n):\n    return n // 2, n % 2\n",
    codeTests: [
      {
        id: "odd",
        description: "halve(7) == (3, 1)",
        run: "result = halve(7)",
        check: "result == (3, 1)",
      },
      {
        id: "even",
        description: "halve(8) == (4, 0)",
        run: "result = halve(8)",
        check: "result == (4, 0)",
      },
      {
        id: "unpacks",
        description: "the caller can unpack the result into two names",
        run: "whole, rest = halve(9)",
        check: "whole == 4 and rest == 1",
      },
    ],
    difficulty: 0.5,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-functions"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-arguments--code-greet-default",
    conceptId: "python-arguments",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function greet(name, greeting=\"Hello\") that returns \"greeting, name!\" — so greet(\"Ada\") is \"Hello, Ada!\" and greet(\"Bo\", \"Hi\") is \"Hi, Bo!\".",
    starterCode: "def greet(name, greeting=\"Hello\"):\n    pass\n",
    referenceSolution: "def greet(name, greeting=\"Hello\"):\n    return f\"{greeting}, {name}!\"\n",
    codeTests: [
      {
        id: "default",
        description: "greet(\"Ada\") == \"Hello, Ada!\"",
        run: "result = greet(\"Ada\")",
        check: "result == \"Hello, Ada!\"",
      },
      {
        id: "override",
        description: "greet(\"Bo\", \"Hi\") == \"Hi, Bo!\"",
        run: "result = greet(\"Bo\", \"Hi\")",
        check: "result == \"Hi, Bo!\"",
      },
      {
        id: "keyword",
        description: "the second argument can be passed by name",
        run: "result = greet(\"Cy\", greeting=\"Yo\")",
        check: "result == \"Yo, Cy!\"",
      },
    ],
    difficulty: 0.35,
    discrimination: 1.2,
    expectedSeconds: 90,
    prereqClosure: ["python-arguments"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-arguments--code-safe-default",
    conceptId: "python-arguments",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function collect(item, bucket=None) that appends item to bucket and returns it, creating a NEW empty list when bucket is not given. Two separate calls with no bucket must not share a list.",
    starterCode: "def collect(item, bucket=None):\n    pass\n",
    referenceSolution: "def collect(item, bucket=None):\n    if bucket is None:\n        bucket = []\n    bucket.append(item)\n    return bucket\n",
    codeTests: [
      {
        id: "first-call",
        description: "collect(1) == [1]",
        run: "result = collect(1)",
        check: "result == [1]",
      },
      {
        id: "no-sharing",
        description: "a second call with no bucket starts empty again",
        run: "collect(1)\nresult = collect(2)",
        check: "result == [2]",
      },
      {
        id: "explicit-bucket",
        description: "a bucket that is passed in is appended to and returned",
        run: "given = [0]\nresult = collect(1, given)",
        check: "result == [0, 1] and given == [0, 1]",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-arguments"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-arguments--code-keyword-only-total",
    conceptId: "python-arguments",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function total(price, quantity=1, discount=0.0) returning price * quantity reduced by the discount fraction. total(10) is 10.0; total(10, 2, 0.5) is 10.0.",
    starterCode: "def total(price, quantity=1, discount=0.0):\n    pass\n",
    referenceSolution: "def total(price, quantity=1, discount=0.0):\n    return price * quantity * (1 - discount)\n",
    codeTests: [
      {
        id: "defaults",
        description: "total(10) == 10.0",
        run: "result = total(10)",
        check: "abs(result - 10.0) < 1e-9",
      },
      {
        id: "quantity",
        description: "total(10, 3) == 30.0",
        run: "result = total(10, 3)",
        check: "abs(result - 30.0) < 1e-9",
      },
      {
        id: "discount",
        description: "total(10, 2, 0.5) == 10.0",
        run: "result = total(10, 2, 0.5)",
        check: "abs(result - 10.0) < 1e-9",
      },
      {
        id: "by-keyword",
        description: "discount can be given by name, skipping quantity",
        run: "result = total(100, discount=0.25)",
        check: "abs(result - 75.0) < 1e-9",
      },
    ],
    difficulty: 0.6,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["python-arguments"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-args-kwargs--code-sum-all",
    conceptId: "python-args-kwargs",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function sum_all(*numbers) that returns the total of however many numbers it is given. sum_all() is 0; sum_all(1, 2, 3) is 6.",
    starterCode: "def sum_all(*numbers):\n    pass\n",
    referenceSolution: "def sum_all(*numbers):\n    return sum(numbers)\n",
    codeTests: [
      {
        id: "three",
        description: "sum_all(1, 2, 3) == 6",
        run: "result = sum_all(1, 2, 3)",
        check: "result == 6",
      },
      {
        id: "none",
        description: "sum_all() == 0 — args is an empty tuple, not None",
        run: "result = sum_all()",
        check: "result == 0",
      },
      {
        id: "one",
        description: "sum_all(5) == 5",
        run: "result = sum_all(5)",
        check: "result == 5",
      },
      {
        id: "spread",
        description: "a list can be spread into the call with *",
        run: "values = [1, 2, 3, 4]\nresult = sum_all(*values)",
        check: "result == 10",
      },
    ],
    difficulty: 0.6,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-args-kwargs"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-args-kwargs--code-describe-options",
    conceptId: "python-args-kwargs",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function options(**settings) that returns a list of \"key=value\" strings, sorted by key. options(b=2, a=1) is [\"a=1\", \"b=2\"].",
    starterCode: "def options(**settings):\n    pass\n",
    referenceSolution: "def options(**settings):\n    return [f\"{k}={settings[k]}\" for k in sorted(settings)]\n",
    codeTests: [
      {
        id: "sorted",
        description: "options(b=2, a=1) == [\"a=1\", \"b=2\"]",
        run: "result = options(b=2, a=1)",
        check: "result == [\"a=1\", \"b=2\"]",
      },
      {
        id: "empty",
        description: "options() == [] — kwargs is an empty dict",
        run: "result = options()",
        check: "result == []",
      },
      {
        id: "spread",
        description: "a dict can be spread into the call with **",
        run: "conf = {\"x\": 1, \"y\": 2}\nresult = options(**conf)",
        check: "result == [\"x=1\", \"y=2\"]",
      },
    ],
    difficulty: 0.75,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["python-args-kwargs"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-args-kwargs--code-passthrough",
    conceptId: "python-args-kwargs",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function call_twice(fn, *args, **kwargs) that calls fn with the given arguments twice and returns a list of the two results. It must work for any signature fn has.",
    starterCode: "def call_twice(fn, *args, **kwargs):\n    pass\n",
    referenceSolution: "def call_twice(fn, *args, **kwargs):\n    return [fn(*args, **kwargs), fn(*args, **kwargs)]\n",
    codeTests: [
      {
        id: "positional",
        description: "call_twice(abs, -3) == [3, 3]",
        run: "result = call_twice(abs, -3)",
        check: "result == [3, 3]",
      },
      {
        id: "keyword",
        description: "keyword arguments pass through untouched",
        run: "def f(a, b=0):\n    return a + b\nresult = call_twice(f, 1, b=2)",
        check: "result == [3, 3]",
      },
      {
        id: "no-args",
        description: "a function taking nothing still works",
        run: "result = call_twice(dict)",
        check: "result == [{}, {}]",
      },
    ],
    difficulty: 0.95,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["python-args-kwargs"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-scope--code-no-leak",
    conceptId: "python-scope",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function tally(numbers) that returns the sum, using a local variable named total. The name total must not exist at module level after the call.",
    starterCode: "def tally(numbers):\n    pass\n",
    referenceSolution: "def tally(numbers):\n    total = 0\n    for n in numbers:\n        total += n\n    return total\n",
    codeTests: [
      {
        id: "sums",
        description: "tally([1, 2, 3]) == 6",
        run: "result = tally([1, 2, 3])",
        check: "result == 6",
      },
      {
        id: "empty",
        description: "tally([]) == 0",
        run: "result = tally([])",
        check: "result == 0",
      },
      {
        id: "local-only",
        description: "the function's local does not leak into the module namespace",
        run: "out = tally([1, 2])\nleaked = \"total\" in dir()",
        check: "out == 3 and not leaked",
      },
    ],
    difficulty: 0.5,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-scope"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-scope--code-counter-closure",
    conceptId: "python-scope",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function make_counter() that returns a function. Each call to that returned function gives the next integer starting at 1. Two counters must count independently.",
    starterCode: "def make_counter():\n    pass\n",
    referenceSolution: "def make_counter():\n    n = 0\n    def bump():\n        nonlocal n\n        n += 1\n        return n\n    return bump\n",
    codeTests: [
      {
        id: "counts",
        description: "successive calls give 1, 2, 3",
        run: "c = make_counter()\nresult = [c(), c(), c()]",
        check: "result == [1, 2, 3]",
      },
      {
        id: "independent",
        description: "two counters do not share state",
        run: "a = make_counter()\nb = make_counter()\na()\na()\nresult = b()",
        check: "result == 1",
      },
      {
        id: "returns-callable",
        description: "make_counter returns a function, not a number",
        run: "c = make_counter()",
        check: "callable(c)",
      },
    ],
    difficulty: 0.95,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["python-scope"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-scope--code-shadow-safely",
    conceptId: "python-scope",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "A module-level list called registry already exists. Write a function add(name) that appends name to that same module-level list and returns its new length — without rebinding registry.",
    starterCode: "registry = []\n\n\ndef add(name):\n    pass\n",
    referenceSolution: "registry = []\n\n\ndef add(name):\n    registry.append(name)\n    return len(registry)\n",
    codeTests: [
      {
        id: "appends",
        description: "the module-level list actually grows",
        run: "add(\"a\")\nadd(\"b\")",
        check: "registry == [\"a\", \"b\"]",
      },
      {
        id: "returns-length",
        description: "the new length comes back",
        run: "result = add(\"x\")",
        check: "result == len(registry) and result >= 1",
      },
      {
        id: "mutation-needs-no-global",
        description: "two calls accumulate in one list",
        run: "add(\"p\")\nresult = add(\"q\")",
        check: "result == 2",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["python-scope"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-lambda--code-make-doubler",
    conceptId: "python-lambda",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Assign to a name called double a LAMBDA expression that takes one number and returns twice it. Use lambda, not def.",
    starterCode: "double = None\n",
    referenceSolution: "double = lambda n: n * 2\n",
    codeTests: [
      {
        id: "doubles",
        description: "double(4) == 8",
        run: "result = double(4)",
        check: "result == 8",
      },
      {
        id: "negative",
        description: "double(-3) == -6",
        run: "result = double(-3)",
        check: "result == -6",
      },
      {
        id: "is-a-lambda",
        description: "double is a callable created by a lambda expression",
        run: "result = callable(double) and getattr(double, \"__name__\", \"\") == \"<lambda>\"",
        check: "result",
      },
    ],
    difficulty: 0.5,
    discrimination: 1.2,
    expectedSeconds: 90,
    prereqClosure: ["python-lambda"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-lambda--code-conditional-expression",
    conceptId: "python-lambda",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Assign to parity a lambda taking one integer and returning \"even\" or \"odd\". A lambda body cannot contain statements, so use a conditional expression.",
    starterCode: "parity = None\n",
    referenceSolution: "parity = lambda n: \"even\" if n % 2 == 0 else \"odd\"\n",
    codeTests: [
      {
        id: "even",
        description: "parity(4) == \"even\"",
        run: "result = parity(4)",
        check: "result == \"even\"",
      },
      {
        id: "odd",
        description: "parity(7) == \"odd\"",
        run: "result = parity(7)",
        check: "result == \"odd\"",
      },
      {
        id: "zero",
        description: "parity(0) == \"even\"",
        run: "result = parity(0)",
        check: "result == \"even\"",
      },
      {
        id: "is-a-lambda",
        description: "parity was built with lambda",
        run: "result = getattr(parity, \"__name__\", \"\") == \"<lambda>\"",
        check: "result",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["python-lambda"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-lambda--code-lambda-as-argument",
    conceptId: "python-lambda",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function apply_to_all(values, fn) that returns a list with fn applied to each value. Then it must work when the caller passes a lambda.",
    starterCode: "def apply_to_all(values, fn):\n    pass\n",
    referenceSolution: "def apply_to_all(values, fn):\n    return [fn(v) for v in values]\n",
    codeTests: [
      {
        id: "with-lambda",
        description: "apply_to_all([1, 2, 3], lambda n: n + 1) == [2, 3, 4]",
        run: "result = apply_to_all([1, 2, 3], lambda n: n + 1)",
        check: "result == [2, 3, 4]",
      },
      {
        id: "with-builtin",
        description: "a named function works just as well",
        run: "result = apply_to_all([-1, -2], abs)",
        check: "result == [1, 2]",
      },
      {
        id: "empty",
        description: "apply_to_all([], lambda n: n) == []",
        run: "result = apply_to_all([], lambda n: n)",
        check: "result == []",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-lambda"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-higher-order--code-map-to-list",
    conceptId: "python-higher-order",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function lengths(words) that uses map to return a LIST of the length of each word. map returns an iterator, so convert it.",
    starterCode: "def lengths(words):\n    pass\n",
    referenceSolution: "def lengths(words):\n    return list(map(len, words))\n",
    codeTests: [
      {
        id: "basic",
        description: "[\"a\", \"bcd\"] gives [1, 3]",
        run: "result = lengths([\"a\", \"bcd\"])",
        check: "result == [1, 3]",
      },
      {
        id: "is-list",
        description: "the result is a list, not a map object",
        run: "result = lengths([\"x\"])",
        check: "isinstance(result, list)",
      },
      {
        id: "reusable",
        description: "the result can be iterated more than once, unlike a raw map object",
        run: "out = lengths([\"ab\", \"c\"])\nresult = (list(out), list(out))",
        check: "result == ([2, 1], [2, 1])",
      },
    ],
    difficulty: 0.6,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-higher-order"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-higher-order--code-filter-truthy",
    conceptId: "python-higher-order",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function keep_positive(numbers) that uses filter to return a LIST of the strictly positive numbers, in order.",
    starterCode: "def keep_positive(numbers):\n    pass\n",
    referenceSolution: "def keep_positive(numbers):\n    return list(filter(lambda n: n > 0, numbers))\n",
    codeTests: [
      {
        id: "mixed",
        description: "[-1, 2, 0, 3] gives [2, 3]",
        run: "result = keep_positive([-1, 2, 0, 3])",
        check: "result == [2, 3]",
      },
      {
        id: "zero-dropped",
        description: "0 is not strictly positive",
        run: "result = keep_positive([0])",
        check: "result == []",
      },
      {
        id: "is-list",
        description: "the result is a list, not a filter object",
        run: "result = keep_positive([1])",
        check: "isinstance(result, list)",
      },
    ],
    difficulty: 0.75,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["python-higher-order"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-higher-order--code-compose",
    conceptId: "python-higher-order",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function compose(f, g) that returns a NEW function applying g first and then f, so compose(f, g)(x) equals f(g(x)).",
    starterCode: "def compose(f, g):\n    pass\n",
    referenceSolution: "def compose(f, g):\n    def composed(x):\n        return f(g(x))\n    return composed\n",
    codeTests: [
      {
        id: "order",
        description: "compose(str, abs)(-3) == '3' — g runs first",
        run: "result = compose(str, abs)(-3)",
        check: "result == \"3\"",
      },
      {
        id: "with-lambdas",
        description: "two lambdas compose as expected",
        run: "h = compose(lambda n: n + 1, lambda n: n * 2)\nresult = h(5)",
        check: "result == 11",
      },
      {
        id: "returns-function",
        description: "compose gives back something callable, not a value",
        run: "result = compose(abs, abs)",
        check: "callable(result)",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["python-higher-order"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-sorting-key--code-by-length",
    conceptId: "python-sorting-key",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function by_length(words) that returns a NEW list of the words ordered shortest first, leaving the input list unchanged.",
    starterCode: "def by_length(words):\n    pass\n",
    referenceSolution: "def by_length(words):\n    return sorted(words, key=len)\n",
    codeTests: [
      {
        id: "basic",
        description: "[\"pear\", \"fig\"] gives [\"fig\", \"pear\"]",
        run: "result = by_length([\"pear\", \"fig\"])",
        check: "result == [\"fig\", \"pear\"]",
      },
      {
        id: "stable",
        description: "words of equal length keep their original order",
        run: "result = by_length([\"bb\", \"aa\", \"c\"])",
        check: "result == [\"c\", \"bb\", \"aa\"]",
      },
      {
        id: "input-unchanged",
        description: "the sorted list is returned and the input is not sorted in place",
        run: "words = [\"pear\", \"fig\"]\nout = by_length(words)",
        check: "out == [\"fig\", \"pear\"] and words == [\"pear\", \"fig\"]",
      },
    ],
    difficulty: 0.6,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-sorting-key"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-sorting-key--code-descending",
    conceptId: "python-sorting-key",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function largest_first(numbers) that returns a new list ordered from largest to smallest. Use sorted with reverse.",
    starterCode: "def largest_first(numbers):\n    pass\n",
    referenceSolution: "def largest_first(numbers):\n    return sorted(numbers, reverse=True)\n",
    codeTests: [
      {
        id: "basic",
        description: "[1, 3, 2] gives [3, 2, 1]",
        run: "result = largest_first([1, 3, 2])",
        check: "result == [3, 2, 1]",
      },
      {
        id: "negatives",
        description: "[-1, -3] gives [-1, -3]",
        run: "result = largest_first([-1, -3])",
        check: "result == [-1, -3]",
      },
      {
        id: "empty",
        description: "an empty list stays empty",
        run: "result = largest_first([])",
        check: "result == []",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["python-sorting-key"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-sorting-key--code-two-criteria",
    conceptId: "python-sorting-key",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Given a list of (name, age) tuples, write rank(people) returning a new list ordered by age from oldest to youngest, and alphabetically by name within the same age.",
    starterCode: "def rank(people):\n    pass\n",
    referenceSolution: "def rank(people):\n    return sorted(people, key=lambda p: (-p[1], p[0]))\n",
    codeTests: [
      {
        id: "age-descending",
        description: "the oldest comes first",
        run: "result = rank([(\"Ada\", 30), (\"Bo\", 40)])",
        check: "result == [(\"Bo\", 40), (\"Ada\", 30)]",
      },
      {
        id: "name-tiebreak",
        description: "same age is broken alphabetically by name",
        run: "result = rank([(\"Cy\", 30), (\"Ada\", 30)])",
        check: "result == [(\"Ada\", 30), (\"Cy\", 30)]",
      },
      {
        id: "combined",
        description: "both rules apply together",
        run: "result = rank([(\"Cy\", 30), (\"Ada\", 30), (\"Bo\", 41)])",
        check: "result == [(\"Bo\", 41), (\"Ada\", 30), (\"Cy\", 30)]",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["python-sorting-key"],
    source: AUTHORED,
    status: "live",
  },
];
