import type { Item, SourceRef } from "../../lib/assessment/types";

/**
 * `code`-format items for the Iterators & Generators and Classes & Objects
 * chapters.
 *
 * Note for the inheritance items: their starterCode already contains the base
 * class, so a test asserting only base-class behaviour passes against the
 * stub. Each such test also asserts the subclass behaviour, which is what
 * makes it discriminate.
 */

const AUTHORED: SourceRef = {
  id: "mathlingo-authored-python",
  tier: "generated",
  title: "Mathlingo authored item (Python sweep)",
};

export const pythonCodeIterationClassesItems: Item[] = [
  {
    id: "python-iterators--code-manual-next",
    conceptId: "python-iterators",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function first_two(iterable) that returns a list of the first two values, using iter() and next() rather than a for loop or a slice.",
    starterCode: "def first_two(iterable):\n    pass\n",
    referenceSolution: "def first_two(iterable):\n    it = iter(iterable)\n    return [next(it), next(it)]\n",
    codeTests: [
      {
        id: "list",
        description: "first_two([1, 2, 3]) == [1, 2]",
        run: "result = first_two([1, 2, 3])",
        check: "result == [1, 2]",
      },
      {
        id: "works-on-iterator",
        description: "it works on something that cannot be subscripted",
        run: "result = first_two(iter([9, 8, 7]))",
        check: "result == [9, 8]",
      },
      {
        id: "string",
        description: "first_two(\"abc\") == [\"a\", \"b\"]",
        run: "result = first_two(\"abc\")",
        check: "result == [\"a\", \"b\"]",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["python-iterators"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-iterators--code-safe-next",
    conceptId: "python-iterators",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function first_or(iterable, default) returning the first value, or the default when the iterable is empty. Use next with its default argument rather than try/except.",
    starterCode: "def first_or(iterable, default):\n    pass\n",
    referenceSolution: "def first_or(iterable, default):\n    return next(iter(iterable), default)\n",
    codeTests: [
      {
        id: "has-values",
        description: "first_or([5, 6], 0) == 5",
        run: "result = first_or([5, 6], 0)",
        check: "result == 5",
      },
      {
        id: "empty",
        description: "first_or([], -1) == -1",
        run: "result = first_or([], -1)",
        check: "result == -1",
      },
      {
        id: "falsy-first-value",
        description: "a first value of 0 is returned, not mistaken for empty",
        run: "result = first_or([0], 99)",
        check: "result == 0",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-iterators"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-iterators--code-count-consumed",
    conceptId: "python-iterators",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function total_and_count(iterable) returning a (sum, count) tuple. It must work when given a one-shot iterator, so walk it exactly once.",
    starterCode: "def total_and_count(iterable):\n    pass\n",
    referenceSolution: "def total_and_count(iterable):\n    total = 0\n    count = 0\n    for value in iterable:\n        total += value\n        count += 1\n    return (total, count)\n",
    codeTests: [
      {
        id: "list",
        description: "total_and_count([1, 2, 3]) == (6, 3)",
        run: "result = total_and_count([1, 2, 3])",
        check: "result == (6, 3)",
      },
      {
        id: "one-shot",
        description: "a generator, which can only be walked once, still gives both numbers",
        run: "result = total_and_count(x for x in [1, 2, 3])",
        check: "result == (6, 3)",
      },
      {
        id: "empty",
        description: "an empty input gives (0, 0)",
        run: "result = total_and_count([])",
        check: "result == (0, 0)",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["python-iterators"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-generators--code-countdown",
    conceptId: "python-generators",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a GENERATOR function countdown(n) that yields n, n-1, ... down to 1. It must use yield, not build and return a list.",
    starterCode: "def countdown(n):\n    pass\n",
    referenceSolution: "def countdown(n):\n    while n > 0:\n        yield n\n        n -= 1\n",
    codeTests: [
      {
        id: "values",
        description: "list(countdown(3)) == [3, 2, 1]",
        run: "result = list(countdown(3))",
        check: "result == [3, 2, 1]",
      },
      {
        id: "zero",
        description: "countdown(0) yields nothing",
        run: "result = list(countdown(0))",
        check: "result == []",
      },
      {
        id: "is-generator",
        description: "calling it returns a generator, not a list",
        run: "import types\ng = countdown(2)",
        check: "isinstance(g, types.GeneratorType)",
      },
    ],
    difficulty: 0.75,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["python-generators"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-generators--code-take-while-even",
    conceptId: "python-generators",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a generator function evens(numbers) that yields only the even values, lazily. Nothing should be computed until the caller iterates.",
    starterCode: "def evens(numbers):\n    pass\n",
    referenceSolution: "def evens(numbers):\n    for n in numbers:\n        if n % 2 == 0:\n            yield n\n",
    codeTests: [
      {
        id: "filters",
        description: "list(evens([1, 2, 3, 4])) == [2, 4]",
        run: "result = list(evens([1, 2, 3, 4]))",
        check: "result == [2, 4]",
      },
      {
        id: "lazy",
        description: "creating it consumes nothing until the caller iterates, and then yields the evens",
        run: "seen = []\ndef source():\n    for n in [2, 3, 4]:\n        seen.append(n)\n        yield n\ng = evens(source())\nbefore = list(seen)\nout = list(g)",
        check: "before == [] and out == [2, 4]",
      },
      {
        id: "none-even",
        description: "an all-odd input yields nothing, while a mixed one still yields the evens",
        run: "odd_only = list(evens([1, 3]))\nmixed = list(evens([1, 2]))",
        check: "odd_only == [] and mixed == [2]",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["python-generators"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-generators--code-running-total",
    conceptId: "python-generators",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a generator function running(numbers) that yields the cumulative total after each value. running([1, 2, 3]) yields 1, 3, 6.",
    starterCode: "def running(numbers):\n    pass\n",
    referenceSolution: "def running(numbers):\n    total = 0\n    for n in numbers:\n        total += n\n        yield total\n",
    codeTests: [
      {
        id: "accumulates",
        description: "list(running([1, 2, 3])) == [1, 3, 6]",
        run: "result = list(running([1, 2, 3]))",
        check: "result == [1, 3, 6]",
      },
      {
        id: "empty",
        description: "an empty input yields nothing",
        run: "result = list(running([]))",
        check: "result == []",
      },
      {
        id: "state-survives-yield",
        description: "the total persists across yields rather than restarting",
        run: "result = list(running([5, 5, 5]))",
        check: "result == [5, 10, 15]",
      },
    ],
    difficulty: 1.15,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["python-generators"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-itertools--code-flatten",
    conceptId: "python-itertools",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function flatten(lists) that returns one list containing every element of the given lists, in order. Use itertools.chain.",
    starterCode: "from itertools import chain\n\n\ndef flatten(lists):\n    pass\n",
    referenceSolution: "from itertools import chain\n\n\ndef flatten(lists):\n    return list(chain.from_iterable(lists))\n",
    codeTests: [
      {
        id: "basic",
        description: "[[1, 2], [3]] gives [1, 2, 3]",
        run: "result = flatten([[1, 2], [3]])",
        check: "result == [1, 2, 3]",
      },
      {
        id: "empty-inner",
        description: "an empty inner list contributes nothing",
        run: "result = flatten([[1], [], [2]])",
        check: "result == [1, 2]",
      },
      {
        id: "one-level",
        description: "only one level is flattened",
        run: "result = flatten([[[1]], [2]])",
        check: "result == [[1], 2]",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["python-itertools"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-itertools--code-first-n-of-iterator",
    conceptId: "python-itertools",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function take(iterable, n) returning a list of the first n values. It must work on an iterator that cannot be sliced. Use itertools.islice.",
    starterCode: "from itertools import islice\n\n\ndef take(iterable, n):\n    pass\n",
    referenceSolution: "from itertools import islice\n\n\ndef take(iterable, n):\n    return list(islice(iterable, n))\n",
    codeTests: [
      {
        id: "list",
        description: "take([1, 2, 3, 4], 2) == [1, 2]",
        run: "result = take([1, 2, 3, 4], 2)",
        check: "result == [1, 2]",
      },
      {
        id: "iterator",
        description: "it works on a generator, which cannot be sliced",
        run: "result = take((x for x in [5, 6, 7]), 2)",
        check: "result == [5, 6]",
      },
      {
        id: "more-than-available",
        description: "asking for more than there is returns what there is",
        run: "result = take([1], 5)",
        check: "result == [1]",
      },
    ],
    difficulty: 0.95,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-itertools"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-itertools--code-pairs",
    conceptId: "python-itertools",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function pairs(items) returning a list of every unordered pair of distinct elements, in input order. pairs([1, 2, 3]) is [(1, 2), (1, 3), (2, 3)]. Use itertools.combinations.",
    starterCode: "from itertools import combinations\n\n\ndef pairs(items):\n    pass\n",
    referenceSolution: "from itertools import combinations\n\n\ndef pairs(items):\n    return list(combinations(items, 2))\n",
    codeTests: [
      {
        id: "three",
        description: "[1, 2, 3] gives three pairs",
        run: "result = pairs([1, 2, 3])",
        check: "result == [(1, 2), (1, 3), (2, 3)]",
      },
      {
        id: "unordered",
        description: "(2, 1) is not listed separately from (1, 2)",
        run: "result = pairs([1, 2])",
        check: "result == [(1, 2)]",
      },
      {
        id: "too-few",
        description: "fewer than two elements gives no pairs",
        run: "result = pairs([1])",
        check: "result == []",
      },
    ],
    difficulty: 1.1,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["python-itertools"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-classes--code-account",
    conceptId: "python-classes",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Account whose __init__ takes an owner and an optional starting balance (default 0), storing both on the instance, and a deposit(amount) method that increases the balance and returns the new value.",
    starterCode: "class Account:\n    pass\n",
    referenceSolution: "class Account:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount):\n        self.balance += amount\n        return self.balance\n",
    codeTests: [
      {
        id: "init",
        description: "the owner and balance are stored",
        run: "a = Account(\"Ada\", 100)",
        check: "a.owner == \"Ada\" and a.balance == 100",
      },
      {
        id: "default-balance",
        description: "the balance defaults to 0",
        run: "a = Account(\"Bo\")",
        check: "a.balance == 0",
      },
      {
        id: "deposit",
        description: "deposit increases the balance and returns it",
        run: "a = Account(\"Cy\", 10)\nresult = a.deposit(5)",
        check: "result == 15 and a.balance == 15",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-classes"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-classes--code-rectangle",
    conceptId: "python-classes",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Rectangle taking width and height, with an area() method and a scaled(factor) method that returns a NEW Rectangle with both sides multiplied, leaving the original alone.",
    starterCode: "class Rectangle:\n    pass\n",
    referenceSolution: "class Rectangle:\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n\n    def area(self):\n        return self.width * self.height\n\n    def scaled(self, factor):\n        return Rectangle(self.width * factor, self.height * factor)\n",
    codeTests: [
      {
        id: "area",
        description: "a 3x4 rectangle has area 12",
        run: "r = Rectangle(3, 4)\nresult = r.area()",
        check: "result == 12",
      },
      {
        id: "scaled-values",
        description: "scaling by 2 doubles both sides",
        run: "r = Rectangle(2, 3).scaled(2)",
        check: "r.width == 4 and r.height == 6",
      },
      {
        id: "original-unchanged",
        description: "the scaled rectangle is new and the original is untouched",
        run: "r = Rectangle(2, 3)\nbig = r.scaled(10)",
        check: "big.area() == 600 and r.width == 2 and r.height == 3",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-classes"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-classes--code-counter-object",
    conceptId: "python-classes",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Tally with a count starting at 0, a bump() method that adds one and returns the new count, and a reset() method that sets it back to 0. Two Tally objects must not share state.",
    starterCode: "class Tally:\n    pass\n",
    referenceSolution: "class Tally:\n    def __init__(self):\n        self.count = 0\n\n    def bump(self):\n        self.count += 1\n        return self.count\n\n    def reset(self):\n        self.count = 0\n",
    codeTests: [
      {
        id: "counts",
        description: "three bumps give 3",
        run: "t = Tally()\nt.bump()\nt.bump()\nresult = t.bump()",
        check: "result == 3",
      },
      {
        id: "independent",
        description: "two tallies count separately",
        run: "a = Tally()\nb = Tally()\na.bump()\na.bump()\nresult = b.bump()",
        check: "result == 1",
      },
      {
        id: "reset",
        description: "reset returns the count to zero",
        run: "t = Tally()\nt.bump()\nt.reset()",
        check: "t.count == 0",
      },
    ],
    difficulty: 0.95,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["python-classes"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-methods--code-per-instance-list",
    conceptId: "python-methods",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Dog taking a name, with a tricks list that starts empty and a learn(trick) method appending to it. Two dogs must NOT share a tricks list.",
    starterCode: "class Dog:\n    pass\n",
    referenceSolution: "class Dog:\n    def __init__(self, name):\n        self.name = name\n        self.tricks = []\n\n    def learn(self, trick):\n        self.tricks.append(trick)\n",
    codeTests: [
      {
        id: "learns",
        description: "a trick is recorded",
        run: "d = Dog(\"Rex\")\nd.learn(\"sit\")",
        check: "d.tricks == [\"sit\"]",
      },
      {
        id: "not-shared",
        description: "a second dog starts with an empty list",
        run: "a = Dog(\"Rex\")\nb = Dog(\"Fido\")\na.learn(\"sit\")",
        check: "b.tricks == []",
      },
      {
        id: "separate-objects",
        description: "the two lists are genuinely different objects",
        run: "a = Dog(\"Rex\")\nb = Dog(\"Fido\")",
        check: "a.tricks is not b.tricks",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["python-methods"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-methods--code-alt-constructor",
    conceptId: "python-methods",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Temperature holding a celsius value, plus a CLASSMETHOD from_fahrenheit(f) that returns a new Temperature converted from Fahrenheit. Use cls, not the class name.",
    starterCode: "class Temperature:\n    pass\n",
    referenceSolution: "class Temperature:\n    def __init__(self, celsius):\n        self.celsius = celsius\n\n    @classmethod\n    def from_fahrenheit(cls, f):\n        return cls((f - 32) / 1.8)\n",
    codeTests: [
      {
        id: "direct",
        description: "the plain constructor stores the value",
        run: "t = Temperature(20)",
        check: "t.celsius == 20",
      },
      {
        id: "converts",
        description: "212F is 100C",
        run: "t = Temperature.from_fahrenheit(212)",
        check: "abs(t.celsius - 100) < 1e-9",
      },
      {
        id: "returns-instance",
        description: "the classmethod hands back a Temperature",
        run: "t = Temperature.from_fahrenheit(32)",
        check: "isinstance(t, Temperature) and abs(t.celsius) < 1e-9",
      },
    ],
    difficulty: 1.15,
    discrimination: 1.2,
    expectedSeconds: 195,
    prereqClosure: ["python-methods"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-methods--code-class-attribute",
    conceptId: "python-methods",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Robot with a CLASS attribute total that counts how many robots have been created, incremented in __init__, plus an instance attribute name.",
    starterCode: "class Robot:\n    pass\n",
    referenceSolution: "class Robot:\n    total = 0\n\n    def __init__(self, name):\n        self.name = name\n        Robot.total += 1\n",
    codeTests: [
      {
        id: "counts",
        description: "creating three robots sets total to 3",
        run: "Robot(\"a\")\nRobot(\"b\")\nRobot(\"c\")",
        check: "Robot.total == 3",
      },
      {
        id: "shared",
        description: "the count is visible from any instance",
        run: "a = Robot(\"x\")\nb = Robot(\"y\")",
        check: "a.total == b.total",
      },
      {
        id: "instance-name",
        description: "each robot keeps its own name",
        run: "a = Robot(\"x\")\nb = Robot(\"y\")",
        check: "a.name == \"x\" and b.name == \"y\"",
      },
    ],
    difficulty: 1.05,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["python-methods"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-dunder--code-repr",
    conceptId: "python-dunder",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Point taking x and y, with a __repr__ that returns exactly \"Point(1, 2)\" for a point with x=1 and y=2.",
    starterCode: "class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n",
    referenceSolution: "class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n    def __repr__(self):\n        return f\"Point({self.x}, {self.y})\"\n",
    codeTests: [
      {
        id: "format",
        description: "repr gives the expected text",
        run: "result = repr(Point(1, 2))",
        check: "result == \"Point(1, 2)\"",
      },
      {
        id: "used-in-a-list",
        description: "a list of points prints using it",
        run: "result = repr([Point(0, 0)])",
        check: "result == \"[Point(0, 0)]\"",
      },
      {
        id: "negatives",
        description: "negative coordinates render correctly",
        run: "result = repr(Point(-1, 3))",
        check: "result == \"Point(-1, 3)\"",
      },
    ],
    difficulty: 0.9,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-dunder"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-dunder--code-eq-and-hash",
    conceptId: "python-dunder",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Extend Point with __eq__ comparing x and y, and __hash__ over the same pair so instances can go in a set. Comparing with a non-Point must return NotImplemented.",
    starterCode: "class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n",
    referenceSolution: "class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n    def __eq__(self, other):\n        if not isinstance(other, Point):\n            return NotImplemented\n        return (self.x, self.y) == (other.x, other.y)\n\n    def __hash__(self):\n        return hash((self.x, self.y))\n",
    codeTests: [
      {
        id: "equal",
        description: "two points with the same coordinates are equal",
        run: "result = Point(1, 2) == Point(1, 2)",
        check: "result is True",
      },
      {
        id: "unequal",
        description: "different coordinates are not equal, while identical ones are",
        run: "same = Point(1, 2) == Point(1, 2)\ndiff = Point(1, 2) == Point(3, 4)",
        check: "same is True and diff is False",
      },
      {
        id: "hashable",
        description: "equal points collapse to one entry in a set",
        run: "result = len({Point(1, 2), Point(1, 2)})",
        check: "result == 1",
      },
      {
        id: "other-type",
        description: "comparing with a non-Point is False, while two equal Points still compare equal",
        run: "other = (Point(1, 2) == \"nope\")\nsame = (Point(1, 2) == Point(1, 2))",
        check: "other is False and same is True",
      },
    ],
    difficulty: 1.25,
    discrimination: 1.2,
    expectedSeconds: 210,
    prereqClosure: ["python-dunder"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-dunder--code-len-and-truthiness",
    conceptId: "python-dunder",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Define a class Basket taking a list of items, with __len__ returning how many items it holds. An empty basket must then be falsy without you writing __bool__.",
    starterCode: "class Basket:\n    def __init__(self, items):\n        self.items = items\n",
    referenceSolution: "class Basket:\n    def __init__(self, items):\n        self.items = items\n\n    def __len__(self):\n        return len(self.items)\n",
    codeTests: [
      {
        id: "len",
        description: "len() reports the item count",
        run: "result = len(Basket([1, 2, 3]))",
        check: "result == 3",
      },
      {
        id: "empty-is-falsy",
        description: "an empty basket is falsy",
        run: "result = bool(Basket([]))",
        check: "result is False",
      },
      {
        id: "nonempty-is-truthy",
        description: "a basket with items is truthy and an empty one is not",
        run: "full = bool(Basket([1]))\nempty = bool(Basket([]))",
        check: "full is True and empty is False",
      },
    ],
    difficulty: 1.2,
    discrimination: 1.2,
    expectedSeconds: 195,
    prereqClosure: ["python-dunder"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-inheritance--code-override",
    conceptId: "python-inheritance",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Given a class Animal with a speak() returning \"...\", define Dog(Animal) whose speak() returns \"woof\". Dog must still be usable anywhere an Animal is.",
    starterCode: "class Animal:\n    def speak(self):\n        return \"...\"\n\n\nclass Dog:\n    pass\n",
    referenceSolution: "class Animal:\n    def speak(self):\n        return \"...\"\n\n\nclass Dog(Animal):\n    def speak(self):\n        return \"woof\"\n",
    codeTests: [
      {
        id: "overrides",
        description: "Dog().speak() == \"woof\"",
        run: "result = Dog().speak()",
        check: "result == \"woof\"",
      },
      {
        id: "base-unchanged",
        description: "the subclass says \"woof\" while Animal still says \"...\"",
        run: "sub = Dog().speak()\nbase = Animal().speak()",
        check: "sub == \"woof\" and base == \"...\"",
      },
      {
        id: "is-a",
        description: "a Dog is an Animal",
        run: "result = isinstance(Dog(), Animal)",
        check: "result is True",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["python-inheritance"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-inheritance--code-super-init",
    conceptId: "python-inheritance",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Given Animal whose __init__ stores a name, define Dog(Animal) whose __init__ takes name and breed, stores the breed, and delegates the name to the base class with super().",
    starterCode: "class Animal:\n    def __init__(self, name):\n        self.name = name\n\n\nclass Dog(Animal):\n    pass\n",
    referenceSolution: "class Animal:\n    def __init__(self, name):\n        self.name = name\n\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n",
    codeTests: [
      {
        id: "both-attributes",
        description: "the dog has both a name and a breed",
        run: "d = Dog(\"Rex\", \"collie\")",
        check: "d.name == \"Rex\" and d.breed == \"collie\"",
      },
      {
        id: "base-ran",
        description: "the base initialiser actually ran rather than the name being set directly",
        run: "d = Dog(\"Fido\", \"lab\")",
        check: "hasattr(d, \"name\") and d.name == \"Fido\"",
      },
      {
        id: "still-an-animal",
        description: "the subclass relationship holds",
        run: "d = Dog(\"Rex\", \"collie\")",
        check: "isinstance(d, Animal)",
      },
    ],
    difficulty: 1.1,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["python-inheritance"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "python-inheritance--code-extend-not-replace",
    conceptId: "python-inheritance",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Given a class Logger with a format(msg) returning the message unchanged, define Timestamped(Logger) whose format returns \"[t] \" prepended to whatever the BASE format produced — by calling super(), not by reimplementing it.",
    starterCode: "class Logger:\n    def format(self, msg):\n        return msg\n\n\nclass Timestamped(Logger):\n    pass\n",
    referenceSolution: "class Logger:\n    def format(self, msg):\n        return msg\n\n\nclass Timestamped(Logger):\n    def format(self, msg):\n        return \"[t] \" + super().format(msg)\n",
    codeTests: [
      {
        id: "prepends",
        description: "Timestamped().format(\"hi\") == \"[t] hi\"",
        run: "result = Timestamped().format(\"hi\")",
        check: "result == \"[t] hi\"",
      },
      {
        id: "uses-base",
        description: "a change to the base implementation flows through",
        run: "class Shouty(Logger):\n    def format(self, msg):\n        return msg.upper()\nclass Both(Timestamped, Shouty):\n    pass\nresult = Both().format(\"hi\")",
        check: "result == \"[t] HI\"",
      },
      {
        id: "base-unchanged",
        description: "the subclass prepends while the base still formats plainly",
        run: "sub = Timestamped().format(\"hi\")\nbase = Logger().format(\"hi\")",
        check: "sub == \"[t] hi\" and base == \"hi\"",
      },
    ],
    difficulty: 1.3,
    discrimination: 1.2,
    expectedSeconds: 210,
    prereqClosure: ["python-inheritance"],
    source: AUTHORED,
    status: "live",
  },
];
