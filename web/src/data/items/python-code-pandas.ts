import type { Item, SourceRef } from "../../lib/assessment/types";

/**
 * `code`-format items for the pandas chapter. Every test imports pandas in
 * its own `run` rather than relying on the import in `starterCode`, which the
 * learner is free to delete, and sticks to API that behaves the same across
 * the pandas the browser's Pyodide ships and the one a contributor is likely
 * to have installed locally.
 */

const AUTHORED: SourceRef = {
  id: "mathlingo-authored-python",
  tier: "generated",
  title: "Mathlingo authored item (Python sweep)",
};

export const pandasChapterCodeItems: Item[] = [
  {
    id: "pandas-io--code-from-records",
    conceptId: "pandas-io",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function build(rows) that turns a list of dicts into a DataFrame with the columns in the order the keys appear. build([{'a': 1, 'b': 2}]) has columns a and b.",
    starterCode: "import pandas as pd\n\n\ndef build(rows):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef build(rows):\n    return pd.DataFrame(rows)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "columns",
        description: "the column names come from the dict keys",
        run: "import pandas as pd\nresult = build([{'a': 1, 'b': 2}])",
        check: "list(result.columns) == ['a', 'b']",
      },
      {
        id: "rows",
        description: "one row per dict",
        run: "import pandas as pd\nresult = build([{'a': 1}, {'a': 2}])",
        check: "len(result) == 2 and list(result['a']) == [1, 2]",
      },
      {
        id: "is-frame",
        description: "the result is a DataFrame",
        run: "import pandas as pd\nresult = build([{'a': 1}])",
        check: "isinstance(result, pd.DataFrame)",
      },
    ],
    difficulty: 0.6,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["pandas-io"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-io--code-csv-roundtrip",
    conceptId: "pandas-io",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function to_csv_text(df) that returns the frame as CSV text WITHOUT the index column, using df.to_csv with no file path.",
    starterCode: "import pandas as pd\n\n\ndef to_csv_text(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef to_csv_text(df):\n    return df.to_csv(index=False)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "no-index",
        description: "the first field of the header is a real column, not a blank index label",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [2]})\nresult = to_csv_text(df)",
        check: "result.splitlines()[0] == 'a,b'",
      },
      {
        id: "values",
        description: "the data row is present",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [2]})\nresult = to_csv_text(df)",
        check: "result.splitlines()[1] == '1,2'",
      },
      {
        id: "is-text",
        description: "a string comes back, not a file handle",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1]})\nresult = to_csv_text(df)",
        check: "isinstance(result, str)",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["pandas-io"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-io--code-select-columns",
    conceptId: "pandas-io",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function keep(df, columns) that returns a new DataFrame with only the named columns, in the order given.",
    starterCode: "import pandas as pd\n\n\ndef keep(df, columns):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef keep(df, columns):\n    return df[list(columns)]\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "subset",
        description: "only the named columns survive",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [2], 'c': [3]})\nresult = keep(df, ['a', 'c'])",
        check: "list(result.columns) == ['a', 'c']",
      },
      {
        id: "order",
        description: "the order follows the argument, not the frame",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [2]})\nresult = keep(df, ['b', 'a'])",
        check: "list(result.columns) == ['b', 'a']",
      },
      {
        id: "original-unchanged",
        description: "the selected frame is returned and the input keeps all its columns",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [2]})\nout = keep(df, ['a'])",
        check: "list(out.columns) == ['a'] and list(df.columns) == ['a', 'b']",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["pandas-io"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-selection--code-cell-by-label",
    conceptId: "pandas-selection",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function cell(df, row_label, column) that returns the single value at that row label and column. Use loc.",
    starterCode: "import pandas as pd\n\n\ndef cell(df, row_label, column):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef cell(df, row_label, column):\n    return df.loc[row_label, column]\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "basic",
        description: "the value at the label is returned",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [10, 20]}, index=[5, 6])\nresult = cell(df, 6, 'a')",
        check: "result == 20",
      },
      {
        id: "label-not-position",
        description: "the row LABEL is used, not the position",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [10, 20]}, index=[5, 6])\nresult = cell(df, 5, 'a')",
        check: "result == 10",
      },
      {
        id: "string-index",
        description: "string labels work the same way",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2]}, index=['x', 'y'])\nresult = cell(df, 'y', 'a')",
        check: "result == 2",
      },
    ],
    difficulty: 0.7,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["pandas-selection"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-selection--code-first-rows",
    conceptId: "pandas-selection",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function first_n(df, n) that returns the first n rows BY POSITION, whatever the index labels are. Use iloc.",
    starterCode: "import pandas as pd\n\n\ndef first_n(df, n):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef first_n(df, n):\n    return df.iloc[:n]\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "count",
        description: "exactly n rows come back",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2, 3, 4]})\nresult = first_n(df, 2)",
        check: "len(result) == 2",
      },
      {
        id: "by-position",
        description: "position wins over label, even for a shuffled integer index",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2, 3]}, index=[9, 8, 7])\nresult = first_n(df, 1)",
        check: "list(result['a']) == [1]",
      },
      {
        id: "more-than-available",
        description: "asking for more rows than exist returns them all rather than raising",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2]})\nresult = first_n(df, 10)",
        check: "len(result) == 2",
      },
    ],
    difficulty: 0.75,
    discrimination: 1.2,
    expectedSeconds: 105,
    prereqClosure: ["pandas-selection"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-selection--code-safe-assign",
    conceptId: "pandas-selection",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function flag_large(df, threshold) that returns a COPY of df with a new boolean column 'large' that is True where column 'amount' exceeds the threshold. Do not modify the input.",
    starterCode: "import pandas as pd\n\n\ndef flag_large(df, threshold):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef flag_large(df, threshold):\n    out = df.copy()\n    out.loc[:, 'large'] = out['amount'] > threshold\n    return out\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "values",
        description: "the flag is True only above the threshold",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [50, 150]})\nresult = flag_large(df, 100)",
        check: "list(result['large']) == [False, True]",
      },
      {
        id: "column-added",
        description: "the returned frame has the new column",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [1]})\nresult = flag_large(df, 0)",
        check: "'large' in result.columns",
      },
      {
        id: "input-unchanged",
        description: "the frame passed in gains no column",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [1]})\nout = flag_large(df, 0)",
        check: "'large' in out.columns and 'large' not in df.columns",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["pandas-selection"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-filtering--code-two-conditions",
    conceptId: "pandas-filtering",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function big_in_region(df, region, threshold) returning the rows where column 'region' equals region AND 'amount' is above threshold.",
    starterCode: "import pandas as pd\n\n\ndef big_in_region(df, region, threshold):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef big_in_region(df, region, threshold):\n    return df[(df['region'] == region) & (df['amount'] > threshold)]\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "both",
        description: "only rows satisfying both conditions survive",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 'n', 's'], 'amount': [50, 150, 200]})\nresult = big_in_region(df, 'n', 100)",
        check: "list(result['amount']) == [150]",
      },
      {
        id: "none-match",
        description: "an empty frame comes back when nothing matches",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n'], 'amount': [1]})\nresult = big_in_region(df, 's', 0)",
        check: "len(result) == 0",
      },
      {
        id: "strict",
        description: "the threshold is exclusive",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n'], 'amount': [100]})\nresult = big_in_region(df, 'n', 100)",
        check: "len(result) == 0",
      },
    ],
    difficulty: 0.9,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-filtering"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-filtering--code-isin-filter",
    conceptId: "pandas-filtering",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function in_regions(df, regions) returning the rows whose 'region' is any of the values in the regions list. Use isin.",
    starterCode: "import pandas as pd\n\n\ndef in_regions(df, regions):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef in_regions(df, regions):\n    return df[df['region'].isin(regions)]\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "subset",
        description: "matching rows are kept",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 's', 'e']})\nresult = in_regions(df, ['n', 'e'])",
        check: "list(result['region']) == ['n', 'e']",
      },
      {
        id: "empty-list",
        description: "an empty list matches nothing",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n']})\nresult = in_regions(df, [])",
        check: "len(result) == 0",
      },
      {
        id: "keeps-index",
        description: "the surviving rows keep their original index labels",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 's', 'n']})\nresult = in_regions(df, ['s'])",
        check: "list(result.index) == [1]",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-filtering"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-filtering--code-count-matches",
    conceptId: "pandas-filtering",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function count_over(df, threshold) that returns HOW MANY rows have 'amount' above the threshold, as an int, by summing the boolean mask rather than building the filtered frame.",
    starterCode: "import pandas as pd\n\n\ndef count_over(df, threshold):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef count_over(df, threshold):\n    return int((df['amount'] > threshold).sum())\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "counts",
        description: "two of three rows are over 100",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [50, 150, 200]})\nresult = count_over(df, 100)",
        check: "result == 2",
      },
      {
        id: "none",
        description: "zero when nothing qualifies",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [1]})\nresult = count_over(df, 100)",
        check: "result == 0",
      },
      {
        id: "is-int",
        description: "a plain int comes back, not a numpy scalar or a frame",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [200]})\nresult = count_over(df, 100)",
        check: "isinstance(result, int) and result == 1",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-filtering"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-missing--code-count-missing",
    conceptId: "pandas-missing",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function missing_per_column(df) that returns a Series giving the number of missing values in each column.",
    starterCode: "import pandas as pd\n\n\ndef missing_per_column(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef missing_per_column(df):\n    return df.isna().sum()\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "counts",
        description: "column a has one missing value and b has none",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, None], 'b': [1, 2]})\nresult = missing_per_column(df)",
        check: "result['a'] == 1 and result['b'] == 0",
      },
      {
        id: "indexed-by-column",
        description: "the index is the column names",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [2]})\nresult = missing_per_column(df)",
        check: "list(result.index) == ['a', 'b']",
      },
      {
        id: "no-missing",
        description: "an all-present frame reports zeros",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2]})\nresult = missing_per_column(df)",
        check: "int(result['a']) == 0",
      },
    ],
    difficulty: 0.75,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-missing"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-missing--code-fill-with-median",
    conceptId: "pandas-missing",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function fill_median(df, column) that returns a COPY of df with missing values in the named column replaced by that column's median. The input must not change.",
    starterCode: "import pandas as pd\n\n\ndef fill_median(df, column):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef fill_median(df, column):\n    out = df.copy()\n    out[column] = out[column].fillna(out[column].median())\n    return out\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "fills",
        description: "the gap becomes the median of the present values",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1.0, 3.0, None]})\nresult = fill_median(df, 'a')",
        check: "abs(result['a'][2] - 2.0) < 1e-9",
      },
      {
        id: "no-missing-left",
        description: "nothing is missing afterwards",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1.0, None]})\nresult = fill_median(df, 'a')",
        check: "int(result['a'].isna().sum()) == 0",
      },
      {
        id: "input-unchanged",
        description: "the frame passed in still has its gap",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1.0, None]})\nout = fill_median(df, 'a')",
        check: "int(out['a'].isna().sum()) == 0 and int(df['a'].isna().sum()) == 1",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["pandas-missing"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-missing--code-drop-subset",
    conceptId: "pandas-missing",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function drop_missing_amount(df) returning the rows where column 'amount' is present, leaving rows that are missing OTHER columns alone.",
    starterCode: "import pandas as pd\n\n\ndef drop_missing_amount(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef drop_missing_amount(df):\n    return df.dropna(subset=['amount'])\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "drops",
        description: "the row with no amount is removed",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [1.0, None], 'note': ['a', 'b']})\nresult = drop_missing_amount(df)",
        check: "len(result) == 1",
      },
      {
        id: "keeps-other-gaps",
        description: "a row missing only 'note' is kept",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [1.0], 'note': [None]})\nresult = drop_missing_amount(df)",
        check: "len(result) == 1",
      },
      {
        id: "all-present",
        description: "nothing is dropped when amount is complete",
        run: "import pandas as pd\ndf = pd.DataFrame({'amount': [1.0, 2.0]})\nresult = drop_missing_amount(df)",
        check: "len(result) == 2",
      },
    ],
    difficulty: 0.95,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-missing"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-dtypes--code-coerce-numeric",
    conceptId: "pandas-dtypes",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function to_number(series) that converts a Series of strings to numbers, turning anything unparseable into NaN rather than raising.",
    starterCode: "import pandas as pd\n\n\ndef to_number(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef to_number(series):\n    return pd.to_numeric(series, errors='coerce')\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "parses",
        description: "numeric strings become numbers",
        run: "import pandas as pd\ns = pd.Series(['1', '2'])\nresult = to_number(s)",
        check: "list(result) == [1, 2]",
      },
      {
        id: "coerces",
        description: "an unparseable value becomes NaN instead of raising",
        run: "import pandas as pd\ns = pd.Series(['1', 'oops'])\nresult = to_number(s)",
        check: "result[0] == 1 and bool(pd.isna(result[1]))",
      },
      {
        id: "numeric-dtype",
        description: "the result is a numeric dtype, not object",
        run: "import pandas as pd\nimport pandas.api.types as t\ns = pd.Series(['1', '2'])\nresult = t.is_numeric_dtype(to_number(s))",
        check: "result",
      },
    ],
    difficulty: 0.9,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["pandas-dtypes"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-dtypes--code-as-category",
    conceptId: "pandas-dtypes",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function categorise(df, column) that returns a COPY of df with the named column converted to the category dtype.",
    starterCode: "import pandas as pd\n\n\ndef categorise(df, column):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef categorise(df, column):\n    out = df.copy()\n    out[column] = out[column].astype('category')\n    return out\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "dtype",
        description: "the column's dtype is category",
        run: "import pandas as pd\ndf = pd.DataFrame({'r': ['n', 's', 'n']})\nresult = categorise(df, 'r')",
        check: "str(result['r'].dtype) == 'category'",
      },
      {
        id: "values-preserved",
        description: "the values themselves are unchanged",
        run: "import pandas as pd\ndf = pd.DataFrame({'r': ['n', 's']})\nresult = categorise(df, 'r')",
        check: "list(result['r']) == ['n', 's']",
      },
      {
        id: "input-unchanged",
        description: "the input column is still object dtype",
        run: "import pandas as pd\ndf = pd.DataFrame({'r': ['n']})\nout = categorise(df, 'r')",
        check: "str(out['r'].dtype) == 'category' and str(df['r'].dtype) != 'category'",
      },
    ],
    difficulty: 0.95,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-dtypes"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-dtypes--code-column-types",
    conceptId: "pandas-dtypes",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function numeric_columns(df) that returns a LIST of the names of the columns holding numeric data, in frame order.",
    starterCode: "import pandas as pd\n\n\ndef numeric_columns(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef numeric_columns(df):\n    return list(df.select_dtypes(include='number').columns)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "picks-numeric",
        description: "only the numeric columns are named",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': ['x'], 'c': [1.5]})\nresult = numeric_columns(df)",
        check: "result == ['a', 'c']",
      },
      {
        id: "none",
        description: "an all-text frame gives an empty list",
        run: "import pandas as pd\ndf = pd.DataFrame({'b': ['x']})\nresult = numeric_columns(df)",
        check: "result == []",
      },
      {
        id: "is-list",
        description: "a list of names comes back, not an Index",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1]})\nresult = numeric_columns(df)",
        check: "isinstance(result, list)",
      },
    ],
    difficulty: 1.05,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-dtypes"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-sorting--code-top-n",
    conceptId: "pandas-sorting",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function top(df, column, n) returning the n rows with the largest values in that column, largest first.",
    starterCode: "import pandas as pd\n\n\ndef top(df, column, n):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef top(df, column, n):\n    return df.nlargest(n, column)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "order",
        description: "the largest comes first",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 9, 5]})\nresult = top(df, 'a', 2)",
        check: "list(result['a']) == [9, 5]",
      },
      {
        id: "count",
        description: "exactly n rows",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2, 3]})\nresult = top(df, 'a', 1)",
        check: "len(result) == 1",
      },
      {
        id: "keeps-index",
        description: "the original index labels travel with the rows",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 9]})\nresult = top(df, 'a', 1)",
        check: "list(result.index) == [1]",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-sorting"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-sorting--code-two-key-sort",
    conceptId: "pandas-sorting",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function order(df) that sorts by column 'region' ascending and, within a region, 'amount' descending — returning a new frame with the index renumbered from 0.",
    starterCode: "import pandas as pd\n\n\ndef order(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef order(df):\n    return df.sort_values(['region', 'amount'], ascending=[True, False]).reset_index(drop=True)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "region-first",
        description: "regions come out in alphabetical order",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['s', 'n'], 'amount': [1, 2]})\nresult = order(df)",
        check: "list(result['region']) == ['n', 's']",
      },
      {
        id: "amount-descending",
        description: "within a region the largest amount is first",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 'n'], 'amount': [1, 9]})\nresult = order(df)",
        check: "list(result['amount']) == [9, 1]",
      },
      {
        id: "index-reset",
        description: "the index is 0..n-1, not the original labels",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['s', 'n'], 'amount': [1, 2]})\nresult = order(df)",
        check: "list(result.index) == [0, 1]",
      },
    ],
    difficulty: 1.15,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["pandas-sorting"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-sorting--code-competition-rank",
    conceptId: "pandas-sorting",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function ranked(series) returning the competition rank of each value, largest value ranked 1, with ties sharing the lower rank number and the next rank skipped.",
    starterCode: "import pandas as pd\n\n\ndef ranked(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef ranked(series):\n    return series.rank(method='min', ascending=False)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "largest-first",
        description: "the biggest value ranks 1",
        run: "import pandas as pd\ns = pd.Series([10, 30, 20])\nresult = ranked(s)",
        check: "result[1] == 1",
      },
      {
        id: "ties-share",
        description: "two equal values both rank 1",
        run: "import pandas as pd\ns = pd.Series([5, 5, 1])\nresult = ranked(s)",
        check: "result[0] == 1 and result[1] == 1",
      },
      {
        id: "gap-after-tie",
        description: "the value after a two-way tie ranks 3, not 2",
        run: "import pandas as pd\ns = pd.Series([5, 5, 1])\nresult = ranked(s)",
        check: "result[2] == 3",
      },
    ],
    difficulty: 1.2,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["pandas-sorting"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-apply--code-vectorised-scale",
    conceptId: "pandas-apply",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function scale(df, column, factor) returning a COPY of df with the named column multiplied by factor. Use the vectorised expression, not apply.",
    starterCode: "import pandas as pd\n\n\ndef scale(df, column, factor):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef scale(df, column, factor):\n    out = df.copy()\n    out[column] = out[column] * factor\n    return out\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "values",
        description: "every value is scaled",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2]})\nresult = scale(df, 'a', 3)",
        check: "list(result['a']) == [3, 6]",
      },
      {
        id: "other-columns",
        description: "other columns are untouched",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [7]})\nresult = scale(df, 'a', 2)",
        check: "list(result['b']) == [7]",
      },
      {
        id: "input-unchanged",
        description: "the scaled frame is returned and the input keeps its values",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1]})\nout = scale(df, 'a', 5)",
        check: "list(out['a']) == [5] and list(df['a']) == [1]",
      },
    ],
    difficulty: 0.8,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-apply"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-apply--code-recode-with-map",
    conceptId: "pandas-apply",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function recode(series, mapping) that replaces each value using the mapping dict, leaving values absent from the mapping as NaN. Use Series.map.",
    starterCode: "import pandas as pd\n\n\ndef recode(series, mapping):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef recode(series, mapping):\n    return series.map(mapping)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "maps",
        description: "known values are replaced",
        run: "import pandas as pd\ns = pd.Series(['n', 's'])\nresult = recode(s, {'n': 'north', 's': 'south'})",
        check: "list(result) == ['north', 'south']",
      },
      {
        id: "unknown-becomes-nan",
        description: "a value not in the mapping becomes NaN",
        run: "import pandas as pd\ns = pd.Series(['n', 'x'])\nresult = recode(s, {'n': 'north'})",
        check: "result[0] == 'north' and bool(pd.isna(result[1]))",
      },
      {
        id: "length",
        description: "one output value per input value",
        run: "import pandas as pd\ns = pd.Series(['a', 'b', 'c'])\nresult = recode(s, {'a': 1})",
        check: "len(result) == 3",
      },
    ],
    difficulty: 0.9,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["pandas-apply"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-apply--code-row-apply",
    conceptId: "pandas-apply",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function combine(df) returning a Series holding column 'a' plus column 'b' for each row, computed with df.apply across rows.",
    starterCode: "import pandas as pd\n\n\ndef combine(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef combine(df):\n    return df.apply(lambda row: row['a'] + row['b'], axis=1)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "sums",
        description: "each row's two values are added",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2], 'b': [10, 20]})\nresult = combine(df)",
        check: "list(result) == [11, 22]",
      },
      {
        id: "length",
        description: "one value per row, not per column",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1, 2, 3], 'b': [1, 1, 1]})\nresult = combine(df)",
        check: "len(result) == 3",
      },
      {
        id: "is-series",
        description: "a Series comes back",
        run: "import pandas as pd\ndf = pd.DataFrame({'a': [1], 'b': [1]})\nresult = combine(df)",
        check: "isinstance(result, pd.Series)",
      },
    ],
    difficulty: 1.1,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["pandas-apply"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-strings--code-clean-text",
    conceptId: "pandas-strings",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function clean(series) that strips surrounding whitespace and lowercases every value, using the .str accessor.",
    starterCode: "import pandas as pd\n\n\ndef clean(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef clean(series):\n    return series.str.strip().str.lower()\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "strips-and-lowers",
        description: "'  North ' becomes 'north'",
        run: "import pandas as pd\ns = pd.Series(['  North '])\nresult = clean(s)",
        check: "list(result) == ['north']",
      },
      {
        id: "already-clean",
        description: "an already-clean value is unchanged",
        run: "import pandas as pd\ns = pd.Series(['x'])\nresult = clean(s)",
        check: "list(result) == ['x']",
      },
      {
        id: "missing-stays-missing",
        description: "a NaN stays NaN rather than raising",
        run: "import pandas as pd\ns = pd.Series(['A', None])\nresult = clean(s)",
        check: "result[0] == 'a' and bool(pd.isna(result[1]))",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-strings"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-strings--code-safe-contains",
    conceptId: "pandas-strings",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function has_text(series, needle) returning a BOOLEAN mask that is True where the value contains needle. Missing values must count as False, not NaN.",
    starterCode: "import pandas as pd\n\n\ndef has_text(series, needle):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef has_text(series, needle):\n    return series.str.contains(needle, na=False)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "matches",
        description: "the matching row is True",
        run: "import pandas as pd\ns = pd.Series(['north', 'south'])\nresult = has_text(s, 'nor')",
        check: "list(result) == [True, False]",
      },
      {
        id: "na-false",
        description: "a missing value is False, not NaN",
        run: "import pandas as pd\ns = pd.Series(['north', None])\nresult = has_text(s, 'nor')",
        check: "list(result) == [True, False]",
      },
      {
        id: "usable-as-mask",
        description: "the mask can index a Series without raising",
        run: "import pandas as pd\ns = pd.Series(['north', None])\nresult = list(s[has_text(s, 'nor')])",
        check: "result == ['north']",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-strings"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-strings--code-split-into-columns",
    conceptId: "pandas-strings",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function split_name(series) that splits each 'First Last' value on the first space and returns a DataFrame with columns named 'first' and 'last'.",
    starterCode: "import pandas as pd\n\n\ndef split_name(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef split_name(series):\n    parts = series.str.split(' ', n=1, expand=True)\n    parts.columns = ['first', 'last']\n    return parts\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "columns",
        description: "the result has the two named columns",
        run: "import pandas as pd\ns = pd.Series(['Ada Lovelace'])\nresult = split_name(s)",
        check: "list(result.columns) == ['first', 'last']",
      },
      {
        id: "values",
        description: "the name is split correctly",
        run: "import pandas as pd\ns = pd.Series(['Ada Lovelace'])\nresult = split_name(s)",
        check: "result['first'][0] == 'Ada' and result['last'][0] == 'Lovelace'",
      },
      {
        id: "only-first-space",
        description: "a middle name stays attached to the last name",
        run: "import pandas as pd\ns = pd.Series(['Ada B Lovelace'])\nresult = split_name(s)",
        check: "result['last'][0] == 'B Lovelace'",
      },
    ],
    difficulty: 1.2,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["pandas-strings"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-datetime--code-parse-dates",
    conceptId: "pandas-datetime",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function as_dates(series) that converts a Series of date strings into real datetimes.",
    starterCode: "import pandas as pd\n\n\ndef as_dates(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef as_dates(series):\n    return pd.to_datetime(series)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "dtype",
        description: "the result is a datetime64 column",
        run: "import pandas as pd\nimport pandas.api.types as t\ns = pd.Series(['2024-01-01'])\nresult = t.is_datetime64_any_dtype(as_dates(s))",
        check: "result",
      },
      {
        id: "value",
        description: "the parsed value is the right day",
        run: "import pandas as pd\ns = pd.Series(['2024-03-05'])\nresult = as_dates(s)",
        check: "result[0].year == 2024 and result[0].month == 3 and result[0].day == 5",
      },
      {
        id: "length",
        description: "one timestamp per input string",
        run: "import pandas as pd\ns = pd.Series(['2024-01-01', '2024-01-02'])\nresult = as_dates(s)",
        check: "len(result) == 2",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-datetime"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-datetime--code-weekday-names",
    conceptId: "pandas-datetime",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function day_names(series) that takes a Series of date strings and returns a Series of weekday names such as 'Monday'. Use the .dt accessor.",
    starterCode: "import pandas as pd\n\n\ndef day_names(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef day_names(series):\n    return pd.to_datetime(series).dt.day_name()\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "known-day",
        description: "2024-01-01 was a Monday",
        run: "import pandas as pd\ns = pd.Series(['2024-01-01'])\nresult = day_names(s)",
        check: "list(result) == ['Monday']",
      },
      {
        id: "several",
        description: "one name per date",
        run: "import pandas as pd\ns = pd.Series(['2024-01-01', '2024-01-02'])\nresult = day_names(s)",
        check: "list(result) == ['Monday', 'Tuesday']",
      },
      {
        id: "is-text",
        description: "the values are strings, not numbers",
        run: "import pandas as pd\ns = pd.Series(['2024-01-03'])\nresult = day_names(s)",
        check: "isinstance(result[0], str)",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-datetime"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-datetime--code-daily-totals",
    conceptId: "pandas-datetime",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function daily(df) that takes a frame with a datetime column 'when' and a numeric column 'amount', and returns a Series of the total amount per calendar day, indexed by day.",
    starterCode: "import pandas as pd\n\n\ndef daily(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef daily(df):\n    out = df.copy()\n    out['when'] = pd.to_datetime(out['when'])\n    return out.set_index('when')['amount'].resample('D').sum()\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "groups",
        description: "two rows on the same day are summed",
        run: "import pandas as pd\ndf = pd.DataFrame({'when': ['2024-01-01', '2024-01-01'], 'amount': [1, 2]})\nresult = daily(df)",
        check: "float(result.iloc[0]) == 3.0",
      },
      {
        id: "separate-days",
        description: "different days stay separate",
        run: "import pandas as pd\ndf = pd.DataFrame({'when': ['2024-01-01', '2024-01-02'], 'amount': [1, 5]})\nresult = daily(df)",
        check: "float(result.iloc[0]) == 1.0 and float(result.iloc[-1]) == 5.0",
      },
      {
        id: "fills-gap",
        description: "a day with no rows still appears, as 0",
        run: "import pandas as pd\ndf = pd.DataFrame({'when': ['2024-01-01', '2024-01-03'], 'amount': [1, 1]})\nresult = daily(df)",
        check: "len(result) == 3 and float(result.iloc[1]) == 0.0",
      },
    ],
    difficulty: 1.3,
    discrimination: 1.2,
    expectedSeconds: 210,
    prereqClosure: ["pandas-datetime"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-pivot--code-long-to-wide",
    conceptId: "pandas-pivot",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function widen(df) turning a long frame with columns 'date', 'region' and 'amount' into a wide frame indexed by date with one column per region, summing duplicates and filling gaps with 0.",
    starterCode: "import pandas as pd\n\n\ndef widen(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef widen(df):\n    return df.pivot_table(index='date', columns='region', values='amount', aggfunc='sum', fill_value=0)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "shape",
        description: "one column per region",
        run: "import pandas as pd\ndf = pd.DataFrame({'date': ['d1', 'd1'], 'region': ['n', 's'], 'amount': [1, 2]})\nresult = widen(df)",
        check: "sorted(result.columns) == ['n', 's']",
      },
      {
        id: "aggregates",
        description: "two rows for the same date and region are summed",
        run: "import pandas as pd\ndf = pd.DataFrame({'date': ['d1', 'd1'], 'region': ['n', 'n'], 'amount': [1, 2]})\nresult = widen(df)",
        check: "int(result.loc['d1', 'n']) == 3",
      },
      {
        id: "fills",
        description: "a missing date/region combination is 0, not NaN",
        run: "import pandas as pd\ndf = pd.DataFrame({'date': ['d1', 'd2'], 'region': ['n', 's'], 'amount': [1, 2]})\nresult = widen(df)",
        check: "int(result.loc['d1', 's']) == 0",
      },
    ],
    difficulty: 1.25,
    discrimination: 1.2,
    expectedSeconds: 195,
    prereqClosure: ["pandas-pivot"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-pivot--code-wide-to-long",
    conceptId: "pandas-pivot",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function lengthen(df) that melts a wide frame — an 'id' column plus one column per measurement — into columns 'id', 'variable' and 'value'.",
    starterCode: "import pandas as pd\n\n\ndef lengthen(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef lengthen(df):\n    return df.melt(id_vars='id')\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "columns",
        description: "the melted frame has the three expected columns",
        run: "import pandas as pd\ndf = pd.DataFrame({'id': [1], 'a': [10], 'b': [20]})\nresult = lengthen(df)",
        check: "list(result.columns) == ['id', 'variable', 'value']",
      },
      {
        id: "rows",
        description: "two measurement columns give two rows per id",
        run: "import pandas as pd\ndf = pd.DataFrame({'id': [1], 'a': [10], 'b': [20]})\nresult = lengthen(df)",
        check: "len(result) == 2",
      },
      {
        id: "values",
        description: "the values follow their variable name",
        run: "import pandas as pd\ndf = pd.DataFrame({'id': [1], 'a': [10], 'b': [20]})\nresult = lengthen(df)",
        check: "dict(zip(result['variable'], result['value'])) == {'a': 10, 'b': 20}",
      },
    ],
    difficulty: 1.15,
    discrimination: 1.2,
    expectedSeconds: 180,
    prereqClosure: ["pandas-pivot"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-pivot--code-crosstab-counts",
    conceptId: "pandas-pivot",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function counts(df) returning a crosstab of how many rows fall in each combination of column 'region' and column 'channel'.",
    starterCode: "import pandas as pd\n\n\ndef counts(df):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef counts(df):\n    return pd.crosstab(df['region'], df['channel'])\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "counts",
        description: "each combination is counted",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 'n', 's'], 'channel': ['web', 'web', 'web']})\nresult = counts(df)",
        check: "int(result.loc['n', 'web']) == 2",
      },
      {
        id: "shape",
        description: "rows are regions and columns are channels",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 's'], 'channel': ['web', 'shop']})\nresult = counts(df)",
        check: "sorted(result.index) == ['n', 's'] and sorted(result.columns) == ['shop', 'web']",
      },
      {
        id: "zero-cell",
        description: "a combination that never occurs is 0",
        run: "import pandas as pd\ndf = pd.DataFrame({'region': ['n', 's'], 'channel': ['web', 'shop']})\nresult = counts(df)",
        check: "int(result.loc['n', 'shop']) == 0",
      },
    ],
    difficulty: 1.1,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["pandas-pivot"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-concat--code-stack-rows",
    conceptId: "pandas-concat",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function stack(frames) that concatenates a list of frames one below another and renumbers the index from 0.",
    starterCode: "import pandas as pd\n\n\ndef stack(frames):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef stack(frames):\n    return pd.concat(frames, ignore_index=True)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "length",
        description: "the row counts add up",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]})\nb = pd.DataFrame({'x': [2, 3]})\nresult = stack([a, b])",
        check: "len(result) == 3",
      },
      {
        id: "index-renumbered",
        description: "the index is 0..n-1 with no duplicates",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]})\nb = pd.DataFrame({'x': [2]})\nresult = stack([a, b])",
        check: "list(result.index) == [0, 1]",
      },
      {
        id: "order",
        description: "the frames keep their given order",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]})\nb = pd.DataFrame({'x': [2]})\nresult = stack([a, b])",
        check: "list(result['x']) == [1, 2]",
      },
    ],
    difficulty: 0.9,
    discrimination: 1.2,
    expectedSeconds: 135,
    prereqClosure: ["pandas-concat"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-concat--code-union-columns",
    conceptId: "pandas-concat",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function stack_ragged(a, b) that stacks two frames by row even when their columns differ, keeping the union of columns and renumbering the index.",
    starterCode: "import pandas as pd\n\n\ndef stack_ragged(a, b):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef stack_ragged(a, b):\n    return pd.concat([a, b], ignore_index=True)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "union",
        description: "both frames' columns appear",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]})\nb = pd.DataFrame({'y': [2]})\nresult = stack_ragged(a, b)",
        check: "sorted(result.columns) == ['x', 'y']",
      },
      {
        id: "gaps-are-nan",
        description: "a column missing from one frame is NaN for those rows",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]})\nb = pd.DataFrame({'y': [2]})\nresult = stack_ragged(a, b)",
        check: "bool(pd.isna(result['y'][0]))",
      },
      {
        id: "rows",
        description: "no rows are lost",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1, 2]})\nb = pd.DataFrame({'y': [3]})\nresult = stack_ragged(a, b)",
        check: "len(result) == 3",
      },
    ],
    difficulty: 1.05,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-concat"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-concat--code-side-by-side",
    conceptId: "pandas-concat",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function beside(a, b) that places two frames side by side, aligning them on the index.",
    starterCode: "import pandas as pd\n\n\ndef beside(a, b):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef beside(a, b):\n    return pd.concat([a, b], axis=1)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "columns",
        description: "the columns of both appear",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]})\nb = pd.DataFrame({'y': [2]})\nresult = beside(a, b)",
        check: "list(result.columns) == ['x', 'y']",
      },
      {
        id: "rows",
        description: "aligned frames keep their row count",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1, 2]})\nb = pd.DataFrame({'y': [3, 4]})\nresult = beside(a, b)",
        check: "len(result) == 2",
      },
      {
        id: "aligns-on-index",
        description: "rows pair by index label, not by position",
        run: "import pandas as pd\na = pd.DataFrame({'x': [1]}, index=[5])\nb = pd.DataFrame({'y': [2]}, index=[5])\nresult = beside(a, b)",
        check: "len(result) == 1 and int(result.loc[5, 'y']) == 2",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-concat"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-window--code-moving-average",
    conceptId: "pandas-window",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function moving_average(series, window) returning the rolling mean over that many values. Positions with an incomplete window should be NaN.",
    starterCode: "import pandas as pd\n\n\ndef moving_average(series, window):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef moving_average(series, window):\n    return series.rolling(window).mean()\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "mean",
        description: "the third value of a 3-window over [1,2,3] is 2",
        run: "import pandas as pd\ns = pd.Series([1, 2, 3])\nresult = moving_average(s, 3)",
        check: "abs(result[2] - 2.0) < 1e-9",
      },
      {
        id: "leading-nan",
        description: "the first two positions are NaN with a window of 3",
        run: "import pandas as pd\ns = pd.Series([1, 2, 3])\nresult = moving_average(s, 3)",
        check: "bool(pd.isna(result[0])) and bool(pd.isna(result[1]))",
      },
      {
        id: "length",
        description: "the result is the same length as the input",
        run: "import pandas as pd\ns = pd.Series([1, 2, 3, 4])\nresult = moving_average(s, 2)",
        check: "len(result) == 4",
      },
    ],
    difficulty: 1.0,
    discrimination: 1.2,
    expectedSeconds: 150,
    prereqClosure: ["pandas-window"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-window--code-running-total",
    conceptId: "pandas-window",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function running_total(series) returning the cumulative sum — each position holding the total of everything up to and including it.",
    starterCode: "import pandas as pd\n\n\ndef running_total(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef running_total(series):\n    return series.cumsum()\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "accumulates",
        description: "[1, 2, 3] gives [1, 3, 6]",
        run: "import pandas as pd\ns = pd.Series([1, 2, 3])\nresult = running_total(s)",
        check: "list(result) == [1, 3, 6]",
      },
      {
        id: "length",
        description: "same length as the input",
        run: "import pandas as pd\ns = pd.Series([1, 2, 3, 4])\nresult = running_total(s)",
        check: "len(result) == 4",
      },
      {
        id: "negatives",
        description: "negative values reduce the running total",
        run: "import pandas as pd\ns = pd.Series([5, -2])\nresult = running_total(s)",
        check: "list(result) == [5, 3]",
      },
    ],
    difficulty: 0.85,
    discrimination: 1.2,
    expectedSeconds: 120,
    prereqClosure: ["pandas-window"],
    source: AUTHORED,
    status: "live",
  },
  {
    id: "pandas-window--code-previous-value",
    conceptId: "pandas-window",
    format: "code",
    cognitive: "apply",
    channels: ["typed"],
    stem: "Write a function change_from_previous(series) returning each value minus the one before it, with NaN in the first position. Use shift or diff.",
    starterCode: "import pandas as pd\n\n\ndef change_from_previous(series):\n    pass\n",
    referenceSolution: "import pandas as pd\n\n\ndef change_from_previous(series):\n    return series - series.shift(1)\n",
    codePackages: ["pandas"],
    codeTests: [
      {
        id: "difference",
        description: "[10, 13, 12] gives [NaN, 3, -1]",
        run: "import pandas as pd\ns = pd.Series([10, 13, 12])\nresult = change_from_previous(s)",
        check: "abs(result[1] - 3) < 1e-9 and abs(result[2] + 1) < 1e-9",
      },
      {
        id: "first-is-nan",
        description: "there is no previous value for the first row",
        run: "import pandas as pd\ns = pd.Series([10, 13])\nresult = change_from_previous(s)",
        check: "bool(pd.isna(result[0]))",
      },
      {
        id: "length",
        description: "same length as the input",
        run: "import pandas as pd\ns = pd.Series([1, 2, 3])\nresult = change_from_previous(s)",
        check: "len(result) == 3",
      },
    ],
    difficulty: 1.1,
    discrimination: 1.2,
    expectedSeconds: 165,
    prereqClosure: ["pandas-window"],
    source: AUTHORED,
    status: "live",
  },
];
