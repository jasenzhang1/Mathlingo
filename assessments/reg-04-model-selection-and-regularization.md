# Regression Cluster 4 — Model Selection & Regularization

AIC/BIC, Forward/Backward/Stepwise Selection, Regularization, LASSO, Ridge Regression, Elastic Net,
LOESS Smoothing (7 concepts). Same format as [foundations-of-probability.md](foundations-of-probability.md).

`lasso` and `ridge-regression` both cash in `vector-norm`'s diamond-vs-circle geometric argument from
the linear-algebra sweep directly, and `ridge-regression`'s A1 verifies numerically that the ridge
shift repairs a genuinely singular X'X.

---

## AIC, BIC (`aic-bic`)
*Prereq: Linear Regression (Probabilistic Version) · ancestors 30 · b₀ = 1.22*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.22 | State AIC and BIC, and their general behavior. | AIC=2k−2ln(L̂); BIC=k·ln(n)−2ln(L̂); both penalize complexity (k) while rewarding fit (likelihood); lower values indicate better models | — |
| R2 | recall | mcq | 0.52 | Compared to AIC, BIC's per-parameter penalty is: | generally larger for reasonably-sized samples (since ln(n)>2 for n>7), favoring simpler models more strongly | claims the penalties are "identical" — missing the ln(n) vs. fixed-2 distinction entirely → `aic-bic` |
| A1 | apply | short-answer | 1.02 | Why does BIC's penalty grow with n while AIC's stays fixed, and what does this imply asymptotically? | BIC's ln(n) term grows without bound, making it increasingly strict about adding parameters as n→∞ — a property called consistency, favoring the true parsimonious model asymptotically; AIC's fixed penalty of 2 per parameter lacks this property and can overfit even with unlimited data *(required: names consistency and AIC's lack of it)* | — |
| E1 | explain | short-answer | 1.72 | Why do AIC/BIC provide a principled alternative to comparing raw likelihood or R² across models with different parameter counts? | per `r-squared`'s finding that R² always increases with more parameters, comparing raw fit metrics directly always favors bigger models; AIC/BIC explicitly subtract a complexity penalty, so a bigger model must achieve a *meaningfully* better fit, not just any improvement, to be preferred *(required: the direct callback to R²'s monotonic-increase problem)* | — |
| T1 | transfer | short-answer | 2.22 | Why might a practitioner prefer AIC when prediction is the goal, and BIC when identifying the "true" model is the goal? | AIC is justified as approximately minimizing prediction error on new data (related to minimizing KL-divergence between the fitted model and the true data-generating distribution); BIC is justified as approximating Bayesian model selection, favoring the model with highest posterior probability — different theoretical goals suit different practical objectives *(required: names the KL-divergence connection for AIC specifically)* | — |
| R3 | recall | short-answer | 0.02 | Fill in the blank: AIC = ___. | 2k − 2ln(L̂), where k is the number of parameters and L̂ is the maximized likelihood | — |
| R4 | recall | short-answer | 0.07 | Fill in the blank: BIC = ___. | k·ln(n) − 2ln(L̂), where n is the sample size | — |
| R5 | recall | short-answer | 0.12 | What does k represent in the AIC and BIC formulas? | the number of estimated parameters in the model | — |
| R6 | recall | mcq | 0.17 | True/false: a lower AIC or BIC value indicates a better model, all else equal. | True — both criteria are constructed so that smaller values reflect a better fit-versus-complexity tradeoff | claims "higher AIC/BIC values indicate a better model" — both are defined so that lower is better, the reverse of a metric like R² → `aic-bic` |
| R7 | recall | short-answer | 0.27 | What is another common name for BIC? | the Schwarz criterion (or Schwarz Bayesian criterion, SBC) | — |
| R8 | recall | mcq | 0.32 | Which of AIC and BIC is also known as the Schwarz criterion? | BIC | picks "AIC" — the Schwarz criterion is specifically BIC's alternate name, derived from Schwarz's Bayesian justification → `aic-bic` |
| R9 | recall | short-answer | 0.37 | Fill in the blank: AIC's per-parameter penalty is a fixed constant of ___. | 2 | — |
| R10 | recall | short-answer | 0.42 | Fill in the blank: BIC's per-parameter penalty is ___. | ln(n), which grows with the sample size | — |
| R11 | recall | mcq | 0.47 | True/false: AIC and BIC can be used to compare non-nested models. | True — unlike a likelihood-ratio test, which requires one model to be a special case of the other, AIC/BIC can compare any two models fit by maximum likelihood on the same response | claims "AIC/BIC require the models being compared to be nested" — that restriction applies to likelihood-ratio tests, not to AIC/BIC → `aic-bic` |
| R12 | recall | short-answer | 0.57 | Above roughly what sample size n does BIC's per-parameter penalty exceed AIC's? | n ≥ 8 (since ln(n) > 2 once n > e² ≈ 7.39) | — |
| R13 | recall | mcq | 0.62 | As n→∞, which criterion has the model-selection consistency property (selecting the true model with probability approaching 1)? | BIC | picks "AIC" — BIC's growing ln(n) penalty is exactly what gives it asymptotic consistency; AIC's fixed penalty lacks this property → `aic-bic` |
| R14 | recall | short-answer | 0.67 | What theoretical property does AIC have (related to prediction) that BIC generally lacks? | AIC is asymptotically efficient — it tends to select the model minimizing predictive error (related to minimizing KL-divergence to the true data-generating process) | — |
| R15 | recall | short-answer | 0.72 | Fill in the blank: both AIC and BIC require computing the model's maximized ___. | likelihood, L̂ | — |
| R16 | recall | mcq | 0.82 | True/false: AIC/BIC can only be applied to linear regression models. | False — AIC and BIC are general criteria applicable to any model fit by maximum likelihood, including GLMs, time-series models, and beyond | claims "AIC/BIC are specific to ordinary linear regression" — they are defined generically from the likelihood function, usable for any MLE-fit model → `aic-bic` |
| A2 | apply | numeric | 0.87 | A model has k=3 parameters and log-likelihood ln(L̂)=−50. Compute its AIC. `[verified: 106]` | AIC = 2(3) − 2(−50) = 6 + 100 = 106 | — |
| A3 | apply | numeric | 0.97 | The same model (k=3, ln(L̂)=−50) is fit on n=50 observations. Compute its BIC. `[verified: 111.74]` | BIC = 3·ln(50) − 2(−50) = 3(3.912) + 100 ≈ 111.74 | — |
| A4 | apply | short-answer | 1.07 | Two models are fit to the same data: Model A has AIC=180, Model B has AIC=195. Which is preferred, and why? | Model A — lower AIC indicates a better tradeoff between fit and complexity, so between these two Model A is the preferred choice by this criterion *(required: correctly identifies the lower value as preferred)* | — |
| A5 | apply | short-answer | 1.17 | Model A has AIC=250, Model B has AIC=245. Which is preferred, and is the 5-point gap likely to be decisive? | Model B is preferred (lower AIC), but a gap of only 5 is fairly modest — common rule-of-thumb guidance treats differences under roughly 2 as negligible and up to ~10 as only moderate evidence, so this is a mild, not overwhelming, preference *(required: names both the direction of preference and that the gap size matters for how confident to be)* | — |
| A6 | apply | short-answer | 1.27 | Why does comparing AIC values only make sense for models fit to the exact same response variable and dataset? | the likelihood term ln(L̂) in both formulas is only comparable across models when it's computed against identical data — a different response transformation (e.g. Y vs. log Y) or a different subset of observations changes the likelihood's scale entirely, making the AIC values not meaningfully comparable *(required: names that the same response/data is required for the likelihoods to be on a common scale)* | — |
| E2 | explain | short-answer | 1.37 | Why does a lower AIC not guarantee a model is "the true model," only that it trades off fit and complexity well? | AIC is explicitly justified as approximately minimizing *predictive* error (KL-divergence to the true data-generating process), not as identifying the exact true model — a slightly misspecified model can still achieve the lowest AIC if it predicts well, which is a different goal from recovering the exact generating process *(required: distinguishes AIC's prediction-focused justification from a "find the true model" claim)* | — |
| E3 | explain | short-answer | 1.47 | Why must AIC/BIC comparisons be restricted to models fit with the same likelihood family and response transformation? | ln(L̂) depends on the specific probability density assumed for the response (Normal vs. Poisson, say) and on any transformation applied to Y; comparing AIC across different families or transformations compares numbers computed on fundamentally different scales, which is not a valid comparison even though both produce a single number labeled "AIC" *(required: names the differing-scale problem across families/transformations)* | — |
| E4 | explain | short-answer | 1.57 | Why does BIC's growing penalty give it asymptotic model-selection consistency while AIC's fixed penalty does not? | as n→∞, BIC's ln(n) penalty eventually overwhelms any finite likelihood improvement an extra, truly unnecessary parameter could provide, so BIC almost surely selects the true (most parsimonious correct) model in the limit; AIC's fixed penalty of 2 never grows to match, so it retains a persistent, non-vanishing chance of overfitting even with unlimited data *(required: names that BIC's penalty eventually dominates any spurious likelihood gain, unlike AIC's fixed one)* | — |
| E5 | explain | short-answer | 1.77 | Why doesn't a small AIC/BIC difference (e.g. under 2) provide strong evidence for preferring one model over another? | AIC/BIC differences below common rule-of-thumb thresholds (often ~2) are considered within the noise of what sampling variability alone could produce — the criteria are themselves computed from a single finite sample, so small gaps don't reliably indicate a real difference in the models' predictive or true-model-approximating quality *(required: names that small gaps fall within expected sampling noise)* | — |
| T2 | transfer | short-answer | 2.07 | How does cross-validation offer an alternative to AIC/BIC for the same model-selection goal, via a different mechanism? | cross-validation directly estimates predictive performance by repeatedly fitting on part of the data and evaluating on held-out data, rather than relying on a theoretical penalty term derived from asymptotic likelihood theory; both pursue the same goal (choosing a model that generalizes, not just fits) but AIC/BIC are computed in closed form from one fit, while cross-validation is a resampling-based, assumption-lighter estimate of the same underlying quantity *(required: names the resampling-vs-closed-form-penalty distinction)* | — |
| T3 | transfer | short-answer | 2.17 | How does BIC's Bayesian justification connect to Bayes' rule? | BIC approximates −2 times the log posterior model probability (up to a constant), under a Bayesian framework where each candidate model has a prior probability and Bayes' rule combines that prior with the data's likelihood to produce a posterior; minimizing BIC is approximately equivalent to maximizing the posterior probability of the model given the data, the same Bayes'-rule combination of prior and likelihood used throughout Bayesian inference *(required: names the posterior-model-probability approximation explicitly)* | —

*Coverage: 16/6/5/3 — 30 items, 0.02…2.22.*

---

## Forward, Backward, Stepwise Selection (`forward-backward-stepwise-selection`)
*Prereq: AIC/BIC · ancestors 31 · b₀ = 1.23*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.23 | Describe forward selection, backward elimination, and stepwise selection. | forward: start with no predictors, add the one most improving the criterion each step; backward: start with all predictors, remove the least useful each step; stepwise: a hybrid allowing both adds and removes | — |
| R2 | recall | mcq | 0.53 | A key limitation shared by all three is: | being greedy, they can get stuck in a locally good but not globally optimal predictor subset | claims they're "guaranteed to find the globally best subset" — the opposite of their actual, well-known limitation → `forward-backward-stepwise-selection` |
| A1 | apply | short-answer | 1.03 | Describe a scenario where forward selection misses the best pair of predictors. | two predictors that are individually useless but powerfully predictive together (an interaction effect) — forward selection, evaluating one variable at a time, may never add either since neither improves the criterion much alone, missing the joint benefit entirely *(required: the specific interaction-effect scenario)* | — |
| E1 | explain | short-answer | 1.73 | Why can backward elimination be infeasible with very many candidate predictors? | it requires fitting the full model with all predictors first, which is expensive or outright impossible if there are more predictors than observations, per `rank`'s bound making the design matrix singular in that regime; forward selection avoids this by never needing the full model *(required: the explicit rank/singularity connection)* | — |
| T1 | transfer | short-answer | 2.23 | Why are modern alternatives like LASSO often preferred over stepwise selection? | LASSO performs variable selection continuously through a single optimization, shrinking some coefficients exactly to zero, rather than a sequence of discrete greedy include/exclude decisions — generally giving more stable results where small data changes are less likely to completely change which variables are selected *(required: the continuous-vs-discrete-decisions contrast)* | — |
| R3 | recall | short-answer | 0.03 | Fill in the blank: forward selection starts with ___ predictors in the model. | zero (no predictors — just the intercept) | — |
| R4 | recall | short-answer | 0.08 | Fill in the blank: backward elimination starts with ___ predictors in the model. | all candidate predictors (the full model) | — |
| R5 | recall | short-answer | 0.13 | Name a criterion commonly used to decide whether to add or remove a predictor at each step. | AIC, BIC, adjusted R², or a p-value threshold (any one) | — |
| R6 | recall | mcq | 0.18 | True/false: stepwise selection allows both adding and removing predictors at each step. | True — that flexibility (able to both add and drop) is exactly what distinguishes it from pure forward or pure backward selection | claims "stepwise selection, like forward selection, can only add predictors, never remove them" — the ability to both add and remove is its defining hybrid feature → `forward-backward-stepwise-selection` |
| R7 | recall | mcq | 0.28 | Which method requires fitting the full model with all candidate predictors as its very first step? | backward elimination | picks "forward selection" — forward selection starts empty and never needs to fit the full model at all → `forward-backward-stepwise-selection` |
| R8 | recall | short-answer | 0.33 | What is the primary drawback shared by all three of these greedy methods? | they can get stuck in a locally good but not globally optimal subset of predictors, since each step only looks one move ahead | — |
| R9 | recall | short-answer | 0.38 | Fill in the blank: at each step of forward selection, the predictor added is the one that most improves the ___. | chosen selection criterion (e.g. AIC, BIC, or R²) among the remaining candidates | — |
| R10 | recall | mcq | 0.43 | True/false: backward elimination can be used even when p>n. | False — it requires fitting the full model with all p predictors first, which is infeasible when p exceeds n (the design matrix cannot have full column rank) | claims "backward elimination works fine even with more predictors than observations, since it just removes predictors" — it still needs to fit the full model as its starting point, which is exactly what p>n prevents → `forward-backward-stepwise-selection` |
| R11 | recall | short-answer | 0.53 | What can happen to a predictor added early in forward selection if it becomes redundant after later additions? | it stays in the model regardless (pure forward selection never revisits or removes earlier choices), potentially leaving a suboptimal, redundant predictor in place | — |
| R12 | recall | mcq | 0.58 | Which of these three methods is generally preferred when p is very large relative to n? | forward selection | picks "backward elimination" — backward elimination is specifically infeasible in that regime since it needs the full model fit first → `forward-backward-stepwise-selection` |
| R13 | recall | short-answer | 0.63 | Fill in the blank: stepwise selection is a ___ of forward and backward approaches. | hybrid (combination) | — |
| R14 | recall | mcq | 0.68 | True/false: forward, backward, and stepwise selection are guaranteed to find the single best-fitting subset of predictors. | False — being greedy, none of the three guarantees a globally optimal subset; only exhaustive best-subset search checks every possible combination | claims "these methods are mathematically guaranteed to find the globally optimal subset, just more efficiently than exhaustive search" — greedy search trades that guarantee away for speed → `forward-backward-stepwise-selection` |
| R15 | recall | short-answer | 0.73 | State a common stopping rule for terminating forward selection. | stop when no remaining candidate predictor further improves the criterion (e.g. adding any predictor would increase AIC/BIC rather than decrease it) | — |
| R16 | recall | mcq | 0.83 | Which of the three methods can "undo" an earlier decision by removing a previously added predictor? | stepwise selection | picks "forward selection" — pure forward selection only ever adds, never removes, previously chosen predictors → `forward-backward-stepwise-selection` |
| A2 | apply | short-answer | 0.88 | With 3 candidate predictors X₁, X₂, X₃, suppose adding X₂ alone gives the largest single-predictor AIC improvement, and then adding X₁ to {X₂} improves AIC further while adding X₃ to {X₂} does not. Trace forward selection's first two steps. | step 1: X₂ is added first (largest individual improvement); step 2: among {X₁, X₃}, X₁ is added next since it improves the criterion further while X₃ does not — the model after two steps is {X₂, X₁} *(required: the correct order and the correct reasoning for each step)* | — |
| A3 | apply | short-answer | 0.98 | Describe a scenario where stepwise selection adds a predictor at one step, then removes it at a later step. | a predictor X₁ improves the criterion when added early (little overlap with what's in the model yet); several steps later, once X₄ and X₅ (which jointly explain much of what X₁ did) have both been added, X₁'s marginal contribution becomes negative under the criterion, and stepwise selection removes it *(required: names that later additions made the earlier one redundant)* | — |
| A4 | apply | short-answer | 1.08 | Why is forward selection computationally cheaper than backward elimination when p is very large? | forward selection never fits a model with more than the (small, growing) number of currently-included predictors, so its most expensive single fit involves at most all p predictors at the very last possible step; backward elimination's *very first* step already requires fitting the full p-predictor model, which is the most expensive fit of all and must happen immediately regardless of how quickly variables are then dropped *(required: contrasts when the expensive full-model fit occurs in each method)* | — |
| A5 | apply | numeric | 1.18 | With p=5 candidate predictors, forward selection runs the full number of steps needed to consider each one. In the worst case, how many total models are fit (summing over all steps, one fit per remaining candidate per step)? `[verified: 15]` | at step 1, 5 models are fit; step 2, 4; step 3, 3; step 4, 2; step 5, 1 — total = 5+4+3+2+1 = p(p+1)/2 = 15 | — |
| A6 | apply | short-answer | 1.28 | Contrast the computational cost of greedy forward selection (per A5) with exhaustive best-subset search for the same p=5 predictors. | exhaustive best-subset search fits 2⁵=32 models (every possible subset), compared to forward selection's 15 — the gap between p(p+1)/2 and 2^p widens dramatically as p grows, which is exactly why greedy methods trade the optimality guarantee for tractability at larger p *(required: the explicit 2^p vs. p(p+1)/2 comparison)* | — |
| E2 | explain | short-answer | 1.38 | Why is "greedy" the accurate term for how these algorithms make their step-by-step decisions? | at each step, the algorithm commits to whichever single best local move (add/remove one predictor) improves the criterion the most right now, without ever looking ahead to whether a different, locally worse choice might open up a better path several steps later — exactly the defining behavior of a greedy algorithm *(required: names the "commit to the locally best move without lookahead" definition)* | — |
| E3 | explain | short-answer | 1.48 | Why might forward and backward selection, applied to the same data, land on different final models? | each method's fixed starting point (empty vs. full model) constrains which sequence of local moves is even possible to make, and greedy search is path-dependent — a predictor that looks locally best from an empty starting model may never get considered the same way starting from the full model, so the two search paths can converge to genuinely different local optima *(required: names the path-dependence of greedy search from different starting points)* | — |
| E4 | explain | short-answer | 1.58 | Why doesn't stepwise selection's ability to remove earlier additions fully solve the local-optimum problem? | stepwise selection still only ever considers single-predictor add/remove moves at each step, based on the current model state — it can undo a bad local choice, but it still can never "see" a combination of two or three simultaneous changes that would jointly help even though no single one of them does individually, the same blind spot pure forward selection has *(required: names that stepwise still only evaluates one-variable-at-a-time moves)* | — |
| E5 | explain | short-answer | 1.78 | Why is p-value-based stepwise selection considered statistically problematic? | testing many candidate predictors' p-values across repeated steps, each time keeping only what "looks significant," is exactly the multiple-comparisons problem `anova`'s T1 warns about — the reported p-values and confidence intervals of the final selected model no longer have their claimed properties, since the selection process itself was driven by the same data being used for inference *(required: names the multiple-comparisons/data-dredging issue specifically)* | — |
| T2 | transfer | short-answer | 2.08 | How is the greedy logic of stepwise selection structurally similar to the greedy splitting used to grow a decision tree, connecting to `splitting-criteria`? | both algorithms repeatedly make the single locally-best choice — the predictor that most improves a criterion here, the split that most improves node purity there — without any guarantee that this sequence of local choices produces the globally optimal structure; both are classic instances of greedy search applied to a combinatorially large space that exhaustive search cannot cover *(required: names the shared "no global optimality guarantee from local greedy choices" structure)* | — |
| T3 | transfer | short-answer | 2.18 | How does the p≫n limitation of backward elimination connect to the general feature-selection challenge discussed in the ML sweep? | just as `curse-of-dimensionality` warns that high-dimensional, low-sample settings break many standard statistical procedures, backward elimination's requirement to fit a full p-parameter model first fails exactly when p exceeds n — motivating the same shift toward methods like LASSO that perform variable selection without ever needing a full, potentially unfittable model as a starting point *(required: names the shared high-dimensional/p≫n motivation for moving beyond backward elimination)* | —

*Coverage: 16/6/5/3 — 30 items, 0.03…2.23.*

---

## Regularization (`regularization`)
*Prereq: Multiple Linear Regression, Bias-Variance Tradeoff · ancestors 29 · b₀ = 1.20*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.2 | Describe regularization's basic mechanism. | add a penalty term to the regression objective discouraging large coefficients, trading a little bias for a reduction in variance | — |
| R2 | recall | mcq | 0.5 | Regularization is particularly useful when: | there are many predictors (possibly more than observations) or predictors are highly correlated — situations where OLS becomes unstable/high-variance | picks the opposite scenario ("few predictors, none correlated") — exactly when regularization is least needed → `regularization` |
| A1 | apply | short-answer | 1.0 | Why does penalizing large coefficients help when predictors are highly correlated? | per `vif`'s finding that Var(β̂) inflates with collinearity, the penalty "reins in" the wildly unstable coefficient estimates multicollinearity would otherwise produce, trading some bias for a large variance reduction *(required: the explicit VIF-inflation callback)* | — |
| E1 | explain | short-answer | 1.7 | Describe the general regularized objective and the role of λ. | minimize [SSE + λ·(penalty on β)]; at λ=0 this reduces to ordinary OLS; as λ→∞, coefficients are pushed toward (or exactly to) zero; λ is chosen via cross-validation, a hyperparameter rather than something learned directly from training data alone, per `hyperparameters`' earlier warning *(required: both extremes of λ and the hyperparameter framing)* | — |
| T1 | transfer | short-answer | 2.2 | How does regularization explicitly implement the bias-variance tradeoff? | a regularized model deliberately accepts increased bias (β̂ is no longer unbiased once λ>0) in exchange for reduced variance, and the optimal λ chosen via CV is precisely the value minimizing *total* expected error (bias²+variance), not bias or variance alone *(required: names that the optimum balances the total, not either term individually)* | — |
| R3 | recall | short-answer | 0.0 | Fill in the blank: the general regularized objective is ___. | minimize [SSE + λ·(penalty on β)] | — |
| R4 | recall | short-answer | 0.05 | What does setting λ=0 reduce the regularized objective to? | ordinary OLS — the penalty term vanishes entirely | — |
| R5 | recall | short-answer | 0.1 | What happens to the coefficients as λ→∞? | they are pushed toward (or, for some penalties, exactly to) zero | — |
| R6 | recall | short-answer | 0.15 | How is λ typically chosen in practice? | via cross-validation, selecting the λ that minimizes estimated out-of-sample prediction error | — |
| R7 | recall | mcq | 0.25 | True/false: regularization typically reduces bias while increasing variance. | False — it does the opposite: it typically increases bias slightly in exchange for a reduction in variance | claims "regularization trades variance for bias, lowering bias at the cost of higher variance" — regularization's whole point is the reverse trade, accepting bias to reduce variance → `regularization` |
| R8 | recall | mcq | 0.3 | Which of these is a standard reason to use regularization? | there are many predictors relative to observations, or predictors are highly correlated | picks "the response variable is measured with perfect precision" — an unrelated, irrelevant consideration for whether regularization is useful → `regularization` |
| R9 | recall | short-answer | 0.35 | Fill in the blank: λ is called a ___, since it is not learned from the training data through the same optimization as β. | hyperparameter | — |
| R10 | recall | short-answer | 0.45 | What is the general name for techniques that add a penalty term to a loss function to discourage complexity? | regularization | — |
| R11 | recall | mcq | 0.55 | True/false: regularization can help even when p<n and predictors are uncorrelated. | True — even in that setting, shrinking coefficients slightly toward zero can reduce variance and improve prediction accuracy, though the benefit is typically smaller than in high-p or highly-correlated settings | claims "regularization is only ever useful when p>n or predictors are collinear" — it can still help modestly outside those extreme cases too → `regularization` |
| R12 | recall | short-answer | 0.6 | What does "shrinkage" refer to in the context of regularization? | the pulling of coefficient estimates toward zero (or toward some other fixed value), relative to the unregularized OLS estimates | — |
| R13 | recall | short-answer | 0.65 | Fill in the blank: regularization trades some ___ for a reduction in ___. | bias; variance | — |
| R14 | recall | mcq | 0.7 | Which of these is NOT a typical benefit attributed to regularization? | it always reduces training-set SSE below what unregularized OLS achieves | picks the one false claim — regularization typically *increases* training SSE slightly (since it doesn't minimize SSE alone), while typically improving test/generalization performance → `regularization` |
| R15 | recall | short-answer | 0.75 | What role does cross-validation play in choosing λ? | it estimates out-of-sample prediction error for each candidate λ by repeatedly training on part of the data and evaluating on held-out data, letting λ be chosen to minimize that estimated error rather than training error | — |
| R16 | recall | mcq | 0.85 | True/false: a regularized model's coefficients are generally smaller in magnitude than the unregularized OLS coefficients. | True — shrinkage toward zero is the defining mechanical effect of adding the penalty term | claims "regularized coefficients are generally larger in magnitude than OLS's, since the penalty forces the model to compensate" — the penalty shrinks coefficients, it does not inflate them → `regularization` |
| A2 | apply | short-answer | 0.8 | With n=50 observations and p=200 candidate predictors (p>n), explain why plain OLS fails and regularization helps. | with p>n, X cannot have full column rank, so XᵀX is singular and OLS has no unique solution at all; adding λI (as in ridge) or an L1 penalty (as in LASSO) makes the regularized objective well-posed and solvable even though ordinary OLS is not, directly addressing the singularity rather than merely improving on a workable-but-unstable fit *(required: names that OLS is literally unsolvable here, not merely unstable)* | — |
| A3 | apply | short-answer | 0.9 | Describe what typically happens to training error and test error as λ increases from 0. | training error increases monotonically from its OLS minimum, since the model is no longer purely minimizing SSE; test error typically first decreases (as reduced variance outweighs the small added bias) before eventually increasing again once λ becomes so large that the added bias dominates — a U-shaped test-error curve *(required: names the different training-vs-test-error trajectories, including the U-shape for test error)* | — |
| A4 | apply | short-answer | 1.05 | Explain conceptually why adding a penalty term makes β̂ biased. | the penalized objective is no longer minimized by the same β that minimizes SSE alone — the penalty explicitly pulls the optimal solution away from the unbiased OLS estimate toward smaller values, so E[β̂_regularized] no longer equals the true β exactly, by the very design of the penalty *(required: names that the penalty changes what's being minimized away from the unbiased OLS objective)* | — |
| A5 | apply | short-answer | 1.15 | Two λ values give similar cross-validated test error; one is much larger than the other. Which is generally preferred, and why? | the larger λ (the simpler, more heavily regularized model) is generally preferred when performance is comparable, following the "one-standard-error rule": among models within one standard error of the best CV performance, prefer the simplest one, since it's less likely to be overfitting to noise in the CV estimate itself *(required: names the preference for simplicity when performance ties, and ideally the one-SE-rule reasoning)* | — |
| A6 | apply | numeric | 1.25 | For a candidate λ=2, a model has SSE=50 and penalty term (on β) equal to 10. Compute the value of the regularized objective. `[verified: 70]` | objective = SSE + λ·penalty = 50 + 2×10 = 70 | — |
| E2 | explain | short-answer | 1.35 | Why doesn't λ=0 (no penalty at all) always give the best predictive model, even though it has zero bias from the penalty itself? | zero regularization bias doesn't mean zero total error — OLS at λ=0 can still have high *variance*, especially with many or correlated predictors, and total expected prediction error is bias² plus variance; a small nonzero λ can lower total error by trading a little bias for a larger reduction in variance *(required: names that total error, not bias alone, is what matters)* | — |
| E3 | explain | short-answer | 1.45 | Why is λ chosen via cross-validation rather than by minimizing the (fully) regularized training objective directly over λ as well as β? | minimizing the training objective over λ as well as β would always select λ=0, since any nonzero penalty only makes the training objective's minimum value worse (higher) — the penalty's entire purpose is to trade training fit for generalization, so its strength has to be tuned against *held-out* performance, not training performance *(required: names that optimizing λ on training data alone always pushes λ toward 0)* | — |
| E4 | explain | short-answer | 1.55 | Why can regularization improve prediction accuracy on new data despite introducing bias? | expected prediction error decomposes as bias² + variance (+ irreducible noise); when variance is the dominant source of error (as with many/correlated predictors), a modest increase in bias² can be more than offset by a larger decrease in variance, lowering total expected error even though the estimator is no longer unbiased *(required: the explicit bias²+variance tradeoff argument)* | — |
| E5 | explain | short-answer | 1.75 | Why is there no single universally "good" value of λ across different datasets or problems? | the optimal balance between bias and variance depends on the specific signal-to-noise ratio, the number and correlation structure of predictors, and the sample size of each particular dataset — a λ that's ideal for one problem's variance-versus-bias tradeoff can be far too weak or too strong for a different problem's tradeoff *(required: names that the ideal bias-variance balance is dataset-specific, not universal)* | — |
| T2 | transfer | short-answer | 2.05 | How does regularization's bias-variance framing connect directly to the general bias-variance decomposition of expected prediction error? | expected test MSE decomposes exactly as bias² + variance + irreducible noise; regularization is a deliberate, controllable way to move along that tradeoff curve by adjusting λ, whereas the decomposition itself is a general fact about any estimator — regularization is simply one concrete lever for navigating the tradeoff that decomposition describes abstractly *(required: names the decomposition as the general principle regularization is a concrete instance of)* | — |
| T3 | transfer | short-answer | 2.15 | How can regularization be reframed as Bayesian MAP estimation with a prior on β? | adding a penalty term λ·(penalty on β) to the loss is mathematically equivalent to placing a prior distribution on β (e.g. a Gaussian prior for an L2 penalty, a Laplace prior for an L1 penalty) and finding the maximum a posteriori estimate rather than the maximum likelihood estimate; λ's strength corresponds to the prior's concentration around zero, so regularization is a frequentist mechanism achieving the same shrinkage a Bayesian prior would produce *(required: names the prior-on-β / MAP-estimation reframing explicitly)* | —

*Coverage: 16/6/5/3 — 30 items, 0…2.2.*

---

## LASSO (`lasso`)
*Prereq: Regularization · ancestors 30 · b₀ = 1.22*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.22 | State LASSO's penalty term. | λΣ\|βⱼ\| — the L1 norm of the coefficients | — |
| R2 | recall | mcq | 0.52 | LASSO's distinctive property is: | it can shrink some coefficients exactly to zero, performing automatic variable selection | claims it "never shrinks coefficients" — the opposite of its defining behavior → `lasso` |
| A1 | apply | short-answer | 1.02 | Using `vector-norm`'s diamond-shaped L1 level sets, explain why LASSO's solutions often land exactly on a corner (a coefficient = 0). | the L1 constraint region is a diamond with corners on the coordinate axes; the optimal point where a loss contour first touches this region is disproportionately likely to land exactly on a corner, unlike ridge's smooth circular region, which has no corners *(required: the direct reuse of that geometric argument, not a fresh justification)* | — |
| E1 | explain | short-answer | 1.72 | Why is LASSO's automatic selection especially useful when p≫n and only a few predictors are truly relevant? | per `curse-of-dimensionality`'s earlier discussion, high-dimensional data with a "sparse" true signal benefits from a principled, automatic way to discard irrelevant dimensions — exactly what LASSO's zero-coefficient behavior provides *(required: the explicit sparsity/high-dimensionality connection)* | — |
| T1 | transfer | short-answer | 2.22 | Describe LASSO's limitation with groups of highly correlated relevant predictors, and what it motivates. | LASSO tends to arbitrarily pick one correlated predictor and zero out the others, rather than keeping all with smaller coefficients — undesirable for interpretation, since which variable "wins" can be unstable across different samples of the same data; this limitation motivates `elastic-net`, covered next *(required: names the arbitrary-selection instability specifically)* | — |
| R3 | recall | short-answer | 0.02 | What does the acronym LASSO stand for? | Least Absolute Shrinkage and Selection Operator | — |
| R4 | recall | short-answer | 0.07 | What norm does LASSO's penalty use? | the L1 norm — the sum of absolute values of the coefficients | — |
| R5 | recall | mcq | 0.12 | True/false: LASSO generally has a closed-form solution like ordinary OLS. | False — because the L1 penalty is not differentiable at zero, LASSO generally requires iterative numerical optimization rather than a single closed-form matrix formula | claims "LASSO's solution is given by a closed-form matrix formula, exactly like ridge's (XᵀX+λI)⁻¹Xᵀy" — LASSO's non-differentiable penalty specifically rules out that kind of closed form → `lasso` |
| R6 | recall | short-answer | 0.17 | What algorithm is commonly used to fit LASSO, given the lack of a closed form? | coordinate descent (optimizing one coefficient at a time via soft-thresholding), or LARS (least angle regression) | — |
| R7 | recall | mcq | 0.27 | LASSO's constraint region in coefficient space (for the equivalent constrained formulation) is shaped like a: | diamond (a cross-polytope), with corners on the coordinate axes | picks "a smooth circle/ellipse" — that describes ridge's L2 constraint region, not LASSO's L1 one → `lasso` |
| R8 | recall | short-answer | 0.32 | Fill in the blank: as λ increases in LASSO, more coefficients become exactly ___. | zero | — |
| R9 | recall | short-answer | 0.37 | What is a "sparse" model, in LASSO's context? | a model where most coefficients are exactly zero, leaving only a small subset of predictors with nonzero effects | — |
| R10 | recall | mcq | 0.42 | True/false: LASSO's penalized objective can be written equivalently as a constrained optimization: minimize SSE subject to Σ\|βⱼ\|≤t. | True — the penalized (Lagrangian) form and this constrained form are equivalent for a corresponding pair of λ and t | claims "the penalized and constrained forms of LASSO are only loosely related heuristics, not mathematically equivalent" — they are formally equivalent via Lagrangian duality → `lasso` |
| R11 | recall | short-answer | 0.47 | What practical benefit does LASSO's sparsity provide for model interpretability? | a sparse model with few nonzero coefficients is easier to interpret and communicate — it directly identifies a small subset of predictors as "the ones that matter," unlike a model where every coefficient is small but nonzero | — |
| R12 | recall | short-answer | 0.52 | Fill in the blank: LASSO's penalty term is λΣ___. | \|βⱼ\|, the sum of absolute values of the coefficients | — |
| R13 | recall | mcq | 0.57 | Which regularization method discussed in this cluster is specifically known for automatic variable selection? | LASSO | picks "ridge regression" — ridge shrinks coefficients smoothly but does not set them exactly to zero, so it does not perform variable selection → `lasso` |
| R14 | recall | mcq | 0.62 | True/false: LASSO's solution path, as λ varies from 0 to ∞, is piecewise linear. | True — the LARS algorithm exploits exactly this piecewise-linear structure to compute the entire solution path efficiently | claims "LASSO's solution path is smooth and nonlinear at every point, unlike ridge's" — the path is actually piecewise *linear*, a specific, exploitable structural property → `lasso` |
| R15 | recall | short-answer | 0.72 | What does "λ_max" refer to, in the context of LASSO's solution path? | the smallest λ value above which every coefficient is shrunk exactly to zero (the model becomes empty) | — |
| R16 | recall | mcq | 0.82 | True/false: LASSO always outperforms ridge regression in prediction accuracy. | False — which performs better depends on the true underlying structure; ridge tends to do better when most predictors have small-but-nonzero true effects, while LASSO tends to do better when the true effect is genuinely sparse | claims "LASSO's variable-selection property makes it strictly better than ridge in every setting" — their relative performance depends on whether the true coefficients are actually sparse → `lasso` |
| A2 | apply | numeric | 0.87 | LASSO's coordinate-wise update uses the soft-threshold operator S(β,λ)=sign(β)·max(\|β\|−λ,0). For λ=0.5, compute S(2, 0.5) and S(0.3, 0.5). `[verified: S(2,0.5)=1.5; S(0.3,0.5)=0]` | S(2,0.5)=sign(2)·max(2−0.5,0)=1.5; S(0.3,0.5)=sign(0.3)·max(0.3−0.5,0)=sign(0.3)·max(−0.2,0)=0 | — |
| A3 | apply | short-answer | 0.97 | Describe coordinate descent's role in fitting LASSO. | it optimizes the LASSO objective one coefficient at a time, holding all others fixed, using the closed-form soft-threshold update at each coordinate; repeating this cycle across all coordinates until convergence avoids needing a single closed-form solution for the whole vector β at once *(required: names the one-coordinate-at-a-time-with-soft-thresholding mechanism)* | — |
| A4 | apply | numeric | 1.07 | Apply the soft-threshold operator to β=−1.2 with λ=0.7. `[verified: -0.5]` | S(−1.2, 0.7) = sign(−1.2)·max(1.2−0.7, 0) = −1·0.5 = −0.5 | — |
| A5 | apply | short-answer | 1.17 | Why does even a moderately-sized λ tend to zero out coefficients whose OLS values were already small? | the soft-threshold operator subtracts λ from a coefficient's magnitude before checking if the result is still positive; a coefficient whose OLS magnitude is smaller than λ has nothing left after that subtraction and lands exactly at 0, while a coefficient with a much larger OLS magnitude survives with its size merely reduced by λ *(required: connects to the soft-threshold operator's max(|β|−λ,0) structure)* | — |
| A6 | apply | short-answer | 1.27 | With p=1000 candidate predictors, n=100 observations, and only 20 truly relevant predictors, why does LASSO's sparsity assumption fit this scenario well? | the true data-generating process here genuinely has a sparse structure — only 20 of 1000 coefficients are truly nonzero — which is exactly the regime LASSO's L1 penalty is designed to recover; a method assuming *all* predictors contribute a little (like ridge) would be fighting against, rather than exploiting, the actual sparse structure of the truth *(required: names that the true sparsity matches LASSO's structural assumption)* | — |
| E2 | explain | short-answer | 1.37 | Why does the corner-touching geometric argument (from `lasso`'s own A1) not produce sparsity for ridge's circular constraint region? | a circle has no corners — every point on its boundary is smooth, so a loss contour touching the boundary at its closest point lands at a generic smooth location, not disproportionately at any coordinate axis; only a region with corners exactly on the axes (like the L1 diamond) makes landing at a corner — meaning some coefficients are exactly zero — a geometrically likely outcome *(required: explicitly names the absence of corners on a circle as the reason)* | — |
| E3 | explain | short-answer | 1.47 | Why is the soft-thresholding operator a natural solution to LASSO's per-coordinate optimization? | for a single coordinate, minimizing a quadratic loss term plus an L1 penalty λ|β| has a closed-form solution that either shrinks the unpenalized optimum toward zero by exactly λ, or sets it to exactly zero if that shrinkage would overshoot past zero — this "shrink-or-zero-out" behavior is precisely what the max(|β|−λ,0) formula implements coordinate by coordinate *(required: names the shrink-or-set-to-zero behavior the formula captures)* | — |
| E4 | explain | short-answer | 1.67 | Why can LASSO's variable selection be unstable across resampled versions of the same dataset? | when several predictors are highly correlated, LASSO's L1 penalty (per its own T1) tends to arbitrarily favor one over the others based on small sample-specific fluctuations in the data; a slightly different resample can tip that arbitrary choice toward a different one of the correlated group, so the *set* of selected variables can vary noticeably even though the overall predictive fit stays similar *(required: connects instability to the arbitrary-selection-among-correlated-predictors behavior)* | — |
| E5 | explain | short-answer | 1.87 | Why is a fully closed-form solution unavailable for LASSO in general, unlike for ridge? | ridge's penalty λΣβⱼ² is differentiable everywhere, so setting the gradient of its objective to zero yields a single linear system with matrix solution (XᵀX+λI)⁻¹Xᵀy; LASSO's penalty λΣ\|βⱼ\| is not differentiable at βⱼ=0 (the absolute value has a kink there), so the objective's minimum cannot generally be found by simply setting a gradient to zero — it requires methods (coordinate descent, subgradients) that handle that non-smooth kink *(required: names the non-differentiability at zero as the specific obstacle)* | — |
| T2 | transfer | short-answer | 2.07 | How does LASSO's L1-penalty-induced sparsity connect to the broader idea of sparse recovery/compressed sensing? | both rely on the same core insight — that an L1 penalty (or constraint) on a vector, when the true signal is genuinely sparse, tends to recover that sparse signal exactly or nearly exactly, even from fewer measurements/observations than the dimension would naively require; LASSO is a statistical-regression instance of the same L1-sparsity principle that underlies compressed sensing's signal-recovery guarantees *(required: names the shared L1-induces-sparse-recovery principle)* | — |
| T3 | transfer | short-answer | 2.27 | How does LASSO's automatic variable selection relate to `feature-selection` as discussed in the ML sweep? | `feature-selection` covers the general problem of choosing which predictors to keep before or during modeling; LASSO is one specific, embedded (built into the fitting procedure itself) method for solving exactly that problem, contrasted with filter methods (ranking predictors independently of the model) or wrapper methods (like forward/backward selection) that sit outside the model-fitting step *(required: names LASSO as an embedded method, distinguished from filter/wrapper approaches)* | —

*Coverage: 16/6/5/3 — 30 items, 0.02…2.27.*

---

## Ridge Regression (`ridge-regression`)
*Prereq: Regularization · ancestors 30 · b₀ = 1.22*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.22 | State ridge's penalty term. | λΣβⱼ² — the squared L2 norm of the coefficients | — |
| R2 | recall | mcq | 0.52 | Unlike LASSO, ridge regression: | shrinks all coefficients smoothly toward zero, generally without setting any exactly to zero | claims it "performs automatic variable selection" — that's LASSO's distinguishing property, not ridge's → `ridge-regression` |
| A1 | apply | derivation | 1.02 | β̂_ridge=(XᵀX+λI)⁻¹Xᵀy. Why does adding λI fix the singularity that multicollinearity can cause? `[verified: det(XtX)=0 for a collinear example, det(XtX+0.1I)=15.01, no longer singular]` | XᵀX is always positive semi-definite (eigenvalues ≥0); adding λI shifts every eigenvalue up by λ>0, guaranteeing strict positive definiteness — hence invertibility — regardless of whether XᵀX alone was singular *(required: the eigenvalue-shift argument, reusing `positive-definite-matrices`' eigenvalue characterization)* | — |
| E1 | explain | short-answer | 1.72 | How do ridge's two extremes (λ→0 and λ→∞) connect to the bias-variance tradeoff? | λ=0 recovers OLS exactly (low bias, high variance); λ→∞ shrinks all coefficients toward zero (predicting a constant — high bias, near-zero variance) — the two extremes of the bias-variance spectrum, with useful models living in between *(required: both extremes explicitly named)* | — |
| T1 | transfer | short-answer | 2.22 | Why might ridge be preferred over LASSO when predictors are highly correlated and most/all are genuinely relevant? | unlike LASSO's tendency (per `lasso`'s T1) to arbitrarily pick one correlated predictor and zero the rest, ridge shrinks correlated predictors' coefficients together and similarly, often giving more stable, interpretable results when sparsity isn't the right assumption *(required: the explicit contrast with LASSO's arbitrary-selection behavior)* | — |
| R3 | recall | short-answer | 0.02 | Fill in the blank: ridge's penalty term is λΣ___. | βⱼ², the sum of squared coefficients | — |
| R4 | recall | short-answer | 0.07 | What norm does ridge regression's penalty use? | the (squared) L2 norm | — |
| R5 | recall | short-answer | 0.12 | State ridge's closed-form solution formula. | β̂_ridge = (XᵀX + λI)⁻¹Xᵀy | — |
| R6 | recall | mcq | 0.17 | True/false: ridge sets some coefficients exactly to zero, like LASSO does. | False — ridge shrinks all coefficients smoothly toward zero but generally never sets any exactly to zero | claims "ridge, like LASSO, performs automatic variable selection by zeroing out coefficients" — that is LASSO's distinguishing behavior; ridge's smooth L2 penalty does not produce exact zeros → `ridge-regression` |
| R7 | recall | mcq | 0.27 | Ridge's constraint region in coefficient space (for the equivalent constrained formulation) is shaped like a: | circle/ellipse (a smooth sphere in higher dimensions), with no corners | picks "a diamond with corners on the coordinate axes" — that describes LASSO's L1 constraint region, not ridge's smooth L2 one → `ridge-regression` |
| R8 | recall | short-answer | 0.32 | Fill in the blank: adding λI to XᵀX ensures the resulting matrix is always ___. | invertible (strictly positive definite), for any λ>0 | — |
| R9 | recall | short-answer | 0.37 | What happens to ridge's coefficients as λ→∞? | they shrink toward zero, approaching (but not exceeding) predicting a constant (the intercept alone) | — |
| R10 | recall | short-answer | 0.42 | What happens to ridge's coefficients as λ→0? | they converge to the ordinary OLS estimates | — |
| R11 | recall | mcq | 0.47 | True/false: ridge regression can be fit even when p>n, unlike plain OLS. | True — because λI makes (XᵀX+λI) invertible for any λ>0, ridge produces a unique solution even when XᵀX alone is singular (as it is whenever p>n) | claims "ridge, like plain OLS, cannot be computed at all when p>n" — the entire point of the λI term is that it remains invertible in exactly that regime → `ridge-regression` |
| R12 | recall | mcq | 0.57 | Which of the following is NOT a property of ridge regression? | it produces sparse solutions with many coefficients set exactly to zero | picks the one false statement — that describes LASSO; ridge instead shrinks all coefficients smoothly without zeroing any out → `ridge-regression` |
| R13 | recall | short-answer | 0.62 | Fill in the blank: ridge is also called ___ regularization. | L2 (or Tikhonov) regularization | — |
| R14 | recall | short-answer | 0.72 | What kind of Bayesian prior on β corresponds to ridge regression's penalty? | a Gaussian (Normal) prior centered at zero | — |
| R15 | recall | mcq | 0.77 | True/false: ridge's penalty function is differentiable everywhere, including at β=0. | True — βⱼ² is a smooth polynomial with no kinks, unlike LASSO's \|βⱼ\|, which has a non-differentiable point at 0 | claims "ridge's penalty, like LASSO's, is non-differentiable at zero" — the squared term is smooth everywhere, which is exactly why ridge has a closed-form solution and LASSO doesn't → `ridge-regression` |
| R16 | recall | mcq | 0.87 | Which regularization method is generally preferred when many correlated predictors are all believed to be genuinely relevant? | ridge regression | picks "LASSO" — LASSO tends to arbitrarily zero out some of the correlated group, which is undesirable exactly when all of them are believed relevant → `ridge-regression` |
| A2 | apply | numeric | 0.82 | XᵀX = [[2,0],[0,2]] and λ=1. Compute XᵀX + λI. `[verified: [[3,0],[0,3]]]` | XᵀX + λI = [[2,0],[0,2]] + [[1,0],[0,1]] = [[3,0],[0,3]] | — |
| A3 | apply | short-answer | 0.92 | How does ridge handle two perfectly correlated predictors differently from LASSO? | ridge tends to split the combined effect roughly evenly between the two correlated predictors (since the L2 penalty penalizes large individual coefficients more than two moderate ones), whereas LASSO's L1 penalty tends to arbitrarily assign the full effect to one of them and zero out the other *(required: names the split-evenly-vs-arbitrarily-pick-one contrast)* | — |
| A4 | apply | numeric | 1.02 | A univariate ridge problem has (scalar) XᵀX=4, Xᵀy=8, and λ=1. Compute β̂_ridge. `[verified: 1.6]` | β̂_ridge = Xᵀy/(XᵀX+λ) = 8/(4+1) = 8/5 = 1.6 | — |
| A5 | apply | short-answer | 1.12 | Why does ridge shrink correlated predictors' coefficients toward each other (a "grouping" tendency), unlike LASSO's arbitrary single-predictor selection? | the L2 penalty Σβⱼ² is minimized, for a fixed combined effect, by splitting that effect roughly equally across correlated predictors rather than concentrating it in one (since squaring penalizes one large coefficient more than two smaller ones summing to the same total); this smooth, convex penalty has no preference for extreme, lopsided solutions the way LASSO's corner-seeking L1 penalty does *(required: names the squaring-penalizes-concentration mechanism)* | — |
| A6 | apply | short-answer | 1.22 | Why must predictors typically be standardized (mean 0, unit variance) before applying ridge regression? | the penalty λΣβⱼ² treats every coefficient's magnitude equally, but a predictor measured on a larger numeric scale naturally has a smaller true coefficient (and vice versa); without standardizing, the penalty would unfairly shrink predictors on smaller scales more aggressively relative to their actual importance, purely as an artifact of units *(required: names the unit-dependence problem standardization corrects)* | — |
| E2 | explain | short-answer | 1.32 | Why does ridge's L2 penalty never produce exactly zero coefficients, in contrast to LASSO's kink at zero? | the derivative of βⱼ² at βⱼ=0 is exactly 0, so there is no penalty-driven "force" pushing a small nonzero coefficient all the way to exactly zero — coefficients shrink smoothly and continuously but asymptotically approach zero without a mechanism that snaps them to it, unlike \|βⱼ\|'s constant-magnitude subgradient at zero that LASSO's soft-thresholding exploits *(required: names the zero-derivative-at-zero property of the squared penalty)* | — |
| E3 | explain | short-answer | 1.42 | Why is ridge's closed-form solution always computable, in a way LASSO's generally is not? | ridge's objective is a smooth, differentiable quadratic function of β everywhere, so setting its gradient to zero always yields a single linear system, (XᵀX+λI)β=Xᵀy, solvable by direct matrix inversion; LASSO's non-differentiable kink at zero (per `lasso`'s E5) rules out that same gradient-set-to-zero approach *(required: contrasts ridge's smooth quadratic objective with LASSO's non-smooth one)* | — |
| E4 | explain | short-answer | 1.62 | Why does scale matter so much for a penalty like ridge's, more than it does for OLS itself? | plain OLS is scale-equivariant — rescaling a predictor just rescales its coefficient inversely, with no change to predictions; ridge's penalty adds an absolute-magnitude cost to each coefficient regardless of that predictor's original scale, so predictors on different scales end up penalized unequally unless standardized first, breaking the scale-equivariance OLS naturally has *(required: contrasts OLS's scale-equivariance with ridge's scale-sensitivity)* | — |
| E5 | explain | short-answer | 1.82 | Why can ridge outperform LASSO on pure prediction accuracy even when the true model isn't sparse? | when many or all predictors genuinely contribute a small true effect, LASSO's tendency to zero some of them out entirely discards real (if small) signal, while ridge's smooth shrinkage retains a little of every predictor's contribution — closer to the true dense structure, so ridge's bias-variance tradeoff is better matched to a non-sparse truth *(required: names that ridge retains partial signal from every predictor, matching a dense true model)* | — |
| T2 | transfer | short-answer | 2.12 | Contrast ridge's Gaussian-prior MAP interpretation with LASSO's Laplace-prior interpretation explicitly. | both are MAP estimates under a prior on β, but a Gaussian prior's density falls off smoothly and never places disproportionate mass exactly at zero, producing ridge's smooth shrinkage; a Laplace (double-exponential) prior has a sharp peak exactly at zero, placing enough prior mass there that the MAP estimate is pulled all the way to exactly zero for small coefficients — the same corner-vs-smooth-region geometry from the frequentist L1-vs-L2 picture, restated in Bayesian terms *(required: names both priors and connects the Laplace prior's peak at zero to LASSO's sparsity)* | — |
| T3 | transfer | short-answer | 2.32 | How does ridge's eigenvalue-shift fix for a singular XᵀX connect to how PCA/near-singular covariance matrices are handled elsewhere? | just as ridge adds λI to shift every eigenvalue of XᵀX up by λ (guaranteeing positive definiteness, per `ridge-regression`'s A1), a near-singular sample covariance matrix in PCA or other multivariate methods is sometimes similarly regularized by adding a small multiple of the identity before inverting it — the same eigenvalue-shift trick for restoring invertibility appears wherever a naturally near-singular matrix needs to be inverted reliably *(required: names the shared eigenvalue-shift-for-invertibility technique across both settings)* | —

*Coverage: 16/6/5/3 — 30 items, 0.02…2.32.*

---

## Elastic Net (`elastic-net`)
*Prereq: LASSO, Ridge Regression · ancestors 32 · b₀ = 1.25*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.25 | State elastic net's penalty. | λ[α·Σ\|βⱼ\| + (1−α)·Σβⱼ²] — a weighted mix of LASSO and ridge, α controlling the blend (α=1 pure LASSO, α=0 pure ridge) | — |
| R2 | recall | mcq | 0.55 | Elastic net is designed to combine: | LASSO's variable-selection ability and ridge's stability with correlated predictors | claims it combines "neither LASSO's nor ridge's properties" — missing the entire point of the hybrid design → `elastic-net` |
| A1 | apply | short-answer | 1.05 | Why does elastic net tend to select or shrink correlated predictors together, rather than arbitrarily picking one (LASSO's tendency)? | the ridge component of the penalty discourages the wild, arbitrary differentiation between correlated predictors that pure LASSO can exhibit, producing a "grouping effect" instead *(required: names the ridge component as the specific mechanism resolving LASSO's T1 limitation)* | — |
| E1 | explain | short-answer | 1.75 | Why does elastic net cost more to properly tune than LASSO or ridge alone? | it introduces an additional hyperparameter α alongside λ, requiring a 2-dimensional cross-validation grid search rather than a 1-dimensional one, per `hyperparameters`' earlier discussion of tuning cost — a genuine practical cost of its added flexibility *(required: names the 2D grid search specifically)* | — |
| T1 | transfer | short-answer | 2.25 | What general lesson about regularization does elastic net illustrate? | different penalty shapes (L1's diamond vs. L2's circle, per `vector-norm`'s geometric argument) produce different shrinkage behaviors (sparse vs. smooth), and a method designer can combine penalty shapes to trade off between their respective strengths — a principle extending well beyond just LASSO/ridge to other regularization schemes in modern ML *(required: names the general combine-penalty-shapes principle, not just elastic net's specific formula)* | — |
| R3 | recall | short-answer | 0.05 | Fill in the blank: elastic net's penalty is λ[α·Σ\|βⱼ\| + (1−α)·Σ___]. | βⱼ² | — |
| R4 | recall | short-answer | 0.1 | What does the parameter α control in elastic net? | the blend between the L1 (LASSO) and L2 (ridge) penalties — α=1 is pure LASSO, α=0 is pure ridge, and values between mix the two | — |
| R5 | recall | short-answer | 0.15 | What special case does elastic net reduce to when α=1? | pure LASSO | — |
| R6 | recall | short-answer | 0.2 | What special case does elastic net reduce to when α=0? | pure ridge regression | — |
| R7 | recall | mcq | 0.3 | How many hyperparameters does elastic net have to tune, compared to LASSO or ridge alone? | two (λ and α), versus one (λ) for LASSO or ridge alone | picks "one, the same as LASSO or ridge" — elastic net's whole design adds the extra α blending parameter on top of λ → `elastic-net` |
| R8 | recall | mcq | 0.35 | True/false: elastic net can produce exact zero coefficients, like LASSO. | True — as long as α>0, elastic net retains some L1 component, which can still shrink coefficients exactly to zero | claims "elastic net, like pure ridge, never sets any coefficient exactly to zero" — that's only true in elastic net's α=0 special case; for α>0 it retains LASSO-like sparsity → `elastic-net` |
| R9 | recall | short-answer | 0.4 | Fill in the blank: elastic net's "grouping effect" refers to ___. | its tendency to select or shrink groups of correlated predictors together, rather than arbitrarily picking just one of them (as pure LASSO tends to) | — |
| R10 | recall | short-answer | 0.5 | What is the valid range of values for α? | α ∈ [0, 1] | — |
| R11 | recall | mcq | 0.6 | True/false: elastic net was designed specifically to address a known limitation of LASSO with correlated predictors. | True — LASSO's tendency to arbitrarily select one predictor from a correlated group (and its inability to select more than n predictors when p>n) directly motivated elastic net's design | claims "elastic net was designed as a pure computational speedup for LASSO, unrelated to its correlated-predictor behavior" — its motivating purpose is specifically LASSO's correlated-predictor and p>n limitations → `elastic-net` |
| R12 | recall | short-answer | 0.65 | Which value of α gives an exactly 50/50 mix of the L1 and L2 penalties? | α = 0.5 | — |
| R13 | recall | short-answer | 0.7 | State the practical downside of elastic net's extra hyperparameter. | it requires a more expensive two-dimensional cross-validation search (over both λ and α) rather than a one-dimensional search, per `hyperparameters`' tuning-cost discussion | — |
| R14 | recall | short-answer | 0.75 | Fill in the blank: cross-validation for elastic net requires searching over a ___-dimensional grid. | two (one dimension for λ, one for α) | — |
| R15 | recall | mcq | 0.85 | True/false: elastic net always performs strictly better than both pure LASSO and pure ridge. | False — its relative performance depends on the data; if the truth is purely sparse, pure LASSO can do as well or better, and if predictors are uncorrelated and all relevant, pure ridge can do as well or better | claims "elastic net's hybrid design guarantees it dominates both special cases on every dataset" — no regularization method is universally best; the right choice depends on the true underlying structure → `elastic-net` |
| R16 | recall | short-answer | 0.95 | What kind of predictor situation does elastic net's grouping effect specifically help with? | groups of highly correlated predictors, where pure LASSO would arbitrarily pick one and zero out the rest | — |
| A2 | apply | numeric | 0.8 | α=0.5, λ=2, β=(1, −2). Compute elastic net's penalty value. `[verified: 8]` | L1 sum=\|1\|+\|−2\|=3; L2 sum=1²+(−2)²=5; penalty=λ[α·3+(1−α)·5]=2[0.5(3)+0.5(5)]=2(4)=8 | — |
| A3 | apply | short-answer | 0.9 | With α=0.8, would the fitted solution behave more like pure LASSO or pure ridge, qualitatively? | more like LASSO, since α=0.8 weights the L1 (sparsity-inducing) component much more heavily than the L2 component; the model would likely still zero out some coefficients but retain a mild grouping effect for correlated predictors from the smaller ridge contribution *(required: names that a high α leans the behavior toward LASSO-like sparsity)* | — |
| A4 | apply | short-answer | 1.0 | Describe the grid-search process for jointly selecting λ and α via cross-validation. | define a grid of candidate α values (e.g. 0, 0.1, ..., 1) and, for each α, a range of candidate λ values; fit the model and compute cross-validated error for every (α, λ) pair on the grid, then select the pair minimizing that cross-validated error *(required: names the two-dimensional nature of the search explicitly)* | — |
| A5 | apply | numeric | 1.15 | Verify that elastic net's penalty formula reduces to ridge's when α=0, for β=(1, −2), λ=2. `[verified: 10]` | penalty = λ[0·3 + 1·5] = 2(5) = 10, matching λΣβⱼ² = 2(1+4) = 10, ridge's ordinary penalty | — |
| A6 | apply | short-answer | 1.25 | Genomics data has p=20,000 candidate genes, with many biologically co-expressed (correlated) genes, and n=200 samples. Why might elastic net suit this better than pure LASSO? | with p≫n and many correlated gene groups, pure LASSO could select at most n≈200 nonzero genes and would tend to arbitrarily pick one gene per co-expressed group, discarding biologically meaningful correlated partners; elastic net's ridge component lets it select more than n genes and keep correlated, co-expressed genes together as a group, better matching the biological reality *(required: names both the p>n selection-limit issue and the grouping benefit for co-expressed genes)* | — |
| E2 | explain | short-answer | 1.4 | Why does combining L1 and L2 penalties give elastic net a grouping effect that pure LASSO lacks? | the added L2 term contributes a strictly convex component to the objective, which (unlike LASSO's purely L1 objective, which can have multiple, non-unique solutions among correlated predictors) makes the optimal solution unique and specifically favors distributing the effect across correlated predictors rather than concentrating it arbitrarily in one, per `elastic-net`'s own A1 mechanism *(required: names strict convexity from the L2 term as the source of uniqueness/grouping)* | — |
| E3 | explain | short-answer | 1.5 | Why is the extra tuning dimension α a real practical cost, not just a minor inconvenience? | a two-dimensional cross-validation grid search over (λ, α) requires fitting the model at every combination of grid points, which scales multiplicatively rather than additively with the number of candidate values for each parameter — doubling the grid resolution for one dimension multiplies, not adds to, the total number of fits required *(required: names the multiplicative, not additive, scaling of a 2D grid search)* | — |
| E4 | explain | short-answer | 1.7 | Why can elastic net select more than n predictors when p>n, unlike pure LASSO? | pure LASSO's L1-only objective is known to select at most n nonzero coefficients when p>n, a mathematical limitation of its optimization structure; the added strictly-convex L2 component in elastic net removes this cap, allowing more than n predictors to have nonzero coefficients simultaneously *(required: names LASSO's specific n-predictor selection cap and that the L2 term removes it)* | — |
| E5 | explain | short-answer | 1.9 | Why isn't elastic net simply "the best of both worlds" with no tradeoffs? | it inherits complexity, not just strengths, from both parents — an extra hyperparameter to tune, more computational cost for the 2D search, and a solution that is a genuine compromise rather than optimal for either the purely-sparse or purely-dense special case; when the truth is exactly sparse or exactly dense, the pure method matching that structure can outperform the compromise *(required: names that it's a compromise, potentially suboptimal versus the pure method matching the true structure)* | — |
| T2 | transfer | short-answer | 2.15 | How does elastic net's convex combination of two penalties reflect a broader pattern in multi-objective optimization? | weighting two competing objectives (here, sparsity-inducing L1 and smooth-shrinkage L2) by a mixing parameter and optimizing their weighted sum is a standard technique across multi-objective optimization generally — elastic net's α is a specific instance of the general "scalarization" approach to trading off multiple design goals within a single optimization problem *(required: names the general scalarization/weighted-combination technique explicitly)* | — |
| T3 | transfer | short-answer | 2.35 | How is elastic net's hybrid design structurally similar to how `ensemble-methods` combine multiple models? | both operate on the principle that combining two approaches, each with a distinct weakness, can produce a result more robust than either alone — elastic net combines LASSO's sparsity with ridge's stability under correlation, just as an ensemble combines several models' predictions to average out any single model's individual blind spots; both trade a little of each single approach's purity for broader robustness *(required: names the shared combine-to-offset-individual-weaknesses principle)* | —

*Coverage: 16/6/5/3 — 30 items, 0.05…2.35.*

---

## LOESS Smoothing (`loess-smoothing`)
*Prereq: Simple Linear Regression · ancestors 20 · b₀ = 1.02*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | Describe LOESS. | fit a separate, simple regression locally around each point of interest, using nearby data weighted by distance — a nonparametric alternative to one global regression line | — |
| R2 | recall | mcq | 0.32 | LOESS is "nonparametric" because it: | doesn't assume a single, fixed global functional form — the fitted curve's shape adapts locally to whatever pattern the data shows | claims it "has no parameters whatsoever" — LOESS still has local regression coefficients at each fitted point, just no single global form → `loess-smoothing` |
| A1 | apply | short-answer | 0.82 | Why does LOESS need a bandwidth/span hyperparameter, and how does it connect to `rbf`'s bandwidth σ? | it controls how many nearby points to include and how much weight distant points get; exactly parallel to RBF's σ from the ML sweep — small bandwidth gives a wiggly, low-bias/high-variance fit (like small-K KNN or small-σ RBF), large bandwidth gives a smooth, high-bias/low-variance fit *(required: the explicit RBF-bandwidth parallel)* | — |
| E1 | explain | short-answer | 1.52 | Why can LOESS capture nonlinear relationships a single global linear regression cannot? | fitting different local lines/curves at different points lets the overall fitted curve bend in ways no single global equation could represent, without needing to specify the exact nonlinear form in advance *(required)* | — |
| T1 | transfer | short-answer | 2.02 | Why is LOESS primarily used for visualization rather than precise, inferential predictions? | it has no simple, fixed set of coefficients to interpret the way a linear regression's β's can be, and its predictions, while often visually insightful, come with less straightforward theoretical guarantees (standard errors, confidence intervals) than parametric regression *(required: contrasts the lack of interpretable coefficients with parametric regression's clean inference)* | — |

*Coverage: 5 items, 0.02…2.02.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| AIC and BIC's penalty growth rates conflated | `aic-bic` |
| greedy stepwise methods assumed to find the global optimum | `forward-backward-stepwise-selection` |
| regularization's use case (many/correlated predictors) inverted | `regularization` |
| LASSO and ridge's shrinkage behaviors (sparse vs. smooth) swapped | `lasso`, `ridge-regression` |
| elastic net's motivation (combining two behaviors) missed | `elastic-net` |
| LOESS mistaken for having no local parameters at all | `loess-smoothing` |

**Cluster total: 35 items across 7 concepts.** All numeric/derivation claims verified, including a
concrete confirmation that the ridge shift repairs a genuinely singular X'X (det 0 → det 15.01).
