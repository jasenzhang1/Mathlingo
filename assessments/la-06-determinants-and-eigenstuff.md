# Linear Algebra Cluster 6 — Determinants & Eigenstuff

Determinant, Determinant Properties, Eigenvalues and Eigenvectors, Diagonalization, Eigendecomposition,
LU Decomposition, Symmetric Matrices (7 concepts). Same format as
[Cluster 1](la-01-vectors-and-operations.md).

`eigendecomposition`'s rotation-matrix counterexample (no real eigenvectors) is deliberately left as a
cliffhanger for `symmetric-matrices`' T1 to resolve — the two items are meant to be read together.

---

## Determinant (`determinant`)
*Prereq: Matrices · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.70 | State det for a 2×2 matrix, and its geometric meaning. | det([[a,b],[c,d]])=ad−bc; geometrically, the signed area (or volume, in higher dimensions) of the parallelepiped formed by the matrix's columns | — |
| R2 | recall | mcq | −0.45 | det(A)=0 means: | A is singular (not invertible) | picks "A is invertible" — backwards → `determinant` |
| R3 | recall | short-answer | −0.30 | State the determinant formula for a 3×3 matrix via cofactor expansion along the first row. | det=a(ei−fh)−b(di−fg)+c(dh−eg) for [[a,b,c],[d,e,f],[g,h,i]] *(required: correct alternating cofactor signs +,−,+)* | — |
| R4 | recall | mcq | −0.10 | For a triangular (upper or lower) matrix, the determinant equals: | the product of the diagonal entries | multiplies all entries of the matrix rather than just the diagonal → `determinant` |
| A1 | apply | numeric | 0.10 | Compute det([[3,1],[2,4]]). `[verified: 10]` | 3·4−1·2=10 | — |
| A2 | apply | numeric | 0.25 | Compute det([[1,2,3],[0,4,5],[0,0,6]]). `[verified: 24]` | triangular matrix, so det = product of diagonal = 1·4·6=24 | — |
| A3 | apply | numeric | 0.35 | Compute det([[2,0],[5,−3]]). `[verified: −6]` | 2·(−3)−0·5=−6 | — |
| A4 | apply | numeric | 0.45 | Compute det([[1,2,1],[2,1,0],[1,1,2]]) by cofactor expansion along the first row. `[verified: −5]` | 1(1·2−0·1)−2(2·2−0·1)+1(2·1−1·1)=2−8+1=−5 | — |
| A5 | apply | short-answer | 0.55 | Two rows of a 3×3 matrix are swapped and the resulting determinant is −7. What was the original determinant? | row swap flips the sign, so original det = 7 | — |
| A6 | apply | numeric | 0.65 | A 4×4 matrix has det=6. One row is multiplied by 3 (only that row). Find the new determinant. `[verified: 18]` | scaling a single row by c scales det by that same factor c (not cⁿ, since only one row changes): 3·6=18 | confuses single-row scaling with whole-matrix scaling cⁿ → `determinant-properties` |
| A7 | apply | numeric | 0.75 | Compute det via row reduction: A=[[2,4],[1,3]]. Reduce to triangular form and take the product of pivots. `[verified: 2]` | R2−(1/2)R1 gives [[2,4],[0,1]]; product of pivots = 2·1=2 (matches direct computation 2·3−4·1=2) | — |
| E1 | explain | short-answer | 0.80 | Explain geometrically why det(A)=0 means the columns are linearly dependent. | the determinant measures the volume of the parallelepiped spanned by the columns; if the columns are dependent, that shape collapses into a lower-dimensional (flat) region, which has zero volume in the original dimension *(required: the "collapses to lower dimension" mechanism)* | — |
| E2 | explain | short-answer | 0.95 | Explain why swapping two rows of a matrix flips the sign of its determinant, using the geometric (volume) interpretation. | the magnitude of the parallelepiped's volume is unchanged by relabeling which edge is which, but swapping two edge vectors reverses the orientation (handedness) of the frame they define, and the determinant's sign tracks exactly that orientation *(required: ties sign specifically to orientation, not magnitude)* | — |
| E3 | explain | short-answer | 1.05 | Explain why adding a multiple of one row to another row does not change the determinant. | geometrically this is a shear — it slides one face of the parallelepiped parallel to itself without changing its base or its height perpendicular to that face, so the enclosed volume is preserved exactly *(required: names the shear/volume-preservation mechanism)* | — |
| E4 | explain | short-answer | 1.15 | Why is cofactor expansion equivalent to row reduction for computing a determinant, despite looking completely different? | both are different bookkeeping schemes for the same multilinear, alternating function of the rows that is uniquely pinned down (up to normalization) by linearity in each row, the sign flip under a swap, and det(I)=1 — any algorithm satisfying these three properties must return the same value *(required: the uniqueness-of-the-alternating-multilinear-function argument)* | — |
| E5 | explain | short-answer | 1.25 | Explain why a matrix with an all-zero row must have determinant zero, using cofactor expansion. | expanding along that all-zero row, every cofactor term is multiplied by a zero entry, so the entire sum collapses to zero regardless of the other rows' values *(required: the direct expansion argument, not just citing the "zero row implies singular" fact)* | — |
| T1 | transfer | short-answer | 1.30 | Why does det(A)=0 for any matrix with a repeated row or column? | a repeated row/column makes the corresponding edges of the parallelepiped point along the same direction rather than genuinely spanning distinct directions, forcing the shape to be flat (zero volume) — a direct instance of E1's collapse argument *(required: connects to the geometric collapse, not just "dependent columns give zero determinant")* | — |
| T2 | transfer | short-answer | 1.55 | In a change-of-variables integral, ∫f(x)dx=∫f(g(u))\|det Jg(u)\|du. Explain why the Jacobian determinant, not the Jacobian matrix itself, appears here. | the integral needs a single scalar volume-scaling factor at each point to rescale the infinitesimal volume element du into dx; the Jacobian matrix describes the full local linear map, but only its determinant captures how much volume that map stretches or shrinks *(required: connects to the local-linearization volume-scaling role of det)* | — |
| T3 | transfer | short-answer | 1.85 | A 3×3 covariance matrix Σ has det(Σ)=0. What does this imply about the data geometrically, and why would a multivariate normal density formula break down? | det(Σ)=0 means the data's spread collapses into a lower-dimensional subspace (e.g. all points lie on a plane) — exactly the "zero-volume" collapse from E1; the normal density's normalizing constant involves 1/√det(Σ), which is undefined once there is no genuine volume to normalize over *(required: connects the geometric collapse to the density formula breaking)* | — |
| T4 | transfer | short-answer | 2.20 | In numerical linear algebra, why do libraries compute log(det A) via the sum of logs of the pivots from LU decomposition, rather than multiplying pivots directly and then taking a log? | multiplying many pivots directly can overflow or underflow floating-point range long before the log is taken (e.g. a determinant near 2¹⁰⁰⁰), whereas summing the logs of the pivots individually keeps every intermediate value in a safe numerical range *(required: the overflow/underflow reasoning, not just "it's more efficient")* | — |

*Coverage: 20 items, −0.70…2.20.*

---

## Determinant Properties (`determinant-properties`)
*Prereq: Determinant · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.60 | State two key determinant properties. | det(AB)=det(A)det(B); det(Aᵀ)=det(A) | — |
| R2 | recall | mcq | −0.35 | For an n×n matrix A, det(cA) equals: | cⁿ·det(A) | picks c·det(A), forgetting the exponent n → `determinant-properties` |
| R3 | recall | short-answer | −0.20 | State the effect of swapping two rows of a matrix on its determinant, and the effect of adding a multiple of one row to another. | swapping two rows multiplies det by −1; adding a multiple of one row to another leaves det unchanged | — |
| R4 | recall | mcq | −0.05 | If A has an all-zero row or column, det(A) equals: | 0 | assumes a zero row only reduces the determinant's magnitude rather than forcing it exactly to zero → `determinant-properties` |
| A1 | apply | numeric | 0.20 | A is 3×3 with det(A)=5. Find det(2A). `[verified: 40]` | 2³·5=40 | — |
| A2 | apply | numeric | 0.35 | A is 4×4 with det(A)=2. Find det(3A). `[verified: 162]` | 3⁴·2=81·2=162 | — |
| A3 | apply | numeric | 0.45 | A and B are both 3×3 with det(A)=4, det(B)=−2. Find det(AB) and det(BA). `[verified: −8 for both]` | det(AB)=det(A)det(B)=4·(−2)=−8; det(BA)=det(B)det(A)=−8 also, since scalar multiplication commutes even though matrix multiplication does not | — |
| A4 | apply | numeric | 0.55 | A is invertible with det(A)=−5. Find det(A⁻¹) and det(Aᵀ). `[verified: −1/5 and −5]` | det(A⁻¹)=1/det(A)=−1/5; det(Aᵀ)=det(A)=−5 | — |
| A5 | apply | numeric | 0.65 | A 3×3 matrix has one row scaled by 4 (only that row); its determinant goes from 5 to what value? `[verified: 20]` | scaling a single row by a scalar c scales det by c (not cⁿ, which only applies when every row is scaled): 4·5=20 | confuses single-row scaling with the whole-matrix cⁿ rule → `determinant-properties` |
| A6 | apply | short-answer | 0.75 | det(A)=0 for a 3×3 matrix A. What must det(A²) and det(A¹⁰⁰) equal? | det(Aᵏ)=det(A)ᵏ=0ᵏ=0 for any positive power k, so both det(A²) and det(A¹⁰⁰) are 0 | — |
| A7 | apply | numeric | 0.85 | A and B are 2×2 with det(A)=3, det(B)=5. Find det(A²B⁻¹). `[verified: 9/5]` | det(A²B⁻¹)=det(A)²·det(B)⁻¹=9·(1/5)=9/5 | — |
| E1 | explain | short-answer | 0.90 | Explain geometrically why det(AB)=det(A)det(B). | applying B then A scales volume first by det(B), then by det(A); the combined scaling factor of the composed transformation is the product of the two individual scaling factors — det(AB)=det(A)det(B) is exactly this composition rule *(required: the sequential-scaling argument, not just citing the algebraic identity)* | — |
| E2 | explain | short-answer | 1.00 | Explain why det(Aᵀ)=det(A) even though transposing swaps rows and columns. | cofactor expansion along a row of A gives the same sum of terms as cofactor expansion along the corresponding column of Aᵀ, because transposing just relabels which index is "row" and which is "column" without changing which products of entries appear or their signs *(required: names the row/column symmetry of the expansion, not just "it's a known identity")* | — |
| E3 | explain | short-answer | 1.10 | Explain why the product rule det(AB)=det(A)det(B) implies det(A)≠0 for every invertible matrix A. | if A is invertible, A·A⁻¹=I, so det(A)det(A⁻¹)=det(I)=1; if det(A) were 0, the left side would be 0, which can never equal 1, so det(A) must be nonzero *(required: the explicit contradiction argument)* | — |
| E4 | explain | short-answer | 1.20 | Why does scaling a single row of an n×n matrix by c scale the whole determinant by c, while scaling the entire matrix by c scales the determinant by cⁿ? | the determinant is linear in each row separately (holding the others fixed), so scaling just one row scales the whole (multilinear) expression by that one factor c; scaling every row by c applies that same factor n separate times, once per row, multiplying to cⁿ *(required: the multilinearity-in-each-row argument)* | — |
| E5 | explain | short-answer | 1.30 | Explain why det(AB)=det(BA) even when AB≠BA as matrices. | det(AB) and det(BA) are both just det(A)·det(B), a product of two scalars, and scalar multiplication commutes regardless of whether the underlying matrix multiplication does; the matrices AB and BA can genuinely differ as matrices while still sharing the same determinant *(required: distinguishes matrix equality from scalar-determinant equality)* | order of matrix multiplication assumed to matter for the determinant the same way it matters for the matrix product itself → `determinant-properties` |
| T1 | transfer | short-answer | 1.40 | Show det(A⁻¹)=1/det(A) follows immediately from det(AB)=det(A)det(B). | A·A⁻¹=I, so det(A)det(A⁻¹)=det(I)=1; dividing gives det(A⁻¹)=1/det(A) directly *(required: the explicit A·A⁻¹=I substitution)* | — |
| T2 | transfer | short-answer | 1.65 | A linear transformation is the composition of a rotation (det=1), a reflection (det=−1), and a scaling by 3 in each of 3 dimensions (det=3³=27). Find the determinant of the composed transformation and state what it means about orientation and volume. | multiply: det=1·(−1)·27=−27; the negative sign means orientation is flipped overall, and the magnitude 27 means volumes are scaled up by a factor of 27 | — |
| T3 | transfer | short-answer | 1.95 | In a change-of-variables from Cartesian to spherical coordinates, the Jacobian determinant is r²sin(φ). Using det(cA)=cⁿdet(A) intuition, explain why this factor depends on position (r,φ) rather than being a single global constant like it would be for a linear map. | the coordinate transformation is nonlinear, so its local scaling factor varies from point to point rather than being fixed as it would for a genuinely linear map cA; det(cA)=cⁿdet(A) only applies to a *uniform* global scaling, whereas the spherical map stretches space differently depending on how far out (r) and how tilted (φ) you are *(required: explains why nonlinearity breaks the constant-scaling picture)* | — |
| T4 | transfer | short-answer | 2.30 | Two economists model a country's economy with transformation matrices A (year 1 policy) and B (year 2 policy) applied in sequence as BA. A colleague claims "since det(AB)=det(BA), it doesn't matter which policy is applied first." Is the colleague right or wrong, and why? | the colleague is wrong about what matters — det(AB)=det(BA) only says the two orderings produce the *same overall volume-scaling factor*; it says nothing about whether the resulting transformations act identically on any individual vector, and in general AB≠BA as matrices, so the order of applying the actual policies changes the outcome even though the aggregate "scaling" statistic happens to coincide *(required: distinguishes the scalar coincidence from genuine matrix non-commutativity)* | equates equal determinants with equal matrices/transformations → `determinant-properties` |

*Coverage: 20 items, −0.60…2.30.*

---

## Eigenvalues and Eigenvectors (`eigenvalues-eigenvectors`)
*Prereq: Invertible Matrices, Determinant · ancestors 12 · b₀ = 0.78*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.22 | Define eigenvalue and eigenvector. | Av=λv for a nonzero vector v and scalar λ | — |
| R2 | recall | mcq | 0.08 | Geometrically, an eigenvector of A: | stays on the same line under A, only scaled | claims it "rotates to a completely different direction" — the opposite of the defining property → `eigenvalues-eigenvectors` |
| R3 | recall | short-answer | 0.25 | State the relationship between the eigenvalues of A and det(A), and between the eigenvalues of A and trace(A). | det(A) equals the product of all eigenvalues (with multiplicity); trace(A) equals the sum of all eigenvalues (with multiplicity) | — |
| R4 | recall | mcq | 0.40 | The eigenvalues of a triangular matrix are: | exactly its diagonal entries | computes eigenvalues via a full characteristic-polynomial expansion, missing that triangular structure makes them immediately readable → `eigenvalues-eigenvectors` |
| A1 | apply | short-answer | 0.58 | A=[[2,0],[0,3]]. Find its eigenvalues and eigenvectors by inspection. | eigenvalues 2, 3 with eigenvectors (1,0), (0,1) respectively — for a diagonal matrix, the eigenvalues *are* the diagonal entries | — |
| A2 | apply | numeric | 0.70 | A=[[4,1],[2,3]]. Find its eigenvalues by solving det(A−λI)=0. `[verified: λ=5,2]` | (4−λ)(3−λ)−2=λ²−7λ+10=0 ⟹ λ=5 or λ=2 | — |
| A3 | apply | numeric | 0.80 | Find the eigenvalues of A=[[5,0,0],[0,−1,0],[0,0,3]]. `[verified: 5,−1,3]` | diagonal matrix, so the eigenvalues are exactly the diagonal entries: 5, −1, 3 | — |
| A4 | apply | numeric | 0.90 | Find the eigenvalues of A=[[1,2],[2,1]] by solving det(A−λI)=0. `[verified: λ=3,−1]` | (1−λ)²−4=0 ⟹ λ²−2λ−3=0 ⟹ (λ−3)(λ+1)=0 ⟹ λ=3 or λ=−1 | — |
| A5 | apply | numeric | 1.00 | A=[[1,2],[2,1]] has eigenvalues 3 and −1 (from A4). Find the eigenvector for λ=3. `[verified: (1,1)]` | (A−3I)v=0 ⟹ [[−2,2],[2,−2]]v=0 ⟹ v=(1,1) (or any scalar multiple) | — |
| A6 | apply | numeric | 1.10 | Use trace and determinant shortcuts to find the eigenvalues of A=[[6,2],[2,3]] without expanding the full characteristic polynomial. `[verified: 7,2]` | trace=9=λ₁+λ₂, det=18−4=14=λ₁λ₂; solving λ²−9λ+14=0 gives λ=7,2, matching both sum and product checks | — |
| A7 | apply | numeric | 1.20 | A is 3×3 with characteristic polynomial λ³−6λ²+11λ−6. Factor it to find all eigenvalues. `[verified: 1,2,3]` | factors as (λ−1)(λ−2)(λ−3), so the eigenvalues are 1, 2, 3 (check: sum=6=trace, product=6=det) | — |
| E1 | explain | short-answer | 1.28 | Why does solving det(A−λI)=0 find the eigenvalues? | Av=λv rearranges to (A−λI)v=0; for this to have a *nonzero* solution v, A−λI must be singular (by `invertible-matrices`' equivalence), which happens exactly when det(A−λI)=0 *(required: the singular-matrix connection, not just "that's the formula")* | — |
| E2 | explain | short-answer | 1.40 | Explain why an eigenvalue of 0 means the matrix is singular, using the definition Av=λv directly. | if λ=0, the eigenvalue equation reads Av=0 for some nonzero vector v, which says v is in the null space of A; a nonzero null space means A is not injective and therefore not invertible, i.e. singular *(required: the direct Av=0 with v≠0 argument)* | — |
| E3 | explain | short-answer | 1.50 | Explain why complex eigenvalues of a real matrix must always come in conjugate pairs. | the characteristic polynomial det(A−λI) has entirely real coefficients (since A is real), and for any polynomial with real coefficients, complex roots occur in conjugate pairs by the conjugate root theorem *(required: ties to the real-coefficient property of the characteristic polynomial)* | — |
| E4 | explain | short-answer | 1.60 | Explain why the number of distinct eigenvalues of an n×n matrix can be less than n, but never more than n. | eigenvalues are roots of the characteristic polynomial, which has degree exactly n, so by the fundamental theorem of algebra it has at most n distinct roots (n counting multiplicity); some roots may repeat, giving fewer than n distinct values, but a degree-n polynomial can never have more than n roots *(required: ties to the degree of the characteristic polynomial)* | — |
| E5 | explain | short-answer | 1.70 | Why does an eigenvector remaining "on the same line" under A directly imply that the entire line spanned by v is mapped to itself? | A(cv)=c(Av)=c(λv)=λ(cv) for any scalar c, by linearity of A; so every point on the line through v — not just v itself — is scaled by λ and stays on that same line, meaning the whole 1-dimensional subspace is invariant under A *(required: the explicit linearity argument extending from v to the whole line)* | — |
| T1 | transfer | short-answer | 1.78 | In a Markov chain, the stationary distribution is exactly an eigenvector of the transition matrix with eigenvalue 1. Using Av=λv, explain why "the distribution doesn't change from one step to the next" translates precisely into this statement. | "unchanged after one step" means (transition matrix)·(distribution) = (the same distribution) — exactly Av=v, which is Av=λv with λ=1 *(required: the direct substitution λ=1)* | — |
| T2 | transfer | short-answer | 2.00 | In PCA, the covariance matrix's largest eigenvalue is 8 with eigenvector (1,0); the smallest eigenvalue is 2 with eigenvector (0,1). If you must summarize the data's spread in one number, why does the largest eigenvalue (not the trace or determinant) tell you the "most informative direction"? | each eigenvalue is the variance of the data specifically along its own eigenvector direction; the largest eigenvalue identifies the single direction along which the data spreads out the most, which is exactly what captures the most information when projecting to fewer dimensions — the trace only gives the total variance summed over all directions, losing which direction that variance lives in *(required: distinguishes direction-specific variance from the aggregate trace)* | — |
| T3 | transfer | short-answer | 2.25 | A PageRank-style transition matrix has largest eigenvalue 1 (the stationary distribution) and second-largest eigenvalue 0.85. Explain why a value of 0.85 close to 1 means the random walk mixes (converges to the stationary distribution) slowly. | after k steps, the deviation from the stationary distribution decays like (second eigenvalue)ᵏ; since 0.85ᵏ shrinks to zero much more slowly than a smaller eigenvalue would (e.g. 0.1ᵏ), a second eigenvalue close to 1 means many steps are needed before the distribution is close to stationary *(required: the (second eigenvalue)^k decay-rate argument)* | — |
| T4 | transfer | short-answer | 2.55 | In a system of differential equations ẋ=Ax modeling a spring-mass system, A has eigenvalues −2±3i. Explain what the real part (−2) and imaginary part (3) each tell you about the physical behavior, without solving the system explicitly. | the real part (−2, negative) governs exponential decay of the amplitude over time — since it's negative, oscillations die out (stable); the imaginary part (3) sets the angular frequency of the oscillation itself — the system oscillates while decaying, like a damped spring rather than a pure exponential or a pure undamped oscillator *(required: separately interprets real part as decay/growth and imaginary part as oscillation frequency)* | — |

*Coverage: 20 items, −0.22…2.55.*

---

## Diagonalization (`diagonalization`)
*Prereq: Eigenvalues and Eigenvectors · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.18 | State what it means for A to be diagonalizable. | A=PDP⁻¹, where D is diagonal (the eigenvalues) and P's columns are the corresponding eigenvectors | — |
| R2 | recall | mcq | 0.12 | A matrix is diagonalizable if and only if: | it has n linearly independent eigenvectors | claims "it's invertible" — invertibility and diagonalizability are unrelated properties (a singular matrix can be diagonalizable; an invertible one might not be) → `diagonalization` |
| R3 | recall | short-answer | 0.28 | State the relationship between algebraic multiplicity and geometric multiplicity that determines whether a matrix is diagonalizable. | A is diagonalizable if and only if, for every eigenvalue, its geometric multiplicity (dimension of the eigenspace) equals its algebraic multiplicity (multiplicity as a root of the characteristic polynomial) | — |
| R4 | recall | mcq | 0.45 | A matrix that is not diagonalizable is called: | defective | — |
| A1 | apply | short-answer | 0.62 | From `eigenvalues-eigenvectors`'s A2 (λ=5,2 for A=[[4,1],[2,3]]), are these eigenvalues distinct? What does that guarantee? | yes, distinct; distinct eigenvalues always give linearly independent eigenvectors, so A is guaranteed diagonalizable | — |
| A2 | apply | short-answer | 0.75 | A=[[1,1],[0,1]] has a repeated eigenvalue λ=1 (algebraic multiplicity 2). Find the eigenspace for λ=1 and determine whether A is diagonalizable. | (A−I)v=0 gives [[0,1],[0,0]]v=0, so v=(1,0) up to scalar — only a 1-dimensional eigenspace (geometric multiplicity 1) against algebraic multiplicity 2, so A is *not* diagonalizable | — |
| A3 | apply | numeric | 0.88 | A=[[3,0],[0,3]] has eigenvalue 3 with algebraic multiplicity 2. Is A diagonalizable? `[verified: yes, trivially]` | A is already diagonal, so its eigenspace for λ=3 is all of ℝ² (geometric multiplicity 2 = algebraic multiplicity 2); A is diagonalizable — in fact A=IAI⁻¹ trivially | assumes any repeated eigenvalue automatically blocks diagonalizability → `diagonalization` |
| A4 | apply | short-answer | 1.00 | A=[[2,1],[0,2]] has eigenvalue 2 with algebraic multiplicity 2. Find dim of its eigenspace and state whether A is diagonalizable. `[verified: dim 1, not diagonalizable]` | (A−2I)v=0 gives [[0,1],[0,0]]v=0, so eigenspace is span{(1,0)}, dimension 1 < algebraic multiplicity 2, so A is not diagonalizable | — |
| A5 | apply | numeric | 1.10 | A=[[4,1],[2,3]] has eigenvalues 5 and 2 (from `eigenvalues-eigenvectors` A2) with eigenvectors (1,1) and (1,−2). Write out P and D such that A=PDP⁻¹. | P=[[1,1],[1,−2]], D=diag(5,2); A=PDP⁻¹ with these matrices (columns of P are the eigenvectors in the same order as D's diagonal) | mismatches the order of eigenvalues in D with the corresponding eigenvector columns in P → `diagonalization` |
| A6 | apply | numeric | 1.18 | A=[[0,1],[−2,3]] has eigenvalues 1 and 2, with eigenvectors (1,1) for λ=1 and (1,2) for λ=2. Compute A² using diagonalization rather than direct matrix multiplication. `[verified: A²=[[−2,3],[−6,7]]]` | A²=PD²P⁻¹ with D²=diag(1,4); direct multiplication check: A²=[[0,1],[−2,3]]²=[[−2,3],[−6,7]], matching | — |
| A7 | apply | short-answer | 1.25 | A 3×3 matrix has eigenvalues 2, 2, 5 with a 2-dimensional eigenspace for λ=2 and a 1-dimensional eigenspace for λ=5. Is A diagonalizable? Why or why not? | yes — every eigenvalue's geometric multiplicity (2 for λ=2, 1 for λ=5) matches its algebraic multiplicity (2 and 1 respectively), so three independent eigenvectors exist in total and A is diagonalizable | — |
| E1 | explain | short-answer | 1.32 | Why do distinct eigenvalues always give linearly independent eigenvectors? | if eigenvectors for two different eigenvalues were dependent (one a scalar multiple of the other), applying A would force both eigenvalue equations to hold on the same vector simultaneously, which is only possible if the eigenvalues actually match — contradicting distinctness *(required: the argument, not just the stated fact)* | — |
| E2 | explain | short-answer | 1.45 | Explain why P must be invertible for A=PDP⁻¹ to make sense, connecting this to the columns of P being eigenvectors. | P⁻¹ existing requires P's columns to be linearly independent; since P's columns are exactly the eigenvectors of A, this is precisely the requirement that A has n linearly independent eigenvectors — the defining condition for diagonalizability *(required: connects invertibility of P to linear independence of the eigenvectors)* | — |
| E3 | explain | short-answer | 1.55 | Explain why a defective matrix (like [[1,1],[0,1]]) cannot be written as PDP⁻¹ for any invertible P. | if A=PDP⁻¹ held, the columns of P would have to be n linearly independent eigenvectors of A; a defective matrix by definition lacks enough independent eigenvectors (its eigenspace dimension falls short of the eigenvalue's multiplicity), so no such invertible P can exist *(required: the contradiction with P needing n independent columns)* | — |
| E4 | explain | short-answer | 1.65 | Explain geometrically what A=PDP⁻¹ does to a vector x, describing the roles of P, D, and P⁻¹ in sequence. | P⁻¹ first re-expresses x in the eigenbasis (its coordinates relative to the eigenvectors); D then simply scales each of those coordinates independently by its own eigenvalue; P finally converts the scaled coordinates back into the original coordinate system *(required: all three steps named in the correct order)* | — |
| E5 | explain | short-answer | 1.75 | Explain why two diagonalizable matrices with exactly the same eigenvalues are not necessarily equal. | A=PDP⁻¹ depends on the choice of eigenvector basis P as well as the eigenvalues D; two matrices can share identical D but have entirely different eigenvector directions (different P), producing different matrices A that merely happen to scale by the same amounts along different sets of directions *(required: distinguishes sharing D from sharing the full factorization)* | — |
| T1 | transfer | short-answer | 1.82 | Why is computing A¹⁰⁰ dramatically easier once A is diagonalized? | A¹⁰⁰=PD¹⁰⁰P⁻¹, and D¹⁰⁰ (diagonal) is trivial — just raise each diagonal entry to the 100th power — compared to multiplying A by itself 100 times directly, which is far more expensive *(required: the explicit PD¹⁰⁰P⁻¹ computation)* | — |
| T2 | transfer | short-answer | 2.05 | A population model xₙ₊₁=Axₙ has A diagonalizable with eigenvalues 1.2 and 0.8. Explain, using diagonalization, what happens to the population's long-term composition as n→∞. | xₙ=PDⁿP⁻¹x₀, and Dⁿ=diag(1.2ⁿ, 0.8ⁿ); as n→∞, 1.2ⁿ grows without bound while 0.8ⁿ shrinks to 0, so the population's composition converges to be dominated entirely by the eigenvector direction for λ=1.2, growing at rate 1.2 per step *(required: the dominant-eigenvalue-wins argument via Dⁿ)* | — |
| T3 | transfer | short-answer | 2.30 | Explain why diagonalizing the coefficient matrix A is the standard method for solving a linear system of differential equations ẋ=Ax, reducing it to a set of independent scalar ODEs. | writing x=Py transforms ẋ=Ax into Pẏ=APy, i.e. ẏ=P⁻¹APy=Dy since D=P⁻¹AP; because D is diagonal, this is n completely uncoupled scalar equations ẏᵢ=λᵢyᵢ, each solved independently as yᵢ(t)=yᵢ(0)eλᵢt, and then x=Py converts back *(required: the substitution x=Py decoupling argument)* | — |
| T4 | transfer | short-answer | 2.60 | A financial risk model diagonalizes a transition matrix A but two of its eigenvalues turn out to be complex conjugates a±bi. Can A still be diagonalized, and what does that reveal about the underlying dynamics? | yes — A can still be diagonalized, just over the complex numbers rather than the reals, with P and D containing complex entries; the presence of complex eigenvalues reveals oscillatory (rotational) behavior in the dynamics rather than pure exponential growth or decay along fixed real directions *(required: both that diagonalization can proceed over ℂ and that complex eigenvalues signal oscillation)* | assumes a matrix with complex eigenvalues cannot be diagonalized at all → `diagonalization` |

*Coverage: 20 items, −0.18…2.60.*

---

## Eigendecomposition (`eigendecomposition`)
*Prereq: Diagonalization · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | How does eigendecomposition relate to diagonalization? | the same factored form A=PDP⁻¹, now emphasized as a *decomposition* of A into simpler pieces rather than a claim about A's diagonalizability | — |
| R2 | recall | mcq | 0.15 | Not every matrix has a real eigendecomposition, because: | some matrices lack enough independent real eigenvectors, or have complex eigenvalues | claims "eigendecomposition always exists for every square matrix" over the reals → `eigendecomposition` |
| R3 | recall | short-answer | 0.30 | State the eigendecomposition formula and identify what each factor represents. | A=PDP⁻¹, where D is diagonal with the eigenvalues, and the columns of P are the corresponding eigenvectors — the same factorization as diagonalization, viewed as breaking A into its constituent "scale along each eigendirection" pieces | — |
| R4 | recall | mcq | 0.48 | The eigendecomposition of A is unique: | no — eigenvectors can be rescaled by any nonzero scalar, and eigenvalue/eigenvector pairs can be reordered, giving multiple valid (P,D) pairs for the same A | assumes P and D are the single, uniquely determined factorization of A → `eigendecomposition` |
| A1 | apply | short-answer | 0.65 | A=[[0,−1],[1,0]] (a 90° rotation). Does it have real eigenvalues? `[verified: complex, discriminant negative]` | no — a 90° rotation sends *every* real vector off its own line, so no real eigenvector can exist; the eigenvalues are complex (λ=±i) | assumes every real square matrix must have real eigenvalues → `eigendecomposition` |
| A2 | apply | numeric | 0.78 | A=[[5,4],[1,2]] has eigenvalues 6 and 1 with eigenvectors (4,1) and (−1,1). Verify PDP⁻¹ reconstructs A by checking a single entry. `[verified: entry (1,1)=5]` | computing PDP⁻¹'s (1,1) entry using P=[[4,−1],[1,1]], D=diag(6,1), P⁻¹=(1/5)[[1,1],[−1,4]] gives (1/5)(24+1)=5, matching A's (1,1) entry | — |
| A3 | apply | numeric | 0.90 | A=[[0,2],[−1,0]] (a scaled rotation-like map). Compute the discriminant of its characteristic polynomial to determine whether its eigenvalues are real. `[verified: discriminant=−8, so complex]` | char poly λ²+2=0 (trace=0, det=2), discriminant=trace²−4det=0−8=−8<0, so eigenvalues are complex (λ=±i√2) and no real eigendecomposition exists | — |
| A4 | apply | numeric | 1.00 | A=[[2,0],[0,3]] (already diagonal). Write its eigendecomposition explicitly. `[verified: P=I]` | A=IAI⁻¹ works trivially, since a diagonal matrix's own standard basis vectors are its eigenvectors and its diagonal entries are its eigenvalues — P can simply be the identity | — |
| A5 | apply | short-answer | 1.10 | A 3×3 matrix has eigenvalues 4, 4, −2 with a full 2-dimensional eigenspace for λ=4. Does it have a real eigendecomposition? | yes — since the geometric multiplicity of λ=4 (2) matches its algebraic multiplicity (2), and λ=−2 contributes its own eigenvector, three independent real eigenvectors exist, so a real eigendecomposition A=PDP⁻¹ is possible | — |
| A6 | apply | numeric | 1.20 | A=[[1,2],[0,3]] (upper triangular). State its eigenvalues immediately, then find the eigenvector for the smaller one. `[verified: eigenvalues 1,3; eigenvector (1,0) for λ=1]` | triangular matrix, so eigenvalues are the diagonal entries 1 and 3 directly; for λ=1, (A−I)v=0 gives [[0,2],[0,2]]v=0, so v=(1,0) | — |
| A7 | apply | numeric | 1.28 | Given eigendecomposition A=PDP⁻¹ with D=diag(2,−3), compute det(A) using only the eigenvalues. `[verified: −6]` | det(A)=det(P)det(D)det(P⁻¹)=det(D) (since det(P)det(P⁻¹)=1)=2·(−3)=−6 | — |
| E1 | explain | short-answer | 1.35 | Explain geometrically why a rotation (other than 0° or 180°) can have no real eigenvectors. | an eigenvector must stay on its own line under the transformation (`eigenvalues-eigenvectors`'s R2); a genuine rotation, by definition, moves *every* vector off its original line, so the defining property of an eigenvector can never be satisfied by any real vector *(required: the direct callback to the "stays on the same line" definition)* | — |
| E2 | explain | short-answer | 1.47 | Explain why eigendecomposition requires n linearly independent eigenvectors to exist, even though every n×n matrix has n eigenvalues (counting multiplicity and complex ones). | having n eigenvalues only guarantees n roots of the characteristic polynomial; it says nothing about whether the corresponding eigenspaces are large enough to supply n independent eigenvectors — a defective matrix has fewer independent eigenvectors than eigenvalues, so P cannot be built as an invertible matrix *(required: distinguishes "having eigenvalues" from "having enough independent eigenvectors")* | — |
| E3 | explain | short-answer | 1.57 | Explain why a matrix can have real entries and real eigenvalues, yet still fail to have a real eigendecomposition. | having real eigenvalues alone doesn't guarantee enough independent real eigenvectors — a defective matrix like [[1,1],[0,1]] has a real repeated eigenvalue (1) but only a one-dimensional eigenspace, so no full set of n independent real eigenvectors exists even though all eigenvalues themselves are real *(required: separates the reality of eigenvalues from the independence of eigenvectors)* | — |
| E4 | explain | short-answer | 1.67 | Explain why eigendecomposition makes computing matrix functions like eᴬ or √A tractable, in a way that is not obvious from the original matrix. | writing A=PDP⁻¹, any function f applied to A becomes f(A)=Pf(D)P⁻¹, and f(D) is trivial to compute since D is diagonal — you just apply f to each eigenvalue independently; without diagonalizing, f(A) would require an infinite series or numerically unstable approximation directly on the full matrix *(required: the f(D)=diagonal-entrywise-application mechanism)* | — |
| E5 | explain | short-answer | 1.77 | Explain why the eigendecomposition of a matrix with a repeated eigenvalue is less "rigid" than one with all distinct eigenvalues — specifically, why the choice of P is less unique. | for a repeated eigenvalue with a multidimensional eigenspace, *any* basis of that eigenspace works as the corresponding columns of P, not just one specific pair of vectors — so there are infinitely many valid choices of eigenvectors (any orthogonal rotation within the eigenspace), unlike a distinct eigenvalue whose eigenspace is a single line with an essentially fixed direction *(required: connects the repeated-eigenvalue eigenspace dimension to non-uniqueness of P)* | — |
| T1 | transfer | short-answer | 1.85 | The Spectral Theorem guarantees symmetric matrices always have real eigenvalues and orthogonal eigenvectors. What special property of symmetric matrices rules out the rotation-like failure seen here? | symmetric matrices cannot rotate vectors the way an antisymmetric or generic matrix can — their transformation only stretches along a set of mutually orthogonal directions, never introducing the kind of directional "twist" that produces complex eigenvalues *(required: names symmetry as ruling out the rotational/twisting behavior specifically)* — resolved fully in `symmetric-matrices`' T1 | — |
| T2 | transfer | short-answer | 2.08 | In quantum mechanics, an observable is represented by a symmetric (Hermitian, in the real case) matrix. Explain why the guaranteed real eigendecomposition of such matrices is physically essential, not just a mathematical convenience. | the eigenvalues of an observable's matrix represent the only possible measured values of a physical quantity (like energy or momentum); if the matrix could have complex eigenvalues, a measurement could in principle yield an unmeasurable, non-real number, which has no physical meaning — the symmetric (Hermitian) structure is exactly what guarantees measured outcomes are always real *(required: connects real eigenvalues to physically meaningful measurement outcomes)* | — |
| T3 | transfer | short-answer | 2.32 | A recommendation-system matrix factorization approximates a user-item ratings matrix using its top few eigenvalue/eigenvector pairs, discarding the rest. Explain why keeping the eigenvectors with the *largest-magnitude* eigenvalues (rather than smallest) preserves the most information. | in A=PDP⁻¹, each eigenvalue's magnitude measures how much that eigendirection contributes to A's overall action; directions with large-magnitude eigenvalues dominate A's effect on any vector, while directions with tiny eigenvalues contribute almost nothing, so truncating to the largest-magnitude terms keeps the components that matter most while discarding near-negligible ones *(required: ties eigenvalue magnitude to contribution/importance)* | — |
| T4 | transfer | short-answer | 2.62 | Two engineers debate: one says "since a rotation matrix has no real eigendecomposition, it has no meaningful eigenvalues at all." Correct this claim using what you know about complex eigenvalues. | the engineer is wrong — a rotation matrix does have eigenvalues, they are just complex (e^{±iθ} for a rotation by angle θ) rather than real; these complex eigenvalues are meaningful and encode the rotation angle exactly, they simply don't correspond to any real eigenvector because no real direction survives the rotation unrotated *(required: corrects the claim by naming the complex eigenvalues explicitly, not just saying "it has some eigenvalues")* | conflates "no real eigendecomposition" with "no eigenvalues exist at all" → `eigendecomposition` |

*Coverage: 20 items, −0.15…2.62.*

---

## LU Decomposition (`lu-decomposition`)
*Prereq: Invertible Matrices · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State what LU decomposition is. | A=LU, with L lower triangular (typically with 1's on the diagonal) and U upper triangular — Gaussian elimination's steps recorded as a matrix factorization | — |
| R2 | recall | mcq | 0.04 | LU decomposition is primarily used for: | solving Ax=b efficiently for *many* different b with the same A | picks "finding eigenvalues," an unrelated use → `lu-decomposition` |
| R3 | recall | short-answer | 0.20 | State the computational cost (in big-O terms) of computing an LU decomposition versus solving a triangular system. | computing LU costs O(n³) via Gaussian elimination; solving each triangular system (forward or back substitution) costs O(n²) | — |
| R4 | recall | mcq | 0.38 | Partial pivoting during LU decomposition means: | rows are reordered at each step to bring the largest available entry into the pivot position, for numerical stability | assumes pivoting is only needed to *avoid* a literal zero pivot, missing that it also improves numerical stability with small (but nonzero) pivots → `lu-decomposition` |
| A1 | apply | short-answer | 0.54 | Why does having A=LU make solving Ax=b for many different b much cheaper than repeating full Gaussian elimination each time? | computing L and U is an O(n³) cost paid once; each new b then only needs two cheap O(n²) triangular solves (forward substitution with L, then back substitution with U), instead of a fresh O(n³) elimination *(required: the explicit cost comparison)* | — |
| A2 | apply | numeric | 0.66 | Find the LU decomposition of A=[[4,3],[6,3]]. `[verified: L=[[1,0],[1.5,1]], U=[[4,3],[0,−1.5]]]` | multiplier m=6/4=1.5; R2−1.5R1 gives [0, 3−1.5·3]=[0,−1.5]; so L=[[1,0],[1.5,1]], U=[[4,3],[0,−1.5]] | — |
| A3 | apply | numeric | 0.78 | Using A=LU with L=[[1,0],[2,1]], U=[[3,1],[0,2]], solve Ly=b for b=(5,16) via forward substitution. `[verified: y=(5,6)]` | y₁=5 directly; 2(5)+y₂=16 ⟹ y₂=6 | — |
| A4 | apply | numeric | 0.88 | Continuing A3 (U=[[3,1],[0,2]], y=(5,6)), solve Ux=y via back substitution to find x. `[verified: x=(2/3,3)]` | 2x₂=6 ⟹ x₂=3; 3x₁+x₂=5 ⟹ 3x₁=2 ⟹ x₁=2/3; so x=(2/3, 3) | — |
| A5 | apply | numeric | 0.98 | A 3×3 matrix has LU decomposition with U having diagonal entries 2, −3, 5. Using det(A)=det(L)det(U) and det(L)=1 (unit lower triangular), find det(A). `[verified: −30]` | det(L)=1 since L has 1's on the diagonal; det(U)=2·(−3)·5=−30; det(A)=1·(−30)=−30 | — |
| A6 | apply | short-answer | 1.08 | A=[[1,2],[2,4]] is singular (det=0). What happens when you attempt Gaussian elimination to find L and U? | eliminating the first column gives R2−2R1=(0,0), producing a zero row and hence a zero pivot in U's second diagonal entry — LU decomposition still formally succeeds (L,U exist) but U is itself singular, correctly reflecting that A is singular | assumes a singular matrix always makes LU decomposition impossible to even carry out → `lu-decomposition` |
| A7 | apply | numeric | 1.16 | A=[[2,1,1],[4,3,3],[8,7,9]]. Find the first elimination multiplier and the updated second row after one elimination step. `[verified: multiplier 2, new row (0,1,1)]` | multiplier m₂₁=4/2=2; new R2=R2−2R1=(4−4,3−2,3−2)=(0,1,1) | — |
| E1 | explain | short-answer | 1.24 | Connect LU decomposition directly to Gaussian elimination. | L records the elimination multipliers used to zero out entries below each pivot; U is the row-echelon result of applying those elimination steps to A *(required: both halves named)* | — |
| E2 | explain | short-answer | 1.36 | Explain why the L matrix in A=LU is unit lower triangular (1's on its diagonal) by convention, rather than the diagonal entries being arbitrary. | fixing L's diagonal to 1 removes an otherwise-free scaling ambiguity between L and U — without this convention, you could rescale a row of L and the corresponding row of U by reciprocal factors and get infinitely many valid (L,U) pairs for the same A; the unit-diagonal convention pins down a unique decomposition (when pivoting is not needed) *(required: names the scaling-ambiguity resolution)* | — |
| E3 | explain | short-answer | 1.46 | Explain why LU decomposition is preferred over computing A⁻¹ directly when solving Ax=b for a single b. | computing A⁻¹ explicitly costs about the same O(n³) as LU, but then multiplying A⁻¹b still costs O(n²) — no cheaper than the triangular solves after LU — while computing A⁻¹ is numerically less stable and wastes effort computing entries of the inverse that are never needed if you only want x *(required: the stability/no-cost-benefit-of-inverse argument, not just "LU is standard")* | — |
| E4 | explain | short-answer | 1.56 | Explain, using the elimination-multiplier interpretation, why L captures exactly the operations needed to *undo* the row reduction that produced U. | each entry of L below the diagonal is the multiplier used to zero out a specific entry during elimination; multiplying by L reintroduces those same eliminated components back into the reduced matrix U in reverse, so LU literally reconstructs A by replaying the elimination process backward *(required: the reconstruction/undo framing)* | — |
| E5 | explain | short-answer | 1.66 | Explain why LU decomposition with partial pivoting is written as PA=LU rather than A=LU, and why this doesn't undermine the method's usefulness for repeated solves. | pivoting permutes rows of A during elimination, so the L and U that come out factor the *permuted* matrix PA, not A itself, requiring the permutation matrix P to record which swaps happened; this still works for repeated solves because Ax=b becomes PAx=Pb, i.e. LUx=Pb, and P is cheap to apply (just reorders b) so the two-triangular-solve efficiency is preserved *(required: explains both why P appears and why efficiency survives)* | — |
| T1 | transfer | short-answer | 1.74 | Why might LU decomposition fail (require row swaps) even for an invertible matrix? Give a tiny example. | A=[[0,1],[1,0]] has a zero in the pivot position, forcing a row swap before elimination can proceed at all, even though A is perfectly invertible (det=−1≠0) — this is exactly why real numerical software uses partial pivoting *(required: the concrete zero-pivot example)* | — |
| T2 | transfer | short-answer | 1.98 | A structural engineering simulation solves Kx=f (stiffness matrix K, load vector f) for hundreds of different load scenarios f on the same structure K. Explain why computing K's LU decomposition once, up front, is the standard approach rather than re-solving from scratch each time. | K's LU factorization costs O(n³) once; each new load scenario then only requires two O(n²) triangular solves rather than a fresh O(n³) elimination, so amortizing the expensive factorization across hundreds of load cases yields massive overall savings — exactly the "many different b" scenario LU is built for *(required: connects to the amortized-cost structure across repeated solves)* | — |
| T3 | transfer | short-answer | 2.22 | In a numerical solver, a matrix is technically invertible (det≠0) but has a pivot that becomes extremely small (though nonzero) during elimination without pivoting. Explain the practical numerical danger, and how partial pivoting addresses it. | dividing by a very small pivot amplifies rounding errors already present in the matrix, causing the computed L and U to diverge badly from the true factorization even though no pivot is exactly zero; partial pivoting swaps in the largest available entry as the pivot at each step, keeping the multipliers bounded and preventing this error amplification *(required: the error-amplification-from-small-divisor mechanism, not just "avoids zero pivots")* | assumes pivoting is only relevant when a pivot is exactly zero → `lu-decomposition` |
| T4 | transfer | short-answer | 2.52 | Cholesky decomposition (A=LLᵀ) is a special, cheaper variant used only for symmetric positive-definite matrices, compared to general LU. Explain what extra structure symmetric positive-definiteness provides that lets Cholesky avoid needing a separate U matrix or pivoting. | for a symmetric positive-definite matrix, the elimination process is automatically numerically stable without needing to search for larger pivots (all pivots that arise are guaranteed positive), and the symmetry means U turns out to just be a scaled version of Lᵀ, so storing both L and U separately is redundant — Cholesky exploits both facts to halve the work and storage compared to general LU *(required: names both the guaranteed-positive-pivots stability and the U=scaled-Lᵀ redundancy)* | — |

*Coverage: 20 items, −0.26…2.52.*

---

## Symmetric Matrices (`symmetric-matrices`)
*Prereq: Matrices · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.70 | Define a symmetric matrix. | A=Aᵀ, i.e. Aᵢⱼ=Aⱼᵢ for all i,j | — |
| R2 | recall | mcq | −0.45 | Which is a genuinely guaranteed property of symmetric matrices? | their eigenvalues are always real, and eigenvectors for distinct eigenvalues are always orthogonal | claims "they're always invertible" — false; a symmetric matrix can easily be singular → `symmetric-matrices` |
| R3 | recall | short-answer | −0.30 | State the Spectral Theorem for symmetric matrices. | every real symmetric matrix has all real eigenvalues and can be diagonalized by an orthogonal matrix, A=QΛQᵀ with Qᵀ=Q⁻¹ | — |
| R4 | recall | mcq | −0.10 | For a symmetric matrix, eigenvectors corresponding to distinct eigenvalues are: | always orthogonal to each other | assumes eigenvectors of a symmetric matrix are only orthogonal if the matrix is also positive-definite → `symmetric-matrices` |
| A1 | apply | short-answer | 0.10 | Is A=[[1,2],[2,3]] symmetric? Is B=[[1,2],[3,4]]? `[verified]` | A: yes (a₁₂=a₂₁=2); B: no (a₁₂=2≠3=a₂₁) | — |
| A2 | apply | short-answer | 0.25 | Is A=[[0,3,1],[3,0,2],[1,2,0]] symmetric? Justify by checking entries. `[verified]` | yes — a₁₂=a₂₁=3, a₁₃=a₃₁=1, a₂₃=a₃₂=2, so A=Aᵀ | — |
| A3 | apply | numeric | 0.35 | Find the eigenvalues of the symmetric matrix A=[[4,2],[2,1]]. `[verified: λ=5,0]` | (4−λ)(1−λ)−4=λ²−5λ=0 ⟹ λ(λ−5)=0 ⟹ λ=0 or λ=5 | — |
| A4 | apply | short-answer | 0.45 | For A=[[4,2],[2,1]] (eigenvalues 0 and 5, from A3), find the eigenvector for λ=0 and verify it's orthogonal to the eigenvector for λ=5, which is (2,1). | (A−0I)v=0 gives [[4,2],[2,1]]v=0, so v=(1,−2); checking orthogonality: (1,−2)·(2,1)=2−2=0 ✓, confirming the guaranteed orthogonality | — |
| A5 | apply | numeric | 0.55 | A=[[3,0,0],[0,3,0],[0,0,−1]] is symmetric (diagonal). What are its eigenvalues, and is it positive definite? `[verified: eigenvalues 3,3,−1; not positive definite]` | eigenvalues are the diagonal entries 3, 3, −1 (already diagonal); since one eigenvalue (−1) is negative, A is not positive definite | — |
| A6 | apply | short-answer | 0.65 | Is AᵀA symmetric for any matrix A? Compute for A=[[1,0],[1,1]] to check. `[verified: AᵀA=[[2,1],[1,1]] is symmetric]` | AᵀA is always symmetric since (AᵀA)ᵀ=Aᵀ(Aᵀ)ᵀ=AᵀA; for A=[[1,0],[1,1]], AᵀA=[[2,1],[1,1]], which is indeed symmetric (off-diagonal entries both 1) | — |
| A7 | apply | numeric | 0.75 | The Hessian of f(x,y)=x²+3xy+y² at any point is a constant symmetric matrix. Write it down and find its eigenvalues. `[verified: eigenvalues 5,−1]` | Hessian=[[2,3],[3,2]]; (2−λ)²−9=0 ⟹ λ²−4λ−5=0 ⟹ (λ−5)(λ+1)=0 ⟹ λ=5,−1 | — |
| E1 | explain | short-answer | 0.80 | Why is a covariance matrix always symmetric? | Cov(Xᵢ,Xⱼ)=Cov(Xⱼ,Xᵢ) directly from the definition of covariance — the (i,j) and (j,i) entries are always equal by construction *(required: the direct algebraic reason, not just "covariance matrices happen to be symmetric")* — a genuine cross-domain link to the probability sweep's `covariance` | — |
| E2 | explain | short-answer | 0.95 | Explain why the mixed second partial derivatives being equal (Clairaut's theorem) is exactly why a Hessian matrix is always symmetric. | Clairaut's theorem states ∂²f/∂xᵢ∂xⱼ=∂²f/∂xⱼ∂xᵢ for sufficiently smooth f; the Hessian's (i,j) entry is exactly ∂²f/∂xᵢ∂xⱼ, so this equality of mixed partials is precisely the statement that the Hessian's (i,j) and (j,i) entries always match *(required: the direct link between Clairaut's theorem and the symmetric-entries condition)* | — |
| E3 | explain | short-answer | 1.05 | Give the two-line proof that a real symmetric matrix has only real eigenvalues, and identify exactly where symmetry (A=Aᵀ) is used. | if Av=λv, taking the conjugate transpose and using Aᵀ=A (and A real) gives v̄ᵀA=λ̄v̄ᵀ; multiplying both sides by v gives λ‖v‖²=λ̄‖v‖², and since ‖v‖²>0 this forces λ=λ̄, i.e. λ is real *(required: identifies symmetry is used precisely to justify v̄ᵀA=λ̄v̄ᵀ)* | — |
| E4 | explain | short-answer | 1.15 | Explain why the eigenvectors of a symmetric matrix with a repeated eigenvalue can still be chosen orthogonal, even though the direct orthogonality proof (used for distinct eigenvalues) doesn't apply. | for a repeated eigenvalue, the eigenspace itself is multidimensional, and any basis chosen for that eigenspace can be replaced by an orthogonal basis using Gram-Schmidt within that subspace; symmetric matrices guarantee the eigenspace has the full dimension needed (matching the algebraic multiplicity), so there is always room to orthogonalize within it *(required: connects to Gram-Schmidt within the multidimensional eigenspace, not just re-asserting the fact)* | — |
| E5 | explain | short-answer | 1.25 | Explain why the quadratic form xᵀAx for a symmetric A becomes a sum of independent squared terms in the eigenbasis, and why this requires symmetry specifically. | writing x=Qy in the orthonormal eigenbasis, xᵀAx=yᵀQᵀAQy=yᵀΛy=Σλᵢyᵢ², since Q diagonalizes A orthogonally; this relies on symmetry to guarantee both that Q is orthogonal (Qᵀ=Q⁻¹, so QᵀAQ=Λ exactly) and that Λ's entries are real, which a non-symmetric matrix's eigendecomposition cannot generally guarantee *(required: names the QᵀAQ=Λ step and why it needs orthogonal Q)* | — |
| T1 | transfer | short-answer | 1.30 | Why is the fact that symmetric matrices always have real eigenvalues essential for covariance matrices to make sense as "measuring spread in various directions" — resolving `eigendecomposition`'s rotation cliffhanger? | a covariance matrix's eigenvalues are variances along its principal directions; a complex eigenvalue would have no meaning as a variance (variances are real, nonnegative quantities); symmetry is exactly what rules out the rotation-like behavior that produced complex eigenvalues in `eigendecomposition`'s counterexample, guaranteeing every covariance matrix's "spread" is measurable along real, orthogonal directions *(required: explicitly resolves the earlier cliffhanger)* | — |
| T2 | transfer | short-answer | 1.55 | An optimization algorithm checks the Hessian's eigenvalues to classify a critical point. At a point, the Hessian [[2,1],[1,2]] has eigenvalues 3 and 1 (both positive). Explain what this guarantees about the critical point, using the quadratic-form connection from E5. | since both eigenvalues are positive, the quadratic form Σλᵢyᵢ² is positive for any nonzero displacement y in the eigenbasis, meaning f increases in every direction away from the critical point — this guarantees the point is a local minimum, and the guarantee is only this clean because the Hessian's symmetry ensures real eigenvalues and a genuine eigenbasis to decompose into *(required: connects positive eigenvalues to the positive-quadratic-form/local-min argument)* | — |
| T3 | transfer | short-answer | 1.85 | PCA on a dataset gives a covariance matrix with eigenvalues 10, 3, 0.01 for three orthogonal principal directions. Explain, using symmetric matrices' guarantee of orthogonal eigenvectors, why projecting onto the top two eigenvectors captures "independent" pieces of variance rather than double-counting information. | because the covariance matrix is symmetric, its eigenvectors for distinct eigenvalues are exactly orthogonal, so the directions of maximum variance (10) and second-most variance (3) are guaranteed to be perpendicular — the variance captured along one is by construction uncorrelated with the variance along the other, meaning no part of the data's spread is being measured twice across the two directions *(required: ties orthogonality specifically to non-overlapping/uncorrelated variance)* | — |
| T4 | transfer | short-answer | 2.20 | A student claims: "Since a rotation matrix has no real eigenvectors, and a symmetric matrix always has real eigenvectors, no symmetric matrix can ever describe a genuine rotation." Evaluate this claim, connecting to `eigendecomposition`'s rotation cliffhanger. | the claim is correct in substance — a nontrivial rotation (other than 0° or 180°) is never symmetric, precisely because it moves every real vector off its own line (as shown in `eigendecomposition`'s counterexample), which is incompatible with the guaranteed real, orthogonal eigenvectors a symmetric matrix must have; symmetric matrices can only stretch along fixed orthogonal directions, never introduce the directional twist that defines a genuine rotation *(required: explicitly confirms the claim and ties it back to the twist-vs-stretch distinction)* | — |

*Coverage: 20 items, −0.70…2.20.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| det(A)=0 read as "invertible" rather than "singular" | `determinant` |
| cⁿ scaling of det(cA) computed as c | `determinant-properties` |
| eigenvector definition reversed (rotates vs. stays on its line) | `eigenvalues-eigenvectors` |
| diagonalizability conflated with invertibility | `diagonalization` |
| every real matrix assumed to have real eigenvalues | `eigendecomposition` |
| LU decomposition assumed always pivot-free | `lu-decomposition` |
| symmetric matrices assumed always invertible | `symmetric-matrices` |

**Cluster total: 140 items across 7 concepts (20 each).** All numeric claims verified, including the
complex eigenvalues of the rotation-matrix counterexample (confirmed via a negative discriminant),
which `symmetric-matrices`' T1 is built to resolve.
