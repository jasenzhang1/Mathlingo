# Linear Algebra Cluster 8 — SVD & Applications

Singular Value Decomposition, Uniqueness of SVD, SVD and the Four Fundamental Subspaces,
Moore-Penrose Inverse, Eckart-Young Theorem, Principal Component Analysis (Matrix Edition) (6 concepts).
Same format as [Cluster 1](la-01-vectors-and-operations.md). This is the final linear-algebra cluster
— `pca-matrix-edition`'s E1 is deliberately the capstone item of the entire 54-concept domain, tying
together Rayleigh quotients, the Spectral Theorem, and Eckart-Young into one coherent method.

---

## Singular Value Decomposition (`svd`)
*Prereq: Positive Definite Matrices, Eigendecomposition · ancestors 17 · b₀ = 0.95*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.05 | State the SVD, and the one thing that distinguishes it from eigendecomposition. | A=UΣVᵀ for *any* m×n matrix A (U, V orthogonal, Σ diagonal with nonnegative singular values) — it exists for every matrix, even rectangular or singular ones, unlike eigendecomposition | — |
| R2 | recall | mcq | 0.25 | The singular values relate to the eigenvalues of AᵀA by: | singular values are the *square roots* of AᵀA's eigenvalues | claims "they ARE the eigenvalues of AᵀA" directly, skipping the square root → `svd` |
| R3 | recall | short-answer | 0.45 | State the shapes of U, Σ, and V in the *full* SVD of an m×n matrix A. | U is m×m orthogonal, V is n×n orthogonal, Σ is m×n and diagonal (rectangular diagonal — entries only on the leading diagonal, zero elsewhere) | assumes Σ must be square like an eigenvalue diagonal matrix → `svd` |
| R4 | recall | short-answer | 0.60 | What is the *reduced* (thin) SVD, and why does it give the same product as the full SVD? | keep only the first r=rank(A) columns of U and V, paired with the r nonzero singular values; the discarded columns of U and V multiply only zero rows/columns of Σ in the full version, contributing nothing to the product | — |
| A1 | apply | numeric | 0.75 | AᵀA has eigenvalues 9 and 4. Find A's singular values. `[verified: 3, 2]` | √9=3, √4=2 | — |
| A2 | apply | numeric | 0.85 | AᵀA has eigenvalues 16 and 1. Find A's singular values. `[verified: 4, 1]` | √16=4, √1=1 | — |
| A3 | apply | short-answer | 0.95 | A = diag(3, −4). Find its singular values, and explain why neither equals −4 despite that being a diagonal entry of A. `[verified: singular values 4 and 3]` | singular values are the magnitudes 3 and 4 of the diagonal entries (sorted 4, 3); they are square roots of AᵀA's eigenvalues, hence always nonnegative — the negative sign is absorbed into a sign flip of the corresponding column of U instead *(required: names where the sign goes)* | expects a singular value to literally equal a negative diagonal entry of A → `svd` |
| A4 | apply | numeric | 1.05 | A = [[3,0],[4,0]]. Compute A's singular values and the first left singular vector u₁. `[verified: σ=5,0; u₁=(0.6,0.8)]` | AᵀA=[[25,0],[0,0]], eigenvalues 25,0 → singular values 5,0; v₁=(1,0)ᵀ, so u₁=Av₁/σ₁=(3,4)ᵀ/5=(0.6,0.8)ᵀ | — |
| A5 | apply | numeric | 1.15 | A has singular values 6 and 2. Find the spectral norm ‖A‖₂ and the Frobenius norm ‖A‖_F. `[verified: 6, √40≈6.32]` | ‖A‖₂=σ₁=6 (largest singular value, not an average); ‖A‖_F=√(σ₁²+σ₂²)=√(36+4)=√40≈6.32 | computes ‖A‖₂ as the sum or average of singular values rather than the max → `svd` |
| A6 | apply | numeric | 1.25 | A 3×3 invertible matrix has singular values 5, 2, 1. Find the magnitude of det(A). `[verified: 10]` | the magnitude of det(A) equals the product of the singular values, since det(U) and det(V) each have magnitude 1: magnitude of det(A) = 5·2·1=10 | — |
| A7 | apply | numeric | 1.35 | A has singular values 100 and 0.01. Compute its condition number and comment on numerical stability. `[verified: 10000]` | κ(A)=σ_max/σ_min=100/0.01=10{,}000; such a large condition number means small perturbations (or rounding error) can be hugely amplified in any solution involving A | — |
| E1 | explain | short-answer | 1.45 | Why does SVD exist for every matrix, while eigendecomposition can fail (per `eigendecomposition`'s rotation counterexample)? | AᵀA is *always* symmetric and positive semi-definite, regardless of what A itself looks like, guaranteeing real, nonnegative eigenvalues via the Spectral Theorem — real square roots of those eigenvalues always exist, so singular values always exist even when A's own eigenvalues might not be real *(required: the AᵀA-is-always-symmetric-PSD argument)* | — |
| E2 | explain | short-answer | 1.57 | If A is symmetric positive definite, show that its SVD and its eigendecomposition coincide. | for symmetric PSD A, the Spectral Theorem gives A=QΛQᵀ with Q orthogonal and Λ≥0 diagonal — this already has exactly the SVD's form, so U=V=Q and Σ=Λ *(required: identifies U=V=Q)* | — |
| E3 | explain | short-answer | 1.67 | Why can the left and right singular vectors of A be entirely unrelated directions, whereas eigenvectors of a diagonalizable matrix live in the same space the matrix acts on? | SVD uses *two* independent orthonormal bases — one for the domain (V), one for the codomain (U) — because A maps between (possibly different) spaces Rⁿ→Rᵐ; eigendecomposition requires a single basis to serve as both input and output coordinates, only possible for a square operator mapping a space to itself *(required: names the domain/codomain distinction)* | — |
| E4 | explain | short-answer | 1.77 | Prove that the largest singular value σ₁ equals the maximum of ‖Ax‖ over all unit vectors x. | writing x in the basis {vᵢ}, Ax=Σσᵢuᵢ(vᵢᵀx) so ‖Ax‖²=Σσᵢ²(vᵢᵀx)²≤σ₁²Σ(vᵢᵀx)²=σ₁²‖x‖²=σ₁², with equality at x=v₁ *(required: shows the inequality and the equality case)* | — |
| E5 | explain | short-answer | 1.87 | Why does computing the SVD by explicitly forming AᵀA and diagonalizing it lose accuracy compared to computing the SVD directly from A? | forming AᵀA squares the condition number (κ(AᵀA)=κ(A)²), so small singular values that were distinguishable in A become indistinguishable from rounding error after squaring; direct methods (e.g. Golub–Kahan bidiagonalization) work on A itself and avoid this amplification *(required: names the condition-number-squaring mechanism)* | — |
| T1 | transfer | short-answer | 1.95 | Why is SVD considered the most universal matrix decomposition in applied linear algebra and data science? | real data matrices are almost always rectangular (more samples than features or vice versa) and SVD requires no squareness at all, unlike eigendecomposition which additionally can fail even on square matrices *(required: both the rectangularity and the eigendecomposition-can-fail points)* | — |
| T2 | transfer | short-answer | 2.17 | In a collaborative-filtering recommender system, the (typically incomplete) user-item rating matrix R is modeled via a low-rank factorization resembling R≈UΣVᵀ. Why must this rely on SVD-style machinery rather than eigendecomposition of R itself? | R is m users by n items and essentially never square, so R itself has no eigendecomposition at all; SVD requires no such assumption and applies regardless of R's shape, which is why latent-factor recommender models are built around it (after a separate completion step fills in missing ratings) | — |
| T3 | transfer | short-answer | 2.37 | In quantum information theory, the Schmidt decomposition writes any bipartite pure state ψ as a sum Σᵢσᵢ(uᵢ⊗vᵢ) of orthonormal basis-state pairs {uᵢ},{vᵢ} from the two subsystems. Why does SVD guarantee this always exists, for any bipartite state? | writing the state's coefficients as a matrix C (indexed by the two subsystems' basis states), the Schmidt decomposition is exactly the SVD C=UΣVᵀ; because SVD exists for *every* matrix regardless of size or rank, a Schmidt decomposition is guaranteed for every bipartite pure state, with the Schmidt coefficients being precisely the singular values *(required: identifies the coefficient-matrix-to-SVD correspondence)* | — |
| T4 | transfer | short-answer | 2.57 | Numerical linear algebra libraries (e.g. LAPACK) compute matrix rank via SVD rather than via row reduction to row-echelon form. Why is SVD-based rank determination more numerically reliable? | row reduction can produce a pivot that is nonzero only due to rounding error, making a rank-deficient matrix appear full rank (or vice versa); SVD instead produces singular values that decay smoothly, so rank can be estimated by counting singular values above a numerically justified tolerance, far more robust to floating-point noise *(required: names the false-pivot failure mode of row reduction)* | confuses row-reduction rank (via pivots) with SVD-based numerical rank as equally reliable in floating point → `svd` |

*Coverage: 20 items, −0.05…2.57.*

---

## Uniqueness of SVD (`uniqueness-of-svd`)
*Prereq: Singular Value Decomposition · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | In what sense is SVD unique, and in what sense isn't it? | the singular *values* are always unique (given a fixed ordering convention); the singular *vectors* are not always unique, especially when singular values repeat | — |
| R2 | recall | mcq | 0.27 | If two singular values are equal, the corresponding singular vectors are: | not pinned down to one pair — any orthonormal basis of the shared subspace works equally well | claims they're "still uniquely determined" even with a repeat → `uniqueness-of-svd` |
| R3 | recall | short-answer | 0.47 | Under what convention are singular values themselves made unique (as an ordered list)? | by convention they're listed in decreasing order σ₁≥σ₂≥⋯≥0; without this ordering, the pairing of indices to values would itself be an arbitrary permutation | treats the descending-order convention as an incidental detail rather than what makes "the first singular value" well-defined → `uniqueness-of-svd` |
| R4 | recall | short-answer | 0.62 | Does the number of nonzero singular values (the rank) ever vary between two valid SVDs of the same A? | no — rank is an intrinsic invariant of A itself, independent of which valid decomposition is chosen; every valid SVD of A has exactly rank(A) nonzero singular values | — |
| A1 | apply | short-answer | 0.77 | Singular values 5, 5, 2 (a repeat). Why aren't the vectors for the two 5's pinned down to one specific pair? | any orthonormal basis of their shared 2-dimensional subspace is an equally valid choice of singular vectors — exactly parallel to eigenvectors being non-unique for a repeated eigenvalue *(required: the shared-subspace argument)* | — |
| A2 | apply | short-answer | 0.87 | A = 3I₂ (i.e. [[3,0],[0,3]]). Describe the full freedom in choosing (U,V), compared to the sign-only freedom when singular values are distinct. | since Σ=3I, A=UΣVᵀ=3UVᵀ requires U=V; any orthogonal V works, so (U,V) ranges over an entire family of equal orthogonal matrices, not just a two-element sign choice *(required: identifies the full orthogonal-group freedom vs. discrete sign freedom)* | — |
| A3 | apply | numeric | 0.97 | A has distinct singular values 7 and 2. Counting only the sign ambiguity of each (uᵢ,vᵢ) pair, how many valid (U,V) combinations produce the same A? `[verified: 4]` | 2 independent sign choices (one per pair, since u and v of a pair must flip together) → 2×2=4 valid combinations | — |
| A4 | apply | short-answer | 1.07 | AᵀA=diag(9,9,4). Verify that v=(1/√2, 1/√2, 0) and v′=(1/√2, −1/√2, 0) are equally valid unit eigenvectors for the repeated eigenvalue 9, both giving singular value 3. `[verified: AᵀAv=9v, AᵀAv′=9v′]` | AᵀAv=(9/√2,9/√2,0)=9v and AᵀAv′=9v′ — both lie in the 2-dimensional eigenspace for λ=9 and are equally legitimate, so either can serve as a right singular vector for σ=3 | — |
| A5 | apply | short-answer | 1.17 | A is a 2×2 matrix with equal singular values σ₁=σ₂=5, i.e. A=5Q for orthogonal Q (a 30° rotation). Name a second, equally valid pair (U′,V′) besides (Q,I). | any pair with U′=QV′ for an *arbitrary* orthogonal V′ works, e.g. V′=rotation by 40° with U′=rotation by 70° — since Σ=5I is unaffected by any joint rotation of the basis | — |
| A6 | apply | short-answer | 1.27 | A symmetric matrix has a repeated eigenvalue 4 with 2-dimensional eigenspace spanned by (1,0,0) and (0,1,0). State the exact SVD analogue of this non-uniqueness. | because A is symmetric, its SVD coincides with its eigendecomposition (U=V=eigenvector matrix); the repeated eigenvalue 4 becomes a repeated singular value 4, and any orthonormal basis of the same eigenspace serves equally well as the corresponding pair of singular vectors | — |
| A7 | apply | numeric | 1.37 | A singular value has multiplicity k=3 (a 3-dimensional degenerate subspace). What is the dimension of the manifold of valid orthonormal bases for that subspace? `[verified: 3]` | the valid bases are related by the orthogonal group O(3), whose dimension is k(k−1)/2=3(2)/2=3 | — |
| E1 | explain | short-answer | 1.47 | Why is the *sign* of singular vectors ambiguous even for distinct singular values? | if (u,v) is a valid singular-vector pair, so is (−u,−v) — the product uvᵀ (and hence its contribution to A) is unchanged by flipping both signs together *(required)* | — |
| E2 | explain | short-answer | 1.57 | Prove that if A has distinct singular values, the pairs (uᵢ,vᵢ) are unique up to a joint sign flip — no more, no less. | from Avᵢ=σᵢuᵢ, vᵢ is the (up to sign) unique unit eigenvector of AᵀA for the simple eigenvalue σᵢ²; uᵢ is then forced to be Avᵢ/σᵢ, so flipping vᵢ's sign forces uᵢ's to flip too, but no other freedom remains since the eigenspace is 1-dimensional *(required: derives uᵢ's dependence on vᵢ)* | — |
| E3 | explain | short-answer | 1.67 | Explain why the non-uniqueness of singular vectors for a repeated singular value is *exactly* the eigenvector non-uniqueness of AᵀA (and AAᵀ) for a repeated eigenvalue — not merely analogous. | right singular vectors are by definition eigenvectors of AᵀA and left singular vectors eigenvectors of AAᵀ; a repeated singular value σ is a repeated eigenvalue σ² in both, so the singular-vector freedom *is* literally the eigenspace freedom inherited via the Spectral Theorem's own non-uniqueness *(required: states the literal identity, not just similarity)* | — |
| E4 | explain | short-answer | 1.77 | Does the uniqueness (or non-uniqueness) of the singular values themselves ever depend on which algorithm computed them? | no — the singular values are the square roots of AᵀA's eigenvalues, and eigenvalues of a fixed symmetric matrix are fixed numbers regardless of algorithm; only the *vectors* have freedom, and even that only affects direction/sign, never the values *(required: separates values from vectors)* | — |
| E5 | explain | short-answer | 1.87 | Why must the sign flip in a singular vector pair be applied to *both* uᵢ and vᵢ together, never to just one alone? | flipping only vᵢ would violate Avᵢ=σᵢuᵢ, since A(−vᵢ)=−σᵢuᵢ≠σᵢuᵢ unless uᵢ is also flipped; the relation forces the two signs to move together, which is why σᵢuᵢvᵢᵀ's contribution to A stays fixed under the joint flip *(required: shows the Av=σu constraint fails under a lone flip)* | — |
| T1 | transfer | short-answer | 1.97 | Why can PCA results from different software or random seeds show "flipped" principal component signs, even when the decomposition is mathematically identical? | the sign ambiguity from E1 means either sign choice is an equally valid singular/eigenvector — a flip between two runs indicates no real disagreement, just an arbitrary choice made differently *(required: connects directly to E1's sign-flip fact)* | treats a sign flip between two PCA runs as evidence of a computational error or genuine disagreement → `uniqueness-of-svd` |
| T2 | transfer | short-answer | 2.17 | Two different SVD implementations (e.g. NumPy vs. MATLAB) can return different U,V for the same matrix A. Is this a bug? | no — whenever A has a repeated singular value, or simply due to arbitrary sign conventions in each library's underlying routine, both implementations are equally valid SVDs of the same A; the difference reflects genuine non-uniqueness, not a computational error *(required: connects to genuine non-uniqueness, not a bug)* | assumes any difference between two libraries' SVD output must indicate a bug in one of them → `uniqueness-of-svd` |
| T3 | transfer | short-answer | 2.37 | In an eigenfaces face-recognition pipeline, two nearly-identical singular values appear. Why might the resulting "eigenface" directions be unstable across different training runs, even though reconstruction quality stays the same? | nearly equal singular values behave like a near-degenerate subspace — small perturbations in the data can rotate the estimated singular vectors substantially within it while barely changing the singular values or the overall reconstruction — so individual eigenfaces are ill-defined even though the *subspace* they span is stable *(required: distinguishes vector instability from stable subspace/reconstruction)* | — |
| T4 | transfer | short-answer | 2.57 | In modal analysis of a symmetric structure with two vibration modes sharing the same natural frequency (a repeated eigenvalue), why can't engineers uniquely identify "the" two individual mode shapes from measurement alone? | a repeated eigenvalue means any orthonormal basis of the corresponding 2-dimensional eigenspace is an equally valid pair of mode shapes — exactly the eigendecomposition non-uniqueness for repeated eigenvalues — so only the *subspace* of possible motions is physically well-defined, not a particular pair of basis mode shapes *(required: names the repeated-eigenvalue/subspace argument)* | assumes a physical system must have one uniquely correct mode shape per frequency → `eigendecomposition` |

*Coverage: 20 items, −0.03…2.57.*

---

## SVD and the Four Fundamental Subspaces (`svd-four-fundamental-subspaces`)
*Prereq: Singular Value Decomposition · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | How does SVD reveal the column space and left null space via U? | the first r columns of U (r=rank(A)) form an orthonormal basis for C(A); the last m−r columns form a basis for the left null space | — |
| R2 | recall | mcq | 0.27 | The number of nonzero singular values equals: | the rank of A | picks "always min(m,n)" — only true when A is full rank; in general it's the rank, which can be smaller → `svd-four-fundamental-subspaces` |
| R3 | recall | short-answer | 0.47 | How does SVD reveal the row space and null space via V? | the first r columns of V (r=rank(A)) form an orthonormal basis for the row space C(Aᵀ); the last n−r columns form a basis for the null space N(A) | — |
| R4 | recall | short-answer | 0.62 | State the two orthogonal-complement relationships that SVD makes concrete via U and V. | row space ⊥ null space, both spanned by disjoint groups of V's orthonormal columns; column space ⊥ left null space, both spanned by disjoint groups of U's orthonormal columns | — |
| A1 | apply | short-answer | 0.77 | A is 5×3 with singular values 4, 2, 0. Find rank(A), and how many columns of V span the null space. `[verified: rank=2, 1 null-space column]` | rank=2 (two nonzero singular values); n−r=3−2=1 column of V spans the null space | — |
| A2 | apply | numeric | 0.87 | A is 4×6 with singular values 8, 3, 3, 0, 0. Find rank(A), the number of columns of U spanning the left null space, and the number of columns of V spanning the null space. `[verified: rank=3; 1 left-null-space column; 3 null-space columns]` | rank=3 (three nonzero singular values); m−r=4−3=1 column of U for left null space; n−r=6−3=3 columns of V for null space | — |
| A3 | apply | short-answer | 0.97 | A is 3×3 and full rank (singular values 5, 4, 2, none zero). Describe all four fundamental subspaces. | column space = row space = all of R³; null space = {0}; left null space = {0} — a full-rank square matrix has only trivial null and left null spaces | — |
| A4 | apply | numeric | 1.07 | A is 2×5 with rank 2 (both singular values nonzero). Find the dimensions of the column space, row space, null space, and left null space. `[verified: 2, 2, 3, 0]` | column space dim=r=2; row space dim=r=2; null space dim=n−r=5−2=3; left null space dim=m−r=2−2=0 (trivial) | assumes every non-square matrix must have a nontrivial null space or left null space → `svd-four-fundamental-subspaces` |
| A5 | apply | numeric | 1.17 | A 5×3 matrix has rank 2. How many columns of U span the column space, and how many span the left null space? `[verified: 2, 3]` | first r=2 columns of U (5×5) span the column space; the remaining m−r=5−2=3 columns span the left null space | — |
| A6 | apply | short-answer | 1.27 | A = [[1,0],[0,0]] (already diagonal, rank 1). Read off all four fundamental subspaces by inspection and verify they match the SVD-derived bases. | A e₁=e₁, A e₂=0, so C(A)=span(e₁), N(A)=span(e₂); Aᵀ has the same form so C(Aᵀ)=span(e₁), N(Aᵀ)=span(e₂) — exactly matching u₁=v₁=e₁ (σ=1) and u₂=v₂=e₂ (σ=0) | — |
| A7 | apply | short-answer | 1.37 | A is m×n with m<n (wide) and full row rank r=m. Which fundamental subspace is guaranteed trivial, and which is generally not? | the left null space is trivial (dimension m−r=0); the null space generally is not, having dimension n−r=n−m>0 whenever n>m | — |
| E1 | explain | short-answer | 1.47 | Why does SVD give such a clean, unified picture of all four fundamental subspaces at once, compared to computing each separately via row reduction? | U's and V's columns split automatically by whether their corresponding singular value is zero or nonzero — one decomposition simultaneously produces orthonormal bases for all four subspaces, rather than requiring four separate computations *(required)* | — |
| E2 | explain | short-answer | 1.57 | Why is the row space ⊥ null space split automatic from the SVD, rather than something that needs a separate proof? | V is an orthogonal matrix by construction, so all of its columns are mutually orthonormal regardless of which group — row space or null space — they're assigned to; the orthogonal-complement relationship is built into V's orthogonality, not proved afterward *(required: names V's orthogonality as the reason)* | — |
| E3 | explain | short-answer | 1.67 | Why is rank(A)=rank(Aᵀ) immediate from the SVD, without a separate row-rank-equals-column-rank argument? | A=UΣVᵀ and Aᵀ=VΣᵀUᵀ share the same nonzero diagonal entries of Σ (just transposed); the count of nonzero singular values — the rank — is identical for A and Aᵀ by inspection *(required)* | — |
| E4 | explain | short-answer | 1.77 | Use the SVD to show that N(A) = N(AᵀA) exactly. | for i≤r, AᵀAvᵢ=σᵢ²vᵢ≠0 since σᵢ≠0; for i>r, Avᵢ=0 so AᵀAvᵢ=Aᵀ0=0 too — the two null spaces are spanned by exactly the same last n−r columns of V *(required: covers both the i≤r and i>r cases)* | — |
| E5 | explain | short-answer | 1.87 | Why do the fundamental subspace dimensions from SVD automatically satisfy rank–nullity (dim row space + dim null space = n)? | V has exactly n orthonormal columns total, split into r assigned to the row space and n−r to the null space by construction; the split exhausts all of V's columns, so the dimensions must sum to n by definition, not as a separately needed theorem *(required)* | — |
| T1 | transfer | short-answer | 1.97 | How does this concept complete `disjointness-four-fundamental-subspaces`'s abstract "orthogonal complements" statement with something concrete? | rather than merely asserting row space ⊥ null space abstractly, SVD hands you the actual orthonormal basis vectors for each (the relevant columns of U and V), making the orthogonal-complement relationship explicit and computable *(required: names the shift from abstract to concrete/computable)* | — |
| T2 | transfer | short-answer | 2.17 | Matrix-completion recommender systems compute a rank-r factorization implicitly identifying an r-dimensional "preference subspace" among users. Relate this to the SVD-derived column space, and note the caveat. | the top-r singular directions of U approximate a basis for a reduced column space — the dominant subspace users' rating patterns are believed to live near; but because entries were missing and had to be filled in first, this only approximates the true four fundamental subspaces of the (never fully observed) underlying matrix *(required: states the missing-data caveat)* | — |
| T3 | transfer | short-answer | 2.37 | Explain, via the fundamental subspaces revealed by SVD, why Ax=b is solvable exactly when b lies in C(A), and how the left null space (via U's last m−r columns) certifies inconsistency. | b is expressible as Ax for some x iff b∈C(A)=span of U's first r columns; the last m−r columns of U (left null space, =N(Aᵀ)) are orthogonal to C(A), so any nonzero component of b along those directions means b∉C(A) and the system has no solution — the Fredholm alternative made concrete *(required: names the Fredholm-alternative connection)* | — |
| T4 | transfer | short-answer | 2.57 | In control theory, observability and controllability of a state-space system are assessed via the (numerical) rank of an observability/controllability matrix. Explain how SVD-based subspace identification differs from an exact rank test here. | rather than testing for an exact zero singular value, practitioners threshold small singular values against a numerical tolerance; directions with singular value below that tolerance are treated as unobservable or uncontrollable, since real sensor noise and rounding mean a true zero is rarely observed exactly *(required: names thresholding in place of an exact-zero test)* | — |

*Coverage: 20 items, −0.03…2.57.*

---

## Moore-Penrose Inverse (`moore-penrose-inverse`)
*Prereq: Singular Value Decomposition · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | Define the pseudo-inverse A⁺ via SVD. | if A=UΣVᵀ, then A⁺=VΣ⁺Uᵀ, where Σ⁺ inverts each nonzero singular value and leaves zeros as zero | — |
| R2 | recall | mcq | 0.27 | The pseudo-inverse is needed specifically because: | some matrices have no ordinary inverse (non-square, or singular) | claims it's "always faster to compute than the ordinary inverse" — a different, unrelated property → `moore-penrose-inverse` |
| R3 | recall | short-answer | 0.47 | State the four Penrose conditions that characterize A⁺ uniquely, independent of the SVD. | AA⁺A=A; A⁺AA⁺=A⁺; (AA⁺)ᵀ=AA⁺; (A⁺A)ᵀ=A⁺A — any matrix satisfying all four is *the* pseudo-inverse of A | — |
| R4 | recall | short-answer | 0.62 | What do AA⁺ and A⁺A represent geometrically? | AA⁺ is the orthogonal projection onto C(A); A⁺A is the orthogonal projection onto C(Aᵀ) (the row space) | — |
| A1 | apply | short-answer | 0.77 | For an invertible square matrix A, what does A⁺ reduce to? | the ordinary inverse A⁻¹ — the pseudo-inverse generalizes but agrees with the regular inverse whenever it exists | — |
| A2 | apply | numeric | 0.87 | A = [2, 1] (1×2, full row rank). Compute A⁺. `[verified: (0.4, 0.2)ᵀ]` | AAᵀ=4+1=5, so A⁺=Aᵀ(AAᵀ)⁻¹=(2,1)ᵀ/5=(0.4,0.2)ᵀ | — |
| A3 | apply | numeric | 0.97 | A = [[1],[2]] (2×1, full column rank). Compute A⁺. `[verified: (0.2, 0.4)]` | AᵀA=1+4=5, so A⁺=(AᵀA)⁻¹Aᵀ=(1/5)(1,2)=(0.2,0.4) | — |
| A4 | apply | numeric | 1.07 | Σ=diag(4, 2, 0). Find Σ⁺. `[verified: diag(0.25, 0.5, 0)]` | invert each nonzero entry and leave zeros as zero: Σ⁺=diag(1/4, 1/2, 0) | — |
| A5 | apply | short-answer | 1.17 | A has singular values 5 and 0.0001. Explain the practical hazard of computing A⁺ by literally inverting every singular value here. | inverting 0.0001 gives 10,000 — any noise present along that direction gets amplified by a factor of 10,000, dominating the result; practical pseudo-inverses truncate singular values below a tolerance to zero instead of inverting them | inverts every singular value literally regardless of size, without considering numerical amplification → `moore-penrose-inverse` |
| A6 | apply | numeric | 1.27 | A = [[2,0],[0,0]] (rank 1). Compute A⁺ and verify AA⁺A=A. `[verified: A⁺=[[0.5,0],[0,0]]; AA⁺A=A]` | A⁺=[[0.5,0],[0,0]]; AA⁺=[[1,0],[0,0]]; (AA⁺)A=[[1,0],[0,0]]·[[2,0],[0,0]]=[[2,0],[0,0]]=A ✓ | — |
| A7 | apply | short-answer | 1.37 | For A full row rank, A⁺=Aᵀ(AAᵀ)⁻¹. Show algebraically that AA⁺=I. | AA⁺=A·Aᵀ(AAᵀ)⁻¹=(AAᵀ)(AAᵀ)⁻¹=I, using that AAᵀ is invertible precisely because A has full row rank *(required: the substitution step)* | — |
| E1 | explain | short-answer | 1.47 | State the key application of the pseudo-inverse to least squares. | solving min‖Ax−b‖² even when A isn't invertible (e.g. an overdetermined system); x=A⁺b gives the minimum-norm least-squares solution *(required)* | — |
| E2 | explain | short-answer | 1.57 | Prove that A⁺=(AᵀA)⁻¹Aᵀ when A has full column rank, starting from A⁺=VΣ⁺Uᵀ. | with full column rank all singular values are nonzero, so Σ⁺Σ=I; (AᵀA)⁻¹Aᵀ=(VΣᵀΣVᵀ)⁻¹VΣᵀUᵀ=VΣ⁻²Vᵀ·VΣUᵀ=VΣ⁻¹Uᵀ=VΣ⁺Uᵀ=A⁺ *(required: the full algebraic chain)* | — |
| E3 | explain | short-answer | 1.67 | Why is (AB)⁺ ≠ B⁺A⁺ in general, unlike ordinary inverses where (AB)⁻¹=B⁻¹A⁻¹? | the pseudo-inverse depends on the specific row/column spaces (projections) of A and B individually, which don't compose simply under matrix multiplication order the way exact algebraic cancellation does for true inverses — the reversal rule relies on invertibility throughout, which A and B individually may lack *(required: names the projection-dependence, not just "it's just true")* | assumes pseudo-inverses obey the same reversal rule as ordinary inverses → `moore-penrose-inverse` |
| E4 | explain | short-answer | 1.77 | Prove that AA⁺ is the orthogonal projector onto C(A). | AA⁺=UΣVᵀ·VΣ⁺Uᵀ=UΣΣ⁺Uᵀ=U·diag(1,…,1,0,…,0)·Uᵀ (r ones), which is exactly the orthogonal projection matrix onto span of the first r columns of U, i.e. C(A) *(required: the ΣΣ⁺=diag(1,…,1,0,…,0) step)* | — |
| E5 | explain | short-answer | 1.87 | How does truncating small singular values before inverting (regularized pseudo-inverse) relate to ridge regression, and how does it differ? | both shrink the influence of small-singular-value directions, but truncation sets them to exactly zero (a hard cutoff via 1/σ→0), while ridge regression shrinks continuously via σ/(σ²+λ) instead of 1/σ, never fully zeroing out any direction *(required: names the hard-cutoff vs. continuous-shrinkage distinction)* | — |
| T1 | transfer | short-answer | 1.97 | Why does the standard OLS formula β̂=(XᵀX)⁻¹Xᵀy break down with perfectly collinear predictors, and how does β̂=X⁺y still give a sensible answer? | perfectly collinear predictors make XᵀX singular, so (XᵀX)⁻¹ doesn't exist; the pseudo-inverse X⁺ sidesteps this entirely by inverting only the nonzero singular values, producing the minimum-norm solution among the (now infinitely many) coefficient vectors that fit equally well *(required: names XᵀX's singularity as the specific failure, and the minimum-norm resolution)* | — |
| T2 | transfer | short-answer | 2.17 | In compressed sensing / sensor calibration, measurements are often fewer than unknowns, giving an underdetermined system with infinitely many exact solutions. Why is the pseudo-inverse's minimum-norm solution the natural default here? | among all vectors that exactly satisfy the (consistent, underdetermined) measurement equations, the minimum-norm one is the unique choice that adds no spurious energy beyond what the measurements actually constrain — a principled default when there's no other information favoring one exact solution over another *(required: names "no spurious extra energy" as the justification)* | — |
| T3 | transfer | short-answer | 2.37 | In robotics, inverse kinematics computes joint velocities q̇ from a desired end-effector velocity ẋ via ẋ=Jq̇, where J (the Jacobian) is often wide (more joints than task dimensions). Why is q̇=J⁺ẋ the standard solution? | with more joints than task dimensions, J is underdetermined and infinitely many joint-velocity combinations achieve the same end-effector motion; the pseudo-inverse returns the minimum-norm one, which corresponds physically to the least joint motion (energy) needed to accomplish the task *(required: connects minimum-norm to minimum joint motion)* | — |
| T4 | transfer | short-answer | 2.57 | Why is calling `pinv(A) @ b` in code considered bad practice compared to a dedicated least-squares solver (e.g. `lstsq`), despite being mathematically equivalent? | forming A⁺ explicitly requires computing and storing a full SVD-based inverse, which is more expensive and less numerically accurate than solving the least-squares problem directly via methods (QR or SVD-based solvers) tuned for that specific system, for the same reason ordinary inverses shouldn't be formed explicitly just to solve Ax=b *(required: names the same "don't form the inverse explicitly" principle as for ordinary inverses)* | assumes pinv(A)@b is as accurate/efficient as a dedicated least-squares solver → `moore-penrose-inverse` |

*Coverage: 20 items, −0.03…2.57.*

---

## Eckart-Young Theorem (`eckart-young`)
*Prereq: Singular Value Decomposition · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | State the Eckart-Young theorem. | the best rank-k approximation to A (minimizing the Frobenius or operator-norm distance) is obtained by truncating A's SVD to its k largest singular values and corresponding vectors | — |
| R2 | recall | mcq | 0.27 | "Best rank-k approximation" means: | the closest rank-k matrix to A, in a specific matrix-norm sense | treats it as "any arbitrary rank-k matrix" — missing that Eckart-Young is a genuine optimality guarantee, not just a construction → `eckart-young` |
| R3 | recall | short-answer | 0.47 | State the exact Frobenius-norm error formula for the best rank-k approximation. | ‖A−A_k‖_F = √(sum of squares of the *dropped* singular values), i.e. √(Σ_{i>k}σᵢ²) | — |
| R4 | recall | short-answer | 0.62 | State the exact spectral (operator) norm error formula for the best rank-k approximation. | ‖A−A_k‖₂ = σ_{k+1}, exactly the largest *dropped* singular value — not a sum | — |
| A1 | apply | numeric | 0.77 | A's singular values are 10, 5, 1. Compute the Frobenius-norm approximation error for the best rank-1 and best rank-2 approximations. `[verified: √26≈5.1, and 1]` | error = √(sum of squares of the *dropped* singular values); rank-1 drops 5,1: √(25+1)≈5.1; rank-2 drops only 1: √1=1 | — |
| A2 | apply | numeric | 0.87 | A's singular values are 8, 4, 2, 1. Compute the rank-2 approximation error in Frobenius norm. `[verified: √5≈2.24]` | drops 2 and 1: √(4+1)=√5≈2.24 | — |
| A3 | apply | numeric | 0.97 | Using the same singular values (8, 4, 2, 1), compute the rank-2 approximation error in spectral norm. `[verified: 2]` | spectral-norm error is exactly the largest dropped singular value: σ₃=2 (not the sum of dropped ones) | confuses the spectral-norm error with the Frobenius-norm error (sums dropped singular values instead of taking the max) → `eckart-young` |
| A4 | apply | numeric | 1.07 | Singular values 20, 10, 5, 1, 0.5. What percentage of the total squared Frobenius norm is retained by the rank-2 approximation? `[verified: ≈95.0%]` | total squares: 400+100+25+1+0.25=526.25; rank-2 retains 400+100=500; 500/526.25≈95.0% | — |
| A5 | apply | numeric | 1.17 | Singular values 6, 5, 4, 3 (slow, gradual decay). Compute the rank-1 approximation error in Frobenius norm and compare it to σ₁. `[verified: √50≈7.07, larger than σ₁=6]` | drops 5,4,3: √(25+16+9)=√50≈7.07 — larger than the value kept (σ₁=6), showing that with no natural gap in the spectrum, low-rank approximation can discard more "size" than it keeps | believes rank-1 error must be small whenever the top singular value is large, missing that slow decay yields large errors → `eckart-young` |
| A6 | apply | numeric | 1.27 | Singular values 100, 40, 15, 5, 2. Find the minimum k such that the spectral-norm error is below 10. `[verified: k=3]` | need σ_{k+1}<10: σ₂=40 (no), σ₃=15 (no), σ₄=5<10 (yes) — so k=3 is the minimum rank achieving spectral-norm error under 10 | — |
| A7 | apply | numeric | 1.37 | Singular values 12, 9, 9, 3. Find the minimum k needed to retain at least 90% of the squared Frobenius norm. `[verified: k=3, ≈97.1%]` | total squares=144+81+81+9=315; k=1: 144/315≈45.7%; k=2: 225/315≈71.4%; k=3: 306/315≈97.1% ≥90% | — |
| E1 | explain | short-answer | 1.47 | Why is Eckart-Young the precise theoretical justification for PCA's dimensionality reduction, rather than just a reasonable heuristic? | keeping the top k principal components is *provably* the best possible rank-k approximation of the (centered) data matrix in the Eckart-Young sense — not merely a plausible choice, an optimal one *(required: the "provably optimal, not heuristic" framing)* | — |
| E2 | explain | short-answer | 1.57 | Why does the spectral-norm error equal exactly σ_{k+1} (the single largest dropped value), while the Frobenius-norm error sums the squares of *all* dropped singular values? | the spectral norm measures the worst-case stretch along any single direction, and no dropped direction stretches more than σ_{k+1}; the Frobenius norm instead measures total squared "energy" across every direction, so it accumulates the contribution of every dropped singular value, not just the largest *(required: contrasts worst-case single-direction vs. total-energy)* | — |
| E3 | explain | short-answer | 1.67 | Why does Eckart-Young's optimality guarantee fail under other norms, such as the entrywise ℓ1-norm or a weighted norm? | the proof relies on the norm being unitarily invariant — unchanged under the orthogonal transformations U and V — a property the ℓ1/entrywise or weighted norms lack; truncated SVD therefore need not minimize those norms, which is why robust PCA and weighted low-rank methods require different algorithms *(required: names unitary invariance as the required property)* | — |
| E4 | explain | short-answer | 1.77 | How does knowing the exact error formula in advance let a practitioner choose a target rank k rigorously, rather than by trial and error? | the cumulative retained-energy curve (Σ_{i≤k}σᵢ² / Σᵢσᵢ²) can be computed directly from the singular value spectrum alone, so a rank meeting any target fidelity (e.g. 95% variance) can be read off without ever needing to compute multiple truncations and measure their error empirically *(required: names computing the target k from the spectrum alone)* | — |
| E5 | explain | short-answer | 1.87 | If σ_k = σ_{k+1} exactly (a tie straddling the truncation boundary), what happens to the uniqueness of the best rank-k approximation matrix A_k? | A_k is not unique in this edge case — different choices of which tied singular direction to keep produce different, equally optimal, rank-k matrices — even though the *value* of the minimal error itself is still uniquely determined *(required: distinguishes non-unique matrix from unique error value)* | — |
| T1 | transfer | short-answer | 1.97 | Why can SVD-based image compression achieve large file-size reduction with minimal visible quality loss for many real images? | real images often have rapidly decaying singular values — most of the "information" concentrates in a few large ones — so a low-rank Eckart-Young approximation captures nearly all of the image's structure while discarding the numerous small, largely-negligible singular values *(required: names the rapid-decay property specifically, not just "SVD compresses well")* | — |
| T2 | transfer | short-answer | 2.17 | In a Netflix-style matrix-factorization recommender, Eckart-Young justifies why keeping the top-k latent factors minimizes reconstruction error on the *training* ratings matrix. What does the theorem *not* guarantee, and why? | it does not guarantee good generalization to unseen (held-out) ratings — Eckart-Young is a statement purely about the training matrix's own reconstruction error, and says nothing about how well the same low-rank structure predicts ratings the matrix never observed *(required: names the training-vs-generalization gap explicitly)* | assumes Eckart-Young's optimality on training data implies optimal predictive accuracy on unseen data → `eckart-young` |
| T3 | transfer | short-answer | 2.37 | In singular spectrum analysis, a noisy time series is embedded into a Hankel matrix, decomposed via SVD, and reconstructed after truncating small singular values. Relate the practical "elbow" choice of truncation rank to the theorem. | Eckart-Young guarantees the truncated reconstruction is the best possible rank-k approximation for whatever k is chosen; the elbow in the singular value spectrum (a sharp drop from signal-dominated to noise-dominated values) identifies a natural k where truncating removes mostly noise rather than signal, though the theorem itself never picks k — only compares each fixed choice's optimality *(required: separates the theorem's optimality-per-k from the separate, heuristic choice of k)* | — |
| T4 | transfer | short-answer | 2.57 | Proper Orthogonal Decomposition (POD), used to reduce large PDE-simulation snapshot matrices to a handful of dominant modes, is essentially an application of Eckart-Young. Explain how. | the snapshot matrix collects simulation states as columns; POD keeps the top-k left singular vectors as reduced-order "modes" — by Eckart-Young, this choice provably captures the maximum possible energy (Frobenius norm) achievable by any k-dimensional mode basis, guaranteeing the reduced model is the best possible linear compression of that specific data for that number of modes *(required: names the snapshot-matrix-to-SVD correspondence)* | — |

*Coverage: 20 items, −0.03…2.57.*

---

## Principal Component Analysis (Matrix Edition) (`pca-matrix-edition`)
*Prereq: SVD, Rayleigh Quotient, Covariance · ancestors 31 · b₀ = 1.23*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.23 | State the connection between PCA and eigenvectors/singular vectors. | PCA's principal components are the eigenvectors of the data's covariance matrix, ordered by eigenvalue (variance explained) — equivalently, the top singular vectors of the centered data matrix | — |
| R2 | recall | mcq | 0.53 | The first principal component captures: | the direction of maximum variance in the data | picks "the direction most correlated with the outcome variable" — PCA is unsupervised and never looks at any outcome → `pca-matrix-edition` |
| R3 | recall | short-answer | 0.73 | Why must data be centered (mean-subtracted) before computing PCA via the covariance matrix or SVD? | covariance is defined relative to the mean; without centering, the computed "directions of maximum variance" would instead reflect the mean's location mixed in with the spread structure, conflating two unrelated things | — |
| R4 | recall | short-answer | 0.88 | State the two equivalent routes to compute PCA, and why the second is often numerically preferred. | (1) eigendecompose the covariance matrix Σ=(1/(n−1))XᵀX, or (2) take the SVD of the centered data matrix X directly; route (2) is preferred because forming XᵀX squares the condition number, losing accuracy in small singular values/eigenvalues that SVD-on-X avoids | — |
| A1 | apply | numeric | 1.03 | A covariance matrix has eigenvalues 8, 3, 1 (total variance 12). What fraction of total variance does the first principal component explain? `[verified: 66.7%]` | 8/12 ≈ 66.7% | — |
| A2 | apply | numeric | 1.13 | Covariance eigenvalues 12, 6, 2 (total 20). What fraction of variance do the first two PCs explain together? `[verified: 90%]` | (12+6)/20=90% | — |
| A3 | apply | short-answer | 1.23 | Covariance eigenvalues 5, 5, 5, 5 (fully isotropic — all equal). What can you conclude about a "first" principal component here? | there is none — with all eigenvalues equal there is no preferred direction; any orthonormal basis of the space is an equally valid choice of principal components (the degenerate case of eigenvector non-uniqueness) | assumes PCA always yields a well-defined ranked ordering of components regardless of the eigenvalue structure → `pca-matrix-edition` |
| A4 | apply | numeric | 1.33 | A centered data matrix X has singular values 6, 3, 1. Find the eigenvalues of the (unnormalized) scatter matrix XᵀX. `[verified: 36, 9, 1]` | eigenvalues of XᵀX are the squares of X's singular values: 6²=36, 3²=9, 1²=1 | — |
| A5 | apply | numeric | 1.43 | 100 samples, covariance eigenvalues 40, 25, 10, 5, 3, 2 (sum 85). How many PCs are needed to explain at least 80% of the variance? `[verified: 3]` | cumulative: 40/85≈47.1%, 65/85≈76.5%, 75/85≈88.2% ≥80% — so k=3 PCs are needed | — |
| A6 | apply | numeric | 1.53 | A centered data matrix X (3 columns) has singular values 6, 3, 1. What is the Frobenius-norm reconstruction error of the rank-2 PCA approximation of X? `[verified: 1]` | truncating to k=2 drops only σ₃=1, so the error is √(1²)=1 (Eckart-Young applied directly to the centered data matrix) | — |
| A7 | apply | short-answer | 1.63 | Standardized data (unit variance each) with p=4 variables and zero pairwise correlation (correlation matrix = I₄). What are the covariance eigenvalues, and what does PCA "find"? | all four eigenvalues equal 1 (since the matrix is the identity); PCA finds no preferred structure at all — any orthonormal basis is an equally valid set of PCs, reflecting that uncorrelated equal-variance data has no meaningful direction to reduce toward | — |
| E1 | explain | short-answer | 1.73 | Trace the full chain connecting Rayleigh quotients, the Spectral Theorem, and Eckart-Young into "PCA." | (1) the first PC maximizes the covariance matrix's Rayleigh quotient (`rayleigh-quotient`); (2) by the Spectral Theorem, that maximizer is exactly the top eigenvector; (3) by Eckart-Young, keeping the top k such components is provably the best rank-k summary of the data — three independently-derived results converging into one coherent method *(required: all three steps named in sequence)* | — |
| E2 | explain | short-answer | 1.83 | Derive, via Lagrange multipliers, why the first principal component direction w (maximizing Var(Xw) subject to ‖w‖=1) is exactly the top eigenvector of the covariance matrix Σ. | Var(Xw)=wᵀΣw; maximize wᵀΣw subject to wᵀw=1 gives Lagrangian L=wᵀΣw−λ(wᵀw−1); setting ∇L=2Σw−2λw=0 yields Σw=λw — w must be an eigenvector of Σ, and the maximized variance λ=wᵀΣw is largest when λ is the largest eigenvalue *(required: shows the ∇L=0 step forcing Σw=λw)* | — |
| E3 | explain | short-answer | 1.93 | Why are the principal components automatically orthogonal to one another (uncorrelated), rather than this being an extra constraint PCA imposes by hand? | Σ is symmetric, so the Spectral Theorem guarantees its eigenvectors can be chosen orthonormal — orthogonality of the PCs is a *consequence* of Σ's symmetry, not an independent requirement imposed on top of the variance-maximization derivation *(required: names symmetry + Spectral Theorem as the source)* | treats PC orthogonality as an arbitrary design choice of the PCA algorithm rather than a forced consequence → `spectral-theorem` |
| E4 | explain | short-answer | 2.03 | Why does running PCA on the correlation matrix (standardized variables) instead of the raw covariance matrix produce different principal components? | covariance-based PCA is scale-dependent — a variable measured in units that happen to give it larger raw variance dominates the top PCs regardless of its actual importance; correlation-based PCA first standardizes each variable to unit variance, removing this scale artifact so the components reflect correlation structure alone *(required: names the scale-dependence issue)* | — |
| E5 | explain | short-answer | 2.13 | Derive that "1 minus the fraction of variance explained by the top k PCs" equals the normalized squared Frobenius truncation error from Eckart-Young. | the covariance eigenvalues equal the squared singular values of the centered data matrix X (up to the constant 1/(n−1)); the fraction Σ_{i≤k}λᵢ/Σᵢλᵢ is therefore identical to Σ_{i≤k}σᵢ²/Σᵢσᵢ², the fraction of squared Frobenius norm retained by the rank-k truncation — so "variance explained" and "energy retained" are the same quantity under two names *(required: shows λᵢ=σᵢ²/(n−1) linking the two formulas)* | — |
| T1 | transfer | short-answer | 2.23 | Why is PCA called unsupervised, and how does contrasting it with LDA make this concrete? | PCA's entire derivation — maximizing variance via the covariance matrix's Rayleigh quotient — never references any outcome or label variable at all; LDA, by contrast, explicitly uses class labels to find discriminative directions, making the supervised/unsupervised distinction concrete rather than terminological *(required: the explicit LDA contrast)* | — |
| T2 | transfer | short-answer | 2.43 | In population genomics (e.g. the 1000 Genomes Project), PCA is applied to genotype matrices to visualize population ancestry clusters along the top PCs. Why is PCA's unsupervised nature well-suited here, and what should not be assumed about the resulting axes? | no phenotype or population label is used to compute the PCs, so any ancestry structure that emerges is discovered rather than imposed, making PCA suitable for finding unknown structure; but the resulting axes are not guaranteed to align with any particular biologically or clinically meaningful category — they simply maximize variance, which happens to often correlate with ancestry *(required: the "discovered, not guaranteed meaningful" caveat)* | — |
| T3 | transfer | short-answer | 2.63 | In the eigenfaces approach to face recognition, PCA on a database of flattened face-image vectors yields a small set of "eigenfaces" that reconstruct new faces well. Connect this directly to Eckart-Young's optimality guarantee. | the eigenfaces are the top principal components of the (centered) face-image data matrix; by Eckart-Young (via E5's identity), keeping the top k of them is provably the best possible rank-k reconstruction of the training faces in squared-error terms, which is exactly why a modest number of eigenfaces suffices for recognizable reconstructions *(required: explicit Eckart-Young connection, not just "PCA compresses well")* | — |
| T4 | transfer | short-answer | 2.83 | In finance, PCA on the covariance matrix of asset returns is used to find dominant risk factors — the first PC often approximates a "market factor" driving co-movement across most stocks. Why is the eigenvalue spectrum's decay rate directly informative about how many independent risk factors realistically drive a market? | a rapidly decaying spectrum (one or two dominant eigenvalues, the rest small) means most return co-movement is explained by very few factors, while a slowly decaying spectrum indicates many roughly-equally-important sources of risk; this is the same Eckart-Young energy-retained logic used to choose a truncation rank, applied here to decide how many risk factors are worth modeling explicitly *(required: connects decay rate to number of meaningful factors via the Eckart-Young framing)* | — |

*Coverage: 20 items, 0.23…2.83. This is the capstone item of the entire linear-algebra domain — E1 explicitly names every prior concept it draws on.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| singular values equated with, rather than square-rooted from, AᵀA's eigenvalues | `svd` |
| confuses row-reduction rank (via pivots) with SVD-based numerical rank as equally reliable in floating point | `svd` |
| singular vectors assumed unique despite repeated singular values | `uniqueness-of-svd` |
| singular vector sign flips treated as computational disagreement | `uniqueness-of-svd` |
| assumes differing SVD output between software implementations indicates a bug | `uniqueness-of-svd` |
| rank confused with min(m,n) rather than the count of nonzero singular values | `svd-four-fundamental-subspaces` |
| assumes every non-square matrix must have a nontrivial null space or left null space | `svd-four-fundamental-subspaces` |
| pseudo-inverse motivation confused with a speed claim | `moore-penrose-inverse` |
| assumes pseudo-inverses obey the same reversal rule, (AB)⁺=B⁺A⁺, as ordinary inverses | `moore-penrose-inverse` |
| "best rank-k approximation" treated as an arbitrary construction rather than a provable optimum | `eckart-young` |
| assumes rank-1 error must be small whenever the top singular value is large, missing slow decay | `eckart-young` |
| assumes training-set optimality (Eckart-Young) implies optimal generalization to unseen data | `eckart-young` |
| PCA treated as using outcome/label information | `pca-matrix-edition` |
| assumes PCA always yields a well-defined ranked ordering of components regardless of the eigenvalue structure | `pca-matrix-edition` |

**Cluster total: 120 items across 6 concepts.** All numeric claims verified. `pca-matrix-edition`'s E1
is the deliberate capstone of the 54-concept linear-algebra sweep.

---

# Linear Algebra: complete

**54 / 54 concepts done.** Total items across all 8 clusters: **~271**.
