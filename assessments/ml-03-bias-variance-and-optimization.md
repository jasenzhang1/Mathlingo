# Machine Learning Cluster 3 — Bias-Variance & Optimization

Bias-Variance Tradeoff, Overfitting and Underfitting, Gradient Descent, Cross Entropy Loss (4
concepts). Same format as [Cluster 1](ml-01-foundations.md). `gradient-descent` reuses
`matrix-calculus`'s own T1 item verbatim as its definitional justification, and `cross-entropy-loss`'s
T1 is the single most consequential cross-domain link in the ML sweep: training a classifier by
gradient descent on cross-entropy *is* computing an MLE.

---

## Bias-Variance Tradeoff (`bias-variance-tradeoff`)
*Prereq: Loss Functions, Variance · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.18 | State the bias-variance decomposition of expected test error. | expected error = bias² + variance + irreducible error | — |
| R2 | recall | mcq | 0.12 | A high-bias model typically: | is too simple and systematically misses real patterns (underfits) | claims it "fits training data too closely" — that's high variance, not high bias → `bias-variance-tradeoff` |
| A1 | apply | short-answer | 0.62 | A linear model fit to genuinely curved data — is this high bias or high variance? A very high-degree polynomial fit to a small dataset — which is it? | linear-on-curved: high bias (the model's form is too rigid, regardless of data amount); high-degree-on-small-data: high variance (flexible enough to fit this sample's noise, unstable across resamples) *(required: both cases correctly attributed)* | swaps the two, e.g. calling the rigid linear model "high variance" → `bias-variance-tradeoff` |
| E1 | explain | short-answer | 1.32 | Why do bias and variance trade off as model complexity changes? | as complexity increases, bias tends to decrease (more flexible models capture more of the true pattern) while variance tends to increase (more flexible models are more sensitive to the specific noise in a given training sample) — directly reflected in the two separate terms of R1's decomposition moving in opposite directions *(required: both directions, tied to the decomposition)* | — |
| T1 | transfer | short-answer | 1.82 | What structural parallel connects this decomposition to `law-of-total-variance` from the probability/statistics sweep? | both decompose one total quantity into interpretable, separately-actionable pieces — "total variance = within-group + between-group" and "total error = bias² + variance + irreducible error" are the same kind of move: splitting an aggregate into causes that can be addressed independently *(required: names the shared decomposition structure explicitly)* | — |

| R3 | recall | short-answer | −0.85 | Define the bias of a model (or estimator) f̂ at a point x. | bias(x) = E[f̂(x)] − f(x), the systematic gap between the average prediction over many training sets and the true function | — |
| R4 | recall | short-answer | −0.8 | Define the variance of a model's prediction at a point x. | variance(x) = E[(f̂(x) − E[f̂(x)])²] — how much the prediction fluctuates across different training sets | — |
| R5 | recall | mcq | −0.6 | "Irreducible error" in the bias-variance decomposition refers to: | noise inherent in the data-generating process that no model, however good, can eliminate | claims it "can be reduced with more training data" — it is by definition irreducible, independent of model or data quantity → `bias-variance-tradeoff` |
| R6 | recall | short-answer | −0.7 | Fill in the blank: expected test error = ___² + ___ + ___. | bias, variance, irreducible error | — |
| R7 | recall | mcq | −0.5 | Which term in the bias-variance decomposition does NOT depend on which model is chosen? | the irreducible error term | selects "variance" — variance changes substantially with model choice (e.g. flexibility) → `bias-variance-tradeoff` |
| R8 | recall | short-answer | −0.55 | True or false: bias and variance in the decomposition are strictly additive, with no interaction term. | true — expected test error is exactly bias² + variance + irreducible error, with no cross term | — |
| R9 | recall | mcq | −0.4 | A model that predicts the same constant output regardless of input exhibits: | very high bias, very low variance | claims "very high variance" — a constant predictor cannot vary at all across training sets → `bias-variance-tradeoff` |
| R10 | recall | short-answer | −0.3 | What does "low bias, high variance" typically describe in terms of model flexibility? | a highly flexible model (e.g. a deep unpruned tree or high-degree polynomial) that fits training data closely but is unstable across different training sets | — |
| R11 | recall | mcq | −0.2 | The bias² + variance + irreducible error decomposition is most directly derived for: | squared-error loss | claims it "holds for any loss function without modification" — the clean additive decomposition is a property of squared error specifically → `bias-variance-tradeoff` |
| R12 | recall | short-answer | −0.1 | What quantity does "expected test error" represent, in the decomposition's own terms? | the average prediction error on new, unseen data, averaged over the randomness of both the training set and the test point | — |
| R13 | recall | mcq | 0.0 | Bias is best described as: | systematic error from the model's assumptions being wrong, present even with infinite data | describes it as "random error from sensitivity to the particular training sample" — that is variance, not bias → `bias-variance-tradeoff` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: the ___ term captures how much a model's predictions change across different training sets drawn from the same distribution. | variance | — |
| R15 | recall | mcq | −0.15 | Which of these best exemplifies a high-variance model? | a 1-nearest-neighbor classifier, whose prediction can flip entirely if one training point changes | selects "a linear regression fit to a small dataset" — linear regression's rigid form makes it comparatively low-variance, high-bias on curved data → `bias-variance-tradeoff` |
| R16 | recall | short-answer | −0.25 | Does bias, on its own, go to zero simply by collecting more training data? | no — bias comes from the model's functional form being wrong; more data reduces variance, not bias, unless the model class itself changes | — |
| A2 | apply | numeric | 0.5 | If bias = 2, variance = 3, and irreducible error = 1, compute expected test error. `[verified: 8]` | bias² + variance + irreducible error = 4 + 3 + 1 = 8 | — |
| A3 | apply | short-answer | 0.55 | An ensemble of deep, unpruned decision trees gives wildly different predictions depending on which random subset of data trained it. Is this a bias or variance symptom? | variance — predictions fluctuate substantially across different training samples, the defining signature of variance rather than a systematic error shared by all fits | — |
| A4 | apply | short-answer | 0.7 | Model A gets 40% train accuracy and 39% test accuracy. Is Model A's error dominated by bias or variance? | bias — both train and test performance are poor and close together, meaning the model's form is too weak to capture the pattern even on data it has already seen (low variance, high bias) | — |
| A5 | apply | short-answer | 0.6 | Comparing 1-nearest-neighbor to 50-nearest-neighbor prediction on the same dataset, which has higher variance? | 1-nearest-neighbor — its prediction depends entirely on a single nearby point and can change drastically with small perturbations to the training set, while 50-NN averages over many points and is comparatively stable | — |
| A6 | apply | numeric | 0.65 | A model has bias = 0 and irreducible error = 2. If expected test error is measured at 5, what is the variance? `[verified: 3]` | expected test error = bias² + variance + irreducible error → 5 = 0 + variance + 2 → variance = 3 | — |
| E2 | explain | short-answer | 1.15 | Why can't a model in general drive both bias and variance to zero simultaneously? | reducing bias typically requires more flexibility to fit the true pattern, but added flexibility makes the model more sensitive to the specific noise in each training sample, raising variance — the two pulls work in opposite directions as complexity changes *(required: names the opposing-pulls mechanism)* | — |
| E3 | explain | short-answer | 1.2 | Why does cross-validation error approximate the expected test error from the decomposition, rather than bias alone? | cross-validation repeatedly holds out different subsets and measures error on unseen data, so it captures both the systematic error from a wrong model form (bias) and the fluctuation from being fit to a particular training subset (variance), not bias alone *(required)* | — |
| E4 | explain | short-answer | 1.4 | Why does increasing model complexity generally increase variance? | a more complex model has more parameters/flexibility to fit whatever specific noise happens to appear in a given training sample, so its fitted function changes more from one training sample to the next *(required)* | — |
| E5 | explain | short-answer | 1.1 | Why is irreducible error a hard lower bound on achievable test error, regardless of model choice? | it represents genuine randomness in how the outcome is generated given the inputs (e.g. measurement noise), which no function of the inputs — however well chosen — can predict away *(required)* | — |
| T2 | transfer | short-answer | 1.7 | How does this decomposition parallel the mean-squared-error decomposition of a point estimator, MSE(θ̂) = Bias(θ̂)² + Var(θ̂), from classical estimation theory? | both decompose a squared-error-based risk into the same two structural pieces — a squared systematic offset and a spread term — because both are instances of the same algebraic identity E[(X−c)²] = (E[X]−c)² + Var(X), applied once to an estimator of a parameter and once to a model's prediction of a function value *(required: names the shared algebraic identity)* | — |
| T3 | transfer | short-answer | 1.6 | How does a regularization strength hyperparameter (like ridge regression's λ) act as a dial on the bias-variance tradeoff? | increasing regularization strength constrains the model's flexibility, deliberately trading a small increase in bias (the fit is pulled away from the unconstrained optimum) for a larger reduction in variance (less sensitivity to the specific training sample) — the same tradeoff as this concept's core decomposition, made continuously tunable *(required: explicit tradeoff framing)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.85…1.82.*

---

## Overfitting and Underfitting (`overfitting-underfitting`)
*Prereq: Bias-Variance Tradeoff · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | Define overfitting and underfitting in bias-variance terms. | overfitting: fits training data (including its noise) too well, generalizes poorly — high variance; underfitting: too simple, misses real patterns even in the training data — high bias | — |
| R2 | recall | mcq | 0.15 | The classic symptom of overfitting is: | low training error but high validation/test error (a large gap) | describes underfitting instead — "high error on both" → `overfitting-underfitting` |
| A1 | apply | short-answer | 0.65 | 99% training accuracy, 60% test accuracy — which is this? 55% training, 54% test (both low) — which is this? | first: overfitting (large train/test gap); second: underfitting (both bad and close together, indicating the model isn't capturing patterns present even in training data) *(required: both cases correctly diagnosed)* | — |
| E1 | explain | short-answer | 1.35 | Why does regularization (L1/L2 penalties, early stopping) combat overfitting by *increasing* bias? | regularization deliberately constrains model flexibility, trading a small increase in bias for a larger reduction in variance — per the bias-variance decomposition, this trade can lower *total* error even though bias alone goes up *(required: the explicit trade framing, using the decomposition)* | — |
| T1 | transfer | short-answer | 1.85 | Why does adding more training data generally fix overfitting but does little for underfitting? | more data helps a flexible model "average out" noise, reducing variance — the same 1/√n scaling from `sample-mean`; but it doesn't change the fundamental limitation of a model whose *form* is too simple to capture the true pattern, however much data it sees *(required: the explicit 1/√n connection, and naming the form-limitation for underfitting)* | assumes more data is a universal fix for poor model performance regardless of its source → `overfitting-underfitting` |

| R3 | recall | short-answer | −0.9 | Define overfitting on its own. | a model fits the training data — including its noise — so closely that it generalizes poorly to new data | — |
| R4 | recall | short-answer | −0.85 | Define underfitting on its own. | a model is too simple to capture real patterns present even in the training data, giving poor performance everywhere | — |
| R5 | recall | mcq | −0.65 | The classic symptom of underfitting is: | high error on both the training set and the test set, close together | describes overfitting instead — "low training error, high test error" → `overfitting-underfitting` |
| R6 | recall | short-answer | −0.75 | Fill in the blank: overfitting corresponds to high ___, while underfitting corresponds to high ___ (in bias-variance terms). | variance; bias | — |
| R7 | recall | mcq | −0.55 | Which of the following is a standard remedy for overfitting? | adding a regularization penalty, gathering more data, or reducing model complexity | selects "increasing model complexity" — this typically worsens overfitting rather than fixing it → `overfitting-underfitting` |
| R8 | recall | short-answer | −0.6 | True or false: a large gap between training and validation error is itself a warning sign, even without knowing the absolute error values. | true — a large gap is the signature of overfitting regardless of the absolute error level | — |
| R9 | recall | mcq | −0.45 | "Early stopping" as a fix for overfitting means: | halting training before the model has fully converged on the training set, once validation error stops improving | claims it means "training for as many epochs as possible" — the opposite; early stopping specifically limits training duration → `overfitting-underfitting` |
| R10 | recall | short-answer | −0.35 | What single quantity is most commonly plotted to visually diagnose overfitting vs underfitting during training? | training error and validation error plotted together against training progress (epochs, tree depth, etc.) | — |
| R11 | recall | mcq | −0.25 | An underfit model's training error is generally: | high, because the model cannot capture the pattern even on data it has already seen | claims it's "always near zero" — that describes overfitting, not underfitting → `overfitting-underfitting` |
| R12 | recall | short-answer | −0.2 | Is underfitting more associated with too little model complexity or too much? | too little — the model's functional form is too restrictive to represent the true relationship | — |
| R13 | recall | mcq | −0.05 | Which remedy specifically targets underfitting rather than overfitting? | increasing model complexity (e.g. adding features, using a more flexible model class) | selects "adding an L2 penalty" — regularization restricts flexibility further, which would worsen underfitting → `overfitting-underfitting` |
| R14 | recall | short-answer | 0.0 | Fill in the blank: a model that memorizes noise in the training set rather than the underlying signal is said to ___. | overfit | — |
| R15 | recall | mcq | −0.1 | Cross-validation helps detect overfitting primarily because it: | estimates performance on data the model did not train on, exposing a train/validation gap | claims it "reduces the amount of noise in the training data" — cross-validation doesn't clean data, it estimates generalization → `overfitting-underfitting` |
| R16 | recall | short-answer | −0.3 | Does an underfit model typically benefit much from being given more training data alone (with no change in model complexity)? | no — since the problem is the model's form being too simple, more data of the same kind doesn't fix that limitation | — |
| A2 | apply | short-answer | 0.6 | A neural network's training loss keeps dropping every epoch while its validation loss starts rising after epoch 20. What's happening, and what would you do? | overfitting is setting in after epoch 20; early stopping around that point (or adding regularization) would prevent the validation loss from continuing to rise | — |
| A3 | apply | short-answer | 0.55 | A linear regression model has R²=0.2 on both training and test data. Overfitting or underfitting? | underfitting — the low, similar performance on both sets indicates the model's form is too limited to capture the relationship, not that it memorized noise | — |
| A4 | apply | numeric | 0.7 | Training error is 0.05 and validation error is 0.45. Compute the train/validation gap, and state whether this pattern signals overfitting. `[verified: 0.40]` | gap = 0.45 − 0.05 = 0.40, a large gap — a strong overfitting signal | — |
| A5 | apply | short-answer | 0.65 | A decision tree is limited to depth 1 (a single split) on a genuinely complex dataset. Is this more likely to overfit or underfit? | underfit — a single split is extremely limited in flexibility and is very unlikely to capture a genuinely complex relationship, regardless of how much data is available | — |
| A6 | apply | short-answer | 0.75 | Two models: Model X has train accuracy 98%, test 97%. Model Y has train accuracy 98%, test 65%. Which shows overfitting? | Model Y — the large train/test gap (33 points) is the overfitting signature; Model X's small gap suggests it's generalizing well | — |
| E2 | explain | short-answer | 1.2 | Why doesn't a small train/validation gap alone guarantee a good model? | both training and validation error could be high and close together — that's underfitting, not success; a small gap only rules out overfitting, it says nothing about whether the absolute error level is acceptable *(required)* | — |
| E3 | explain | short-answer | 1.25 | Why can gathering more training data reduce overfitting even without changing the model? | more data makes it harder for a flexible model to fit sample-specific noise exactly, since the same noise pattern is unlikely to repeat consistently across a larger sample — the model is forced toward capturing the genuine, shared signal instead *(required)* | — |
| E4 | explain | short-answer | 1.45 | Why is validation performance, not training performance, the correct signal for deciding when to stop early? | training error decreases (or stays flat) monotonically as training continues, so it can never itself signal that the model has started fitting noise; only performance on data withheld from training can reveal when further training is hurting generalization *(required)* | — |
| E5 | explain | short-answer | 1.15 | Why can a model simultaneously overfit in some regions of feature space and underfit in others? | model complexity is often not spatially uniform in its effect — e.g. a tree may have deep, noise-fitting branches in densely-sampled regions while staying too coarse in sparsely-sampled regions, so the aggregate error masks genuinely different local failure modes *(required)* | — |
| T2 | transfer | short-answer | 1.75 | How does the overfitting/underfitting distinction map onto the "goodness of fit vs. parsimony" tension familiar from model selection criteria like AIC/BIC in regression? | AIC/BIC explicitly penalize model complexity to avoid selecting an overfit model that merely improves in-sample fit through more parameters, while still rewarding likelihood gains that resolve real underfitting — the same overfit/underfit tension this concept describes, formalized as a single scored tradeoff rather than diagnosed qualitatively *(required: names the explicit complexity-penalty mechanism)* | — |
| T3 | transfer | short-answer | 1.9 | Why does a k-fold cross-validation curve (error vs. model complexity) typically look U-shaped, and how does each side of the U map to this concept? | the left side of the U (low complexity) shows high error from underfitting; the right side (high complexity) shows rising validation error from overfitting, even as training error keeps falling; the minimum of the U is the complexity level balancing the two, operationalizing the bias-variance tradeoff as a literal curve one can read off *(required: both sides of the U correctly attributed)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.9…1.9.*

---

## Gradient Descent (`gradient-descent`)
*Prereq: Loss Functions, Matrix Calculus · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | State the gradient descent update rule, and identify η. | θ ← θ − η∇L(θ); η is the learning rate, controlling step size | — |
| R2 | recall | mcq | 0.0 | If η is set too large, gradient descent may: | overshoot the minimum and potentially diverge (oscillate or blow up) | claims it "converges faster with no downside" — a large step size risks instability, not free speed → `gradient-descent` |
| A1 | apply | numeric | 0.5 | L(θ)=(θ−3)², so ∇L(θ)=2(θ−3). Starting at θ=0 with η=0.1, compute θ after one step. `[verified: 0.6]` | ∇L(0)=−6; θ_new=0−0.1(−6)=0.6 | — |
| E1 | explain | short-answer | 1.2 | Using `matrix-calculus`'s fact that the gradient points toward steepest ascent, explain why the update *subtracts* η∇L(θ). | this is exactly `matrix-calculus`'s own T1 argument, now the definitional heart of gradient descent: since ∇L points toward steeper ascent, subtracting it moves toward steeper descent — the correct direction for minimizing L *(required: the explicit reuse of that argument)* | — |
| T1 | transfer | short-answer | 1.7 | Why is stochastic gradient descent (computing the gradient from one or a small minibatch, not the whole dataset) used for huge datasets despite giving a noisier gradient estimate each step? | it trades a noisier per-step estimate for a dramatically cheaper cost per step, letting many more steps run in the same compute budget; the noise itself can even help escape shallow local minima that full-batch gradient descent might get stuck in *(required: both the cost tradeoff and the noise-can-help point)* | — |

| R3 | recall | short-answer | −0.9 | What does ∇L(θ) represent in the gradient descent update rule? | the gradient of the loss function with respect to the parameters θ — the direction of steepest ascent of L at the current θ | — |
| R4 | recall | short-answer | −0.85 | What is the role of the learning rate η in gradient descent? | it scales the size of each parameter update step along the negative gradient direction | — |
| R5 | recall | mcq | −0.6 | If η is set far too small, gradient descent will: | still converge to the minimum eventually, but very slowly | claims it "will diverge" — a tiny step size makes convergence slow, not unstable → `gradient-descent` |
| R6 | recall | short-answer | −0.7 | Fill in the blank: θ ← θ − η ___. | ∇L(θ) | — |
| R7 | recall | mcq | −0.5 | Gradient descent moves the parameters in the direction of: | the negative gradient, i.e. steepest descent | claims it moves "in the direction of the gradient itself" — that's steepest ascent, the opposite of what minimizing requires → `gradient-descent` |
| R8 | recall | short-answer | −0.55 | True or false: gradient descent is guaranteed to find the global minimum for any loss function. | false — it is only guaranteed for convex loss functions; on non-convex losses it can get stuck at a local minimum or saddle point | — |
| R9 | recall | mcq | −0.4 | A "local minimum" that gradient descent can get stuck at is: | a point where the loss is lower than all nearby points but not necessarily the lowest possible loss overall | describes it as "always identical to the global minimum" — the entire concern with non-convex optimization is that they can differ → `gradient-descent` |
| R10 | recall | short-answer | −0.3 | What condition on the loss function guarantees gradient descent (with a suitable η) converges to the unique global minimum? | convexity of the loss function | — |
| R11 | recall | mcq | −0.2 | "Batch" gradient descent, as opposed to stochastic gradient descent, computes the gradient using: | the entire training dataset at every step | claims it uses "a single random example per step" — that describes stochastic gradient descent, not batch → `gradient-descent` |
| R12 | recall | short-answer | −0.1 | What stopping criterion is commonly used to decide when to halt gradient descent? | when the gradient's magnitude (or the change in loss) falls below a small threshold, or after a fixed number of iterations | — |
| R13 | recall | mcq | 0.0 | A "saddle point" in optimization is: | a point where the gradient is zero but it is a minimum along some directions and a maximum along others | describes it as "always a global minimum" — a saddle point is specifically not a minimum in every direction → `gradient-descent` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: the learning rate η is an example of a ___, a value set before training rather than learned from data. | hyperparameter | — |
| R15 | recall | mcq | −0.15 | Momentum-based gradient descent variants primarily help with: | accelerating convergence and smoothing out oscillations in narrow, curved loss valleys | claims they "remove the need for a learning rate entirely" — momentum still uses a learning rate, it adds an additional averaging term → `gradient-descent` |
| R16 | recall | short-answer | −0.25 | Does gradient descent require the loss function to be differentiable everywhere it's applied? | yes, in its standard form — it needs ∇L(θ) to exist at the points visited; non-differentiable losses need subgradient methods instead | — |
| A2 | apply | numeric | 0.55 | Continuing from A1 (θ=0.6 after step one, L(θ)=(θ−3)², η=0.1), compute θ after a second step. `[verified: 1.08]` | ∇L(0.6) = 2(0.6−3) = −4.8; θ_new = 0.6 − 0.1(−4.8) = 0.6 + 0.48 = 1.08 | — |
| A3 | apply | numeric | 0.5 | L(θ)=θ², η=0.5, starting at θ=4. Compute θ after one step. `[verified: 0.0]` | ∇L(4)=2(4)=8; θ_new=4−0.5(8)=4−4=0 | — |
| A4 | apply | numeric | 0.65 | L(θ)=θ², η=1.5 (too large), starting at θ=2. Compute θ after one step, and state whether \|θ\| grew or shrank. `[verified: −4.0, grew]` | ∇L(2)=4; θ_new=2−1.5(4)=2−6=−4; \|−4\|=4 > \|2\|=2, so the magnitude grew — a symptom of instability from too-large η | — |
| A5 | apply | short-answer | 0.6 | The loss vs. iteration curve oscillates wildly and increases over time. What hyperparameter is the most likely culprit, and what fix would you try? | the learning rate η is likely too large, causing the updates to repeatedly overshoot the minimum; reducing η is the standard first fix | — |
| A6 | apply | short-answer | 0.7 | Training on a dataset of 10 million examples, would batch or stochastic gradient descent typically make more progress per unit of wall-clock time? | stochastic (or minibatch) gradient descent — computing the full-dataset gradient for batch GD is extremely expensive per step, so many cheap noisy steps typically beat few expensive exact ones in wall-clock terms | — |
| E2 | explain | short-answer | 1.15 | Why does gradient descent on a convex loss function always converge to the global minimum (with a suitably small η), while on a non-convex loss it might not? | convexity guarantees there is exactly one local minimum, which is also the global minimum, so any point of zero gradient found is the right answer; non-convex losses can have multiple local minima and saddle points, so following the gradient downhill only guarantees reaching *some* stationary point, not necessarily the best one *(required)* | — |
| E3 | explain | short-answer | 1.25 | Why does a decaying (shrinking over time) learning rate schedule often work better than a fixed η? | a larger η early on lets the algorithm make fast progress from a poor starting point, while a smaller η later prevents overshooting once it's near the minimum — fixing a single η forces a compromise between these two needs at every step *(required)* | — |
| E4 | explain | short-answer | 1.3 | Why is gradient descent applied to L(θ) rather than searching over θ exhaustively? | for continuous, high-dimensional θ, exhaustive search is computationally infeasible (the space grows exponentially with dimension), while the gradient gives a locally optimal direction to move in using only local information, making iterative improvement tractable *(required)* | — |
| E5 | explain | short-answer | 1.1 | Why does minibatch gradient descent (a compromise between batch and stochastic) tend to be the practical default? | it captures most of stochastic GD's speed advantage per step (avoiding the cost of the full dataset) while averaging over enough examples to reduce the gradient estimate's noise compared to a single-example step, balancing speed against stability *(required)* | — |
| T2 | transfer | short-answer | 1.75 | How does gradient descent's convexity requirement for guaranteed global convergence connect to why logistic regression's loss (log-loss) is specifically chosen to be convex, unlike, say, misclassification rate? | logistic regression is trained by gradient descent, so its loss must be differentiable and, ideally, convex for training to reliably reach the global optimum; misclassification rate is neither differentiable nor convex, which is exactly why a smooth, convex surrogate like log-loss is used instead *(required: connects the convexity requirement to the surrogate-loss design choice)* | — |
| T3 | transfer | short-answer | 1.65 | Why does the Hessian (matrix of second derivatives) matter for understanding gradient descent's convergence speed, connecting to `matrix-calculus`? | the Hessian's eigenvalues describe the loss surface's curvature in each direction; a large ratio between the largest and smallest eigenvalue ("ill-conditioning") makes the loss surface a narrow valley, causing plain gradient descent to zig-zag slowly — exactly the second-derivative generalization `matrix-calculus` introduces beyond the first-derivative gradient this concept uses directly *(required: names the Hessian/curvature connection)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.9…1.75.*

---

## Cross Entropy Loss (`cross-entropy-loss`)
*Prereq: Loss Functions, Likelihood vs Probability · ancestors 12 · b₀ = 0.78*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.22 | State binary cross-entropy loss. | L = −[y log(ŷ) + (1−y) log(1−ŷ)], y the true 0/1 label, ŷ the predicted probability | — |
| R2 | recall | mcq | 0.08 | Cross-entropy loss is directly connected to which earlier concept? | negative log-likelihood — minimizing cross-entropy is exactly maximizing likelihood, from `mle` | treats the connection as coincidental resemblance rather than an exact algebraic identity → `cross-entropy-loss` |
| A1 | apply | numeric | 0.58 | y=1, ŷ=0.9. Compute the loss. Then y=1, ŷ=0.1. Compare. `[verified: 0.105 vs 2.303]` | −log(0.9)≈0.105; −log(0.1)≈2.303 — much larger, reflecting a severe penalty for confident wrongness | — |
| E1 | explain | short-answer | 1.28 | Why does cross-entropy loss go to infinity as a confidently wrong prediction approaches ŷ→0 (with y=1)? Why is that desirable? | −log(ŷ)→∞ as ŷ→0; this unbounded penalty heavily discourages a classifier from being both wrong *and* overconfident, which is a genuinely desirable property — a merely-wrong-but-uncertain prediction is punished far less *(required: the limit argument and why it's a desirable design choice)* | — |
| T1 | transfer | short-answer | 1.78 | Show that minimizing cross-entropy over a dataset of Bernoulli-labeled examples is exactly equivalent to maximizing likelihood (`mle`), and explain what that means for gradient-descent-trained classifiers. | the negative log-likelihood of n independent Bernoulli(ŷᵢ) observations is exactly Σ−[yᵢlog(ŷᵢ)+(1−yᵢ)log(1−ŷᵢ)] — the sum of per-example cross-entropy terms; minimizing this sum via gradient descent is therefore literally computing an MLE, connecting a modern training objective directly to classical estimation theory *(required: the explicit sum-of-log-likelihoods identity, not just "they're related")* | states the connection is a helpful analogy rather than an exact mathematical identity → `cross-entropy-loss` |

| R3 | recall | short-answer | −0.85 | What do y and ŷ represent in binary cross-entropy loss? | y is the true 0/1 class label; ŷ is the model's predicted probability that the label is 1 | — |
| R4 | recall | short-answer | −0.8 | What is the range of possible values for binary cross-entropy loss on a single example? | 0 to +∞ — it is 0 only for a perfectly confident correct prediction and grows unboundedly as a confident prediction becomes more wrong | — |
| R5 | recall | mcq | −0.6 | Cross-entropy loss compared to squared-error loss for classification: | penalizes confident wrong predictions much more heavily, since it grows without bound as ŷ approaches the wrong extreme | claims cross-entropy is "bounded above, unlike squared error" — it's the reverse; cross-entropy is the one that is unbounded → `cross-entropy-loss` |
| R6 | recall | short-answer | −0.7 | Fill in the blank: L = −[y log(ŷ) + (1−y) log(___)]. | 1 − ŷ | — |
| R7 | recall | mcq | −0.5 | For multi-class classification, cross-entropy loss generalizes to: | −Σ yᵢ log(ŷᵢ), summed over all classes, with y one-hot encoded | claims it "only ever applies to exactly two classes" — the categorical cross-entropy generalization to K classes is standard → `cross-entropy-loss` |
| R8 | recall | short-answer | −0.55 | True or false: cross-entropy loss is zero when ŷ exactly equals the true label y (0 or 1). | true — a perfectly confident and correct prediction gives zero loss | — |
| R9 | recall | mcq | −0.4 | ŷ in binary cross-entropy must lie in which range for the loss to be well-defined? | strictly between 0 and 1, i.e. (0, 1) | claims "any real number is fine" — log(ŷ) and log(1−ŷ) are undefined or infinite outside (0,1) → `cross-entropy-loss` |
| R10 | recall | short-answer | −0.3 | What activation function is standardly paired with binary cross-entropy to ensure ŷ falls in a valid range? | the sigmoid (logistic) function, which squashes its input to (0, 1) | — |
| R11 | recall | mcq | −0.2 | The name "cross-entropy" originates from: | information theory, measuring the average number of bits needed to encode outcomes from the true distribution using a code optimized for the predicted distribution | claims it originates from "the cross-validation procedure" — an unrelated concept that happens to share the word "cross" → `cross-entropy-loss` |
| R12 | recall | short-answer | −0.1 | What loss value does an uninformative predictor (ŷ=0.5 always) give for a positive example (y=1)? | −log(0.5) ≈ 0.693 | — |
| R13 | recall | mcq | 0.0 | Which prediction incurs a HIGHER cross-entropy loss for a true label y=1? | ŷ=0.2 (confidently wrong) over ŷ=0.6 (mildly right) | selects "ŷ=0.6" — 0.6 is on the correct side of 0.5 and gives lower loss than the confidently wrong 0.2 → `cross-entropy-loss` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: cross-entropy loss is the negative ___ of the predicted probability assigned to the true class. | log (logarithm) | — |
| R15 | recall | mcq | −0.15 | Cross-entropy loss is used for classification rather than squared error mainly because it: | produces better-behaved gradients for probability-valued outputs and connects directly to maximum likelihood estimation | claims it's used "because it's cheaper to compute than squared error" — the choice is about statistical/optimization properties, not raw computational cost → `cross-entropy-loss` |
| R16 | recall | short-answer | −0.25 | Is binary cross-entropy loss symmetric in y=0 vs y=1 — i.e. does mislabeling in either direction get penalized the same way for the same predicted confidence? | yes, by construction — swapping y=0↔1 and ŷ↔(1−ŷ) leaves the loss value unchanged, so the penalty structure is symmetric | — |
| A2 | apply | numeric | 0.5 | y=1, ŷ=0.5 (an uninformative guess). Compute the loss. `[verified: 0.693]` | −log(0.5) ≈ 0.693 | — |
| A3 | apply | numeric | 0.55 | y=0, ŷ=0.99 (confidently wrong). Compute the loss. `[verified: 4.605]` | L = −[0·log(0.99) + 1·log(1−0.99)] = −log(0.01) ≈ 4.605 | — |
| A4 | apply | numeric | 0.6 | y=1, ŷ=0.7. Compute the loss, and compare to y=1, ŷ=0.3. `[verified: 0.357 vs 1.204]` | −log(0.7)≈0.357; −log(0.3)≈1.204 — over 3× larger, penalizing the less-confident-and-wrong prediction more | — |
| A5 | apply | short-answer | 0.65 | Two classifiers both correctly classify a test point (ŷ>0.5 for a true positive), but Classifier A outputs ŷ=0.99 and Classifier B outputs ŷ=0.51. Which has lower cross-entropy loss on this example? | Classifier A — cross-entropy rewards confident correctness with a loss closer to zero, while B's barely-over-the-threshold confidence still incurs a nontrivial loss despite the same 0/1 classification outcome | — |
| A6 | apply | short-answer | 0.7 | For a 3-class problem with true class 2 (one-hot: [0,1,0]) and predicted [0.1, 0.7, 0.2], which term in the categorical cross-entropy sum is the only one that contributes (given y is one-hot)? | only the term for the true class, −1·log(0.7) — every other term has yᵢ=0 and contributes exactly 0 to the sum | — |
| E2 | explain | short-answer | 1.15 | Why is sigmoid, rather than a linear output, needed before applying binary cross-entropy? | cross-entropy requires ŷ to be a valid probability in (0,1) for log(ŷ) and log(1−ŷ) to be finite and meaningful; sigmoid guarantees this range, while a raw linear output could be any real number, including values where the loss is undefined *(required)* | — |
| E3 | explain | short-answer | 1.2 | Why does cross-entropy loss provide better gradient signal than squared error for a sigmoid-output classifier? | squared error combined with sigmoid can produce very small gradients when the prediction is confidently wrong (the sigmoid saturates), slowing learning exactly when correction is needed most; cross-entropy's gradient with sigmoid cancels this saturation term algebraically, keeping the gradient proportional to the error size *(required: names the saturation problem squared-error/sigmoid has and that cross-entropy avoids it)* | — |
| E4 | explain | short-answer | 1.35 | Why does categorical cross-entropy only involve a single log term per example, even though it's written as a sum over all classes? | because the true label is one-hot encoded, yᵢ is zero for every class except the true one, so all terms except the true class's vanish from the sum, leaving effectively −log(ŷ_true) *(required)* | — |
| E5 | explain | short-answer | 1.1 | Why is average cross-entropy over a dataset, rather than the sum, typically reported and monitored during training? | averaging makes the reported loss comparable across datasets or batches of different sizes, and it's what's typically plotted for convergence diagnostics — the sum would simply grow with dataset size regardless of how well the model is doing *(required)* | — |
| T2 | transfer | short-answer | 1.7 | How does cross-entropy's information-theoretic origin (expected bits to encode the true distribution using a code built for the predicted one) explain why it's minimized exactly when ŷ matches the true distribution? | cross-entropy H(p,q) = H(p) + KL(p‖q), where H(p) is the true distribution's own entropy (fixed) and KL(p‖q) is the extra "wasted" bits from using the wrong code q; since KL divergence is minimized (at zero) exactly when q=p, minimizing cross-entropy is equivalent to minimizing the divergence between predicted and true distributions *(required: the explicit H(p,q)=H(p)+KL(p‖q) decomposition)* | — |
| T3 | transfer | short-answer | 1.6 | Why does using cross-entropy loss with a softmax output layer, rather than a per-class independent sigmoid with cross-entropy, matter for genuinely mutually-exclusive multi-class problems? | softmax forces the predicted class probabilities to sum to exactly 1, matching the actual constraint that an example belongs to exactly one class; independent per-class sigmoids would let the "probabilities" sum to anything, misrepresenting a genuinely single-label problem as if each class were an independent binary decision *(required: names the sum-to-one constraint mismatch)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.85…1.78.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| high bias and high variance symptoms swapped | `bias-variance-tradeoff` |
| irreducible error believed reducible with more data | `bias-variance-tradeoff` |
| bias and variance definitions swapped | `bias-variance-tradeoff` |
| constant predictor's variance/bias character reversed | `bias-variance-tradeoff` |
| overfitting and underfitting's train/test gap signatures reversed | `overfitting-underfitting` |
| more data assumed to fix any performance problem | `overfitting-underfitting` |
| underfitting remedy confused with an overfitting remedy | `overfitting-underfitting` |
| early stopping's direction (halting vs. extending training) reversed | `overfitting-underfitting` |
| large learning rate assumed harmless | `gradient-descent` |
| gradient sign (ascent vs descent) reversed | `gradient-descent` |
| batch vs. stochastic gradient descent definitions swapped | `gradient-descent` |
| saddle point conflated with a global minimum | `gradient-descent` |
| cross-entropy/MLE connection treated as analogy rather than identity | `cross-entropy-loss` |
| cross-entropy's boundedness compared to squared error reversed | `cross-entropy-loss` |
| cross-entropy's valid probability domain misunderstood | `cross-entropy-loss` |
| cross-entropy's "cross" mistaken for a reference to cross-validation | `cross-entropy-loss` |

**Cluster total: 120 items across 4 concepts (30 each).** All numeric claims verified, including the
exact cross-entropy values at ŷ=0.9 and ŷ=0.1 showing the ~22× loss penalty for confident wrongness,
and the new gradient-descent worked steps (θ=1.08 continuing from A1; θ=0 for L(θ)=θ² at η=0.5; the
η=1.5 divergence example growing from θ=2 to θ=−4).
