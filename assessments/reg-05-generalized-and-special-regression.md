# Regression Cluster 5 — Generalized & Special Regression

Mixed Effect Models, Logistic Regression, Probit Regression, Generalized Linear Model, Cox
Proportional Hazards Model (5 concepts). Same format as
[foundations-of-probability.md](foundations-of-probability.md). This is the final cluster of the
regression domain. `logistic-regression`'s E1 cashes in `exponential-family`'s logit derivation from
the probability sweep directly, and `glm`'s E1 fulfills that same concept's T1 forward-pointer about a
generic fitting algorithm for the whole exponential family.

---

## Mixed Effect Models (`mixed-effect-models`)
*Prereq: Multiple Linear Regression, Sampling Methods · ancestors 27 · b₀ = 1.17*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.17 | Distinguish fixed and random effects in a mixed model. | fixed effects: population-level coefficients, the same for everyone; random effects: allow individual/group-level variation (e.g. per-subject random intercepts), drawn from a distribution | — |
| R2 | recall | mcq | 0.13 | Mixed models are especially appropriate when: | observations are clustered/grouped (repeated measures per patient, students within schools), violating ordinary regression's independence assumption | claims they're for data where "all observations are completely independent" — the opposite of when they're needed → `mixed-effect-models` |
| A1 | apply | short-answer | 0.63 | Why does treating repeated measurements from the same patient as independent understate the true standard errors? | measurements from the same patient are correlated (their own baseline health affects all their readings similarly), so they carry less genuinely new information than the same count of measurements from different patients would — echoing `paired-t-test`'s earlier independence-violation point *(required: the direct callback to that earlier concept)* | — |
| E1 | explain | short-answer | 1.33 | What role does a random intercept play, and how does it connect to `law-of-total-variance`? | it lets each subject have its own baseline level while still estimating a single shared set of fixed-effect coefficients; the random-effect variance captures between-group variability and the residual captures within-group variability — exactly the within/between decomposition from that earlier concept *(required: the explicit within/between framing)* | — |
| T1 | transfer | short-answer | 1.83 | How does `sampling-methods`' cluster sampling naturally produce data suited to mixed-effects analysis? | cluster-sampled data (whole households or schools surveyed together) has the same within-cluster correlation structure mixed models are designed to handle — both arise from the identical underlying reality of grouped observations *(required: names the shared correlation structure explicitly)* | — |
| R3 | recall | short-answer | -0.42 | Fill in the blank: a random intercept model allows each ___ to have its own baseline level. | subject (or group/cluster) | — |
| R4 | recall | short-answer | -0.37 | What does "REML" stand for? | Restricted (or Residual) Maximum Likelihood | — |
| R5 | recall | mcq | -0.32 | True/false: a mixed model can include both random intercepts and random slopes. | True — a model can let both the baseline level and the effect of a predictor vary randomly across groups simultaneously | claims "a mixed model can only ever include a random intercept, never a random slope" — random slopes are a standard, common extension → `mixed-effect-models` |
| R6 | recall | short-answer | -0.22 | What is a "random slope" in a mixed model? | a coefficient (the effect of a predictor on the outcome) that is allowed to vary randomly across groups/subjects, rather than being fixed at one value for everyone | — |
| R7 | recall | mcq | -0.12 | Which of these is a classic example of clustered data suited to a mixed model? | repeated blood-pressure measurements taken from the same patients over several visits | picks "a simple random sample of unrelated individuals surveyed once each" — that data has no clustering/repeated-measures structure requiring a mixed model → `mixed-effect-models` |
| R8 | recall | short-answer | -0.02 | Fill in the blank: the two main variance components in a random-intercept model are the ___ variance and the ___ variance. | between-group (random-intercept) variance, and within-group (residual) variance | — |
| R9 | recall | short-answer | 0.03 | What does BLUP stand for? | Best Linear Unbiased Predictor (the standard estimator for a mixed model's random effects) | — |
| R10 | recall | mcq | 0.08 | True/false: fixed effects vary across individuals/groups the same way random effects do. | False — fixed effects are single, population-level coefficients shared by everyone; only random effects are allowed to vary across groups, drawn from a distribution | claims "fixed and random effects both vary randomly across groups; the labels are just different names for the same thing" — fixed effects are deliberately held constant across the population, the defining contrast with random effects → `mixed-effect-models` |
| R11 | recall | short-answer | 0.18 | Which estimation method is typically preferred for estimating variance components in a mixed model? | REML (restricted maximum likelihood), since it corrects for the degrees of freedom used up estimating the fixed effects | — |
| R12 | recall | mcq | 0.23 | Which term describes coefficients assumed to be the same across the entire population, in a mixed model? | fixed effects | picks "random effects" — random effects are precisely the coefficients allowed to differ across groups, the opposite of population-level fixed effects → `mixed-effect-models` |
| R13 | recall | short-answer | 0.33 | Fill in the blank: mixed models are also commonly called ___ models. | hierarchical (or multilevel) models | — |
| R14 | recall | mcq | 0.38 | True/false: ignoring a clustered/hierarchical structure and using ordinary regression typically understates the true standard errors. | True — treating correlated, clustered observations as fully independent overstates the effective sample size, understating standard errors | claims "ignoring clustering has no systematic effect on standard errors, only on point estimates" — it systematically understates SEs by overstating the effective independent information present → `mixed-effect-models` |
| R15 | recall | short-answer | 0.43 | What does the "intraclass correlation" (ICC) measure in this context? | the proportion of total variance in the outcome attributable to between-group (rather than within-group) differences | — |
| R16 | recall | mcq | 0.53 | Which of the following is typically treated as a fixed, rather than random, effect in a mixed model? | a predictor of scientific interest whose specific levels are the entire population of interest (e.g. "treatment vs. control") | picks "a large, essentially random sample of patients drawn from a larger population" — patients are the canonical example of a random effect, representing a sample from a broader population, not a fixed, exhaustive set of interest → `mixed-effect-models` |
| A2 | apply | short-answer | 0.48 | Give an example of a scenario calling for a random slope, not just a random intercept. | tracking each patient's response to a drug over time: each patient may have their own baseline health (random intercept) *and* their own individual rate of improvement over time (random slope) — some patients respond faster, some slower, which a shared, single slope for everyone would fail to capture *(required: a concrete per-subject varying-rate scenario)* | — |
| A3 | apply | short-answer | 0.58 | Why is REML typically preferred over ordinary ML for estimating a mixed model's variance components? | ordinary ML estimates of variance components are biased downward in finite samples because it doesn't account for the degrees of freedom already used up estimating the fixed effects; REML explicitly corrects for this, producing less biased variance-component estimates, echoing the same n vs. n−p−1 correction seen in `linear-regression-probabilistic-version` *(required: names the degrees-of-freedom correction REML applies)* | — |
| A4 | apply | numeric | 0.68 | A dataset has an intraclass correlation (ICC) of 0.3. What proportion of the total variance is attributable to between-group differences? `[verified: 30%]` | ICC=0.3 means 30% of the total variance is between-group, and the remaining 70% is within-group | — |
| A5 | apply | short-answer | 0.78 | What happens to a mixed model as the estimated random-effect variance shrinks toward zero? | the model effectively converges to ordinary (non-hierarchical) regression, since a random-effect variance of zero means every group shares the exact same intercept (or slope) — there's no meaningful between-group variability left to model *(required: names convergence to ordinary regression as the random-effect variance vanishes)* | — |
| A6 | apply | short-answer | 0.88 | Students are nested within classrooms, which are nested within schools. What modeling structure does this call for? | a three-level (multilevel/hierarchical) mixed model, with random effects at both the classroom level (capturing classroom-to-classroom variation) and the school level (capturing school-to-school variation), on top of the student-level residual variance *(required: names both nesting levels explicitly, not just one)* | — |
| E2 | explain | short-answer | 1.03 | Why does the estimated random-effect variance itself carry meaningful scientific interpretation, beyond just improving standard errors? | its magnitude directly quantifies how much groups genuinely differ from one another — a small random-intercept variance says groups are quite similar on average, while a large one says group membership itself is a substantial source of variability in the outcome, a scientifically interesting fact in its own right, not merely a technical correction *(required: names the random-effect variance as substantively interpretable, not just a technical fix)* | — |
| E3 | explain | short-answer | 1.13 | Why might a fixed-effects model (a dummy variable per group) be used instead of a mixed model, and what is the key tradeoff? | a fixed-effects-per-group model avoids any distributional assumption about how groups vary, but costs one degree of freedom per group (potentially many, with many groups) and cannot generalize predictions to a new, unobserved group, since there's no fitted distribution of group effects to draw from; a mixed model spends far fewer degrees of freedom (just the random-effect variance) and can predict for new groups by shrinking toward the population average *(required: names both the degrees-of-freedom cost and the inability to generalize to new groups)* | — |
| E4 | explain | short-answer | 1.23 | What determines whether a grouping variable should be treated as fixed or random in a mixed model? | whether the specific levels observed are the entire population of scientific interest (treat as fixed — e.g. comparing exactly "drug A vs. drug B") or are a sample drawn from a larger population the analyst wants to generalize beyond (treat as random — e.g. a sample of patients or clinics standing in for a broader population) *(required: names the exhaustive-population-of-interest vs. sampled-from-a-larger-population distinction)* | — |
| E5 | explain | short-answer | 1.53 | Why does BLUP shrink an individual group's estimated random effect toward the overall population mean, rather than simply using that group's raw average? | a group's raw average is a noisy estimate, especially with few observations per group; BLUP combines the group's own (noisy) information with the population-wide information (a form of partial pooling), weighting the shrinkage by how reliable the group's own data is — groups with less data get pulled more strongly toward the overall mean, borrowing strength from the rest of the sample *(required: names the partial-pooling/borrowing-strength mechanism, weighted by within-group data reliability)* | — |
| T2 | transfer | short-answer | 2.03 | How is BLUP's shrinkage of group-level estimates toward the overall mean analogous to ridge regression's shrinkage of coefficients toward zero? | both trade a little bias for reduced variance by deliberately pulling a noisy, unconstrained estimate toward a more stable reference point (the population mean for BLUP, zero for ridge) — the amount of shrinkage in both cases is governed by how much genuine, reliable signal versus noise the raw estimate carries, the same bias-variance logic applied to different "raw" quantities *(required: names the shared bias-variance shrinkage-toward-a-reference-point principle)* | — |
| T3 | transfer | short-answer | 2.13 | How does the hierarchical/nested structure of mixed models foreshadow Bayesian hierarchical models more broadly? | a mixed model's random effects, drawn from a shared distribution (e.g. Normal with an estimated variance), are structurally identical to a Bayesian hierarchical model's group-level parameters drawn from a shared prior — mixed models are essentially a frequentist (REML/ML-fit) special case of the fully Bayesian hierarchical modeling framework the graphical-models domain develops further, sharing the same "groups borrow strength from each other through a common distribution" idea *(required: names the shared groups-drawn-from-a-common-distribution structure)* | —

*Coverage: 16/6/5/3 — 30 items, −0.42…2.13.*

---

## Logistic Regression (`logistic-regression`)
*Prereq: MLE, Bernoulli and Binomial Distributions, Multiple Linear Regression · ancestors 33 · b₀ = 1.26*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.26 | State the logistic regression model. | P(Y=1\|X) = sigmoid(Xβ) = 1/(1+e^(−Xβ)) | — |
| R2 | recall | mcq | 0.56 | Logistic regression's coefficients are estimated via: | maximum likelihood, since the Bernoulli/binomial likelihood has no OLS-style closed form | claims OLS is used "exactly like linear regression" — logistic regression's likelihood requires MLE, not least squares → `logistic-regression` |
| A1 | apply | numeric | 1.06 | Xβ=2 for an observation. Compute P(Y=1\|X). `[verified: 0.8808]` | sigmoid(2) = 1/(1+e⁻²) ≈ 0.881 | — |
| E1 | explain | short-answer | 1.76 | Why is a logistic coefficient interpreted through log-odds, connecting to `exponential-family`? | `exponential-family` showed the logit η(θ)=ln(θ/(1−θ)) is the natural parameter for the Bernoulli exponential family; logistic regression literally models this natural parameter as linear in X — a one-unit increase in Xⱼ adds βⱼ to the log-odds, exactly the natural-parameter interpretation established earlier, not a fresh coincidence *(required: names the natural-parameter identity explicitly)* | — |
| T1 | transfer | short-answer | 2.26 | Why is ordinary linear regression a poor choice for modeling a binary outcome's probability directly? | a linear function's output is unbounded and can predict probabilities below 0 or above 1, which is nonsensical; the sigmoid link guarantees outputs stay within [0,1] — exactly the problem a proper link function (as in `cross-entropy-loss`'s ML-sweep framing) is designed to solve *(required: names the unbounded-output problem specifically)* | — |
| R3 | recall | short-answer | 0.06 | Fill in the blank: sigmoid(z) = ___. | 1/(1+e⁻ᶻ) | — |
| R4 | recall | short-answer | 0.11 | What is the "log-odds" (logit) of a probability p, in formula terms? | ln(p/(1−p)) | — |
| R5 | recall | mcq | 0.16 | True/false: logistic regression directly predicts a continuous probability, not a binary label. | True — the model outputs P(Y=1\|X) ∈ (0,1); converting that to a binary label requires an additional thresholding step | claims "logistic regression's raw output is already a 0/1 binary label" — its raw output is a continuous probability, which is then thresholded to get a label → `logistic-regression` |
| R6 | recall | short-answer | 0.21 | What is the range of the sigmoid function's output? | the open interval (0, 1) — it never exactly reaches 0 or 1 | — |
| R7 | recall | mcq | 0.31 | What quantity does exp(βⱼ) represent? | the multiplicative change in the odds of Y=1 for a one-unit increase in Xⱼ (the odds ratio) | claims exp(βⱼ) represents "the additive change in probability for a one-unit increase in Xⱼ" — exp(βⱼ) is a multiplicative odds ratio, not an additive probability change → `logistic-regression` |
| R8 | recall | short-answer | 0.36 | Fill in the blank: the default classification decision boundary is P(Y=1\|X) = ___. | 0.5 | — |
| R9 | recall | short-answer | 0.41 | Name a common numerical algorithm used to fit logistic regression, since there is no closed-form solution. | Newton-Raphson (or Iteratively Reweighted Least Squares, IRLS) | — |
| R10 | recall | mcq | 0.46 | True/false: logistic regression assumes a linear relationship between X and P(Y=1\|X) directly. | False — it assumes a linear relationship between X and the *log-odds* of P(Y=1\|X), not P(Y=1\|X) itself, which is a nonlinear (sigmoid) function of Xβ | claims "logistic regression assumes P(Y=1\|X) itself is a linear function of X" — that's exactly the unbounded-output problem logistic regression's sigmoid link is designed to avoid; linearity holds only for the log-odds → `logistic-regression` |
| R11 | recall | short-answer | 0.51 | What does βⱼ=0 imply about predictor Xⱼ's effect on the log-odds of Y=1? | Xⱼ has no effect on the log-odds (and hence no effect on P(Y=1\|X)), holding the other predictors fixed | — |
| R12 | recall | short-answer | 0.61 | Fill in the blank: logit(p) = ln(___ / ___). | p / (1−p) | — |
| R13 | recall | mcq | 0.66 | Which distribution is assumed for Y given X in logistic regression? | Bernoulli (with success probability sigmoid(Xβ)) | picks "Normal, with mean sigmoid(Xβ)" — Y is a binary 0/1 outcome, modeled as Bernoulli, not a continuous Normal variable → `logistic-regression` |
| R14 | recall | mcq | 0.71 | True/false: sigmoid(0) = 0.5. | True — 1/(1+e⁰) = 1/(1+1) = 0.5 exactly | claims "sigmoid(0) = 0" — that confuses the sigmoid's midpoint with one of its asymptotic limits → `logistic-regression` |
| R15 | recall | short-answer | 0.81 | What is an "odds ratio," in words? | the ratio of the odds of an event occurring under one condition to the odds under another (or comparison) condition | — |
| R16 | recall | mcq | 0.91 | Logistic regression's negative log-likelihood loss function is also known, in the ML sweep, as: | binary cross-entropy loss | picks "mean squared error" — logistic regression's loss is binary cross-entropy, not the squared-error loss used for ordinary linear regression → `logistic-regression` |
| A2 | apply | numeric | 0.86 | Xβ=0 for an observation. Compute P(Y=1\|X). `[verified: 0.5]` | sigmoid(0) = 1/(1+e⁰) = 0.5 | — |
| A3 | apply | numeric | 0.96 | A predictor has coefficient β=0.5. Compute the corresponding odds ratio, exp(β). `[verified: 1.6487]` | exp(0.5) ≈ 1.6487 — a one-unit increase in that predictor multiplies the odds of Y=1 by about 1.65 | — |
| A4 | apply | short-answer | 1.11 | Interpret an odds ratio of 2 for a predictor Xⱼ. | a one-unit increase in Xⱼ is associated with the odds of Y=1 doubling, holding all other predictors constant — not that the probability itself doubles, since probability and odds are related nonlinearly *(required: distinguishes "odds double" from "probability doubles")* | — |
| A5 | apply | numeric | 1.21 | Xβ=−1 for an observation. Compute P(Y=1\|X). `[verified: 0.2689]` | sigmoid(−1) = 1/(1+e¹) ≈ 0.2689 | — |
| A6 | apply | short-answer | 1.31 | Describe how the classification threshold could be adjusted away from 0.5, and why one might do this. | for imbalanced classes (e.g. a rare disease with only 2% positive cases), a threshold below 0.5 can be chosen to increase sensitivity (catch more true positives) at the cost of more false positives, or above 0.5 to reduce false positives at the cost of missing more true positives; the "right" threshold depends on the relative costs of the two types of error in the specific application *(required: names imbalanced classes or asymmetric error costs as the motivation)* | — |
| E2 | explain | short-answer | 1.41 | Why is the sigmoid function's output restricted to the open interval (0,1), never exactly reaching either endpoint? | sigmoid(z)=1/(1+e⁻ᶻ) has e⁻ᶻ strictly positive for any finite z, so the denominator 1+e⁻ᶻ is always strictly greater than 1, making the ratio strictly less than 1; similarly the denominator is always finite for finite z, keeping the ratio strictly greater than 0 — only in the limits z→±∞ does it approach (without reaching) 0 or 1 *(required: the explicit algebraic argument for both bounds)* | — |
| E3 | explain | short-answer | 1.51 | Why does exp(βⱼ) represent a multiplicative effect on the odds, rather than an additive effect on the probability? | the model is linear in the *log-odds*: log-odds = Xβ, so a one-unit increase in Xⱼ adds βⱼ to the log-odds; adding βⱼ to a logarithm is equivalent to multiplying the original (odds) quantity by exp(βⱼ) — exponentiating an additive shift in log-space always produces a multiplicative shift in the original space *(required: the explicit log-to-multiplicative-shift argument)* | — |
| E4 | explain | short-answer | 1.61 | Why does fitting logistic regression require an iterative method like Newton-Raphson instead of a closed-form solution? | the log-likelihood involves the sigmoid function nonlinearly, so setting its gradient (the score equations) to zero produces a system of nonlinear equations in β, unlike OLS's linear normal equations — there is generally no algebraic way to solve nonlinear equations like these in closed form, requiring iterative numerical approximation instead *(required: names the nonlinearity of the score equations as the specific obstacle)* | — |
| E5 | explain | short-answer | 1.81 | Why can the same linear predictor Xβ produce very different marginal effects on probability at different points, even though the model is linear in log-odds? | the sigmoid function's slope (derivative) varies with Xβ — it's steepest near Xβ=0 (P≈0.5) and flattens out near the extremes (P near 0 or 1); a fixed additive change in log-odds therefore translates into a large probability change near the midpoint but a tiny one near the extremes, since the sigmoid curve itself is nonlinear even though the log-odds is linear in X *(required: names the sigmoid's varying slope as the source of the nonuniform marginal effect)* | — |
| T2 | transfer | short-answer | 2.01 | How is logistic regression's negative log-likelihood objective identical to binary cross-entropy loss from the ML sweep? | for a Bernoulli outcome with predicted probability p̂, both quantities are defined as −[y·ln(p̂) + (1−y)·ln(1−p̂)] summed over observations — logistic regression's MLE fitting procedure and minimizing binary cross-entropy loss are the exact same optimization problem stated in two different vocabularies, not merely similar objectives *(required: the explicit formula match, not just "they're related")* | — |
| T3 | transfer | short-answer | 2.11 | How is logistic regression a special case of a single-layer neural network with a sigmoid activation function? | a single neuron computing sigmoid(Xw+b) and trained by minimizing binary cross-entropy loss is mathematically identical to logistic regression's sigmoid(Xβ) model fit by MLE — logistic regression *is* the simplest possible neural network (one layer, one neuron, sigmoid activation), which is why `neural-networks` builds on it directly as a starting point rather than a separate technique *(required: names it as literally the one-neuron special case, not merely an analogy)* | —

*Coverage: 16/6/5/3 — 30 items, 0.06…2.26.*

---

## Probit Regression (`probit-regression`)
*Prereq: Logistic Regression, Normal Distribution · ancestors 35 · b₀ = 1.29*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.29 | Describe probit regression's link function. | the standard Normal CDF Φ(Xβ), used in place of the logistic sigmoid | — |
| R2 | recall | mcq | 0.59 | In practice, logistic and probit regression typically give: | very similar predicted probabilities for most datasets, since the logistic and Normal CDFs have similar shapes | claims they give "identical predictions always" — an overclaim; they're similar, not identical, differing mainly in tail decay speed → `probit-regression` |
| A1 | apply | short-answer | 1.09 | Why are probit's coefficients harder to interpret than logistic's log-odds interpretation? | the Normal CDF Φ has no simple closed-form inverse relationship to a familiar quantity like "odds," so probit coefficients lack the convenient odds-ratio reading logistic regression enjoys, even when the two models' predictions are similar *(required: names the missing simple-inverse relationship)* | — |
| E1 | explain | short-answer | 1.79 | Why might probit be preferred in fields assuming an underlying latent Normal variable? | probit can be derived as Y=1 exactly when a latent Y*=Xβ+ε exceeds a threshold, with ε~Normal(0,1) — giving it a natural threshold-crossing interpretation tied to an underlying continuous, Normally-distributed process (e.g. "utility" in economic choice models) *(required: the explicit latent-variable derivation)* | — |
| T1 | transfer | short-answer | 2.29 | Why is the logistic-vs-probit choice largely a matter of convention, and why might logistic still be the default? | per R2, predictions are typically very similar in practice; logistic regression's easier-to-communicate odds-ratio interpretation makes it the practical default, unless a specific theoretical reason (like E1's latent-variable motivation) favors probit *(required: connects to both R2's similarity finding and E1's specific exception)* | — |
| R3 | recall | short-answer | 0.09 | Fill in the blank: probit's link function is Φ(Xβ), where Φ is ___. | the standard Normal (Gaussian) CDF | — |
| R4 | recall | short-answer | 0.14 | What is the latent variable Y* in the probit derivation? | an unobserved continuous variable, Y*=Xβ+ε, whose sign determines the observed binary outcome Y | — |
| R5 | recall | mcq | 0.19 | True/false: probit's latent-variable formulation assumes ε ~ Normal(0,1). | True — the standard Normal error assumption on the latent variable is exactly what produces the Φ(Xβ) link function | claims "probit assumes ε ~ Normal(0, σ²) with σ² freely estimated" — σ² is fixed at 1 by convention (it's not separately identifiable from β in this model), not freely estimated → `probit-regression` |
| R6 | recall | short-answer | 0.24 | Fill in the blank: Y=1 exactly when Y* ___ the threshold. | exceeds (is greater than) | — |
| R7 | recall | mcq | 0.34 | Unlike logistic regression's convenient odds-ratio interpretation, probit coefficients are typically interpreted via: | marginal effects — the change in P(Y=1\|X) for a small change in a predictor, evaluated at specific covariate values | claims probit coefficients are "interpreted identically via odds ratios, using exp(β) exactly as in logistic regression" — the Normal CDF has no odds-ratio-style closed-form interpretation the way the logistic sigmoid does → `probit-regression` |
| R8 | recall | mcq | 0.39 | True/false: the logistic and standard Normal CDFs have similar but not identical shapes. | True — both are S-shaped (sigmoidal) curves centered at 0.5, but they differ in tail behavior and require a rescaling factor to align closely | claims "the logistic and standard Normal CDFs are mathematically identical functions" — they are similarly shaped S-curves but are genuinely different functions, differing especially in the tails → `probit-regression` |
| R9 | recall | short-answer | 0.44 | What is the key qualitative difference between the logistic and standard Normal CDF in the tails? | the logistic distribution has heavier tails than the standard Normal — it approaches 0 and 1 more slowly as \|Xβ\| grows | — |
| R10 | recall | short-answer | 0.54 | Fill in the blank: probit regression is fit via ___. | maximum likelihood, using an iterative numerical method (e.g. Newton-Raphson), the same general estimation approach as logistic regression | — |
| R11 | recall | short-answer | 0.64 | Which field commonly favors probit due to its latent-variable "utility" interpretation? | economics (particularly discrete choice / random utility models) | — |
| R12 | recall | mcq | 0.69 | True/false: probit's coefficients are typically similar in magnitude to logistic regression's coefficients on the same data, without any rescaling. | False — probit coefficients are typically noticeably smaller in magnitude than logistic ones on the same data, since the standard Normal has a narrower spread than the logistic distribution; a common rescaling is to multiply logistic coefficients by roughly 0.625 (or probit by roughly 1.6) to compare | claims "probit and logistic coefficients from the same fit are directly comparable in magnitude with no adjustment needed" — the two link functions have different inherent scales, requiring a rescaling factor for direct comparison → `probit-regression` |
| R13 | recall | short-answer | 0.74 | What does Φ(0) equal? | 0.5 | — |
| R14 | recall | short-answer | 0.79 | Fill in the blank: probit's decision boundary (P=0.5) occurs where Xβ = ___. | 0 | — |
| R15 | recall | mcq | 0.84 | True/false: probit can be extended to multinomial (more than two categories) outcomes, like logistic regression can. | True — multinomial probit is a well-established extension, analogous to multinomial logistic regression | claims "probit is fundamentally limited to binary outcomes and has no multi-category extension" — multinomial probit exists, paralleling multinomial logistic regression → `probit-regression` |
| R16 | recall | mcq | 0.94 | Which of these is a defining feature of probit's link function? | it is based on the cumulative distribution function of the standard Normal distribution | picks "it is based on the logistic sigmoid function" — that describes logistic regression's link, not probit's Φ-based link → `probit-regression` |
| A2 | apply | numeric | 0.89 | Xβ=0 for an observation, under probit. Compute P(Y=1\|X). `[verified: 0.5]` | Φ(0) = 0.5, the standard Normal CDF at its center | — |
| A3 | apply | numeric | 0.99 | Xβ=1.0 for an observation, under probit. Compute P(Y=1\|X). `[verified: 0.8413]` | Φ(1.0) ≈ 0.8413 | — |
| A4 | apply | short-answer | 1.14 | Why does probit's marginal effect of Xⱼ on P(Y=1\|X) depend on the value of X, rather than being simply βⱼ? | the marginal effect is ∂P/∂Xⱼ = φ(Xβ)·βⱼ, where φ is the standard Normal density (the derivative of Φ); since φ(Xβ) itself varies with Xβ — largest near Xβ=0, smallest at the extremes — the same βⱼ translates into a different actual probability change depending on where X currently sits *(required: names the φ(Xβ)·βⱼ formula and its dependence on Xβ)* | — |
| A5 | apply | numeric | 1.24 | Xβ=−1.0 for an observation, under probit. Compute P(Y=1\|X). `[verified: 0.1587]` | Φ(−1.0) ≈ 0.1587 | — |
| A6 | apply | short-answer | 1.34 | An economist prefers probit for a binary-choice model, citing random utility theory. Explain the connection. | random utility theory models a choice as occurring when the latent, unobserved "utility difference" between two options exceeds zero; if that utility difference is assumed Normally distributed (a common, tractable assumption), the resulting choice probability is exactly Φ(Xβ) — probit's latent-variable derivation *is* a random utility model with Normal noise, giving it a direct theoretical grounding in that field *(required: names the latent-utility-difference-crosses-zero framing explicitly)* | — |
| E2 | explain | short-answer | 1.44 | Why do probit and logit typically give very similar predicted probabilities, despite having different functional forms? | both Φ and the logistic sigmoid are symmetric, S-shaped curves centered at 0.5 that saturate toward 0 and 1; once their scales are roughly aligned (since one has a somewhat wider spread than the other), the two curves overlap closely across most of their range, differing meaningfully only in the extreme tails where data is typically sparse anyway *(required: names both the shared S-shape/symmetry and that differences concentrate in the tails)* | — |
| E3 | explain | short-answer | 1.54 | Why was the standard Normal CDF's lack of a simple closed-form expression historically a genuine computational cost, compared to the logistic sigmoid's closed form? | the logistic sigmoid 1/(1+e⁻ᶻ) can be evaluated directly with elementary functions, while Φ(z) requires numerically integrating the Normal density (or using a tabulated/approximated erf function) — before fast numerical routines were standard, this made logistic regression considerably cheaper to fit and evaluate repeatedly during iterative optimization *(required: names the closed-form-vs-requires-numerical-integration contrast)* | — |
| E4 | explain | short-answer | 1.64 | Why does the latent-variable interpretation make probit a natural fit for "random utility" discrete-choice models in economics? | economic choice theory models decisions as maximizing an unobserved (latent) utility; probit's Y*=Xβ+ε formulation, with Y=1 exactly when that latent quantity crosses a threshold, matches this framework directly — the model isn't merely fit to binary data, it's derived from the same "compare a latent continuous quantity to a threshold" logic economists already use to model choice *(required: names the direct match between the latent-threshold structure and utility-maximization choice theory)* | — |
| E5 | explain | short-answer | 1.94 | Why can't probit coefficients be directly compared in magnitude to logistic regression coefficients from the same data without rescaling? | the two models' latent error terms have different fixed variances by convention (Normal with variance 1 for probit versus a logistic distribution with variance π²/3 for logit); since β is only identified relative to that fixed error scale, the same underlying relationship produces numerically different-sized coefficients purely because of this scale convention difference, not because the models disagree substantively *(required: names the differing fixed error-variance convention as the source of the scale mismatch)* | — |
| T2 | transfer | short-answer | 2.04 | How does the logistic distribution's heavier tails (versus the Normal's) relate to robustness against extreme predictor values? | a heavier-tailed distribution assigns more probability mass to extreme values, so the logistic link's predicted probabilities change more gradually (are less "confident") for extreme Xβ than probit's Normal-based link, which saturates toward 0 or 1 faster; this makes logistic regression's predictions somewhat less sensitive to (more robust against) a few extreme observations pulling Xβ far from the center *(required: connects heavier tails to slower saturation and reduced sensitivity to extreme values)* | — |
| T3 | transfer | short-answer | 2.24 | How does probit's "latent variable crosses a threshold" structure connect to signal detection theory models used in psychology? | signal detection theory models a perceptual decision (e.g. "did I detect the signal?") as occurring when an internal, unobserved sensory strength variable exceeds a decision threshold, with that internal variable typically assumed Normally distributed — structurally identical to probit's Y*=Xβ+ε crossing a threshold to produce Y; probit regression is effectively fitting the same latent-threshold-crossing model that signal detection theory uses to describe perceptual judgments *(required: names the shared latent-continuous-variable-crosses-a-threshold structure)* | —

*Coverage: 16/6/5/3 — 30 items, 0.09…2.29.*

---

## Generalized Linear Model (GLM) (`glm`)
*Prereq: Logistic Regression, Exponential Family · ancestors 36 · b₀ = 1.31*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.31 | Describe the three parts of the GLM framework. | (1) a response distribution from the exponential family; (2) a linear predictor Xβ; (3) a link function connecting the linear predictor to the response's mean | — |
| R2 | recall | mcq | 0.61 | Linear, logistic, and Poisson regression differ primarily in: | which exponential-family distribution is assumed for the response, and correspondingly which link function is used | claims "they're actually the same model with different names" — missing the genuine distributional differences unified only by the shared framework → `glm` |
| A1 | apply | short-answer | 1.11 | Match each response/link pair to its method: (a) Normal, identity link (b) Bernoulli, logit link (c) Poisson, log link. | (a) ordinary linear regression (b) logistic regression (c) Poisson regression (count data) | — |
| E1 | explain | short-answer | 1.81 | Why is restricting GLM to exponential-family responses essential, not incidental? | `exponential-family`'s natural parameter η(θ) is exactly what makes one generic fitting algorithm (iteratively reweighted least squares) work for any member of the family without distribution-specific code — precisely the practical payoff that concept's T1 anticipated in the probability sweep *(required: the direct reuse of that earlier concept's forward-pointer)* | — |
| T1 | transfer | short-answer | 2.31 | Why does understanding the GLM framework let a practitioner adapt quickly to a new outcome type? | once the three-part structure (response distribution, linear predictor, link function) is understood, swapping the response distribution (e.g. Normal to Poisson) is a natural, incremental change rather than learning an entirely new toolset from scratch *(required: names the "swap one component, keep the structure" framing)* | — |
| R3 | recall | short-answer | 0.11 | Fill in the blank: the three components of a GLM are ___, ___, and ___. | a random component (the exponential-family response distribution), a systematic component (the linear predictor Xβ), and a link function connecting them | — |
| R4 | recall | short-answer | 0.16 | What is a "canonical link function"? | the link function that arises naturally from a distribution's exponential-family form, connecting the linear predictor directly to that distribution's natural parameter | — |
| R5 | recall | mcq | 0.21 | True/false: every exponential-family distribution has a unique canonical link. | True — each exponential-family distribution's natural parameter defines exactly one canonical link function for it | claims "any link function can be considered equally canonical for any distribution" — the canonical link is a specific, distribution-determined function, not an arbitrary choice → `glm` |
| R6 | recall | short-answer | 0.26 | Fill in the blank: for Poisson regression, the canonical link is the ___ function. | log (natural logarithm) | — |
| R7 | recall | short-answer | 0.36 | What does "deviance" measure in a GLM, and what does it generalize from OLS? | a measure of how far the fitted model's likelihood is from a perfectly-fitting ("saturated") model; it generalizes SSE's role in ordinary linear regression as a badness-of-fit measure | — |
| R8 | recall | mcq | 0.41 | In GLM terms, what is the link function for ordinary linear regression? | the identity link, g(μ)=μ | picks "the log link" — ordinary linear regression corresponds to the identity link, not the log link (which is Poisson regression's canonical link) → `glm` |
| R9 | recall | short-answer | 0.46 | What algorithm is typically used to fit any GLM? | Iteratively Reweighted Least Squares (IRLS) | — |
| R10 | recall | short-answer | 0.56 | Fill in the blank: for logistic regression, the canonical link is the ___ function. | logit (log-odds) | — |
| R11 | recall | mcq | 0.66 | True/false: a GLM must always use its distribution's canonical link function; no other choice is valid. | False — non-canonical link functions can be used (e.g. probit for a Bernoulli response instead of logit), though the canonical link is often preferred for its convenient estimation properties | claims "using anything other than the canonical link is mathematically invalid for a GLM" — non-canonical links are a valid, sometimes-used alternative, just without the canonical link's estimation convenience → `glm` |
| R12 | recall | short-answer | 0.71 | What does the "systematic component" of a GLM refer to? | the linear predictor, Xβ — the linear combination of predictors and coefficients | — |
| R13 | recall | mcq | 0.76 | Which of these is NOT one of a GLM's three defining components? | a quadratic penalty term on the coefficients | picks a component that belongs to regularized regression, not the basic GLM framework — a GLM's three components are the random component, systematic component, and link function → `glm` |
| R14 | recall | short-answer | 0.86 | Fill in the blank: the general GLM equation is g(E[Y\|X]) = ___. | Xβ, the linear predictor | — |
| R15 | recall | mcq | 0.91 | True/false: GLMs require the response variable to be continuous. | False — GLMs accommodate binary (logistic), count (Poisson), and other discrete or bounded response types, not only continuous ones | claims "a GLM's response must always be a continuous, real-valued variable" — the entire point of the GLM framework is to unify continuous, binary, count, and other response types under one structure → `glm` |
| R16 | recall | short-answer | 0.96 | What family of distributions must a GLM's response distribution belong to? | the exponential family | — |
| A2 | apply | short-answer | 0.81 | Match a Gamma-distributed response to an appropriate GLM use case, and explain why. | modeling positive, continuous, right-skewed data — such as insurance claim amounts or wait times — since the Gamma distribution is defined only on positive values and naturally captures right-skew, unlike the Normal distribution's symmetric, unbounded support *(required: names a concrete positive-skewed-data use case and why Gamma's support fits it)* | — |
| A3 | apply | short-answer | 0.91 | Why does Poisson regression's log link ensure predicted counts μ are always positive? | the model states log(μ)=Xβ, so μ=exp(Xβ); since the exponential function's output is always positive regardless of the sign or magnitude of Xβ, predicted counts can never come out negative — a property an identity link (μ=Xβ directly) would not guarantee *(required: the explicit exp(Xβ)-is-always-positive argument)* | — |
| A4 | apply | short-answer | 1.01 | Describe the basic iterative idea behind IRLS. | at each iteration, IRLS computes weights based on the current fitted values' variance (which, for non-Normal responses, generally depends on the mean), then solves a weighted-least-squares problem using those weights to update the coefficient estimates — repeating until the coefficients converge, similar in spirit to `homoskedasticity`'s WLS but with weights recomputed at every step *(required: names the reweight-then-solve-WLS repeated-iteration structure)* | — |
| A5 | apply | numeric | 1.16 | A Poisson GLM has log(μ)=Xβ=2 for an observation. Compute the predicted mean count μ. `[verified: 7.389]` | μ = exp(2) ≈ 7.389 | — |
| A6 | apply | short-answer | 1.26 | Describe deviance's role in comparing two nested GLMs. | the difference in deviance between a smaller (nested) model and a larger model approximately follows a chi-square distribution (with degrees of freedom equal to the difference in parameter counts) under the null hypothesis that the extra parameters don't help — directly analogous to how `anova`'s F-test compares nested linear models via SSR/SSE differences *(required: names the deviance-difference-follows-chi-square test, and ideally the ANOVA F-test parallel)* | — |
| E2 | explain | short-answer | 1.36 | Why does using the canonical link function simplify a GLM's estimation, connecting to `exponential-family`? | with the canonical link, the linear predictor Xβ is set directly equal to the distribution's natural parameter η(θ), which makes the score equations (the MLE's first-order conditions) take a particularly clean form — Xᵀ(y−μ)=0, structurally identical to OLS's normal equations — a simplification that doesn't occur automatically with a non-canonical link *(required: names the natural-parameter-equals-linear-predictor simplification)* | — |
| E3 | explain | short-answer | 1.46 | Why must a GLM's link function be monotonic and invertible? | the link function connects E[Y\|X] to Xβ in both directions — fitting requires computing predicted means from the linear predictor (needing the inverse link) and the estimation procedure requires that a given mean corresponds to exactly one value of the linear predictor (needing monotonicity), so a non-monotonic or non-invertible link would make the mapping between predictions and the linear predictor ambiguous or undefined *(required: names the need for a well-defined two-way mapping between μ and Xβ)* | — |
| E4 | explain | short-answer | 1.66 | Why does GLM deviance reduce to (a constant times) SSE for the Normal-distribution, identity-link special case? | deviance is defined from the difference in log-likelihood between the fitted model and a perfectly-fitting saturated model; for a Normal response, the log-likelihood's only β-dependent term is exactly −(1/2σ²)Σ(yᵢ−μᵢ)², so the deviance reduces algebraically to a constant multiple of SSE, showing that OLS's familiar SSE is itself just the Normal-response special case of the more general deviance concept *(required: the explicit reduction of the Normal log-likelihood term to SSE)* | — |
| E5 | explain | short-answer | 1.86 | Why is choosing the response distribution generally considered the single most important modeling decision in a GLM, more so than the link function choice? | the response distribution determines the fundamental assumptions about the data's variance structure and support (e.g. counts can't be negative, binary outcomes have bounded variance tied to their mean) — getting this wrong misrepresents the actual data-generating process at a structural level, whereas the link function mainly affects interpretability and estimation convenience, with canonical and non-canonical choices often giving similar predictive fits *(required: contrasts the distribution's structural role with the link's largely interpretive/convenience role)* | — |
| T2 | transfer | short-answer | 2.06 | How does the GLM framework's unification of linear, logistic, and Poisson regression relate to (and differ from) the "general linear model" some textbooks use for ANOVA and regression together? | the "general linear model" (used for ANOVA, regression, and t-tests) is the special case where the response is Normal with the identity link — everything in that older framework is one specific member of the broader GLM family this cluster develops; the "generalized" linear model's contribution is extending that single Normal-identity case to the entire exponential family with an appropriate link, a genuinely larger unification, not just a naming coincidence *(required: names the general-linear-model-as-one-special-case relationship explicitly)* | — |
| T3 | transfer | short-answer | 2.16 | Why might a practitioner need to extend beyond the strict GLM framework when Poisson regression's mean-equals-variance assumption fails (overdispersion)? | the Poisson distribution forces Var(Y)=E(Y) exactly, but real count data frequently shows variance exceeding the mean (overdispersion); standard errors computed under the strict Poisson GLM would then be systematically too small, so practitioners turn to extensions like quasi-Poisson or negative binomial regression that relax this rigid mean-variance link while keeping the same log-linear structure *(required: names overdispersion as the mean≠variance violation and that it specifically corrupts standard errors, not the point estimates)* | —

*Coverage: 16/6/5/3 — 30 items, 0.11…2.31.*

---

## Cox Proportional Hazards Model (`cox-proportional-hazards-model`)
*Prereq: GLM · ancestors 37 · b₀ = 1.32*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.32 | State the Cox model's hazard function. | h(t\|X) = h₀(t)·exp(Xβ) — a baseline hazard (depending only on time) times a proportional factor depending on covariates | — |
| R2 | recall | mcq | 0.62 | The "proportional hazards" assumption means: | the hazard ratio between two individuals with different covariates stays constant over time | claims "the hazard is constant over time for everyone" — that describes a constant baseline hazard specifically, not the proportionality assumption naming the model → `cox-proportional-hazards-model` |
| A1 | apply | short-answer | 1.12 | Why can h₀(t) be left completely unspecified, and what does that make the model? | covariate effects (β) can be estimated without assuming any particular parametric shape for the baseline hazard — this is exactly what makes Cox regression "semi-parametric," a genuine robustness property *(required: names semi-parametric and the robustness payoff)* | — |
| E1 | explain | short-answer | 1.82 | Why does Cox regression handle censored data naturally, unlike ordinary regression? | its likelihood is built specifically to incorporate the information that a censored subject's event time is *known to exceed* some observed value, even without the exact time — rather than discarding or mistreating that partial information the way ordinary regression would *(required: the explicit "known to exceed" partial-information argument)* | — |
| T1 | transfer | short-answer | 2.32 | How does Cox regression still connect to the GLM structure despite being semi-parametric? | it retains a linear predictor Xβ and a log-link-style structure (exp(Xβ)) multiplying the baseline hazard, preserving the "linear predictor through a link function" pattern even while relaxing the fully-parametric response-distribution assumption GLMs otherwise require *(required: names both the retained linear-predictor structure and the relaxed assumption)* | — |
| R3 | recall | short-answer | 0.12 | What does the hazard function h(t\|X) represent? | the instantaneous risk of the event occurring at time t, given survival up to time t and covariates X | — |
| R4 | recall | short-answer | 0.17 | What does exp(βⱼ) represent in the Cox model? | the hazard ratio — the multiplicative factor by which the hazard changes for a one-unit increase in Xⱼ, holding other covariates fixed | — |
| R5 | recall | mcq | 0.22 | True/false: Cox regression estimates the baseline hazard h₀(t) explicitly as part of fitting β. | False — β is estimated via partial likelihood without ever needing to estimate h₀(t)'s specific functional form; that's exactly what makes the model "semi-parametric" | claims "Cox regression requires estimating h₀(t) as a parametric curve before β can be estimated" — the entire point of the partial-likelihood approach is that β is estimated *without* specifying or estimating h₀(t) at all → `cox-proportional-hazards-model` |
| R6 | recall | short-answer | 0.27 | What is "censoring" in survival analysis? | when the event of interest is not observed for a subject within the study period (e.g. the subject is still alive/event-free when the study ends, or drops out) — their exact event time is only known to exceed some value | — |
| R7 | recall | short-answer | 0.37 | What likelihood method is used to estimate β in Cox regression? | the partial likelihood, which depends only on β (and the ranking of event times), not on the unspecified baseline hazard h₀(t) | — |
| R8 | recall | short-answer | 0.42 | Fill in the blank: a hazard ratio greater than 1 indicates increased ___. | risk (hazard) of the event occurring, relative to the reference/baseline covariate value | — |
| R9 | recall | short-answer | 0.47 | What does "semi-parametric" mean for the Cox model? | it combines a parametric part (the linear predictor exp(Xβ)) with a nonparametric, unspecified part (the baseline hazard h₀(t)) | — |
| R10 | recall | mcq | 0.52 | True/false: Cox regression coefficients are interpreted via hazard ratios, similarly to how logistic regression coefficients are interpreted via odds ratios. | True — both use exp(β) as a multiplicative effect on a ratio-type quantity (hazard ratio vs. odds ratio), a shared interpretive pattern across these link-function-based models | claims "Cox regression coefficients are interpreted as direct additive changes in survival time, unlike logistic regression's odds ratios" — Cox coefficients follow the same exp(β)-as-multiplicative-ratio interpretation pattern as logistic regression, not an additive one → `cox-proportional-hazards-model` |
| R11 | recall | short-answer | 0.62 | How is the survival function S(t) related to the (cumulative) hazard? | S(t) = exp(−H(t)), where H(t) is the cumulative hazard up to time t (the integral of h(t) from 0 to t) | — |
| R12 | recall | mcq | 0.67 | Which of the following is a type of censoring commonly handled in survival analysis? | right-censoring (a subject's event time is only known to exceed the observation's end) | picks "measurement error in the covariates" — that's a different data-quality issue, not a form of censoring on the event time itself → `cox-proportional-hazards-model` |
| R13 | recall | short-answer | 0.72 | Fill in the blank: the "proportional hazards" name comes from the fact that the ratio of hazards for two individuals is ___ over time. | constant (unchanging) | — |
| R14 | recall | mcq | 0.77 | True/false: Cox regression requires specifying a parametric distribution for survival times (e.g. exponential, Weibull). | False — that requirement describes fully parametric survival models; Cox regression deliberately avoids it by leaving the baseline hazard's shape unspecified | claims "Cox regression, like a fully parametric survival model, requires choosing a distribution (exponential, Weibull, etc.) for the survival times" — Cox's semi-parametric design specifically avoids that requirement → `cox-proportional-hazards-model` |
| R15 | recall | short-answer | 0.87 | Name a common diagnostic used to check the proportional-hazards assumption itself. | Schoenfeld residuals (testing whether they're correlated with time) | — |
| R16 | recall | short-answer | 0.92 | Name a field/domain that commonly uses Cox regression. | medicine/biostatistics (e.g. clinical trials, time-to-event outcomes), though also used in engineering (failure-time analysis) and economics | — |
| A2 | apply | numeric | 0.82 | A predictor has β=0.4 in a Cox model. Compute the hazard ratio, exp(0.4). `[verified: 1.4918]` | exp(0.4) ≈ 1.4918 — a one-unit increase in that predictor multiplies the hazard by about 1.49 | — |
| A3 | apply | short-answer | 0.97 | Interpret a hazard ratio of 0.5 for a treatment variable (treated=1 vs. untreated=0). | treated individuals face half the instantaneous risk (hazard) of the event, at any given time, compared to untreated individuals with the same values of the other covariates — a protective effect *(required: names the halved-risk interpretation and correctly identifies it as protective)* | — |
| A4 | apply | short-answer | 1.07 | Why does Cox's partial likelihood handle right-censored data (e.g. a patient still event-free at the study's end) naturally, rather than needing to discard those observations? | the partial likelihood is built from the *ranking* of who experiences the event at each observed event time, among those still at risk — a censored subject still contributes to the risk set (they were known to survive at least until their censoring time) even though their exact event time is unknown, so their partial information is used rather than thrown away *(required: names that censored subjects still contribute to the risk set)* | — |
| A5 | apply | numeric | 1.17 | A predictor has β=−0.7 in a Cox model. Compute the hazard ratio, exp(−0.7). `[verified: 0.4966]` | exp(−0.7) ≈ 0.4966 — a one-unit increase in that predictor roughly halves the hazard | — |
| A6 | apply | short-answer | 1.27 | Two patients differ only in one binary covariate (0 vs. 1), with β=0.3 for that covariate. How much more hazard does the "1" group face, relative to the "0" group? `[verified: exp(0.3)≈1.3499]` | the hazard ratio is exp(0.3) ≈ 1.35, so the "1" group faces about 35% higher hazard than the "0" group at any given time, holding all other covariates fixed | — |
| E2 | explain | short-answer | 1.42 | Why is Cox's partial likelihood sufficient to estimate β without ever needing to specify h₀(t)? | the partial likelihood is constructed as a product, over each observed event time, of the probability that the subject who actually experienced the event was the one who did, out of everyone still at risk — the baseline hazard h₀(t) appears identically in every risk-set member's individual hazard at that instant and cancels out of that ratio entirely, leaving an expression that depends only on β *(required: names the h₀(t)-cancels-out-of-the-ratio mechanism)* | — |
| E3 | explain | short-answer | 1.52 | Why does Cox regression's semi-parametric nature make it more robust than a fully parametric survival model, at some cost? | by not assuming any specific functional form for h₀(t), Cox regression is protected from bias if the true baseline hazard shape doesn't match a chosen parametric family (exponential, Weibull, etc.); the cost is that it cannot directly estimate absolute survival probabilities or hazard values without additional steps (e.g. the Breslow estimator) to recover h₀(t) afterward, something a correctly-specified parametric model could do more directly *(required: names both the robustness-to-misspecified-shape benefit and the cost of not directly getting absolute hazard/survival estimates)* | — |
| E4 | explain | short-answer | 1.62 | Why does checking the proportional-hazards assumption matter for interpreting the model correctly? | if the hazard ratio actually changes over time (e.g. a treatment effect that fades), a single fitted β averages over that time-varying pattern, potentially masking an early strong effect or a late reversal — the "hazard ratio of exp(β)" interpretation becomes misleading, since it implies a constant ratio that the data don't actually support *(required: names that a violated assumption makes the single-β interpretation misleading, not just technically imprecise)* | — |
| E5 | explain | short-answer | 1.92 | Why is right-censoring fundamentally different from simply missing data that could just be dropped? | a censored subject's event time is not unknown in the ordinary "missing" sense — it is known to exceed a specific observed value, which is genuine partial information; dropping censored observations would discard that partial information and typically bias the analysis toward subjects who experienced the event earliest (survivorship bias), unlike ordinary missing data which carries no comparable partial information at all *(required: names that censored observations carry known-lower-bound information, unlike truly missing data)* | — |
| T2 | transfer | short-answer | 2.02 | How does Cox's hazard ratio (exp(β)) fit into a broader pattern of "ratio interpretations" seen across this regression cluster? | logistic regression's exp(β) gives an odds ratio, Poisson regression's exp(β) (from `glm`) gives a rate ratio for counts, and Cox regression's exp(β) gives a hazard ratio — all three arise from the identical mathematical fact that a linear model on a log-transformed quantity (log-odds, log-rate, log-hazard) translates an additive shift in β into a multiplicative shift in the original ratio-type quantity, the same log-to-multiplicative argument `logistic-regression`'s E1 makes explicit *(required: names all three ratio interpretations and the shared log-link-produces-multiplicative-effect mechanism)* | — |
| T3 | transfer | short-answer | 2.22 | How does partial likelihood's "leave the nuisance parameter unspecified" strategy connect to the general idea of nuisance parameters in estimation theory? | h₀(t) here plays exactly the role of a nuisance parameter — a quantity that must be accounted for but isn't itself of scientific interest; partial likelihood is one specific technique (alongside profile likelihood and other nuisance-parameter methods from `estimation-theory`) for estimating the parameter of interest (β) while sidestepping the need to estimate the nuisance parameter directly, a general strategy that recurs throughout statistics whenever a model has parameters of differing scientific importance *(required: names h₀(t) as a nuisance parameter and connects to the general nuisance-parameter-elimination strategy)* | —

*Coverage: 16/6/5/3 — 30 items, 0.12…2.32. This is the final concept of the regression domain.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| clustered/repeated-measures data treated as satisfying independence | `mixed-effect-models` |
| logistic regression's coefficients estimated via OLS rather than MLE | `logistic-regression` |
| logistic and probit predictions assumed identical rather than merely similar | `probit-regression` |
| GLM's shared response distributions treated as making the methods identical | `glm` |
| proportional-hazards assumption confused with a constant-hazard assumption | `cox-proportional-hazards-model` |

**Cluster total: 150 items across 5 concepts (30 each).** All numeric claims verified, including the
added recall/apply arithmetic (sigmoid/Φ evaluations, odds and hazard ratios via exp(β), ICC, Poisson
GLM means) checked by direct computation.

---

# Regression: complete

**29 / 29 concepts done.** Total items across all 5 clusters (reg-01…reg-05): **30 + 150 + 180 + 210 +
150 = 720** (reg-02 through reg-05 were expanded from their original ~145-item total so every topic in
those four clusters carries a full 30-item pool; reg-01 is unchanged at 30).
