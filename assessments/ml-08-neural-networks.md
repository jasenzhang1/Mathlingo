# Machine Learning Cluster 8 — Neural Networks

Perceptron, Neural Networks, Backpropagation (3 concepts). Same format as
[Cluster 1](ml-01-foundations.md). All three concepts were added to the graph for this sweep (see
`concepts.md`) — the graph previously had no representation of neural networks at all, despite
`variational-inference-vaes` already assuming one existed.

---

## Perceptron (`perceptron`)
*Prereq: Classification vs Regression · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Describe the perceptron. | a weighted sum of inputs plus a bias term, passed through a threshold (step) function to output a binary classification | — |
| R2 | recall | mcq | −0.45 | The perceptron's learning rule updates weights by: | a simple mistake-driven rule: if a point is misclassified, nudge the weights toward making that point's prediction more correct | claims it "uses calculus/gradients like modern methods" — the original perceptron rule predates and differs from gradient descent, though related in spirit → `perceptron` |
| A1 | apply | numeric | 0.1 | w=(1,−1), b=0. Classify x=(2,3) via sign(w·x+b). `[verified: score=-1]` | w·x+b = 2−3+0 = −1; sign(−1) = negative class | — |
| E1 | explain | short-answer | 0.8 | Describe the perceptron's famous XOR limitation and its historical consequence. | a single perceptron can only learn linearly separable patterns and cannot represent XOR, since no single straight line separates XOR's positive and negative cases; this limitation historically contributed to a decline in neural-network research (the "AI winter") until multi-layer networks were shown to overcome it *(required: names XOR specifically and the historical consequence)* | — |
| T1 | transfer | short-answer | 1.3 | Why is a single perceptron's decision boundary mathematically the same *kind* of object as logistic regression's or a linear SVM's boundary? | all three find some linear separating hyperplane; they differ mainly in *how* that boundary is trained (perceptron's mistake-driven update, logistic regression's likelihood-based gradient descent, SVM's margin maximization), not in the *form* of the boundary itself *(required: names all three training mechanisms as the actual difference)* | — |
| R3 | recall | short-answer | −0.65 | State the perceptron's decision rule in terms of w, x, and b. | predict positive class if w·x + b > 0 (equivalently sign(w·x+b) ≥ 0), else predict the negative class | — |
| R4 | recall | mcq | −0.6 | The activation function used in the original perceptron is: | a step (threshold) function | picks "the sigmoid function" — the sigmoid is used in logistic regression and later neural nets; the classical perceptron uses a hard step function → `perceptron` |
| R5 | recall | short-answer | −0.55 | Fill in the blank: geometrically, w·x + b = 0 defines a ___ separating the two classes. | hyperplane (a line in 2D) | — |
| R6 | recall | mcq | −0.5 | The perceptron convergence theorem guarantees that the mistake-driven learning rule will find a separating hyperplane: | in a finite number of steps, but only if the data is linearly separable | picks "in a finite number of steps regardless of whether the data is separable" — the guarantee requires linear separability; the rule may never converge on non-separable data → `perceptron` |
| R7 | recall | short-answer | −0.45 | What is the role of the bias term b in a perceptron? | it shifts the decision boundary away from passing through the origin, allowing more flexible placement of the separating hyperplane | — |
| R8 | recall | mcq | −0.4 | Who is credited with inventing the perceptron? | Frank Rosenblatt, in 1958 | picks "Geoffrey Hinton" — Hinton is a central figure in later deep learning (e.g. backpropagation popularization), not the original perceptron → `perceptron` |
| R9 | recall | short-answer | −0.35 | True or false: the perceptron produces a calibrated probability of class membership. | False — it produces a hard binary decision (via the step function), not a probability | — |
| R10 | recall | short-answer | −0.3 | Fill in the blank: the perceptron's weight update rule adjusts weights only when a training example is ___. | misclassified | — |
| R11 | recall | mcq | −0.25 | Compared to an SVM, the perceptron's separating hyperplane (when one exists): | is not guaranteed to maximize the margin between classes — it stops at the first separating hyperplane it finds | picks "always coincides exactly with the SVM's maximum-margin hyperplane" — the perceptron has no margin-maximization objective, so its solution generally differs from the SVM's → `perceptron` |
| R12 | recall | short-answer | −0.2 | What does it mean for a dataset to be "linearly separable"? | there exists at least one hyperplane that perfectly separates the two classes with no misclassifications | — |
| R13 | recall | mcq | −0.15 | The step function used as the perceptron's activation is problematic for gradient-based training methods because it is: | non-differentiable (constant almost everywhere, with an undefined derivative at the threshold) | picks "too computationally expensive to evaluate" — the step function is trivially cheap to evaluate; the real obstacle to gradient methods is its lack of a useful derivative → `perceptron` |
| R14 | recall | short-answer | −0.1 | What kind of classification problem is the perceptron originally designed for — binary or multi-class? | binary classification | — |
| R15 | recall | short-answer | −0.05 | Fill in the blank: on non-linearly-separable data, the perceptron's mistake-driven updates may ___ rather than converge. | oscillate indefinitely (never settle on a fixed set of weights) | — |
| R16 | recall | mcq | 0.0 | The "AI winter" following the perceptron's XOR limitation was significantly influenced by: | Minsky and Papert's 1969 book *Perceptrons*, which formally proved the single-layer limitation | picks "a lack of available computing hardware alone" — while hardware limits played a role generally, the specific triggering event tied to the perceptron was Minsky and Papert's theoretical critique → `perceptron` |
| A2 | apply | numeric | 0.05 | w=(2,−1), b=1. Classify x=(3,4) via sign(w·x+b). `[verified: score=3]` | w·x+b = 2(3) + (−1)(4) + 1 = 6 − 4 + 1 = 3; sign(3) = positive class | — |
| A3 | apply | short-answer | 0.15 | A perceptron misclassifies a positive example (true label +1, predicted −1) with input x=(1,2) at learning rate η=0.5. How does the weight vector update? | w ← w + η(y − ŷ)x = w + 0.5(1 − (−1))(1,2) = w + (1, 2), so each weight increases by the scaled input value | — |
| A4 | apply | short-answer | 0.25 | A dataset consists of two classes that form two concentric circles (one inside the other). Can a single perceptron ever perfectly separate them? | no — no hyperplane can separate two concentric circles, since the classes are not linearly separable; the perceptron's mistake-driven update would never converge to a perfect separator here | — |
| A5 | apply | mcq | 0.35 | Two features in a dataset have wildly different scales (one ranges 0–1, the other 0–10,000). What effect is this likely to have on perceptron training? | the update rule's step size will be dominated by the large-scale feature, potentially making training slow or unstable unless features are rescaled | picks "no effect — the perceptron's update rule is scale-invariant" — the update wx+= η(y−ŷ)x, is directly proportional to x's raw scale, so unscaled features distort training → `perceptron` |
| A6 | apply | short-answer | 0.45 | Why would you expect a perceptron trained on the XOR dataset to never stop updating its weights (never converge)? | XOR is not linearly separable, so no weight vector correctly classifies all four points; the mistake-driven rule keeps finding at least one misclassified point at every pass and keeps adjusting weights indefinitely | — |
| E2 | explain | short-answer | 0.85 | Explain precisely why the perceptron's mistake-driven update rule is guaranteed to converge on linearly separable data, at a high level. | the update nudges the weight vector in the direction that reduces the margin of error on the misclassified point; a classical proof (the perceptron convergence theorem) bounds the number of possible mistakes by a quantity involving the margin of the best separating hyperplane and the data's norm, showing the process must terminate in finitely many steps *(required: names that the mistake bound comes from the margin/data-norm relationship, not just "it eventually works")* | — |
| E3 | explain | short-answer | 0.95 | Why does the perceptron's simplicity (a linear model with a hard threshold) make it historically important despite its practical limitations? | it was the first trainable model to demonstrate that a machine could learn a decision rule directly from labeled examples via a simple, automatic update procedure, establishing the basic template — weighted inputs, an activation, and error-driven learning — that all later neural networks build on and generalize *(required: names it as establishing the template later networks build on)* | — |
| E4 | explain | short-answer | 1.05 | Why is "a single perceptron can't learn XOR" a statement about linear separability specifically, not about perceptrons being fundamentally weak learners? | XOR's positive and negative examples are arranged so that no single straight line (or hyperplane) can separate them; this is a geometric property of the *dataset*, and any model restricted to a single linear decision boundary — not perceptrons uniquely — would fail the same way *(required: frames it as a property of linear boundaries generally, not perceptrons specifically)* | — |
| E5 | explain | short-answer | 1.15 | Why doesn't the perceptron's convergence guarantee say anything about how *good* (in margin terms) the found separating hyperplane is? | the proof only guarantees termination once *some* zero-error hyperplane is reached — it says nothing about maximizing the distance between the hyperplane and the nearest points of either class, which is a different, additional objective that only methods like the SVM explicitly optimize for *(required: distinguishes "converges to a separator" from "converges to the best-margin separator")* | — |
| T2 | transfer | short-answer | 1.4 | How does the perceptron's mistake-driven update rule foreshadow stochastic gradient descent, used to train later neural networks? | both update parameters incrementally, one (or a few) examples at a time, moving weights in a direction that reduces error on the current example — the perceptron's rule is essentially SGD applied to a (non-differentiable) 0-1-style loss, and swapping in a smooth loss and activation is exactly what turns it into the gradient-based training used by `neural-networks` *(required: names the incremental, per-example update as the shared mechanism)* | — |
| T3 | transfer | short-answer | 1.5 | Why does stacking multiple perceptron-like units into layers, as `neural-networks` does next, directly address the XOR limitation identified here? | a single perceptron can only represent one linear boundary, but a hidden layer of several such units can each learn a different linear sub-boundary, and combining their outputs in a further layer lets the network represent a nonlinear decision region like XOR's — the historical fix to this exact limitation *(required: connects the multi-layer solution directly back to this concept's XOR failure)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.7…1.5.*

---

## Neural Networks (`neural-networks`)
*Prereq: Perceptron, Matrix Calculus · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.18 | Describe a feedforward neural network. | multiple layers of perceptron-like units, each applying a weighted sum plus a *nonlinear* activation function, with one layer's output feeding the next | — |
| R2 | recall | mcq | 0.12 | Without a nonlinear activation between layers, stacking multiple linear layers is: | mathematically equivalent to a single linear layer | claims it's "more powerful than a single layer" — composing linear functions only ever gives another linear function → `neural-networks` |
| A1 | apply | derivation | 0.62 | Show that two layers with weights W₁, W₂ but no activation between them are equivalent to one layer with weight W₂W₁. `[verified: matrix composition is associative/trivial to check]` | applying W₁ then W₂ to input x gives W₂(W₁x)=(W₂W₁)x — exactly `matrix-multiplication`'s composition-of-linear-transformations fact, directly reused *(required: the explicit (W₂W₁)x derivation)* | — |
| E1 | explain | short-answer | 1.32 | State the Universal Approximation Theorem informally, and explain why deep (many-layer) networks are often preferred over wide-but-shallow ones despite it. | a sufficiently wide network with one hidden layer and a nonlinear activation can approximate any continuous function arbitrarily well; nonetheless, depth can represent certain functions far more efficiently — with exponentially fewer total parameters — than width alone can *(required: both the theorem statement and the efficiency argument for depth)* | — |
| T1 | transfer | short-answer | 1.82 | How does adding one hidden layer let a small network solve the XOR problem a single perceptron can't? | hidden units can learn intermediate, linearly-separable sub-patterns of the input (e.g. each hidden unit detects a different linear combination), and the output layer then combines those intermediate results — a composition of simple linear pieces that together represents a genuinely nonlinear function *(required: the "intermediate linearly-separable sub-patterns, combined" mechanism)* | — |
| R3 | recall | short-answer | −0.13 | Name the three basic types of layers in a standard feedforward neural network. | input layer, one or more hidden layers, and an output layer | — |
| R4 | recall | mcq | −0.08 | An "activation function" in a neural network is applied: | after the weighted sum plus bias at each unit, to introduce nonlinearity | picks "only once, at the very end of the entire network" — activation functions are applied at every hidden unit in every layer, not only at the final output → `neural-networks` |
| R5 | recall | short-answer | −0.03 | Fill in the blank: "MLP" is a common abbreviation for a fully-connected feedforward neural network, standing for ___. | multi-layer perceptron | — |
| R6 | recall | mcq | 0.02 | Name a commonly used nonlinear activation function besides the sigmoid. | ReLU (rectified linear unit) | picks "the identity function, f(x)=x" — the identity function is linear, and using it everywhere collapses the network to a single linear layer, exactly the failure this concept's R2 describes → `neural-networks` |
| R7 | recall | short-answer | 0.07 | What does "fully-connected" (or "dense") mean, describing a layer in a neural network? | every unit in that layer receives input from every unit in the previous layer | — |
| R8 | recall | short-answer | 0.12 | Fill in the blank: the "forward pass" of a neural network refers to computing the network's ___ from a given input. | output (prediction) | — |
| R9 | recall | mcq | 0.17 | A "hidden layer" is called hidden because: | its outputs are internal, intermediate computations not directly observed as the network's final prediction | picks "its weights are kept secret from the model's own training algorithm" — the training algorithm has full access to every weight; "hidden" refers to the layer's outputs not being the final prediction, not to secrecy → `neural-networks` |
| R10 | recall | short-answer | 0.22 | What two things does every unit (neuron) in a layer compute, before the activation function is applied? | a weighted sum of its inputs, plus a bias term | — |
| R11 | recall | mcq | 0.27 | True or false: a "deep" neural network specifically refers to one with many hidden layers. | True | picks "False" — "deep" is precisely the term used for networks with many stacked hidden layers, as opposed to "shallow" (one hidden layer) networks → `neural-networks` |
| R12 | recall | short-answer | 0.32 | What determines the total number of trainable parameters (weights and biases) in a fully-connected layer? | the number of units in that layer times the number of units in the previous layer (for the weights), plus one bias per unit | — |
| R13 | recall | mcq | 0.37 | The Universal Approximation Theorem states that a network with a single hidden layer, given enough hidden units, can: | approximate any continuous function on a compact domain arbitrarily well | picks "learn to perfectly fit any finite dataset with zero training error" — that describes overfitting/memorization on a specific dataset, not the theorem's actual claim about approximating a continuous function → `neural-networks` |
| R14 | recall | short-answer | 0.42 | What is the difference between a "wide" and a "deep" network, holding total parameter count roughly fixed? | wide: few layers, each with many units; deep: many layers, each potentially with fewer units | — |
| R15 | recall | short-answer | 0.47 | Fill in the blank: without any nonlinear activation function, a network with any number of stacked linear layers is mathematically equivalent to a ___ layer. | single linear | — |
| R16 | recall | mcq | 0.52 | The output layer's activation function is often chosen differently from the hidden layers' because: | it needs to match the prediction task's output type (e.g. sigmoid for binary probability, softmax for multi-class, linear/identity for regression) | picks "output layers never use an activation function of any kind" — output layers commonly do use a task-appropriate activation (sigmoid, softmax); only regression outputs typically skip a nonlinear one → `neural-networks` |
| A2 | apply | numeric | 0.57 | A single hidden unit has weights (1, −1), bias 0, and ReLU activation. For input x=(1,2), compute its output. `[verified: 0]` | pre-activation = 1(1) + (−1)(2) + 0 = −1; ReLU(−1) = max(0, −1) = 0 | — |
| A3 | apply | numeric | 0.62 | A 2-unit hidden layer has weights W₁=[[1,−1],[0.5,2]], bias b₁=(0,−1), ReLU activation, feeding a single output unit with weights (1,1) and bias 0.5. For input x=(1,2), compute the network's output. `[verified: 4.0]` | hidden pre-activations: (1·1+(−1)·2+0, 0.5·1+2·2+(−1)) = (−1, 3.5); after ReLU: (0, 3.5); output = 1(0)+1(3.5)+0.5 = 4.0 | — |
| A4 | apply | short-answer | 0.67 | A network's designer removes all activation functions between layers, keeping the same number of layers. What happens to its representational power? | it collapses to being mathematically equivalent to a single linear layer, regardless of how many layers are stacked — it loses all ability to represent nonlinear functions | — |
| A5 | apply | mcq | 0.72 | A network has 10 input units, one hidden layer of 4 units, and 1 output unit, fully connected. How many weight parameters (excluding biases) does it have? | 44 — (10×4) from input to hidden, plus (4×1) from hidden to output | picks "15 — simply the sum of the layer sizes 10+4+1" — parameter count for fully-connected layers is the *product* of consecutive layer sizes, not their sum → `neural-networks` |
| A6 | apply | short-answer | 0.77 | Why might a practitioner prefer a deeper network with fewer units per layer over a shallow, very wide one, for the same total parameter budget? | depth can represent certain functions far more parameter-efficiently than width alone (E1's efficiency point), so a deep-but-narrow architecture can achieve comparable or better expressive power for the same parameter budget | — |
| E2 | explain | short-answer | 1.37 | Why does using a nonlinear activation function at *every* layer, not just the last one, matter for the network's overall expressiveness? | if any layer lacks a nonlinearity, that layer's transformation can be algebraically absorbed into the adjacent linear layers, effectively removing it as a distinct computational step; nonlinearity must appear between every pair of linear transformations to prevent that collapse *(required: explains why even one missing nonlinearity causes collapse of that portion of the network)* | — |
| E3 | explain | short-answer | 1.42 | Explain why the Universal Approximation Theorem, despite guaranteeing a wide-enough shallow network *can* approximate any function, doesn't make deep networks unnecessary in practice. | the theorem is an existence result — it says a sufficiently wide network *could* represent the function, but says nothing about how many units that requires or whether such a network is learnable by gradient descent from finite data; deep networks can represent the same functions with vastly fewer total parameters and often train more effectively in practice *(required: distinguishes "existence" from "practically learnable/efficient")* | — |
| E4 | explain | short-answer | 1.47 | Why does the number of trainable parameters in a fully-connected network grow so quickly with both depth and width? | each fully-connected layer's parameter count is the *product* of that layer's and the previous layer's unit counts, so both stacking more layers and widening any single layer multiplies rather than adds to the total, causing parameter counts to grow rapidly *(required: names the multiplicative, not additive, growth)* | — |
| E5 | explain | short-answer | 1.52 | Why is the choice of output-layer activation function tied directly to the type of prediction task, rather than being an arbitrary architectural choice? | the output activation must map the network's raw pre-activation values into the range and form the task's loss function expects — a probability in [0,1] for binary classification (sigmoid), a probability distribution over classes for multi-class (softmax), or an unrestricted real value for regression (identity) — so it is dictated by the task, not chosen freely *(required: connects the activation choice to matching the task's required output range)* | — |
| T2 | transfer | short-answer | 1.87 | How does the "weighted sum plus nonlinearity" structure of each unit here connect directly back to `perceptron`, the previous concept? | each individual unit in a neural network is structurally identical to a perceptron — a weighted sum of inputs plus a bias, passed through an activation function — the network's innovation is stacking many such units in layers and replacing the perceptron's hard step function with a differentiable activation *(required: names the identical weighted-sum-plus-activation structure)* | — |
| T3 | transfer | short-answer | 1.92 | Why does the need to train all these weights and biases directly set up the problem `backpropagation` is designed to solve, the next concept in this cluster? | a network like this one can have thousands or millions of parameters spread across many layers, and none of them are directly observed as "correct" — only the final output's error is known; an efficient way to compute how every individual weight, however deep in the network, should change to reduce that final error is exactly the problem backpropagation solves *(required: frames the parameter-count-and-depth problem as motivating an efficient gradient computation)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.18…1.92.*

---

## Backpropagation (`backpropagation`)
*Prereq: Neural Networks, Gradient Descent · ancestors 16 · b₀ = 0.92*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.08 | Describe backpropagation. | an efficient algorithm computing the gradient of a network's loss with respect to every weight, using the chain rule, propagating backward from the output layer toward the input | — |
| R2 | recall | mcq | 0.22 | Backpropagation is: | the method used to *compute* the gradients that gradient descent then uses to update weights | treats it as "an alternative to gradient descent" — the two are complementary, not competing: one computes, the other applies → `backpropagation` |
| A1 | apply | short-answer | 0.72 | Why does computing the gradient for a weight in an early layer require the chain rule applied through every subsequent layer? | that early weight's effect on the final loss is mediated by everything that happens to its output as it passes through every later layer — the chain rule composes each layer's local derivative to trace that full path *(required: the "mediated through every later layer" argument)* | — |
| E1 | explain | short-answer | 1.42 | Why is backpropagation efficient compared to numerically perturbing each weight individually? | it computes *all* the gradients in roughly the cost of one forward pass, by carefully reusing intermediate computations through the chain rule — a naive perturb-and-recompute approach would need a separate forward pass per weight, astronomically slower for networks with millions of parameters *(required: the "one forward pass for all gradients" efficiency argument)* | — |
| T1 | transfer | short-answer | 1.92 | Describe the vanishing gradient problem and why it makes early layers of very deep networks learn slowly. | the chain rule multiplies many terms together across layers; if each term has magnitude less than 1, the product shrinks exponentially with depth, leaving early-layer gradients extremely small and their weights nearly frozen — motivating techniques like residual connections or activation functions (e.g. ReLU instead of sigmoid) chosen to mitigate the shrinkage *(required: the exponential-product mechanism and at least one named mitigation)* | — |
| R3 | recall | short-answer | −0.03 | What are the two named passes involved in training a network with backpropagation? | a forward pass (computing the output/loss) and a backward pass (computing gradients via backpropagation) | — |
| R4 | recall | mcq | 0.02 | Backpropagation computes the gradient of the loss with respect to: | every weight and bias in the network | picks "only the weights in the final output layer" — backpropagation's entire purpose is propagating gradients back through *every* layer, not just the last one → `backpropagation` |
| R5 | recall | short-answer | 0.07 | Fill in the blank: backpropagation relies fundamentally on the ___ rule from calculus. | chain | — |
| R6 | recall | mcq | 0.12 | Backpropagation requires the network's activation functions to be: | differentiable (almost everywhere), so a local gradient can be computed at each unit | picks "monotonic" — monotonicity is a separate property some, but not all, useful activations have; differentiability (not monotonicity) is what the chain rule needs → `backpropagation` |
| R7 | recall | short-answer | 0.17 | What does "propagating backward" mean in backpropagation? | gradients are computed starting from the output layer's loss and worked back toward the input layer, layer by layer | — |
| R8 | recall | short-answer | 0.22 | Fill in the blank: after backpropagation computes the gradients, an ___ like gradient descent actually updates the weights. | optimizer | — |
| R9 | recall | mcq | 0.27 | Once gradients are computed by backpropagation, a basic gradient descent weight update looks like: | w ← w − η·(∂L/∂w), where η is the learning rate | picks "w ← w + (∂L/∂w), with no learning rate" — a proper update needs a learning rate to control step size and moves *against* the gradient (subtracting) to reduce the loss → `backpropagation` |
| R10 | recall | short-answer | 0.32 | What must be defined before backpropagation can compute any gradients at all? | a loss function measuring the discrepancy between the network's prediction and the true target | — |
| R11 | recall | mcq | 0.37 | "Automatic differentiation," the general technique underlying modern backpropagation implementations, is: | a systematic way to compute exact derivatives of any differentiable computation graph by applying the chain rule to elementary operations | picks "a way to numerically approximate gradients using finite differences" — that describes numerical differentiation, a different (much slower and less exact) technique that backpropagation specifically avoids → `backpropagation` |
| R12 | recall | short-answer | 0.42 | What is a "computational graph," in the context of backpropagation? | a graph representation of a sequence of operations (e.g. matrix multiplies, additions, activations) used to compute the network's output from its input and parameters | — |
| R13 | recall | short-answer | 0.47 | Fill in the blank: at each unit, backpropagation multiplies the "local" gradient by the gradient flowing in from ___. | later layers (the layer(s) closer to the output, i.e. the upstream gradient) | — |
| R14 | recall | mcq | 0.52 | "Mini-batch" gradient descent, commonly paired with backpropagation, computes gradients: | averaged over a small subset (batch) of training examples at each update step | picks "using the entire dataset for every single weight update, always" — that describes full-batch gradient descent specifically, not the mini-batch variant, which uses a small subset per step → `backpropagation` |
| R15 | recall | short-answer | 0.57 | True or false: backpropagation itself updates the network's weights. | False — backpropagation only computes the gradients; a separate optimizer (e.g. gradient descent) performs the actual weight update | — |
| R16 | recall | short-answer | 0.62 | What is the "exploding gradient" problem, as the mirror image of the vanishing gradient problem? | when the chain rule's multiplied terms have magnitude greater than 1 across many layers, gradients grow exponentially with depth instead of shrinking, leading to unstable, very large weight updates | — |
| A2 | apply | numeric | 0.67 | A tiny network computes ŷ = wx with x=2, w=3, target y=5, and loss L=(ŷ−y)². Compute ∂L/∂w. `[verified: 4]` | ŷ=6; ∂L/∂ŷ = 2(ŷ−y) = 2(1) = 2; ∂ŷ/∂w = x = 2; chain rule: ∂L/∂w = 2×2 = 4 | — |
| A3 | apply | short-answer | 0.72 | A network uses the step function as its hidden-layer activation. Why can't standard backpropagation train it? | the step function's derivative is zero everywhere except at the discontinuity (where it's undefined), so the chain rule produces a zero (or undefined) gradient almost everywhere, giving the optimizer no useful signal to update weights | — |
| A4 | apply | short-answer | 0.77 | A 100-layer network shows gradients in its first layer that are essentially zero after backpropagation, while the last layer's gradients are healthy. What problem does this describe? | the vanishing gradient problem — the chain rule's product of many small per-layer terms across 100 layers has shrunk to nearly zero by the time it reaches the first layer | — |
| A5 | apply | mcq | 0.82 | A practitioner switches a deep network's hidden activations from sigmoid to ReLU and observes training becomes noticeably faster and more stable in early layers. What does this most directly illustrate? | ReLU's derivative (1 for positive inputs) doesn't shrink toward zero the way sigmoid's does, reducing the vanishing gradient problem in the chain-rule product | picks "ReLU eliminates the need for backpropagation entirely" — backpropagation is still required to compute gradients through a ReLU network; only the *severity* of vanishing gradients changes → `backpropagation` |
| A6 | apply | short-answer | 0.87 | Why would computing every weight's gradient by numerically perturbing it one at a time be impractical for a network with 10 million parameters? | it would require roughly 10 million separate forward passes (one per parameter perturbed), compared to backpropagation's roughly one forward and one backward pass to get *all* gradients at once — astronomically more expensive | — |
| E2 | explain | short-answer | 1.47 | Why does backpropagation's efficiency depend specifically on reusing intermediate values computed during the forward pass? | many of the local derivatives needed during the backward pass (e.g. an activation's derivative at the value it output) are functions of quantities already computed going forward; storing and reusing them, rather than recomputing them from scratch, is exactly what keeps the backward pass's cost comparable to the forward pass *(required: names the reuse of forward-pass intermediates as the source of the efficiency)* | — |
| E3 | explain | short-answer | 1.52 | Explain why the chain rule, applied layer by layer, correctly computes the gradient for a weight buried deep in an early layer. | that early weight influences the loss only indirectly, through a long chain of transformations by every subsequent layer; the multivariate chain rule expresses the total derivative as a product of each layer's local derivative along that entire path, so multiplying all those local derivatives together correctly accounts for the weight's full, indirect effect on the loss *(required: explains the full-indirect-effect argument, not just "the chain rule applies")* | — |
| E4 | explain | short-answer | 1.57 | Why is understanding backpropagation as "gradient computation" rather than "learning" itself an important distinction? | backpropagation only produces the values ∂L/∂w for every weight; it makes no decision about how much or in what manner to change the weights — that decision (the update rule, the learning rate, momentum, adaptive scaling) belongs entirely to the separate optimizer, so the same computed gradients could be fed to very different optimizers with different learning outcomes *(required: separates gradient computation from the optimizer's decision role)* | — |
| E5 | explain | short-answer | 1.62 | Why does the exploding gradient problem tend to cause visibly unstable training (e.g. loss suddenly spiking to NaN), while the vanishing gradient problem instead causes training to silently stall? | exploding gradients produce enormous weight updates that can overshoot far past any reasonable parameter values, causing numerical overflow or wild oscillation; vanishing gradients instead produce updates so tiny that early-layer weights barely move at all, so training proceeds smoothly in appearance but makes essentially no progress on those layers *(required: contrasts the visible-instability vs. silent-stall failure modes)* | — |
| T2 | transfer | short-answer | 1.97 | How does the vanishing gradient problem identified here motivate the residual (skip) connections used in architectures covered later in the ML domain? | a residual connection adds the layer's input directly to its output, so the gradient flowing backward through that connection has a path with local derivative exactly 1, avoiding the shrinking multiplicative chain that causes vanishing gradients through purely sequential layers *(required: names the identity/skip-path mechanism as directly countering the multiplicative shrinkage)* | — |
| T3 | transfer | short-answer | 2.02 | Why does backpropagation's reliance on the chain rule make it a direct algorithmic descendant of the same `matrix-calculus` gradient techniques used to derive the normal equations in the regression domain? | both use the same underlying principle — computing how a scalar objective (squared error; the network's loss) changes with respect to many parameters by systematically applying calculus rules through a sequence of operations; backpropagation is that same gradient-computation idea applied recursively through a deep, composed sequence of layers rather than one linear transformation *(required: names the shared gradient-of-a-composed-function principle)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.08…2.02.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| perceptron learning rule confused with gradient-based methods | `perceptron` |
| stacked linear layers assumed more expressive than one linear layer | `neural-networks` |
| backpropagation and gradient descent treated as alternatives rather than complementary | `backpropagation` |
| vanishing gradients attributed to a single bad layer rather than the multiplicative chain | `backpropagation` |

**Cluster total: 90 items across 3 concepts (30 each).** All numeric/derivation claims verified,
including the direct reuse of `matrix-multiplication`'s composition-of-linear-transformations fact to
prove that un-activated stacked layers collapse to one, the worked forward-pass example
(hidden pre-activations (−1, 3.5), ReLU'd to (0, 3.5), output 4.0), and the worked chain-rule gradient
∂L/∂w = 4 for ŷ=wx, x=2, w=3, y=5.
