# Cluster 9 — Statistics Foundations

Population vs Sample → Sample Variance (6 concepts). This is the first cluster from the `statistics`
domain proper, rather than `probability` — the transition point where the bank starts asking not just
"what is true of a distribution" but "what can be learned about one from data." Same format as
[Cluster 1](foundations-of-probability.md).

---

## Population vs Sample (`population-vs-sample`)
*Root · ancestors 0 · b₀ = −0.50*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.5 | Define "population" and "sample," and state which one a statistical analysis actually observes. | population = the complete set of units of interest; sample = the (usually much smaller) subset actually observed; conclusions about the population are drawn *from* the sample | — |
| R2 | recall | mcq | −1.3 | A "census" is: | an attempt to measure the *entire* population | calls it "a type of hypothesis test," conflating a data-collection strategy with an inferential procedure → `population-vs-sample` |
| A1 | apply | short-answer | −0.7 | A quality engineer wants the average lifespan of a batch of 10,000 bulbs. Why is testing the *entire* batch (a census) an especially bad idea here? | testing lifespan is destructive — you run each bulb until it fails; testing all 10,000 destroys the entire batch you were trying to sell, so sampling isn't just convenient, it's essential *(required: names the destructive-testing mechanism specifically)* | — |
| A2 | apply | short-answer | −0.55 | A pollster wants opinions of "American voters," but that population changes daily (people turn 18, move, pass away). Why does this make even *defining* the population for a poll surprisingly tricky? | the population is a moving, fuzzy target requiring a snapshot-in-time definition (e.g. "registered voters as of a specific date"), and different reasonable definitions (registered vs. eligible vs. likely voters) can yield genuinely different results — the population definition is itself a methodological choice *(required)* | treats "American voters" as an unambiguous, fixed group → `population-vs-sample` |
| E1 | explain | short-answer | 0.0 | Give an example of a population that isn't a group of people at all, and that is inherently hypothetical or infinite. | e.g. "all possible outcomes of rolling this die forever," or "all possible measurements a scale could produce" (used to model measurement error) — a population is any well-defined collection the researcher wants to draw conclusions about, not limited to a finite, enumerable human group *(required: a genuinely infinite/hypothetical example, not just a large finite group)* | — |
| E2 | explain | short-answer | 0.2 | State, in one or two sentences, the fundamental logical structure of statistical inference using the population/sample distinction. | we never observe the population directly; we use properties of the *sample* (statistics) to make inferences about properties of the *population* (parameters), with quantifiable uncertainty about how good that inference is *(required)* — this is exactly what `parameter-vs-statistic` names precisely next | — |
| T1 | transfer | short-answer | 0.5 | A tech company wants to know what percentage of "all possible users" would click a new button — but no fixed population of "all possible users" exists (the app doesn't exist for most of them yet). How does A/B testing handle this? | it redefines the practical population as something like "all future visits to the app under conditions like today's," and the sample is the visitors who happen to arrive during the test window — reframing an intractable question about all possible future users into a tractable statistical question about a well-defined, if somewhat artificial, population *(required)* | treats A/B testing as somehow avoiding the population/sample framework rather than redefining the population pragmatically → `population-vs-sample` |
| R3 | recall | short-answer | −1.6 | What symbol conventionally denotes population size, and what symbol denotes sample size? | N (population size) and n (sample size) | — |
| R4 | recall | mcq | −1.55 | Which best defines a "sampling frame"? | the actual list or mechanism from which sample units are physically drawn — which may not perfectly match the true population | defines it as "the population itself," conflating the theoretical population with the practical list used to draw from it → `population-vs-sample` |
| R5 | recall | short-answer | −1.5 | True or false: a sample can be larger than the population it is drawn from. | false — a sample is by definition a subset of the population, so it can be at most as large as the population (equal only for a census) | — |
| R6 | recall | short-answer | −1.45 | Fill in the blank: the ____ is the complete set of units about which conclusions are to be drawn. | population | — |
| R7 | recall | mcq | −1.4 | Which of the following is an example of a finite population? | all students currently enrolled at a given university | picks "all possible outcomes of an unending random process," which is an infinite/hypothetical population, not a finite one → `population-vs-sample` |
| R8 | recall | short-answer | −1.4 | Define "unit of observation" in the population/sample framework. | the individual member of the population (or sample) on which a single measurement or data point is collected — e.g. one patient, one household, one bulb | — |
| R9 | recall | mcq | −1.35 | "Sampling error" is best defined as: | the difference between a sample statistic and the true population parameter, arising purely from studying only part of the population | defines it as "a mistake made while collecting data," confusing sampling error (inherent to sampling itself) with a measurement or recording mistake → `population-vs-sample` |
| R10 | recall | short-answer | −1.3 | True or false: a population must always consist of a finite, countable number of units. | false — a population can be infinite or hypothetical, e.g. "all possible measurements a scale could ever produce" | — |
| R11 | recall | short-answer | −1.3 | Fill in the blank: a sample that accurately reflects the characteristics of the population it was drawn from is called a ____ sample. | representative | — |
| R12 | recall | mcq | −1.25 | "Sampling bias" refers to: | a systematic tendency for certain population members to be over- or under-represented in the sample | describes it as "random variation in which units happen to be selected," confusing systematic bias with ordinary random sampling variability → `population-vs-sample` |
| R13 | recall | short-answer | −1.2 | State the single main practical reason a sample is used instead of studying the entire population. | it is usually too costly, slow, or logistically impossible to measure every unit in the population, so a subset is studied instead | — |
| R14 | recall | mcq | −1.2 | The relationship between a population and a sample is best described as: | the sample is a subset of the population | describes the population as "a larger sample," inverting which term is the subset of which → `population-vs-sample` |
| R15 | recall | short-answer | −1.15 | True or false: using a very large sample eliminates sampling error entirely. | false — sampling error shrinks toward zero as the sample approaches the full population (a census), but is eliminated only at an actual census, not merely by "large" | — |
| R16 | recall | short-answer | −1.1 | Fill in the blank: an attempt to measure every single unit in the population is called a ____. | census | — |
| A3 | apply | short-answer | −0.65 | A researcher has commute-time data for 50 of a factory's 5,000 employees. Identify the population and the sample in this study. | population: all 5,000 factory employees; sample: the 50 employees whose commute times were actually measured | — |
| A4 | apply | mcq | −0.6 | A school with 2,000 students wants its average GPA, and pulls official transcripts for all 2,000 students. Is this a sample or a census? | a census — every unit of the defined population was measured, not a subset | calls it "a very large sample," failing to recognize that measuring 100% of the population makes it a census by definition → `population-vs-sample` |
| A5 | apply | short-answer | −0.5 | A clinical trial enrolls 200 of an estimated 50,000 eligible patients nationwide. State the approximate values of N and n for this study. | N ≈ 50,000 (the eligible population); n = 200 (the enrolled sample) | — |
| A6 | apply | short-answer | −0.45 | A city government wants the opinions of "all city residents" on a new policy. Give two concrete reasons a full census of every resident is impractical here. | e.g. prohibitive cost and time to reach every resident; the resident population changes continuously (births, moves, deaths) faster than a census could be completed; some residents are unreachable or unwilling to respond *(required: two genuinely distinct practical reasons)* | — |
| E3 | explain | short-answer | 0.05 | Explain why the choice of how to define the population can itself change what counts as "sampling bias" in a study. | bias is defined relative to a target population — a phone survey is "biased" against non-phone-owners only if the population is defined as *all* adults; if instead the population is defined as "adults with a phone," the same sample is no longer biased on that dimension — the population definition sets the standard the sample is judged against *(required)* | — |
| E4 | explain | short-answer | 0.1 | Distinguish sampling error from non-sampling error (e.g. a miscalibrated scale), and explain why only the former shrinks as sample size grows. | sampling error comes purely from observing a subset rather than the whole population, and shrinks toward zero as n grows toward N; non-sampling error (measurement mistakes, non-response, data entry errors) is a flaw in *how* each observation was collected, and collecting more flawed observations does not fix it — it can even accumulate *(required: both halves, with the explicit reason more data doesn't help non-sampling error)* | — |
| E5 | explain | short-answer | 0.25 | Explain why a well-defined, genuinely finite population can still require care in specifying its sampling frame. | the true population and the practical list used to draw the sample (the frame) can diverge — e.g. a frame built from a phone directory omits unlisted numbers even though the target population is "all households"; the frame is only ever an approximation to the population, and gaps between them are a source of bias distinct from ordinary sampling error *(required)* | — |
| T2 | transfer | short-answer | 0.6 | A machine learning engineer trains a model on one million images scraped from the internet and calls this "the entire population of images." Critique this framing using the population/sample distinction. | the scraped million images are themselves a *sample* — a non-random, availability-driven subset of the true (effectively infinite, constantly growing) population of all images that could ever exist or be photographed; calling it "the population" hides the fact that this specific collection method (what's publicly posted online, in whatever languages/regions/platforms the scraper covered) can introduce exactly the kind of systematic sampling bias R12 describes *(required: identifies the million images as a sample, and names at least one plausible source of bias in how it was collected)* | treats a large, conveniently-available dataset as automatically equivalent to "the population" simply because of its size → `population-vs-sample` |
| T3 | transfer | short-answer | 0.75 | An epidemiologist studying a rare disease can only access medical records of patients who were formally diagnosed. Explain why "all diagnosed patients" is not the same as the disease's true population, and why the distinction matters for the study's conclusions. | the true population of interest is "everyone who has the disease," which includes undiagnosed and misdiagnosed cases; "all diagnosed patients" is itself a biased sample of that true population, systematically excluding people with mild/atypical presentations, no healthcare access, or cases caught too late — conclusions drawn from diagnosed-only records may not generalize to the true population the researcher actually cares about *(required: explicitly distinguishes the two populations and names a plausible systematic exclusion)* | treats "all diagnosed patients" as if it were already the full population of interest, with no acknowledgment of the gap → `population-vs-sample` |

*Coverage: 16/6/5/3 — 30 items, −1.6…0.75.*

---

## Parameter vs Statistic (`parameter-vs-statistic`)
*Prereq: Population vs Sample · ancestors 1 · b₀ = −0.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.15 | Define "parameter" and "statistic," stating which is fixed and which is random before data collection. | parameter describes the population — a fixed (usually unknown) number; statistic describes the sample, computed from data — *random* before the sample is drawn (a different sample gives a different value), fixed once actually computed | — |
| R2 | recall | mcq | −0.9 | The standard notational convention is: | Greek letters for parameters, Latin letters (often hatted) for statistics — e.g. μ vs X̄, p vs p̂ | reverses the convention → `parameter-vs-statistic` |
| A1 | apply | short-answer | −0.35 | Classify each as parameter or statistic: (a) the true defect proportion across an entire factory's annual output (b) the defect proportion found in a batch of 50 items you inspected (c) the average height of all trees in a national park (d) the average height from a 200-tree survey of that park. | (a) parameter (b) statistic (c) parameter (d) statistic | — |
| A2 | apply | short-answer | −0.2 | "A statistic is random before you observe data, fixed once you have it." Explain this using X̄ specifically: is it random or fixed *before* drawing a sample of n=30? *After* computing it? | before: X̄ is a random variable — a function of the not-yet-realized sample, which would come out differently on a hypothetical repeat; after: it's one specific fixed number (e.g. "72.3 inches") *(required: both halves, with the "hypothetical repeat" framing)* — this duality is the crux of `sampling-distribution` later | — |
| E1 | explain | short-answer | 0.35 | Why is treating a statistic as a random variable, before data collection, essential to doing anything useful with statistics? | only by treating X̄ as random can we discuss its *distribution* (mean, variance, shape), which is exactly what lets us quantify how much to trust a specific computed value as an estimate — without this framing there is no way to attach any notion of precision or uncertainty to an estimate at all *(required)* | — |
| E2 | explain | short-answer | 0.5 | Distinguish "the sample mean is close to the population mean" from "the sample mean equals the population mean." Why does conflating them matter? | a statistic is an estimate carrying inherent sampling variability — a specific X̄=72.3 is a best guess with quantifiable uncertainty (formalized later via standard error and confidence intervals), not an assertion that it exactly equals the true μ; treating a statistic as if it *were* the parameter, with no acknowledgment of estimation error, is a root cause of statistical overconfidence *(required)* | — |
| T1 | transfer | short-answer | 0.85 | A news article reports "the poll shows 52% support," with no margin of error. Using the parameter/statistic distinction, explain what the 52% *is*, and why omitting the margin of error makes the report fundamentally incomplete. | 52% is a *statistic*, estimating the true, unknown *parameter* (population support); since the statistic is a random quantity that would come out somewhat differently with a different sample, reporting it alone implicitly suggests certainty about the parameter rather than an estimate with quantifiable sampling variability *(required: names the 52% as a statistic explicitly, not just "an estimate")* | treats the 52% as directly informative about the population with no need for further qualification → `parameter-vs-statistic` |
| R3 | recall | short-answer | −1.6 | Give the Greek-letter symbol conventionally used for the population mean, and the Latin symbol for the sample mean. | μ (population mean) and X̄ (sample mean) | — |
| R4 | recall | mcq | −1.55 | Which symbol denotes the population proportion? | p | picks p̂, which denotes the *sample* proportion (the statistic), not the population parameter → `parameter-vs-statistic` |
| R5 | recall | short-answer | −1.5 | True or false: a parameter's true numeric value is generally known exactly by the researcher. | false — a parameter is typically fixed but *unknown*; the entire point of computing a statistic is to estimate it | — |
| R6 | recall | short-answer | −1.45 | Fill in the blank: the population standard deviation is denoted ____, while the sample standard deviation is denoted ____. | σ (population); s (sample) | — |
| R7 | recall | mcq | −1.4 | A "hat" symbol (e.g. p̂, β̂) conventionally indicates: | an estimate of a parameter, computed from sample data | claims the hat denotes "the true population value," inverting what the hat notation signals → `parameter-vs-statistic` |
| R8 | recall | short-answer | −1.35 | Define "estimator" as distinct from "estimate." | an estimator is the general rule/formula (e.g. X̄=(1/n)ΣXᵢ) used to produce a guess at a parameter; an estimate is the specific numeric value that formula produces from one particular sample | — |
| R9 | recall | mcq | −1.3 | Which of these is a parameter, not a statistic? | the true average height of every adult in a country | picks "the average height measured from a survey of 1,000 adults," which is a statistic (computed from a sample), not a parameter → `parameter-vs-statistic` |
| R10 | recall | short-answer | −1.25 | True or false: two different samples from the same population will generally produce the same value of a given statistic. | false — different samples generally give different statistic values (sampling variability); this is exactly why a statistic is treated as random before the data is drawn | — |
| R11 | recall | short-answer | −1.2 | Fill in the blank: the population variance is denoted ____ (using Greek notation), while the sample variance is denoted ____. | σ² (population); s² (sample) | — |
| R12 | recall | mcq | −1.15 | The number of parameters in a population is: | fixed at however many the population's distribution actually has (e.g. two for Normal: μ and σ²) — it does not grow with sample size | claims "the number of parameters grows as you collect more data," confusing the fixed number of population parameters with the number of statistics one could choose to compute → `parameter-vs-statistic` |
| R13 | recall | short-answer | −1.1 | State the general goal of "estimation" using the parameter/statistic vocabulary. | using a statistic, computed from a sample, to produce a best guess (with quantifiable uncertainty) at an unknown population parameter | — |
| R14 | recall | mcq | −1.05 | β̂₁ in a fitted regression line is: | a statistic — an estimate of the true (unknown) population slope β₁, computed from this particular sample of data | describes β̂₁ as "the true underlying relationship," conflating the fitted estimate with the population parameter it estimates → `parameter-vs-statistic` |
| R15 | recall | short-answer | −1.0 | True or false: it is possible, in principle, to know a parameter's exact value without ever needing to estimate it. | true — if you could conduct a full census of the population, the "statistic" computed from the census data *is* the parameter, with zero sampling error; estimation is needed specifically because a census is usually impractical | — |
| R16 | recall | short-answer | −0.95 | Fill in the blank: the branch of statistics devoted to producing good guesses at unknown parameters from sample data is called ____. | estimation (theory) | — |
| A3 | apply | short-answer | −0.3 | A pharmaceutical company wants "the true cure rate" of a new drug across all future patients who might take it, and estimates it from a trial of 300 patients. Identify the parameter and the statistic. | parameter: the true (unknown) cure rate across the whole population of potential patients; statistic: the observed cure rate among the 300 trial patients | — |
| A4 | apply | mcq | −0.25 | A weather agency reports "average rainfall this year was 42.1 inches," computed from every rain gauge reading at every station all year. Is 42.1 a parameter or a statistic here? | a parameter — since every relevant measurement (the full population of readings) was used, 42.1 is the exact (population) value, not an estimate from a subset | calls it "a statistic," missing that a full accounting of every reading makes this a population quantity, not a sample-based estimate → `parameter-vs-statistic` |
| A5 | apply | short-answer | −0.15 | A poll estimates p̂=0.61 support for a candidate from 800 respondents. What is the corresponding population parameter, in words and symbol? | the true (unknown) proportion of the entire voting population who support the candidate, denoted p | — |
| A6 | apply | short-answer | −0.1 | Two separate samples of size 40 are drawn from the same population and each produces a different X̄. Explain, using estimator vs. estimate vocabulary, why this is expected rather than a sign of error. | X̄ is an estimator — a random variable before data is observed — so different samples (different realizations of that randomness) naturally produce different specific estimates; this variability is normal sampling variability, not a mistake *(required: uses "estimator" and "estimate" correctly)* | — |
| E3 | explain | short-answer | 0.4 | Explain why a parameter is often called a "fixed but unknown" quantity, while a statistic is called "known but random" (before it's random, and known after). | the parameter has one true value determined by the population itself, which does not change depending on who studies it — it is unknown only because we lack full access to the population; a statistic, by contrast, would take different values across hypothetically repeated samples (random), yet once computed from actual data, its value is simply read off (known) *(required: both halves, with the "fixed" vs. "random" framing explicit)* | — |
| E4 | explain | short-answer | 0.45 | Explain why the number of possible statistics one could compute from a sample is essentially unlimited, while the number of parameters describing a given population's distribution is fixed. | a statistic is just any function of the sample data — the mean, the median, the maximum, a custom weighted combination — so there is no limit to how many can be defined; a parameter, by contrast, is a property *of the population's underlying distribution itself* (e.g. Normal has exactly two: μ, σ²), which does not change no matter how creatively you compute statistics from a sample *(required)* | — |
| E5 | explain | short-answer | 0.55 | Explain why "the statistic is unbiased for the parameter" is a claim about behavior across repeated sampling, not a claim about any single computed value. | unbiasedness means E[statistic]=parameter — a statement about the *average* value the statistic would take across many hypothetical repeated samples; it says nothing about how close any one specific computed estimate is to the true parameter on that occasion, which can differ from the parameter by a lot even for an unbiased estimator *(required: distinguishes the long-run averaging claim from a single-sample guarantee)* | — |
| T2 | transfer | short-answer | 0.95 | A/B testing dashboards often display "conversion rate: 3.2%" for a website variant with no further qualification. Using the parameter/statistic distinction, explain what's being hidden by presenting the number this way, and what a more complete presentation would include. | 3.2% is a statistic computed from the visitors who happened to see this variant during the test — it estimates the true (unknown) long-run conversion rate parameter for that variant; presenting it bare hides that a different batch of visitors would likely have produced a somewhat different number, so a complete presentation would pair it with a measure of sampling uncertainty (e.g. a confidence interval or standard error) rather than the bare point estimate *(required: names 3.2% as a statistic estimating an unknown parameter, and names the missing uncertainty measure)* | — |
| T3 | transfer | short-answer | 1.05 | A hiring manager sees that a candidate's take-home coding test score (one data point) exceeds the "average score of past successful hires" (a statistic from many past hires) and concludes the candidate is definitely above the true population standard for success. Critique this reasoning using the parameter/statistic distinction. | the "average score of past successful hires" is itself only a *statistic*, an estimate of some underlying population parameter (the true average ability associated with success), not the parameter itself — comparing one new data point against an estimate that carries its own sampling uncertainty, and treating the comparison as definitive, ignores that both the statistic and the new observation are subject to variability the manager isn't accounting for *(required: identifies the past-hires average as a statistic with its own uncertainty, not a fixed known standard)* | treats a statistic computed from past data as if it were the true, exactly-known population parameter → `parameter-vs-statistic` |

*Coverage: 16/6/5/3 — 30 items, −1.6…1.05.*

---

## Data Types (`data-types`)
*Prereq: Population vs Sample · ancestors 1 · b₀ = −0.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.15 | Name the four basic data types (nominal, ordinal, discrete quantitative, continuous quantitative) with one example each. | nominal: eye color; ordinal: T-shirt size; discrete: number of children; continuous: height | — |
| R2 | recall | mcq | −0.9 | Which is *ordinal* (not nominal)? (a) blood type (A,B,AB,O) (b) T-shirt size (S,M,L,XL) (c) zip code. | (b) — a genuine order | picks zip code, mistaking a numeric-*looking* label for ordered quantitative data → `data-types` |
| A1 | apply | short-answer | −0.35 | Classify: (a) number of siblings (b) 1–5 star satisfaction rating (c) exact weight in kg (d) favorite programming language. | (a) discrete (b) ordinal (c) continuous (d) nominal | — |
| A2 | apply | short-answer | −0.2 | Why does computing the "average" zip code, or the "average" favorite programming language, produce a meaningless number even though the arithmetic itself is valid? | nominal categories have no inherent numeric ordering or magnitude — any numeric labels (like zip codes) are just labels, and averaging labels doesn't correspond to a meaningful "typical value"; the meaningful summary for nominal data is a mode or a set of proportions, not a mean *(required)* | — |
| E1 | explain | short-answer | 0.35 | Why does ordinal data (e.g. a 1–5 rating) sit in an awkward middle ground between nominal and quantitative? | it has order (unlike nominal) but the *gaps* between categories aren't necessarily equal or meaningful (unlike quantitative data) — is the "1→2" difference really the same amount of satisfaction change as "4→5"? This ambiguity is exactly what makes computing a mean of ordinal data a debated practice *(required)* | — |
| E2 | explain | short-answer | 0.5 | Give a concrete example of a statistical method that requires quantitative (not categorical) data, and explain what goes wrong applying it to categorical data anyway. | e.g. a standard deviation or a t-test assumes numeric, ordered, meaningfully-spaced data; applying either to nominal categories produces a number that is computable but uninterpretable — this is exactly the mechanism connecting data type to test selection, covered later in the hypothesis-testing cluster *(required: a real method named, not just "some tests don't work")* | — |
| T1 | transfer | short-answer | 0.85 | A researcher codes "Disagree"=1, "Neutral"=2, "Agree"=3 and reports the average response, 2.3, as "the average opinion." Critique this practice, and suggest a more defensible summary. | treating a 1-2-3 coding as truly quantitative (equally-spaced, meaningful gaps) is an *assumption*, not a fact — the "2.3" implicitly assumes "Neutral" sits exactly halfway between "Disagree" and "Agree" in some meaningful sense; a median response, or the full distribution of category percentages, avoids this assumption *(required: names the equal-spacing assumption specifically, and offers a concrete alternative)* | accepts the 2.3 average at face value without questioning the equal-spacing assumption → `data-types` |
| R3 | recall | short-answer | −1.6 | Name the two broad super-categories that nominal and ordinal data both fall under, and that discrete and continuous data both fall under. | nominal and ordinal are both *categorical* (qualitative) data; discrete and continuous are both *quantitative* (numerical) data | — |
| R4 | recall | mcq | −1.55 | Which is an example of *discrete* quantitative data? | the number of emails received in a day | picks "exact height in centimeters," which is continuous (can take any value in a range), not discrete (countable, gaps between possible values) → `data-types` |
| R5 | recall | short-answer | −1.5 | True or false: continuous data can, in principle, take any value within some range. | true — continuous data is measured, not counted, so between any two possible values another is always possible (limited only by measurement precision) | — |
| R6 | recall | short-answer | −1.45 | Fill in the blank: data with categories that have no inherent order (e.g. blood type, eye color) is called ____ data. | nominal | — |
| R7 | recall | mcq | −1.4 | Which best distinguishes discrete from continuous quantitative data? | discrete data is countable, with gaps between possible values (e.g. 0,1,2,...); continuous data can take any value in an interval | claims the difference is "whether the data is written as a whole number or a decimal," a superficial formatting distinction rather than the true countable-vs-any-value structural one → `data-types` |
| R8 | recall | short-answer | −1.35 | Define "categorical data" and give one example not already used in this cluster's examples. | data that places observations into named categories, e.g. type of pet owned (dog, cat, bird, none) | — |
| R9 | recall | mcq | −1.3 | Number of goals scored in a soccer match is best classified as: | discrete quantitative | classifies it as continuous, missing that goal counts are whole numbers with genuine countable gaps between them → `data-types` |
| R10 | recall | short-answer | −1.25 | True or false: ordinal data has meaningfully equal gaps between its categories. | false — ordinal data has a genuine order, but the size of the gap between adjacent categories is not guaranteed to be equal or even meaningful | — |
| R11 | recall | short-answer | −1.2 | Fill in the blank: a variable that can take on any of a small, fixed number of unordered category labels is ____ data. | nominal | — |
| R12 | recall | mcq | −1.15 | Exact reaction time measured in milliseconds is: | continuous quantitative data | classifies it as discrete, mistaking fine-grained decimal measurement precision for genuine countability → `data-types` |
| R13 | recall | short-answer | −1.1 | State the four basic data types in order from "least" to "most" numerically structured. | nominal, ordinal, discrete quantitative, continuous quantitative | — |
| R14 | recall | mcq | −1.05 | A Likert-scale item ("Strongly Disagree" to "Strongly Agree") is most naturally classified as: | ordinal data | classifies it as nominal, missing the genuine order (Strongly Disagree < Disagree < ... < Strongly Agree) present in a Likert scale → `data-types` |
| R15 | recall | short-answer | −1.0 | True or false: the number of children in a family is continuous data. | false — it is discrete (whole-number counts; you can't have 2.3 children) | — |
| R16 | recall | short-answer | −0.95 | Fill in the blank: a variable measured on a scale where equal numeric differences correspond to equal real differences, and which supports meaningful ratios (e.g. weight, income), is sometimes further distinguished as ____ data. | ratio (data) | — |
| A3 | apply | short-answer | −0.3 | Classify: (a) marital status (b) Olympic medal (gold/silver/bronze) (c) number of cars owned (d) exact time to complete a race. | (a) nominal (b) ordinal (c) discrete (d) continuous | — |
| A4 | apply | mcq | −0.25 | A dataset records "customer satisfaction" as Low/Medium/High. Which summary statistic is most defensible without further justification? | the mode, or the percentage of customers in each category | picks "the arithmetic mean," which silently assumes Low/Medium/High are equally spaced numerically — an unjustified assumption for ordinal data → `data-types` |
| A5 | apply | short-answer | −0.15 | A survey records shoe size (e.g. 8, 8.5, 9, 9.5, ...). Is this discrete or continuous, and why might reasonable people disagree? | technically discrete — shoe sizes only come in fixed half-size increments, so not every real number is possible; however it's sometimes treated as approximately continuous because it's a proxy for an underlying continuous quantity (foot length) *(required: names the fixed-increment structure as the reason it's discrete)* | — |
| A6 | apply | short-answer | −0.1 | A dataset codes "region" as 1=North, 2=South, 3=East, 4=West. Explain why computing the mean of this column would be meaningless. | the numbers 1–4 are arbitrary labels for a nominal variable with no inherent order or magnitude — "2.5" (the mean of North and East, say) corresponds to no meaningful region, since the numeric coding carries no quantitative information at all *(required)* | — |
| E3 | explain | short-answer | 0.4 | Explain why the same underlying real-world quantity (e.g. age) can sometimes be recorded as continuous, sometimes as discrete, and sometimes even as ordinal or nominal. | it depends how the variable is *measured and recorded*, not on some fixed property of "age" itself: exact age in days is continuous-ish; age in completed years is discrete; age bracket ("18-25", "26-35", ...) is ordinal; "minor vs. adult" is nominal (or even binary) — the data type is a property of the measurement scheme, not an intrinsic fact about the concept being measured *(required: the explicit "depends on measurement scheme" framing, with at least two of the four levels illustrated)* | — |
| E4 | explain | short-answer | 0.45 | Explain why nominal data has strictly *less* structure than ordinal data, which has strictly less structure than quantitative data — in terms of what operations are valid at each level. | nominal supports only equality comparisons (same category or not); ordinal adds a valid ordering (greater/less than) on top of that; quantitative adds meaningful numeric differences (and for ratio data, meaningful ratios) on top of *that* — each level is a strict superset of the operations the level below it supports, which is why summary statistics valid at a higher level (like the mean) are not automatically valid at a lower one *(required: the nested-operations argument)* | — |
| E5 | explain | short-answer | 0.55 | A dataset stores "temperature in Celsius." Explain why this is quantitative but arguably *not* ratio data (no meaningful "twice as hot"), even though it's clearly continuous. | Celsius has an arbitrary zero point (0°C is water's freezing point, not "no temperature"), so ratios are not meaningful: 20°C is not "twice as hot" as 10°C in any physical sense, even though the interval-level differences (20−10=10 degrees) are meaningful; this shows continuous/quantitative and ratio-vs-interval are separate distinctions *(required: the arbitrary-zero argument specifically)* | — |
| T2 | transfer | short-answer | 0.95 | A machine learning model one-hot-encodes a nominal "city" column into several 0/1 columns before fitting a linear regression. Explain, using the data-types framework, why this encoding choice is necessary rather than just feeding in an arbitrary integer code for each city. | feeding raw integer codes (city 1, city 2, ...) into a model that treats numbers quantitatively would falsely imply an ordering and meaningful numeric distances between cities that don't exist for nominal data — one-hot encoding avoids this by representing each category as its own independent binary indicator, preserving the "no inherent order or magnitude" structure that nominal data actually has *(required: explicitly connects the encoding choice back to nominal data's lack of order/magnitude)* | — |
| T3 | transfer | short-answer | 1.05 | A hospital dashboard displays patient pain level (self-reported 0–10 scale) as if it were a ratio-scale continuous variable, computing "average pain reduction as a percentage." Critique this, using the data-types framework. | a 0–10 pain scale is genuinely ordinal (more pain = higher number) but its status as truly equally-spaced, ratio-scale data is questionable — is the jump from 2 to 3 the same amount of pain increase as 7 to 8? Is "pain level 8" really "twice" pain level 4 in any physically meaningful sense? Treating it as ratio data to compute a "percentage reduction" imports an equal-spacing and true-zero assumption the scale doesn't clearly support *(required: questions both the equal-spacing and the true-zero assumptions specifically)* | treats a subjective ordinal rating scale as automatically ratio-scale quantitative data because it's reported using numbers → `data-types` |

*Coverage: 16/6/5/3 — 30 items, −1.6…1.05.*

---

## Sampling Methods (`sampling-methods`)
*Prereq: Population vs Sample · ancestors 1 · b₀ = −0.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.15 | Define simple random sampling (SRS). | every subset of the population of the given sample size is equally likely to be chosen | — |
| R2 | recall | mcq | −0.9 | The key property distinguishing "probability sampling" (SRS, stratified, cluster) from convenience sampling is: | every unit has a *known, nonzero* probability of selection under probability sampling | claims probability sampling is "always cheaper" — often the *opposite* is true (convenience sampling is typically the cheap option) → `sampling-methods` |
| A1 | apply | short-answer | −0.35 | A university wants precise opinions across the whole student body, worried that opinions vary a lot by major. Stratified sampling (by major) or SRS? | stratified — proportional representation from each major typically gives a *more precise* whole-population estimate than SRS when opinions genuinely differ a lot across strata (high between-stratum variance), directly echoing `law-of-total-variance`'s decomposition *(required: the between-stratum variance connection)* | — |
| A2 | apply | short-answer | −0.2 | A national survey randomly selects 50 towns, then interviews *every* household in each. Name the method, and its main advantage and disadvantage. | cluster sampling; advantage: far cheaper/more logistically feasible (interviewers visit only 50 towns); disadvantage: typically *less* precise than SRS at the same sample size, since households within the same town tend to resemble each other, reducing the effective information gained per additional household | — |
| E1 | explain | short-answer | 0.35 | Using within/between-group variance intuition, explain why stratified sampling tends to beat SRS when strata differ a lot, while cluster sampling tends to underperform SRS when units within a cluster are similar. | stratified sampling eliminates between-stratum variance from the sampling error by construction, sampling proportionally from every stratum; cluster sampling concentrates the sample within highly similar (low within-cluster-variance, but replicated) groups, wasting some of the sample's effective information *(required: both halves, in `law-of-total-variance` terms)* | — |
| E2 | explain | short-answer | 0.5 | Why can't convenience sampling be analyzed with the same confidence-interval/hypothesis-test theory built for probability sampling? | that theory (sampling distributions, standard errors) is built on every unit having a *known* selection probability; convenience samples provide no such thing, so the direction and magnitude of any resulting bias is fundamentally unknown and uncorrectable by formula alone *(required)* | — |
| T1 | transfer | short-answer | 0.85 | A 1930s pollster sampled 2+ million respondents from phone directories and car registrations and still predicted the wrong election outcome. Explain why a *massive* convenience sample can be far less trustworthy than a much smaller probability sample, and why "more data" didn't fix it. | phone/car ownership at the time correlated with wealth, which correlated with political preference — the sampling frame itself systematically excluded a large, non-random swath of voters; the resulting huge sample gives a precisely-estimated *wrong* number, because this kind of bias doesn't shrink with sample size the way random sampling error does *(required: names the correlated-exclusion mechanism, and that bias ≠ sampling error in how it scales with n)* — the real 1936 *Literary Digest* poll disaster | assumes a larger convenience sample is automatically more trustworthy than a smaller one → `sampling-methods` |
| R3 | recall | short-answer | −1.6 | Name the four main probability sampling methods mentioned in this concept. | simple random sampling, stratified sampling, cluster sampling, and (implicitly) systematic sampling | — |
| R4 | recall | mcq | −1.55 | Systematic sampling refers to: | selecting every k-th unit from a list, after a random starting point | describes it as "selecting whichever units are most convenient to reach," confusing systematic sampling with convenience sampling → `sampling-methods` |
| R5 | recall | short-answer | −1.5 | True or false: in stratified sampling, the population is divided into subgroups (strata), and a sample is drawn from within each subgroup. | true | — |
| R6 | recall | short-answer | −1.45 | Fill in the blank: in cluster sampling, entire pre-existing groups (clusters) are randomly selected, and then ____ within each selected cluster is/are sampled or measured. | every unit (or a further sub-sample of units) | — |
| R7 | recall | mcq | −1.4 | "Convenience sampling" is best defined as: | selecting whichever units happen to be easiest to access, with no randomization | describes it as "a type of probability sampling that favors low-cost units," misclassifying convenience sampling as a probability-sampling method when it is precisely the non-probability alternative → `sampling-methods` |
| R8 | recall | short-answer | −1.35 | Define "sampling frame" in the specific context of choosing a sampling method. | the actual accessible list or mechanism (e.g. a phone directory, a roster of employees) from which units will be drawn, which may or may not match the true target population exactly | — |
| R9 | recall | mcq | −1.3 | Stratified sampling is most useful when: | the population's subgroups differ substantially from each other on the variable of interest | claims it's useful "whenever the population is very large," missing that the actual justification is heterogeneity between subgroups, not population size alone → `sampling-methods` |
| R10 | recall | short-answer | −1.25 | True or false: cluster sampling generally requires visiting/measuring every member of the population, just organized by cluster. | false — cluster sampling's main appeal is that only the *selected* clusters need to be visited, not every cluster in the population | — |
| R11 | recall | short-answer | −1.2 | Fill in the blank: sampling in which every unit has a known, nonzero chance of selection is broadly called ____ sampling. | probability | — |
| R12 | recall | mcq | −1.15 | Which sampling method is illustrated by "randomly select 30 of 400 hospital wards, then interview every patient in each selected ward"? | cluster sampling | calls it "stratified sampling," missing that strata are sampled *from* (partially), while here entire clusters are taken wholesale → `sampling-methods` |
| R13 | recall | short-answer | −1.1 | State the primary practical advantage that makes cluster sampling attractive despite its precision cost. | it is typically far cheaper and more logistically feasible than SRS, since data collectors only need to travel to/access a limited number of clusters rather than units scattered across the whole population | — |
| R14 | recall | mcq | −1.05 | An online poll that anyone can click to answer is an example of: | convenience sampling (or self-selection sampling) | classifies it as "systematic sampling," missing that there is no fixed selection interval or randomization mechanism at all — it's whoever chooses to click → `sampling-methods` |
| R15 | recall | short-answer | −1.0 | True or false: simple random sampling guarantees that every possible *subset* of the given sample size is equally likely to be chosen, not just every individual unit. | true — this is the actual defining property of SRS, slightly stronger than "every unit has an equal chance" | — |
| R16 | recall | short-answer | −0.95 | Fill in the blank: sampling that draws respondents based on who volunteers or opts in, rather than by any randomization, is a form of ____ sampling. | convenience (or self-selection) | — |
| A3 | apply | short-answer | −0.3 | A researcher wants to survey factory workers about workplace safety and randomly selects 20 of the factory's 60 shifts, interviewing every worker present during each selected shift. Name the sampling method used. | cluster sampling — shifts are the clusters, randomly selected, with every worker within a selected cluster included | — |
| A4 | apply | mcq | −0.25 | A retailer wants customer satisfaction broken out precisely by store location, since satisfaction is known to vary a lot store-to-store, and samples proportionally from every store. This is: | stratified sampling | calls it "cluster sampling," missing that here *every* store contributes to the sample (a stratum, sampled from), rather than only a randomly selected subset of stores (clusters, taken wholesale) → `sampling-methods` |
| A5 | apply | short-answer | −0.15 | An auditor numbers all 500 invoices from 1 to 500 and selects every 10th one starting from a randomly chosen number between 1 and 10. Name this method and identify one risk specific to it. | systematic sampling; risk: if there's a hidden periodic pattern in how the invoices are ordered (e.g. every 10th invoice happens to be a large end-of-month order), the fixed interval could systematically over- or under-represent that pattern | — |
| A6 | apply | short-answer | −0.1 | A researcher wants an unbiased sample of a company's 10,000 employees but only has access to the internal directory of the 6,000 employees who opted into a company newsletter. Identify the sampling-frame problem here. | the sampling frame (the 6,000 newsletter subscribers) does not match the true target population (all 10,000 employees) — any sample drawn from this frame can only ever represent newsletter subscribers, systematically excluding the 4,000 non-subscribers, whatever way they might differ | — |
| E3 | explain | short-answer | 0.4 | Explain why systematic sampling can behave almost like SRS in practice, but can fail badly if the population list has periodicity. | with no hidden pattern, a fixed-interval systematic sample behaves much like a random sample in expectation; but if the list itself cycles with a period that lines up with the sampling interval (e.g. sampling every 7th day of retail data, always landing on a Sunday), the sample becomes badly non-representative of the full cycle *(required: names the periodicity-alignment failure mode specifically)* | — |
| E4 | explain | short-answer | 0.45 | Explain why "probability sampling" is the property that makes formal margin-of-error and confidence-interval calculations valid, while convenience sampling has no such guarantee. | the standard error/confidence-interval machinery is derived assuming each unit's selection probability is known, which lets sampling variability be quantified from first principles; convenience samples provide no known selection probabilities at all, so there is no valid mathematical basis for attaching a margin of error to a convenience-sample estimate, however large it is *(required)* | — |
| E5 | explain | short-answer | 0.55 | Compare stratified and cluster sampling on the *same* axis: what do they do oppositely regarding "which subgroups end up represented in the sample"? | stratified sampling guarantees *every* subgroup (stratum) is represented, proportionally or otherwise, by drawing from each one; cluster sampling guarantees only a *subset* of subgroups (clusters) is represented at all, since entire clusters not selected contribute nothing to the sample — stratified maximizes coverage across subgroups, cluster sacrifices it for cost *(required: the explicit contrast on subgroup coverage)* | — |
| T2 | transfer | short-answer | 0.95 | A researcher studying rare-disease patients uses "snowball sampling" — recruiting initial patients, then asking each to refer others they know with the disease. Is this probability sampling? What specific bias might it introduce? | no — referral-based recruitment gives no unit a known selection probability, so it's a form of non-probability (convenience-adjacent) sampling; it likely oversamples patients who are socially well-connected within the patient community and undersamples isolated patients, potentially biasing estimates of disease severity, treatment experience, or outcomes toward the more socially engaged subgroup *(required: identifies it as non-probability sampling, and names the social-connectivity bias mechanism)* | — |
| T3 | transfer | short-answer | 1.05 | An education researcher wants to estimate average test scores across a country's schools. They have a budget for only 40 schools' worth of travel and testing. Compare, in terms of precision and cost, drawing 40 schools via cluster sampling (test all students at each) versus SRS of individual students spread across many schools. | cluster sampling (40 whole schools) is far cheaper for a fixed testing budget, since travel/logistics scale with the number of *schools* visited, not students tested; but because students within the same school tend to resemble each other (similar curricula, teacher quality, socioeconomic context), the effective information gained per student is lower than an equally-sized SRS spread across many more, more heterogeneous schools — the researcher is trading precision for a feasible budget, echoing this concept's own advantage/disadvantage pattern for cluster sampling *(required: names both the cost mechanism and the within-cluster-similarity precision cost)* | assumes cluster sampling of the same total sample size as SRS delivers equivalent statistical precision, ignoring within-cluster correlation → `sampling-methods` |

*Coverage: 16/6/5/3 — 30 items, −1.6…1.05.*

---

## Sample Mean (`sample-mean`)
*Prereq: Parameter vs Statistic, Expectation · ancestors 12 · b₀ = 0.78*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.22 | Define X̄, and state E[X̄] and Var(X̄) in terms of μ, σ². | X̄=(1/n)ΣXᵢ; E[X̄]=μ; Var(X̄)=σ²/n | — |
| R2 | recall | mcq | 0.08 | As n increases, Var(X̄): | decreases toward 0 | picks "stays the same," missing the n in the denominator entirely → `sample-mean` |
| A1 | apply | numeric | 0.58 | A population has σ=12. Find Var(X̄) and SD(X̄) for n=36. `[verified: 4, 2]` | Var(X̄)=144/36=4; SD(X̄)=2 | forgets to divide the *variance* by n before taking a square root, or divides the SD by n directly → `sample-mean` |
| A2 | apply | numeric | 0.7 | Same population: how large must n be to get SD(X̄) ≤ 1? `[verified: n≥144]` | 12/√n ≤ 1 ⟹ √n ≥ 12 ⟹ n ≥ 144 | — |
| E1 | explain | short-answer | 1.28 | Explain why Var(X̄)=σ²/n is exactly the mathematical content behind the Law of Large Numbers, and why the *rate* (SD shrinks as 1/√n, not 1/n) matters. | this is precisely the Chebyshev-based proof of the Weak LLN from `law-of-large-numbers`, now viewed through X̄'s own variance; the square-root scaling means quadrupling n only *halves* the standard error, not quarters it — a genuinely important, slightly counterintuitive rate *(required: the explicit 1/√n scaling, not just "more data helps")* | assumes SD(X̄) shrinks proportionally to 1/n → `sample-mean` |
| E2 | explain | short-answer | 1.4 | What does "the sampling distribution of X̄" mean, and how does it differ from the distribution of a single observation Xᵢ? | it describes how X̄ itself would vary *across hypothetical repeated samples* of size n — each fresh sample gives a (generally) different X̄, and the sampling distribution describes that variability; this is a fundamentally different object from the distribution of one raw data point *(required)* | conflates the sampling distribution of X̄ with the population distribution of a single Xᵢ → `sample-mean` |
| T1 | transfer | short-answer | 1.78 | A firm wants to *halve* its survey's standard error while keeping everything else fixed. Using σ/√n, how much more data is needed — twice as much? Why might this surprise a manager expecting "half the error needs twice the effort"? `[verified: needs 4x, since σ/√(4n)=σ/(2√n)]` | to halve SD(X̄), n must *quadruple* (σ/√(4n) = σ/(2√n), exactly half); this square-root law means diminishing returns on data collection, with real budget implications *(required: the explicit quadrupling, not just "more than double")* | assumes halving the error requires only doubling the sample size, matching linear intuition → `sample-mean` |
| R3 | recall | short-answer | −1.3 | State the formula for X̄ given observations X₁,...,Xₙ. | X̄ = (1/n)ΣXᵢ | — |
| R4 | recall | mcq | −1.15 | X̄ is described as a random variable rather than a fixed number because: | its value depends on which specific sample was drawn, which is itself random | claims X̄ is random "because the population itself is random," rather than because *which sample happened to be drawn* is random → `sample-mean` |
| R5 | recall | short-answer | −1.05 | True or false: E[X̄] equals the population mean μ, regardless of n. | true — E[X̄]=μ for any n≥1, by linearity of expectation | — |
| R6 | recall | short-answer | −0.95 | Fill in the blank: the formula for the standard error of X̄ is SD(X̄) = ____. | σ/√n | — |
| R7 | recall | mcq | −0.85 | Which of these correctly states Var(X̄)? | σ²/n | states Var(X̄)=σ² (forgetting the division by n entirely, confusing the variance of one observation with the variance of the mean) → `sample-mean` |
| R8 | recall | short-answer | −0.75 | Define "sampling distribution of X̄" in one sentence. | the probability distribution describing how X̄ itself would vary across hypothetically repeated samples of the same size n from the same population | — |
| R9 | recall | mcq | −0.65 | As n→∞, Var(X̄) approaches: | 0 | claims Var(X̄) approaches σ² as n→∞, confusing the shrinking variance of the *mean* with the fixed population variance → `sample-mean` |
| R10 | recall | short-answer | −0.55 | True or false: for a fixed n, a population with a larger σ produces a larger SD(X̄). | true — SD(X̄)=σ/√n increases directly with σ, holding n fixed | — |
| R11 | recall | short-answer | −0.45 | Fill in the blank: X̄ being an unbiased estimator of μ means E[X̄] = ____. | μ | — |
| R12 | recall | mcq | −0.35 | If n=1 (a "sample" of a single observation), Var(X̄) equals: | σ², the full population variance | claims Var(X̄) is always smaller than σ² regardless of n, missing that at n=1, Var(X̄)=σ²/1=σ² exactly → `sample-mean` |
| R13 | recall | short-answer | −0.28 | State, in words, what "the mean of the sampling distribution of X̄" equals. | it equals the population mean μ — this is exactly the statement E[X̄]=μ | — |
| R14 | recall | mcq | −0.2 | Doubling n changes SD(X̄) by a factor of: | 1/√2 ≈ 0.707 | claims doubling n halves SD(X̄) (a factor of 1/2), applying a linear rather than square-root scaling → `sample-mean` |
| R15 | recall | short-answer | −0.1 | True or false: the sampling distribution of X̄ is always exactly Normal, for any population and any n. | false — it is exactly Normal only if the population itself is Normal; otherwise it is only *approximately* Normal for large n, via the Central Limit Theorem | — |
| R16 | recall | short-answer | 0.0 | Fill in the blank: the shrinking of Var(X̄) as n grows is the formal content behind the ____ (a law from earlier in this bank). | Law of Large Numbers | — |
| A3 | apply | numeric | 0.6 | A population has σ=20. Find Var(X̄) and SD(X̄) for n=25. `[verified: 16, 4]` | Var(X̄)=400/25=16; SD(X̄)=4 | — |
| A4 | apply | numeric | 0.65 | A population has σ=15. Find SD(X̄) for n=225. `[verified: 1]` | SD(X̄)=15/√225=15/15=1 | forgets to take the square root of n, computing 15/225 instead → `sample-mean` |
| A5 | apply | short-answer | 0.72 | A sample of n=50 is drawn from a population with μ=200. State E[X̄] without further calculation, and explain why no calculation is needed. | E[X̄]=200 — this follows immediately from E[X̄]=μ for any n, so no further arithmetic involving n or σ is required *(required: names the E[X̄]=μ fact as the reason)* | — |
| A6 | apply | numeric | 0.78 | Population σ=9. How large must n be for SD(X̄) to be at most 0.5? `[verified: n≥324]` | 9/√n ≤ 0.5 ⟹ √n ≥ 18 ⟹ n ≥ 324 | — |
| E3 | explain | short-answer | 1.3 | Explain why X̄ being unbiased (E[X̄]=μ) does *not* by itself guarantee that any single computed X̄ is close to μ. | unbiasedness is a statement about the *average* value of X̄ across repeated sampling, not about the spread of that distribution; a single sample could still produce an X̄ far from μ if Var(X̄) is large — closeness to μ on any given occasion depends on the variance (hence n and σ) as well as unbiasedness *(required: distinguishes the mean-of-the-distribution claim from a single-draw guarantee)* | — |
| E4 | explain | short-answer | 1.35 | Explain, intuitively, why averaging n observations reduces variability compared to looking at one observation alone. | individual observations' random deviations from μ partly cancel when averaged — some are above μ, some below, and summing them (before dividing by n) causes some of that noise to offset, so the average fluctuates less around μ than any single raw observation does *(required: the "deviations partly cancel" mechanism)* | — |
| E5 | explain | short-answer | 1.45 | Explain why Var(X̄)=σ²/n specifically requires the Xᵢ to be independent — what would change if they were correlated? | the derivation Var(ΣXᵢ)=ΣVar(Xᵢ) (giving nσ², then divided by n² to get σ²/n) relies on all pairwise covariances being 0, which holds only under independence; with positively correlated Xᵢ, extra Cov terms add to the variance, making Var(X̄) *larger* than σ²/n suggests — averaging correlated data reduces variability less than the independent-data formula promises *(required: names the covariance terms specifically)* | — |
| T2 | transfer | numeric | 1.9 | A study currently uses n=64 and gets SD(X̄)=3. The team wants SD(X̄)=1.5 instead. Using the σ/√n relationship, find the required n. `[verified: n=256]` | current SD(X̄)=3=σ/8 ⟹ σ=24; need 24/√n=1.5 ⟹ √n=16 ⟹ n=256 — four times the original 64, confirming the same quadrupling law as T1 | assumes doubling n (to 128) is enough to halve SD(X̄), rather than quadrupling it → `sample-mean` |
| T3 | transfer | short-answer | 2.0 | A data science team pools daily samples of n=100 from a highly volatile, non-Normal metric (e.g. daily error counts on a flaky system) to estimate its true mean. A stakeholder objects: "the raw daily counts are wildly non-Normal, so averaging them is invalid." Evaluate this objection using what you know about X̄'s sampling distribution. | the objection conflates the distribution of the *raw data* with the distribution of *X̄*; even for a highly non-Normal population, X̄'s sampling distribution becomes approximately Normal as n grows (Central Limit Theorem, foreshadowed by R16's Law of Large Numbers point about shrinking variance) — at n=100, Normal-based reasoning about X̄ is typically reasonable even though the raw daily counts themselves look nothing like a Normal curve *(required: explicitly separates the raw-data distribution from X̄'s sampling distribution)* | rejects averaging as invalid because the raw data isn't Normal, without distinguishing the raw data's distribution from X̄'s → `sample-mean` |

*Coverage: 16/6/5/3 — 30 items, −1.3…2.0.*

---

## Sample Variance (`sample-variance`)
*Prereq: Sample Mean, Variance · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | State the formula for sample variance s², and name which earlier concept established its unbiasedness. | s² = (1/(n−1))Σ(Xᵢ−X̄)²; unbiasedness with the (n−1) divisor was established in `unbiased-estimator` | — |
| R2 | recall | mcq | 0.15 | For iid *Normal* data, (n−1)s²/σ² follows exactly: | χ²_{n−1} | picks χ²ₙ — the classic off-by-one on the degrees of freedom → `sample-variance` |
| A1 | apply | numeric | 0.65 | A sample of n=10 normal observations has s²=25, with true σ²=20. Compute (n−1)s²/σ² and name its exact distribution. `[verified: 11.25]` | (9)(25)/20 = 11.25, distributed as χ²₉ | uses n=10 rather than n−1=9 degrees of freedom → `sample-variance` |
| A2 | apply | short-answer | 0.8 | Explain, at a high level, why the degrees of freedom is (n−1), not n — connect to X̄ being computed from the *same* data used to compute s². | knowing X̄ and any n−1 of the n deviations (Xᵢ−X̄) determines the *last* deviation automatically, since all n deviations must sum to exactly 0 — only n−1 pieces of information are genuinely free to vary *(required: the "last deviation is forced" argument)* — the same fact that originally motivated Bessel's correction in `unbiased-estimator` | — |
| E1 | explain | derivation | 1.35 | Prove Σ(Xᵢ−X̄) = 0 for *any* sample, and explain how this shows only n−1 of the n deviations are free. `[verified numerically]` | Σ(Xᵢ−X̄) = ΣXᵢ − nX̄ = ΣXᵢ − n(ΣXᵢ/n) = 0, always *(required: the one-line proof)*; since the n deviations must sum to 0, fixing any n−1 of them forces the last | — |
| E2 | explain | short-answer | 1.55 | Why does the exact result (n−1)s²/σ² ~ χ²_{n−1} specifically require the Xᵢ to be *Normal*? What breaks if they were Exponential instead? | Chi-Square was defined (Cluster 4) as a sum of *squared standard normals*; the (approximately) standardized deviations (Xᵢ−X̄)/σ only behave like standard normals when the underlying data itself is Normal — for Exponential data the exact chi-square result fails, though an *approximate* version can hold for large n via CLT-type reasoning *(required: the explicit tie back to chi-square's own definition, not just "it needs normality")* | — |
| T1 | transfer | short-answer | 1.85 | An engineer wants a 95% confidence interval for a machine's true variance σ² (not just its mean). Explain, conceptually, how (n−1)s²/σ² having a *known* distribution (χ²_{n−1}) is exactly what makes this possible — and how the same logical template will reappear for confidence intervals on the mean. | because (n−1)s²/σ² has a fully known distribution regardless of the true σ², chi-square critical values can bracket it with 95% probability, and that probability statement can be algebraically rearranged to isolate σ² — producing an interval; the *same template* (a statistic with a known sampling distribution, rearranged into a probability statement about the parameter) is exactly how confidence intervals on μ will be built next, just with a different underlying distribution *(required: names the shared template explicitly, not just this one application)* | — |
| R3 | recall | short-answer | −1.3 | State the divisor used in the sample variance formula, and name it. | n−1, known as Bessel's correction | — |
| R4 | recall | mcq | −1.15 | Dividing by n instead of n−1 when computing sample variance produces an estimator that is: | biased downward (on average underestimates σ²) | claims dividing by n produces an *unbiased* estimator, missing the entire reason n−1 is used instead → `sample-variance` |
| R5 | recall | short-answer | −1.05 | True or false: s² is computed using deviations from X̄, not from the true population mean μ. | true — s²=(1/(n−1))Σ(Xᵢ−X̄)², using the sample mean, since μ is generally unknown | — |
| R6 | recall | short-answer | −0.95 | Fill in the blank: "degrees of freedom" for the sample variance of a single sample of size n is ____. | n−1 | — |
| R7 | recall | mcq | −0.85 | s (the sample standard deviation) is obtained from s² by: | taking the square root | computes s by dividing s² by n, rather than taking a square root at all → `sample-variance` |
| R8 | recall | short-answer | −0.75 | Define "Bessel's correction" in one sentence. | the use of n−1 rather than n as the divisor in the sample variance formula, which corrects the downward bias that would otherwise result from estimating deviations around X̄ instead of the true μ | — |
| R9 | recall | mcq | −0.65 | For iid Normal data, the quantity (n−1)s²/σ² follows which named distribution? | Chi-Square with n−1 degrees of freedom | picks "the standard Normal distribution," confusing the exact sampling distribution of a squared/summed quantity (chi-square) with that of a linear statistic like Z → `sample-variance` |
| R10 | recall | short-answer | −0.55 | True or false: s² is always at least as large as 0. | true — as a sum of squared deviations divided by a positive constant, s² is always ≥ 0 | — |
| R11 | recall | short-answer | −0.45 | Fill in the blank: as n grows large, s² converges toward ____ (its population counterpart). | σ² | — |
| R12 | recall | mcq | −0.35 | Which correctly states E[s²] (using the n−1 divisor)? | σ², exactly — s² is an unbiased estimator of σ² | claims E[s²]=σ²·(n−1)/n, which is actually the bias of the *n-divisor* version, not the corrected n−1 version this concept defines → `sample-variance` |
| R13 | recall | short-answer | −0.25 | State the general formula for s² in summation notation. | s² = (1/(n−1))·Σᵢ₌₁ⁿ(Xᵢ−X̄)² | — |
| R14 | recall | mcq | −0.15 | For n=2 observations, the degrees of freedom for s² is: | 1 | claims the degrees of freedom for n=2 is 2, ignoring the n−1 rule entirely → `sample-variance` |
| R15 | recall | short-answer | −0.05 | True or false: for n=1 (a single observation), s² is undefined. | true — with only one observation, X̄ equals that observation exactly, so every deviation is 0 and the n−1=0 divisor makes s² undefined (0/0) | — |
| R16 | recall | short-answer | 0.05 | Fill in the blank: the sample variance s² is said to be an ____ estimator of the population variance σ², when the n−1 divisor is used. | unbiased | — |
| A3 | apply | numeric | 0.7 | A sample of n=5 has values 2,4,4,4,6. Compute X̄ and s². `[verified: X̄=4, s²=2]` | X̄=(2+4+4+4+6)/5=4; deviations: −2,0,0,0,2; squared: 4,0,0,0,4, sum=8; s²=8/4=2 | — |
| A4 | apply | numeric | 0.75 | A sample of n=8 normal observations has s²=36, with true σ²=30. Compute (n−1)s²/σ² and name its exact distribution. `[verified: 8.4]` | (7)(36)/30=8.4, distributed as χ²₇ | uses n=8 rather than n−1=7 degrees of freedom → `sample-variance` |
| A5 | apply | short-answer | 0.8 | A sample of n=20 has s=5. State the estimated variance and the degrees of freedom associated with it. | s²=25; degrees of freedom = 19 | — |
| A6 | apply | short-answer | 0.85 | A colleague computes "variance" by dividing the sum of squared deviations by n=12 instead of n−1=11. Is their result larger or smaller than the correctly computed s², and by what factor? | smaller, by a factor of 11/12 — dividing the same numerator by a larger denominator (12 vs. 11) always produces a smaller quotient, which is exactly the mechanism behind the n-divisor's downward bias *(required: names the direction and the specific 11/12 factor)* | — |
| E3 | explain | short-answer | 1.4 | Explain why using n instead of n−1 systematically *underestimates* σ² on average, in terms of X̄ itself. | X̄ is computed from the very same sample used to compute the deviations, and X̄ is (by construction, via least-squares) the value that *minimizes* Σ(Xᵢ−c)² over all choices of c — so deviations taken around the sample's own X̄ are always at least as small, in sum of squares, as deviations around the true (unknown) μ would be, making the n-divisor version systematically too small on average *(required: the "X̄ minimizes the sum of squared deviations" argument)* | — |
| E4 | explain | short-answer | 1.45 | Explain why the sample variance formula uses squared deviations rather than, say, the sum of raw (signed) deviations. | R6's earlier fact — Σ(Xᵢ−X̄)=0 always — means raw signed deviations always cancel to exactly zero regardless of how spread out the data is, making them useless as a measure of spread; squaring first ensures negative and positive deviations both contribute positively, so the sum reflects genuine dispersion *(required: explicitly connects to the always-zero raw-sum fact)* | — |
| E5 | explain | short-answer | 1.5 | Explain, conceptually, why the correction factor needed (n vs. n−1) shrinks in relative importance as n grows large. | the ratio (n−1)/n approaches 1 as n grows (e.g. 9/10=0.9 at n=10, but 999/1000=0.999 at n=1000), so the bias from using the wrong divisor becomes negligible for large samples even though it matters a great deal for small ones — this is why the distinction is emphasized especially for small-sample statistics *(required: the explicit ratio-approaching-1 argument)* | — |
| T2 | transfer | short-answer | 1.95 | A manufacturing quality team switches from n=5 samples to n=50 samples per batch, but keeps using the (incorrect) n-divisor for variance out of habit. Explain why this mistake matters far less at n=50 than it did at n=5, using E5's reasoning, and identify one situation where it would still matter. | at n=50, the correction factor (n−1)/n = 49/50 = 0.98 is close to 1, so the resulting bias is small in relative terms; at n=5 it was 4/5=0.8, a substantial 20% underestimate; it would still matter whenever small samples are used regardless of the total data collected historically — e.g. any single small batch's variance estimate, or any downstream calculation (like a confidence interval or t-statistic) that is itself sensitive to a biased variance input *(required: quantifies both ratios and names a persisting small-n use case)* | — |
| T3 | transfer | short-answer | 2.05 | A/B testing infrastructure computes variance for many small experiment segments (some with as few as n=4 users) to build confidence intervals. Explain why getting the n vs. n−1 choice right matters disproportionately here, compared to a system that only ever analyzes million-row datasets. | E5's ratio argument shows the n-vs-n−1 correction factor is far from 1 at small n (e.g. 3/4=0.75 at n=4, a 25% systematic underestimate of variance), which would make every downstream confidence interval or standard error built on that biased variance too narrow — falsely overstating precision on exactly the small, uncertain segments where correct uncertainty quantification matters most; at millions of rows the same coding mistake would be nearly invisible *(required: connects the small-n bias magnitude to a concrete downstream consequence — overstated precision — not just "it's more biased")* | assumes a variance-computation bug is equally serious regardless of sample size → `sample-variance` |

*Coverage: 16/6/5/3 — 30 items, −1.3…2.05. T1 is the explicit bridge into [Cluster 10](hypothesis-testing-machinery.md).*

---

## Cluster 9 misconception index

| Tag | Blame |
|---|---|
| population treated as an unambiguous, always-finite, human group | `population-vs-sample` |
| sampling frame conflated with the population itself | `population-vs-sample` |
| finite population wrongly assumed for every study | `population-vs-sample` |
| sampling error confused with a data-collection mistake | `population-vs-sample` |
| a large, conveniently-available dataset treated as "the population" | `population-vs-sample` |
| parameter/statistic notational convention reversed | `parameter-vs-statistic` |
| a statistic reported as if it *were* the parameter, with no uncertainty | `parameter-vs-statistic` |
| hat notation misread as denoting the true parameter rather than an estimate | `parameter-vs-statistic` |
| fixed number of population parameters confused with the unlimited number of possible statistics | `parameter-vs-statistic` |
| numeric-looking labels (zip codes) treated as ordinal or quantitative | `data-types` |
| ordinal data averaged without acknowledging the equal-spacing assumption | `data-types` |
| discrete/continuous distinction reduced to "whole number vs. decimal" formatting | `data-types` |
| Likert-type ordinal data misclassified as nominal | `data-types` |
| ordinal pain/rating scale treated as ratio-scale data with a true zero | `data-types` |
| convenience sampling assumed to improve with sheer sample size | `sampling-methods` |
| systematic sampling confused with convenience sampling | `sampling-methods` |
| stratified and cluster sampling's subgroup-coverage roles conflated | `sampling-methods` |
| cluster sample assumed as precise as an equal-size SRS | `sampling-methods` |
| SD(X̄) assumed to shrink as 1/n rather than 1/√n | `sample-mean` |
| Var(X̄) confused with σ² directly (forgetting the division by n) | `sample-mean` |
| degrees of freedom off-by-one (n vs n−1) in sample variance | `sample-variance` |
| n-divisor variance wrongly believed unbiased | `sample-variance` |
| variance-formula bias treated as equally serious at any sample size | `sample-variance` |

**Cluster total: 180 items across 6 concepts (30 each).** All numeric claims verified, including the
exact chi-square statistic values, the 1/√n scaling law, and the quadrupling-of-n results for halving
standard error. This cluster is the hinge of the whole bank: it takes every probability-theory result
built in Clusters 1–8 and turns it around to face data instead of distributions — `sample-mean`'s
variance IS the Weak LLN's proof mechanism read backwards, and `sample-variance`'s T1 hands off
directly into the sampling-distribution machinery of Cluster 10.
