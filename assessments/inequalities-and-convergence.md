# Cluster 7 — Inequalities & Convergence

Markov's Inequality → Order Statistics (6 concepts). Same format as [Cluster 1](foundations-of-probability.md).

The inequalities chain by construction here: Chebyshev is *derived from* Markov (not just stated
alongside it), and the Weak Law of Large Numbers is derived from Chebyshev in turn — so a learner who
has worked through this cluster in order has actually built the LLN from first principles, not just
been told it's true. `order-statistics`' E2 also closes a loop back to `mle`'s surprising Uniform
result from Cluster 6.

---

## Markov's Inequality (`markov-inequality`)
*Prereq: Expectation · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | State Markov's inequality and its one requirement on X. | P(X≥a) ≤ E[X]/a for a>0; requires X ≥ 0 | omits the nonnegativity requirement → `markov-inequality` |
| R2 | recall | mcq | 0.0 | Markov's inequality requires knowing: | only E[X] | picks "both E[X] and Var(X)" — that's Chebyshev's requirement, not Markov's → `markov-inequality` |
| A1 | apply | numeric | 0.5 | A factory's daily output (nonnegative) has mean 100 units. Bound P(output ≥ 150). `[verified: 0.667]` | P ≤ 100/150 ≈ 0.667 | — |
| A2 | apply | numeric | 0.6 | Same factory: bound P(output ≥ 500). `[verified: 0.2]` | P ≤ 100/500 = 0.2 — the bound tightens (in relative usefulness) as the threshold grows relative to the mean | — |
| E1 | explain | derivation | 1.2 | Prove Markov's inequality using the indicator trick a·1{X≥a} ≤ X. | pointwise: if X≥a, LHS=a≤X=RHS; if X<a, LHS=0≤X=RHS (using X≥0) — so the inequality holds for every outcome; taking expectations, E[a·1{X≥a}] = a·P(X≥a) ≤ E[X] *(required: both cases of the pointwise inequality)* | — |
| E2 | explain | short-answer | 1.4 | Explain why Markov's bound can be very loose. What does it say for a ≤ E[X]? | for a ≤ E[X], the bound E[X]/a ≥ 1 is vacuous (probabilities can't exceed 1); even for a > E[X], the bound can be far above the true tail probability, because it uses *only* the mean and nothing about the distribution's shape or spread *(required: names the a≤E[X] vacuous case specifically)* — motivating the sharper bound `chebyshev-inequality` builds next | — |
| T1 | transfer | short-answer | 1.7 | A fund reports mean daily profit $10,000 (profit ≥ $0 always). Bound P(profit ≥ $1,000,000) via Markov, and explain why the *true* probability could be dramatically lower than this bound. `[verified: bound=0.01]` | bound = 10,000/1,000,000 = 0.01 (1%); the true probability is likely far below this in practice — Markov's bound, using *only* the mean, must remain valid even for some adversarially-shaped distribution that puts as much mass as possible near the threshold, so it can't rule out that worst case without more information *(required: names that the bound is the best guarantee possible from the mean alone, achievable by some distribution)* | reports the 1% bound as if it were the actual probability of a million-dollar day → `markov-inequality` |
| R3 | recall | short-answer | −0.26 | Fill in the blank: Markov's inequality states P(X≥a) ≤ ___, for a>0. | E[X]/a | — |
| R4 | recall | mcq | −0.22 | Markov's inequality, in its basic form, applies to which kind of random variable X? | a nonnegative random variable (X≥0) | claims it applies to any random variable regardless of sign, missing the stated requirement in R1 | — |
| R5 | recall | mcq | −0.18 | True or false: Markov's inequality (basic form) can be applied to a random variable that takes negative values. | False — the basic form requires X≥0; a negative-valued X needs the |X|-based version instead | claims the basic form works for any X, ignoring the nonnegativity requirement | — |
| R6 | recall | short-answer | −0.14 | State the version of Markov's inequality applicable to a general (possibly negative) random variable X, using \|X\|. | P(\|X\|≥a) ≤ E[\|X\|]/a, for a>0 | — |
| R7 | recall | mcq | −0.1 | For a = E[X] exactly, Markov's inequality gives a bound of: | 1 — a vacuous bound, since probabilities never exceed 1 | claims the bound is 0 at a=E[X], missing that E[X]/a=1 exactly when a=E[X] → `markov-inequality` |
| R8 | recall | mcq | −0.06 | True or false: Markov's inequality is a two-sided bound, simultaneously bounding both P(X≥a) and P(X≤−a). | False — the basic form is one-sided, bounding only the upper tail P(X≥a) for nonnegative X | claims Markov automatically bounds both tails, missing that it says nothing about the lower tail | — |
| R9 | recall | short-answer | −0.02 | Fill in the blank: Markov's inequality requires the threshold a to be ___. | positive (a>0) | — |
| R10 | recall | mcq | 0.02 | Which of these is an equivalent rearrangement of Markov's inequality? | a·P(X≥a) ≤ E[X] | rearranges to P(X≥a)·E[X] ≤ a, multiplying the wrong two quantities together → `markov-inequality` |
| R11 | recall | mcq | 0.06 | True or false: a generalized version of Markov's inequality exists using higher powers, e.g. applying the basic inequality to \|X\|^k. | True — this generalized form (E3) underlies both Chebyshev's inequality (k=2, centered) and other moment-based tail bounds | claims Markov's inequality only ever exists in its most basic first-moment form, with no generalization → `markov-inequality` |
| R12 | recall | short-answer | 0.1 | Fill in the blank: applying Markov's inequality to Y=(X−μ)² produces ___. | Chebyshev's inequality | — |
| R13 | recall | short-answer | 0.14 | State the generalized Markov inequality using a nonnegative, nondecreasing function φ. | P(X≥a) ≤ E[φ(X)]/φ(a), for any nonnegative nondecreasing φ and a with φ(a)>0 | — |
| R14 | recall | mcq | 0.18 | Markov's inequality is often called "distribution-free" because: | it uses only the mean E[X], with no other assumption about the distribution's shape | claims it's distribution-free because it applies only to Normal-like distributions, the opposite of the actual reason | — |
| R15 | recall | mcq | 0.22 | True or false: for a fixed E[X], Markov's bound E[X]/a gets *smaller* (tighter, more informative) as the threshold a increases. | True — the bound shrinks as 1/a, so higher thresholds get tighter relative bounds | claims the bound gets looser as a grows, backwards from the actual 1/a behavior → `markov-inequality` |
| R16 | recall | short-answer | 0.26 | What is the name of the proof technique used in E1 to derive Markov's inequality? | the "indicator trick" — bounding a·1{X≥a} ≤ X pointwise, then taking expectations | — |
| A3 | apply | numeric | 0.44 | X≥0 with E[X]=50. Bound P(X≥200). `[verified: 0.25]` | P(X≥200) ≤ 50/200 = 0.25 | — |
| A4 | apply | numeric | 0.56 | X≥0 with E[X]=20. Find the smallest threshold a such that Markov's bound on P(X≥a) is at most 0.1. `[verified: a=200]` | 20/a ≤ 0.1 ⟹ a ≥ 200 | solves 20/a ≤ 0.1 as a ≤ 200 instead of a ≥ 200, inverting the direction of the inequality | — |
| A5 | apply | numeric | 0.5 | Insurance claims (nonnegative) have mean $500. Bound P(claim ≥ $5,000). `[verified: 0.1]` | P(claim≥5000) ≤ 500/5000 = 0.1 | — |
| A6 | apply | numeric | 0.62 | Using the generalized Markov inequality with φ(x)=x², for X with E[X²]=64, bound P(\|X\|≥16). `[verified: 0.25]` | P(\|X\|≥16) ≤ E[X²]/16² = 64/256 = 0.25 | applies the basic (first-power) Markov formula E[X]/16 directly to X² without squaring the threshold, using the wrong denominator | — |
| E3 | explain | derivation | 1.3 | Derive the generalized Markov inequality P(X≥a) ≤ E[φ(X)]/φ(a), for φ nonnegative and nondecreasing, using the same indicator trick as E1. | since φ is nondecreasing, X≥a implies φ(X)≥φ(a); so φ(a)·1{X≥a} ≤ φ(X) pointwise (the same two-case check as E1, now on φ(X) rather than X directly); taking expectations, φ(a)·P(X≥a) ≤ E[φ(X)], and dividing by φ(a)>0 gives the result *(required: the "X≥a implies φ(X)≥φ(a)" step, which is where nondecreasing-ness is used)* | — |
| E4 | explain | short-answer | 1.42 | Explain why Markov's inequality remains genuinely useful in practice despite being, as E2 shows, often very loose. | its looseness is the price of needing *no assumption at all* about the distribution's shape beyond nonnegativity and a known mean — in settings where the true distribution is unknown, hard to characterize, or adversarial (as in worst-case algorithm analysis), a guaranteed-safe bound that holds no matter what the true shape turns out to be is valuable precisely because it can't be invalidated by an unexpected distribution, unlike a bound that assumes (say) normality *(required: connects looseness to the tradeoff for universality, not just restating that it's loose)* | — |
| E5 | explain | derivation | 1.52 | Show that Markov's inequality is "tight" — that there exists a distribution for which the bound P(X≥a) ≤ E[X]/a holds with *equality*. | consider the two-point distribution X=0 with probability 1−(E[X]/a) and X=a with probability E[X]/a (valid whenever E[X]/a ≤ 1); its mean is 0·(1−E[X]/a) + a·(E[X]/a) = E[X], matching the required mean, and P(X≥a) = P(X=a) = E[X]/a exactly *(required: verifies both that the constructed distribution has the correct mean, and that its tail probability hits the bound exactly)* — so no universally valid bound using only the mean could ever be improved | — |
| T2 | transfer | short-answer | 1.8 | A computer scientist analyzing a randomized algorithm knows its expected runtime is T but has no handle on the runtime distribution's shape. Using Markov's inequality, explain how they can still guarantee "the algorithm finishes within 10T time with probability at least 90%" — and why this kind of guarantee is valuable in CS even though it's loose. | Markov gives P(runtime≥10T) ≤ T/(10T) = 0.1, so P(runtime<10T) ≥ 0.9 directly; this guarantee requires no knowledge of the runtime distribution's shape at all — only its mean — which is exactly what makes it valuable for algorithm analysis, where worst-case or adversarial input distributions are a real concern and a distribution-specific bound (like one assuming Normality) could be invalidated by an unusual input *(required: the numeric 90% guarantee and naming why distribution-freeness matters for algorithm analysis specifically)* | insists a useful runtime guarantee requires first characterizing the full runtime distribution, missing that Markov needs only the mean → `markov-inequality` |
| T3 | transfer | short-answer | 1.9 | A network engineer only knows a router's average queue length is 20 packets (queue length is always ≥0), not its full distribution. Bound the probability the queue exceeds 100 packets (a capacity threshold), and explain why Markov's inequality is well-suited to capacity planning when the true queue-length distribution is complex or unknown. `[verified: bound=0.2]` | P(queue≥100) ≤ 20/100 = 0.2; capacity planners often only have reliable estimates of *average* load, not the full (and often heavy-tailed, bursty) distribution of queue lengths — Markov's inequality lets them derive a valid, conservative worst-case guarantee from that one average alone, without needing to model or assume anything about the queue's actual shape, which is exactly the situation this concept is built for *(required: the numeric bound and the explicit connection to not needing the full distribution)* | assumes a queue-overflow probability bound requires first modeling the full arrival/service distribution before anything useful can be said → `markov-inequality` |

*Coverage: 16/6/5/3 — 30 items, −0.3…1.9.*

---

## Chebyshev's Inequality (`chebyshev-inequality`)
*Prereq: Markov's Inequality, Variance · ancestors 12 · b₀ = 0.78*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.22 | State Chebyshev's inequality in standardized form (k standard deviations). | P(\|X−μ\| ≥ kσ) ≤ 1/k² | — |
| R2 | recall | mcq | 0.08 | Compared to the empirical 68-95-99.7 rule, Chebyshev's advantage is: | it applies to *any* distribution with finite variance, not just Normal | picks "it gives a tighter bound" — backwards; Chebyshev trades sharpness for universality → `chebyshev-inequality` |
| A1 | apply | numeric | 0.58 | A distribution (shape unknown) has mean 50, SD 5. Bound P(\|X−50\| ≥ 15). `[verified: k=3, bound=1/9≈0.111]` | k=15/5=3; bound ≤ 1/9 ≈ 0.111 | — |
| A2 | apply | numeric | 0.7 | Same setup: bound P(X<35 or X>65) — note this is the *same event* as A1, just rephrased. `[verified: 1/9]` | identical computation, ≤ 1/9 | treats it as a different, harder problem requiring a fresh calculation → `chebyshev-inequality` |
| E1 | explain | derivation | 1.28 | Derive Chebyshev's inequality *from Markov's*, applied to Y=(X−μ)². | Y=(X−μ)² ≥ 0; apply Markov with threshold k²σ²: P(Y≥k²σ²) ≤ E[Y]/(k²σ²) = σ²/(k²σ²) = 1/k²; note {Y≥k²σ²} is exactly {\|X−μ\|≥kσ}, so P(\|X−μ\|≥kσ) ≤ 1/k² *(required: the full substitution, including the event-equivalence step)* | — |
| E2 | explain | short-answer | 1.45 | Chebyshev is generally tighter than Markov for the same event, but still typically much looser than the *true* probability for well-behaved distributions. Illustrate with a Normal example. `[verified: true P(\|Z\|≥3)≈0.0027 vs Chebyshev's 1/9≈0.111]` | for standard Normal, the true P(\|Z\|≥3) ≈ 0.0027, while Chebyshev's guaranteed-safe bound is 1/9 ≈ 0.111 — roughly 40× looser *(required: the concrete numeric contrast, not just "it's looser")* | — |
| T1 | transfer | short-answer | 1.78 | Without assuming any shape for a stock's daily returns (mean 0%, SD 2%), Chebyshev-bound P(\|return\| ≥ 6%). Why might a risk manager prefer this conservative bound over a Normal-based calculation that gives a much smaller number? `[verified: k=3, bound≈0.111]` | bound ≤ 1/9 ≈ 11.1%; a risk manager may prefer the looser-but-universally-valid Chebyshev bound *because* it doesn't assume normality — real financial returns are well known to have fatter tails than Normal, so a Normal-based number can badly understate real tail risk, while Chebyshev's guarantee holds regardless of the true shape *(required: names the normality assumption as the actual risk being avoided)* | uses the tighter Normal-based number without questioning whether the normality assumption is safe for this data → `chebyshev-inequality` |
| R3 | recall | short-answer | −0.18 | Fill in the blank: the general (non-standardized) form of Chebyshev's inequality states P(\|X−μ\|≥a) ≤ ___. | σ²/a² | — |
| R4 | recall | mcq | −0.14 | Applying Chebyshev's inequality requires knowing: | the mean and a finite variance | claims only the mean is needed, missing that Chebyshev (unlike Markov) requires the variance as well → `chebyshev-inequality` |
| R5 | recall | mcq | −0.1 | True or false: Chebyshev's inequality applies only to Normally distributed random variables. | False — it applies to *any* distribution with finite mean and variance, which is exactly R2's stated advantage over the empirical 68-95-99.7 rule | claims Chebyshev requires normality, exactly the opposite of what makes it useful → `chebyshev-inequality` |
| R6 | recall | short-answer | −0.06 | Fill in the blank: a one-sided version of Chebyshev's inequality, bounding only P(X−μ≥a) rather than the two-sided \|X−μ\|≥a, is called ___. | Cantelli's inequality | — |
| R7 | recall | mcq | −0.02 | As k→∞ in the standardized form P(\|X−μ\|≥kσ) ≤ 1/k², the bound: | approaches 0 | claims the bound approaches 1, backwards from the actual 1/k² → 0 behavior | — |
| R8 | recall | mcq | 0.02 | True or false: because Chebyshev's inequality is derived by applying Markov's inequality to (X−μ)² rather than to X directly, it makes use of *more* information about the distribution (the variance) than Markov's bound does. | True — this is exactly why Chebyshev is generally tighter than Markov for the same underlying event (R2) | claims Chebyshev uses exactly the same information as Markov, missing that the variance is genuinely extra information → `chebyshev-inequality` |
| R9 | recall | short-answer | 0.06 | Fill in the blank: the standardized form of Chebyshev's inequality states P(\|X−μ\|≥kσ) ≤ ___. | 1/k² | — |
| R10 | recall | mcq | 0.1 | For k=1, Chebyshev's standardized bound gives: | 1 — a vacuous bound, since 1/1²=1 and probabilities can't exceed 1 | claims the bound is 0 at k=1, the opposite of the actual vacuous value | — |
| R11 | recall | short-answer | 0.14 | State the complement form of Chebyshev's inequality: a lower bound on P(\|X−μ\|<kσ). | P(\|X−μ\|<kσ) ≥ 1 − 1/k² | — |
| R12 | recall | mcq | 0.18 | True or false: Chebyshev's inequality requires the random variable to have finite variance. | True — without finite σ², the bound 1/k² has no meaning | claims Chebyshev applies even when the variance is infinite, missing the finite-variance requirement | — |
| R13 | recall | short-answer | 0.22 | Fill in the blank: in the non-standardized (absolute-units) form, Chebyshev's inequality bounds P(\|X−μ\|≥___), where the threshold is measured in the original units of X rather than in standard deviations. | a (a generic absolute deviation threshold, as opposed to kσ in the standardized form) | — |
| R14 | recall | mcq | 0.26 | For k<1 in the standardized form, the bound 1/k² is: | greater than 1, and therefore vacuous | claims the bound is still meaningful (less than 1) for k<1, missing that 1/k²>1 whenever k<1 → `chebyshev-inequality` |
| R15 | recall | mcq | 0.3 | True or false: Chebyshev's bound requires no assumption about the distribution's symmetry around its mean. | True — it holds for symmetric and skewed distributions alike, as long as the variance is finite | claims Chebyshev's inequality is only valid for symmetric distributions, an unnecessary and incorrect extra assumption → `chebyshev-inequality` |
| R16 | recall | short-answer | 0.34 | Fill in the blank: Chebyshev's inequality is derived by applying ___'s inequality to the random variable (X−μ)². | Markov | — |
| A3 | apply | numeric | 0.48 | A quantity has mean 200, SD 10. Bound P(\|X−200\|≥30). `[verified: k=3, bound≈0.111]` | k=30/10=3; bound ≤ 1/9 ≈ 0.111 | uses k=30 directly (forgetting to divide by the SD), computing 1/900 instead → `chebyshev-inequality` |
| A4 | apply | numeric | 0.62 | A quantity has mean 0, SD 4. Using the complement form, lower-bound P(\|X\|<12). `[verified: k=3, ≥8/9≈0.889]` | k=12/4=3; P(\|X\|<12) ≥ 1−1/9 = 8/9 ≈ 0.889 | forgets to subtract from 1, reporting the bound as 1/9 itself instead of its complement → `chebyshev-inequality` |
| A5 | apply | numeric | 0.66 | A quantity has mean 100, SD 20. Find the smallest k such that Chebyshev's bound on P(\|X−100\|≥20k) is at most 0.04, and state the corresponding threshold in original units. `[verified: k=5, threshold=100±100]` | 1/k² ≤ 0.04 ⟹ k² ≥ 25 ⟹ k=5; threshold is 100 ± 5(20) = 100 ± 100, i.e. the interval [0,200] | solves 1/k²≤0.04 as k≤5 instead of k≥5, since increasing k *tightens* (shrinks) the bound rather than loosening it | — |
| A6 | apply | numeric | 0.72 | A quantity has mean 50, variance 25 (SD 5). Use the two-sided Chebyshev bound on P(\|X−50\|≥20) to also bound the one-sided event P(X≥70). `[verified: 0.0625]` | P(\|X−50\|≥20) ≤ 25/400 = 0.0625 (k=4); since {X≥70} ⊆ {\|X−50\|≥20}, P(X≥70) ≤ P(\|X−50\|≥20) ≤ 0.0625 as well *(the two-sided bound is a valid, if slightly conservative, bound on the one-sided event too)* | computes P(X≥70) as exactly half of the two-sided bound (0.03125), assuming the two-sided bound splits evenly without justification → `chebyshev-inequality` |
| E3 | explain | derivation | 1.36 | Derive the complement form of Chebyshev's inequality, P(\|X−μ\|<kσ) ≥ 1−1/k², directly from the standard form. | the events {\|X−μ\|≥kσ} and {\|X−μ\|<kσ} are complements, so their probabilities sum to 1: P(\|X−μ\|<kσ) = 1 − P(\|X−μ\|≥kσ); substituting the standard bound P(\|X−μ\|≥kσ) ≤ 1/k² gives P(\|X−μ\|<kσ) ≥ 1 − 1/k² *(required: the explicit complement-probabilities-sum-to-1 step, and that the inequality direction flips correctly under subtraction)* | — |
| E4 | explain | short-answer | 1.5 | Explain why Chebyshev's inequality, unlike Markov's basic form, naturally bounds deviations in *either* direction from the mean simultaneously. | Chebyshev is built around \|X−μ\|, the *absolute* deviation from the mean, which is symmetric by construction — it doesn't distinguish X being far above μ from X being far below μ, so a single bound on \|X−μ\|≥kσ automatically covers both {X≥μ+kσ} and {X≤μ−kσ} at once; Markov's basic form, by contrast, is stated directly on X (or a shifted nonnegative version of it) and only bounds one tail unless separately reapplied *(required: ties the two-sidedness to the absolute-value structure specifically)* | — |
| E5 | explain | derivation | 1.6 | Derive Chebyshev's inequality directly via integration (for continuous X), without invoking Markov's inequality by name. | P(\|X−μ\|≥kσ) = ∫_{\|x−μ\|≥kσ} f(x) dx ≤ ∫_{\|x−μ\|≥kσ} [(x−μ)²/(k²σ²)] f(x) dx, since (x−μ)²/(k²σ²) ≥ 1 exactly on the region being integrated over; extending the integral to all of ℝ can only increase it (the integrand is nonnegative), giving ≤ (1/(k²σ²)) ∫_{−∞}^{∞} (x−μ)² f(x) dx = σ²/(k²σ²) = 1/k² *(required: the pointwise inequality on the restricted region, and the extend-to-all-of-ℝ step)* — structurally the same idea as E1's Markov-based proof, worked directly instead | — |
| T2 | transfer | short-answer | 1.85 | A manufacturer only knows a part's dimension has mean μ and SD σ (the true distribution shape is unknown, due to complex machinery variability), and wants to guarantee at least 95% of parts fall within a symmetric tolerance band around μ. Using Chebyshev, find how many SDs wide the tolerance band must be, and contrast with a "Six Sigma"-style approach that assumes normality. `[verified: k≈4.47]` | need 1−1/k² ≥ 0.95 ⟹ 1/k² ≤ 0.05 ⟹ k² ≥ 20 ⟹ k ≈ 4.47 SDs on each side; a Normal-based ("Six Sigma") calculation would claim a far narrower band achieves 95% coverage (roughly ±1.96σ), but that number is only valid *if* the dimension really is Normally distributed — Chebyshev's wider ±4.47σ band is the price of a guarantee that holds no matter what the true (possibly non-Normal) shape turns out to be *(required: the numeric k≈4.47 and the explicit contrast with the much narrower Normal-based figure)* | uses the Normal-based ±1.96σ tolerance band without checking whether the normality assumption is actually justified for this manufacturing process → `chebyshev-inequality` |
| T3 | transfer | short-answer | 1.95 | A financial regulator sets a bank's minimum capital reserve using Chebyshev's inequality on portfolio returns (known mean and variance, but suspected fat tails) rather than a Normal-based Value-at-Risk calculation. Explain why a regulator might deliberately prefer the more conservative Chebyshev-based reserve requirement. | a Normal-based VaR calculation understates tail risk whenever real returns have heavier tails than Normal (a well-documented feature of financial data, echoing T1); Chebyshev's guarantee, using only the mean and variance, holds *regardless* of the true shape of the return distribution, so a reserve requirement built on it remains valid even under fat-tailed, non-Normal returns — a regulator prioritizing systemic safety over capital efficiency would accept the higher reserve Chebyshev demands as the cost of a guarantee that can't be invalidated by an unexpected distribution shape *(required: names both the fat-tail risk of the Normal assumption and that Chebyshev's guarantee doesn't depend on shape)* | assumes the Normal-based reserve is always adequate since it "usually works for well-diversified portfolios," missing the systemic-risk argument for the more conservative bound → `chebyshev-inequality` |

*Coverage: 16/6/5/3 — 30 items, −0.22…1.95.*

---

## Jensen's Inequality (`jensen-inequality`)
*Prereq: Expectation · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | State Jensen's inequality for a convex function g. | E[g(X)] ≥ g(E[X]) | — |
| R2 | recall | mcq | 0.0 | Which is FALSE? (a) for convex g, E[g(X)]≥g(E[X]) (b) equality holds if X is a constant (c) Jensen's inequality only applies to linear g. | (c) is false | picks (c) as a true statement — backwards: linear g gives *trivial* equality; the inequality's actual content is for genuinely nonlinear (strictly convex/concave) g → `jensen-inequality` |
| A1 | apply | short-answer | 0.5 | Use Jensen (g(x)=x², convex) to explain why Var(X)=E[X²]−(E[X])² is *always* ≥ 0. | E[X²] ≥ (E[X])² directly by Jensen, since x² is convex; so E[X²]−(E[X])² ≥ 0 — the "obvious" fact that variance can't be negative is Jensen's inequality in disguise *(required: makes the connection explicit, not just cites Var≥0 as a separate axiom)* | — |
| A2 | apply | numeric | 0.65 | A round trip covers equal distance at 60mph and 40mph. Is the trip's average speed the arithmetic mean, 50mph? Use Jensen (g(x)=1/x, convex for x>0) to explain why the true average is *lower*, and compute it. `[verified: 48mph]` | true average speed is the harmonic mean: 2/(1/60+1/40) = 48mph, below 50 — more *time* is spent at the slower 40mph leg (same distance, lower speed = more time), dragging the true average down *(required: the harmonic-mean computation and the "more time at the slow speed" mechanism)* | computes 50mph, applying the arithmetic mean to rates without checking whether that's the right average for this quantity → `jensen-inequality` |
| E1 | explain | derivation | 1.2 | Prove Jensen's inequality using the tangent-line definition of convexity: g(x) ≥ g(x₀) + g′(x₀)(x−x₀) at any x₀. | set x₀=E[X], x=X: g(X) ≥ g(E[X]) + g′(E[X])(X−E[X]); take expectations of both sides: E[g(X)] ≥ g(E[X]) + g′(E[X])·E[X−E[X]] = g(E[X]) + g′(E[X])·0 = g(E[X]) *(required: the E[X−E[X]]=0 cancellation must be shown)* | — |
| E2 | explain | short-answer | 1.35 | Explain intuitively why convexity causes E[g(X)] ≥ g(E[X]) — what is convexity "doing" to spread-out inputs? | a convex function curves upward, so averaging the *outputs* of a spread-out set of inputs tends to land higher than applying g to the single averaged *input* — spread gets "punished upward" by a convex function (and correspondingly "rewarded downward" by a concave one, the mechanism behind risk aversion under a concave utility) *(required: the spread/curvature intuition, not just restating the formula)* | — |
| T1 | transfer | short-answer | 1.7 | A stock alternates +50%, −50% in successive years. The arithmetic average return is (50%+(−50%))/2 = 0%, suggesting break-even. Trace $100 through both years and explain, via Jensen, why the arithmetic mean of returns systematically *overstates* true compounded growth. `[verified: 100→150→75]` | $100 → $150 (+50%) → $75 (−50%) — a real **25% loss**, not break-even; compounded growth is a *multiplicative* (geometric) quantity, and by Jensen's inequality applied to the concavity of log-growth, the arithmetic mean of percentage returns is always ≥ the true compounded growth rate — the correct summary statistic here is the *geometric* mean, not the arithmetic one *(required: the $100→$75 trace, not just citing the geometric-mean rule)* | reports 0% expected return as consistent with the actual $75 outcome, not recognizing the discrepancy as meaningful → `jensen-inequality` |
| R3 | recall | short-answer | −0.26 | Fill in the blank: for a *concave* function g, Jensen's inequality states E[g(X)] ___ g(E[X]). | ≤ — the inequality direction flips compared to the convex case | — |
| R4 | recall | mcq | −0.22 | For strictly convex g, equality E[g(X)]=g(E[X]) holds if and only if: | X is (almost surely) a constant — no genuine spread for the curvature to act on | claims equality also holds whenever E[X]=0, an unrelated and incorrect condition → `jensen-inequality` |
| R5 | recall | mcq | −0.18 | True or false: Jensen's inequality applies to any function g, regardless of whether it's convex or concave. | False — it specifically requires g to be convex (or concave, with the flipped direction); for a general non-convex, non-concave g no such inequality holds | claims Jensen's inequality is a universal fact about any function g, missing the convexity/concavity requirement entirely → `jensen-inequality` |
| R6 | recall | short-answer | −0.14 | Define convexity of a twice-differentiable function g. | g is convex if g″(x) ≥ 0 for all x in its domain (equivalently, the graph of g lies below every chord connecting two of its points) | — |
| R7 | recall | mcq | −0.1 | g(x) = −ln(x) for x>0 is: | convex | claims −ln(x) is concave, the opposite of the correct classification (ln(x) itself is concave; negating it flips convexity) → `jensen-inequality` |
| R8 | recall | mcq | −0.06 | True or false: since g(x)=ln(x) is concave, Jensen's inequality gives E[ln X] ≤ ln(E[X]). | True — this is the concave-case direction, and the basis of the AM–GM inequality (E5) | claims the inequality should read E[ln X] ≥ ln(E[X]) for concave g, using the convex-case direction by mistake → `jensen-inequality` |
| R9 | recall | short-answer | −0.02 | Fill in the blank: for a linear function g(x)=ax+b, Jensen's inequality becomes an ___, not a strict inequality. | equality — a linear g is both convex and concave (non-strictly), so E[g(X)]=g(E[X]) always | — |
| R10 | recall | mcq | 0.02 | Jensen's inequality is fundamentally a statement about: | the relationship between "averaging the outputs of a nonlinear function" and "applying the function to the averaged input" | describes it as a statement purely about the function g's shape in isolation, missing that it's specifically about the interplay between averaging and nonlinearity | — |
| R11 | recall | mcq | 0.06 | True or false: strict convexity (not merely convexity) is needed to guarantee strict inequality (E[g(X)] > g(E[X])) whenever X is genuinely random (not almost surely constant). | True — with only (non-strict) convexity, some genuinely random X could still achieve equality, as R9's linear case shows in the boundary case | claims ordinary convexity alone is always enough to force strict inequality for any non-constant X, missing the linear boundary case | — |
| R12 | recall | short-answer | 0.1 | Fill in the blank: g(x)=x² is ___, so Jensen's inequality gives E[X²] ≥ (E[X])². | convex | — |
| R13 | recall | short-answer | 0.14 | Name a concave utility function commonly used in economics to model risk aversion, and state what Jensen's inequality implies about it. | e.g. u(x)=ln(x) or u(x)=√x; since u is concave, Jensen gives E[u(X)] ≤ u(E[X]) — a risky gamble's expected utility is at most the utility of its expected (certain) payoff, formalizing risk aversion | — |
| R14 | recall | mcq | 0.18 | Which is a direct real-world consequence of Jensen's inequality applied to a concave utility function? | risk-averse agents prefer a certain payoff to a risky gamble with the same expected monetary value | claims Jensen implies risk-averse agents are *indifferent* between the certain payoff and the risky gamble, missing that the inequality is strict (for genuinely random, non-degenerate gambles) → `jensen-inequality` |
| R15 | recall | mcq | 0.22 | True or false: the nonnegativity of KL divergence (Gibbs' inequality) is a direct application of Jensen's inequality to −log. | True — this is exactly the edge that makes `jensen-inequality` a prerequisite of `kl-divergence` in the concept graph | claims KL divergence's nonnegativity is unrelated to Jensen's inequality and follows from an entirely separate argument → `jensen-inequality` |
| R16 | recall | short-answer | 0.26 | Fill in the blank: for convex g, Jensen's inequality gives g(E[X]) as a ___ bound on E[g(X)]. | lower | — |
| A3 | apply | numeric | 0.42 | g(x)=x² (convex). E[X]=4, E[X²]=20. Verify Jensen's inequality holds, and identify what the gap equals. `[verified: 20≥16, gap=4]` | E[X²]=20 ≥ g(E[X])=4²=16 ✓; the gap 20−16=4 is exactly Var(X), consistent with A1's general identity | checks 20 ≥ 16 but reports the gap as some other quantity, not recognizing it as exactly Var(X) → `jensen-inequality` |
| A4 | apply | numeric | 0.56 | A risk-averse investor has utility u(x)=√x (concave). Wealth outcome is $100 or $400, each with probability 0.5. Compare E[u(X)] to u(E[X]). `[verified: E[u(X)]=15, u(E[X])≈15.81]` | E[X]=250; u(E[X])=√250≈15.81; E[u(X)]=(√100+√400)/2=(10+20)/2=15 — confirming E[u(X)] ≤ u(E[X]) for concave u, exactly R8's direction | computes E[u(X)] as u of the average outcome (√250) instead of averaging the two separately-computed utilities, collapsing the two quantities Jensen is comparing | — |
| A5 | apply | numeric | 0.6 | g(x)=eˣ (convex). X = −1 or +1, each with probability 0.5. Compare E[g(X)] to g(E[X]). `[verified: E[g(X)]≈1.543, g(E[X])=1]` | E[X]=0, so g(E[X])=e⁰=1; E[g(X)]=(e⁻¹+e¹)/2≈(0.368+2.718)/2≈1.543 — confirming E[g(X)] ≥ g(E[X]) for convex g | averages the two X values first (getting 0) and then applies g once, computing e⁰=1 for both sides and missing the gap Jensen predicts | — |
| A6 | apply | numeric | 0.64 | g(x)=1/x (convex for x>0). X = 2 or 8, each with probability 0.5. Compare E[g(X)] to g(E[X]) — structurally the same setup as A2's average-speed problem. `[verified: E[g(X)]=0.3125, g(E[X])=0.2]` | E[X]=5, g(E[X])=1/5=0.2; E[g(X)]=(1/2+1/8)/2=0.3125 ≥ 0.2, confirming Jensen's direction for this convex g | computes 1/E[X] and reports it as equal to E[1/X], the exact confusion A2 targets for the harmonic-mean scenario → `jensen-inequality` |
| E3 | explain | derivation | 1.3 | Derive the *concave*-case direction of Jensen's inequality, E[g(X)] ≤ g(E[X]), from the convex-case result already proven in E1. | if g is concave, then −g is convex; applying the convex-case result (E1) to −g gives E[−g(X)] ≥ −g(E[X]), i.e. −E[g(X)] ≥ −g(E[X]); multiplying both sides by −1 flips the inequality direction, giving E[g(X)] ≤ g(E[X]) *(required: the −g substitution and the explicit sign-flip step)* | — |
| E4 | explain | derivation | 1.44 | Sketch how Jensen's inequality, applied to −log (a convex function), proves that KL divergence is always ≥ 0 — the property `jensen-inequality` supplies to `kl-divergence`. | KL(P‖Q) = E_P[−ln(q(X)/p(X))]; since −ln is convex, Jensen gives E_P[−ln(q(X)/p(X))] ≥ −ln(E_P[q(X)/p(X)]); the expectation E_P[q(X)/p(X)] = Σ p(x)·q(x)/p(x) = Σ q(x) = 1 (a probability distribution summing to 1), so −ln(1)=0, giving KL(P‖Q) ≥ 0 *(required: the substitution of −log, the E_P[q/p]=1 simplification, and the final −ln(1)=0 step)* | — |
| E5 | explain | derivation | 1.58 | Derive the AM–GM inequality (arithmetic mean ≥ geometric mean) for n positive numbers x₁,...,xₙ, as a consequence of Jensen's inequality applied to ln (concave), with X uniform on {x₁,...,xₙ}. | let X be uniform on {x₁,...,xₙ}; since ln is concave, Jensen (R8) gives E[ln X] ≤ ln(E[X]); the left side is (1/n)Σ ln(xᵢ) = ln((x₁x₂⋯xₙ)^{1/n}), the log of the geometric mean; the right side is ln((x₁+...+xₙ)/n), the log of the arithmetic mean; since ln is increasing, ln(GM) ≤ ln(AM) implies GM ≤ AM directly *(required: the identification of both sides as log-GM and log-AM, and the final increasing-function step)* | — |
| T2 | transfer | short-answer | 1.8 | An insurance actuary sets a premium above the expected payout (E[loss]), and a customer with concave utility (risk-averse) still finds buying the policy worthwhile, even though on average they'll pay more than they'll receive back. Using Jensen's inequality, explain why this trade is rational for the customer. | the customer's utility of a *certain* smaller wealth after paying the premium can exceed their *expected* utility of the uncertain, unhedged loss — by Jensen (concave u), E[u(uncertain wealth)] ≤ u(E[uncertain wealth]), so a risk-averse customer values certainty itself; the insurer's premium effectively buys the customer a move from an uncertain outcome to a certain one, and Jensen's gap (R13's risk-aversion consequence) is exactly the value the customer is willing to pay for that certainty, even above the expected payout *(required: connects the premium's rationality directly to the Jensen gap for concave utility)* | assumes buying insurance above expected payout is only rational if the customer is being irrational or miscalculating, missing the Jensen-based risk-aversion argument → `jensen-inequality` |
| T3 | transfer | short-answer | 1.9 | In variational inference, machine learning practitioners maximize a tractable "evidence lower bound" (ELBO) instead of an intractable true log-likelihood, justified by log E[X] ≥ E[log X] (Jensen applied to the concave log). Explain, at a conceptual level, why this single inequality licenses the entire variational-inference strategy. | the true log-likelihood involves a log of an intractable expectation/integral (log E[·]); since log is concave, Jensen guarantees log E[·] ≥ E[log ·] always — so E[log ·] (the ELBO) is *guaranteed* to sit at or below the true quantity no matter what, meaning maximizing the ELBO can never overshoot the true log-likelihood, only approach it from below; this one-directional guarantee is precisely what makes "optimize the tractable lower bound instead" a mathematically safe substitute rather than a heuristic approximation with unknown error direction *(required: names that the guarantee is one-directional — always a lower bound — and why that one-directionality is what makes the substitution safe)* | treats the ELBO as merely an approximation to the log-likelihood with unclear direction of error, missing that Jensen guarantees it is always a lower bound specifically → `jensen-inequality` |

*Coverage: 16/6/5/3 — 30 items, −0.3…1.9.*

---

## Modes of Convergence (`modes-of-convergence`)
*Prereq: Random Variables · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Define convergence in probability. | Xₙ →ₚ X if, for every ε>0, P(\|Xₙ−X\|>ε) → 0 as n→∞ | — |
| R2 | recall | mcq | −0.3 | The correct implication hierarchy, strongest to weakest, is: | almost sure ⟹ in probability ⟹ in distribution | reverses the order, believing convergence in distribution is the strongest → `modes-of-convergence` |
| A1 | apply | short-answer | 0.2 | Xₙ = X + 1/n for a fixed random variable X. Does Xₙ → X almost surely? In probability? | yes to both — Xₙ(ω) − X(ω) = 1/n → 0 for *every* outcome ω, so convergence is sure (hence almost sure), and almost sure convergence implies convergence in probability | — |
| A2 | apply | short-answer | 0.35 | The classic "typewriter sequence": Xₙ is the indicator of a shrinking-width interval that sweeps repeatedly across [0,1], hitting every point infinitely often. It converges to 0 in probability but *not* almost surely. Explain why this construction achieves that split. | P(Xₙ=1) → 0 as the interval widths shrink (giving convergence in probability), yet for *every fixed* ω, Xₙ(ω)=1 happens infinitely often as the sweep repeats forever — so no individual sample path ever settles at 0, which is exactly what almost sure convergence would require *(required: distinguishes "probability of being 1 shrinks" from "every individual path eventually stays near 0")* | — |
| E1 | explain | short-answer | 0.9 | Why is convergence in distribution the *weakest* of the three modes? | it only requires the CDFs Fₙ(x) → F(x) pointwise (at continuity points) — it says nothing about Xₙ and X being numerically close to each other, and the two sequences need not even live on the same probability space *(required, contrasted with convergence in probability, which does require \|Xₙ−X\| to actually shrink)* | — |
| E2 | explain | short-answer | 1.05 | Using the typewriter sequence from A2, explain the mechanism by which convergence in probability fails to imply almost sure convergence. | the set of outcomes where Xₙ is "far" from the limit keeps shrinking in probability at each n, but that set keeps *moving around* the sample space rather than shrinking to nothing and staying there — so every individual ω keeps getting hit again and again, even though the probability of being hit at any single fixed n goes to 0 *(required: the "shrinking but moving" mechanism, not just "it's a known counterexample")* | — |
| T1 | transfer | short-answer | 1.4 | An estimator θ̂ₙ is known to converge to the true θ *in probability*. A colleague says: "that means for any realization of the data, θ̂ₙ eventually gets arbitrarily close to θ and stays there." Is the colleague right? What would need to be true instead? | no — the colleague has described *almost sure* convergence, a strictly stronger claim; convergence in probability only guarantees the *probability* of a large deviation shrinks at each fixed n, not that any particular realized sequence permanently settles down; the colleague's statement would require θ̂ₙ → θ almost surely | accepts the colleague's description as an equivalent restatement of convergence in probability → `modes-of-convergence` |
| R3 | recall | short-answer | −0.56 | Define convergence in distribution. | Xₙ →_d X if Fₙ(x) → F(x) at every point x where F is continuous | — |
| R4 | recall | mcq | −0.52 | Almost sure convergence, Xₙ → X a.s., means: | P(lim_{n→∞} Xₙ = X) = 1 — the set of outcomes ω for which the ordinary sequence Xₙ(ω) converges to X(ω) has probability 1 | means P(Xₙ = X) → 1 as n→∞, confusing the probability of equality at each n with the probability of the limit actually existing and equaling X | — |
| R5 | recall | mcq | −0.48 | True or false: convergence in probability implies convergence in distribution. | True — this is the weakest implication in the hierarchy from R2, and it always holds | claims convergence in probability and convergence in distribution are unrelated, missing the standard implication | — |
| R6 | recall | short-answer | −0.44 | Briefly explain why convergence in distribution does NOT, in general, imply convergence in probability. | convergence in distribution only constrains the CDFs Fₙ and F to get close — Xₙ and X need not even be defined on the same probability space, or track each other outcome-by-outcome at all, so there's nothing requiring \|Xₙ−X\| itself to shrink | — |
| R7 | recall | mcq | −0.4 | Convergence in Lᵖ (in p-th mean) is defined as: | E[\|Xₙ−X\|^p] → 0 as n→∞ | defines it as Var(Xₙ)→0, confusing convergence in mean-square (a special case, p=2, centered at X rather than at a fixed value) with a general Lᵖ statement | — |
| R8 | recall | mcq | −0.36 | True or false: convergence in L² (mean square) implies convergence in probability. | True — by Chebyshev/Markov applied to E[(Xₙ−X)²], this is a standard implication | claims L² convergence and convergence in probability are unrelated modes, missing the Chebyshev-based implication | — |
| R9 | recall | short-answer | −0.32 | Fill in the blank: R2's hierarchy can be extended — almost sure convergence implies convergence in probability, which implies convergence in ___. | distribution (the weakest of the three named modes) | — |
| R10 | recall | mcq | −0.28 | L² convergence (E[(Xₙ−X)²]→0) is also commonly called: | convergence in quadratic mean | calls it "convergence in probability," conflating two genuinely distinct (though related) modes → `modes-of-convergence` |
| R11 | recall | mcq | −0.24 | True or false: if Xₙ → c in distribution for a *constant* c, then Xₙ → c in probability as well. | True — convergence in distribution to a constant is a special case strong enough to upgrade to convergence in probability | claims convergence in distribution never upgrades to convergence in probability, even in this special constant-limit case → `modes-of-convergence` |
| R12 | recall | short-answer | −0.2 | Fill in the blank: convergence in probability requires that, for every ε>0, ___. | P(\|Xₙ−X\| > ε) → 0 as n → ∞ | — |
| R13 | recall | short-answer | −0.16 | State the basic idea of Slutsky's theorem in one line. | if Xₙ → X in distribution and Yₙ → c in probability (a constant), then combinations like Xₙ+Yₙ and XₙYₙ converge in distribution to X+c and Xc respectively | — |
| R14 | recall | mcq | −0.12 | Which mode of convergence is the conclusion of the Central Limit Theorem stated in? | convergence in distribution — the standardized sample mean converges in distribution to N(0,1) | claims the CLT's conclusion is convergence in probability or almost surely, both strictly stronger than what the CLT actually delivers → `modes-of-convergence` |
| R15 | recall | mcq | −0.08 | True or false: almost sure convergence always implies convergence in Lᵖ (for any p). | False — almost sure convergence alone doesn't control the *size* of the deviations, only that they eventually vanish pathwise; extra conditions (e.g. a dominating bound, as in the dominated convergence theorem) are needed for Lᵖ convergence to follow | claims almost sure convergence is the strongest mode and therefore automatically implies every other mode including Lᵖ, missing that a.s. and Lᵖ are not strictly comparable in general → `modes-of-convergence` |
| R16 | recall | short-answer | −0.04 | Fill in the blank: the sample mean X̄ₙ converging to μ in probability, for iid data with finite variance, is guaranteed by the ___. | Weak Law of Large Numbers | — |
| A3 | apply | short-answer | 0.24 | Xₙ = Y/n, for a fixed random variable Y with finite variance. Does Xₙ → 0 in probability? | yes — for every outcome ω, Xₙ(ω) = Y(ω)/n → 0 as n→∞, so convergence holds surely (hence almost surely), and almost sure convergence implies convergence in probability (the same reasoning as A1) | claims Xₙ fails to converge because Y is random and its value is "unknown," missing that the *same* Y(ω) is being divided by a growing n for every fixed ω | — |
| A4 | apply | numeric | 0.3 | Xₙ ~ N(0, 1/n). Using Chebyshev, find the smallest n guaranteeing P(\|Xₙ\| ≥ 0.1) ≤ 0.01. `[verified: n=10000]` | Var(Xₙ)=1/n; Chebyshev gives P(\|Xₙ\|≥0.1) ≤ (1/n)/(0.1)² = 100/n; setting 100/n ≤ 0.01 gives n ≥ 10000 — confirming Xₙ → 0 in probability, since the bound shrinks for any fixed threshold as n grows | sets up the Chebyshev bound as (1/n)/0.1 instead of (1/n)/(0.1)², forgetting to square the threshold | — |
| A5 | apply | short-answer | 0.4 | Xₙ = n with probability 1/n, and 0 otherwise. Show Xₙ → 0 in probability, and contrast this with E[Xₙ]. | for any 0<ε<1, P(\|Xₙ\|≥ε) = P(Xₙ=n) = 1/n → 0, so Xₙ → 0 in probability; yet E[Xₙ] = n·(1/n) + 0·(1−1/n) = 1 for *every* n — the expectation stays fixed at 1 and never approaches 0, showing convergence in probability does not by itself guarantee convergence of expectations | assumes E[Xₙ]→0 must follow automatically from Xₙ→0 in probability, missing that this is exactly the counterexample the item is illustrating → `modes-of-convergence` |
| A6 | apply | short-answer | 0.32 | Xₙ ~ Bernoulli(pₙ), with pₙ = 1/n². Show directly from the definition that Xₙ → 0 in probability. | for any ε ∈ (0,1), P(\|Xₙ\|≥ε) = P(Xₙ=1) = pₙ = 1/n² → 0 as n→∞, which is exactly the definition of Xₙ →ₚ 0 | checks only that pₙ is small for one specific n rather than verifying the limit pₙ→0 as n→∞ | — |
| E3 | explain | short-answer | 0.98 | Explain why convergence in L² (mean square) is generally considered a *stronger* mode than convergence in probability, using the Chebyshev-based implication from R8. | Chebyshev's inequality gives P(\|Xₙ−X\|≥ε) ≤ E[(Xₙ−X)²]/ε²; if E[(Xₙ−X)²]→0 (L² convergence), the right side goes to 0 for any fixed ε, forcing P(\|Xₙ−X\|≥ε)→0 as well — so L² convergence *forces* convergence in probability, but not vice versa (a sequence's probabilities of large deviation can shrink without its squared deviations having a controlled, finite average) *(required: the explicit Chebyshev chain, not just asserting L² is stronger)* | — |
| E4 | explain | short-answer | 1.12 | Using A5's example, explain precisely what convergence in probability does and does not guarantee about an estimator's average behavior. | it guarantees that the *probability* of the estimator being far from its target shrinks to 0 as n grows — but, as A5 shows concretely, it says nothing about the estimator's expectation, which can remain fixed (or even diverge) even as the probability of a large deviation vanishes; practically, this means "θ̂ₙ is consistent (converges in probability)" is a weaker guarantee than "E[θ̂ₙ]→θ," and the two can genuinely come apart *(required: explicit reference to A5's numeric example as the mechanism)* | — |
| E5 | explain | short-answer | 1.2 | Explain, structurally, why almost sure convergence and Lᵖ convergence are not directly comparable in general — i.e. why neither implies the other without additional conditions. | almost sure convergence is a *pathwise* statement (each individual ω's sequence eventually settles near the limit, with no control on how large deviations get along the way before settling), while Lᵖ convergence is an *averaged magnitude* statement (the expected size of the deviation must shrink, which can fail if occasional huge deviations on a shrinking-probability set keep the average large); a.s. convergence with unbounded excursions can fail Lᵖ convergence, while an Lᵖ-convergent sequence need not settle down pathwise for every individual ω, only on average *(required: names the pathwise-vs-averaged distinction as the reason neither implies the other)* | — |
| T2 | transfer | short-answer | 1.5 | A stock's price forecast model is described as "the forecast error converges to 0 in probability as more data arrives." A trader plans to eventually stop hedging entirely once the model has "enough data," reasoning that the error is going to 0. Explain the flaw, using A5's mechanism. | convergence in probability only means the *probability* of a large forecast error shrinks — it does not rule out that, on the rare (shrinking-probability) occasions an error does occur, it could be enormous, exactly as in A5 where a rare n-sized spike kept E[Xₙ]=1 even as P(Xₙ≠0)→0; a trader relying on "the error is basically 0 now" without accounting for that rare-but-large-magnitude tail risk could face catastrophic losses precisely when the rare deviation occurs *(required: explicit tie to A5's shrinking-probability-but-large-magnitude structure)* | treats "converges to 0 in probability" as equivalent to "the error is now reliably small in every case," missing the rare-large-deviation possibility → `modes-of-convergence` |
| T3 | transfer | short-answer | 1.6 | A machine learning paper proves a model's training loss "converges in distribution to a fixed value c as the dataset grows." A reviewer asks whether this guarantees the loss on any *specific* future large training run will actually be close to c. Using R11 and the general hierarchy, answer the reviewer. | since the limit here is a *constant* c rather than a genuinely random limiting distribution, R11 applies: convergence in distribution to a constant upgrades to convergence in probability — so yes, for any ε>0, P(\|loss_n−c\|>ε)→0, meaning a specific large run's loss is increasingly likely to be close to c; had the limit instead been a genuinely random variable (not a constant), convergence in distribution alone would say nothing about any individual run's actual value, only about the shape of the histogram across many hypothetical runs *(required: explicitly invokes the constant-limit special case from R11, and contrasts with the general non-constant case)* | assumes convergence in distribution never says anything about individual runs, missing R11's special case for a constant limit → `modes-of-convergence` |

*Coverage: 16/6/5/3 — 30 items, −0.6…1.6.*

---

## Law of Large Numbers (`law-of-large-numbers`)
*Prereq: Modes of Convergence, Chebyshev's Inequality · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | State the Weak Law of Large Numbers. | for iid X₁,…,Xₙ with mean μ and finite variance, X̄ₙ →ₚ μ as n→∞ | — |
| R2 | recall | mcq | 0.15 | The Strong LLN differs from the Weak LLN in that it establishes: | convergence *almost surely* rather than merely in probability | claims it establishes "a faster rate of convergence" — the distinction is the *mode* of convergence, not the speed → `law-of-large-numbers` |
| A1 | apply | derivation | 0.65 | X̄ₙ has mean μ and variance σ²/n. Apply Chebyshev to bound P(\|X̄ₙ−μ\|≥ε) for fixed ε, and show it → 0 as n→∞. | P(\|X̄ₙ−μ\|≥ε) ≤ Var(X̄ₙ)/ε² = σ²/(nε²) → 0 as n→∞, for any fixed ε>0 — this *is* the definition of convergence in probability *(required: identifies that the shrinking bound is exactly what the definition demands)* | — |
| A2 | apply | numeric | 0.8 | For iid Xᵢ with σ²=4, find the smallest n (via A1's Chebyshev bound) guaranteeing P(\|X̄ₙ−μ\|≥0.5) ≤ 0.05. `[verified: n=320]` | σ²/(nε²) ≤ 0.05 ⟹ n ≥ σ²/(ε²·0.05) = 4/0.0125 = 320 | — |
| E1 | explain | derivation | 1.35 | Prove the Weak LLN formally, combining X̄ₙ's variance σ²/n with Chebyshev's inequality. | Var(X̄ₙ)=σ²/n (from properties of an average of iid terms); Chebyshev gives P(\|X̄ₙ−μ\|≥ε) ≤ σ²/(nε²); since this → 0 for every fixed ε>0, X̄ₙ →ₚ μ *by definition* *(required: the explicit "by definition of convergence in probability" closing step, tying the bound to the formal statement being proved)* | — |
| E2 | explain | short-answer | 1.5 | Why does the LLN require *finite variance*? What breaks for a Cauchy-distributed Xᵢ (no finite mean or variance)? | without finite variance, the Chebyshev proof technique collapses entirely (the bound σ²/(nε²) is infinite/meaningless); for genuinely heavy-tailed cases like Cauchy, the sample mean does not converge to any constant as n grows at all *(required: connects the proof's breakdown to the actual failure of convergence, not just "variance is needed for the proof")* | — |
| T1 | transfer | short-answer | 1.85 | A casino relies on LLN across millions of bets despite genuine per-bet uncertainty. Explain why LLN makes the casino's *average* profit per bet reliably close to its expected edge as bet count grows — and why this same logic would *not* protect a gambler making one enormous, one-time bet with "the same underlying probabilities." | LLN concentrates the *average over many repeated trials*; the casino's average profit per hand converges to its expected edge as n→∞, so total profit grows reliably; a single large bet is essentially n=1, where LLN provides no guarantee — the outcome variance for one trial stays large regardless of what happens as n→∞ elsewhere *(required: names that LLN's protection comes from repetition, not from the probabilities themselves)* | argues the gambler is equally protected because "the probabilities are the same" → `law-of-large-numbers` |
| R3 | recall | short-answer | −0.11 | Fill in the blank: for iid X₁,...,Xₙ with variance σ², Var(X̄ₙ) = ___. | σ²/n | — |
| R4 | recall | mcq | −0.07 | As n → ∞, Var(X̄ₙ) = σ²/n: | approaches 0 | claims Var(X̄ₙ) stays fixed at σ² regardless of n, missing that averaging shrinks variance by the factor 1/n | — |
| R5 | recall | mcq | −0.03 | True or false: the standard LLN requires the Xᵢ to be identically distributed, not just independent. | True — the "iid" (independent AND identically distributed) assumption is required for the standard statement, with μ and σ² shared across all the Xᵢ | claims independence alone (without identical distribution) is sufficient for the standard LLN as stated, missing the identically-distributed half of "iid" → `law-of-large-numbers` |
| R6 | recall | short-answer | 0.01 | State the Strong Law of Large Numbers formally. | for iid X₁,...,Xₙ with mean μ (and finite variance, in the version used here), X̄ₙ → μ almost surely as n→∞ | — |
| R7 | recall | mcq | 0.05 | Which earlier concept's inequality is used to prove the Weak LLN in this bank? | Chebyshev's inequality | credits Markov's inequality directly, skipping that Chebyshev (itself built on Markov) is the one applied to X̄ₙ in A1/E1 → `law-of-large-numbers` |
| R8 | recall | mcq | 0.09 | True or false: LLN says the raw sum ΣXᵢ converges to nμ as n→∞. | False — LLN concerns the *average* X̄ₙ converging to μ; the raw sum ΣXᵢ generally grows without bound (it's on the order of nμ, but doesn't "converge" to anything) | claims LLN is a statement about the sum converging to a fixed number, confusing the sum's growing scale with the average's convergence → `law-of-large-numbers` |
| R9 | recall | short-answer | 0.13 | Fill in the blank: LLN concerns convergence of the sample ___ to the population ___. | mean; mean | — |
| R10 | recall | mcq | 0.17 | Which condition, if violated (as with a Cauchy-distributed Xᵢ), breaks the standard LLN's proof technique and conclusion? | finite mean (and, for this cluster's Chebyshev-based proof, finite variance) | claims LLN breaks only if the Xᵢ are dependent, missing that the deeper issue with Cauchy is the lack of a finite mean at all | — |
| R11 | recall | mcq | 0.21 | True or false: the Strong LLN implies the Weak LLN, since almost sure convergence implies convergence in probability. | True — this is a direct instance of `modes-of-convergence`'s implication hierarchy applied to X̄ₙ | claims the Weak and Strong LLN are logically independent results with no implication between them, missing the convergence-mode hierarchy connecting them | — |
| R12 | recall | short-answer | 0.25 | Fill in the blank: LLN is the formal mathematical justification for interpreting probability as a ___. | long-run relative frequency | — |
| R13 | recall | short-answer | 0.29 | What does the acronym "iid" stand for, as used in the LLN's hypothesis? | independent and identically distributed | — |
| R14 | recall | mcq | 0.33 | LLN describes the behavior of X̄ₙ as: | the sample size n → ∞ | describes LLN as a statement about a single fixed n, missing that it's fundamentally an asymptotic (limiting) statement | — |
| R15 | recall | mcq | 0.37 | True or false: for any fixed finite n, LLN by itself guarantees X̄ₙ is close to μ. | False — LLN is an asymptotic statement about the *limit* as n→∞; for any specific finite n, only a finite-sample tool like Chebyshev (A1/A2) gives an actual quantitative guarantee | claims LLN directly guarantees closeness for every finite n, missing that it's purely a limiting statement requiring n→∞ → `law-of-large-numbers` |
| R16 | recall | short-answer | 0.41 | Fill in the blank: the Weak LLN's conclusion is convergence in ___. | probability | — |
| A3 | apply | numeric | 0.58 | iid data with σ²=9. Find the smallest n such that Var(X̄ₙ) ≤ 0.01. `[verified: n=900]` | Var(X̄ₙ)=σ²/n=9/n ≤ 0.01 ⟹ n ≥ 900 | solves 9/n≤0.01 as n≤900, inverting the direction | — |
| A4 | apply | numeric | 0.7 | Xᵢ ~ Bernoulli(0.5), so σ²=0.25. Using Chebyshev, bound P(\|X̄₁₀₀₀−0.5\|≥0.05) for n=1000. `[verified: 0.1]` | P ≤ σ²/(nε²) = 0.25/(1000·0.0025) = 0.1 | plugs in σ instead of σ² into the Chebyshev bound formula, getting a different (incorrect) numeric answer | — |
| A5 | apply | numeric | 0.75 | Fair die rolls, σ²=35/12≈2.9167. Find the smallest n such that Chebyshev bounds P(\|X̄ₙ−3.5\|≥0.1) ≤ 0.05. `[verified: n=5834]` | σ²/(nε²) ≤ 0.05 ⟹ n ≥ σ²/(ε²·0.05) = 2.9167/(0.01×0.05) = 2.9167/0.0005 ≈ 5833.3, so n=5834 | uses ε=0.1 without squaring it in the denominator, getting a much smaller (incorrect) required n | — |
| A6 | apply | short-answer | 0.72 | Explain, using LLN, why averaging n iid samples of g(X) for some function g provides a valid Monte Carlo estimate of E[g(X)] as n grows. | let Yᵢ=g(Xᵢ); the Yᵢ are themselves iid with mean E[g(X)] (assuming it's finite), so by the Weak LLN, Ȳₙ = (1/n)Σg(Xᵢ) →ₚ E[g(X)] — averaging simulated function values converges in probability to the true expectation, which is exactly the justification for Monte Carlo estimation | assumes LLN requires g(X) itself to be a "nice" distribution like Normal, missing that LLN applies to the average of *any* iid sequence with finite mean, including g(Xᵢ) for essentially any reasonable g → `law-of-large-numbers` |
| E3 | explain | short-answer | 1.42 | Explain why the Strong LLN implies the Weak LLN, using `modes-of-convergence`'s hierarchy. | the Strong LLN's conclusion is X̄ₙ → μ *almost surely*; `modes-of-convergence` establishes that almost sure convergence always implies convergence in probability (R2/R9 of that concept); applying that general implication to the specific sequence X̄ₙ converging a.s. to μ immediately yields X̄ₙ →ₚ μ, which is exactly the Weak LLN's statement *(required: the explicit citation of the general a.s.⟹probability implication, applied to this specific case)* | — |
| E4 | explain | short-answer | 1.55 | Give an example of identically distributed but *not independent* random variables where the sample mean fails to converge to the population mean, illustrating why independence (not just identical distribution) is essential to LLN. | let X₁ be any random variable with mean μ and finite variance, and set Xᵢ=X₁ for every i (perfectly correlated, identically distributed copies); then X̄ₙ = (1/n)·n·X₁ = X₁ for every n — the sample mean never moves at all, let alone converges to μ, unless X₁ happens to equal μ exactly; the identical-distribution assumption alone says nothing about how the variables relate to each other across draws, and it's independence that guarantees genuinely new information arrives with each additional observation *(required: the explicit Xᵢ=X₁ construction and the observation that X̄ₙ never changes)* | — |
| E5 | explain | derivation | 1.65 | Using Var(X̄ₙ)=σ²/n, derive how the *standard deviation* of X̄ₙ scales with n, and explain why this is called a "1/√n rate" of convergence. | SD(X̄ₙ) = √(Var(X̄ₙ)) = √(σ²/n) = σ/√n; as n grows, this shrinks proportionally to 1/√n — so halving the standard deviation (doubling precision) requires *quadrupling* n, not just doubling it, since (1/√(4n)) = (1/2)(1/√n) *(required: the σ/√n derivation and the explicit "quadruple n to halve SD" consequence)* — this is the origin of the "standard error" scaling seen throughout later statistics clusters | — |
| T2 | transfer | short-answer | 1.9 | A computational scientist runs a Monte Carlo simulation with n=10,000 trials to estimate an integral, and finds the estimate's standard error too large for their needs. A colleague suggests simply doubling n to 20,000 to halve the error. Using E5's rate, evaluate this suggestion. | the colleague is wrong to expect a halved error from doubling n — by E5's 1/√n scaling, doubling n only shrinks the standard error by a factor of 1/√2≈0.707 (about 29%), not by half; achieving an actual halving of the standard error requires *quadrupling* n to 40,000, a far more expensive computation than the colleague's proposal accounts for *(required: the numeric 1/√2 factor for doubling n, and the correct quadrupling figure for actually halving the error)* | assumes doubling the sample size halves the standard error, missing the square-root scaling from E5 → `law-of-large-numbers` |
| T3 | transfer | short-answer | 2.0 | An A/B test has been running for a week with n=50,000 visitors per arm, and the team debates whether running it "just one more day" (adding roughly 7,000 more visitors per arm) will meaningfully sharpen the estimate of the conversion-rate difference. Using the 1/√n scaling, assess this. | going from n=50,000 to n=57,000 changes the standard error by a factor of √(50000/57000)≈0.936 — only about a 6.4% reduction in standard error for a 14% increase in sample size; the 1/√n law means each *additional* batch of visitors buys proportionally less precision than the batch before it, so "one more day" delivers a real but modest sharpening, not the meaningful jump the team might be hoping for *(required: the approximate 6% figure or equivalent quantitative diminishing-returns statement, not just "more data helps a little")* | assumes each additional day of data contributes roughly the same precision gain as the very first days of the test, missing the diminishing 1/√n returns → `law-of-large-numbers` |

*Coverage: 16/6/5/3 — 30 items, −0.15…2.0. A1/E1 together are literally the proof of the Weak LLN, split across an apply item and its explain-level restatement.*

---

## Order Statistics (`order-statistics`)
*Prereq: CDF · ancestors 6 · b₀ = 0.47*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.53 | Define the order statistics X₍₁₎,…,X₍ₙ₎ of a sample. Which is the min, which the max? | the sorted values X₍₁₎≤X₍₂₎≤⋯≤X₍ₙ₎; X₍₁₎ is the min, X₍ₙ₎ is the max | — |
| R2 | recall | mcq | −0.23 | The CDF of the maximum, F₍ₙ₎(x), equals: | F(x)ⁿ | picks 1−[1−F(x)]ⁿ — that's actually the CDF of the *minimum*, an easy swap → `order-statistics` |
| A1 | apply | derivation | 0.27 | Derive F₍ₙ₎(x) = F(x)ⁿ directly from independence. | max≤x ⟺ *all* n values ≤x; by independence, P(all ≤x) = ΠP(Xᵢ≤x) = F(x)ⁿ *(required: the "all, not just one" equivalence)* | — |
| A2 | apply | numeric | 0.4 | X₁,…,X₅ iid Uniform(0,1). Find P(max ≤ 0.9). `[verified: 0.59049]` | F(x)=x for Uniform(0,1), so F₍₅₎(0.9) = 0.9⁵ ≈ 0.590 | uses n·F(x) instead of F(x)ⁿ → `order-statistics` |
| E1 | explain | derivation | 0.97 | Derive the CDF of the minimum, F₍₁₎(x) = 1−[1−F(x)]ⁿ, via the complement trick. | P(min≤x) = 1 − P(min>x) = 1 − P(all Xᵢ>x) = 1 − [1−F(x)]ⁿ by independence *(required: the complement step)* | — |
| E2 | explain | short-answer | 1.1 | Connect order statistics directly to `mle`'s earlier finding: the Uniform(0,θ) MLE is θ̂=max(x₁,…,xₙ). Explain what this reveals about *why* that MLE is biased. | θ̂ = X₍ₙ₎, the largest order statistic; every observed value is ≤ θ (by the distribution's own support), so their max is too — meaning X₍ₙ₎ *never exceeds* θ and therefore E[X₍ₙ₎] < θ strictly, confirming the MLE systematically underestimates θ *(required: the "max can never exceed θ" argument, naming X₍ₙ₎ explicitly as an order statistic)* | — |
| T1 | transfer | short-answer | 1.47 | Civil engineers designing a levee need the distribution of the *worst* (highest) flood level over 100 years, not the average year's level. Explain why an extreme's distribution behaves very differently from a single year's, and why "average year plus a margin" can be a dangerously inadequate design approach. | the max's distribution concentrates in the upper tail of the single-year distribution, and — for many common underlying distributions — E[max over n years] can keep growing with n, unlike the single-year mean, which stays fixed; a fixed additive margin built from typical-year data ignores that extremes are governed by tail behavior with its own scaling, and can badly underestimate the risk of an unlucky multi-year confluence *(required: names that the max's expectation grows with n while the single-year mean doesn't)* | assumes the worst-case-over-100-years level is well-approximated by the single-year mean plus a fixed buffer → `order-statistics` |
| R3 | recall | short-answer | −0.49 | Fill in the blank: the CDF of the minimum, F₍₁₎(x) = ___. | 1 − [1−F(x)]ⁿ | — |
| R4 | recall | mcq | −0.45 | The quantity X₍ₙ₎ − X₍₁₎ (max minus min) is called: | the sample range | calls it "the sample variance," confusing the range with an entirely different spread measure | — |
| R5 | recall | mcq | −0.41 | True or false: the standard order-statistics formulas (for F₍₁₎, F₍ₙ₎, and the general F₍ₖ₎) require the underlying X₁,...,Xₙ to be iid. | True — independence is used explicitly in A1's "all ≤ x" product step, and identical distribution ensures every Xᵢ shares the same F | claims the formulas hold for any Xᵢ regardless of independence, missing that A1's derivation explicitly relies on it | — |
| R6 | recall | short-answer | −0.37 | For an odd sample size n, define the sample median as a specific order statistic. | the median is X₍(n+1)/2₎, the order statistic sitting exactly in the middle of the sorted sample | — |
| R7 | recall | mcq | −0.33 | For continuous iid Xᵢ, the probability of a tie among the order statistics (two Xᵢ taking exactly the same value) is: | 0 — a standard consequence of a continuous distribution assigning probability 0 to any single point | claims ties occur with positive probability for continuous variables, ignoring that any single value has probability 0 under a continuous distribution | — |
| R8 | recall | mcq | −0.29 | True or false: by definition, the order statistics of a fixed sample always satisfy X₍₁₎ ≤ X₍₂₎ ≤ ⋯ ≤ X₍ₙ₎. | True — "order statistics" means the sample values sorted into nondecreasing order, so this holds by construction, not as a probabilistic claim | claims the ordering only holds "on average" or with high probability, missing that it's a deterministic consequence of the definition (sorting) → `order-statistics` |
| R9 | recall | short-answer | −0.25 | State the general formula for the density of the k-th order statistic, f₍ₖ₎(x), for continuous iid X₁,...,Xₙ. | f₍ₖ₎(x) = [n!/((k−1)!(n−k)!)] · [F(x)]^{k−1} · [1−F(x)]^{n−k} · f(x) | — |
| R10 | recall | mcq | −0.21 | Which value of k in the general order-statistic density formula (R9) recovers the density of the maximum? | k=n | picks k=1, which recovers the density of the *minimum* instead of the maximum → `order-statistics` |
| R11 | recall | mcq | −0.17 | True or false: for a sample of size n=1, the minimum and maximum order statistics coincide with the single observation. | True — X₍₁₎=X₍ₙ₎=X₁ trivially when n=1 | claims the min and max are undefined or differ even for a single observation, missing the trivial n=1 case → `order-statistics` |
| R12 | recall | short-answer | −0.13 | Fill in the blank: order statistics are used to define sample ___, such as the median and other percentiles. | quantiles (percentiles) | — |
| R13 | recall | short-answer | −0.09 | Informally, what is the p-th percentile of a sample, in terms of order statistics? | (roughly) the order statistic X₍ₖ₎ where k is chosen so that about p% of the sorted sample falls at or below that value | — |
| R14 | recall | mcq | −0.05 | In reliability engineering, which order statistic typically summarizes a "weakest-link" system's failure point? | the minimum, X₍₁₎ — the system fails as soon as its weakest component does | picks the maximum, X₍ₙ₎, which describes the *strongest* component rather than the weakest-link failure point → `order-statistics` |
| R15 | recall | mcq | −0.01 | True or false: order statistics discard the information of which specific original observation (X₁, X₂, etc.) produced each sorted value. | True — X₍₁₎,...,X₍ₙ₎ only record the sorted *values*, not their original labels/indices | claims order statistics preserve the original labeling, missing that sorting is exactly what discards that information → `order-statistics` |
| R16 | recall | short-answer | 0.03 | In the general k-th order statistic formula, what does the term [F(x)]^{k−1} represent? | the probability that k−1 of the n observations fall below the value x | — |
| A3 | apply | numeric | 0.3 | X₁,...,X₄ iid Uniform(0,1). Find P(min ≤ 0.2). `[verified: 0.5904]` | F₍₁₎(0.2) = 1−(1−0.2)⁴ = 1−0.8⁴ = 1−0.4096 = 0.5904 | computes 1−0.2⁴ instead of 1−(1−0.2)⁴, using F(x) instead of 1−F(x) inside the bracket → `order-statistics` |
| A4 | apply | numeric | 0.45 | X₁,...,X₆ iid Exponential(1), so F(x)=1−e⁻ˣ. Find P(max ≤ 2). `[verified: ≈0.4179]` | F₍₆₎(2) = F(2)⁶ = (1−e⁻²)⁶ ≈ (0.8647)⁶ ≈ 0.4179 | applies F₍₁₎'s formula (1−[1−F(x)]ⁿ) to the maximum instead of F₍ₙ₎'s formula F(x)ⁿ, swapping min and max again → `order-statistics` |
| A5 | apply | numeric | 0.35 | X₁,X₂,X₃ iid Uniform(0,1). Find P(min > 0.5). `[verified: 0.125]` | P(min>0.5) = P(all Xᵢ>0.5) = (1−0.5)³ = 0.125 | computes P(min>0.5) as 1−F₍₁₎(0.5) using the wrong complement, or as 0.5³ directly without the (1−F(x)) structure → `order-statistics` |
| A6 | apply | numeric | 0.5 | X₁,X₂,X₃ iid Uniform(0,1). Using R9's general formula, find the density of the median (k=2) at x=0.5. `[verified: 1.5]` | f₍₂₎(0.5) = [3!/(1!·1!)]·(0.5)¹·(0.5)¹·1 = 6·0.25 = 1.5 | uses the wrong factorial coefficient, e.g. 3! instead of 3!/(1!1!), or omits the f(x)=1 factor for Uniform | — |
| E3 | explain | derivation | 1.18 | Derive the general k-th order statistic density formula f₍ₖ₎(x) = [n!/((k−1)!(n−k)!)]·F(x)^{k−1}·[1−F(x)]^{n−k}·f(x), using a combinatorial argument. | for X₍ₖ₎ to land in a small interval [x,x+dx], exactly one of the n observations must fall in that interval (probability f(x)dx), exactly k−1 of the remaining must fall below x (probability F(x)^{k−1}), and the remaining n−k must fall above x (probability [1−F(x)]^{n−k}); the multinomial coefficient n!/((k−1)!·1!·(n−k)!) counts the number of ways to assign which of the n observations plays each of these three roles *(required: the three-role partition — below, at, above — and the multinomial coefficient counting the assignments)* | — |
| E4 | explain | short-answer | 1.28 | Explain why the sample minimum of n iid Uniform(0,1) variables converges to 0 in probability as n→∞, connecting to `modes-of-convergence`'s definition. | by R3, P(min ≤ ε) = 1−(1−ε)ⁿ for any fixed ε>0, and since 0<1−ε<1, (1−ε)ⁿ→0 as n→∞, so P(min≤ε)→1, i.e. P(min>ε)→0; this is exactly the definition of the minimum converging to 0 in probability (`modes-of-convergence`'s R12), since the probability of the minimum staying above any fixed threshold shrinks to nothing as more observations are added *(required: the explicit (1−ε)ⁿ→0 limit and the tie to the formal convergence-in-probability definition)* | — |
| E5 | explain | derivation | 1.38 | For n=2 iid continuous X₁, X₂, derive the joint density of (min, max) — call them (U,V) with U≤V — in terms of f and F. | for u<v, either X₁=u, X₂=v or X₁=v, X₂=u (the only two orderings giving min=u, max=v); each ordering contributes density f(u)f(v) by independence, and there are 2 orderings, so f_{U,V}(u,v) = 2f(u)f(v) for u<v (and 0 for u≥v) *(required: the "2 orderings, each contributing f(u)f(v)" counting argument)* — this joint structure is what the marginal formulas (R9, for k=1 and k=2 with n=2) are built from | — |
| T2 | transfer | short-answer | 1.55 | A chain has n=10 links, each with iid strength Xᵢ (continuous distribution, known F). The chain's overall strength is the *minimum* link strength (weakest-link failure), not the average link strength. A quality engineer proposes certifying the chain based on the *average* link strength meeting a spec. Using order statistics, explain why this is the wrong quantity to certify against. | the chain fails when its *weakest* link fails, so the relevant quantity is the distribution of X₍₁₎ (the minimum, R14), whose CDF is F₍₁₎(x)=1−[1−F(x)]¹⁰ — a link population can have a perfectly good *average* strength while still having a nontrivial probability that at least one of 10 links is weak enough to fail; certifying on the average ignores exactly the tail behavior that the minimum-order-statistic formula captures, and understates the true failure probability *(required: identifies the minimum as the operative quantity and explains why average strength can mask a weak-link risk)* | agrees that certifying on average link strength is sufficient, since "the average summarizes the whole population" → `order-statistics` |
| T3 | transfer | short-answer | 1.65 | Sports commentators note that the marathon world record keeps slowly improving decade after decade, even though individual human running ability (the underlying distribution of runners' times) hasn't obviously changed much. Using order-statistics logic (echoing E4/T1), explain why the record — a running maximum (of performance) over a growing pool of competitors — is expected to keep improving even with a fixed underlying ability distribution. | the "world record" is effectively an extreme order statistic (the best performance, i.e. a max of *performance*, or equivalently a min of *time*) over the pool of everyone who has ever competed; as the number of competitors n grows over time (more athletes, more races, a larger global talent pool), the extreme order statistic's expected value keeps shifting toward the tail of the fixed underlying ability distribution — exactly the mechanism from T1's flood-levee example, where E[extreme] grows with n even though the single-year (single-athlete) distribution stays fixed *(required: identifies the record as an extreme order statistic over a growing n, echoing T1's mechanism)* | assumes the record's improvement must mean the underlying human ability distribution itself has shifted, missing the pure sample-size/extreme-value effect | — |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.65.*

---

## Cluster 7 misconception index

| Tag | Blame |
|---|---|
| Markov bound quoted as the actual probability | `markov-inequality` |
| Chebyshev's universality mistaken for tightness | `chebyshev-inequality` |
| arithmetic mean applied to rates/multiplicative quantities | `jensen-inequality` |
| convergence-in-distribution mistaken for the strongest mode | `modes-of-convergence` |
| convergence in probability conflated with almost-sure convergence | `modes-of-convergence` |
| LLN's protection attributed to "favorable probabilities" rather than repetition | `law-of-large-numbers` |
| max/min CDF formulas swapped | `order-statistics` |
| Markov's inequality applied without the nonnegativity requirement, or to a negative-valued X | `markov-inequality` |
| Chebyshev's bound direction reversed (which way tightening a threshold moves the bound) | `chebyshev-inequality` |
| Jensen's inequality direction confused between convex and concave g | `jensen-inequality` |
| convergence in probability assumed to control expectations or magnitude of rare deviations | `modes-of-convergence` |
| LLN treated as a finite-sample guarantee rather than an asymptotic (n→∞) statement | `law-of-large-numbers` |
| 1/√n rate mistaken for a 1/n rate (doubling n assumed to halve the standard error) | `law-of-large-numbers` |
| order-statistic combinatorial coefficient or F(x)/(1−F(x)) roles swapped in the general k-th density formula | `order-statistics` |

**Cluster total: 180 items across 6 concepts (30 each).** All numeric claims verified, including the standard
normal tail probability at 3 SD (0.0027) checked against an erf-based CDF, confirming Chebyshev's bound
is roughly 40× looser there. The cluster's central achievement is structural, not just content: Chebyshev
is *built from* Markov, and the Weak Law of Large Numbers is *built from* Chebyshev, inside the items
themselves (A1/E1 of `law-of-large-numbers`) — a learner who completes this cluster in order has
assembled the LLN's proof from two earlier results, not been handed it as a black box.
