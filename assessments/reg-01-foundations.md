# Regression Cluster 1 — Foundations

Regression, Regress to the Mean, Linear Regression Terminology, Simple Linear Regression, Ordinary
Least Squares, Normal Equations (6 concepts). Same format as
[foundations-of-probability.md](foundations-of-probability.md). `normal-equations`' T1 is the payoff
item of this cluster: it shows OLS's central formula is literally `vector-projection`'s orthogonality
principle from the linear-algebra sweep, not merely inspired by it.

---

## Regression (`regression`)
*Root · ancestors 0 · b₀ = −0.50*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.5 | Define regression. | modeling the relationship between a response/outcome variable and one or more predictors, to predict or explain the response | — |
| R2 | recall | mcq | −1.2 | Regression is broadly a form of: | supervised learning, specifically for continuous outcomes | picks "unsupervised learning" — regression requires a known outcome to fit against, per `classification-vs-regression` in the ML sweep → `regression` |
| A1 | apply | short-answer | −0.75 | Is predicting a house's sale price from its square footage a regression problem? | yes | — |
| E1 | explain | short-answer | 0.0 | Why is regression both a prediction tool and an explanation tool, and how can these goals conflict? | prediction asks "what will Y be for a new X," while explanation asks "how does X relate to Y"; a model optimized purely for prediction accuracy (a complex ensemble) can be far harder to interpret than a simple linear model, so the two goals can pull in different directions *(required: names the tension explicitly)* | — |
| T1 | transfer | short-answer | 0.5 | Why does "regression" as a statistical term have an older, more specific history than its broad modern usage? | the term originated with Galton's studies of height and inherited traits, where he observed the literal phenomenon `regress-to-the-mean` names — the modern broad sense (any model predicting a continuous outcome) is a later generalization of that original, specific meaning *(required: names Galton's original observation as the origin)* | — |
| R3 | recall | short-answer | −1.48 | In the model Y = f(X) + ε, what does ε represent? | the error/noise term — the part of Y not explained by f(X) | — |
| R4 | recall | mcq | −1.42 | Which of these is a regression problem? | predicting tomorrow's high temperature (a number) | picks "classifying an email as spam or not" — that's a categorical outcome, a classification task → `regression` |
| R5 | recall | mcq | −1.36 | True or false: regression models can only capture linear relationships between X and Y. | False — nonlinear regression models exist (polynomial, spline, etc.) | picks "True" — conflates "regression" as a task family with "linear regression" as one specific model within it → `regression` |
| R6 | recall | short-answer | −1.3 | What distinguishes a regression problem from a classification problem? | regression predicts a continuous/numeric outcome; classification predicts a discrete category or label | — |
| R7 | recall | short-answer | −1.24 | What does the regression function E[Y\|X=x] describe? | the average (expected) value of the response Y at a given predictor value x | — |
| R8 | recall | short-answer | −1.18 | Give one real-world example of a regression problem. | e.g. predicting a house's sale price from its features, or predicting a patient's blood pressure from age and weight | — |
| R9 | recall | mcq | −1.12 | Simple regression, as opposed to multiple regression, uses: | exactly one predictor variable | picks "exactly one response variable" — every regression model (simple or multiple) has one response; the count that distinguishes them is predictors → `regression` |
| R10 | recall | short-answer | −1.06 | Fill in the blank: the variable being predicted in a regression model is called the ___ variable. | response (or dependent, or outcome) variable | — |
| R11 | recall | mcq | −1.0 | Which of the following is *not* a typical goal of a regression model? | assigning data points into discrete groups with no predefined outcome | picks "forecasting a future numeric value" as the odd one out — that is a core regression goal, not the exception → `regression` |
| R12 | recall | short-answer | −0.94 | What does it mean for a regression model to be "parametric"? | it assumes a fixed functional form for f(X) governed by a finite set of parameters to be estimated from data | — |
| R13 | recall | mcq | −0.88 | True or false: for a model to be called "regression," its predictor variables must be normally distributed. | False — normality assumptions (where they appear) concern the error term in some inference procedures, not a requirement for the predictors, and are not part of what defines regression | picks "True" — confuses distributional assumptions used for some inference procedures with the basic definition of the task → `regression` |
| R14 | recall | mcq | −0.82 | The statistical term "regression" is credited to: | Francis Galton, from his studies of height and inherited traits | picks "Carl Friedrich Gauss" — Gauss developed least squares, but the term "regression" itself comes from Galton → `regression` |
| R15 | recall | short-answer | −0.76 | What is the difference between a deterministic and a statistical relationship, in the sense that defines regression? | deterministic: Y is exactly determined by X with no randomness; statistical: Y = f(X) + noise, so X does not fully determine Y | — |
| R16 | recall | short-answer | −0.7 | Fill in the blank: a regression model with more than one predictor variable is called ___ regression. | multiple | — |
| A2 | apply | short-answer | −0.64 | A company wants to forecast next quarter's revenue from ad spend, headcount, and season. Is this a regression problem? | yes — the outcome (revenue) is continuous/numeric and there are multiple predictors, so it's a multiple regression problem | — |
| A3 | apply | mcq | −0.58 | Which of these is best modeled as classification, not regression? | predicting whether an email is spam (yes/no) | picks "predicting a stock's closing price" — that outcome is continuous, so it is regression, not classification → `regression` |
| A4 | apply | short-answer | −0.52 | A researcher wants to explain *how* temperature affects ice cream sales, not just predict future sales. Which of regression's two goals is this? | explanation, not pure prediction — understanding the relationship rather than only forecasting an outcome | — |
| A5 | apply | short-answer | −0.46 | A dataset has response Y = exam score (0–100) and predictor X = hours studied. Regression or classification, and why? | regression — the response Y is a continuous numeric quantity, not a category | — |
| A6 | apply | numeric | −0.4 | A fitted model predicts ŷ = 50 + 2x. What does it predict for x = 10? `[verified: 70]` | ŷ = 50 + 2(10) = 70 | — |
| E2 | explain | short-answer | 0.1 | Why is choosing a linear versus a nonlinear regression model a modeling decision, not something the data forces on its own? | the true relationship may be nonlinear, but a linear model can still be a reasonable, more interpretable approximation; the choice trades off bias, interpretability, and data requirements rather than being dictated purely by the underlying truth *(required: names the tradeoff, not just "pick whichever fits")* | — |
| E3 | explain | short-answer | 0.2 | Explain precisely why regression is classified as "supervised" learning. | the training data includes known response values Y alongside the predictors — that labeled outcome is the "supervision" the model learns from, in contrast to unsupervised methods like clustering that receive no such labels *(required: names the labeled-outcome distinction)* | — |
| E4 | explain | short-answer | 0.3 | Why can two variables show a strong regression relationship without one causing the other? | a fitted regression only captures statistical association; a confounding variable influencing both X and Y (or reverse causation) can produce the same observed relationship without any causal link from X to Y *(required: names confounding or reverse causation as the mechanism)* | — |
| E5 | explain | short-answer | 0.4 | Why is a continuous outcome variable necessary but not sufficient for a problem to be classic regression? | a continuous Y is what separates the regression family from classification's categorical outputs, but that alone says nothing about which model (linear, nonlinear, parametric, nonparametric) is appropriate — the outcome type only fixes the broad task, not the method *(required: the necessary-but-not-sufficient distinction)* | — |
| T2 | transfer | short-answer | 0.6 | How does the regression/classification split relate to the general supervised-learning task typology? | it is the same axis `supervised-vs-unsupervised-learning` sits above — supervised tasks split by response type, continuous (regression) versus categorical (classification) — so "regression" names one branch of a task taxonomy shared across statistics and ML *(required: names the shared taxonomy)* | — |
| T3 | transfer | short-answer | 0.8 | Why might an economist and an ML engineer both call their model "regression" yet judge it by very different criteria? | an economist typically wants interpretable, causally-defensible coefficients to explain a relationship, while an ML engineer typically wants the lowest predictive error regardless of interpretability — the same mathematical object (Y = f(X) + ε) serves explanation in one field and prediction in the other, so "good" is judged differently *(required: contrasts explanation-focused and prediction-focused evaluation)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.5…0.8.*

---

## Regress to the Mean (`regress-to-the-mean`)
*Prereq: Regression · ancestors 1 · b₀ = −0.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.15 | Describe regression to the mean. | extreme observations tend to be followed by less extreme ones, due to random variation alone, not necessarily any causal process | — |
| R2 | recall | mcq | −0.9 | A common mistake related to this phenomenon is: | wrongly attributing a natural statistical reversion to a causal intervention | claims this fallacy "never happens in practice" — it's a widely-documented, common error → `regress-to-the-mean` |
| A1 | apply | short-answer | −0.35 | A baseball player has an unusually good season, then performs closer to his career average the next. Why does regression to the mean predict this alone, with no ability change? | if observed performance = true ability + random noise, an extreme observed value likely had a large positive noise component by chance; fresh, independent noise the following season is unlikely to repeat that same extreme luck, pulling the observation back toward the true ability level *(required: the noise-decomposition argument)* | — |
| E1 | explain | short-answer | 0.35 | Explain the statistical mechanism behind regression to the mean precisely. | an extreme observed value is more likely to reflect a merely-good true ability plus a large favorable noise draw than an exceptional true ability alone; since noise is independent across measurements, the next measurement's noise won't replicate that same favorable draw, so the observation moves back toward the mean *(required: the "noise doesn't replicate" argument, not just restating the phenomenon)* | — |
| T1 | transfer | short-answer | 0.85 | Flight instructors observed praised pilots performing worse next time and criticized pilots performing better, concluding criticism works better than praise. Why is this likely a fallacy? | pilots singled out for praise had an unusually *good* landing (likely inflated by favorable chance), and pilots criticized had an unusually *bad* one (likely deflated by unfavorable chance); both groups would regress toward their typical performance regardless of any feedback given, mimicking a causal effect that was never actually there *(required: applies the mechanism to both directions, praise and criticism)* | attributes the pattern to feedback effectiveness without considering that both extremes would regress regardless → `regress-to-the-mean` |
| R3 | recall | short-answer | −1.1 | Fill in the blank: regression to the mean is driven by imperfect ___ between the two measurement occasions. | correlation (a correlation below 1 between the first and second measurement) | — |
| R4 | recall | mcq | −1.05 | If the correlation between two successive measurements of the same quantity were exactly 1, regression to the mean would: | not occur — an extreme first score would predict an equally extreme second score | picks "still occur exactly as before" — regression to the mean's strength depends directly on how far correlation falls short of 1 → `regress-to-the-mean` |
| R5 | recall | mcq | −1.0 | True or false: regression to the mean only pulls unusually *high* scores back down, never unusually *low* scores up. | False — it acts symmetrically in both directions | picks "True" — the phenomenon is symmetric: unusually low scores also tend to rise back toward the mean → `regress-to-the-mean` |
| R6 | recall | short-answer | −0.95 | What is the "regression fallacy"? | mistaking the mean-reverting pattern of regression to the mean for evidence of a real causal effect (an intervention, a treatment, a jinx) | — |
| R7 | recall | short-answer | −0.9 | Name a colloquial example often cited as regression to the mean in sports. | the "Sports Illustrated cover jinx" or the "sophomore slump" — a standout rookie season is often followed by a merely-average one | — |
| R8 | recall | mcq | −0.85 | Regression to the mean was first described by: | Francis Galton, studying the heights of parents and their children | picks "Ronald Fisher" — Fisher formalized much of modern statistics but the phenomenon is Galton's original observation → `regress-to-the-mean` |
| R9 | recall | short-answer | −0.8 | Does regression to the mean imply that the true underlying population mean is changing over time? | no — it describes how individual extreme observations move on repeated measurement; the population mean itself is not shifting | — |
| R10 | recall | mcq | −0.75 | Regression to the mean is best described as a consequence of: | random noise in individual measurements, not any change in underlying ability or a causal intervention | picks "measurement improving over time due to practice" — that would be a genuine causal/learning effect, not the statistical phenomenon → `regress-to-the-mean` |
| R11 | recall | short-answer | −0.7 | In what settings does regression to the mean typically apply? | any repeated-measurement setting where the two measurements are imperfectly correlated — test-retest scores, athletic performance across seasons, disease symptom severity on successive visits, etc. | — |
| R12 | recall | short-answer | −0.65 | True or false: regression to the mean is a form of measurement bias in the instrument used. | False — it is a statistical/probabilistic phenomenon arising from selecting on an extreme value, not a flaw in the measuring instrument | — |
| R13 | recall | mcq | −0.6 | Selecting a group *because* they had extreme scores, then re-measuring them, will typically show the group's average: | move closer to the overall mean on remeasurement | picks "stay exactly as extreme as before" — that would require the two measurements to be perfectly correlated, which is not the general case → `regress-to-the-mean` |
| R14 | recall | short-answer | −0.55 | Fill in the blank: patients selected for a clinical trial because they had unusually severe symptoms will often show improvement on remeasurement even with an ___ treatment. | ineffective (inert/placebo) treatment | — |
| R15 | recall | short-answer | −0.5 | Why is "regression toward the mean" a better name than "regression toward the population"? | the pull is specifically toward *that individual's or group's own expected/typical value*, not toward some other population's mean | — |
| R16 | recall | short-answer | −0.45 | What role does chance ("luck") play in the standard explanation of regression to the mean? | an extreme observed score is more likely to include a large lucky (or unlucky) chance component than a typical score does, and that chance component is unlikely to repeat on remeasurement | — |
| A2 | apply | short-answer | −0.4 | A student scores unusually low on a midterm and then improves on the final. A tutor claims their tutoring caused the improvement. What should you check before accepting that claim? | whether the improvement is larger than what regression to the mean alone would predict from an extreme low score — without a control group or a comparison to the expected reversion, tutoring's causal effect can't be separated from mere reversion | — |
| A3 | apply | short-answer | −0.3 | A hospital's worst-performing surgeons one year are enrolled in a retraining program; next year their outcomes improve. Why is this weak evidence the program worked? | surgeons selected for being unusually bad in one period likely had some bad luck mixed with genuinely low skill; even with no program at all, their next-period outcomes would be expected to regress toward their typical (better) level | — |
| A4 | apply | mcq | −0.2 | A company gives a bonus only to salespeople in the bottom 10% of performance, hoping to "shock" them into recovery. If next quarter's sales rise for that group, this most plausibly reflects: | regression to the mean, since the group was selected for an extreme low score | picks "clear proof the bonus caused recovery" — without a comparison to what reversion alone predicts, the improvement can't be attributed to the bonus → `regress-to-the-mean` |
| A5 | apply | short-answer | −0.1 | Two tests are given a week apart with correlation r = 0.6 between them. A student scores 3 standard deviations above the mean on test 1. Roughly how does regression to the mean describe their expected test-2 score? | expected to be pulled back toward the mean, at roughly r × 3 = 1.8 standard deviations above the mean rather than the full 3 | — |
| E2 | explain | short-answer | 0.45 | Why does regression to the mean get *stronger* (pulls harder toward the mean) as the correlation between the two measurements gets weaker? | weaker correlation means less of an extreme first score is attributable to a stable, persistent trait and more is attributable to transient noise; since noise doesn't repeat, a lower correlation means more of the original extremity is expected to vanish on remeasurement *(required: connects correlation strength to how much of the extremity was "real")* | — |
| E3 | explain | short-answer | 0.55 | Why is regression to the mean often mistaken for evidence of a causal effect specifically when an intervention follows selection on an extreme score? | interventions are frequently applied exactly when someone is at an extreme (worst performers get retraining, sickest patients get treated); the ensuing improvement would have partly happened anyway from reversion alone, and that "anyway" component is invisible without a comparison group *(required: names selection-on-extreme-then-intervene as the trap)* | — |
| E4 | explain | short-answer | 0.65 | Explain why a randomized control group is the standard fix for distinguishing a true treatment effect from regression to the mean. | a control group selected on the same extreme criterion, but left untreated, experiences the same expected reversion; comparing the treated group's improvement to the control group's improvement isolates any effect beyond what reversion alone would produce *(required: names the matched-selection control group)* | — |
| E5 | explain | short-answer | 0.75 | Why does regression to the mean not contradict the idea that skill or ability is real and stable? | it describes what happens to the *noise* component of an extreme *observed* score, not a claim that ability itself is unstable; a genuinely high-ability performer still regresses somewhat toward their own (still-high) typical level, not all the way to the overall population mean *(required: distinguishes "own typical level" from "population mean")* | — |
| T2 | transfer | short-answer | 0.95 | How does regression to the mean connect to the correlation coefficient's role in `simple-linear-regression`, later in this cluster? | the amount of reversion is quantitatively governed by the correlation r between the two measurements (E2), which is exactly the same r that appears in the slope formula β̂₁ = r·(s_Y/s_X) — regression to the mean is a special case of that same linear-prediction machinery applied to predicting a second measurement from a first *(required: names the shared role of r)* | — |
| T3 | transfer | short-answer | 1.05 | Why do policy evaluations that target the "worst" schools, hospitals, or precincts for intervention need to be especially careful about regression to the mean? | selecting units precisely because they scored worst in one period all but guarantees some reversion in the next period regardless of any policy; without a comparably-selected control group, evaluators risk crediting a policy for an improvement that regression to the mean alone would have produced *(required: applies the general mechanism to a policy-evaluation setting)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.15…1.05.*

---

## Linear Regression Terminology (`linear-regression-terminology`)
*Prereq: Regression · ancestors 1 · b₀ = −0.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.15 | Define response, predictor, coefficient, and residual. | response/dependent variable (Y, predicted); predictor/independent variable(s) (X, used to predict); coefficients (β, the weights); residual (observed minus predicted Y) | — |
| R2 | recall | mcq | −0.9 | "Independent variable" in regression terminology means: | simply the input/predictor variable(s), regardless of whether they're statistically independent of each other | reads it as claiming the predictors are "statistically independent (in the probability sense) from each other" — a genuinely common, understandable confusion → `linear-regression-terminology` |
| A1 | apply | short-answer | −0.35 | In "sales = β₀ + β₁·advertising + ε," identify each term's role. | response: sales; predictor: advertising; intercept: β₀; coefficient: β₁; error term: ε | — |
| E1 | explain | short-answer | 0.35 | Why can calling X "independent" be genuinely misleading given `independence-set-theory`'s precise probabilistic meaning? | predictors in a multiple regression are often correlated with each other — a phenomenon important enough to have its own name, multicollinearity (`vif`, later in this domain) — directly contradicting the probabilistic sense of "independent" that the same word carries elsewhere in this curriculum *(required: names multicollinearity as the direct contradiction)* | — |
| T1 | transfer | short-answer | 0.85 | Why does this terminology confusion matter across fields? | economics often says "exogenous/endogenous," ML often says "features/target," and statistics says "independent/dependent" — all naming the same roles with field-specific vocabulary, a genuine barrier to interdisciplinary communication if the correspondence isn't recognized *(required: at least two alternate vocabularies named)* | — |
| R3 | recall | short-answer | −1.1 | What is a "residual"? | the observed minus the predicted value of the response for a given data point: e = y − ŷ | — |
| R4 | recall | mcq | −1.05 | The fitted (predicted) value ŷ differs from the error term ε in that: | ŷ is computed from the estimated model; ε is the unobservable true error in the population model | picks "they are two names for the same quantity" — ε is a theoretical, unobserved quantity in the population model, while ŷ and the residual e are computed from the fitted model → `linear-regression-terminology` |
| R5 | recall | short-answer | −1.0 | What does the intercept β₀ represent in a regression model? | the predicted value of the response when all predictors equal zero | — |
| R6 | recall | mcq | −0.95 | A "coefficient" (slope) βⱼ in a multiple regression measures: | the expected change in the response per one-unit change in that predictor, holding the other predictors fixed | picks "the correlation between that predictor and the response" — a coefficient reflects a *holding-others-fixed* partial relationship, not a raw correlation → `linear-regression-terminology` |
| R7 | recall | short-answer | −0.9 | Fill in the blank: another common name for a "predictor" variable, especially in economics, is the ___ variable. | exogenous (or explanatory, or regressor) variable | — |
| R8 | recall | short-answer | −0.85 | What is a "covariate"? | a predictor variable, especially one included primarily to control for its effect rather than as the main variable of interest | — |
| R9 | recall | mcq | −0.8 | True or false: "regressor" is just another word for "predictor variable." | True | picks "False" — "regressor" is a standard synonym for "predictor variable," used especially in econometrics → `linear-regression-terminology` |
| R10 | recall | mcq | −0.75 | In machine-learning terminology, the regression "target" corresponds to the statistics term: | response (or dependent) variable | picks "predictor" — the ML "target" is what's being predicted, matching "response," not "predictor" → `linear-regression-terminology` |
| R11 | recall | short-answer | −0.7 | What is a "dummy variable" in regression terminology? | a binary (0/1) predictor used to encode a categorical variable's levels | — |
| R12 | recall | short-answer | −0.65 | What does the "design matrix" X contain, row by row? | one row per observation, with columns for each predictor (and typically a column of 1s for the intercept) | — |
| R13 | recall | mcq | −0.6 | An "interaction term" in a regression model represents: | the effect of one predictor depending on the level of another predictor | picks "the sum of two predictors' individual effects" — that is exactly what an interaction term is *not*; it's an effect beyond the two separate additive effects → `linear-regression-terminology` |
| R14 | recall | short-answer | −0.55 | Fill in the blank: in the equation Y = β₀ + β₁X₁ + β₂X₂ + ε, X₁ and X₂ are the ___ and β₀, β₁, β₂ are the ___. | predictors; coefficients (parameters) | — |
| R15 | recall | short-answer | −0.5 | What is a "standardized" regression coefficient? | a coefficient computed after rescaling all variables to have mean 0 and standard deviation 1, so coefficients across predictors become comparable in magnitude | — |
| R16 | recall | mcq | −0.45 | True or false: the terms "endogenous variable" (economics) and "response variable" (statistics) refer to the same role in a regression model. | True | picks "False" — both name the variable being explained/predicted by the model; they are field-specific synonyms → `linear-regression-terminology` |
| A2 | apply | short-answer | −0.4 | In "GPA = β₀ + β₁·study_hours + β₂·sleep_hours + ε," name the response, the predictors, and the error term. | response: GPA; predictors: study_hours, sleep_hours; error term: ε | — |
| A3 | apply | short-answer | −0.3 | A model includes a dummy variable "is_weekend" (1 if weekend, 0 otherwise). What does its coefficient represent? | the expected difference in the response between weekend and non-weekend observations, holding other predictors fixed | — |
| A4 | apply | mcq | −0.2 | A dataset has predictors "square footage" and "is_waterfront" (0/1) with an interaction term between them. What does a large, positive interaction coefficient suggest? | the effect of square footage on price is stronger for waterfront properties than non-waterfront ones | picks "square footage and waterfront status are statistically independent" — a large interaction coefficient is direct evidence *against* their effects being independent/additive → `linear-regression-terminology` |
| A5 | apply | short-answer | −0.1 | You compute a residual of −5 for a data point where the observed response was 95. What was the predicted value ŷ? | e = y − ŷ, so ŷ = y − e = 95 − (−5) = 100 | — |
| A6 | apply | numeric | 0.0 | A fitted model gives ŷ = 3 + 2x₁ − x₂. For x₁ = 4, x₂ = 5, what is ŷ? `[verified: 6]` | ŷ = 3 + 2(4) − 5 = 3 + 8 − 5 = 6 | — |
| E2 | explain | short-answer | 0.45 | Why does a coefficient's "holding other predictors fixed" interpretation break down once predictors are highly correlated? | if two predictors move together almost in lockstep, there is little real data variation showing one changing while the other stays fixed, so the coefficient's "all else equal" meaning becomes statistically fragile and poorly estimated even though it's still formally defined *(required: connects the interpretation to available data variation, not just definitional wording)* | — |
| E3 | explain | short-answer | 0.55 | Why is it useful to distinguish the "error term" ε from the "residual" e, even though both represent leftover variation? | ε is a property of the true, unknown population model and is never directly observed; the residual e is computed from the *fitted* model on a particular sample, and is only an estimate of what ε happened to be for that observation *(required: population-vs-sample distinction)* | — |
| E4 | explain | short-answer | 0.65 | Why do standardized coefficients help compare the relative importance of predictors measured in very different units? | a raw coefficient's size depends on the scale of its predictor (dollars vs. years vs. percent), so raw coefficients aren't directly comparable; standardizing puts every predictor on the same unit-free scale, so coefficient magnitude reflects relative influence rather than scale *(required: names the unit-comparability problem raw coefficients have)* | — |
| E5 | explain | short-answer | 0.75 | Why can adding an interaction term change the interpretation of the individual (main-effect) coefficients already in the model? | once an interaction term is present, a main-effect coefficient like β₁ for X₁ now represents X₁'s effect specifically *when the interacting variable equals zero*, not its overall average effect — the two coefficients' meanings become linked *(required: the "effect when the other variable is zero" reinterpretation)* | — |
| T2 | transfer | short-answer | 0.95 | How does the design matrix X, introduced here, connect directly to `normal-equations` later in this cluster? | the normal equations XᵀXβ̂ = Xᵀy are written entirely in terms of this same design matrix X — the row-per-observation, column-per-predictor object defined here is exactly the matrix that OLS's closed-form solution operates on *(required: names X as the identical object reused)* | — |
| T3 | transfer | short-answer | 1.05 | Why does inconsistent terminology across fields (features/target vs. independent/dependent vs. exogenous/endogenous) create real risk when reading interdisciplinary research or documentation? | a reader unfamiliar with a field's convention might misidentify which variable is being predicted versus which is doing the predicting, especially since "independent variable" is ambiguous with the probabilistic sense of independence (E1) — the risk compounds when multiple non-obvious synonyms are in play at once *(required: names the compounding-ambiguity risk, not just "vocabularies differ")* | — |

*Coverage: 16/6/5/3 — 30 items, −1.15…1.05.*

---

## Simple Linear Regression (`simple-linear-regression`)
*Prereq: Linear Regression Terminology, Covariance, Sample Variance · ancestors 19 · b₀ = 1.00*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.0 | State the simple linear regression model and its least-squares slope/intercept estimates. | Y=β₀+β₁X+ε; β̂₁=Cov(X,Y)/Var(X); β̂₀=Ȳ−β̂₁X̄ | — |
| R2 | recall | mcq | 0.3 | β̂₁ relates to the correlation coefficient r by: | β̂₁ = r·(s_Y/s_X) — the slope equals correlation scaled by the ratio of standard deviations | claims β̂₁ "always equals r exactly" — only true when s_X=s_Y → `simple-linear-regression` |
| A1 | apply | numeric | 0.8 | Cov(X,Y)=6, Var(X)=3, X̄=10, Ȳ=50. Find β̂₁ and β̂₀. `[verified: 2, 30]` | β̂₁=6/3=2; β̂₀=50−2(10)=30 | — |
| E1 | explain | derivation | 1.5 | Derive β̂₁=Cov(X,Y)/Var(X) from first principles by minimizing Σ(yᵢ−β₀−β₁xᵢ)². | take partial derivatives with respect to β₀ and β₁, set both to zero, and solve the resulting system — a direct application of `matrix-calculus`'s gradient-zero optimization technique, even in this simple two-parameter case *(required: the actual partial-derivative-and-solve steps, not just quoting the formula)* | — |
| T1 | transfer | short-answer | 2.0 | Why does measuring the same relationship in different units (inches vs. cm) change the numerical slope but not the correlation? `[verified: β̂₁=r·(sy/sx), an algebraic identity]` | β̂₁=r·(s_Y/s_X) depends on the *scales* of X and Y through their standard deviations, while r is unit-free and stays exactly the same regardless of units — slope and correlation describe the same relationship but answer different questions ("how much does Y change per unit X" vs. "how strong is the linear association") *(required: the explicit unit-dependence contrast)* | — |
| R3 | recall | short-answer | 0.05 | What criterion does the least-squares line minimize? | the sum of squared residuals, Σ(yᵢ − ŷᵢ)², over choices of β₀ and β₁ | — |
| R4 | recall | mcq | 0.1 | The least-squares regression line is guaranteed to pass through: | the point (X̄, Ȳ) — the mean of X and the mean of Y | picks "the origin (0, 0)" — only true if X̄=Ȳ=0; in general the line passes through the sample means, not the origin → `simple-linear-regression` |
| R5 | recall | short-answer | 0.15 | Fill in the blank: β̂₀ = Ȳ − β̂₁X̄ is the least-squares estimate of the ___. | intercept | — |
| R6 | recall | mcq | 0.2 | In simple linear regression, the sign of β̂₁ (positive or negative) always matches the sign of: | the correlation coefficient r between X and Y | picks "the sign of X̄" — the mean of X has no bearing on the slope's sign → `simple-linear-regression` |
| R7 | recall | short-answer | 0.25 | What is a "fitted value" ŷᵢ? | the value predicted by the regression line for observation i: ŷᵢ = β̂₀ + β̂₁xᵢ | — |
| R8 | recall | mcq | 0.28 | True or false: in simple linear regression, R² equals the square of the correlation coefficient r. | True | picks "False" — in the single-predictor case R² and r² are algebraically identical → `simple-linear-regression` |
| R9 | recall | mcq | 0.32 | "Least squares" minimizes squared *vertical* distances from points to the line because: | the model treats X as fixed/known and Y as the quantity with error, so only Y's deviations are penalized | picks "vertical and perpendicular distance are mathematically identical" — they are not; minimizing perpendicular distance is a different method (total least squares) → `simple-linear-regression` |
| R10 | recall | short-answer | 0.36 | What does it mean for regressing Y on X to give a different line than regressing X on Y? | the two regressions minimize error in different directions (vertical vs. horizontal residuals) and generally produce different slopes, both passing through (X̄, Ȳ) but not coinciding unless r = ±1 | — |
| R11 | recall | short-answer | 0.4 | Fill in the blank: Var(X) in the denominator of β̂₁ = Cov(X,Y)/Var(X) must be ___ for the formula to be defined. | nonzero (X must have some variation) | — |
| R12 | recall | mcq | 0.44 | If Cov(X,Y) = 0, the least-squares slope β̂₁ is: | exactly 0 — no linear relationship is fit | picks "undefined" — the formula is perfectly well-defined at Cov=0 as long as Var(X)≠0, it simply gives a slope of zero → `simple-linear-regression` |
| R13 | recall | short-answer | 0.48 | What is the regression line's equation, once β̂₀ and β̂₁ are estimated? | ŷ = β̂₀ + β̂₁x | — |
| R14 | recall | short-answer | 0.52 | Fill in the blank: the least-squares slope formula β̂₁ = Cov(X,Y)/Var(X) requires estimating ___ quantities from the data. | two — the sample covariance of X and Y, and the sample variance of X | — |
| R15 | recall | mcq | 0.56 | A larger |r| (correlation closer to ±1), holding s_X and s_Y fixed, means the fitted line's points lie: | closer to the line itself — the linear fit explains more of the variation in Y | picks "farther from the line" — a stronger correlation means a *tighter*, more linear scatter, not a looser one → `simple-linear-regression` |
| R16 | recall | short-answer | 0.6 | What two summary statistics of X and Y, besides their means, does β̂₁ = Cov(X,Y)/Var(X) require? | the sample covariance between X and Y, and the sample variance of X | — |
| A2 | apply | numeric | 0.65 | Cov(X,Y) = 12, Var(X) = 4, X̄ = 5, Ȳ = 20. Find β̂₁ and β̂₀. `[verified: 3, 5]` | β̂₁ = 12/4 = 3; β̂₀ = 20 − 3(5) = 5 | — |
| A3 | apply | numeric | 0.68 | r = 0.8, s_Y = 10, s_X = 5. Find β̂₁. `[verified: 1.6]` | β̂₁ = r·(s_Y/s_X) = 0.8·(10/5) = 1.6 | — |
| A4 | apply | numeric | 0.71 | A fitted line is ŷ = 2 + 3x. What is the fitted value at x = 4? `[verified: 14]` | ŷ = 2 + 3(4) = 14 | — |
| A5 | apply | short-answer | 0.74 | A dataset shows Var(X) = 0 (X is constant across all observations). Why can't simple linear regression be fit? | β̂₁ = Cov(X,Y)/Var(X) requires dividing by Var(X); with X constant there is no variation in the predictor to relate to variation in Y, so the slope is undefined | — |
| A6 | apply | short-answer | 0.77 | Two analysts fit "Y on X" and "X on Y" to the same data and get different slopes. Is this a mistake? | no — regressing Y on X and X on Y are different optimization problems (minimizing different residual directions) and are expected to give different lines unless the correlation is exactly ±1 | — |
| E2 | explain | short-answer | 1.55 | Why does the least-squares line necessarily pass through (X̄, Ȳ)? | the normal equations (from setting the intercept's partial derivative to zero) directly imply Ȳ = β̂₀ + β̂₁X̄, i.e. β̂₀ = Ȳ − β̂₁X̄ — the fitted line is constructed so that its residuals sum to zero, which forces it through the means *(required: connects to the residuals-sum-to-zero / intercept normal equation)* | — |
| E3 | explain | short-answer | 1.6 | Why is R² = r² specifically in *simple* linear regression, but not generally true in multiple regression? | with one predictor, the fraction of Y's variance explained by the single linear fit reduces algebraically to the squared correlation between X and Y; with multiple predictors, R² reflects the joint explanatory power of several correlated predictors at once, which is no longer any single pairwise r² *(required: names the one-vs-many-predictor distinction)* | — |
| E4 | explain | short-answer | 1.65 | Why does minimizing squared vertical residuals (rather than perpendicular distance) make sense when X is treated as a fixed, error-free predictor? | if X has no measurement error and only Y is subject to noise, then the "error" being fit is entirely in the Y direction, so penalizing vertical deviations directly targets the quantity assumed to be noisy, unlike perpendicular distance which would also penalize hypothetical error in X *(required: ties the vertical-only penalty to the X-is-fixed assumption)* | — |
| E5 | explain | short-answer | 1.7 | Explain why β̂₁ can be written equivalently as Cov(X,Y)/Var(X) or as r·(s_Y/s_X), and why both forms are useful. | since r = Cov(X,Y)/(s_X s_Y), substituting gives Cov(X,Y)/Var(X) = r·s_Y²/(s_X s_Y·s_X) = r·(s_Y/s_X) — the covariance form is natural from the least-squares derivation, while the r-form makes the unit-dependence (T1) and the strength-of-association role explicit *(required: shows the algebraic equivalence, not just asserts it)* | — |
| T2 | transfer | short-answer | 2.05 | How does β̂₁ = Cov(X,Y)/Var(X) generalize into the matrix formula β̂ = (XᵀX)⁻¹Xᵀy that appears later in `normal-equations`? | in the single-predictor case, XᵀX reduces essentially to a scalar proportional to Var(X) and Xᵀy to a scalar proportional to Cov(X,Y) (after centering); the matrix formula is the exact multi-predictor generalization of this same covariance-over-variance ratio *(required: names the centered-scalar-to-matrix generalization)* | — |
| T3 | transfer | short-answer | 2.1 | Why does the fact that regressing Y on X and X on Y give different lines matter for causal interpretation? | a fitted slope answers "how does the predicted Y change with X" specifically, and swapping the roles is not simply invertible — a large β̂₁ predicting Y from X does not imply a correspondingly simple relationship predicting X from Y, which cautions against reading a regression slope as automatically reversible or causal *(required: connects the asymmetry to a causal-interpretation caution)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.0…2.1.*

---

## Ordinary Least Squares (`ordinary-least-squares`)
*Prereq: Simple Linear Regression · ancestors 20 · b₀ = 1.02*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | Define OLS. | choosing coefficients to minimize the sum of squared residuals, Σ(yᵢ−ŷᵢ)² | — |
| R2 | recall | mcq | 0.32 | OLS minimizes *squared* error rather than absolute error because: | squared error is differentiable everywhere and yields a closed-form solution, which absolute error lacks | claims squared error is "always more accurate" — a vague, unjustified claim, not the actual mathematical reason → `ordinary-least-squares` |
| A1 | apply | short-answer | 0.82 | Why doesn't minimizing Σ\|yᵢ−ŷᵢ\| have as clean a closed-form solution as OLS? | \|·\| is not differentiable at 0, so setting a derivative to zero — the technique that produces OLS's clean formula — doesn't work the same way for absolute error *(required: names the non-differentiability at 0 specifically)* | — |
| E1 | explain | short-answer | 1.52 | State the Gauss-Markov theorem informally. | under linearity, no perfect collinearity, zero-mean errors, homoskedasticity, and no autocorrelation, OLS is BLUE — the Best Linear Unbiased Estimator, having the smallest variance among *all* linear unbiased estimators *(required: names "BLUE" and at least the key assumptions)* | — |
| T1 | transfer | short-answer | 2.02 | Why can OLS still perform poorly with nearly collinear predictors, even though Gauss-Markov guarantees it's BLUE? | BLUE only means best *among unbiased* linear estimators — accepting a little bias (as ridge regression does) can achieve much lower *total* error via the bias-variance tradeoff when collinearity has inflated OLS's variance dramatically *(required: the explicit "best among unbiased, not best overall" distinction)* | — |
| R3 | recall | short-answer | 0.07 | What does "BLUE" stand for in the Gauss-Markov theorem? | Best Linear Unbiased Estimator | — |
| R4 | recall | mcq | 0.1 | Under the Gauss-Markov assumptions, OLS achieves the smallest variance among: | all linear, unbiased estimators of the coefficients | picks "all possible estimators, linear or not" — Gauss-Markov's guarantee is restricted to the *linear and unbiased* class, not every conceivable estimator → `ordinary-least-squares` |
| R5 | recall | short-answer | 0.13 | Name the five classical Gauss-Markov assumptions. | linearity in parameters, no perfect collinearity among predictors, zero conditional mean of the errors, homoskedasticity (constant error variance), and no autocorrelation between errors | — |
| R6 | recall | mcq | 0.16 | "Homoskedasticity" refers to: | the error term having constant variance across all values of the predictors | picks "the errors having zero mean" — that is a separate Gauss-Markov assumption; homoskedasticity is specifically about constant variance → `ordinary-least-squares` |
| R7 | recall | short-answer | 0.19 | Fill in the blank: OLS minimizes the ___, denoted RSS. | residual sum of squares, Σ(yᵢ−ŷᵢ)² | — |
| R8 | recall | short-answer | 0.22 | Under what additional assumption on the errors does the OLS estimator coincide exactly with the maximum likelihood estimator? | that the errors are normally (Gaussian) distributed | — |
| R9 | recall | mcq | 0.25 | Compared to fitting Σ\|yᵢ−ŷᵢ\| (least absolute deviations), OLS is generally: | more sensitive to outliers, since squaring amplifies the influence of large residuals | picks "less sensitive to outliers" — squaring residuals actively *increases* the influence of large deviations relative to absolute-value loss → `ordinary-least-squares` |
| R10 | recall | short-answer | 0.28 | What does "no perfect collinearity" mean as a Gauss-Markov assumption? | no predictor is an exact linear combination of the others, which would make (XᵀX) singular and the OLS solution undefined | — |
| R11 | recall | short-answer | 0.31 | Is OLS a specific method, or one member of a broader family of estimators? | one member of a broader family — it is the specific estimator that minimizes squared error; other members of the family (e.g. least absolute deviations) minimize different loss functions | — |
| R12 | recall | mcq | 0.34 | If the Gauss-Markov assumptions hold except that the errors are heteroskedastic (non-constant variance), OLS remains: | unbiased, but no longer the minimum-variance linear unbiased estimator | picks "biased" — heteroskedasticity alone does not bias OLS's coefficient estimates; it only affects efficiency and the standard errors → `ordinary-least-squares` |
| R13 | recall | short-answer | 0.37 | What quantity does OLS treat as fixed (non-random) under the classical assumptions? | the predictor values X — only Y (through the error term) is treated as random | — |
| R14 | recall | short-answer | 0.4 | Fill in the blank: OLS applied with a single predictor reduces exactly to the ___ formulas from the previous concept. | simple linear regression | — |
| R15 | recall | mcq | 0.43 | "No autocorrelation" among the errors means: | the error for one observation is uncorrelated with the error for any other observation | picks "the predictors are uncorrelated with each other" — that describes collinearity, a separate assumption, not autocorrelation of the errors → `ordinary-least-squares` |
| R16 | recall | short-answer | 0.46 | What is the practical consequence of the OLS solution requiring (XᵀX) to be invertible? | if predictors are perfectly collinear (or there are more predictors than observations), (XᵀX) is singular and the OLS coefficients cannot be uniquely determined by the standard formula | — |
| A2 | apply | short-answer | 0.5 | A dataset has heteroskedastic errors (variance grows with X). Is the OLS coefficient estimate still unbiased? | yes — heteroskedasticity violates one Gauss-Markov assumption but does not by itself introduce bias into the coefficient estimates; it does mean OLS is no longer the minimum-variance linear unbiased estimator and the usual standard errors are unreliable | — |
| A3 | apply | short-answer | 0.55 | A predictor is an exact linear combination of two others in the dataset. What breaks in OLS as a result? | perfect collinearity makes (XᵀX) singular, so it cannot be inverted and the normal-equations formula for β̂ has no unique solution | — |
| A4 | apply | short-answer | 0.6 | Why would fitting a model with least absolute deviations instead of OLS be preferred on data with several extreme outliers? | least absolute deviations penalizes large residuals only linearly rather than quadratically, so a few extreme outliers pull the fitted line far less than they would under OLS's squared-error criterion | — |
| A5 | apply | mcq | 0.65 | A regression has errors that are normally distributed and satisfy all other Gauss-Markov assumptions. In this case OLS is: | both BLUE (Gauss-Markov) and the maximum likelihood estimator | picks "BLUE but never equivalent to any likelihood-based estimator" — under normal errors, OLS and MLE for the coefficients coincide exactly, in addition to OLS being BLUE → `ordinary-least-squares` |
| A6 | apply | numeric | 0.7 | A fitted OLS line gives residuals −2, 1, 1 for three data points. What is the residual sum of squares? `[verified: 6]` | RSS = (−2)² + 1² + 1² = 4 + 1 + 1 = 6 | — |
| E2 | explain | short-answer | 1.55 | Why does OLS remain unbiased under heteroskedasticity but stop being the *minimum-variance* linear unbiased estimator? | unbiasedness only requires the errors to have zero conditional mean, which heteroskedasticity doesn't violate; but Gauss-Markov's *minimum-variance* guarantee specifically relies on constant error variance in its proof — once variance differs across observations, a differently-weighted linear combination (as in weighted least squares) can achieve lower variance *(required: separates the two properties and what each depends on)* | — |
| E3 | explain | short-answer | 1.6 | Explain why OLS coincides with the maximum likelihood estimator precisely when errors are Gaussian. | the Gaussian log-likelihood's only term depending on β is −(1/2σ²)Σ(yᵢ−xᵢβ)², so maximizing that likelihood is algebraically identical to minimizing Σ(yᵢ−xᵢβ)² — exactly OLS's objective; a different error distribution would give a different likelihood and generally a different maximizer *(required: shows why the Gaussian log-likelihood reduces to the squared-error objective)* | — |
| E4 | explain | short-answer | 1.65 | Why is squared error, not absolute error, the "natural" loss function under Gaussian noise specifically? | the Gaussian density's exponent is a squared term, −(y−μ)²/2σ², so log-likelihood maximization under Gaussian noise directly produces a squared-error objective; a different noise distribution (e.g. Laplace) would instead make *absolute* error the maximum-likelihood-natural loss *(required: ties the squared-error form to the Gaussian density's functional form)* | — |
| E5 | explain | short-answer | 1.7 | Why does perfect collinearity make the OLS solution non-unique rather than simply "wrong"? | when one predictor is an exact linear combination of others, infinitely many coefficient vectors produce the identical fitted values and identical RSS — there is no single best answer to distinguish among them, so "non-unique" (not "incorrect") is the precise failure mode *(required: the infinitely-many-equally-good-solutions framing)* | — |
| T2 | transfer | short-answer | 2.05 | How does the Gauss-Markov theorem's restriction to "linear, unbiased" estimators foreshadow the bias-variance tradeoff exploited by regularized regression later in this domain? | Gauss-Markov proves OLS is optimal only within a restricted class (linear and unbiased); ridge and lasso regression step *outside* that class by deliberately introducing bias, which the theorem says nothing about, in exchange for lower variance — the theorem's careful qualifiers are exactly what later leaves room for those methods to beat OLS on total error *(required: names the theorem's qualifiers as the room regularization exploits)* | — |
| T3 | transfer | short-answer | 2.1 | Why does the OLS-equals-MLE-under-Gaussian-errors result connect statistics' least-squares tradition to the broader machine-learning framing of "loss functions"? | it shows squared-error loss isn't an arbitrary engineering choice — it is exactly the loss that maximum likelihood estimation produces under a Gaussian noise assumption, the same way cross-entropy loss in classification corresponds to a Bernoulli/categorical likelihood; "choose a loss function" and "choose a noise/likelihood model" are two views of the same decision *(required: draws the parallel to loss-function choice as an implicit likelihood choice)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.02…2.1.*

---

## Normal Equations (`normal-equations`)
*Prereq: Ordinary Least Squares, Matrix Multiplication · ancestors 24 · b₀ = 1.11*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.11 | State the normal equations in matrix form. | XᵀXβ̂ = Xᵀy, giving β̂=(XᵀX)⁻¹Xᵀy when XᵀX is invertible | — |
| R2 | recall | mcq | 0.41 | The normal equations are derived by: | setting the gradient of ‖y−Xβ‖² with respect to β equal to zero | describes them as "a guessed, reasonable formula" — they follow directly from calculus, not intuition → `normal-equations` |
| A1 | apply | short-answer | 0.91 | Why must XᵀX be invertible to solve for β̂ directly, and what's the fallback when it isn't? | invertibility is required by `invertible-matrices`' equivalence for the formula (XᵀX)⁻¹Xᵀy to make sense at all; when XᵀX is singular, the Moore-Penrose pseudo-inverse (from the linear-algebra sweep) still yields the minimum-norm solution *(required: names the pseudo-inverse fallback explicitly)* | — |
| E1 | explain | derivation | 1.61 | Derive the normal equations by differentiating ‖y−Xβ‖². | ∇_β[(y−Xβ)ᵀ(y−Xβ)] = −2Xᵀ(y−Xβ) = 0, giving XᵀXβ=Xᵀy *(required: the explicit gradient computation, reusing `matrix-calculus`'s machinery)* | — |
| T1 | transfer | short-answer | 2.11 | Connect Xᵀ(y−Xβ̂)=0 directly to `vector-projection`'s orthogonality principle. | this rearrangement of the normal equations says the residual vector (y−Xβ̂) is orthogonal to *every column* of X — exactly the defining property of an orthogonal projection from `vector-projection`, confirming OLS geometrically projects y onto C(X), not just by analogy but as the identical mathematical condition *(required: states it as the identical condition, not a resemblance)* | — |
| R3 | recall | short-answer | 0.16 | Why are they called the "normal" equations? | "normal" here means perpendicular/orthogonal — the equations state that the residual vector is normal (orthogonal) to the column space of X | — |
| R4 | recall | mcq | 0.2 | The closed-form solution β̂ = (XᵀX)⁻¹Xᵀy requires: | (XᵀX) to be invertible | picks "X itself to be a square matrix" — X is generally rectangular (n observations by p predictors); it's the square matrix XᵀX that must be invertible → `normal-equations` |
| R5 | recall | short-answer | 0.24 | What is the computational cost, in terms of the number of predictors p, of directly inverting (XᵀX)? | roughly O(p³) | — |
| R6 | recall | mcq | 0.28 | A common numerically-stable alternative to explicitly inverting (XᵀX) is: | QR decomposition of X | picks "always using single-precision floating point" — that would make numerical stability worse, not better; QR decomposition is the standard, more stable alternative → `normal-equations` |
| R7 | recall | short-answer | 0.32 | Fill in the blank: XᵀXβ̂ = Xᵀy is a system of ___ linear equations, one for each coefficient. | p (the number of coefficients/predictors, including the intercept) | — |
| R8 | recall | short-answer | 0.36 | What object plays the role of the "gradient set to zero" in deriving the normal equations? | ∇_β‖y−Xβ‖², the gradient of the sum of squared residuals with respect to β | — |
| R9 | recall | mcq | 0.4 | When (XᵀX) is singular (not invertible), the standard fallback for obtaining a solution is: | the Moore-Penrose pseudo-inverse, which yields the minimum-norm solution | picks "there is no solution, the model cannot be fit at all" — a solution set still exists (infinitely many), and the pseudo-inverse selects the minimum-norm one among them → `normal-equations` |
| R10 | recall | short-answer | 0.44 | What is C(X), as it appears in the geometric interpretation of the normal equations? | the column space of X — the set of all vectors Xβ reachable by some choice of β | — |
| R11 | recall | short-answer | 0.48 | Fill in the blank: geometrically, β̂ is the coefficient vector such that Xβ̂ is the orthogonal ___ of y onto C(X). | projection | — |
| R12 | recall | mcq | 0.52 | Solving the normal equations directly (via matrix inversion) versus using gradient descent to minimize ‖y−Xβ‖² are: | two different routes to the same optimal β̂, since the objective is convex with a unique minimum | picks "guaranteed to converge to different answers" — for this convex quadratic objective, both methods converge to the same unique minimizer → `normal-equations` |
| R13 | recall | short-answer | 0.56 | What does Xᵀ(y−Xβ̂) = 0 say about the residual vector? | it is orthogonal to every column of X | — |
| R14 | recall | short-answer | 0.6 | Fill in the blank: the normal equations are obtained by setting the ___ of the squared-error objective to zero. | gradient (with respect to β) | — |
| R15 | recall | mcq | 0.64 | If X has more predictors than observations (p > n), the matrix (XᵀX) is: | necessarily singular, so the standard closed-form solution doesn't directly apply | picks "always invertible regardless of n and p" — with more columns than rows in X, XᵀX cannot have full rank → `normal-equations` |
| R16 | recall | short-answer | 0.68 | What are the two matrix quantities that must be computed before solving the normal equations? | XᵀX and Xᵀy | — |
| A2 | apply | numeric | 0.72 | X = [[1,1],[1,2],[1,3]] (a column of 1s for the intercept and an X column), y = [2,3,5]. Compute XᵀX and Xᵀy. `[verified: XᵀX=[[3,6],[6,14]], Xᵀy=[10,23]]` | XᵀX = [[3,6],[6,14]]; Xᵀy = [10,23], from summing 1s, x-values, x², and cross-products directly | — |
| A3 | apply | numeric | 0.76 | Using A2's XᵀX = [[3,6],[6,14]] and Xᵀy = [10,23], solve for β̂. `[verified: β̂₀=1/3, β̂₁=1.5]` | solving the 2×2 system gives β̂₀ = 1/3 ≈ 0.333, β̂₁ = 1.5 | — |
| A4 | apply | short-answer | 0.8 | A design matrix X has two identical columns (the same predictor entered twice). What breaks when solving the normal equations directly? | the two identical columns make XᵀX singular (rank-deficient), so it cannot be inverted and β̂ = (XᵀX)⁻¹Xᵀy is undefined | — |
| A5 | apply | short-answer | 0.84 | Why would an engineer prefer QR decomposition over forming and inverting XᵀX directly on data with nearly collinear predictors? | forming XᵀX squares the condition number of X, amplifying any near-singularity and numerical error; QR decomposition works with X directly and avoids that squaring, giving a more numerically stable solution | — |
| A6 | apply | short-answer | 0.88 | A colleague claims solving the normal equations and minimizing RSS by gradient descent should, in principle, give different final coefficients. Are they right? | no — RSS is a convex quadratic function of β with a single global minimum; both a direct algebraic solve of the normal equations and a correctly-converged gradient descent reach that same unique minimizer | — |
| E2 | explain | short-answer | 1.65 | Why does forming XᵀX explicitly worsen numerical conditioning compared to working with X directly? | the condition number of XᵀX is the *square* of the condition number of X, so any near-collinearity already present in X is amplified when XᵀX is formed, making the matrix inversion step more sensitive to floating-point error *(required: names the squared-condition-number effect)* | — |
| E3 | explain | short-answer | 1.7 | Explain precisely why the "normal" equations name is not a coincidence with `vector-projection`'s use of "normal" to mean perpendicular. | both uses of "normal" mean the same geometric idea — orthogonal — and here it describes literally the same fact: the residual vector Xᵀ(y−Xβ̂)=0 states that the error is perpendicular to the column space, exactly the defining condition of an orthogonal projection *(required: ties the word "normal" itself to orthogonality, not just to the equations' name)* | — |
| E4 | explain | short-answer | 1.75 | Why is the closed-form solution β̂ = (XᵀX)⁻¹Xᵀy rarely used as-is in production numerical software? | explicit matrix inversion is both computationally wasteful (solving a linear system directly is cheaper) and numerically less stable than alternatives like QR or SVD-based decompositions, especially as (XᵀX)'s conditioning worsens *(required: names both the efficiency and stability reasons)* | — |
| E5 | explain | short-answer | 1.8 | Why does the pseudo-inverse solution specifically choose the *minimum-norm* β̂ among the infinitely many solutions when XᵀX is singular? | when predictors are exactly collinear, infinitely many β vectors give the identical fitted values Xβ̂ and thus identical RSS; among that entire solution set, the pseudo-inverse's construction (via the SVD) picks out the one with smallest ‖β̂‖, a well-defined, mathematically natural tie-breaking rule *(required: names that all solutions in the set achieve equal RSS, and the pseudo-inverse breaks the tie by norm)* | — |
| T2 | transfer | short-answer | 2.15 | How does the closed-form normal-equations solution connect forward to ridge regression's closed form later in the domain? | ridge regression's closed form, β̂_ridge = (XᵀX + λI)⁻¹Xᵀy, is the normal equations with a λI term added to the matrix being inverted — that added term is specifically what repairs a singular or ill-conditioned XᵀX, directly solving the invertibility problem raised here *(required: names the λI addition as directly fixing this section's invertibility issue)* | — |
| T3 | transfer | short-answer | 2.2 | Why is the normal equations' orthogonality condition described as the "payoff" connecting this cluster back to the linear-algebra sweep's `vector-projection`? | it shows that OLS — an apparently statistical procedure for minimizing squared prediction error — is, without any additional assumption, *identical* to the purely geometric operation of orthogonally projecting a vector onto a subspace; the statistical estimation problem and the linear-algebra projection problem turn out to be the same problem stated in two vocabularies *(required: frames it as the same problem in two vocabularies, not merely "related")* | — |

*Coverage: 16/6/5/3 — 30 items, 0.11…2.2.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| regression assumed unsupervised | `regression` |
| statistical reversion misattributed to a causal intervention | `regress-to-the-mean` |
| "independent variable" read as statistically independent predictors | `linear-regression-terminology` |
| regression slope conflated with correlation, ignoring unit-dependence | `simple-linear-regression` |
| Gauss-Markov's BLUE guarantee treated as "best overall," ignoring the unbiased qualifier | `ordinary-least-squares` |
| normal equations treated as an assumed formula rather than a calculus result | `normal-equations` |

**Cluster total: 180 items across 6 concepts (30 each).** All numeric claims verified, including the
β̂₁=r·(s_Y/s_X) identity used directly in `simple-linear-regression`'s T1, and the worked normal-equations
example (XᵀX=[[3,6],[6,14]], Xᵀy=[10,23], β̂=[1/3, 1.5]) checked by hand.
