# Machine Learning Cluster 7 — Clustering & Dimensionality Reduction

Clustering Methods, K-Means Clustering, SVD for Clustering, Probabilistic PCA, Kernel PCA, t-SNE,
UMAP, Independent Component Analysis, Principal Component Analysis (9 concepts). Same format as
[Cluster 1](ml-01-foundations.md).

`k-means-clustering`'s E1 reaches directly back into `law-of-total-variance`, and `ica`'s items are
built entirely around `covariance`'s Cov(X,X²)=0 counterexample from the probability/statistics
sweep — ICA exists precisely because PCA's uncorrelatedness doesn't rule out that kind of dependence.

---

## Clustering Methods (`clustering-methods`)
*Prereq: Supervised vs Unsupervised Learning · ancestors 3 · b₀ = 0.19*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.81 | Define clustering. | grouping data points so that points within a cluster are more similar to each other than to points in other clusters — an unsupervised task | — |
| R2 | recall | mcq | −0.55 | The main challenge in evaluating a clustering result, compared to a supervised classifier, is: | there's no ground-truth labels to compare against — "good" clustering is somewhat subjective/task-dependent | claims "clustering always achieves 100% accuracy" — accuracy isn't even a well-defined concept without labels → `clustering-methods` |
| A1 | apply | short-answer | 0.0 | Is clustering supervised or unsupervised? Why? | unsupervised — no labels are used; the algorithm discovers group structure entirely on its own | — |
| E1 | explain | short-answer | 0.69 | Why can different clustering algorithms give genuinely different groupings of the same data? | unlike supervised learning, where labels define a single target to converge toward, clustering has no objectively correct partition — different algorithmic assumptions (e.g. K-means assuming roughly spherical, similarly-sized clusters) lead to different, equally defensible results *(required: names that there's no single correct answer, unlike supervised learning)* | — |
| T1 | transfer | short-answer | 1.19 | Why is choosing the number of clusters K often one of the hardest practical decisions in clustering? | unlike supervised learning's cross-validation, which directly measures prediction accuracy, there's no similarly objective score to optimize over K — heuristics like the "elbow method" exist but require subjective judgment about where a curve bends *(required: the contrast with supervised CV's objectivity)* | — |
| R3 | recall | short-answer | −0.76 | What is a "distance metric," and why does clustering depend on one? | a function measuring dissimilarity between two data points (e.g. Euclidean distance); clustering groups points based on how close or far apart they are under this measure | — |
| R4 | recall | mcq | −0.71 | "Hierarchical" clustering, contrasted with "partitional" clustering (like K-means), produces: | a nested tree of clusters at multiple levels of granularity (a dendrogram), rather than one single flat partition | picks "exactly one flat partition, same as K-means" — hierarchical clustering's defining feature is precisely that it produces a nested multi-level structure, not a single partition → `clustering-methods` |
| R5 | recall | short-answer | −0.66 | Fill in the blank: "hard" clustering assigns each point to exactly one cluster, while "soft" (fuzzy) clustering assigns each point ___. | a degree of membership (a probability or weight) across multiple clusters | — |
| R6 | recall | mcq | −0.61 | Which of these is a common distance/similarity metric used in clustering? | cosine similarity | picks "R-squared" — R² is a regression goodness-of-fit measure, not a distance metric between data points → `clustering-methods` |
| R7 | recall | short-answer | −0.56 | Name a common evaluation metric used to judge clustering quality without ground-truth labels. | the silhouette score (or within-cluster inertia/sum of squared distances) | — |
| R8 | recall | mcq | −0.51 | Centroid-based clustering (like K-means) differs from density-based clustering (like DBSCAN) mainly in that: | centroid-based methods assume roughly compact, spherical clusters around a center point, while density-based methods find clusters as dense regions of arbitrary shape | picks "centroid-based methods require labeled data and density-based methods don't" — both families are equally unsupervised; the real distinction is the shape/structure assumption each makes → `clustering-methods` |
| R9 | recall | short-answer | −0.46 | Why does feature scaling typically matter for distance-based clustering algorithms? | distance metrics like Euclidean distance are dominated by features with larger numeric ranges, so unscaled features can make clustering results depend on arbitrary unit choices rather than genuine structure | — |
| R10 | recall | short-answer | −0.41 | Fill in the blank: clustering does not require a training set with ___, unlike classification. | labels (known ground-truth categories) | — |
| R11 | recall | mcq | −0.36 | The "curse of dimensionality" is relevant to clustering because: | in very high dimensions, distances between points tend to become less discriminative, making "nearest" and "farthest" points less meaningful | picks "clustering becomes computationally impossible in any dimension above 3" — clustering remains computationally feasible in high dimensions; the issue is that distances become less informative, not that computation halts → `clustering-methods` |
| R12 | recall | short-answer | −0.31 | Name a real-world application commonly solved with clustering. | e.g. customer segmentation, anomaly/outlier detection, or document/topic grouping | — |
| R13 | recall | mcq | −0.26 | True or false: clustering algorithms always produce the same result regardless of feature scaling. | False | picks "True" — since most clustering methods rely on a distance metric, rescaling features can change which points appear "close" and alter the resulting clusters → `clustering-methods` |
| R14 | recall | short-answer | −0.21 | What is a "dendrogram"? | a tree diagram used to visualize the nested cluster structure produced by hierarchical clustering, showing the order in which clusters were merged (or split) | — |
| R15 | recall | mcq | −0.16 | Compared to supervised classification, clustering results are: | inherently harder to validate objectively, since there is no ground truth to compare predictions against | picks "always more accurate, since there's no labeling error to worry about" — the absence of labels removes labeling *error*, but it also removes the ability to objectively measure accuracy at all → `clustering-methods` |
| R16 | recall | short-answer | −0.11 | Fill in the blank: a clustering algorithm groups points so that within-cluster similarity is ___ and between-cluster similarity is ___. | high; low | — |
| A2 | apply | short-answer | −0.06 | An analyst has purchase-history data for 10,000 customers with no labels of any kind. They want to identify natural customer segments. Is this a clustering task? | yes — grouping unlabeled data points into natural segments based on similarity is exactly the clustering task | — |
| A3 | apply | mcq | 0.05 | A dataset has one feature measured in dollars (0–1,000,000) and another in years (0–100), and Euclidean-distance K-means is applied without scaling. What is the likely consequence? | the dollar feature will dominate the distance calculations, effectively making clusters based almost entirely on that one feature | picks "no consequence — Euclidean distance automatically adjusts for each feature's scale" — Euclidean distance has no built-in scale adjustment; that's exactly why feature scaling is a separate, necessary preprocessing step → `clustering-methods` |
| A4 | apply | short-answer | 0.15 | Two clustering results on the same data give different numbers of clusters and different groupings, with no labels to check against. How would you decide which is "better"? | using internal validation metrics computed without labels (e.g. silhouette score, within-cluster inertia relative to between-cluster separation), and domain judgment about which grouping is more meaningful or actionable | — |
| A5 | apply | short-answer | 0.25 | A researcher wants to cluster documents by topic and considers using raw word counts versus TF-IDF vectors with cosine similarity. Why might cosine similarity on normalized vectors work better for text? | cosine similarity focuses on the *direction* (relative word proportions) rather than the raw *magnitude* of the vectors, which avoids longer documents appearing artificially "far" from shorter ones on the same topic simply due to word count | — |
| A6 | apply | short-answer | 0.35 | A dataset's clusters, when visualized, form two long, thin, curved arcs. Would you expect a distance-to-single-centroid clustering approach to group them correctly? | probably not — an approach assuming compact, roughly spherical clusters around a centroid is a poor match for elongated, curved cluster shapes; a density-based or graph-based method would likely do better | — |
| E2 | explain | short-answer | 0.74 | Why does the choice of distance metric itself encode an assumption about what "similar" means, before any algorithm runs? | Euclidean distance treats all feature directions equally and penalizes large raw differences, while cosine similarity cares only about direction/proportion, and other metrics (Manhattan, Mahalanobis) make yet other implicit assumptions — the metric chosen predetermines which points will be judged close, independent of the clustering algorithm applied afterward *(required: names that the metric itself encodes a similarity assumption prior to the algorithm)* | — |
| E3 | explain | short-answer | 0.79 | Why can two "reasonable" clustering algorithms on identical data produce meaningfully different numbers of natural-looking clusters? | different algorithms embed different structural assumptions (centroid-based methods favor convex, similarly-sized groups; density-based methods favor contiguous dense regions of any shape), so what counts as a single coherent cluster to one algorithm's assumptions may look like two or more to another's *(required: connects differing structural assumptions to differing cluster counts)* | — |
| E4 | explain | short-answer | 0.84 | Explain why internal validation metrics like the silhouette score can be misleading when used to select the "correct" number of clusters. | such metrics are typically built from the same distance/compactness ideas the clustering algorithm itself optimizes, so they can be biased toward whatever structural assumption the algorithm already makes, rather than reflecting some independent, ground-truth notion of a genuinely correct clustering *(required: names the circularity between the algorithm's own assumptions and the metric's assumptions)* | — |
| E5 | explain | short-answer | 0.89 | Why does the lack of ground-truth labels make the entire evaluation problem for clustering fundamentally different from classification, not just harder in the same way? | classification evaluation compares predictions to a single, externally-defined correct answer per example; clustering has no such external reference at all, so "quality" must be defined internally (compactness, separation) or via a task-specific downstream use, which are qualitatively different kinds of evidence, not merely noisier versions of accuracy *(required: frames it as a different kind of evaluation problem, not merely "harder to measure")* | — |
| T2 | transfer | short-answer | 1.24 | How does the distance-metric dependency identified here connect forward to `k-means-clustering`'s reliance on Euclidean distance specifically? | K-means is one concrete instantiation of this general dependency — it commits to Euclidean distance to a centroid as its specific similarity notion, inheriting every consequence discussed here (the need for scaling, the implicit spherical-cluster assumption) as concrete properties of that one algorithm *(required: frames K-means as one specific instance of the general metric-dependency point)* | — |
| T3 | transfer | short-answer | 1.29 | Why does clustering's reliance on a chosen distance metric make it philosophically closer to an exploratory tool than to a definitive answer-producing procedure? | since no single distance metric or algorithmic assumption is objectively "correct" for a given dataset, clustering results are best read as one lens among several for exploring possible structure, to be validated against domain knowledge or downstream task performance, rather than as a single authoritative partition of the data *(required: frames clustering as exploratory rather than definitive, and why)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.81…1.29.*

---

## K-Means Clustering (`k-means-clustering`)
*Prereq: Clustering Methods · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Describe the K-means algorithm's iterative steps. | initialize K centroids; assign each point to its nearest centroid; update each centroid to the mean of its assigned points; repeat until convergence | — |
| R2 | recall | mcq | −0.45 | K-means is guaranteed to converge to: | some local optimum, which can depend on the initial centroid placement | claims it always finds "the global optimum every time" — a genuinely important, common overstatement → `k-means-clustering` |
| A1 | apply | short-answer | 0.1 | Why does running K-means multiple times with different random initializations, keeping the best result, mitigate R2's local-optimum problem? | different initializations can converge to different local optima; running several and keeping the one with the lowest total within-cluster distance increases the chance of landing near the global optimum, without any guarantee of finding it exactly *(required)* | — |
| E1 | explain | short-answer | 0.8 | What objective is K-means minimizing, and how does that connect to `law-of-total-variance`'s decomposition? | it minimizes the total within-cluster sum of squared distances to each centroid; since total variance is fixed regardless of clustering, minimizing within-cluster variance is *exactly* equivalent to maximizing between-cluster variance — the identical decomposition from that earlier concept, now optimized directly *(required: the explicit "fixed total, so minimizing one term maximizes the other" argument)* | — |
| T1 | transfer | short-answer | 1.3 | Why does K-means perform poorly on clusters with very different shapes or sizes? | using Euclidean distance to a single centroid implicitly assumes clusters are roughly spherical and similarly sized; a tight small cluster next to a large elongated one violates this assumption, and methods like DBSCAN or Gaussian mixture models, which don't share this implicit assumption, often handle such cases better *(required: names the spherical/equal-size assumption specifically)* | — |
| R3 | recall | short-answer | −0.65 | Fill in the blank: the value K in K-means must be ___ before the algorithm runs. | chosen/specified in advance (it is a hyperparameter, not learned from the data) | — |
| R4 | recall | mcq | −0.6 | K-means uses which distance metric by default? | Euclidean distance | picks "edit (Levenshtein) distance" — edit distance measures string dissimilarity and has no role in standard K-means, which operates on numeric feature vectors → `k-means-clustering` |
| R5 | recall | short-answer | −0.55 | What is a "centroid" in K-means? | the mean (average position) of all points currently assigned to a cluster | — |
| R6 | recall | mcq | −0.5 | "K-means++" is a specific technique for: | smarter initialization of the starting centroids, spreading them out to reduce the chance of a poor local optimum | picks "automatically choosing the best value of K" — K-means++ addresses centroid initialization; it does not choose K, which must still be specified separately → `k-means-clustering` |
| R7 | recall | short-answer | −0.45 | What is the standard name for the classic iterative K-means algorithm (assign, then update)? | Lloyd's algorithm | — |
| R8 | recall | mcq | −0.4 | Each iteration of K-means costs roughly: | O(nKd), where n is the number of points, K the number of clusters, and d the number of dimensions | picks "O(n²), regardless of K or dimensionality" — the standard assignment step compares each of n points to each of K centroids across d dimensions, giving O(nKd), not a fixed O(n²) → `k-means-clustering` |
| R9 | recall | short-answer | −0.35 | Fill in the blank: K-means alternates between an assignment step and an ___ step. | update (recompute each centroid as the mean of its assigned points) | — |
| R10 | recall | mcq | −0.3 | True or false: K-means can end up with an empty cluster (no points assigned to some centroid) during iteration. | True | picks "False" — an empty cluster is a known, documented edge case in K-means, typically handled by reinitializing that centroid or splitting the largest cluster → `k-means-clustering` |
| R11 | recall | short-answer | −0.25 | What does "within-cluster sum of squares" (WCSS/inertia) measure? | the total squared distance from each point to its assigned cluster's centroid, summed over all points | — |
| R12 | recall | mcq | −0.2 | Mini-batch K-means differs from standard K-means primarily by: | updating centroids using small random subsets (mini-batches) of the data at each step, rather than the full dataset, for scalability | picks "using a completely different distance metric" — mini-batch K-means uses the same Euclidean-distance objective; the difference is purely computational (using subsets of data), not the metric → `k-means-clustering` |
| R13 | recall | short-answer | −0.15 | Fill in the blank: K-means is often described as a "hard"-assignment special case of the ___ algorithm applied to a Gaussian mixture model. | EM (Expectation-Maximization) | — |
| R14 | recall | short-answer | −0.1 | What stopping criterion is typically used to decide K-means has converged? | centroid assignments (or centroid positions) stop changing between iterations, or a maximum number of iterations is reached | — |
| R15 | recall | mcq | −0.05 | K-means' initial centroid positions are usually chosen: | randomly (or via K-means++), from the data or a plausible range of feature values | picks "by first running a supervised classifier to find good starting points" — K-means is unsupervised and has no access to labels to train a classifier for initialization → `k-means-clustering` |
| R16 | recall | short-answer | 0.0 | Fill in the blank: running K-means multiple times with different initializations and keeping the run with the lowest ___ is a standard mitigation for local optima. | within-cluster sum of squares (WCSS/inertia) | — |
| A2 | apply | numeric | 0.05 | Points {1, 2, 3, 9, 10}, initial centroids c₁=2, c₂=9. After one assignment step (nearest centroid), what are the two clusters? `[verified: {1,2,3}, {9,10}]` | distances to c₁=2 vs c₂=9 put 1,2,3 closer to c₁ and 9,10 closer to c₂ | — |
| A3 | apply | numeric | 0.1 | Using A2's clusters {1,2,3} and {9,10}, compute the updated centroids. `[verified: 2.0, 9.5]` | new c₁ = (1+2+3)/3 = 2.0; new c₂ = (9+10)/2 = 9.5 | — |
| A4 | apply | short-answer | 0.15 | You run K-means with K=3 on data that actually has 2 well-separated clusters. What is the likely outcome? | K-means will still produce 3 clusters (it always returns exactly K clusters), likely by splitting one of the two true clusters into two parts | — |
| A5 | apply | mcq | 0.2 | A dataset has one cluster of 1,000 tightly-packed points and another of 10 widely-scattered points. Running standard K-means with K=2 is likely to: | struggle to correctly separate them, since K-means implicitly favors similarly-sized, similarly-shaped clusters | picks "handle this perfectly, since K-means has no assumptions about cluster size" — K-means' centroid/distance objective implicitly favors comparably-sized, compact clusters, which this scenario violates → `k-means-clustering` |
| A6 | apply | short-answer | 0.25 | Why would running K-means 10 times with different random initializations and keeping the lowest-WCSS result be worth the extra computation for a business-critical segmentation task? | K-means is only guaranteed to reach *a* local optimum, which can vary substantially with initialization; multiple restarts increase the chance of finding a much better (lower-WCSS) partition, which matters when the resulting segments drive real business decisions | — |
| E2 | explain | short-answer | 0.85 | Why does K-means' assignment step (assign to nearest centroid) always decrease or maintain the WCSS objective, never increase it? | for any fixed set of centroids, assigning each point to its *nearest* centroid is exactly the assignment that minimizes that point's contribution to the total squared distance — so the assignment step can only move the objective toward its minimum given those centroids, never away from it *(required: explains why "nearest" assignment is locally objective-minimizing)* | — |
| E3 | explain | short-answer | 0.9 | Why does the update step (recomputing centroids as means) also never increase the WCSS objective? | for a fixed cluster assignment, the mean of each cluster's points is exactly the single point minimizing the sum of squared distances to all points in that cluster — so recentering at the mean can only decrease or maintain that cluster's contribution to WCSS *(required: names the mean-minimizes-sum-of-squares fact specifically)* | — |
| E4 | explain | short-answer | 0.95 | Given E2 and E3, why does K-means always converge, even though it's only guaranteed to find a local optimum? | since both the assignment step and the update step never increase WCSS, and there are only finitely many possible assignments of points to K clusters, WCSS forms a non-increasing sequence over a finite set of possible values, which must eventually stop decreasing — but nothing in this argument rules out stopping at a local rather than global minimum *(required: connects "non-increasing over finitely many states" to guaranteed convergence, and why that doesn't guarantee global optimality)* | — |
| E5 | explain | short-answer | 1.0 | Why is choosing a poor initial set of centroids specifically likely to trap K-means at a bad local optimum? | if two initial centroids happen to land inside the same true cluster, the algorithm has no mechanism within a single run to later "notice" that a different, more natural K-way split exists — it can only refine the partition implied by its current centroid positions, so a bad starting configuration can permanently mis-partition the data *(required: explains why the algorithm can't correct a fundamentally bad initial split within one run)* | — |
| T2 | transfer | short-answer | 1.35 | How does viewing K-means as EM's hard-assignment special case (R13) connect it to `probabilistic-pca` and Gaussian mixture models elsewhere in this cluster? | K-means is what EM on a Gaussian mixture model reduces to in the limit of equal, spherical, shared covariances and hard (winner-take-all) rather than soft cluster assignments — the same conceptual move (deterministic special case of a more general probabilistic model) that `probabilistic-pca` makes for ordinary PCA *(required: names the shared "deterministic limit of a probabilistic model" pattern)* | — |
| T3 | transfer | short-answer | 1.4 | Why does K-means' reliance on Euclidean distance to a centroid make it a natural first algorithm to run after dimensionality reduction, as in `svd-for-clustering`? | reducing to a lower-dimensional space first can mitigate the curse-of-dimensionality problem where Euclidean distances become less discriminative, directly supporting the distance-based assumption K-means' whole objective depends on *(required: connects K-means' Euclidean-distance dependency to the motivation for reducing dimensionality first)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.7…1.4.*

---

## SVD for Clustering (`svd-for-clustering`)
*Prereq: Clustering Methods, SVD · ancestors 22 · b₀ = 1.07*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.07 | Describe the basic idea of SVD-for-clustering. | apply SVD to reduce dimensionality first (keeping only the top few components), then run a clustering algorithm on the reduced representation | — |
| R2 | recall | mcq | 0.37 | A key reason to cluster in the reduced space rather than the original high-dimensional one is: | it can mitigate the curse of dimensionality's problem of distances becoming uninformative in high dimensions | claims "SVD always improves clustering accuracy with no downside" — an overclaim, not the actual justification → `svd-for-clustering` |
| A1 | apply | short-answer | 0.87 | Using Eckart-Young's guarantee, why does using the top-k SVD components (rather than a random k features) preserve the most structure for a given reduced dimensionality? | Eckart-Young proves the top-k SVD truncation is the *provably best* rank-k approximation of the data in a specific norm; a random feature subset carries no such optimality guarantee and typically preserves far less of the data's structure *(required: the explicit optimality claim)* | — |
| E1 | explain | short-answer | 1.57 | What is a genuine risk of this approach? | SVD/PCA-based reduction preserves *variance*, but directions of maximum variance aren't always the directions that best separate meaningful clusters — true cluster structure could, in principle, lie along a low-variance direction that gets discarded *(required)* | — |
| T1 | transfer | short-answer | 2.07 | Why can spectral clustering find structures (like two interleaving crescent shapes) that K-means on raw or SVD-reduced data cannot? | spectral clustering uses eigenvectors of a similarity/graph matrix rather than raw Euclidean distance, capturing cluster structure through *graph connectivity* rather than straight-line or centroid-distance geometry — this lets it separate non-convex shapes that no simple distance-based method can *(required: names graph connectivity as the distinguishing mechanism)* | — |
| R3 | recall | short-answer | 0.12 | What does "truncated SVD" mean, as used before clustering? | keeping only the top k singular values and their corresponding singular vectors, discarding the rest, to obtain a rank-k approximation of the data | — |
| R4 | recall | mcq | 0.17 | A well-known named application of SVD-for-clustering-style dimensionality reduction on text data is: | Latent Semantic Analysis (LSA), which reduces a term-document matrix before clustering or comparing documents | picks "the Fast Fourier Transform" — the FFT is an unrelated signal-processing technique, not an SVD-based text dimensionality-reduction method → `svd-for-clustering` |
| R5 | recall | short-answer | 0.22 | Fill in the blank: choosing how many top singular components k to keep is often guided by a ___ plot of explained variance. | scree | — |
| R6 | recall | mcq | 0.27 | Compared to running clustering directly on raw high-dimensional data, running it on SVD-reduced data is typically: | computationally cheaper, since the clustering algorithm operates on far fewer dimensions | picks "computationally more expensive, since SVD adds a costly preprocessing step with no offsetting benefit" — SVD adds an upfront cost, but the subsequent clustering runs on fewer dimensions, typically making the *overall* pipeline cheaper, and also mitigating the curse of dimensionality → `svd-for-clustering` |
| R7 | recall | short-answer | 0.32 | What is "spectral clustering," at a high level? | a clustering method that builds a similarity graph over the data and clusters using the eigenvectors of that graph's Laplacian matrix, rather than raw Euclidean distance | — |
| R8 | recall | short-answer | 0.37 | True or false: SVD-for-clustering requires centering the data first, the same way PCA does. | False — SVD can be applied directly to the raw data matrix; centering first specifically turns it into PCA, but plain truncated SVD does not require it | — |
| R9 | recall | mcq | 0.42 | Reducing dimensionality before clustering primarily helps address: | the curse of dimensionality's problem of distances becoming uninformative in high-dimensional space | picks "the problem of clusters having different numbers of points" — cluster-size imbalance is a separate issue that dimensionality reduction does not address → `svd-for-clustering` |
| R10 | recall | short-answer | 0.47 | Fill in the blank: SVD-for-clustering's risk is that the discarded low-variance directions might have held ___. | genuine cluster-separating structure | — |
| R11 | recall | mcq | 0.52 | Recommender systems' use of SVD (finding latent user/item factors) is related to SVD-for-clustering because both: | rely on the same underlying low-rank matrix factorization to find compact, informative representations | picks "both require labeled user preference data to work at all" — SVD-based factorization is unsupervised; it doesn't require labels, just the raw ratings/data matrix → `svd-for-clustering` |
| R12 | recall | short-answer | 0.57 | What guarantee does Eckart-Young give about the top-k truncated SVD specifically? | it is the provably best rank-k approximation of the original matrix, in the sense of minimizing reconstruction error (Frobenius norm) among all rank-k matrices | — |
| R13 | recall | short-answer | 0.62 | Fill in the blank: after truncating to k singular components, each data point is represented by a vector of only ___ numbers instead of its original full feature count. | k | — |
| R14 | recall | mcq | 0.67 | A key difference between plain truncated-SVD-then-cluster and spectral clustering is: | spectral clustering uses a similarity graph's Laplacian eigenvectors, which can capture non-convex cluster shapes that a straightforward SVD-then-Euclidean-clustering pipeline cannot | picks "spectral clustering does not use any linear algebra at all" — spectral clustering is fundamentally built on eigendecomposition, just of a different matrix (the graph Laplacian) than plain SVD → `svd-for-clustering` |
| R15 | recall | short-answer | 0.72 | Why is reducing dimensionality before clustering sometimes described as "denoising"? | discarding low-variance directions often removes noise-dominated dimensions, potentially making the remaining structure (used for clustering) cleaner, though this benefit isn't guaranteed if real structure lived in those discarded directions | — |
| R16 | recall | short-answer | 0.77 | Fill in the blank: singular values in SVD are typically sorted in ___ order, so "top-k" means the k largest. | descending | — |
| A2 | apply | short-answer | 0.82 | A 500-dimensional dataset's singular values drop off sharply after the first 5. Would keeping only the top 5 components before clustering likely lose much information? | not much — a sharp drop-off means most of the data's variance/structure is concentrated in those top 5 components, so truncating there retains the bulk of the meaningful signal | — |
| A3 | apply | short-answer | 0.92 | A team clusters raw 10,000-dimensional gene-expression data directly and gets poor, seemingly arbitrary clusters. What would you suggest they try first? | reduce dimensionality first (e.g. via truncated SVD to a much smaller k), since distances in 10,000 dimensions are likely uninformative due to the curse of dimensionality, before re-running the clustering algorithm | — |
| A4 | apply | mcq | 0.97 | A dataset's true cluster-separating signal happens to lie along a direction with very low overall variance. What is the likely consequence of SVD-then-cluster with a small k? | the low-variance direction is discarded in the truncation, so the resulting clusters likely miss the true structure entirely | picks "no consequence, since SVD always preserves cluster-relevant structure regardless of variance" — SVD explicitly keeps high-variance directions and discards low-variance ones, with no guarantee that cluster-relevant structure lives in the high-variance directions → `svd-for-clustering` |
| A5 | apply | short-answer | 1.02 | Why might an analyst prefer running spectral clustering directly over first reducing with SVD and then applying K-means, if the suspected clusters are non-convex? | spectral clustering's graph-based Laplacian eigenvectors are specifically designed to capture non-convex, connectivity-based cluster structure, which SVD-then-K-means (still ultimately relying on Euclidean-style geometry) is not built to find | — |
| A6 | apply | short-answer | 1.07 | A practitioner uses a scree plot and sees explained variance leveling off after component 8. What value of k would this scree plot suggest? | k = 8 — the "elbow" where additional components stop meaningfully adding explained variance is the natural cutoff | — |
| E2 | explain | short-answer | 1.62 | Why is the "provably best rank-k approximation" property of truncated SVD (Eckart-Young) not the same thing as "provably best for clustering"? | Eckart-Young's optimality is defined with respect to *reconstruction error* (minimizing how much information is lost overall, in a variance sense) — it says nothing about whether the retained directions are the ones that best *separate* clusters, which is a different, task-specific criterion *(required: distinguishes reconstruction-optimality from cluster-separation-optimality)* | — |
| E3 | explain | short-answer | 1.67 | Explain precisely why distances become "uninformative" in high dimensions, motivating dimensionality reduction before clustering. | as dimensionality grows, under common data-generating assumptions the ratio between the nearest and farthest point's distance to any given point tends toward 1 — every point ends up looking roughly equally (un)similar to every other point, which strips distance-based clustering of any meaningful signal to exploit *(required: the nearest/farthest-distance-ratio-collapses argument, not just "distances get big")* | — |
| E4 | explain | short-answer | 1.72 | Why does SVD-for-clustering's benefit depend on the data's *true* cluster structure being correlated with its high-variance directions? | SVD's truncation criterion is purely about preserving variance, with no reference to any notion of clusters; the technique only helps clustering downstream to the extent that the directions carrying the most variance happen to also be the directions separating the true groups — a coincidence the data must supply, not a guarantee SVD provides *(required: names that variance-preservation and cluster-separation are only accidentally aligned, not guaranteed to be)* | — |
| E5 | explain | short-answer | 1.77 | Why might a practitioner run clustering on both the raw high-dimensional data and the SVD-reduced data and compare results, rather than trusting the reduced version alone? | since E4's alignment between high-variance and cluster-separating directions isn't guaranteed, comparing both results can reveal whether reduction changed the clustering meaningfully — a large discrepancy is a warning sign that useful structure may have been discarded in the truncation *(required: frames the comparison as a check against E4's un-guaranteed assumption)* | — |
| T2 | transfer | short-answer | 2.12 | How does SVD-for-clustering's reliance on Eckart-Young connect it directly back to `svd-matrix-edition`'s original compression use case in the linear-algebra sweep? | both use the exact same fact — that truncating to the top-k singular values/vectors gives the provably best rank-k approximation — applied to two different goals: minimizing storage/reconstruction error for compression there, and reducing dimensionality before clustering here; it's the identical theorem serving two different downstream purposes *(required: names Eckart-Young as the literal shared theorem, not an analogy)* | — |
| T3 | transfer | short-answer | 2.17 | Why does the tension identified in E4 (variance ≠ cluster separation) recur almost identically in `pca`'s later coverage of PCA for supervised prediction? | both cases share the same structural flaw: an unsupervised variance-maximizing criterion (SVD/PCA) has no mechanism to know which directions matter for a specific downstream task (clustering here, prediction there), so discarding low-variance directions can just as easily discard exactly the information that task needed *(required: names the shared "unsupervised criterion blind to the downstream task" structure)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.07…2.17.*

---

## Probabilistic PCA (`probabilistic-pca`)
*Prereq: PCA, MLE · ancestors 36 · b₀ = 1.31*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.31 | Describe Probabilistic PCA (PPCA). | a generative model explaining observed data X as arising from a lower-dimensional latent Z via a linear map plus Gaussian noise: X=WZ+μ+ε | — |
| R2 | recall | mcq | 0.61 | Ordinary PCA is recovered from PPCA: | as the noise variance ε→0, in the maximum-likelihood limit | claims the two are "completely unrelated methods" — PPCA's MLE solution recovers classic PCA exactly in that limit → `probabilistic-pca` |
| A1 | apply | short-answer | 1.11 | What practical advantage does PPCA have over ordinary PCA, specifically because it's a full probabilistic model? | it can naturally handle missing data via the EM algorithm, and provides a principled way to choose the number of latent dimensions via likelihood-based model comparison — neither is available directly from ordinary (deterministic) PCA *(required: both capabilities named)* | — |
| E1 | explain | short-answer | 1.81 | How does framing PCA as PPCA connect it to `mle`? | the principal components can be found by maximizing the likelihood of the observed data under this Gaussian latent-variable model, rather than the purely geometric route (eigenvectors of the covariance matrix) — two different derivations converging on the same answer in the noise→0 limit *(required: names both derivation routes and their convergence)* | — |
| T1 | transfer | short-answer | 2.31 | Why does PPCA's explicit noise model make it a stepping stone toward more complex generative models? | its probabilistic structure extends naturally to models like Factor Analysis (different noise variance per feature) and eventually neural-network-based generative models like VAEs, since PPCA already frames dimensionality reduction as a generative, likelihood-based process rather than a purely geometric one *(required: names at least one concrete extension)* | — |
| R3 | recall | short-answer | 0.36 | In PPCA's model X = WZ + μ + ε, what is Z? | the low-dimensional latent variable, assumed to follow a standard Normal distribution, Z ~ N(0, I) | — |
| R4 | recall | mcq | 0.41 | In PPCA, the noise term ε is assumed to follow: | an isotropic Gaussian, ε ~ N(0, σ²I), with the same variance σ² in every observed dimension | picks "a different, feature-specific variance in each dimension" — that describes the more general Factor Analysis model; PPCA specifically assumes a single shared (isotropic) noise variance → `probabilistic-pca` |
| R5 | recall | short-answer | 0.46 | Fill in the blank: the matrix W in PPCA is often called the ___ matrix, mapping the latent space into the observed space. | factor loading | — |
| R6 | recall | mcq | 0.51 | Under PPCA, the marginal distribution of the observed data X, after integrating out Z, is: | Gaussian, with covariance WWᵀ + σ²I | picks "a mixture of Gaussians, one per data point" — integrating out a Gaussian latent variable under linear-Gaussian assumptions gives a single Gaussian marginal, not a mixture → `probabilistic-pca` |
| R7 | recall | short-answer | 0.56 | What algorithm is commonly used to fit PPCA's parameters, especially convenient when data has missing values? | the EM (Expectation-Maximization) algorithm | — |
| R8 | recall | short-answer | 0.61 | Why is PPCA's maximum-likelihood solution for W not unique, but only unique up to an arbitrary rotation? | rotating W by any orthogonal matrix R (W → WR) leaves the marginal covariance WWᵀ + σ²I unchanged, since (WR)(WR)ᵀ = WRRᵀWᵀ = WWᵀ, so many W's achieve the identical likelihood | — |
| R9 | recall | mcq | 0.66 | Compared to ordinary PCA, choosing the number of latent dimensions in PPCA can be guided by: | comparing the likelihood (or a criterion like BIC) across models with different numbers of latent dimensions | picks "there is no principled way to choose the number of dimensions in PPCA either" — PPCA's likelihood-based framework specifically enables comparing different latent dimensionalities via standard model-comparison tools, unlike ordinary PCA → `probabilistic-pca` |
| R10 | recall | short-answer | 0.71 | Fill in the blank: PPCA is called a "generative" model because it specifies a process for how the observed data X could have been ___. | generated (sampled), starting from a latent Z and passing it through a linear map plus noise | — |
| R11 | recall | short-answer | 0.76 | What happens to PPCA's noise variance σ² as it is estimated to recover exactly ordinary PCA? | σ² → 0, in the maximum-likelihood limit | — |
| R12 | recall | mcq | 0.81 | Handling missing data naturally is a practical advantage of PPCA over ordinary PCA because: | as a probabilistic model, PPCA can marginalize over the missing entries via its likelihood (typically within the EM algorithm), rather than requiring imputation beforehand | picks "PPCA simply ignores missing data with no special handling required" — PPCA doesn't ignore missing values; it explicitly incorporates them into the likelihood/EM computation, which is the actual mechanism for the advantage → `probabilistic-pca` |
| R13 | recall | short-answer | 0.86 | What is Factor Analysis's key structural difference from PPCA? | Factor Analysis allows a separate (non-isotropic) noise variance for each observed dimension, rather than PPCA's single shared isotropic noise variance | — |
| R14 | recall | short-answer | 0.91 | Fill in the blank: because PPCA is a full probabilistic model, points can be assigned a ___ under the model, not just a projected coordinate. | likelihood (a probability/density value) | — |
| R15 | recall | mcq | 0.96 | A "latent variable model" like PPCA is defined by: | assuming the observed data is generated from unobserved (latent) variables via some probabilistic process | picks "a model where all variables, observed and latent, are directly measured" — "latent" specifically means *not* directly observed; that's the defining feature of a latent variable model → `probabilistic-pca` |
| R16 | recall | short-answer | 1.01 | Why is PPCA sometimes preferred for generating new synthetic data resembling the training set, unlike ordinary PCA? | since PPCA defines a full generative probability model, one can sample a new Z ~ N(0,I), map it through W, and add noise to produce a new synthetic X — ordinary PCA has no such sampling procedure built in | — |
| A2 | apply | short-answer | 1.06 | A dataset has 15% of its entries missing at random. Why might a practitioner reach for PPCA (fit via EM) rather than ordinary PCA after mean-imputing the missing values? | mean-imputation can distort the covariance structure and understate variance; PPCA's EM fitting incorporates the missing-data uncertainty directly into the likelihood, generally giving a more principled estimate of the underlying structure | — |
| A3 | apply | short-answer | 1.16 | Two analysts fit PPCA to the same data and get different W matrices but identical marginal likelihoods. Is one of them wrong? | not necessarily — since W is only identified up to an arbitrary orthogonal rotation (R8), two different W matrices related by a rotation can produce exactly the same marginal covariance WWᵀ + σ²I and thus the same likelihood | — |
| A4 | apply | mcq | 1.21 | A team wants to compare a 2-latent-dimension PPCA model to a 5-latent-dimension PPCA model on the same data, using an objective criterion. Which approach is appropriate? | comparing their likelihoods (e.g. via a model-selection criterion like BIC, which also penalizes added complexity) | picks "this comparison is impossible for any PCA-family method" — it's specifically impossible for *ordinary* deterministic PCA (which has no likelihood), but PPCA's probabilistic framing makes likelihood-based comparison exactly the tool available → `probabilistic-pca` |
| A5 | apply | short-answer | 1.26 | As σ² is estimated to be very small in a fitted PPCA model, what does this suggest about how well the linear latent-variable structure fits the data? | it suggests the data lies very close to the linear subspace spanned by W, with little unexplained noise — the model is converging toward the deterministic PCA limit | — |
| A6 | apply | short-answer | 1.29 | Why would generating synthetic samples from a PPCA model fit to face images likely produce blurrier or less realistic images than a modern deep generative model? | PPCA's generative process is restricted to a *linear* map from latent to observed space plus isotropic Gaussian noise, which can only capture linear structure in the data — deep generative models use nonlinear mappings that can represent far more complex, realistic image structure | — |
| E2 | explain | short-answer | 1.86 | Why does the rotational non-identifiability of W (R8) not actually matter for most practical uses of PPCA? | quantities that matter in practice — the marginal covariance WWᵀ + σ²I, the likelihood, reconstructed/projected data, and the subspace W spans — are all invariant to which particular rotation R is chosen, so the non-uniqueness affects only the specific numerical values in W, not any quantity actually used downstream *(required: names that the practically-relevant quantities are rotation-invariant even though W itself isn't unique)* | — |
| E3 | explain | short-answer | 1.91 | Explain precisely why marginalizing out the Gaussian latent Z in the linear-Gaussian model X = WZ + μ + ε yields another Gaussian for X. | a linear transformation of a Gaussian random variable, plus another independent Gaussian, is itself Gaussian — this is exactly the closure-under-linear-combination property of the Normal family, so WZ (linear in the Gaussian Z) plus the independent Gaussian noise ε is again Gaussian, with covariance WWᵀ (from Z) plus σ²I (from ε) *(required: names the Gaussian closure-under-linear-transformation property as the mechanism)* | — |
| E4 | explain | short-answer | 1.96 | Why is EM particularly well-suited to fitting PPCA, compared to directly maximizing the marginal likelihood? | the latent variable Z is unobserved, so EM's alternation — computing the expected value of Z given current parameters (E-step), then updating parameters to maximize the expected complete-data log-likelihood (M-step) — sidesteps having to directly optimize the marginal likelihood, which can be a harder, more implicit optimization problem *(required: names the E-step/M-step alternation and why it avoids direct marginal-likelihood optimization)* | — |
| E5 | explain | short-answer | 2.01 | Why does the σ² → 0 limit specifically recover ordinary PCA's *eigenvector* solution, and not some other arbitrary linear projection? | as σ² shrinks to 0, PPCA's maximum-likelihood W converges to (a rotation of) the top-k eigenvectors of the sample covariance matrix scaled by their eigenvalues — because in that limit, the model is forced to explain all the data's variance purely through W, and the directions that best explain variance are exactly ordinary PCA's principal components by definition *(required: connects the zero-noise limit to variance being entirely explained by W, hence recovering the top eigenvectors)* | — |
| T2 | transfer | short-answer | 2.36 | How does PPCA's latent-variable generative structure directly foreshadow `variational-inference-vaes`, appearing later in the ML/graphical-models material? | a VAE is exactly this same idea — a latent Z generating an observed X through some mapping plus noise — with the linear map W replaced by a flexible neural network and the exact EM-based inference replaced by variational approximate inference, since the nonlinear mapping makes the exact marginal likelihood intractable *(required: names both replacements — nonlinear map, variational inference — as the direct generalization)* | — |
| T3 | transfer | short-answer | 2.41 | Why does PPCA's ability to assign a likelihood to data points, unlike ordinary PCA, make it directly useful for anomaly detection? | a point far from the model's learned subspace (or otherwise atypical under the fitted Gaussian) will receive a low likelihood under PPCA's explicit probabilistic model, giving a principled, quantitative anomaly score — ordinary PCA offers no equivalent probabilistic score, only a geometric reconstruction-error heuristic *(required: names the likelihood-as-anomaly-score capability specifically enabled by PPCA's probabilistic structure)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.31…2.41.*

---

## Kernel PCA (`kernel-pca`)
*Prereq: PCA, Kernel · ancestors 36 · b₀ = 1.31*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.31 | Describe kernel PCA. | apply the kernel trick to PCA, performing ordinary PCA implicitly in a (possibly infinite-dimensional) feature space defined by a kernel, without explicitly computing that mapping | — |
| R2 | recall | mcq | 0.61 | Kernel PCA is useful when: | the data's true structure is nonlinear (e.g. a curved manifold) — ordinary PCA would miss it entirely | claims it's for data whose "true structure lies along a linear subspace" — that's exactly when ordinary PCA already suffices → `kernel-pca` |
| A1 | apply | short-answer | 1.11 | Why does ordinary PCA fail on data lying exactly along a curved spiral in 2D, while kernel PCA can succeed? | ordinary PCA can only find linear (straight-line) directions of maximum variance; a curved spiral has no single linear direction capturing its structure, while an appropriate nonlinear kernel can effectively "unroll" the spiral into a space where it does become linear *(required)* | — |
| E1 | explain | short-answer | 1.81 | How does kernel PCA connect to `mercers-theorem`? | the kernel matrix used must satisfy Mercer's positive-semi-definiteness condition to correspond to any legitimate underlying feature-space PCA at all — the identical validity requirement reused from the SVM cluster, not a separate rule *(required: names it as the same requirement)* | — |
| T1 | transfer | short-answer | 2.31 | Why is kernel PCA less commonly used for visualization than newer techniques like t-SNE or UMAP? | kernel PCA's objective still preserves global variance/structure like ordinary PCA, which doesn't always produce the visually clear cluster separation that t-SNE/UMAP's local-neighborhood-preserving objectives are specifically designed to produce *(required: the objective-mismatch argument)* | — |
| R3 | recall | short-answer | 0.36 | What matrix does kernel PCA eigendecompose, in place of the covariance matrix ordinary PCA uses? | the kernel (Gram) matrix K, where each entry K_ij = k(xᵢ, xⱼ) | — |
| R4 | recall | mcq | 0.41 | The "kernel trick" refers to: | computing inner products in an implicit, possibly high- or infinite-dimensional feature space, without ever explicitly computing the mapped feature vectors | picks "reducing the number of features before applying PCA" — that describes ordinary feature selection or dimensionality reduction, not the kernel trick, which avoids explicit feature mapping altogether → `kernel-pca` |
| R5 | recall | short-answer | 0.46 | Fill in the blank: for a kernel to be usable in kernel PCA, it must satisfy ___, the same positive-semi-definiteness condition required elsewhere for kernels. | Mercer's condition (theorem) | — |
| R6 | recall | mcq | 0.51 | Name a commonly used kernel function in kernel PCA. | the RBF (Gaussian) kernel | picks "the identity kernel, k(x,x')=x" — that expression isn't even a valid inner-product-style kernel form; common valid choices are RBF, polynomial, or linear (dot product) kernels → `kernel-pca` |
| R7 | recall | short-answer | 0.56 | What is the "pre-image problem" in kernel PCA? | given a point's projection in the kernel-PCA feature space, there is generally no exact corresponding point back in the original input space — mapping back (reconstruction) is only approximate | — |
| R8 | recall | mcq | 0.61 | Kernel PCA's computational cost scales primarily with: | the number of data points n, since it works with an n×n kernel matrix regardless of the original (or implicit feature-space) dimensionality | picks "the dimensionality of the implicit feature space, which must be computed explicitly" — the entire point of the kernel trick is to *avoid* ever computing the implicit feature space explicitly → `kernel-pca` |
| R9 | recall | short-answer | 0.66 | Why must the kernel matrix be explicitly "centered" before eigendecomposition in kernel PCA? | because centering the data (subtracting the mean) in the implicit feature space can't be done directly, so a specific formula centers the kernel matrix itself to achieve an equivalent effect | — |
| R10 | recall | short-answer | 0.71 | Fill in the blank: unlike ordinary PCA, kernel PCA does not produce an explicit loading matrix W; its components are defined via eigenvectors of the ___. | kernel matrix | — |
| R11 | recall | mcq | 0.76 | The RBF kernel's bandwidth (length-scale) hyperparameter in kernel PCA controls: | how "local" versus "global" the notion of similarity between points is, strongly affecting the resulting embedding | picks "the number of principal components extracted" — the number of components extracted is chosen separately (like choosing k in ordinary PCA); the bandwidth instead shapes the kernel's own similarity notion → `kernel-pca` |
| R12 | recall | short-answer | 0.81 | True or false: kernel PCA can be applied even when the implicit feature space has infinite dimensions. | True — because the kernel trick never explicitly constructs that feature space, only its inner products, an infinite-dimensional implicit space (e.g. under the RBF kernel) poses no computational problem | — |
| R13 | recall | short-answer | 0.86 | What kind of structure in the data is kernel PCA specifically able to capture that ordinary PCA cannot? | nonlinear structure — curved manifolds or nonlinear relationships among features that no single linear projection can capture | — |
| R14 | recall | mcq | 0.91 | Choosing kernel PCA's kernel and its hyperparameters (e.g. an RBF bandwidth) is: | an important, often difficult modeling choice that significantly affects the resulting embedding | picks "unimportant, since any valid kernel gives essentially the same result" — different kernels (and hyperparameters) can produce dramatically different embeddings; this choice matters a great deal → `kernel-pca` |
| R15 | recall | short-answer | 0.96 | Fill in the blank: setting the kernel to a simple linear (dot-product) kernel in kernel PCA recovers exactly ___. | ordinary (linear) PCA | — |
| R16 | recall | short-answer | 1.01 | Why is the eigenvalue spectrum of the centered kernel matrix, not the raw data's covariance matrix, used to decide how many kernel-PCA components to keep? | kernel PCA operates entirely through the kernel matrix (since the implicit feature space is never explicit), so the analogous scree-plot/explained-variance analysis must be based on that kernel matrix's eigenvalues instead of a covariance matrix computed directly on raw features | — |
| A2 | apply | short-answer | 1.06 | A dataset lies on a curved 2D manifold embedded in 3D space (like a Swiss roll). Would you expect linear PCA or kernel PCA (with an appropriate kernel) to better recover its intrinsic structure? | kernel PCA — its nonlinear implicit feature mapping can "unroll" curved manifold structure that ordinary linear PCA, restricted to straight-line projections, cannot represent | — |
| A3 | apply | mcq | 1.11 | A practitioner applies kernel PCA with the linear kernel k(x,x')=xᵀx' to a dataset and compares the result to ordinary PCA on the same data. What should they find? | essentially the same embedding — the linear kernel makes kernel PCA mathematically reduce to ordinary PCA | picks "a completely different, unrelated embedding" — the linear kernel is specifically the case where kernel PCA's machinery collapses back to standard linear PCA → `kernel-pca` |
| A4 | apply | short-answer | 1.16 | You've computed a kernel-PCA embedding for a new business application, but a colleague asks to see the reconstructed original-space data points from that embedding. Why is this harder than in ordinary PCA? | ordinary PCA's projection is invertible using the same loading matrix W; kernel PCA has no explicit W and generally no exact way to invert the implicit nonlinear feature mapping back to input space — this is the pre-image problem, and only approximate reconstructions are typically possible | — |
| A5 | apply | short-answer | 1.21 | A team tries several RBF bandwidth values for kernel PCA and gets very different-looking 2D embeddings each time. Is this expected? | yes — the bandwidth directly controls the notion of similarity the kernel encodes, so different bandwidths can produce substantially different embeddings; this hyperparameter typically needs tuning for the task at hand | — |
| A6 | apply | short-answer | 1.26 | Why would kernel PCA's O(n²)–O(n³) kernel-matrix computation become a practical bottleneck on a dataset with a million points, even though the original feature dimensionality is small? | kernel PCA's cost is driven by the number of *data points* n (via the n×n kernel matrix), not by the original feature count, so even low-dimensional data with a huge number of points makes kernel PCA computationally expensive | — |
| E2 | explain | short-answer | 1.86 | Why does the kernel trick let kernel PCA operate in an infinite-dimensional feature space without ever running out of memory or time to compute it? | the kernel trick reformulates PCA entirely in terms of inner products k(xᵢ,xⱼ) between pairs of data points, and the kernel function computes that inner product directly, without ever materializing the (possibly infinite-dimensional) mapped vectors φ(x) themselves *(required: names that only inner products, never the mapped vectors, need to be computed)* | — |
| E3 | explain | short-answer | 1.91 | Explain precisely why centering the kernel matrix is mathematically necessary, not just a numerical nicety. | ordinary PCA's derivation assumes the data has zero mean; in the implicit feature space, the mapped points φ(xᵢ) generally do *not* have zero mean, and since φ is never explicitly computed, the mean can't be subtracted directly — a specific algebraic formula instead adjusts the kernel matrix K to be equivalent to what would result from centered φ(xᵢ), which is required for the PCA eigendecomposition to be mathematically valid *(required: connects centering to PCA's zero-mean assumption, and why it can't be done directly on φ)* | — |
| E4 | explain | short-answer | 1.96 | Why does the pre-image problem arise fundamentally from the kernel trick's own defining feature, rather than being a fixable implementation gap? | the kernel trick's entire benefit comes from never explicitly constructing the feature-space mapping φ; but reconstructing an original-space point from a feature-space projection requires inverting φ, which by construction was never made explicit or necessarily even invertible — the same property that makes kernel PCA powerful is exactly what makes exact reconstruction impossible *(required: frames the pre-image problem as a direct consequence of the kernel trick's core mechanism)* | — |
| E5 | explain | short-answer | 2.01 | Why can kernel PCA with a poorly chosen kernel perform worse than ordinary linear PCA, even on data with genuine nonlinear structure? | the kernel's implicit feature space and similarity notion must actually match the data's true structure for kernel PCA's flexibility to help; a mismatched kernel (wrong bandwidth, wrong functional form) can produce a less meaningful embedding than simply finding the best linear projection, since flexibility without a good match to the data's structure is not automatically an advantage *(required: names that flexibility alone doesn't guarantee improvement without a good kernel-data match)* | — |
| T2 | transfer | short-answer | 2.36 | How does kernel PCA's use of Mercer's condition connect it to the exact same validity requirement discussed for GPs in `gp-regression`? | both kernel PCA and a Gaussian Process require their kernel function to be positive-semi-definite (Mercer's condition) for the underlying mathematical object — an implicit feature space here, a valid covariance function there — to correspond to something legitimate; it is the identical algebraic requirement reused across both methods, not a coincidental similarity *(required: names Mercer's condition as the literal shared requirement, echoing this topic's own E4 in this cluster)* | — |
| T3 | transfer | short-answer | 2.41 | Why does kernel PCA's overall design — apply the kernel trick to an existing linear method — generalize as a template applicable well beyond PCA? | the same trick of replacing explicit dot products with a kernel function works for any linear method whose computations can be expressed purely in terms of inner products between data points — this is exactly how kernelized SVMs, kernel ridge regression, and other "kernelized" linear methods are constructed, with kernel PCA being one specific instance of that general template *(required: names the "any inner-product-based linear method can be kernelized" generalization)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.31…2.41.*

---

## t-SNE (`t-sne`)
*Prereq: Clustering Methods, KL Divergence · ancestors 15 · b₀ = 0.89*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.11 | Describe t-SNE's goal. | create a low-dimensional embedding where points close together in the original high-dimensional space remain close in the embedding — preserving local neighborhood structure, using KL-divergence to measure fidelity | — |
| R2 | recall | mcq | 0.19 | t-SNE is primarily designed to preserve: | local neighborhood structure — which points are each other's nearest neighbors | claims it preserves "global distances between all pairs, however far apart" — the opposite priority, and the source of a serious misreading of t-SNE plots → `t-sne` |
| A1 | apply | short-answer | 0.69 | Why is it a serious mistake to interpret the distance between two well-separated clusters in a t-SNE plot as meaningfully indicating how different they are? | t-SNE only optimizes local neighborhood preservation (R2); global arrangement and inter-cluster distances in the final plot are largely arbitrary artifacts of the optimization, not a reliable measure of true dissimilarity *(required: connects directly to R2's local-only focus)* | reads inter-cluster plot distance as a quantitative dissimilarity measure → `t-sne` |
| E1 | explain | short-answer | 1.39 | What role does KL-divergence play in t-SNE precisely? | it measures the difference between two probability distributions over "being neighbors" — one from similarities in the original high-dimensional space, one from the low-dimensional embedding; t-SNE optimizes the embedding to minimize this KL-divergence, directly reusing `kl-divergence` from the probability/statistics sweep as its loss function *(required: names both distributions and the minimization objective)* | — |
| T1 | transfer | short-answer | 1.89 | Why can running t-SNE multiple times on the same data produce visually different overall arrangements, even with similar local groupings? | t-SNE's optimization is non-convex, like K-means, and susceptible to different local optima depending on initialization and the perplexity hyperparameter; combined with its explicit focus on local rather than global structure, the overall layout can vary while local neighbor relationships stay largely consistent *(required: both the non-convexity and local-focus points)* | — |
| R3 | recall | short-answer | −0.06 | What is the "perplexity" hyperparameter in t-SNE, roughly speaking? | a parameter that roughly controls the effective number of nearest neighbors considered around each point when defining local similarities | — |
| R4 | recall | mcq | −0.01 | In the low-dimensional embedding, t-SNE models pairwise similarities using: | a Student-t distribution with heavy tails | picks "the same Gaussian distribution used in the high-dimensional space" — t-SNE deliberately uses a *different*, heavier-tailed distribution (Student-t) in the low-dimensional space, which is central to how it avoids the crowding problem → `t-sne` |
| R5 | recall | short-answer | 0.04 | Fill in the blank: t-SNE stands for t-Distributed ___ Neighbor Embedding. | Stochastic | — |
| R6 | recall | mcq | 0.09 | The "crowding problem" that motivates t-SNE's use of a heavy-tailed distribution in low dimensions refers to: | the fact that moderately distant points in high dimensions can't all be faithfully placed at moderate distances in a low-dimensional space, causing them to be crowded together | picks "the algorithm running too slowly on large datasets" — that's a separate computational-efficiency concern; the crowding problem is specifically about the geometry of fitting high-dimensional distances into fewer dimensions → `t-sne` |
| R7 | recall | short-answer | 0.14 | What optimization method does t-SNE use to find the low-dimensional embedding minimizing the KL divergence? | gradient descent | — |
| R8 | recall | mcq | 0.19 | t-SNE is best described as: | transductive — it produces an embedding only for the specific points it was run on, without a built-in way to embed new points | picks "inductive, with a straightforward formula to embed new, unseen points" — the opposite is true; naive t-SNE has no native mechanism to place new points into an existing embedding → `t-sne` |
| R9 | recall | short-answer | 0.24 | Fill in the blank: the naive computational cost of t-SNE scales roughly as ___ with the number of points n. | O(n²) | — |
| R10 | recall | mcq | 0.29 | The Barnes-Hut approximation is used with t-SNE primarily to: | speed up computation, reducing the cost from roughly O(n²) toward O(n log n) | picks "reduce the number of output dimensions automatically" — Barnes-Hut is a computational speedup technique borrowed from N-body physics simulations; it doesn't change or choose the output dimensionality → `t-sne` |
| R11 | recall | short-answer | 0.34 | What does pᵢⱼ represent in t-SNE's formulation? | the (symmetrized) probability that points i and j would be picked as neighbors, based on their similarity in the original high-dimensional space | — |
| R12 | recall | short-answer | 0.39 | Fill in the blank: t-SNE minimizes the KL divergence between the high-dimensional similarity distribution P and the low-dimensional similarity distribution ___. | Q | — |
| R13 | recall | mcq | 0.44 | True or false: cluster sizes (area covered) in a t-SNE plot reliably indicate the relative size or density of the true underlying clusters. | False | picks "True" — t-SNE's local-neighborhood focus does not reliably preserve relative density or apparent size; visually larger clusters in the plot don't necessarily correspond to genuinely larger or denser clusters → `t-sne` |
| R14 | recall | short-answer | 0.49 | Why does t-SNE typically require standardizing/preprocessing features before use, similar to other distance-based methods? | its similarity computations in the original space rely on distances between points, so features on very different scales would distort which points appear "close," biasing the resulting embedding | — |
| R15 | recall | short-answer | 0.54 | What is the typical output dimensionality of a t-SNE embedding used for visualization? | 2 (or sometimes 3) dimensions | — |
| R16 | recall | mcq | 0.59 | t-SNE's similarities in the original high-dimensional space are computed using: | a Gaussian centered on each point, converting distances into conditional probabilities | picks "raw Euclidean distances, used directly with no probabilistic conversion" — t-SNE's key step converts distances into a probability distribution (a Gaussian-based conditional probability) rather than using raw distances directly → `t-sne` |
| A2 | apply | short-answer | 0.64 | A dataset has 100,000 points, and a colleague wants to run standard (non-Barnes-Hut) t-SNE on it. Why might this be impractical? | naive t-SNE costs roughly O(n²), so at n=100,000 the pairwise similarity computations become extremely expensive in both time and memory, making the Barnes-Hut or similar approximation essentially necessary at this scale | — |
| A3 | apply | short-answer | 0.74 | A t-SNE run with perplexity=5 versus perplexity=50 on the same dataset gives visibly different embeddings. Why is this expected? | perplexity controls the effective neighborhood size used to define local similarities; a small perplexity emphasizes very local structure (small tight clusters), while a large perplexity considers a broader neighborhood, both leading to legitimately different (not simply "wrong") embeddings | — |
| A4 | apply | mcq | 0.84 | A new batch of test points arrives after a t-SNE embedding has already been computed on the training data. What is the standard way to place the new points into the existing embedding? | there is no standard exact method — t-SNE would typically need to be rerun on the combined data, or an approximate out-of-sample extension technique used | picks "simply apply the same fitted transformation formula used for the training points" — t-SNE has no fitted parametric transformation to reuse; this is precisely its transductive limitation → `t-sne` |
| A5 | apply | short-answer | 0.94 | A t-SNE plot shows two clusters that appear 10 units apart, and another pair that appear 2 units apart. Can you conclude the first pair is roughly 5 times more dissimilar than the second? | no — t-SNE's optimization only targets faithful local-neighborhood preservation; inter-cluster distances in the final layout are largely an artifact of the optimization and not a calibrated, quantitative measure of dissimilarity | — |
| A6 | apply | short-answer | 0.99 | Why might increasing perplexity help reveal more global structure in a t-SNE plot, while still not making inter-cluster distances quantitatively trustworthy? | a larger perplexity considers a wider neighborhood when defining similarities, capturing somewhat more large-scale structure than a very small perplexity would; but t-SNE's objective is still fundamentally about neighbor preservation, not calibrated global-distance preservation, so the caution about over-interpreting distances still applies | — |
| E2 | explain | short-answer | 1.44 | Explain precisely why using a heavy-tailed Student-t distribution (rather than a Gaussian) in the low-dimensional space specifically alleviates the crowding problem. | a heavy-tailed distribution assigns comparatively more probability mass to *moderate* distances than a Gaussian does, so points at a moderate distance in the low-dimensional embedding can still receive an appropriately non-negligible similarity — this gives the optimization "room" to push dissimilar points further apart without over-penalizing moderately-distant points, resolving the crowding that occurs if a Gaussian is used in the low-dimensional space too *(required: names the heavier-tailed distribution's larger probability mass at moderate distances as the mechanism)* | — |
| E3 | explain | short-answer | 1.49 | Why does minimizing KL(P‖Q) rather than KL(Q‖P) matter for what kinds of errors t-SNE's objective tolerates? | KL(P‖Q) heavily penalizes cases where P (true high-dimensional similarity) is large but Q (embedding similarity) is small — i.e. failing to keep truly nearby points close together — while tolerating the reverse (placing genuinely distant points close in the embedding) comparatively lightly; this asymmetry is exactly why t-SNE reliably preserves local neighbor structure but not global distances *(required: names the asymmetric penalty and connects it to local-preservation-over-global-fidelity)* | — |
| E4 | explain | short-answer | 1.54 | Why does the choice of perplexity implicitly encode an assumption about the "natural" scale of structure in the data? | perplexity roughly sets the number of neighbors each point's local similarity distribution is calibrated against, so a chosen perplexity effectively tells the algorithm what counts as a meaningful "neighborhood" — too small a perplexity can fragment genuine larger-scale clusters into many small pieces, and too large a perplexity can blur together genuinely distinct smaller clusters *(required: connects the perplexity choice directly to assumptions about the scale of meaningful structure)* | — |
| E5 | explain | short-answer | 1.59 | Why can t-SNE plots be genuinely useful for exploratory analysis despite all these caveats about global structure and reproducibility? | even though global arrangement and exact distances aren't trustworthy, t-SNE's explicit optimization target — preserving which points are each other's near neighbors — is exactly the property most exploratory questions ("do these categories separate at all?", "are there subgroups within this class?") actually depend on, making it a genuinely informative tool as long as it's read for local grouping rather than global geometry *(required: connects usefulness specifically to what the objective *does* guarantee, local neighbor preservation)* | — |
| T2 | transfer | short-answer | 1.94 | How does t-SNE's reuse of KL divergence as its optimization objective connect directly back to `kl-divergence` from the probability/statistics sweep? | t-SNE minimizes exactly the same KL divergence defined there, applied to two specific distributions it constructs — the high-dimensional neighbor-probability distribution P and the low-dimensional neighbor-probability distribution Q — making t-SNE's loss function a direct, literal application of that earlier concept rather than a new, unrelated notion of "difference" *(required: names it as the identical KL-divergence object reused, not merely a similar idea)* | — |
| T3 | transfer | short-answer | 1.99 | Why does t-SNE's local-preservation-only design foreshadow UMAP's attempt, covered next, to also better preserve some global structure? | t-SNE's deliberate trade-off — sacrificing global fidelity for strong local-neighborhood preservation — leaves an open gap that later methods try to close; UMAP's different mathematical foundation (topological/manifold-learning theory) is explicitly motivated by trying to retain more of that global structure while keeping t-SNE's local-preservation strength *(required: frames UMAP as a direct response to the specific local-vs-global trade-off established here)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.11…1.99.*

---

## UMAP (`umap`)
*Prereq: t-SNE · ancestors 16 · b₀ = 0.92*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.08 | Describe UMAP's relationship to t-SNE. | a similar goal — preserving local neighborhood structure for visualization — built on a different mathematical foundation (topological/manifold-learning theory), generally faster, and often better at preserving some global structure | — |
| R2 | recall | mcq | 0.22 | Compared to t-SNE, UMAP is generally: | faster, with better scalability to large datasets, at similar or better visual quality | claims it's "slower but more accurate" — backwards on the main practical advantage → `umap` |
| A1 | apply | short-answer | 0.72 | Should distances between well-separated clusters in a UMAP plot be trusted as precisely meaningful? | UMAP is generally considered somewhat better than t-SNE at preserving some global structure, but the same general caution applies — local neighbor relationships are the primary thing preserved, and inter-cluster distances should still be interpreted cautiously, not as precisely quantitative *(required: the caveat, not an unqualified "yes")* | — |
| E1 | explain | short-answer | 1.42 | Why are both t-SNE and UMAP considered visualization tools primarily, rather than general preprocessing steps before further modeling? | their nonlinear, local-structure-focused embeddings don't necessarily preserve the global, linear relationships that downstream models (like linear regression) might rely on — unlike ordinary PCA, which is commonly used as a preprocessing step precisely because it preserves linear structure *(required: the PCA contrast)* | — |
| T1 | transfer | short-answer | 1.92 | Why might a data scientist choose UMAP over t-SNE for a dataset with millions of points? | UMAP's speed and scalability advantage (R2) makes it tractable where t-SNE would be computationally prohibitive, while the same caution about over-interpreting global plot geometry still applies regardless of which tool is used *(required: connects to R2's speed point specifically)* | — |
| R3 | recall | short-answer | −0.03 | What mathematical foundation is UMAP built on, distinguishing it from t-SNE's more purely probabilistic formulation? | topological data analysis / manifold learning theory, using fuzzy simplicial sets to represent the data's structure | — |
| R4 | recall | mcq | 0.02 | Name a key hyperparameter of UMAP that controls the size of the local neighborhood considered around each point. | n_neighbors | picks "learning rate, exactly as in ordinary gradient descent for linear regression" — UMAP does use an optimization procedure with its own learning rate, but the hyperparameter specifically controlling *neighborhood size* is n_neighbors, distinct from the optimizer's step size → `umap` |
| R5 | recall | short-answer | 0.07 | Fill in the blank: UMAP's "min_dist" hyperparameter controls how tightly points are allowed to be packed ___ in the low-dimensional embedding. | together (it sets a minimum allowed distance between embedded points) | — |
| R6 | recall | mcq | 0.12 | UMAP's typical optimization approach uses: | stochastic gradient descent with negative sampling | picks "an exact closed-form eigendecomposition, with no iterative optimization" — UMAP's embedding is found through an iterative stochastic optimization procedure, not a single closed-form linear-algebra solve → `umap` |
| R7 | recall | short-answer | 0.17 | True or false: UMAP can only reduce data to 2 or 3 output dimensions, the same as t-SNE is typically used for. | False — UMAP can be used as a general-purpose dimensionality-reduction tool to any target number of dimensions, not only 2 or 3 for visualization | — |
| R8 | recall | mcq | 0.22 | Compared to t-SNE, one practical advantage of UMAP is: | it provides a more straightforward approximate way to transform new, previously unseen points into an existing embedding | picks "it requires no hyperparameters to be set at all" — UMAP still has important hyperparameters (n_neighbors, min_dist) that must be chosen, just like t-SNE's perplexity → `umap` |
| R9 | recall | short-answer | 0.27 | Fill in the blank: UMAP's loss function, unlike t-SNE's KL divergence, is typically described as a form of ___. | cross-entropy (between fuzzy topological representations of the high- and low-dimensional data) | — |
| R10 | recall | short-answer | 0.32 | What does a small min_dist value in UMAP tend to produce in the resulting embedding? | tightly-packed, dense clusters, since points are allowed to sit very close together | — |
| R11 | recall | mcq | 0.37 | Why is UMAP generally considered faster than t-SNE on large datasets? | it uses more computationally efficient approximate nearest-neighbor search and optimization techniques | picks "it simply skips computing similarities for most of the data" — UMAP still constructs a graph over neighborhood relationships for the data; its speed comes from efficient approximate algorithms, not from omitting most of the data → `umap` |
| R12 | recall | short-answer | 0.42 | What kind of underlying data structure does UMAP construct to represent the high-dimensional data's neighborhood relationships? | a weighted graph (fuzzy simplicial set) connecting each point to its nearby neighbors | — |
| R13 | recall | short-answer | 0.47 | Fill in the blank: increasing n_neighbors in UMAP tends to emphasize more ___ structure in the resulting embedding, at the expense of fine local detail. | global (larger-scale) | — |
| R14 | recall | mcq | 0.52 | True or false: UMAP, like t-SNE, involves a non-convex optimization and can give somewhat different results across different runs. | True | picks "False" — UMAP's optimization is also non-convex and stochastic, so it shares this sensitivity with t-SNE, even though it is often considered somewhat more stable → `umap` |
| R15 | recall | short-answer | 0.57 | Besides visualization, name one other common use case for UMAP as a general dimensionality-reduction technique. | preprocessing before clustering, or as a feature-reduction step before another downstream model | — |
| R16 | recall | short-answer | 0.62 | Why is UMAP's ability to embed new points considered only "approximate," not exact? | the transform of a new point relies on locating its approximate neighbors within the previously-fit structure and positioning it accordingly, which is a heuristic extension of the already-approximate, non-convex optimization, not a guaranteed exact placement | — |
| A2 | apply | short-answer | 0.67 | A team needs to embed 2 million data points into a low-dimensional space for downstream clustering, with a tight time budget. Why would UMAP typically be preferred over t-SNE here? | UMAP's more efficient optimization and neighbor-search algorithms scale better to very large datasets, making it more likely to complete within a tight time budget than t-SNE, which becomes very costly at that scale even with Barnes-Hut acceleration | — |
| A3 | apply | short-answer | 0.77 | A production system needs to embed new incoming data points into a UMAP space that was fit previously, without retraining from scratch each time. Is this workflow supported? | yes, approximately — UMAP provides a transform method to project new points into an existing embedding, unlike t-SNE which has no comparable native mechanism | — |
| A4 | apply | mcq | 0.87 | A UMAP embedding is run with a very large n_neighbors value. What is the most likely effect on the resulting plot? | it will emphasize broader, more global structure, potentially blurring some finer local groupings | picks "it will have no effect, since n_neighbors only affects computation speed" — n_neighbors directly changes what scale of structure the embedding preserves, not merely the algorithm's runtime → `umap` |
| A5 | apply | short-answer | 0.97 | Two UMAP runs on the same dataset with different random seeds produce visibly different cluster arrangements, though with similar local groupings. Is this a bug? | no — like t-SNE, UMAP's optimization is stochastic and non-convex, so different runs (or seeds) can converge to different, but often similarly-informative, layouts; this reflects an expected property of the method, not an implementation error | — |
| A6 | apply | short-answer | 1.02 | Should a data scientist trust that two clusters appearing far apart in a UMAP plot are more different from each other than two clusters appearing close together? | with more caution than raw distances suggest — UMAP is somewhat better than t-SNE at preserving some global structure, but its primary target is still local-neighborhood preservation, so global distances should be interpreted as suggestive rather than precisely quantitative | — |
| E2 | explain | short-answer | 1.47 | Explain why UMAP's topological/manifold-learning foundation is theoretically framed differently from t-SNE's purely probabilistic one, even though both produce broadly similar-looking outputs. | UMAP models the data as approximately lying on a manifold and represents neighborhood structure via a fuzzy simplicial set constructed from that manifold assumption, then optimizes an embedding to have a similar fuzzy topological representation; t-SNE instead directly defines and matches probability distributions over neighbor relationships — different mathematical machinery aimed at a similar practical goal of preserving local structure *(required: names the manifold/topological framing versus the probability-distribution framing as genuinely different foundations)* | — |
| E3 | explain | short-answer | 1.52 | Why does UMAP's efficient approximate nearest-neighbor search meaningfully change its computational scaling compared to t-SNE's naive pairwise approach? | approximate nearest-neighbor algorithms avoid computing similarities between every pair of points, instead efficiently finding each point's likely nearest neighbors in roughly log-linear time, which is what allows UMAP's overall pipeline to scale far better than a naive O(n²) all-pairs computation on very large datasets *(required: connects the approximate-neighbor-search technique directly to the improved scaling)* | — |
| E4 | explain | short-answer | 1.57 | Why does UMAP's ability to provide an (approximate) transform for new points make it more suitable than t-SNE for certain production ML pipelines? | many production pipelines need to repeatedly embed new incoming data consistently with a previously established representation (e.g. for downstream clustering or nearest-neighbor lookups); t-SNE's fully transductive nature would require expensive full re-runs on the combined old-and-new data each time, whereas UMAP's transform avoids that cost *(required: names the repeated-new-data production scenario and why transductive re-running is impractical there)* | — |
| E5 | explain | short-answer | 1.62 | Why is "UMAP generally preserves more global structure than t-SNE" a claim that still deserves the same caution t-SNE's plots require? | "more" global structure preservation is a comparative, not an absolute, claim — UMAP's primary optimization target remains local-neighborhood fidelity, so while its embeddings are often somewhat more globally faithful in practice, nothing in its objective *guarantees* calibrated global distances, unlike, say, a method that directly optimizes to preserve pairwise distances *(required: distinguishes the comparative "somewhat better" claim from an absolute guarantee)* | — |
| T2 | transfer | short-answer | 1.97 | How does the local-preservation trade-off shared by t-SNE and UMAP connect both methods back to `curse-of-dimensionality`, introduced much earlier in the machine-learning material? | both methods exist specifically because high-dimensional distances become uninformative for global structure (the curse of dimensionality); rather than fighting that directly, both deliberately restrict their objective to *local* neighborhood relationships, which remain meaningful even when global distance comparisons do not *(required: connects the local-only design choice directly to the curse of dimensionality's effect on global distances)* | — |
| T3 | transfer | short-answer | 2.02 | Why do both t-SNE and UMAP ultimately belong to the same broader family as `svd-for-clustering` and `kernel-pca`, despite their very different mathematical machinery? | all four are dimensionality-reduction techniques used to make downstream analysis (visualization, clustering) more tractable in a lower-dimensional space, each choosing a different definition of what structure to preserve — variance (SVD/kernel PCA) versus local neighbor relationships (t-SNE/UMAP) — which is the real axis distinguishing them, not whether they use "modern" versus "classical" techniques *(required: frames the real distinguishing axis as what structure is preserved, not the sophistication of the method)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.08…2.02.*

---

## Independent Component Analysis (`ica`)
*Prereq: PCA, KL Divergence · ancestors 35 · b₀ = 1.29*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.29 | Describe ICA's goal, and contrast it with PCA's. | recover statistically independent source signals from a mixture (e.g. the "cocktail party problem"); PCA instead finds merely *uncorrelated* directions, a strictly weaker condition | — |
| R2 | recall | mcq | 0.59 | The key difference between ICA and PCA is: | ICA seeks statistically independent components — a stronger condition than PCA's mere uncorrelatedness | claims "ICA and PCA are the same algorithm" — misses the entire point of the stronger independence requirement → `ica` |
| A1 | apply | short-answer | 1.09 | Using `covariance`'s counterexample (X~Uniform(−1,1), Y=X², Cov=0 but Y is fully determined by X), explain why PCA's uncorrelated components could still be dependent, and why ICA tries to rule this out. | Cov=0 does not imply independence, per that exact counterexample — PCA's components being uncorrelated says nothing about hidden nonlinear dependence between them; ICA explicitly targets the stronger condition of statistical independence, which would rule out even that kind of hidden dependence *(required: the direct reuse of the Cov(X,X²)=0 example, not a fresh one)* | — |
| E1 | explain | short-answer | 1.79 | Why does ICA typically require at most one true source to be Gaussian? | for Gaussian variables specifically, uncorrelated and independent coincide — a special property unique to the Gaussian/multivariate-normal family; ICA's use of higher-order statistical structure beyond correlation provides no extra information when sources are genuinely Gaussian, so it can't outperform PCA in that special case *(required: the Gaussian uncorrelated=independent special case)* | — |
| T1 | transfer | short-answer | 2.29 | Explain the cocktail party problem, and why ICA (not PCA) is the right tool for it. | several microphones each record a different linear mixture of the same underlying independent speakers; ICA, by seeking statistically independent components, matches the true generative structure (independent original voices) and can separate them, while PCA's merely-uncorrelated components generally fail to correctly unmix the sources *(required: names the linear-mixture setup and why independence, not mere uncorrelatedness, is the right target)* | — |
| R3 | recall | short-answer | 0.34 | In ICA's generative model X = AS, what do A and S represent? | A is the (unknown) mixing matrix, and S is the vector of independent source signals being mixed | — |
| R4 | recall | mcq | 0.39 | ICA's goal is to estimate an "unmixing" matrix W such that: | WX ≈ S, recovering (an estimate of) the original independent sources | picks "WX ≈ X, reconstructing the observed mixtures exactly" — that would just be an identity-like transformation; ICA's actual goal is recovering the underlying sources S, not reproducing the observed mixture X | — |
| R5 | recall | short-answer | 0.44 | Fill in the blank: ICA requires that at most one of the true underlying sources be ___. | Gaussian | — |
| R6 | recall | mcq | 0.49 | Name a commonly used practical algorithm for performing ICA. | FastICA | picks "Lloyd's algorithm" — Lloyd's algorithm is the classic K-means procedure, unrelated to independent component analysis → `ica` |
| R7 | recall | short-answer | 0.54 | What preprocessing step, closely related to PCA, is typically applied to data before running ICA? | whitening — centering the data and decorrelating/rescaling it (often via PCA) so it has zero mean and identity covariance | — |
| R8 | recall | mcq | 0.59 | ICA typically maximizes a measure of: | non-Gaussianity of the estimated components (e.g. via kurtosis or negentropy) | picks "the variance explained by each component, exactly as PCA does" — variance-maximization is specifically PCA's criterion; ICA instead targets non-Gaussianity as a proxy for statistical independence → `ica` |
| R9 | recall | short-answer | 0.64 | Why does ICA rely on higher-order statistics (like kurtosis), rather than just covariance, unlike PCA? | covariance (second-order statistics) only captures linear/uncorrelated structure, which is not enough to detect the stronger condition of statistical independence; higher-order statistics are needed to distinguish independence from mere uncorrelatedness | — |
| R10 | recall | mcq | 0.69 | The "permutation and scaling ambiguity" in ICA refers to the fact that: | the recovered independent components can come out in any order and at any scale/sign, without a unique canonical ordering or magnitude | picks "ICA sometimes fails to converge to any solution at all" — the ambiguity is about the *recovered solution's* order and scale being non-unique, not about convergence failing → `ica` |
| R11 | recall | short-answer | 0.74 | Fill in the blank: the intuitive justification for maximizing non-Gaussianity in ICA comes from the ___ theorem — a mixture of independent signals tends to look more Gaussian than any individual source. | Central Limit | — |
| R12 | recall | short-answer | 0.79 | What is "blind source separation," the general problem ICA is a solution to? | recovering a set of underlying independent source signals from only their observed mixtures, without knowing the mixing process in advance | — |
| R13 | recall | mcq | 0.84 | For standard (square-mixing) ICA to be well-posed, the number of observed mixtures (sensors) should typically be: | equal to the number of underlying independent sources | picks "always strictly greater than the number of sources, with no upper limit needed" — the classic square-ICA setup specifically assumes an equal number of sensors and sources, for the mixing matrix A to be invertible | — |
| R14 | recall | short-answer | 0.89 | Name a real-world application of ICA outside of audio separation. | e.g. removing eye-blink or muscle artifacts from EEG brain-signal recordings, or separating independent sources in financial time series | — |
| R15 | recall | true-false-mcq | 0.94 | True or false: if all the true sources in an ICA problem were exactly Gaussian, ICA could still uniquely recover them. | False | picks "True" — with all-Gaussian sources, uncorrelated and independent coincide, so there's no distinguishing statistical signal (beyond covariance) left for ICA's non-Gaussianity criterion to exploit, making the sources unidentifiable → `ica` |
| R16 | recall | short-answer | 0.99 | What does "negentropy" measure, as used in some ICA algorithms? | a measure of how far a distribution is from being Gaussian (based on differential entropy), used as a proxy objective for non-Gaussianity | — |
| A2 | apply | short-answer | 1.04 | Two microphones record a mixture of two independent speakers' voices. Why can't simply looking at the raw recordings separate the speakers? | each microphone's raw signal is a linear combination of *both* speakers' voices simultaneously; ICA is needed to algorithmically estimate the unmixing matrix that reverses that combination and isolates each speaker | — |
| A3 | apply | mcq | 1.09 | A dataset's two "independent" components estimated by ICA turn out, on inspection, to be swapped in order and flipped in sign relative to the true sources. Is this a bug in the ICA algorithm? | no — this is the expected permutation and scaling ambiguity inherent to ICA; the algorithm has no way to determine the canonical order or sign of the recovered sources | picks "yes — a correctly working ICA algorithm always recovers sources in their original order and scale" — that guarantee doesn't exist for ICA; permutation/scaling ambiguity is a fundamental, expected limitation, not an implementation flaw → `ica` |
| A4 | apply | short-answer | 1.14 | A researcher applies ICA to data where two of the three true underlying sources are Gaussian. What problem would you expect? | with more than one Gaussian source, ICA's non-Gaussianity-based identifiability breaks down for those Gaussian sources — they cannot be reliably distinguished from each other via ICA's criterion, even though the third (non-Gaussian) source may still be separable | — |
| A5 | apply | short-answer | 1.19 | Why is whitening the data before running ICA a helpful preprocessing step rather than an optional convenience? | whitening removes the second-order (covariance) structure first, so the subsequent ICA optimization can focus specifically on the higher-order statistics that reveal independence, rather than having to simultaneously handle both correlation and non-Gaussianity | — |
| A6 | apply | short-answer | 1.24 | A dataset's PCA components turn out uncorrelated but still visibly show a curved (nonlinear) relationship in a scatterplot. What would you expect ICA to do differently from PCA here? | ICA specifically targets statistical independence, a stronger condition than uncorrelatedness, so it would attempt to find a transformation ruling out even this kind of nonlinear dependence — while PCA, targeting only variance/uncorrelatedness, would leave such structure unaddressed | — |
| E2 | explain | short-answer | 1.84 | Explain precisely why maximizing non-Gaussianity is a reasonable proxy for finding statistically independent components, rather than an arbitrary heuristic. | by the Central Limit Theorem's intuition, any linear mixture of independent, non-Gaussian sources tends to look more Gaussian than the individual sources themselves; so if an estimated unmixing direction produces a component that is as *non*-Gaussian as possible, that direction is likely to correspond to "unmixing back" to something close to a single original source rather than a blend of several *(required: connects the CLT-style "mixtures are more Gaussian" intuition to why maximizing non-Gaussianity finds unmixed sources)* | — |
| E3 | explain | short-answer | 1.89 | Why does ICA's permutation and scaling ambiguity not actually limit its usefulness for most practical applications like audio source separation? | in applications like the cocktail-party problem, what matters practically is recovering each source's *waveform* (up to a constant scale and possibly an arbitrary ordering across output channels), not identifying which specific numbered channel corresponds to which speaker — a human listener (or downstream system) can trivially reorder or rescale the recovered signals after the fact *(required: explains why order/scale don't matter for the actual downstream use case)* | — |
| E4 | explain | short-answer | 1.94 | Why does the requirement "at most one source can be Gaussian" follow directly from ICA's reliance on higher-order statistics, rather than being an arbitrary restriction? | a Gaussian distribution is fully characterized by its mean and covariance alone — all its higher-order statistics beyond covariance carry no extra information; if two or more sources were Gaussian, no amount of higher-order-statistic-based analysis could distinguish how they're mixed together, since Gaussian mixtures of Gaussians are themselves indistinguishable via any rotation *(required: connects the Gaussian's "fully described by first two moments" property to the identifiability failure)* | — |
| E5 | explain | short-answer | 1.99 | Explain why ICA's requirement of (roughly) as many observed mixtures as sources is a meaningful practical constraint, not just a technical footnote. | the standard square-mixing-matrix formulation needs to invert A to estimate the unmixing matrix W, which requires A to be square and invertible; with fewer sensors than sources, the mixing is fundamentally under-determined (multiple different sets of sources could produce the same fewer observed mixtures), and standard ICA has no unique solution *(required: connects the sensor-source count constraint to invertibility/identifiability of the mixing process)* | — |
| T2 | transfer | short-answer | 2.34 | How does ICA's reliance on whitening as a preprocessing step connect it directly back to `pca`, later in this same cluster? | whitening is typically performed by first running PCA to decorrelate the data and rescaling each component to unit variance — the exact same eigendecomposition-of-the-covariance-matrix procedure `pca` performs — making PCA a literal preprocessing subroutine inside a standard ICA pipeline, not merely a conceptually related idea *(required: names PCA as the literal mechanism used for whitening, not just a related technique)* | — |
| T3 | transfer | short-answer | 2.39 | Why does ICA's use of higher-order statistics (beyond the mean and covariance PCA relies on) illustrate a general theme about the limits of Gaussian-based modeling elsewhere in the curriculum? | many methods (PCA, OLS under Gauss-Markov, GP regression) rely entirely on means and covariances because those fully describe a Gaussian; ICA specifically exists to detect exactly the kind of structure (genuine statistical dependence beyond correlation) that a purely mean-and-covariance-based, Gaussian-flavored toolkit is blind to, illustrating why higher-order or non-Gaussian methods are sometimes essential *(required: names the general "Gaussian-based tools are blind to non-covariance structure" theme, using ICA as the counterexample)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.29…2.39.*

---

## Principal Component Analysis (`pca`)
*Prereq: PCA (Matrix Edition), Covariance Matrix · ancestors 33 · b₀ = 1.26*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.26 | State PCA's practical goal, as distinct from `pca-matrix-edition`'s linear-algebra machinery. | dimensionality reduction — project high-dimensional data onto fewer dimensions (the top principal components) while retaining as much variance as possible | — |
| R2 | recall | mcq | 0.56 | PCA is used for all of these *except*: | directly improving predictive accuracy on every possible task, guaranteed | picks a legitimate use (visualization, or preprocessing before another algorithm) as the exception, missing that PCA's unsupervised nature is exactly what makes the "guaranteed improvement" claim false | 
| A1 | apply | short-answer | 1.06 | A 1000-feature dataset has its top 10 principal components explaining 95% of total variance. What's the practical implication for dimensionality reduction before modeling? | the data's effective dimensionality is far lower than 1000 — reducing to just 10 components retains nearly all the variance, letting downstream models train on a dramatically smaller, largely-equivalent representation *(required: the "effective dimensionality much lower" framing)* | — |
| E1 | explain | short-answer | 1.76 | What's the key risk of PCA-based reduction for supervised tasks, and what alternative exists because of it? | PCA is unsupervised and never looks at the outcome; a low-variance direction it discards could, in principle, be the *most* predictive direction for a specific supervised task — this is exactly why supervised alternatives like LDA or partial least squares exist, echoing `lda`'s earlier contrast with PCA *(required: the direct callback to lda's supervised/unsupervised distinction)* | — |
| T1 | transfer | short-answer | 2.26 | How does the "elbow" heuristic for choosing the number of components to keep connect to `eckart-young`'s point about image compression? | both are the same underlying phenomenon: real data's structure typically concentrates in a few dominant directions (rapidly decaying singular values), so a scree plot's marginal-variance elbow and image compression's negligible small singular values are two views of the identical fact *(required: names the shared rapid-decay phenomenon explicitly)* | — |

*Coverage: 5 items, 0.26…2.26. This is the final concept of the machine-learning domain's clustering/dimensionality-reduction arc, closing the loop back to `eckart-young` from the linear-algebra sweep.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| clustering evaluated as if ground-truth accuracy existed | `clustering-methods` |
| K-means assumed to guarantee the global optimum | `k-means-clustering` |
| SVD-based reduction assumed to have no downside for clustering | `svd-for-clustering` |
| PPCA and ordinary PCA treated as unrelated methods | `probabilistic-pca` |
| kernel PCA's use case (nonlinear structure) inverted | `kernel-pca` |
| t-SNE/UMAP inter-cluster plot distances read as quantitatively meaningful | `t-sne`, `umap` |
| PCA's uncorrelatedness conflated with independence | `ica` |
| PCA assumed to universally improve downstream predictive accuracy | `pca` |

**Cluster total: 45 items across 9 concepts.** `ica`'s A1 is the most pointed cross-domain callback
in the ML sweep — the exact Cov(X,X²)=0 counterexample from the probability sweep is what ICA, as a
method, exists to rule out.
