# Cluster 5 — Joint & Conditional Structure

Joint/Marginal/Conditional Distribution, Covariance, Law of Total Expectation (5 concepts). Same
format as [Cluster 1](foundations-of-probability.md).

**Shared example.** Four of the five concepts below reuse the same 2×2 joint table (X,Y ∈ {0,1},
p(0,0)=0.1, p(0,1)=0.2, p(1,0)=0.3, p(1,1)=0.4) as it gets progressively more structure added — its
marginals, its conditionals, its covariance. Reusing one table across a concept chain, rather than a
fresh example per concept, is deliberate: it lets a learner see the *same* dependence show up in four
different computations, which is a more durable lesson than four unrelated worked examples would be.

**Closure note.** `joint-distribution` lists only `random-variables` as a prerequisite — the univariate
PMF/PDF concepts are *siblings*, not ancestors, in the current graph. So the item below introduces
joint PMF notation as this concept's own new material, rather than treating "PMF" as already-known
machinery being extended to two dimensions. That's a reasonable reading either way; flagged here in
case the graph is ever tightened to require `pmf`/`pdf` explicitly.

---

## Joint Distribution (`joint-distribution`)
*Prereq: Random Variables · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Define the joint PMF of discrete X, Y, and its normalization condition. | p(x,y) = P(X=x, Y=y); Σₓ Σᵧ p(x,y) = 1 | — |
| R2 | recall | mcq | −0.4 | Σₓ Σᵧ p(x,y) always equals: | 1 | answers "depends on X, Y" — missing that normalization is a defining requirement, not a property to check case by case → `joint-distribution` |
| A1 | apply | numeric | 0.2 | p(0,0)=0.1, p(0,1)=0.2, p(1,0)=0.3, p(1,1)=0.4. Verify validity and find P(X=1,Y=1). `[verified: sums to 1; 0.4]` | sums to 1.0 ✓; P(1,1)=0.4 | — |
| A2 | apply | short-answer | 0.35 | Using the same table, find P(X=Y) and P(X≠Y). `[verified: 0.5, 0.5]` | P(X=Y)=p(0,0)+p(1,1)=0.5; P(X≠Y)=0.5 | — |
| E1 | explain | derivation | 0.9 | Sum the table's rows and columns to get "totals" for X alone and Y alone, then use them to check whether X and Y are independent. `[verified: X-totals 0.3/0.7, Y-totals 0.4/0.6, product 0.12≠0.1]` | row totals: 0.3, 0.7; column totals: 0.4, 0.6; independence would require p(0,0)=0.3×0.4=0.12, but the table has p(0,0)=0.1 ≠ 0.12 — so X, Y are dependent *(required: the explicit product comparison)* | checks only one cell or assumes the totals matching some pattern is sufficient → `joint-distribution` |
| E2 | explain | short-answer | 1.05 | Explain why a joint distribution carries *strictly more* information than its two row/column totals — why can't you reconstruct the joint table from the totals alone? | the totals are consistent with *many* different joint tables — e.g. the independent-product table (0.12, 0.18, 0.28, 0.42) has the *same* totals (0.3/0.7 and 0.4/0.6) as the actual dependent table (0.1, 0.2, 0.3, 0.4) `[verified]`, yet the two tables clearly differ *(required: an explicit second table with matching totals, not just an assertion)* | — |
| T1 | transfer | short-answer | 1.4 | A hospital records (blood pressure category, cholesterol category) per patient. Explain why knowing the blood pressure distribution *and* the cholesterol distribution separately still can't tell a doctor whether high readings of both tend to occur in the *same* patients. What does the joint distribution capture that the two separate ones can't? | the co-occurrence / dependence structure — precisely E2's point, applied to a real diagnostic setting: two very different patterns of overlap between the conditions are compatible with identical separate distributions of each condition alone *(required)* | assumes knowing both separate distributions is "basically" the same as knowing the joint one → `joint-distribution` |
| R3 | recall | short-answer | −0.58 | Define the joint CDF F(x,y) of X and Y. | F(x,y) = P(X≤x, Y≤y) | — |
| R4 | recall | mcq | −0.52 | For continuous X,Y, the joint density f(x,y) must satisfy which pair of conditions? (a) f(x,y)≥0 and ∫∫f(x,y)dxdy=1 over the plane (b) f(x,y)≤1 everywhere and ∫∫f=1 (c) f(x,y)≥0 and f(x,y)≤1 everywhere (d) f must be symmetric in x and y | (a) | picks (b) or (c) — believing a density is itself bounded by 1 like a probability, when only its integral is constrained → `joint-distribution` |
| R5 | recall | mcq | −0.46 | True or false: a joint PMF table for two binary random variables has 4 independent free parameters. | False — 3; the four cell probabilities must sum to 1, so only 3 are free | claims 4, forgetting the normalization constraint removes one degree of freedom → `joint-distribution` |
| R6 | recall | short-answer | −0.4 | Fill in the blank: for a jointly continuous (X,Y), the density f(x,y) is obtained from the joint CDF by ___. | f(x,y) = ∂²F(x,y)/∂x∂y | — |
| R7 | recall | mcq | −0.34 | Which of the following is NOT part of the definition of a valid joint PMF? (a) p(x,y) ≥ 0 for all x,y (b) Σₓ Σᵧ p(x,y) = 1 (c) p(x,y) ≤ p_X(x) for all y (d) p(x,y) is defined for every pair in the support | (c) — true of any valid joint PMF, but a *consequence*, not part of the definition itself, which only requires nonnegativity and normalization | selects (a) or (b) as "not part of the definition", not recognizing them as the actual defining conditions → `joint-distribution` |
| R8 | recall | short-answer | −0.28 | What does it mean for (X,Y) to be "jointly discrete"? | both X and Y take values in a countable set, and the joint distribution is fully described by a PMF p(x,y) over pairs in that countable set | — |
| R9 | recall | mcq | −0.22 | For n random variables X₁,...,Xₙ, their joint distribution is a function of how many arguments? | n | — |
| R10 | recall | short-answer | −0.16 | Fill in the blank: the support of a joint PMF p(x,y) is the set of pairs (x,y) such that ___. | p(x,y) > 0 | — |
| R11 | recall | mcq | −0.1 | True or false: for a jointly continuous (X,Y), f(x₀,y₀) at a specific point can be interpreted directly as a probability. | False — f(x₀,y₀) is a density, not a probability; only P((X,Y)∈A) = ∫∫_A f(x,y)dxdy for a region A has probability meaning | treats f(x₀,y₀) as P(X=x₀,Y=y₀), carrying discrete-case intuition into the continuous case → `joint-distribution` |
| R12 | recall | short-answer | −0.04 | State the formula for P((X,Y) ∈ A) in terms of a continuous joint density f(x,y). | P((X,Y)∈A) = ∫∫_A f(x,y) dx dy | — |
| R13 | recall | mcq | 0.02 | Which condition is sufficient, on its own, for f(x,y) to be a valid joint density? (a) nonnegative everywhere (b) integrates to 1 over the whole plane (c) both nonnegative everywhere AND integrating to 1 over the whole plane (d) f is symmetric, f(x,y)=f(y,x) | (c) | picks (a) or (b) alone, missing that both conditions are jointly required → `joint-distribution` |
| R14 | recall | short-answer | 0.06 | Fill in the blank: for a discrete joint PMF, Σₓ Σᵧ p(x,y) = ___. | 1 | — |
| R15 | recall | short-answer | 0.1 | In one sentence, what extra information does a joint distribution carry that the two individual (marginal) distributions, considered separately, do not? | the co-occurrence / dependence structure between X and Y — how likely particular *combinations* of values are, not just how likely each variable's values are on their own | — |
| R16 | recall | mcq | 0.14 | A joint PMF table with 3 possible values of X and 2 possible values of Y has how many free (independently choosable) parameters? | 5 — there are 3×2=6 cells, and normalization removes one degree of freedom, leaving 6−1=5 | answers 6, forgetting the normalization constraint → `joint-distribution` |
| A3 | apply | numeric | 0.28 | X∈{1,2}, Y∈{1,2,3}: p(1,1)=0.05, p(1,2)=0.1, p(1,3)=0.15, p(2,1)=0.2, p(2,2)=0.25, p(2,3)=0.25. Find P(X=2, Y≤2). `[verified: 0.45]` | P(X=2,Y≤2) = p(2,1)+p(2,2) = 0.45 | — |
| A4 | apply | numeric | 0.4 | Continuous joint density f(x,y)=6xy² on [0,1]². Verify it's valid and find P(X<0.5, Y<0.5). `[verified: total=1; 0.03125]` | total integral = (∫₀¹6x dx)(∫₀¹y²dy) = 3 × ⅓ = 1 ✓; P(X<0.5,Y<0.5) = (∫₀^0.5 6x dx)(∫₀^0.5 y² dy) = 0.75 × 0.041667 = 0.03125 | integrates only one of the two variables over its restricted range and leaves the other at its full-range value → `joint-distribution` |
| A5 | apply | short-answer | 0.3 | p(0,0)=0.15, p(0,1)=0.25, p(1,0)=0.1, p(1,1)=0.5. Verify validity and find P(X+Y=1). `[verified: sums to 1; 0.35]` | sums to 1.0 ✓; P(X+Y=1)=p(0,1)+p(1,0)=0.35 | includes p(1,1) in the X+Y=1 event, confusing "X+Y=1" with "X=1 or Y=1" → `joint-distribution` |
| A6 | apply | short-answer | 0.45 | A fair die (X∈{1,...,6}) is rolled independently of a fair coin (Y∈{H,T}). Write the joint PMF p(x,y) for any pair and verify it sums to 1. `[verified: sums to 1]` | p(x,y) = (1/6)(1/2) = 1/12 for each of the 12 pairs; Σ = 12 × 1/12 = 1 ✓ | combines the two probabilities by adding (1/6 + 1/2) instead of multiplying, for the independent joint PMF → `joint-distribution` |
| E3 | explain | short-answer | 0.95 | For jointly continuous X,Y, explain why P(X=x₀,Y=y₀) = 0 for any specific point, even though the density f(x₀,y₀) can be strictly positive. | probability for continuous variables is an *area/volume* under the density over a region; a single point has zero area, so the double integral over that point is 0 regardless of how large f(x₀,y₀) is there — density and probability are related but not the same quantity *(required: the area-vs-height distinction, not just an assertion)* | — |
| E4 | explain | derivation | 1.0 | Prove that the joint CDF F(x,y)=P(X≤x,Y≤y) is non-decreasing in each argument separately. | fix y; for x₁<x₂, the event {X≤x₁,Y≤y} ⊆ {X≤x₂,Y≤y}, so by monotonicity of probability F(x₁,y)≤F(x₂,y); the symmetric argument with x fixed gives monotonicity in y *(required: the subset-of-events argument, not just the claim)* | — |
| E5 | explain | short-answer | 1.15 | Explain why F(x,y) → 1 as both x→∞ and y→∞, and F(x,y) → 0 as x→−∞ for any fixed y. | as x,y→∞, {X≤x,Y≤y} approaches the entire sample space, so its probability approaches 1; as x→−∞ (fixed y), {X≤x,Y≤y} approaches the empty event, so its probability approaches 0 *(required: both limiting arguments)* | — |
| T2 | transfer | short-answer | 1.5 | An online store tracks (device type: mobile/desktop, purchase amount). Knowing 60% of visits are mobile and the average purchase is $40 doesn't tell marketing whether *mobile* visits tend to be the low-value ones worth a targeted discount. Explain, in joint-vs-marginal terms, what the team needs and why the two marginals alone can't supply it. | the team needs the *joint* distribution of (device type, purchase amount) — how purchase amount is distributed *within* the mobile subgroup — because the two marginals are compatible with many different joint tables, some where mobile purchases skew low and others where they don't, exactly as in E2 *(required: explicit tie to the non-uniqueness of joint tables given marginals)* | assumes the marginal purchase-amount distribution applies equally to the mobile subgroup, without checking the joint structure → `joint-distribution` |
| T3 | transfer | short-answer | 1.6 | Geneticists report each of two gene loci's individual allele frequencies (each marginal), but that alone can't reveal "linkage disequilibrium" — a tendency for particular alleles at the two loci to be co-inherited. Explain what additional information linkage disequilibrium requires and why the marginals can't detect it. | linkage disequilibrium is a statement about the *joint* distribution of the two loci's alleles — whether specific allele *combinations* occur more or less often than the marginals alone would predict; two loci can share any fixed pair of marginal allele frequencies while their joint co-inheritance pattern varies freely, so only the joint table can show linkage disequilibrium *(required)* | treats matching individual allele frequencies as ruling out linkage disequilibrium → `joint-distribution` |

*Coverage: 16/6/5/3 — 30 items, −0.6…1.6. E1's ad hoc row/column sums are exactly what `marginal-distribution` formalizes next.*

---

## Marginal Distribution (`marginal-distribution`)
*Prereq: Joint Distribution · ancestors 6 · b₀ = 0.47*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.53 | Define the marginal PMF of X in terms of a joint PMF p(x,y). | p_X(x) = Σᵧ p(x,y) — sum out y | — |
| R2 | recall | mcq | −0.35 | To get the marginal of X from a joint PMF table, you: | sum each row (or column, depending on layout) | picks "take the diagonal entries" — confuses marginalizing with the P(X=Y) computation from the previous concept's A2 → `marginal-distribution` |
| A1 | apply | numeric | 0.27 | Using the shared table, find the marginal PMFs of X and of Y — this is exactly `joint-distribution`'s E1, now formalized and named. `[verified: 0.3/0.7 and 0.4/0.6]` | p_X = (0.3, 0.7); p_Y = (0.4, 0.6) | — |
| A2 | apply | short-answer | 0.4 | For continuous joint density f(x,y)=x+y on [0,1]², find the marginal density f_X(x). `[verified: integrates to 1]` | f_X(x) = ∫₀¹ (x+y) dy = x + 0.5, for 0≤x≤1; check: ∫₀¹(x+0.5)dx = 1 ✓ | forgets to integrate out y and leaves f_X as a function of both x and y → `marginal-distribution` |
| E1 | explain | derivation | 0.97 | Prove that a marginal PMF (obtained by summing out the other variable) is itself a valid PMF, using only the joint PMF's own normalization. | Σₓ p_X(x) = Σₓ Σᵧ p(x,y) = 1, directly from the joint's defining condition *(required: the double-sum equality, not just "it must be valid because it's a distribution")* | — |
| E2 | explain | short-answer | 1.1 | Using an explicit example, show that two *different* joint distributions can share the *same* two marginals. | the shared table's dependent joint (0.1, 0.2, 0.3, 0.4) and the independent-product table built from its own marginals (0.12, 0.18, 0.28, 0.42) both have marginals (0.3, 0.7) and (0.4, 0.6) `[verified]`, yet the joint entries differ — proving the marginals alone don't pin down the joint *(required: the concrete second table)* | — |
| T1 | transfer | short-answer | 1.47 | A weather app reports P(rain tomorrow)=30% and, separately, P(traffic jam tomorrow)=40%. Someone computes P(rain AND traffic)=0.3×0.4=12%. What assumption does this secretly make, and why is it probably wrong here? | it assumes X, Y are *independent* — that the joint pmf is the product of the marginals, exactly the assumption E2 showed the marginals alone cannot justify; rain plausibly *causes* more traffic (positive dependence), so treating them as independent likely **understates** the true joint probability | accepts the 12% figure as correct because "that's how you combine two probabilities" → `marginal-distribution` |
| R3 | recall | short-answer | −0.5 | Define the marginal density of continuous Y in terms of a joint density f(x,y). | f_Y(y) = ∫ f(x,y) dx — integrate out x | — |
| R4 | recall | mcq | −0.44 | Marginalizing a joint PMF over Y means: | summing p(x,y) over all values of y | picks "fixing y at one value" — confuses marginalizing (summing away) with conditioning (fixing) → `marginal-distribution` |
| R5 | recall | mcq | −0.38 | True or false: the marginal distribution of X can always be recovered uniquely from the joint distribution. | True — p_X(x)=Σᵧp(x,y) is a deterministic function of the joint; the reverse (recovering the joint from the marginals) is generally NOT unique | claims false, or claims the reverse recovery is also always possible → `marginal-distribution` |
| R6 | recall | short-answer | −0.32 | Fill in the blank: p_Y(y) = Σₓ ___. | p(x,y) | — |
| R7 | recall | mcq | −0.26 | Which formula correctly defines the marginal PMF of X? (a) p_X(x) = Σᵧ p(x,y) (b) p_X(x) = p(x,x) (c) p_X(x) = max_y p(x,y) (d) p_X(x) = p(x,y)/p_Y(y) | (a) | picks (d) — that's the conditional PMF formula, not the marginal → `marginal-distribution` |
| R8 | recall | short-answer | −0.2 | For three jointly distributed variables X, Y, Z, how do you obtain the marginal PMF of X alone? | sum (or integrate) the joint PMF/density over all values of both Y and Z | — |
| R9 | recall | mcq | −0.14 | The marginal CDF F_X(x) relates to the joint CDF F(x,y) by: | F_X(x) = lim_{y→∞} F(x,y) | claims F_X(x) = F(x, x), confusing the marginal limit with evaluating the joint CDF at equal arguments → `marginal-distribution` |
| R10 | recall | mcq | −0.08 | True or false: two genuinely different joint distributions can have identical marginals for X and for Y. | True — this is exactly what `joint-distribution`'s E2 and this concept's E2 demonstrate with an explicit pair of tables | claims false — that matching marginals forces the joints to match → `marginal-distribution` |
| R11 | recall | short-answer | −0.02 | Fill in the blank: the process of computing p_X(x) from a joint PMF p(x,y) is called ___. | marginalization (or "marginalizing out" Y) | — |
| R12 | recall | mcq | 0.04 | Which operation turns a joint PMF into a marginal PMF? | summing over the full range of the other variable | — |
| R13 | recall | short-answer | 0.1 | State the general n-variable marginalization formula for obtaining p(x₁) from a joint PMF over n discrete variables X₁,...,Xₙ. | p(x₁) = Σ over all values of x₂,...,xₙ of p(x₁,x₂,...,xₙ) | — |
| R14 | recall | mcq | 0.16 | A marginal PMF p_X(x), once computed from a joint PMF, must itself: | be nonnegative and sum to 1 over the values of X — it is a valid PMF in its own right | claims it need only sum to 1 within the row it came from, not over all of X's values → `marginal-distribution` |
| R15 | recall | short-answer | 0.22 | True or false, with one line of justification: knowing only the two marginal distributions p_X and p_Y is enough to reconstruct the joint p(x,y) whenever X and Y happen to be independent. | True — under independence p(x,y)=p_X(x)p_Y(y), so the marginals determine the joint exactly in that special case; it is dependence that breaks this | claims the marginals never determine the joint, even under independence → `marginal-distribution` |
| R16 | recall | mcq | 0.28 | For a continuous jointly distributed (X,Y), the marginal density f_X(x) is obtained by: | integrating f(x,y) over all y, holding x fixed | integrates over x instead of y, or evaluates f(x,y) at a single y rather than integrating → `marginal-distribution` |
| A3 | apply | numeric | 0.3 | X∈{1,2}, Y∈{1,2,3}: p(1,1)=0.1, p(1,2)=0.05, p(1,3)=0.15, p(2,1)=0.3, p(2,2)=0.25, p(2,3)=0.15. Find the marginal PMF of X. `[verified: 0.3, 0.7]` | p_X(1)=0.1+0.05+0.15=0.3; p_X(2)=0.3+0.25+0.15=0.7 | sums along the wrong axis, producing a "marginal of X" that is actually a marginal of Y → `marginal-distribution` |
| A4 | apply | numeric | 0.42 | Continuous joint density f(x,y)=2 on the triangle 0≤x≤y≤1. Find the marginal density f_X(x). `[verified: integrates to 1]` | f_X(x) = ∫ₓ¹ 2 dy = 2(1−x), for 0≤x≤1; check ∫₀¹ 2(1−x) dx = 1 ✓ | integrates y over the full [0,1] range instead of respecting the triangle's constraint x≤y≤1, over-counting mass outside the support → `marginal-distribution` |
| A5 | apply | short-answer | 0.33 | Three binary variables X,Y,Z: p(0,0,0)=0.05, p(0,0,1)=0.1, p(0,1,0)=0.05, p(0,1,1)=0.1, p(1,0,0)=0.15, p(1,0,1)=0.2, p(1,1,0)=0.15, p(1,1,1)=0.2. Find the marginal PMF of X. `[verified: sums to 1; 0.3, 0.7]` | p_X(0)=0.05+0.1+0.05+0.1=0.3; p_X(1)=0.15+0.2+0.15+0.2=0.7 | sums out only one of the two other variables, leaving a partial joint over (X,Z) or (X,Y) mislabeled as "the marginal" → `marginal-distribution` |
| A6 | apply | short-answer | 0.5 | Using A3's marginal p_X=(0.3, 0.7), find P(X≤1) and P(X>1). `[verified: 0.3, 0.7]` | P(X≤1)=p_X(1)=0.3; P(X>1)=p_X(2)=0.7 | adds p_X(1)+p_X(2) for P(X≤1), effectively double-counting the whole sample space → `marginal-distribution` |
| E3 | explain | derivation | 1.02 | Extend E1's proof: show that summing a joint PMF over n−1 of n variables always yields a valid PMF over the remaining variable, for general n. | p(xᵢ) = Σ over the other n−1 variables of p(x₁,...,xₙ) ≥ 0 termwise; summing further over xᵢ gives Σ_{xᵢ} p(xᵢ) = Σ over *all* n variables of the joint = 1, by the joint's own normalization *(required: the full-sum equality, generalizing E1's double-sum argument)* | — |
| E4 | explain | short-answer | 1.15 | Explain why marginalizing a *continuous* joint density means integrating rather than summing, and how this connects back to the discrete definition. | probability mass in the continuous case is spread over intervals rather than concentrated at points, so "summing over all values of y" becomes, in the limit of infinitesimally fine partitions, integrating f(x,y) dy over y — the same operation as the discrete sum, with Σ replaced by ∫ as the outcome space becomes uncountable *(required: the sum-to-integral limiting argument)* | — |
| E5 | explain | short-answer | 1.28 | Using explicit numbers, show that identical marginals p_X=(0.5,0.5), p_Y=(0.5,0.5) can arise from a joint with positive dependence, a joint with negative dependence, AND the independent joint — i.e. marginals reveal nothing about even the *direction* of any dependence. | independent: p=(0.25,0.25,0.25,0.25); positive dependence: p(0,0)=0.4,p(0,1)=0.1,p(1,0)=0.1,p(1,1)=0.4 (X,Y tend to match); negative dependence: p(0,0)=0.1,p(0,1)=0.4,p(1,0)=0.4,p(1,1)=0.1 (X,Y tend to differ) — all three tables have marginals (0.5,0.5) and (0.5,0.5) `[verified]`, yet the dependence direction differs completely *(required: all three explicit tables)* | — |
| T2 | transfer | short-answer | 1.55 | A university reports the marginal distribution of applicants' test scores and, separately, of their family income bracket. A critic claims these two marginals are "basically everything you need" to know whether the admitted class is income-diverse across every score band. Explain why the marginals alone can't answer that, and what the university would actually need to check. | the university needs the *joint* distribution of (income bracket, score) among admits — specifically whether high scores cluster in high-income applicants — because, as in E5, many different joint tables share the same two marginals, some income-diverse across score bands and some not *(required)* | assumes matching marginals of score and income guarantee diversity across score bands → `marginal-distribution` |
| T3 | transfer | short-answer | 1.65 | An ecologist has the marginal abundance distribution of species A and of species B across sites, and wants to know if the species tend to co-occur (both abundant at the same sites) or exclude each other. Explain why the two marginals cannot distinguish these ecological patterns, and what additional information is required. | co-occurrence vs. exclusion is a statement about the *joint* distribution of (A-abundance, B-abundance) — whether high-A sites also tend to be high-B (positive dependence) or low-B (negative dependence) — and as E5 shows explicitly, the same pair of marginals is compatible with either pattern, so only the joint table can distinguish them *(required)* | infers co-occurrence or exclusion directly from the two separate marginal abundance distributions → `marginal-distribution` |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.65.*

---

## Conditional Distribution (`conditional-distribution`)
*Prereq: Joint Distribution, Conditional Probability · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define the conditional PMF of Y given X=x, and its one requirement. | p(y\|x) = p(x,y)/p_X(x), requiring p_X(x) > 0 | — |
| R2 | recall | mcq | −0.2 | As a function of y for *fixed* x, p(y\|x) is: | a valid PMF — nonnegative and summing to 1 over y | claims it "always equals the marginal p_Y(y)" — true only under independence, not in general → `conditional-distribution` |
| A1 | apply | numeric | 0.34 | Using the shared table (marginals p_X = 0.3, 0.7), find the conditional PMF of Y given X=0. `[verified: 1/3, 2/3]` | p(0\|0)=0.1/0.3=1/3; p(1\|0)=0.2/0.3=2/3 | divides by the wrong marginal, e.g. by p_Y instead of p_X → `conditional-distribution` |
| A2 | apply | numeric | 0.5 | Find the conditional PMF of Y given X=1, and compare to A1's answer given X=0. What does the difference tell you about independence? `[verified: 0.4286, 0.5714 — differs from A1]` | p(0\|1)≈0.4286, p(1\|1)≈0.5714 — different from (1/3, 2/3), confirming X and Y are dependent, consistent with `joint-distribution`'s E1 finding | finds the conditionals differ but doesn't connect that to dependence → `conditional-distribution` |
| E1 | explain | derivation | 1.04 | Prove p(·\|x) is a valid PMF over y, for any fixed x with p_X(x)>0. | Σᵧ p(y\|x) = Σᵧ p(x,y)/p_X(x) = p_X(x)/p_X(x) = 1, using the marginal's own defining sum from `marginal-distribution` *(required)* | — |
| E2 | explain | short-answer | 1.2 | Explain why "X, Y independent" is *equivalent* to "the conditional distribution of Y given X=x doesn't depend on x" — argue both directions. | independence ⟹ p(x,y)=p_X(x)p_Y(y) ⟹ p(y\|x)=p_Y(y) for every x (no dependence on x); conversely, if p(y\|x)=p_Y(y) for every x, then p(x,y)=p(y\|x)p_X(x)=p_Y(y)p_X(x), which is exactly the independence factorization *(required: both directions, not just one)* | — |
| T1 | transfer | short-answer | 1.54 | A doctor reports: given a patient smokes, P(lung disease)=15%; given they don't, P(lung disease)=2%. Why can quoting one overall (marginal) P(lung disease) for the whole population be seriously misleading for an individual patient? | the marginal is a weighted blend of the two very different conditional risks (weighted by smoking prevalence — a first, informal glimpse of `law-of-total-expectation`); reporting only the blend obscures a huge disparity that matters enormously depending on which conditional group a specific patient actually falls into *(required)* | treats the marginal risk as informative for a specific, known smoker or non-smoker → `conditional-distribution` |
| R3 | recall | short-answer | −0.4 | Define the conditional density of continuous Y given X=x, and its one requirement. | f(y\|x) = f(x,y)/f_X(x), requiring f_X(x) > 0 | — |
| R4 | recall | mcq | −0.34 | For fixed x, Σᵧ p(y\|x) equals: | 1 | answers "p_X(x)" or "depends on x", not recognizing that p(·\|x) is a normalized PMF in its own right → `conditional-distribution` |
| R5 | recall | mcq | −0.28 | True or false: p(x\|y) and p(y\|x) are generally equal. | False — they're related by Bayes' rule, p(x\|y) = p(y\|x)p_X(x)/p_Y(y), and are equal only in special cases | claims they're always equal — the P(A\|B) vs P(B\|A) confusion in variable form → `conditional-distribution` |
| R6 | recall | short-answer | −0.22 | Fill in the blank: p(y\|x) = ___ / ___. | p(x,y) / p_X(x) | — |
| R7 | recall | mcq | −0.16 | The definition of the conditional PMF p(y\|x) requires which condition? | p_X(x) > 0 | requires p(x,y) > 0 instead of p_X(x) > 0, missing which quantity must be nonzero to divide by it → `conditional-distribution` |
| R8 | recall | short-answer | −0.1 | If X and Y are independent, what does the conditional distribution p(y\|x) simplify to? | p_Y(y), for every x with p_X(x) > 0 — conditioning on X carries no information about Y | — |
| R9 | recall | mcq | −0.04 | Which statement correctly expresses "X and Y are independent" using conditional distributions? | p(y\|x) = p_Y(y) for every x with p_X(x) > 0 | states p(y\|x) = p(x\|y), confusing independence with the symmetric-conditional condition → `conditional-distribution` |
| R10 | recall | mcq | 0.02 | True or false: the conditional distribution of Y given X=x is itself a valid probability distribution over y. | True — nonnegative and sums (or integrates) to 1 over y, as E1 proves | claims false, treating p(y\|x) as merely a scaled fragment rather than a full distribution → `conditional-distribution` |
| R11 | recall | short-answer | 0.08 | For jointly continuous X, Y, write f(y\|x) in terms of the joint and marginal densities. | f(y\|x) = f(x,y) / f_X(x) | — |
| R12 | recall | mcq | 0.14 | Conditioning on X=x can be thought of as: | taking the "slice" of the joint distribution at that x and renormalizing it (dividing by p_X(x)) so it sums to 1 | describes it as deleting all other values of x from the sample space permanently, rather than renormalizing a slice → `conditional-distribution` |
| R13 | recall | short-answer | 0.2 | State Bayes' rule for random variables, relating p(x\|y) to p(y\|x). | p(x\|y) = p(y\|x) p_X(x) / p_Y(y) | — |
| R14 | recall | mcq | 0.26 | Which quantity normalizes the joint p(x,y) into the conditional p(y\|x)? | p_X(x), the marginal of the variable being conditioned on | normalizes by p_Y(y) instead, producing p(x\|y) rather than the requested p(y\|x) → `conditional-distribution` |
| R15 | recall | mcq | 0.32 | True or false: the conditional PMFs p(·\|x) must be identical across all values of x. | False — they're identical across x only in the special case of independence; in general each x gives a different conditional distribution | claims they must always match, since each is "still a distribution over the same Y" → `conditional-distribution` |
| R16 | recall | short-answer | 0.38 | Fill in the blank: the operation of computing p(y\|x) from a joint distribution p(x,y) is called ___. | conditioning | — |
| A3 | apply | numeric | 0.4 | X∈{1,2}, Y∈{1,2,3}: p(1,1)=0.1, p(1,2)=0.05, p(1,3)=0.15, p(2,1)=0.3, p(2,2)=0.25, p(2,3)=0.15 (marginal p_X=(0.3,0.7)). Find the conditional PMF of Y given X=1. `[verified: 1/3, 1/6, 1/2]` | p(1\|X=1)=0.1/0.3=1/3; p(2\|X=1)=0.05/0.3=1/6; p(3\|X=1)=0.15/0.3=1/2 | divides each cell by the row's own total (0.3) inconsistently, e.g. divides only the first cell → `conditional-distribution` |
| A4 | apply | numeric | 0.55 | Continuous joint density f(x,y)=2 on the triangle 0≤x≤y≤1. Find the conditional density f(y\|X=0.5). `[verified: uniform on [0.5,1], height 2]` | f_X(0.5)=2(1−0.5)=1; f(y\|X=0.5)=f(0.5,y)/1=2 for 0.5≤y≤1 — a Uniform(0.5,1) density; check ∫_{0.5}^1 2 dy = 1 ✓ | forgets that the support of Y given X=0.5 is restricted to [0.5,1] by the triangle constraint, and integrates over [0,1] instead → `conditional-distribution` |
| A5 | apply | numeric | 0.6 | Using A3's table, find the conditional PMF of Y given X=2. `[verified: 3/7≈0.4286, 5/14≈0.3571, 3/14≈0.2143]` | p(1\|X=2)=0.3/0.7≈0.4286; p(2\|X=2)=0.25/0.7≈0.3571; p(3\|X=2)=0.15/0.7≈0.2143 | reuses X=1's conditional distribution from A3 instead of recomputing with X=2's row and its own marginal p_X(2) → `conditional-distribution` |
| A6 | apply | numeric | 0.45 | A joint PMF has p(x,y)=0.12 at one cell, and the marginal at that x-value is p_X(x)=0.4. Find p(y\|x) at that cell. `[verified: 0.3]` | p(y\|x) = 0.12/0.4 = 0.3 | divides p_X(x) by p(x,y) instead of the reverse → `conditional-distribution` |
| E3 | explain | derivation | 1.1 | Derive Bayes' rule for random variables, p(x\|y) = p(y\|x)p_X(x)/p_Y(y), starting only from the definition of a conditional PMF. | by definition p(x\|y) = p(x,y)/p_Y(y); also by definition p(y\|x) = p(x,y)/p_X(x), so p(x,y) = p(y\|x)p_X(x); substituting gives p(x\|y) = p(y\|x)p_X(x)/p_Y(y) *(required: the substitution step shown explicitly)* | — |
| E4 | explain | short-answer | 1.25 | Explain why the definition of p(y\|x) requires p_X(x) > 0, and what goes wrong if you try to condition on an x with p_X(x) = 0. | conditioning divides the joint probability by p_X(x); if p_X(x)=0 the division is by zero and undefined, and there is no meaningful "slice" to renormalize because that value of X essentially never occurs, so there is no conditional evidence to condition on *(required: names the zero-division and the "no evidence" reading)* | — |
| E5 | explain | derivation | 1.4 | Prove that averaging the conditional PMF p(y\|x) over x, weighted by p_X(x), recovers the marginal p_Y(y): Σₓ p(y\|x)p_X(x) = p_Y(y). | Σₓ p(y\|x)p_X(x) = Σₓ [p(x,y)/p_X(x)]·p_X(x) = Σₓ p(x,y) = p_Y(y), by the marginal's own defining sum *(required: the cancellation step and the identification with the marginal definition)* — this is the discrete law of total probability, and the direct ancestor of `law-of-total-expectation` | — |
| T2 | transfer | short-answer | 1.65 | An A/B test reports an overall (marginal) conversion rate of 8%, blending a 5% rate in Control and a 12% rate in Treatment. A product manager tells a specific user who was just assigned to Treatment "you have an 8% chance of converting." What's wrong, in conditional-distribution terms? | the manager should use the *conditional* rate given this user's actual group, P(convert\|Treatment)=12%, not the marginal 8% that blends both groups; the marginal is only appropriate before the group assignment is known, exactly as T1's smoking example shows for a *known* smoker or non-smoker *(required)* | treats the marginal 8% as the right number for a user whose group is already known → `conditional-distribution` |
| T3 | transfer | short-answer | 1.75 | A spam filter is described as "highly accurate because P(word 'free' appears \| email is spam) = 90%." A user infers "so if my email contains 'free', there's a 90% chance it's spam." Explain why this reasoning is invalid, and what's actually needed to support the user's conclusion. | the user has swapped p(y\|x) for p(x\|y) — 90% is P('free' \| spam), not P(spam \| 'free'); by Bayes' rule (E3), computing P(spam \| 'free') also requires the base rate P(spam) and P('free' \| not spam), which are missing; if spam is rare, P(spam \| 'free') can be far below 90% *(required: names the swapped-conditional error and that the base rate is missing)* | accepts the 90%-spam-given-'free' conclusion directly from the stated 90% figure → `conditional-distribution` |

*Coverage: 16/6/5/3 — 30 items, −0.46…1.75.*

---

## Covariance (`covariance`)
*Prereq: Expectation, Joint Distribution · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | Define Cov(X,Y) and give its shortcut formula. | E[(X−E[X])(Y−E[Y])] = E[XY] − E[X]E[Y] | — |
| R2 | recall | mcq | 0.04 | Which is FALSE? (a) Cov(X,X)=Var(X) (b) independent ⟹ Cov=0 (c) Cov=0 ⟹ independent. | (c) is false | picks (c) as true — **the single most important misconception in this topic**, treating zero covariance and independence as equivalent → `covariance` |
| A1 | apply | numeric | 0.54 | E[X]=2, E[Y]=3, E[XY]=8. Find Cov(X,Y). `[verified: 2]` | 8 − 2·3 = 2 | — |
| A2 | apply | numeric | 0.65 | Using the shared table, compute E[X], E[Y], E[XY], and Cov(X,Y). `[verified: 0.7, 0.6, 0.4, −0.02]` | E[X]=0.7, E[Y]=0.6; E[XY] gets a contribution only from (1,1): 1·1·0.4=0.4; Cov = 0.4 − 0.42 = −0.02 | sums X·Y·p(x,y) over all four cells but forgets that three of the four terms vanish (whenever X=0 or Y=0), double-counting → `covariance` |
| E1 | explain | derivation | 1.24 | Derive Cov(X,Y) = E[XY] − E[X]E[Y] from E[(X−E[X])(Y−E[Y])], via linearity. | expand the product: XY − X·E[Y] − Y·E[X] + E[X]E[Y]; apply linearity termwise, using that E[X], E[Y] are constants under the expectation *(required, full expansion)* — the two-variable analog of `variance`'s shortcut-formula derivation | — |
| E2 | explain | derivation | 1.4 | Construct an explicit example where Cov(X,Y)=0 but X and Y are *maximally* dependent (Y is a deterministic function of X). | X ~ Uniform(−1,1), Y = X². Cov(X,Y) = E[X³] − E[X]E[X²] = 0 − 0 = 0 (E[X³]=0 by odd symmetry, E[X]=0) `[verified numerically: ≈0]`, yet Y is *completely determined* by X — about as dependent as two variables can be *(required: the explicit counterexample with both moments computed, not just an assertion that one exists)* | — |
| T1 | transfer | short-answer | 1.74 | An analyst finds Cov(hours studied, exam score) > 0 in one class, and separately Cov(ice cream sales, drowning deaths) > 0 across cities, concluding "studying causes higher scores" and "ice cream causes drowning." What's wrong with using covariance alone to justify either causal claim — and what's genuinely different between the two examples? | covariance measures *association only*, never causation on its own; the ice-cream/drowning case is a classic *confounder* (hot weather drives both); studying and scores plausibly do reflect a real causal link — but the covariance number alone cannot distinguish the two cases, which is exactly the point *(required: names confounding for the second case, and states that covariance can't discriminate the two situations)* | accepts the study-hours claim as justified because it "makes intuitive sense," without noticing the argument used to support it is identical to the flawed ice-cream argument → `covariance` |
| R3 | recall | short-answer | −0.2 | Define the correlation coefficient ρ(X,Y) in terms of Cov(X,Y). | ρ(X,Y) = Cov(X,Y) / (σ_X σ_Y), the covariance rescaled by the two standard deviations | — |
| R4 | recall | mcq | −0.14 | Cov(X,X) equals: | Var(X) | claims Cov(X,X) = E[X]² or = 0, not recognizing the definition collapses to the variance when Y=X → `covariance` |
| R5 | recall | mcq | −0.08 | True or false: Cov(aX+b, Y) = a·Cov(X,Y), for constants a,b. | True — shifting by b doesn't change covariance, and scaling by a scales it linearly | claims the shift b also scales the covariance, or that Cov is unaffected by the multiplier a → `covariance` |
| R6 | recall | short-answer | −0.02 | Fill in the blank: Cov(X,Y) = E[XY] − ___. | E[X]E[Y] | — |
| R7 | recall | mcq | 0.1 | The units of Cov(X,Y) are: | the product of X's units and Y's units (e.g. dollars × years) | claims Cov is always unitless, confusing it with correlation ρ, which *is* unitless → `covariance` |
| R8 | recall | short-answer | 0.16 | In one sentence, what does a positive covariance indicate about how X and Y tend to move together? | X and Y tend to be above their own means at the same time, and below their own means at the same time — they move in the same direction | — |
| R9 | recall | mcq | 0.22 | True or false: Cov(X,Y) = Cov(Y,X). | True — covariance is symmetric in its two arguments by definition | claims covariance is asymmetric, confusing it with a conditional quantity → `covariance` |
| R10 | recall | short-answer | 0.28 | Fill in the blank: for any constant c, Cov(X,c) = ___. | 0 | — |
| R11 | recall | mcq | 0.34 | Which property correctly states covariance's bilinearity? | Cov(aX+bY, Z) = a·Cov(X,Z) + b·Cov(Y,Z) | states Cov(aX+bY,Z) = ab·Cov(X,Z)·Cov(Y,Z), conflating linearity with a multiplicative rule → `covariance` |
| R12 | recall | short-answer | 0.4 | State the formula for Var(X+Y) in terms of Var(X), Var(Y), and Cov(X,Y). | Var(X+Y) = Var(X) + Var(Y) + 2Cov(X,Y) | — |
| R13 | recall | mcq | 0.46 | Unlike the correlation coefficient ρ, the range of possible values for Cov(X,Y) is: | any real number — covariance is unbounded, while ρ is always in [−1,1] | claims Cov is also confined to [−1,1], conflating covariance with correlation → `covariance` |
| R14 | recall | mcq | 0.52 | By definition, "X and Y are uncorrelated" means: | Cov(X,Y) = 0 | states it means X and Y are independent, the exact confusion R2 targets, restated as a direct definitional question → `covariance` |
| R15 | recall | short-answer | 0.58 | Fill in the blank: the "centered" version of X used in covariance's definition E[(X−E[X])(Y−E[Y])] is X − ___. | E[X], the mean of X | — |
| R16 | recall | mcq | 0.6 | Var(X−Y) equals: | Var(X) + Var(Y) − 2Cov(X,Y) | states Var(X)+Var(Y)+2Cov(X,Y) for the difference, using the sum formula's sign instead of the difference's → `covariance` |
| A3 | apply | numeric | 0.5 | E[X]=5, E[Y]=10, E[XY]=48. Find Cov(X,Y). `[verified: −2]` | 48 − 5×10 = −2 | — |
| A4 | apply | numeric | 0.62 | p(0,0)=0.2, p(0,1)=0.3, p(1,0)=0.1, p(1,1)=0.4. Compute E[X], E[Y], E[XY], and Cov(X,Y). `[verified: 0.5, 0.7, 0.4, 0.05]` | E[X]=p(1,0)+p(1,1)=0.5; E[Y]=p(0,1)+p(1,1)=0.7; E[XY]=1·1·p(1,1)=0.4; Cov=0.4−0.5×0.7=0.05 | computes E[XY] by summing X·Y·p(x,y) over all four cells without noticing three of the four vanish → `covariance` |
| A5 | apply | numeric | 0.7 | Var(X)=4, Var(Y)=9, Cov(X,Y)=3. Find Var(X+Y) and Var(X−Y). `[verified: 19, 7]` | Var(X+Y)=4+9+2(3)=19; Var(X−Y)=4+9−2(3)=7 | uses the same sign for both sums, e.g. adds 2Cov in both cases → `covariance` |
| A6 | apply | numeric | 0.66 | Cov(X,Y)=1.5. Find Cov(2X+1, −3Y+5). `[verified: −9]` | Cov(2X+1,−3Y+5) = (2)(−3)Cov(X,Y) = −9, using bilinearity and that additive constants drop out | multiplies by (2)(−3)(1)(5) or otherwise lets the additive constants 1 and 5 affect the result → `covariance` |
| E3 | explain | derivation | 1.3 | Derive Cov(aX+b, Y) = a·Cov(X,Y) directly from the definition E[(aX+b−E[aX+b])(Y−E[Y])]. | E[aX+b] = aE[X]+b by linearity, so aX+b−E[aX+b] = a(X−E[X]); substituting, Cov(aX+b,Y) = E[a(X−E[X])(Y−E[Y])] = a·E[(X−E[X])(Y−E[Y])] = a·Cov(X,Y) *(required: the substitution and the pulling-a-out-of-the-expectation step)* | — |
| E4 | explain | short-answer | 1.42 | Explain, from the definition, why Cov(X,c) = 0 for any constant c. | a constant c has E[c]=c, so c−E[c]=0 identically; Cov(X,c)=E[(X−E[X])·0] = E[0] = 0 — a constant has zero variability to covary with anything *(required: the c−E[c]=0 step)* | — |
| E5 | explain | derivation | 1.55 | Derive Var(X+Y) = Var(X) + Var(Y) + 2Cov(X,Y) starting from Var(X+Y) = E[((X+Y)−E[X+Y])²]. | (X+Y)−E[X+Y] = (X−E[X]) + (Y−E[Y]); squaring, ((X−E[X])+(Y−E[Y]))² = (X−E[X])² + (Y−E[Y])² + 2(X−E[X])(Y−E[Y]); taking E[·] termwise gives Var(X) + Var(Y) + 2Cov(X,Y) *(required: the expansion and termwise-expectation steps, not just citing the formula)* | — |
| T2 | transfer | short-answer | 1.85 | A portfolio holds two stocks with Var(A)=100, Var(B)=100, but Cov(A,B)=−60 (they tend to move in opposite directions). An investor claims "diversifying across two risky assets never reduces risk below what one alone has." Using Var(X+Y), show this is false here. | Var(A+B) = 100+100+2(−60) = 80 `[verified]` — strictly less than either individual variance (100), because the negative covariance partially cancels the two assets' fluctuations; the claim fails precisely because it ignores the covariance term *(required: the numeric Var(A+B) computation and the cancellation explanation)* | assumes portfolio variance is always Var(A)+Var(B) regardless of Cov(A,B) → `covariance` |
| T3 | transfer | short-answer | 1.95 | A factory finds Cov(machine temperature, defect rate) > 0 and a manager concludes "raising the machine's temperature causes more defects, so lowering it will fix quality." What additional evidence, beyond the covariance sign, would the manager need before acting on that conclusion — and what's a plausible confound? | covariance alone establishes association, not the direction or existence of causation (T1's point); a plausible confound is machine age or wear — an aging machine might run hotter *and* independently produce more defects, so cooling it artificially might not reduce defects at all; ruling this out needs a controlled experiment (e.g. varying temperature while holding machine age/wear fixed) or a designed intervention, not just the observed covariance *(required: names a plausible confound and that an intervention/experiment is needed)* | treats the positive covariance as sufficient grounds to act on the temperature variable directly → `covariance` |

*Coverage: 16/6/5/3 — 30 items, −0.26…1.95. E2 is the standout item in this cluster.*

---

## Law of Total Expectation (`law-of-total-expectation`)
*Prereq: Expectation, Conditional Distribution · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.18 | State the law of total expectation. | E[X] = E[E[X\|Y]] | — |
| R2 | recall | mcq | 0.12 | E[X\|Y] is itself: | a random variable — a function of Y | treats E[X\|Y] as a fixed number, missing the conceptual leap the whole law depends on → `law-of-total-expectation` |
| A1 | apply | numeric | 0.62 | Revisit `expectation`'s insurance example formally: let Y=1{claim occurs}. E[payout\|Y=0]=0, E[payout\|Y=1]=$3000, P(Y=1)=0.1. Recover the $300 premium via the law. `[verified: 300]` | E[payout] = P(Y=0)·0 + P(Y=1)·3000 = 0.9(0)+0.1(3000) = $300 — the same answer as before, now produced by the named tool rather than ad hoc case-splitting | — |
| A2 | apply | numeric | 0.75 | A student uses one of two study methods, chosen 50/50 at random: Method A gives expected score 75, Method B gives expected score 85. Find the overall expected score. `[verified: 80]` | 0.5(75) + 0.5(85) = 80 | averages the two *methods* as if they were equally represented in some other proportion, or forgets to weight by the 50/50 split explicitly when the split isn't even → `law-of-total-expectation` |
| E1 | explain | derivation | 1.32 | Prove the law of total expectation for the discrete case, via a double-sum swap. | E[X] = Σₓ x·p_X(x) = Σₓ x Σᵧ p(x,y) = Σᵧ Σₓ x·p(x\|y)·p_Y(y) = Σᵧ p_Y(y)·E[X\|Y=y] = E[E[X\|Y]] *(required: the sum-order swap must be shown explicitly, not asserted)* | — |
| E2 | explain | short-answer | 1.5 | Why must E[X\|Y] be treated as a random variable (a function of Y) *before* E[E[X\|Y]] means anything? What would go wrong if it were just one number? | if E[X\|Y] were a single fixed number, the outer expectation E[·] would have nothing to average over — the whole point of the law is that E[X\|Y=y] varies with y, and the *outer* expectation averages that variation, weighted by how likely each y is *(required)* | — |
| T1 | transfer | short-answer | 1.82 | A company's revenue depends on which of 3 equally-likely marketing campaigns ran, with expected revenues $2M, $3M, and $7M. An analyst says "just always run the $7M one — it's the guaranteed best." What's the error, using the law of total expectation to frame it? | the analyst conflates *highest expected value* with *guaranteed outcome* — expected revenue is a long-run average, not a promise; the $7M campaign could carry far higher variance (huge upside and huge downside) that the expectation number alone says nothing about, which is exactly the kind of question `law-of-total-variance` exists to answer next | agrees $7M is "guaranteed" simply because it's the largest of the three expected values → `law-of-total-expectation` |
| R3 | recall | short-answer | −0.12 | State the continuous version of the law of total expectation, using an integral over Y's density. | E[X] = ∫ E[X\|Y=y] f_Y(y) dy | — |
| R4 | recall | mcq | −0.06 | For a *fixed* value y, E[X\|Y=y] is: | a single fixed number — the average of X restricted to the slice where Y=y | claims E[X\|Y=y] is still a random variable even after y is fixed, conflating it with the unfixed E[X\|Y] → `law-of-total-expectation` |
| R5 | recall | mcq | 0.0 | True or false: the law of total expectation, E[X]=E[E[X\|Y]], requires X and Y to be independent. | False — it holds for *any* jointly distributed X, Y, independent or not; it's an identity, not a special case | claims independence is required, not realizing the law holds unconditionally → `law-of-total-expectation` |
| R6 | recall | short-answer | 0.06 | Fill in the blank: E[X] = E[___]. | E[X\|Y] | — |
| R7 | recall | mcq | 0.18 | For a partition of the sample space into events A₁,...,Aₙ, the law of total expectation states: | E[X] = Σᵢ E[X\|Aᵢ] P(Aᵢ) | states E[X] = Σᵢ E[X\|Aᵢ], omitting the P(Aᵢ) weights entirely → `law-of-total-expectation` |
| R8 | recall | short-answer | 0.24 | In words, what does E[X\|Y=y] represent? | the average value of X, restricted to (computed only over) the outcomes where Y takes the specific value y | — |
| R9 | recall | mcq | 0.3 | The law of total expectation, E[X]=E[E[X\|Y]], is also commonly called: | the tower property | calls it "the law of large numbers", confusing two unrelated named results → `law-of-total-expectation` |
| R10 | recall | mcq | 0.36 | True or false: E[X\|Y] and E[X] are always numerically equal. | False — E[X\|Y] is generally a random variable (unless X,Y independent), while E[X] is a fixed number; they coincide in value only after E[X\|Y] itself is averaged over Y | claims they're always the same number, missing that E[X\|Y] varies with Y in general → `law-of-total-expectation` |
| R11 | recall | short-answer | 0.42 | Fill in the blank: for a partition A₁,...,Aₙ of the sample space, E[X] = Σᵢ ___. | E[X\|Aᵢ] P(Aᵢ) | — |
| R12 | recall | mcq | 0.48 | The general "tower property" states E[E[X\|Y,Z]\|Y] equals: | E[X\|Y] — conditioning on more information (Y,Z), then averaging back down to just Y, is the same as conditioning on Y alone | claims it equals E[X\|Z], dropping the wrong variable from the tower | — |
| R13 | recall | short-answer | 0.54 | For discrete X,Y, write E[X\|Y=y] in terms of the conditional PMF p(x\|y). | E[X\|Y=y] = Σₓ x·p(x\|y) | — |
| R14 | recall | mcq | 0.6 | Which of these is NOT required for the law of total expectation E[X]=E[E[X\|Y]] to hold? | independence between X and Y | independence being required is the exact false belief this item targets; correct recall is that no such requirement exists → `law-of-total-expectation` |
| R15 | recall | mcq | 0.66 | True or false: the law of total expectation can be read as "E[X] is a weighted average of the conditional expectations E[X\|Y=y], weighted by how likely each y is." | True — this is exactly the interpretation behind A1–A3's worked examples | claims false, missing the weighted-average reading of the formula | — |
| R16 | recall | short-answer | 0.7 | Fill in the blank: E[X\|Y=y] = Σₓ x · ___. | p(x\|y), the conditional PMF of X given Y=y | — |
| A3 | apply | numeric | 0.66 | A factory uses 3 machines with usage probabilities 0.5, 0.3, 0.2 and expected outputs 100, 120, 90 respectively. Find the overall expected output. `[verified: 104]` | E[output] = 0.5(100) + 0.3(120) + 0.2(90) = 50+36+18 = 104 | forgets one of the three terms, e.g. averages only two of the three machines → `law-of-total-expectation` |
| A4 | apply | numeric | 0.7 | E[X\|Y=0]=10, E[X\|Y=1]=50, P(Y=1)=0.05. Find E[X]. `[verified: 12]` | E[X] = 0.95(10) + 0.05(50) = 9.5+2.5 = 12 | uses 0.5/0.5 weights instead of the actual 0.95/0.05 split given in the problem → `law-of-total-expectation` |
| A5 | apply | numeric | 0.68 | Roll a fair die; X = the number shown, Y = 1{roll is even}. Find E[X\|Y=even], E[X\|Y=odd], and use the law to recover E[X]. `[verified: 4, 3, 3.5]` | E[X\|even]=(2+4+6)/3=4; E[X\|odd]=(1+3+5)/3=3; E[X]=0.5(4)+0.5(3)=3.5 — matching the die's known unconditional mean, a useful consistency check | computes the unconditional average of all six faces (3.5) directly, skipping the conditioning step the item asks for → `law-of-total-expectation` |
| A6 | apply | numeric | 0.8 | Using the shared table p(0,0)=0.1, p(0,1)=0.2, p(1,0)=0.3, p(1,1)=0.4, find E[X\|Y=0], E[X\|Y=1], and use the law to recover E[X]. `[verified: 0.75, 0.6667, 0.7]` | P(X=1\|Y=0)=0.3/0.4=0.75; P(X=1\|Y=1)=0.4/0.6≈0.6667; E[X]=0.4(0.75)+0.6(0.6667)=0.7 — matching `covariance`'s directly-computed E[X]=0.7, a cross-check between the two methods | computes E[X\|Y] weighted by P(X) instead of P(Y), inverting which marginal supplies the weights → `law-of-total-expectation` |
| E3 | explain | derivation | 1.38 | Derive the continuous law of total expectation, E[X] = ∫ E[X\|Y=y] f_Y(y) dy, the analog of E1's discrete double-sum proof. | E[X] = ∫∫ x f(x,y) dx dy = ∫ [∫ x f(x\|y) dx] f_Y(y) dy = ∫ E[X\|Y=y] f_Y(y) dy, using f(x,y)=f(x\|y)f_Y(y) and swapping the order of integration *(required: the factorization and swap shown explicitly)* | — |
| E4 | explain | short-answer | 1.6 | Explain what the "tower property" name is getting at, using the general identity E[E[X\|Y,Z]\|Y] = E[X\|Y]. | conditioning on more information first (Y and Z) and then averaging back down to less information (just Y) gives the same result as conditioning on the smaller information set directly — you can't "extract" more from the finer conditioning than the coarser one already captures once you average back down; the levels stack like a tower, and collapsing from the top always lands on the same place as starting at the level you collapsed to *(required: the collapsing/stacking interpretation, not just restating the formula)* | — |
| E5 | explain | derivation | 1.7 | Prove the law of total expectation for a general partition {A₁,...,Aₙ} of the sample space (not phrased via a random variable Y): E[X] = Σᵢ E[X\|Aᵢ] P(Aᵢ). | E[X] = Σ_ω X(ω)P(ω) = Σᵢ Σ_{ω∈Aᵢ} X(ω)P(ω), splitting the sum by which partition block ω falls in (valid since the Aᵢ are disjoint and exhaustive); each inner sum is P(Aᵢ)·Σ_{ω∈Aᵢ} X(ω)P(ω)/P(Aᵢ) = P(Aᵢ)·E[X\|Aᵢ] by the definition of conditional expectation on an event, giving Σᵢ E[X\|Aᵢ]P(Aᵢ) *(required: the partition-based split and the identification of the inner sum with E[X\|Aᵢ])* | — |
| T2 | transfer | short-answer | 1.9 | A call center estimates average handle time using only day-shift calls (4 minutes), unaware that night-shift calls average 7 minutes and make up 30% of total call volume. Using the law of total expectation, find the true overall average handle time and explain the staffing-planning error in using 4 minutes alone. `[verified: 4.9]` | true E[time] = 0.7(4) + 0.3(7) = 2.8+2.1 = 4.9 minutes; using 4 minutes alone silently drops the night shift's weight entirely, understating the true average and any staffing plan built on it — exactly A3–A4's mechanism applied to operations *(required: the numeric recomputation and naming what's silently dropped)* | uses the day-shift-only average as if it already represented the full population → `law-of-total-expectation` |
| T3 | transfer | short-answer | 2.0 | A hedge fund reports expected return of 15% conditional on a bull market and −5% conditional on a bear market, with P(bull)=0.6. A junior analyst, who has only ever seen bull markets in their short career, plans around "the" expected return of 15%. Find the true overall expected return and explain the planning error. `[verified: 7]` | true E[return] = 0.6(15) + 0.4(−5) = 9−2 = 7%; the analyst's 15% figure is the *bull-market-conditional* expectation, not the unconditional one — treating a conditional expectation as if it were the marginal, understating downside exposure the bear-market scenario contributes *(required: the numeric recomputation and naming the conditional-vs-marginal confusion)* | plans using the bull-market conditional return as though it were the full expected return → `law-of-total-expectation` |

*Coverage: 16/6/5/3 — 30 items, −0.18…2.0.*

---

## Cluster 5 misconception index

| Tag | Blame |
|---|---|
| joint normalization treated as case-dependent rather than definitional | `joint-distribution` |
| marginal computed as diagonal entries, not row/column sums | `marginal-distribution` |
| dividing by the wrong marginal in a conditional PMF | `conditional-distribution` |
| independence assumed from matching marginals alone | `marginal-distribution` or `joint-distribution` |
| **Cov(X,Y)=0 treated as implying independence** | `covariance` |
| covariance/correlation cited as proof of causation | `covariance` |
| E[X\|Y] treated as a fixed number rather than a function of Y | `law-of-total-expectation` |
| continuous density believed bounded like a probability (f(x,y) ≤ 1) | `joint-distribution` |
| "marginalizing" (summing away) confused with "conditioning" (fixing a value) | `marginal-distribution` |
| p(x\|y) and p(y\|x) assumed equal, without Bayes' rule | `conditional-distribution` |
| a conditional distribution computed for one value of x reused for another x without recomputing | `conditional-distribution` |
| covariance assumed bounded to [−1,1] like correlation | `covariance` |
| law of total expectation assumed to require X, Y independent | `law-of-total-expectation` |

**Cluster total: 150 items across 5 concepts (30 each).** All numeric claims verified — the table computations by
script, and the Cov(X, X²)=0 counterexample by both closed form and a 2-million-sample Monte Carlo
check. The shared 2×2 table running through the first four concepts is the throughline: the same
dependence gets discovered informally in `joint-distribution`, named in `marginal-distribution`, put to
use in `conditional-distribution`, and finally measured in `covariance`.
