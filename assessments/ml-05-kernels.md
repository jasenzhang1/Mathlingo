# Machine Learning Cluster 5 — Kernels

Kernel, Mercer's Theorem, Radial Basis Function (3 concepts). Same format as
[Cluster 1](ml-01-foundations.md).

---

## Kernel (`kernel`)
*Prereq: Dot Product · ancestors 3 · b₀ = 0.19*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.81 | Define a kernel function. | a function K(x,y) computing something equivalent to a dot product in some (possibly high- or infinite-dimensional) feature space, without explicitly computing the feature mapping | — |
| R2 | recall | mcq | −0.55 | The kernel trick lets a dot-product-based algorithm operate in a high-dimensional space: | without ever explicitly transforming the data — dot products are simply replaced by kernel evaluations | claims it works "by explicitly transforming every data point into that space first" — defeats the entire point of the trick → `kernel` |
| A1 | apply | short-answer | 0.0 | The polynomial kernel K(x,y)=(x·y+1)² implicitly includes squared and cross terms of the original features. Why does computing those explicit terms get expensive for many features, while evaluating (x·y+1)² stays cheap? | the number of squared/cross-term features grows quadratically (or worse, for higher-degree kernels) with the original feature count, but the kernel formula itself is always just one dot product plus a constant, squared — a fixed, cheap computation regardless of how many implicit features it represents *(required: the quadratic-blowup-vs-fixed-cost contrast)* | — |
| E1 | explain | short-answer | 0.69 | Why is the kernel trick especially powerful when the implied feature space is infinite-dimensional? | explicitly computing an infinite-dimensional feature vector is literally impossible, yet the kernel function itself still evaluates to an ordinary finite number — the trick makes an otherwise-impossible computation trivial *(required)* | — |
| T1 | transfer | short-answer | 1.19 | Why can any algorithm written using only dot products between data points be "kernelized"? | if the raw feature vectors never appear anywhere except inside dot products, every dot product can be swapped for a kernel evaluation without changing the algorithm's structure at all, gaining nonlinear power for free — this is exactly why SVM was designed around this property *(required: the "raw vectors only appear inside dot products" condition)* | — |

| R3 | recall | short-answer | −1.15 | What mathematical operation does a kernel function K(x,y) implicitly compute? | a dot product (inner product) between the images of x and y under some feature mapping φ, i.e. K(x,y) = φ(x)·φ(y) | — |
| R4 | recall | short-answer | −1.1 | Name two classic examples of kernel functions. | the polynomial kernel and the RBF (Gaussian) kernel | — |
| R5 | recall | mcq | −0.9 | The main computational benefit of the kernel trick is: | avoiding the explicit computation of a (possibly very high- or infinite-dimensional) feature mapping | claims the benefit is "reducing the number of training examples needed" — the kernel trick is about computational cost per operation, not sample size → `kernel` |
| R6 | recall | short-answer | −1.0 | Fill in the blank: K(x,y) = φ(x)·φ(y), where φ is the (possibly implicit) ___ mapping. | feature |
| R7 | recall | mcq | −0.7 | Which of the following algorithms can be "kernelized"? | any algorithm that only ever uses dot products between data points, such as SVM | claims "only SVM specifically can be kernelized, no other algorithm" — the kernel trick applies generally to any dot-product-based algorithm (e.g. kernelized PCA, kernelized ridge regression), not uniquely to SVM → `kernel` |
| R8 | recall | short-answer | −0.75 | True or false: a kernel function must always return a non-negative value. | false — a kernel value can be negative; it just needs to correspond to some valid dot product, and dot products can be negative | — |
| R9 | recall | mcq | −0.55 | The linear kernel K(x,y)=x·y corresponds to: | no transformation at all — φ is simply the identity mapping | claims it "corresponds to an infinite-dimensional feature space, like RBF" — the linear kernel is the trivial, untransformed case → `kernel` |
| R10 | recall | short-answer | −0.45 | What must be true of the feature space dimensionality for the kernel trick to provide a genuine computational advantage? | the feature space must be higher-dimensional than the original space (often much higher, or infinite), so that computing the kernel directly is cheaper than materializing the mapped features | — |
| R11 | recall | mcq | −0.35 | A "kernel matrix" (or Gram matrix) is: | the matrix of K(xᵢ,xⱼ) values computed for every pair of points in a dataset | claims it's "the matrix of raw feature values before any kernel is applied" — the kernel matrix specifically holds pairwise kernel evaluations, not raw features → `kernel` |
| R12 | recall | short-answer | −0.25 | What kind of algorithms benefit most directly from the kernel trick? | algorithms whose only interaction with the data is through dot products (or distances derivable from dot products), such as SVM, kernelized PCA, and kernel ridge regression | — |
| R13 | recall | mcq | −0.15 | Choosing a kernel function is a decision analogous to: | choosing a feature transformation or basis expansion, without paying its full computational cost | claims it's "purely a hyperparameter with no relation to feature engineering" — a kernel *is* an implicit feature transformation, closely tied to feature engineering → `kernel` |
| R14 | recall | short-answer | −0.05 | Fill in the blank: swapping every dot product in an algorithm for a kernel evaluation is called "___izing" the algorithm. | kernel |
| R15 | recall | mcq | −0.2 | The polynomial kernel K(x,y)=(x·y+c)^d implicitly represents: | all monomial feature combinations up to degree d | claims it represents "only linear combinations of the original features" — that describes the linear kernel, not a degree-d polynomial kernel → `kernel` |
| R16 | recall | short-answer | −0.6 | Does using a kernel change what an algorithm is fundamentally trying to optimize, or just how efficiently it computes that optimization? | just how it's computed — the kernel trick is a computational technique; the underlying optimization objective (e.g. maximizing margin) is unchanged, only carried out implicitly in a transformed space | — |
| A2 | apply | short-answer | 0.05 | For 2 original features, the degree-2 polynomial kernel implicitly represents 6 monomial features (1, x₁, x₂, x₁², x₂², x₁x₂). For 100 original features, would explicitly listing all degree-2 monomials be more or less expensive relative to just evaluating the kernel formula? | far more expensive — the number of degree-2 monomials for 100 features grows roughly quadratically (~5,050 terms), while the kernel formula (x·y+c)² is still just one dot product, squared, regardless of feature count | — |
| A3 | apply | short-answer | 0.1 | Is the constant function K(x,y)=1 for all x,y a valid kernel? | yes — it corresponds to mapping every point to the same constant feature (e.g. φ(x)=1), and the dot product of two 1's is 1, satisfying the definition, even though it's a degenerate/useless one | — |
| A4 | apply | numeric | 0.15 | The linear kernel K(x,y)=x·y for x=(1,2), y=(3,1). Compute K(x,y). `[verified: 5]` | x·y = 1(3)+2(1) = 3+2 = 5 | — |
| A5 | apply | short-answer | 0.05 | The polynomial kernel K(x,y)=(x·y+1)² for x·y=5 (from A4). Compute K(x,y). `[verified: 36]` | (5+1)² = 36 | — |
| A6 | apply | short-answer | 0.2 | An algorithm's code explicitly extracts and stores φ(x) for every data point before computing dot products between them. Is this genuinely using the kernel trick? | no — the kernel trick specifically means never materializing φ(x); explicitly computing and storing it defeats the purpose, even if the eventual dot products give the same numbers | — |
| E2 | explain | short-answer | 0.6 | Why does a kernel function need to correspond to *some* valid dot product in *some* feature space, rather than being any arbitrary function of x and y? | if K doesn't correspond to a real dot product, the algorithm's underlying geometric/optimization guarantees (e.g. SVM's convexity, which relies on a valid inner-product structure) can break down, since the math was derived assuming a genuine inner product exists *(required)* | — |
| E3 | explain | short-answer | 0.75 | Why is the linear kernel considered a "trivial" special case of the kernel trick rather than a genuine use of it? | it corresponds to the identity feature mapping φ(x)=x, so evaluating it provides no access to a higher-dimensional or nonlinear representation — it's technically a valid kernel, but it doesn't unlock any of the power the trick exists for *(required)* | — |
| E4 | explain | short-answer | 0.65 | Why does the kernel trick's usefulness scale with how expensive the implicit feature mapping would otherwise be to compute directly? | the trick's entire value proposition is replacing an expensive explicit computation with a cheap one; when the implicit mapping is low-dimensional and cheap to compute directly anyway, there's little to be saved, but as the mapping's dimensionality grows the savings become dramatic or even the only way to proceed at all *(required)* | — |
| E5 | explain | short-answer | 0.7 | Why can a kernel be defined between objects that aren't naturally vectors at all (e.g. strings, graphs, sequences)? | since a kernel only needs to return a value equivalent to some dot product in some feature space, and never needs the objects to be explicitly embedded as vectors first, any pair of comparable objects can have a valid kernel defined over them as long as a suitable implicit feature space exists *(required)* | — |
| T2 | transfer | short-answer | 1.35 | How does the idea of a kernel as an "implicit feature transformation" connect to feature engineering practices used with linear models? | classical feature engineering explicitly constructs new features (e.g. adding x² or x₁x₂ terms) before feeding them to a linear model, hoping to capture nonlinearity that way; the kernel trick achieves the same *effect* — access to those same nonlinear combinations — without ever explicitly constructing the features, trading manual feature design for an implicit, often much richer, automatically-defined one *(required: names the shared goal via different mechanisms)* | — |
| T3 | transfer | short-answer | 1.25 | Why is understanding kernels a prerequisite for both `svm` and `rbf`, as reflected in the graph's prerequisite structure? | both concepts build directly on the general kernel-trick machinery this concept defines — `svm` uses it to find nonlinear decision boundaries efficiently, and `rbf` is itself a specific, extremely common choice of kernel function; neither concept can be properly understood without first grasping what a kernel computes and why swapping dot products for kernel evaluations is valid *(required: names both downstream dependencies)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.15…1.35.*

---

## Mercer's Theorem (`mercers-theorem`)
*Prereq: Kernel, Positive Definite Matrices · ancestors 17 · b₀ = 0.95*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.05 | State Mercer's theorem informally. | K(x,y) is a valid kernel (corresponds to some dot product in some feature space) if and only if the matrix of K(xᵢ,xⱼ) values, for any finite set of points, is positive semi-definite | — |
| R2 | recall | mcq | 0.25 | Mercer's theorem is useful because it: | lets you verify whether a proposed function is a legitimate kernel, without constructing the feature mapping explicitly | claims it "lets you compute the explicit feature mapping for any kernel" — the opposite; it sidesteps needing the mapping at all → `mercers-theorem` |
| A1 | apply | short-answer | 0.75 | Why is checking positive semi-definiteness of the kernel matrix a more practical verification method than constructing the feature mapping explicitly? | the kernel matrix check is a finite, directly computable test on actual data points; constructing the feature mapping may be impossible outright (e.g. for an infinite-dimensional feature space), so the matrix check is the only practical route *(required)* | — |
| E1 | explain | short-answer | 1.45 | Connect Mercer's theorem directly to `positive-definite-matrices` from the linear-algebra sweep. | the kernel matrix condition *is* the positive-semi-definiteness condition from that earlier concept, applied here to a matrix built from kernel evaluations rather than an arbitrary matrix — not an analogy, the same test *(required: states it as the identical condition, not a parallel one)* | — |
| T1 | transfer | short-answer | 1.95 | Why must a practitioner inventing a custom kernel (e.g. for comparing DNA sequences) verify Mercer's condition before using it in an SVM? | using a function that fails Mercer's condition means it doesn't correspond to any real dot product in any feature space, which can make the SVM's underlying optimization problem ill-posed or non-convex, with no guarantee of a stable or meaningful solution *(required: names the optimization-validity risk specifically)* | — |

| R3 | recall | short-answer | −0.75 | What matrix does Mercer's theorem's condition apply to? | the kernel (Gram) matrix — the matrix of K(xᵢ,xⱼ) values for a finite set of points | — |
| R4 | recall | short-answer | −0.7 | What property must the kernel matrix have for K to be a valid ("Mercer") kernel? | positive semi-definiteness, for every finite set of points | — |
| R5 | recall | mcq | −0.5 | If a proposed function fails Mercer's condition on some set of points, it: | is not a valid kernel — no feature mapping φ exists whose dot product it equals for that data | claims it "just means the feature space it corresponds to is unusually large" — failing the condition means no consistent feature space exists at all, not merely a large one → `mercers-theorem` |
| R6 | recall | short-answer | −0.6 | Fill in the blank: Mercer's theorem says K is a valid kernel if and only if its kernel matrix is ___ for every finite set of points. | positive semi-definite |
| R7 | recall | mcq | −0.4 | Mercer's theorem is best classified as a: | necessary and sufficient condition — an "if and only if" characterization of valid kernels | claims it's "only a sufficient, not necessary, condition" — the theorem is explicitly an iff statement → `mercers-theorem` |
| R8 | recall | short-answer | −0.45 | True or false: Mercer's theorem tells you what the implicit feature mapping φ actually looks like. | false — it only certifies that *some* valid φ exists (via the positive semi-definiteness test), without constructing or describing it | — |
| R9 | recall | mcq | −0.25 | Checking Mercer's condition requires: | evaluating K on the actual data points and checking the resulting matrix's eigenvalues (or another PSD test) | claims it requires "deriving the feature mapping φ symbolically first" — the entire point is that this check can be done without ever deriving φ → `mercers-theorem` |
| R10 | recall | short-answer | −0.15 | What earlier linear-algebra concept does "positive semi-definite" directly draw on? | positive (semi-)definite matrices — eigenvalues all non-negative, or equivalently xᵀMx ≥ 0 for all x | — |
| R11 | recall | mcq | −0.05 | A matrix is positive semi-definite if: | xᵀMx ≥ 0 for all vectors x | claims it means "all of the matrix's entries are non-negative" — PSD is a condition on the quadratic form xᵀMx, not on individual entries → `mercers-theorem` |
| R12 | recall | short-answer | 0.05 | Fill in the blank: Mercer's theorem is applied specifically to the ___ matrix, not to the raw data matrix. | kernel (Gram) |
| R13 | recall | mcq | 0.15 | Which of these is a practical reason to verify Mercer's condition before deploying a custom kernel? | using an invalid kernel can make the downstream optimization (e.g. an SVM's) ill-posed or non-convex | claims the only reason is "aesthetic — invalid kernels still work fine in practice" — an invalid kernel can genuinely break the optimization's guarantees, not just look inelegant → `mercers-theorem` |
| R14 | recall | short-answer | 0.2 | Does Mercer's theorem apply to a single pair of points, or to a finite collection of points at once? | a finite collection — the kernel matrix's positive semi-definiteness must hold across the whole set of pairwise evaluations, not just for any single pair | — |
| R15 | recall | mcq | 0.1 | The RBF kernel's status under Mercer's theorem is: | it is a valid Mercer kernel, corresponding to an infinite-dimensional feature space | claims "RBF fails Mercer's condition because its feature space is infinite" — infinite-dimensionality doesn't disqualify a kernel; RBF is a textbook example of a valid Mercer kernel → `mercers-theorem` |
| R16 | recall | short-answer | 0.0 | True or false: two different valid kernels can correspond to the same feature space via different feature mappings. | true — multiple distinct φ mappings can produce the same dot-product structure, so Mercer's condition doesn't pin down a unique φ | — |
| A2 | apply | short-answer | 0.85 | A researcher proposes K(x,y) = −x·y (the negative dot product). Is this a valid Mercer kernel? | no — its kernel matrix is the negation of the (positive semi-definite) linear kernel's matrix, which generally has negative eigenvalues, violating positive semi-definiteness | — |
| A3 | apply | short-answer | 0.8 | For a single data point x, is the 1×1 kernel matrix [K(x,x)] guaranteed to be positive semi-definite if K is a valid kernel? | yes — K(x,x) = φ(x)·φ(x) = ‖φ(x)‖² ≥ 0 always, so the 1×1 matrix trivially satisfies PSD | — |
| A4 | apply | short-answer | 0.9 | Why can't checking Mercer's condition on only 2 points ever be enough to certify K as a valid kernel in general? | the theorem requires the condition to hold for *every* finite set of points; passing the check on a small sample doesn't rule out a larger or differently-chosen set of points producing a matrix that fails the PSD test | — |
| A5 | apply | short-answer | 0.7 | Someone proposes using cosine similarity directly as a kernel. Would you expect it to be positive semi-definite in general? | it generally is, for the standard cosine similarity (normalized dot product) — since it's a rescaled dot product, it inherits a valid inner-product structure, though the specific PSD check should still be run for the actual data being used | — |
| A6 | apply | short-answer | 0.75 | If a custom string-comparison "kernel" for DNA sequences fails the PSD check, what practical consequence follows for an SVM trained with it? | the SVM's optimization problem loses its convexity guarantee, so training might not converge reliably to a well-defined global solution, and the resulting classifier's behavior becomes unpredictable | — |
| E2 | explain | short-answer | 1.25 | Why is testing positive semi-definiteness on real data points more practical than attempting to construct φ explicitly? | the PSD test only requires evaluating K at actual data points and checking a finite matrix's eigenvalues — a concrete, computable procedure; constructing φ explicitly may be impossible outright (infinite-dimensional cases) or require deep, kernel-specific mathematical derivation that a practitioner shouldn't need to redo for every custom kernel *(required)* | — |
| E3 | explain | short-answer | 1.3 | Why does Mercer's theorem being an "if and only if" statement matter more than a one-directional sufficient condition would? | an iff characterization means passing the PSD test is a complete guarantee — no valid kernel is ever wrongly rejected, and no invalid one is ever wrongly accepted; a merely sufficient condition would leave open the possibility of valid kernels failing the test for no good reason *(required)* | — |
| E4 | explain | short-answer | 1.35 | Why does a kernel corresponding to an infinite-dimensional feature space still pass a finite computational test (the PSD check on a finite kernel matrix)? | the PSD condition is checked on the finite matrix of pairwise K(xᵢ,xⱼ) values for the actual (finite) sample at hand, not on the infinite feature space itself — the theorem's guarantee is that this finite, computable test is exactly equivalent to the existence of *some* (possibly infinite-dimensional) consistent feature space *(required)* | — |
| E5 | explain | short-answer | 1.2 | Why does Mercer's theorem matter more for custom, hand-designed kernels than for well-established ones like RBF or polynomial? | established kernels have already been proven to satisfy Mercer's condition once, in general, so practitioners can rely on that prior verification; a newly invented kernel (e.g. for a specialized domain like DNA sequences) carries no such guarantee and must be checked before it can be trusted in an optimization that assumes a valid inner-product structure *(required)* | — |
| T2 | transfer | short-answer | 1.85 | How does Mercer's condition connect to the earlier concept of a covariance matrix always being positive semi-definite? | both are instances of the same underlying fact: any matrix built as an inner product (or expectation of products) of a set of vectors is automatically PSD — a covariance matrix is E[(X−μ)(X−μ)ᵀ], structurally a sum of outer products, just as a kernel matrix's entries are dot products φ(xᵢ)·φ(xⱼ); Mercer's theorem is this same PSD-from-inner-product-structure fact, applied to kernel evaluations instead of centered random vectors *(required: names the shared inner-product-implies-PSD structure)* | — |
| T3 | transfer | short-answer | 1.8 | Why is verifying Mercer's condition analogous to checking that a proposed distance function is a genuine metric before using it in a nearest-neighbor algorithm? | both checks exist for the same reason: an algorithm's correctness guarantees were derived assuming a specific mathematical structure (a valid inner product for kernels, the triangle inequality etc. for a metric); using a function that merely *looks* like the right kind of object without actually satisfying its defining properties can silently produce ill-defined or misleading results, so both fields require an explicit verification step before trusting a custom function *(required: names the shared "verify structural assumption before use" pattern)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.75…1.95.*

---

## Radial Basis Function (`rbf`)
*Prereq: Kernel · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | State the RBF (Gaussian) kernel formula. | K(x,y) = exp(−‖x−y‖²/(2σ²)) — a function of only the distance between x and y | — |
| R2 | recall | mcq | −0.45 | The RBF kernel's value is largest when: | x and y are identical (distance=0, giving K=1) | claims it's largest when "x and y are far apart" — backwards; the kernel decays with distance → `rbf` |
| A1 | apply | numeric | 0.1 | σ=1, ‖x−y‖²=2. Compute K(x,y). `[verified: 0.368]` | exp(−2/2)=exp(−1)≈0.368 | — |
| E1 | explain | short-answer | 0.8 | Explain the role of σ, connecting it to KNN's K parameter. | small σ makes influence drop off quickly with distance (very local, flexible, like small-K KNN — low bias, high variance); large σ extends influence far (more global/smooth, like large-K KNN — more bias, less variance) *(required: the explicit KNN parallel in both directions)* | — |
| T1 | transfer | short-answer | 1.3 | Why does RBF correspond to an infinite-dimensional feature space, unlike the polynomial kernel, and what does that imply for SVM flexibility? | the RBF kernel can be expanded as an infinite series of polynomial-like terms of increasing degree, corresponding to an infinite-dimensional feature space; an SVM using it can in principle fit an arbitrarily flexible boundary, though in practice σ and the margin/regularization tradeoff still constrain it to avoid overfitting *(required: names the infinite-series expansion, and the practical constraints that remain)* | — |

| R3 | recall | short-answer | −1.1 | What other name is the RBF kernel commonly known by? | the Gaussian kernel | — |
| R4 | recall | short-answer | −1.05 | What single quantity does the RBF kernel's value depend on? | the (squared) distance between x and y, ‖x−y‖² — nothing else about their individual positions | — |
| R5 | recall | mcq | −0.85 | The σ (or equivalently, the bandwidth/γ) parameter in the RBF kernel controls: | how quickly the kernel's influence decays with distance | claims it "controls the maximum possible value the kernel can output" — the maximum is always 1 (at zero distance) regardless of σ; σ controls the decay rate, not the ceiling → `rbf` |
| R6 | recall | short-answer | −0.95 | Fill in the blank: K(x,y) = exp(−‖x−y‖²/(2___²)). | σ |
| R7 | recall | mcq | −0.65 | As ‖x−y‖ → ∞, the RBF kernel's value approaches: | 0 | claims it "approaches 1, the same as at zero distance" — the kernel value strictly decays toward 0 as points get farther apart → `rbf` |
| R8 | recall | short-answer | −0.75 | True or false: the RBF kernel's output is always in the range (0, 1]. | true — exp of a non-positive exponent is always in (0,1], reaching exactly 1 only when x=y | — |
| R9 | recall | mcq | −0.5 | A small σ in the RBF kernel produces: | a very localized notion of similarity — only very nearby points are considered similar at all | claims small σ "makes distant points count as highly similar" — a small σ makes the exponential decay steeper, so influence drops off *faster*, not slower, with distance → `rbf` |
| R10 | recall | short-answer | −0.4 | What is the RBF kernel used for, most commonly, in the classifier catalogue? | as the kernel choice for SVM to fit nonlinear decision boundaries | — |
| R11 | recall | mcq | −0.3 | Is the RBF kernel a valid Mercer kernel? | yes — it satisfies Mercer's positive semi-definiteness condition | claims "no, it fails Mercer's condition because it corresponds to an infinite-dimensional space" — infinite dimensionality doesn't disqualify a kernel; RBF is a canonical valid Mercer kernel → `rbf` |
| R12 | recall | short-answer | −0.2 | What is the RBF kernel's value when x=y exactly (zero distance)? | exactly 1 | — |
| R13 | recall | mcq | −0.1 | Compared to the polynomial kernel, the RBF kernel's implicit feature space is: | infinite-dimensional, unlike the polynomial kernel's finite-dimensional space | claims RBF's feature space is "always lower-dimensional than the polynomial kernel's" — RBF's implicit space is infinite-dimensional, strictly larger than any finite polynomial-kernel space → `rbf` |
| R14 | recall | short-answer | 0.0 | Fill in the blank: γ, an alternative parameterization sometimes used instead of σ, is typically defined as ___ (in terms of σ). | 1/(2σ²) |
| R15 | recall | mcq | −0.05 | A very large σ in the RBF kernel makes an SVM's decision boundary tend toward: | a smoother, more global, less flexible boundary | claims large σ produces "an extremely jagged, highly local boundary" — that describes the effect of a *small* σ, not a large one → `rbf` |
| R16 | recall | short-answer | −0.15 | Is the RBF kernel's value ever negative? | no — it is always in (0,1], since it's an exponential of a real number, which is always positive | — |
| A2 | apply | numeric | 0.15 | σ=1, ‖x−y‖²=0. Compute K(x,y). `[verified: 1.0]` | exp(−0/2) = exp(0) = 1.0 | — |
| A3 | apply | numeric | 0.2 | σ=3, ‖x−y‖²=9. Compute K(x,y). `[verified: 0.607]` | exp(−9/(2·9)) = exp(−0.5) ≈ 0.607 | — |
| A4 | apply | numeric | 0.25 | σ=1, ‖x−y‖²=4. Compute K(x,y). `[verified: 0.135]` | exp(−4/2) = exp(−2) ≈ 0.135 | — |
| A5 | apply | short-answer | 0.3 | Comparing σ=0.5 and σ=5 for the same pair of moderately-distant points, which gives the higher kernel value? | σ=5 — a larger σ makes the exponential decay more slowly with distance, so the same distance yields a higher (less-decayed) kernel value | — |
| A6 | apply | short-answer | 0.35 | If two feature vectors are identical after being measured with sensor noise (nearly but not exactly equal), would you expect K(x,y) to be close to 1 or close to 0? | close to 1 — since ‖x−y‖² is very small (near-identical points), the exponent is close to 0, giving a kernel value close to its maximum of 1 | — |
| E2 | explain | short-answer | 0.65 | Why is the RBF kernel considered a similarity measure rather than a distance measure? | it transforms distance into a bounded, decreasing score (via the negative exponential), so larger raw distances produce *smaller* kernel values — the kernel behaves like "how alike are these points," the inverse relationship a distance measure has *(required)* | — |
| E3 | explain | short-answer | 0.85 | Why does the RBF kernel depend only on ‖x−y‖ rather than on x and y's absolute positions? | it is defined purely as a function of the difference (distance) between the two points, making it translation-invariant — shifting both x and y by the same amount leaves K(x,y) completely unchanged *(required)* | — |
| E4 | explain | short-answer | 0.9 | Why does σ act as an effective "smoothness" or "resolution" control on an RBF-kernel SVM's decision boundary? | σ sets how quickly similarity decays with distance, which directly determines how far a training point's influence reaches; small σ lets nearby points dominate locally (fine resolution, jagged boundary), large σ blends influence over a wide area (coarse resolution, smooth boundary) *(required)* | — |
| E5 | explain | short-answer | 0.75 | Why does the RBF kernel never output exactly 0, even for very distant points? | the exponential function exp(t) is strictly positive for any finite real t, so however large the (negative) exponent gets from a large distance, the result asymptotically approaches but never reaches exactly zero *(required)* | — |
| T2 | transfer | short-answer | 1.4 | How does the RBF kernel's σ parameter functionally parallel a Gaussian probability density's standard deviation? | in both cases, σ controls the spread/width of a bell-shaped exp(−(·)²/2σ²) curve — a small σ concentrates probability mass (or kernel influence) tightly near the center, a large σ spreads it out; the RBF kernel is literally an unnormalized Gaussian density evaluated at the distance between two points, so the parallel is exact, not just an analogy *(required: names the shared Gaussian functional form)* | — |
| T3 | transfer | short-answer | 1.35 | Why might choosing σ for an RBF-kernel SVM be understood as implicitly choosing a point on the bias-variance spectrum, connecting to `bias-variance-tradeoff`? | small σ produces a highly flexible, locally-sensitive boundary (low bias, high variance, similar to small-K KNN) while large σ produces a smoother, more constrained boundary (higher bias, lower variance, similar to large-K KNN) — σ is functioning as a complexity dial exactly like K does for KNN, cementing the parallel this concept's own E1 item draws explicitly *(required: explicit bias-variance framing tied to σ)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.1…1.4.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| kernel trick believed to still require explicit transformation | `kernel` |
| kernel-trick applicability believed limited to SVM alone | `kernel` |
| linear kernel wrongly treated as infinite-dimensional like RBF | `kernel` |
| kernel matrix confused with the raw feature/data matrix | `kernel` |
| Mercer's theorem believed to construct the feature map | `mercers-theorem` |
| positive semi-definiteness confused with all-entries-non-negative | `mercers-theorem` |
| Mercer's theorem treated as merely sufficient rather than iff | `mercers-theorem` |
| RBF kernel wrongly believed to fail Mercer's condition due to infinite dimensionality | `mercers-theorem` |
| RBF kernel's decay direction reversed | `rbf` |
| RBF σ's effect on locality (small vs. large σ) reversed | `rbf` |
| RBF kernel's output range/ceiling misunderstood | `rbf` |

**Cluster total: 90 items across 3 concepts (30 each).** All numeric claims verified, including the
worked RBF values at (σ=1,d²=0)→1.0, (σ=3,d²=9)→0.607, (σ=1,d²=4)→0.135, and the polynomial-kernel
evaluation (x·y+1)²=36 for x·y=5.
