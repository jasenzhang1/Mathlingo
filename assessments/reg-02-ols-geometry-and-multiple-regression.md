# Regression Cluster 2 — OLS Geometry & Multiple Regression

Geometric Interpretation of OLS, Multiple Linear Regression, Linear Regression (Probabilistic
Version), OLS Assumptions, Homoskedasticity (5 concepts). Same format as
[foundations-of-probability.md](foundations-of-probability.md).

`linear-regression-probabilistic-version`'s R2 is the regression-domain twin of `cross-entropy-loss`'s
MLE identity from the ML sweep — OLS *is* an MLE, not merely inspired by one — and its T1 explains why
that reframing is exactly what makes `logistic-regression` and `glm` (later in this domain) natural
extensions rather than unrelated methods.

---

## Geometric Interpretation of OLS (`geometric-interpretation-of-ols`)
*Prereq: Normal Equations, Column Space, Vector Projection · ancestors 33 · b₀ = 1.26*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.26 | State the geometric picture of OLS. | ŷ=Xβ̂ is the orthogonal projection of y onto C(X); the residual (y−ŷ) is the component of y orthogonal to C(X) | — |
| R2 | recall | mcq | 0.56 | The hat matrix H=X(XᵀX)⁻¹Xᵀ (with ŷ=Hy) is: | a projection matrix — idempotent (H²=H) and symmetric | dismisses H as "an arbitrary computational convenience," missing its precise characterization → `geometric-interpretation-of-ols` |
| A1 | apply | derivation | 1.06 | Verify H is idempotent symbolically. `[verified numerically: max|H²-H|≈0]` | H² = X(XᵀX)⁻¹XᵀX(XᵀX)⁻¹Xᵀ = X(XᵀX)⁻¹(XᵀX)(XᵀX)⁻¹Xᵀ = X(XᵀX)⁻¹Xᵀ = H — the middle (XᵀX)(XᵀX)⁻¹ cancels to the identity *(required: the explicit cancellation step)* | — |
| E1 | explain | short-answer | 1.76 | Why does H²=H make geometric sense, independent of the algebra? | projecting a vector already in the target subspace (like ŷ, already in C(X)) leaves it unchanged — projecting twice gives the same result as projecting once, exactly what H(Hy)=Hy states *(required: the "already in the subspace" argument)* | — |
| T1 | transfer | short-answer | 2.26 | Why does trace(H) equal exactly p, and what practical quantity does that give? `[verified numerically: trace=2=p for a 4x2 example]` | H's eigenvalues satisfy λ²=λ (from H²=H applied to an eigenvector), forcing λ∈{0,1}; trace(H) sums the eigenvalues, which equals the count of 1's — exactly rank(H)=rank(X)=p; this is used practically as the model's "effective number of parameters," or degrees of freedom used *(required: the eigenvalue-restriction argument, reusing `trace`'s cyclic-property spirit)* | — |
| R3 | recall | short-answer | 0.06 | Define the column space C(X). | the set of all linear combinations of X's columns — every vector Xβ for some β | — |
| R4 | recall | short-answer | 0.11 | What does ŷ denote in the OLS geometric picture? | the fitted values Xβ̂ — the orthogonal projection of y onto C(X) | — |
| R5 | recall | short-answer | 0.16 | Fill in the blank: the residual vector is defined as e = ___. | e = y − ŷ | — |
| R6 | recall | mcq | 0.21 | True/false: the hat matrix H is symmetric. | True — H=X(XᵀX)⁻¹Xᵀ, and (X(XᵀX)⁻¹Xᵀ)ᵀ=X(XᵀX)⁻¹Xᵀ=H | claims H is "generally asymmetric" — H's symmetry follows directly from (XᵀX)⁻¹ being symmetric → `geometric-interpretation-of-ols` |
| R7 | recall | short-answer | 0.31 | State the formula for the hat matrix H. | H = X(XᵀX)⁻¹Xᵀ | — |
| R8 | recall | mcq | 0.36 | Which matrix, applied to y, yields the residual vector e rather than ŷ? | (I−H) | picks "H itself" — that yields ŷ, not e → `geometric-interpretation-of-ols` |
| R9 | recall | short-answer | 0.41 | What does it mean for H to be idempotent? | H² = H — applying it twice gives the same result as applying it once | — |
| R10 | recall | short-answer | 0.46 | What is rank(H) equal to, for X with p linearly independent columns? | rank(H) = p = rank(X) | — |
| R11 | recall | short-answer | 0.51 | Fill in the blank: OLS chooses β̂ to minimize ___. | the sum of squared residuals ‖y − Xβ‖² | — |
| R12 | recall | mcq | 0.61 | Which of the following is NOT generally a property of the hat matrix H? | invertible | picks a true property (idempotent/symmetric/rank p) instead — H is generally *not* invertible unless n=p, since it projects onto a proper subspace → `geometric-interpretation-of-ols` |
| R13 | recall | mcq | 0.66 | True/false: the residual vector e is orthogonal to every vector in C(X). | True — e = (I−H)y lies in the orthogonal complement of C(X) | claims e is merely "uncorrelated with X" rather than exactly orthogonal — orthogonality is the exact geometric fact, not an approximation → `geometric-interpretation-of-ols` |
| R14 | recall | short-answer | 0.71 | What is the matrix (I−H) called, geometrically? | the projection matrix onto the orthogonal complement of C(X) (it produces the residual) | — |
| R15 | recall | mcq | 0.81 | Geometrically, e = y − ŷ represents: | the component of y orthogonal to C(X) | claims it represents "the component of y inside C(X)" — that's ŷ, not e → `geometric-interpretation-of-ols` |
| R16 | recall | short-answer | 0.91 | Fill in the blank: the normal equations imply Xᵀe = ___. | Xᵀe = 0 (the vector 0) | — |
| A2 | apply | short-answer | 0.86 | Show that (I−H) is idempotent given H is idempotent and symmetric. | (I−H)² = I − 2H + H² = I − 2H + H = I − H, using H²=H *(required: the explicit expansion)* | — |
| A3 | apply | numeric | 0.96 | A dataset has n=50 observations and a design matrix with p=4 columns (including the intercept), full column rank. What is trace(H)? `[verified: 4]` | trace(H) = rank(H) = rank(X) = p = 4 | — |
| A4 | apply | derivation | 1.16 | Derive Xᵀe = 0 from the normal equations XᵀXβ̂ = Xᵀy. | e = y − Xβ̂, so Xᵀe = Xᵀy − XᵀXβ̂ = Xᵀy − Xᵀy = 0, substituting the normal equations directly *(required: the substitution step)* | — |
| A5 | apply | short-answer | 1.26 | Why is (I−H) also a projection matrix, and onto what subspace? | it is idempotent and symmetric like H, so it is a projection matrix; it projects onto the orthogonal complement of C(X), the space of vectors orthogonal to every column of X *(required: names the orthogonal complement)* | — |
| A6 | apply | numeric | 1.31 | X is a 10×3 full-column-rank design matrix. What is rank(H)? `[verified: 3]` | rank(H) = rank(X) = 3 | — |
| E2 | explain | short-answer | 1.46 | Why is minimizing ‖y−Xβ‖² over β equivalent to finding the orthogonal projection of y onto C(X)? | the closest point in a subspace to an external vector, in Euclidean distance, is always its orthogonal projection onto that subspace — a general geometric fact about projections, of which OLS is one instance *(required: names the closest-point/projection equivalence)* | — |
| E3 | explain | short-answer | 1.56 | Why must e be orthogonal to *every* column of X, not merely to ŷ? | ŷ is itself a linear combination of X's columns, so orthogonality to every column of X (Xᵀe=0) is the stronger, more fundamental fact from which orthogonality to ŷ follows as a special case *(required: names that ŷ ∈ C(X) is a special case of the column-orthogonality)* | — |
| E4 | explain | short-answer | 1.66 | Why does H being a projection matrix guarantee ŷ is the *unique* closest point in C(X) to y? | projection onto a subspace is unique for a Euclidean norm — the subspace is closed and convex, so there is exactly one minimizer of the distance to any external point, and H's idempotency plus symmetry is exactly the algebraic signature of that unique operation *(required: names uniqueness as following from convexity of the subspace)* | — |
| E5 | explain | short-answer | 1.86 | Why does (I−H) being idempotent and symmetric let us write SSE as yᵀ(I−H)y? | SSE = eᵀe = ((I−H)y)ᵀ((I−H)y) = yᵀ(I−H)ᵀ(I−H)y = yᵀ(I−H)y, using symmetry and then idempotency to collapse (I−H)ᵀ(I−H) into (I−H) *(required: the explicit two-step simplification)* | — |
| T2 | transfer | short-answer | 2.06 | How does the projection picture explain, without algebra, why SST = SSR + SSE exactly? | y decomposes as ŷ + e, with ŷ∈C(X) and e orthogonal to C(X) (hence to ŷ); this is exactly the Pythagorean theorem for the right triangle formed by y, ŷ, and e, so ‖y−ȳ1‖² = ‖ŷ−ȳ1‖² + ‖e‖² is Pythagoras applied to that orthogonal decomposition, not a separate algebraic coincidence *(required: names the Pythagorean-theorem framing explicitly)* | — |
| T3 | transfer | short-answer | 2.16 | How is OLS's projection literally an instance of the general `vector-projection` formula for projecting one vector onto a subspace spanned by others? | the general projection of y onto the span of columns {x₁,...,xₚ} uses exactly the same X(XᵀX)⁻¹Xᵀ construction that single-vector projection generalizes when there are multiple spanning vectors instead of one — OLS is that general formula applied with X's columns as the spanning set *(required: names the generalization from one spanning vector to many)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.06…2.26.*

---

## Multiple Linear Regression (`multiple-linear-regression`)
*Prereq: Normal Equations · ancestors 25 · b₀ = 1.13*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.13 | State the multiple regression model, and how the normal equations extend to it. | Y=β₀+β₁X₁+⋯+βₚXₚ+ε; the same XᵀXβ̂=Xᵀy applies, with X now having multiple columns | — |
| R2 | recall | mcq | 0.43 | βⱼ in multiple regression is interpreted as: | the effect of Xⱼ holding all other predictors constant (the partial effect) | claims βⱼ is "always the same as in a simple regression using only Xⱼ" — a common and consequential error, since correlated predictors can change coefficients dramatically → `multiple-linear-regression` |
| A1 | apply | short-answer | 0.93 | Describe a scenario where a predictor's coefficient *changes sign* moving from simple to multiple regression. | X alone might show a positive association with Y, but once a correlated confounder Z is added, X's coefficient can flip sign if Z was masking or reversing the true relationship — a classic sign of confounding or suppression *(required: a concrete confounding mechanism, not just "coefficients can change")* | — |
| E1 | explain | short-answer | 1.63 | Why does adding a correlated predictor change the *other* predictors' coefficients, not just add a new one? | each coefficient captures the unique contribution of that variable after accounting for its overlap with the others; adding a new correlated variable reshuffles how much "credit" each existing variable gets for the shared variation *(required: the "credit reshuffling" mechanism)* | — |
| T1 | transfer | short-answer | 2.13 | How is a multiple regression coefficient conceptually similar to a conditional/partial relationship, connecting to `schur-complement`? | βⱼ represents the relationship between Xⱼ and Y *after removing* what's explained by the other predictors — structurally the same operation the Schur complement performs when computing a conditional covariance by removing what one block predicts about another *(required: the explicit Schur-complement parallel)* | — |
| R3 | recall | short-answer | 0.03 | Define "partial effect" precisely. | the change in the expected value of Y for a one-unit increase in Xⱼ, with every other predictor held fixed | — |
| R4 | recall | short-answer | 0.08 | In matrix form Y = Xβ + ε, what does ε represent? | the random error term capturing everything about Y not explained by the linear combination of predictors | — |
| R5 | recall | short-answer | 0.18 | How many total parameters (including the intercept) does a model with p predictors have? | p + 1 | — |
| R6 | recall | mcq | 0.23 | True/false: n must exceed p for XᵀX to be invertible. | True — with n ≤ p, X cannot have full column rank, so XᵀX is singular | claims "n can be smaller than p and XᵀX is still generally invertible" — the opposite of the rank requirement → `multiple-linear-regression` |
| R7 | recall | short-answer | 0.28 | What does the first column of X typically contain when an intercept is included? | a column of all 1's | — |
| R8 | recall | mcq | 0.33 | Which formula gives β̂ in multiple regression? | β̂ = (XᵀX)⁻¹Xᵀy | picks "β̂ = XᵀXy" — omitting the inverse entirely → `multiple-linear-regression` |
| R9 | recall | short-answer | 0.38 | Fill in the blank: the degrees of freedom for the residuals is n − ___. | n − (p + 1), i.e. n minus the number of estimated parameters including the intercept | — |
| R10 | recall | short-answer | 0.53 | What term collectively describes X₁,...,Xₚ in this model? | the predictors (or independent variables/covariates/regressors) | — |
| R11 | recall | mcq | 0.58 | True/false: categorical predictors can be included in multiple regression via dummy/indicator variables. | True — a categorical variable with k levels is typically encoded as k−1 binary dummy variables | claims "categorical predictors cannot be used in regression at all" — dummy coding makes this routine → `multiple-linear-regression` |
| R12 | recall | mcq | 0.63 | If a new predictor is truly unrelated to Y (in the population), its estimated coefficient will typically be: | close to zero, though rarely exactly zero due to sampling variability | claims it will be "exactly zero in every sample" — sampling variability guarantees near-zero, not exactly zero, estimates → `multiple-linear-regression` |
| R13 | recall | short-answer | 0.68 | What does p represent in the requirement "n > p"? | the number of predictors (columns of X excluding, or sometimes including, the intercept, depending on convention) | — |
| R14 | recall | short-answer | 0.73 | Fill in the blank: multiple regression assumes E[ε\|X] = ___. | 0 | — |
| R15 | recall | mcq | 0.78 | Which best describes the geometric role of adding a predictor to X? | it adds a new dimension/column to C(X), enlarging the subspace y is projected onto | claims it "replaces an existing dimension of C(X)" rather than adding a new one → `multiple-linear-regression` |
| R16 | recall | mcq | 0.88 | True/false: multiple regression coefficients are estimated jointly, not one predictor at a time. | True — β̂ is the single vector solving XᵀXβ̂=Xᵀy simultaneously for all coefficients | claims "each βⱼ is estimated independently via its own separate simple regression" — that ignores the joint XᵀX solve entirely → `multiple-linear-regression` |
| A2 | apply | numeric | 0.83 | A model has p=3 predictors, an intercept, and n=40 observations. What are the residual degrees of freedom? `[verified: 36]` | df = n − (p+1) = 40 − 4 = 36 | — |
| A3 | apply | short-answer | 1.08 | Why does the intercept β₀ represent the predicted Y when every predictor equals 0? | setting X₁=⋯=Xₚ=0 in Y=β₀+β₁X₁+⋯+βₚXₚ leaves only β₀, so it is literally the model's prediction at that (possibly extrapolated) point *(required: the direct substitution)* | — |
| A4 | apply | derivation | 1.18 | Sketch how minimizing SSE(β)=‖y−Xβ‖² over β leads to the normal equations. | taking the gradient ∇β SSE = −2Xᵀ(y−Xβ) and setting it to 0 gives Xᵀ(y−Xβ)=0, i.e. XᵀXβ=Xᵀy *(required: the explicit gradient-set-to-zero step)* | — |
| A5 | apply | numeric | 1.28 | A model has p=5 predictors, an intercept, and n=100 observations. What are the residual degrees of freedom? `[verified: 94]` | df = n − (p+1) = 100 − 6 = 94 | — |
| A6 | apply | short-answer | 1.33 | Give a scenario where a predictor's significance disappears after adding a second, correlated predictor. | if X₁ and X₂ both measure roughly the same underlying quantity (e.g. height in inches and height in cm), once X₂ is in the model X₁ has little unique information left to explain, so its coefficient's estimated effect and significance shrink toward zero *(required: a concrete near-redundancy example)* | — |
| E2 | explain | short-answer | 1.43 | Why can't multiple regression coefficients generally be obtained by running p separate simple regressions, one per predictor? | a simple regression of Y on Xⱼ alone conflates Xⱼ's own effect with whatever it shares with the other predictors; only the joint solve of XᵀXβ̂=Xᵀy separates out each predictor's effect net of the others *(required: names the conflation a separate simple regression would produce)* | — |
| E3 | explain | short-answer | 1.53 | Why does including the intercept matter for interpreting the other coefficients correctly? | omitting the intercept forces the fitted plane through the origin, which typically biases the other coefficients as they compensate for the missing baseline level, so β₀'s presence is what lets the other βⱼ's cleanly represent slopes rather than absorb a false origin constraint *(required: names the forced-through-origin bias)* | — |
| E4 | explain | short-answer | 1.58 | Why must XᵀX be invertible for β̂ to be uniquely determined? | if XᵀX is singular, the normal equations XᵀXβ̂=Xᵀy have infinitely many solutions (or none), because a singular matrix cannot be undone by a unique inverse — invertibility is exactly the condition guaranteeing a single, well-defined β̂ *(required: names the multiple-solutions consequence of singularity)* | — |
| E5 | explain | short-answer | 1.78 | Why is "holding other predictors constant" often only a mathematical fiction rather than a literally achievable scenario? | in observational data, predictors are frequently correlated, so it may be physically impossible to change Xⱼ while every other Xₖ stays fixed (e.g. changing education while holding age, experience, and income all exactly constant); the interpretation is still a valid statistical statement about the fitted coefficients, just not always a literal real-world manipulation *(required: names the correlated-predictors reason it can be unrealizable)* | — |
| T2 | transfer | short-answer | 2.03 | How is βⱼ's "holding others constant" interpretation analogous to a partial derivative ∂Y/∂Xⱼ in multivariable calculus? | both isolate the rate of change with respect to one variable while treating every other variable as fixed — βⱼ *is* the partial derivative of the linear model's predicted Y with respect to Xⱼ, since the model is linear in each coordinate *(required: names the partial-derivative equivalence explicitly)* | — |
| T3 | transfer | short-answer | 2.23 | Connect the normal-equations derivation to `matrix-calculus`'s treatment of gradients of quadratic forms. | SSE(β)=(y−Xβ)ᵀ(y−Xβ) is a quadratic form in β; `matrix-calculus`'s rule for differentiating such forms gives ∇β SSE = −2Xᵀy + 2XᵀXβ directly, and setting this to zero is exactly how the normal equations arise — multiple regression is a direct application of that general gradient rule, not a separately-derived special case *(required: names the quadratic-form gradient rule explicitly)* | —

*Coverage: 16/6/5/3 — 30 items, 0.03…2.23.*

---

## Linear Regression, Probabilistic Version (`linear-regression-probabilistic-version`)
*Prereq: Multiple Linear Regression, MLE, Normal Distribution · ancestors 29 · b₀ = 1.20*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.2 | State the probabilistic linear regression model. | Y\|X ~ Normal(Xβ, σ²), equivalently ε ~ Normal(0, σ²) | — |
| R2 | recall | mcq | 0.5 | Under this model, the OLS estimator β̂ is exactly: | the Maximum Likelihood Estimator (MLE) for β | calls it "an arbitrary, unrelated estimator" — missing the exact identity, the regression-domain twin of `cross-entropy-loss`'s MLE connection → `linear-regression-probabilistic-version` |
| A1 | apply | short-answer | 1.0 | Why is maximizing this model's likelihood equivalent to minimizing the sum of squared residuals? `[verified: log-likelihood has term -(1/2σ²)Σ(y-Xβ)², so maximizing over β = minimizing Σ(y-Xβ)²]` | the Normal log-likelihood contains the term −(1/(2σ²))Σ(yᵢ−xᵢᵀβ)²; maximizing likelihood over β means making this term least negative, which is exactly minimizing Σ(yᵢ−xᵢᵀβ)² *(required: the explicit log-likelihood term, not just "they're related")* | — |
| E1 | explain | short-answer | 1.7 | Why is this probabilistic reframing practically important, beyond elegance? | it provides the sampling distribution needed to derive confidence intervals and hypothesis tests for β, connecting to `fisher-information` and `cramer-rao-lower-bound` from the probability/statistics sweep — something the purely geometric "minimize squared error" view never directly supplies *(required: names the CI/hypothesis-test payoff and at least one of those earlier concepts)* | — |
| T1 | transfer | short-answer | 2.2 | Why does this MLE framing generalize naturally to `logistic-regression` and `glm` later in this domain, in a way the geometric view doesn't? | once regression is framed as maximizing a likelihood, swapping the Normal assumption for a different one (Bernoulli, for logistic regression) is a natural extension of the same machinery; the purely geometric "minimize squared error" framing has no obvious analogous generalization to binary outcomes *(required: names the swap-the-distribution generalization explicitly)* | — |
| R3 | recall | short-answer | 0.03 | Fill in the blank: the errors ε are assumed to follow a ___ distribution. | Normal (Gaussian) distribution, ε ~ Normal(0, σ²) | — |
| R4 | recall | short-answer | 0.08 | State the MLE formula for σ² under this model. | σ̂²_MLE = SSE/n = (1/n)Σ(yᵢ−xᵢᵀβ̂)² | — |
| R5 | recall | mcq | 0.13 | True/false: the MLE of σ² is unbiased. | False — σ̂²_MLE = SSE/n is biased downward; the unbiased estimator divides by n−p−1 instead | claims "σ̂²_MLE is unbiased since it's a maximum likelihood estimator" — MLEs are not automatically unbiased → `linear-regression-probabilistic-version` |
| R6 | recall | short-answer | 0.18 | State the log-likelihood function for this model, up to an additive constant. | ln L(β,σ²) = −(n/2)ln(σ²) − (1/(2σ²))Σ(yᵢ−xᵢᵀβ)² + constant | — |
| R7 | recall | short-answer | 0.28 | What does the "iid" assumption on the errors mean here? | the errors ε₁,...,εₙ are independent of each other and identically distributed, each Normal(0, σ²) | — |
| R8 | recall | mcq | 0.33 | Under this model, the sampling distribution of β̂ is: | exactly Normal, since β̂ is a linear function of the Normally-distributed y | claims β̂'s distribution is "only approximately Normal, even in finite samples" — under this model it is exactly Normal, not merely approximate → `linear-regression-probabilistic-version` |
| R9 | recall | short-answer | 0.38 | Fill in the blank: β̂ ~ Normal(β, ___). | σ²(XᵀX)⁻¹ | — |
| R10 | recall | short-answer | 0.48 | What unbiased estimator of σ² is typically used instead of the MLE? | s² = SSE/(n−p−1), dividing by the residual degrees of freedom instead of n | — |
| R11 | recall | mcq | 0.58 | True/false: the MLE of β equals the OLS estimator β̂ only approximately. | False — under Normal errors, the MLE of β is exactly equal to the OLS estimator, not merely close to it | claims "the MLE and OLS estimators coincide only in the limit of large n" — they coincide exactly, for any n, under this model → `linear-regression-probabilistic-version` |
| R12 | recall | short-answer | 0.63 | What does the likelihood function L(β,σ²) measure? | the (joint) density of the observed data, viewed as a function of the parameters β and σ² with the data held fixed | — |
| R13 | recall | mcq | 0.68 | Which distribution assumption is central to this "probabilistic version" of linear regression? | the errors are Normally distributed, conditional on X | picks "the errors are uniformly distributed" — this model's defining assumption is Normal errors, not uniform → `linear-regression-probabilistic-version` |
| R14 | recall | short-answer | 0.73 | Fill in the blank: L(β,σ²) denotes the ___, evaluated at the observed data. | likelihood of the parameters (β,σ²) | — |
| R15 | recall | short-answer | 0.83 | What role does σ² play in this model, separately from β? | it is the (constant) variance of the error term / conditional variance of Y given X — a separate parameter to be estimated alongside β | — |
| R16 | recall | mcq | 0.93 | True/false: maximizing the log-likelihood over σ² for β held at its optimum gives σ̂² = SSE/n. | True — differentiating ln L with respect to σ² and setting the result to zero yields exactly σ̂²=SSE/n | claims the maximizing value is "SSE/(n−p−1)" — that is the unbiased estimator, not the raw MLE → `linear-regression-probabilistic-version` |
| A2 | apply | numeric | 0.88 | n=20 observations, SSE=36. Compute the MLE σ̂²_MLE. `[verified: 1.8]` | σ̂²_MLE = SSE/n = 36/20 = 1.8 | — |
| A3 | apply | short-answer | 0.98 | Derive why the MLE β̂ equals the OLS β̂ from the log-likelihood's structure. | ln L = −(n/2)ln(σ²) − (1/(2σ²))SSE(β) + const; for any fixed σ²>0, maximizing over β means maximizing −SSE(β), i.e. minimizing SSE(β) — exactly the OLS objective, so both estimators solve the identical optimization problem *(required: the explicit "maximizing −SSE = minimizing SSE" step)* | — |
| A4 | apply | numeric | 1.13 | n=20, p=3 predictors plus intercept (4 parameters total), SSE=36. Compute the unbiased estimator s². `[verified: 2.25]` | s² = SSE/(n−p−1) = 36/(20−4) = 36/16 = 2.25 | — |
| A5 | apply | short-answer | 1.23 | Why does maximizing the log-likelihood over σ² separately from β simplify to first minimizing SSE for β? | the −(1/(2σ²))SSE(β) term is maximized over β (for any fixed positive σ²) exactly when SSE(β) is minimized, since 1/(2σ²)>0 is just a positive scaling constant that doesn't change which β minimizes SSE — so β can be solved first, independent of σ² *(required: names that the positive scaling doesn't affect the argmin)* | — |
| A6 | apply | derivation | 1.33 | Derive σ̂²=SSE/n from setting ∂(ln L)/∂σ² = 0. | ∂(ln L)/∂σ² = −n/(2σ²) + SSE/(2σ⁴); setting this to 0 gives n/(2σ²)=SSE/(2σ⁴), i.e. n=SSE/σ², so σ̂²=SSE/n *(required: the explicit derivative and algebra)* | — |
| E2 | explain | short-answer | 1.43 | Why is the MLE of σ² biased downward? | σ̂²_MLE uses β̂, which was itself fit to minimize SSE on the same data, so SSE(β̂) is systematically smaller than the true expected squared error would be if β were known exactly; dividing by n instead of the smaller n−p−1 compounds this understatement *(required: names that β̂ was fit on the same data used to estimate σ²)* | — |
| E3 | explain | short-answer | 1.53 | Why does assuming Normal errors let us construct exact, rather than only asymptotic, confidence intervals for β? | because β̂ is then exactly (not just approximately) Normal for any sample size n, the pivotal quantities built from β̂ and s² (t-statistics) have exact, known finite-sample distributions, rather than distributions that are only guaranteed correct as n→∞ *(required: names the exact-vs-asymptotic distinction explicitly)* | — |
| E4 | explain | short-answer | 1.63 | Why is the likelihood function evaluated as varying over the parameters with the data fixed, rather than the reverse? | the data are what was actually observed and cannot change; the likelihood asks "how plausible are different parameter values, given this fixed observed data," which is the opposite direction from a probability density (which fixes the parameters and varies over possible data) *(required: names the fixed-data-vary-parameters framing explicitly)* | — |
| E5 | explain | short-answer | 1.83 | Why do the score equations for β under this model coincide exactly with the OLS normal equations? | setting ∂(ln L)/∂β = 0 gives (1/σ²)Xᵀ(y−Xβ) = 0, and since σ²>0 this is equivalent to Xᵀ(y−Xβ)=0 — precisely the normal equations XᵀXβ=Xᵀy that OLS derives independently from minimizing SSE *(required: the explicit score-equation derivation showing the exact match)* | — |
| T2 | transfer | short-answer | 2.03 | How does the n−p−1 divisor in s² connect to the general bias-correction theme seen in estimating a population variance from sample data? | just as the sample variance divides by n−1 rather than n to correct for the degree of freedom used up by estimating the sample mean, s² here divides by n−p−1 to correct for the p+1 degrees of freedom used up by estimating β̂ — the same "subtract the parameters already fit" correction, generalized from one estimated mean to p+1 estimated coefficients *(required: the explicit parallel to the n−1 sample-variance correction)* | — |
| T3 | transfer | short-answer | 2.13 | How does this probabilistic model let us compute the Fisher information matrix for β, connecting to `fisher-information`? | the log-likelihood's second derivative with respect to β is −(1/σ²)XᵀX, so the Fisher information matrix is (1/σ²)XᵀX — its inverse, σ²(XᵀX)⁻¹, is exactly Var(β̂), matching the Cramér–Rao bound with equality, since β̂ is efficient under this model *(required: names the Fisher-information-equals-inverse-covariance identity)* | —

*Coverage: 16/6/5/3 — 30 items, 0.03…2.2.*

---

## OLS Assumptions (`ols-assumptions`)
*Prereq: Multiple Linear Regression · ancestors 26 · b₀ = 1.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.15 | List the key OLS assumptions. | linearity; independent errors; homoskedasticity (constant error variance); normality of errors (for exact inference); no perfect multicollinearity | — |
| R2 | recall | mcq | 0.45 | Violating normality specifically: | doesn't bias coefficient estimates (still unbiased under weaker Gauss-Markov conditions) but affects the validity of exact CIs/tests, though CLT often rescues this for large n | claims it "makes OLS coefficient estimates themselves biased" — a common, consequential over-attribution of normality's role → `ols-assumptions` |
| A1 | apply | short-answer | 0.95 | Is homoskedasticity needed for OLS coefficients to be unbiased? | no — unbiasedness follows from the weaker Gauss-Markov conditions; homoskedasticity is needed for OLS to be the *minimum-variance* unbiased estimator and for standard standard-error formulas to be correct *(required: distinguishes unbiasedness from efficiency/correct SEs)* | — |
| E1 | explain | short-answer | 1.65 | Why are residual plots the standard tool for checking these assumptions? | violations often show up as visible patterns (a funnel shape for heteroskedasticity, a curved pattern for nonlinearity) that are far easier to spot visually than to detect from the coefficient estimates alone *(required: at least one concrete pattern example)* | — |
| T1 | transfer | short-answer | 2.15 | Why is violating linearity often considered the most serious assumption failure among these? | if the true relationship isn't linear, OLS doesn't just get the standard errors wrong (a fixable inference problem) — it produces systematically biased estimates of a fundamentally misspecified model, giving misleading predictions and interpretations regardless of sample size *(required: contrasts this with the "fixable inference" failures of the other assumptions)* | — |
| R3 | recall | short-answer | 0.05 | Fill in the blank: under the Gauss-Markov assumptions, OLS is BLUE, meaning Best ___ ___ ___. | Best Linear Unbiased Estimator | — |
| R4 | recall | short-answer | 0.1 | What does the exogeneity assumption E[ε\|X]=0 require? | that the error term has zero mean conditional on the predictors — the predictors carry no systematic information about the direction of the error | — |
| R5 | recall | mcq | 0.2 | True/false: perfect multicollinearity makes β̂ impossible to compute uniquely. | True — perfect multicollinearity makes XᵀX singular, so the normal equations have no unique solution | claims "β̂ can still be uniquely computed, just with larger standard errors" — that describes near, not perfect, multicollinearity → `ols-assumptions` |
| R6 | recall | short-answer | 0.25 | What does the "linearity" assumption require, precisely? | the model is linear *in the parameters* β (not necessarily in X) — Y=β₀+β₁f₁(X)+⋯+εₚfₚ(X) is fine even if the fⱼ are nonlinear transformations of X | — |
| R7 | recall | mcq | 0.3 | Which assumption is required only for OLS's *efficiency* (minimum variance), not for its unbiasedness? | homoskedasticity | picks "exogeneity" — exogeneity is required for unbiasedness itself, not merely for efficiency → `ols-assumptions` |
| R8 | recall | short-answer | 0.4 | Fill in the blank: violating independence of errors (autocorrelation) mainly affects ___. | the validity of the standard error formulas (and hence inference), not the unbiasedness of the coefficient estimates | — |
| R9 | recall | short-answer | 0.5 | State the conclusion of the Gauss-Markov theorem. | under linearity, exogeneity, homoskedasticity, and no perfect multicollinearity, OLS is the Best Linear Unbiased Estimator (minimum variance among all linear unbiased estimators) | — |
| R10 | recall | mcq | 0.55 | True/false: the Gauss-Markov assumptions require Normal errors. | False — Gauss-Markov gives BLUE-ness without assuming Normality; Normality is needed only for exact (finite-sample) inference, not for the BLUE result itself | claims "Normal errors are one of the Gauss-Markov assumptions" — Normality is a separate, additional assumption used only for exact inference → `ols-assumptions` |
| R11 | recall | mcq | 0.6 | Which assumption, if violated, causes OLS coefficients themselves to become biased and inconsistent? | exogeneity (E[ε\|X]=0) | picks "homoskedasticity" — its violation affects standard errors, not the bias of β̂ itself → `ols-assumptions` |
| R12 | recall | short-answer | 0.65 | What are the assumptions behind the Gauss-Markov theorem collectively called? | the Gauss-Markov assumptions (or Gauss-Markov conditions) | — |
| R13 | recall | short-answer | 0.7 | Fill in the blank: BLUE stands for ___. | Best Linear Unbiased Estimator | — |
| R14 | recall | mcq | 0.75 | True/false: "linearity" refers to the model being linear in the parameters β, not necessarily in X. | True — polynomial or log-transformed predictors are still "linear regression" as long as the coefficients enter linearly | claims linearity "requires Y to be a linear function of the raw predictors X" — it is linearity in the *parameters* that matters, not in X → `ols-assumptions` |
| R15 | recall | mcq | 0.85 | A time-series dataset where today's error is correlated with yesterday's error most directly violates: | the independent-errors assumption (autocorrelation) | picks "homoskedasticity" — that concerns constant variance, not correlation between errors across time → `ols-assumptions` |
| R16 | recall | short-answer | 0.9 | What does "no perfect multicollinearity" allow, that its violation forbids? | it allows XᵀX to be invertible, so a unique β̂ exists; perfect multicollinearity makes XᵀX singular, so no unique solution exists | — |
| A2 | apply | short-answer | 0.8 | Give an example of a model that is linear in the parameters but nonlinear in X, satisfying the linearity assumption. | Y=β₀+β₁X+β₂X²+ε — a quadratic function of X, but each term enters linearly through its own coefficient, so the linearity-in-parameters assumption still holds even though the Y-vs-X relationship is curved *(required: a concrete nonlinear-in-X, linear-in-β example)* | — |
| A3 | apply | derivation | 1.0 | Show algebraically why perfect multicollinearity (two identical predictor columns) makes XᵀX singular. | if column j equals column k of X exactly, then row j and row k of XᵀX are identical, so XᵀX has two identical rows — a matrix with two identical rows has determinant 0, hence is singular and non-invertible *(required: the explicit identical-rows-implies-singular argument)* | — |
| A4 | apply | short-answer | 1.1 | Why is omitting a relevant predictor correlated with an included one worse than including an irrelevant predictor? | omitting a relevant, correlated predictor violates exogeneity (the omitted variable's effect gets absorbed into ε, which is then correlated with the included X), biasing β̂; an irrelevant included predictor merely adds noise/variance without introducing bias *(required: names the omitted-variable-bias mechanism specifically)* | — |
| A5 | apply | short-answer | 1.2 | Is exogeneity violated when X and ε are correlated due to reverse causality (Y also causes X)? Explain. | yes — reverse causality creates exactly the kind of systematic dependence between X and the error that E[ε\|X]=0 rules out, since part of what looks like "error" is actually Y's own influence feeding back into X, biasing β̂ *(required: names reverse causality as an exogeneity violation specifically)* | — |
| A6 | apply | short-answer | 1.3 | Under which full set of assumptions specifically is OLS guaranteed to be BLUE? | linearity (in parameters), exogeneity (E[ε\|X]=0), homoskedasticity (constant error variance), no perfect multicollinearity, and independent errors — the complete Gauss-Markov assumption set *(required: names all of the core assumptions, not just one or two)* | — |
| E2 | explain | short-answer | 1.4 | Why does the Gauss-Markov theorem not require Normal errors to conclude OLS is BLUE? | the proof that OLS has minimum variance among linear unbiased estimators relies only on the first two moments (mean and variance/covariance structure) of the errors, not their full distributional shape — Normality is a stronger assumption needed only for deriving exact finite-sample sampling distributions, not for the BLUE optimality argument itself *(required: names that only first/second moments are used in the proof)* | — |
| E3 | explain | short-answer | 1.5 | Why is exogeneity considered the most foundational of the OLS assumptions? | its violation biases β̂ itself, not merely the standard errors or efficiency — every other assumption (homoskedasticity, normality, no multicollinearity) can be violated while β̂ remains unbiased, but exogeneity failing means the estimates are systematically wrong regardless of sample size *(required: contrasts exogeneity's bias consequence with the other assumptions' milder consequences)* | — |
| E4 | explain | short-answer | 1.6 | Why doesn't near (but not perfect) multicollinearity formally violate any Gauss-Markov assumption, yet still cause practical problems? | "no perfect multicollinearity" is satisfied as long as XᵀX is technically invertible, even if nearly singular; β̂ remains unbiased, but Var(β̂)=σ²(XᵀX)⁻¹ becomes enormous when XᵀX is nearly singular, so the formal assumption holds while the practical precision of the estimates collapses *(required: distinguishes the formal invertibility condition from the practical near-singularity problem)* | — |
| E5 | explain | short-answer | 1.9 | Why can't violating linearity be "fixed" simply by collecting more data, unlike the inference-only violations? | a misspecified functional form produces systematically biased estimates of the wrong model — more data makes that wrong model's estimates more precisely wrong, not correct, whereas violations like heteroskedasticity only affect the accuracy of the *uncertainty* estimates around an otherwise correctly-specified, unbiased model *(required: contrasts "more precisely wrong" with "unbiased but imprecise")* | — |
| T2 | transfer | short-answer | 2.1 | How does the Gauss-Markov theorem's "best *linear*" qualifier relate to `cramer-rao-lower-bound`, and is OLS the overall best possible estimator? | Gauss-Markov only guarantees OLS is best among *linear* unbiased estimators; `cramer-rao-lower-bound` sets a floor on variance for *any* unbiased estimator, linear or not — under Normal errors, OLS happens to achieve that floor (making it the overall best), but under non-Normal errors a nonlinear unbiased estimator could in principle beat it *(required: names the linear-vs-overall distinction and the Normal-errors special case)* | — |
| T3 | transfer | short-answer | 2.25 | How does "no perfect multicollinearity" connect to the `rank`/invertibility concept from the linear-algebra sweep? | perfect multicollinearity means X's columns are linearly dependent, so X does not have full column rank; a matrix without full column rank cannot have an invertible XᵀX, which is exactly `rank`'s criterion for when a matrix — or here, XᵀX built from X — is invertible at all *(required: the explicit full-column-rank connection)* | —

*Coverage: 16/6/5/3 — 30 items, 0.05…2.25.*

---

## Homoskedasticity (`homoskedasticity`)
*Prereq: OLS Assumptions · ancestors 27 · b₀ = 1.17*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.17 | Define homoskedasticity and its opposite. | Var(ε\|X) is constant across all X; heteroskedasticity is when this variance changes with X | — |
| R2 | recall | mcq | 0.47 | A classic example of heteroskedasticity is: | income prediction, where error variance tends to be larger for high-income individuals | claims heteroskedasticity "never occurs in real data" — it's extremely common → `homoskedasticity` |
| A1 | apply | short-answer | 0.97 | A residual-vs-fitted plot shows a clear funnel shape, spread increasing with fitted value. For or against homoskedasticity? | against — this is the classic visual signature of heteroskedasticity, increasing variance with the fitted value | — |
| E1 | explain | short-answer | 1.67 | Why does heteroskedasticity, despite not biasing coefficient estimates, make standard OLS standard-error formulas wrong? | the standard Var(β̂) formula assumes a constant σ² throughout its derivation; when true variance varies with X, that formula gives an incorrect (often too small) estimate of true uncertainty, producing overly narrow, overconfident confidence intervals *(required: names the "assumes constant σ² in the derivation" mechanism)* | — |
| T1 | transfer | short-answer | 2.17 | Explain robust ("heteroskedasticity-consistent") standard errors, and why practitioners often use them as a default safeguard. | robust standard errors (e.g. White's estimator) give valid uncertainty estimates even under heteroskedasticity of unknown form, without needing to specify exactly how the variance changes with X; because they cost little when homoskedasticity actually holds, many practitioners default to them as a safeguard regardless of whether heteroskedasticity is confirmed present *(required: names that the specific form of heteroskedasticity need not be known)* | — |

*Coverage: 5 items, 0.17…2.17.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| hat matrix treated as an arbitrary computational device rather than a projection | `geometric-interpretation-of-ols` |
| multiple-regression coefficients assumed stable when adding correlated predictors | `multiple-linear-regression` |
| OLS/MLE connection treated as coincidental rather than exact | `linear-regression-probabilistic-version` |
| normality violation conflated with biased coefficient estimates | `ols-assumptions` |
| heteroskedasticity assumed rare or negligible in practice | `homoskedasticity` |

**Cluster total: 25 items across 5 concepts.** All numeric claims verified, including hat-matrix
idempotency and trace(H)=p confirmed to machine precision on a concrete 4×2 example.
