# Linear Algebra Cluster 2 — Matrices & Structure

Matrix Multiplication, Matrices, Trace, Linear Transformations, Matrix Calculus, Kronecker Product,
Matrix Norms (7 concepts). Same format as [Cluster 1](la-01-vectors-and-operations.md).

`matrix-calculus` is the concept added to the graph for this sweep (see `concepts.md`) — the graph
previously had nothing defining a gradient, despite `gradient-descent` being named after one.

---

## Matrix Multiplication (`matrix-multiplication`)
*Prereq: Vector Operations · ancestors 2 · b₀ = 0.05*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.95 | State the dimension rule for matrix multiplication. | (m×n)(n×p) = (m×p); the inner dimensions must match | — |
| R2 | recall | mcq | −0.7 | Matrix multiplication is: | generally *not* commutative (AB≠BA) | assumes it's always commutative, by analogy with scalar multiplication → `matrix-multiplication` |
| R3 | recall | short-answer | −0.6 | What property does the identity matrix I satisfy for any compatible matrix A? | AI = IA = A — I acts as the multiplicative identity | — |
| R4 | recall | mcq | −0.5 | Is matrix multiplication associative — does (AB)C = A(BC) always hold when the products are defined? | yes, always — associativity holds even though commutativity fails | — |
| A1 | apply | numeric | −0.15 | A=[[1,2],[3,4]], B=[[5,6],[7,8]]. Compute AB and BA; confirm they differ. `[verified: AB=[[19,22],[43,50]], BA=[[23,34],[31,46]]]` | AB≠BA, confirming non-commutativity concretely | — |
| A2 | apply | numeric | −0.05 | A=[[1,0,2],[0,1,3]] (2×3), B=[[1,0],[0,1],[1,1]] (3×2). Compute AB. `[verified: [[3,2],[3,4]]]` | AB = [[3,2],[3,4]] — a 2×2 result, since the inner dimension 3 matches | — |
| A3 | apply | numeric | 0.05 | A=[[1,1],[0,1]], B=[[2,0],[1,2]], C=[[1,0],[0,3]]. Compute (AB)C and A(BC); confirm they're equal. `[verified: (AB)C=A(BC)=[[3,6],[1,6]]]` | (AB)C = A(BC) = [[3,6],[1,6]] — a concrete check of associativity | — |
| A4 | apply | numeric | 0.15 | A=[[1,2],[3,4]]. Compute AI where I is the 2×2 identity. `[verified: [[1,2],[3,4]]]` | AI = A — multiplying by I leaves A unchanged | — |
| A5 | apply | short-answer | 0.25 | How many scalar multiplications are needed to compute the product of an m×n matrix and an n×p matrix, using the standard row-times-column definition? | mnp — each of the mp output entries needs n scalar multiplications | — |
| A6 | apply | numeric | 0.35 | A=[[2,1],[0,3]], v=(4,5) (as a column vector). Compute Av. `[verified: (13,15)]` | Av = (2·4+1·5, 0·4+3·5) = (13,15) — matrix-vector product is a special case (p=1) of matrix multiplication | — |
| A7 | apply | short-answer | 0.45 | A is 2×3 and B is 2×3. Is AB defined? Is BA defined? Explain using the dimension rule. | Neither is defined as written — the inner dimensions (3 vs 2) don't match either way; only Aᵀ B or ABᵀ (etc.) could work, since compatibility depends on exact shapes, not just "both matrices exist" | — |
| E1 | explain | short-answer | 0.55 | Why is matrix multiplication defined row-times-column, rather than some other rule? Connect to function composition. | (AB)x = A(Bx) — applying B first, then A; matrix multiplication *is* function composition of the two linear maps, and the row-times-column rule is exactly what makes that composition identity hold *(required)* | — |
| E2 | explain | short-answer | 0.65 | Using the composition view from E1, explain why (AB)C = A(BC) must hold, without computing any entries. | both sides compute the composite map "apply C, then B, then A" to any vector x — composition of functions is associative simply because there's only one way to chain three function applications in a fixed order, regardless of how the chain is parenthesized *(required: the function-composition argument, not entrywise verification)* | — |
| E3 | explain | short-answer | 0.75 | Why does the identity matrix I act as a multiplicative identity, in terms of the linear transformation it represents? | I represents the identity transformation, T(x)=x, which does nothing to any vector; composing "do nothing" before or after any transformation A leaves A's action unchanged, so AI=IA=A *(required)* | — |
| E4 | explain | short-answer | 0.85 | Given associativity, why can the *order* in which you parenthesize a chain of matrix products still affect computational cost, even though the final result is the same? | associativity guarantees the *result* is identical for any parenthesization, but each parenthesization creates different intermediate matrices with different sizes, so the number of scalar multiplications (from A5's mnp rule) can differ drastically between orderings — correctness and cost are separate questions *(required: distinguishes result-equality from cost-difference)* | — |
| E5 | explain | short-answer | 0.95 | If A and B are both block matrices with compatible block sizes, why can AB be computed by treating each block like a single "entry" and multiplying blocks together (in the same row-times-column pattern)? | the row-times-column definition only relies on the entries forming a ring under addition and multiplication (matrices satisfy this too, as long as block sizes align), so block matrices multiply exactly like scalar matrices with blocks standing in for individual entries *(required)* | — |
| T1 | transfer | short-answer | 1.05 | Why does the order of matrix multiplication matter in practice — e.g. rotating an image then scaling it, versus scaling then rotating? | rotate-then-scale and scale-then-rotate are different compositions of transformations and generally produce visibly different results (e.g. an off-center rotation followed by scaling moves things differently than scaling first) — a direct real-world instance of AB≠BA *(required: a concrete geometric example, not just "order matters")* | — |
| R3 | recall | short-answer | −0.9 | Fill in the blank: matrix multiplication is ___ but generally not commutative. | associative | — |
| R4 | recall | mcq | −0.85 | The identity matrix I satisfies, for any compatible A: | AI = IA = A | claims only AI=A holds while IA≠A in general, missing that the identity acts as identity from both sides → `matrix-multiplication` |
| R5 | recall | mcq | −0.8 | True or false: (AB)C = A(BC) for compatible matrices A, B, C. | true | answers "false," missing associativity, one of the two properties matrix multiplication does satisfy → `matrix-multiplication` |
| R6 | recall | short-answer | −0.75 | What is (AB)ᵀ, in terms of Aᵀ and Bᵀ? | Bᵀ Aᵀ — the transpose of a product reverses the order | — |
| R7 | recall | mcq | −0.65 | Matrix multiplication distributes over matrix addition as: | A(B+C) = AB + AC | states it as A(B+C) = AB + C, dropping the second A entirely → `matrix-multiplication` |
| R8 | recall | short-answer | −0.6 | Fill in the blank: multiplying any matrix A by a compatible zero matrix gives the ___. | zero matrix | — |
| R9 | recall | mcq | −0.55 | If A is m×n and B is p×q, the product AB is defined only when: | n=p — the inner dimensions match | claims AB is defined whenever m=q, confusing the inner-dimension matching condition with a condition on the outer dimensions → `matrix-multiplication` |
| R10 | recall | short-answer | −0.5 | For A (m×n) and B (n×p), what is the shape of AB? | m×p | — |
| R11 | recall | mcq | −0.45 | (AB)ᵀ = BᵀAᵀ demonstrates that the transpose operation: | reverses the order of a matrix product | claims transpose preserves the order, i.e. (AB)ᵀ=AᵀBᵀ, the identity that generally fails to even have compatible dimensions → `matrix-multiplication` |
| R12 | recall | mcq | −0.4 | True or false: for square matrices, AI=A but IA≠A in general. | false — both AI=A and IA=A hold | answers "true," contradicting R4's two-sided identity property → `matrix-multiplication` |
| R13 | recall | short-answer | −0.35 | Why is the naive cost of multiplying two n×n matrices described as O(n³)? | computing each of the n² entries of the product requires an n-term dot product (n multiplications and additions), giving n²×n = n³ total operations | — |
| R14 | recall | mcq | −0.3 | A(B+C) = AB+AC is called: | left-distributivity of matrix multiplication over addition | calls it "commutativity," a property matrix multiplication does not generally have at all → `matrix-multiplication` |
| R15 | recall | short-answer | −0.25 | Does (A+B)C = AC+BC hold in general, and what is this property called? | yes — this is right-distributivity of matrix multiplication over addition | — |
| R16 | recall | mcq | −0.2 | Multiplying an m×n matrix by an n×1 vector produces: | an m×1 vector — the special case of matrix-vector multiplication | claims the result is an n×1 vector, keeping the wrong dimension from the input rather than the output side of the map → `matrix-multiplication` |
| A2 | apply | numeric | −0.05 | A=[[2,0],[1,3]], I the 2×2 identity. Compute AI. `[verified: AI=A]` | AI = [[2,0],[1,3]] = A | — |
| A3 | apply | numeric | 0.05 | Using A1's A, B, verify (AB)ᵀ = BᵀAᵀ. `[verified: both equal [[19,43],[22,50]]]` | (AB)ᵀ = [[19,43],[22,50]]; BᵀAᵀ = [[5,7],[6,8]][[1,3],[2,4]] = [[19,43],[22,50]] — they match | — |
| A4 | apply | numeric | 0.15 | A is 2×3, B is 3×4. What are the dimensions of AB? `[verified: 2×4]` | 2×4 | — |
| A5 | apply | short-answer | 0.25 | A is 2×3, C is 2×4. Is AC defined? | no — the inner dimensions (3 and 2) don't match, so AC is undefined | — |
| A6 | apply | numeric | 0.35 | A=I (2×2 identity), B=[[7,−2],[4,9]]. Compute AB and BA. `[verified: both equal B]` | AB = B and BA = B — multiplying by the identity commutes trivially, even though matrix multiplication is not commutative in general | — |
| E2 | explain | short-answer | 0.65 | Why does associativity (AB)C=A(BC) follow from viewing matrices as functions, without checking entries? | both sides compute the same composition of linear maps, applying C first, then B, then A, to any input vector x; function composition is always associative ((f∘g)∘h = f∘(g∘h)) regardless of which functions are involved, so matrix multiplication inherits associativity for free once recognized as function composition *(required: the explicit function-composition framing)* | — |
| E3 | explain | short-answer | 0.75 | Why does (AB)ᵀ = BᵀAᵀ reverse the order, rather than keeping AᵀBᵀ? | transposing swaps rows and columns, reversing the roles of which map is applied first; tracing through (AB)x = A(Bx) and transposing shows the composition order must flip to keep dimensions and the underlying linear map consistent — AᵀBᵀ would generally not even have compatible dimensions unless A and B happen to be square and equal-sized *(required: the dimension-consistency argument, not just citing the identity)* | — |
| E4 | explain | derivation | 0.85 | Verify A(B+C)=AB+AC entry by entry from the definition of matrix multiplication. | the (i,j) entry of A(B+C) is Σₖ Aᵢₖ(B+C)ₖⱼ = Σₖ Aᵢₖ(Bₖⱼ+Cₖⱼ) = ΣₖAᵢₖBₖⱼ + ΣₖAᵢₖCₖⱼ, exactly the (i,j) entry of AB plus the (i,j) entry of AC — the real-number distributive law applied inside the sum, entry by entry *(required: the explicit entry-wise expansion)* | — |
| E5 | explain | short-answer | 0.95 | Why does the O(n³) naive cost of matrix multiplication matter practically, and what does the existence of faster algorithms (e.g. Strassen's) imply? | for large n, an O(n³) algorithm becomes prohibitively slow — doubling n makes it 8× slower — so this cost is a genuine bottleneck in large-scale ML and numerical computing; Strassen's algorithm and its successors reduce the exponent below 3 by cleverly reusing intermediate products, trading more additions for fewer multiplications, showing the naive row-times-column rule is not the only or fastest way to compute a product *(required: names both the practical bottleneck and what a faster algorithm implies)* | — |
| T2 | transfer | short-answer | 1.15 | Neural network forward passes chain many matrix multiplications. Why does associativity matter for how a framework can group these multiplications for efficiency? | associativity guarantees W₃(W₂(W₁x)) = ((W₃W₂)W₁)x = (W₃(W₂W₁))x for weight matrices Wᵢ, so a framework is free to choose whichever grouping minimizes total computation — e.g. precomputing W₃W₂ once if it will be reused across many inputs x — without changing the mathematical result *(required: names the reordering freedom associativity licenses, with a concrete grouping example)* | — |
| T3 | transfer | short-answer | 1.25 | Why is it useful, when debugging a batched GPU matrix-multiplication implementation, that A(B+C)=AB+AC holds *exactly* in exact arithmetic, even though floating-point results can differ slightly? | the mathematical guarantee means any two mathematically equivalent ways of computing a result should agree exactly; when floating-point implementations of the two sides disagree by more than expected rounding error, that discrepancy signals an actual bug (a shape mismatch silently broadcasting wrong, or a transposition error) rather than acceptable numerical noise, since the true algebraic identity is exact, not approximate *(required: distinguishes expected rounding error from a genuine bug signal)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.9…1.25.*
| T2 | transfer | short-answer | 1.2 | A neural network layer computes h = W₂(W₁x). Why does swapping W₁ and W₂ (computing W₁(W₂x) instead) generally produce a completely different network, not just a relabeling? | each Wᵢ represents a distinct linear transformation with its own learned structure; composition order determines which transformation acts on the raw input first and which acts on the already-transformed output, so swapping them changes what each Wᵢ is being asked to do, generally yielding different outputs entirely (AB≠BA) *(required)* | — |
| T3 | transfer | short-answer | 1.35 | You must compute the product of three matrices A(30×1), B(1×30), C(30×1). Using A6/A5's cost formula, explain why computing (AB)C vs A(BC) leads to wildly different costs, even though both give the same final answer. | (AB) is 30×30, so (AB)C costs 30·1·30 + 30·30·1 = 900+900=1800 multiplications; but BC is 1×1, so A(BC) costs 30·1·1 + 30·1·1 = 60 — associativity guarantees the same numeric result, yet the *order of parenthesization* changes cost by more than 25× *(required: concrete cost comparison using the mnp rule)* | — |
| T4 | transfer | short-answer | 1.5 | Two matrices A and B generally satisfy AB≠BA, but some special pairs *do* commute — e.g. any two diagonal matrices, or A and Aⁿ for any n. Why does diagonal structure guarantee commutativity when general matrices don't? | diagonal matrices act independently on each coordinate axis (scaling axis i by dᵢ), so applying two diagonal scalings in either order just multiplies the same per-axis factors together, which is commutative for scalars — general matrices mix coordinates together, so the order in which two different "mixings" happen generally changes the result *(required: connects diagonal structure's independence-per-axis to why commutativity emerges as a special case)* | — |

*Coverage: 20 items, −0.95…1.5.*

---

## Matrices (`matrices`)
*Prereq: Matrix Multiplication · ancestors 3 · b₀ = 0.19*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.81 | Viewed as a linear transformation, what do a matrix's columns represent? | each column is the image of the corresponding standard basis vector under the transformation | — |
| R2 | recall | mcq | −0.55 | An m×n matrix, as a linear transformation, maps vectors from: | Rⁿ to Rᵐ | reverses it, mapping Rᵐ to Rⁿ → `matrices` |
| R3 | recall | short-answer | −0.40 | What does it mean for a matrix to represent a linear transformation "in a given basis"? | the matrix's columns are the images of that basis's vectors, expressed as coordinates in the (possibly different) output basis — the same transformation looks like a different matrix in a different basis | — |
| R4 | recall | mcq | −0.20 | A square n×n matrix, viewed as a transformation, maps: | Rⁿ to itself (an "endomorphism") | thinks a square matrix must map to a different space than it maps from → `matrices` |
| A1 | apply | numeric | 0.0 | A=[[2,0],[0,3]]. Compute A applied to (1,1). | (2,3) | — |
| A2 | apply | numeric | 0.10 | A=[[0,−1],[1,0]] (a 90° rotation). Compute A applied to (2,−1). `[verified: (1,2)]` | A(2,−1)=(1,2) — rotating the point 90° counterclockwise | — |
| A3 | apply | numeric | 0.20 | A=[[1,2],[0,1]] (a shear). Compute A applied to (1,3). `[verified: (7,3)]` | A(1,3)=(7,3) — the x-coordinate shifts by twice the y-coordinate, while y is unchanged | — |
| A4 | apply | numeric | 0.30 | A=[[1,0],[0,0]] (projection onto the x-axis). Compute A applied to (5,7). `[verified: (5,0)]` | A(5,7)=(5,0) — the y-component is discarded entirely | — |
| A5 | apply | numeric | 0.40 | A=diag(2,−1). Compute A applied to (3,4), and describe geometrically what each axis does. `[verified: (6,−4)]` | A(3,4)=(6,−4) — the x-axis is stretched by 2, the y-axis is flipped and left the same length | — |
| A6 | apply | numeric | 0.50 | R=[[0,−1],[1,0]] (rotate 90°), S=diag(2,3) (scale). Compute S(R(1,0)) and (SR)(1,0), confirming they match. `[verified: both (0,3)]` | S(R(1,0))=S(0,1)=(0,3); (SR)(1,0)=(0,3) — matches, since the matrix of a composition is the product of the matrices | — |
| A7 | apply | short-answer | 0.60 | A=[[3,1],[2,4]]. Without computing Av for any v, state where e₁=(1,0) and e₂=(0,1) are sent. | e₁ is sent to A's first column, (3,2); e₂ is sent to A's second column, (1,4) — reading columns directly, no multiplication needed | — |
| E1 | explain | short-answer | 0.69 | Why do the columns of A tell you exactly where the basis vectors go? | Ae_i picks out the i-th column of A by the definition of matrix-vector multiplication, so the image of each basis vector *is* the corresponding column *(required)* | — |
| E2 | explain | short-answer | 0.80 | Why does composing two linear transformations correspond to multiplying their matrices, rather than adding them or some other operation? | applying transformation B then A to a vector x gives A(Bx); since A(Bx)=(AB)x by the definition of matrix multiplication, the single matrix describing "do B then A" is exactly the product AB — multiplication is forced by wanting composition, not addition, to be represented *(required: derives AB from A(Bx) as the composed map)* | — |
| E3 | explain | short-answer | 0.90 | Two different m×n matrices A and B can agree on where they send every basis vector only if A=B. Why does this make "a matrix" and "a linear transformation Rⁿ→Rᵐ" essentially the same object? | a linear transformation is completely determined once you know where it sends each basis vector (by linearity, every other vector's image follows); since a matrix's columns record exactly those basis images, distinct matrices always give distinct transformations and vice versa — the correspondence is one-to-one *(required: ties uniqueness to linearity, not just "matrices and transformations are related")* | — |
| E4 | explain | short-answer | 1.00 | Why does an n×n matrix with two identical columns necessarily represent a transformation that is not invertible? | two identical columns mean two different standard basis vectors are sent to the same output, so the transformation cannot be "undone" — an inverse would have to send that one output back to two different inputs, which is impossible for a function *(required: connects identical columns to the transformation failing to be one-to-one)* | — |
| E5 | explain | short-answer | 1.10 | Why is "a matrix" overloaded to mean both "a table of numbers" and "a linear transformation," and why does keeping the two views separate matter when debugging code? | the same array of numbers can be interpreted purely as stored data (e.g. a lookup table) or as an operator meant to act on vectors; conflating them can lead to applying a data matrix as if it were a transformation (or vice versa), producing numerically valid but semantically meaningless results *(required: gives a concrete consequence of conflating the two views)* | — |
| T1 | transfer | short-answer | 1.19 | Distinguish "a matrix as a table of pixel data" from "a matrix as a linear transformation," using an image-rotation example. | a data matrix stores values (e.g. pixel intensities) with no transformation implied; a transformation matrix, applied to coordinate vectors, actively maps each pixel's position to a new one (e.g. rotating the image) — the same word "matrix" names two conceptually different objects here *(required)* | — |
| R3 | recall | short-answer | −0.76 | Fill in the blank: the entry Aᵢⱼ of a matrix sits in row ___ and column ___. | i; j | — |
| R4 | recall | mcq | −0.71 | A matrix with the same number of rows and columns is called: | square | calls it "diagonal," confusing the shape property (equal rows and columns) with a structural property about which entries are zero → `matrices` |
| R5 | recall | mcq | −0.66 | True or false: matrix addition is defined entrywise, requiring both matrices to have the same dimensions. | true | answers "false," missing that matrices of different sizes cannot be added at all → `matrices` |
| R6 | recall | short-answer | −0.61 | What is the transpose Aᵀ of a matrix A, informally? | the matrix obtained by swapping A's rows and columns, so Aᵀᵢⱼ = Aⱼᵢ | — |
| R7 | recall | mcq | −0.5 | A matrix A is symmetric when: | Aᵀ = A | claims A is symmetric whenever it's square, omitting the actual entry-matching condition Aᵀ=A → `matrices` |
| R8 | recall | short-answer | −0.45 | Fill in the blank: a matrix with all off-diagonal entries equal to 0 is called a ___ matrix. | diagonal | — |
| R9 | recall | mcq | −0.4 | The zero matrix, added to any compatible matrix A, gives: | A unchanged | claims it gives the zero matrix regardless of A, confusing addition with multiplication by zero → `matrices` |
| R10 | recall | short-answer | −0.35 | How is scalar multiplication of a matrix defined? | entrywise — every entry of A is multiplied by the scalar c | — |
| R11 | recall | mcq | −0.3 | Two matrices are equal if and only if: | they have the same dimensions and every corresponding entry matches | claims two matrices are equal whenever they have the same dimensions, regardless of their entries → `matrices` |
| R12 | recall | mcq | −0.25 | True or false: every square matrix is automatically symmetric. | false | answers "true," missing that squareness is necessary but far from sufficient for symmetry → `matrices` |
| R13 | recall | short-answer | −0.2 | What structural distinction separates a "row vector" from a 1×n matrix? | none — a row vector is literally a 1×n matrix, a special case with only one row | — |
| R14 | recall | mcq | −0.15 | In the notation Aᵢⱼ, which index is conventionally the row index? | i, the first index | claims j (the second index) is the row index, reversing the standard row-then-column convention → `matrices` |
| R15 | recall | short-answer | −0.1 | What is the identity matrix I, described as a special diagonal matrix? | the diagonal matrix with every diagonal entry equal to 1 | — |
| R16 | recall | mcq | −0.05 | A matrix representing a linear transformation from Rⁿ to Rᵐ has shape: | m×n | claims the shape is n×m, swapping which dimension comes from the domain and which from the codomain → `matrices` |
| A2 | apply | numeric | 0.1 | A=[[1,2],[3,4]]. Compute Aᵀ. `[verified]` | [[1,3],[2,4]] | — |
| A3 | apply | short-answer | 0.2 | A=[[5,−1],[−1,7]]. Is A symmetric? `[verified: Aᵀ=A]` | yes — the off-diagonal entries match (both −1), so Aᵀ=A | — |
| A4 | apply | numeric | 0.3 | A=[[2,3],[3,5]]. Compute A applied to (1,1). `[verified: (5,8)]` | (2+3, 3+5) = (5,8) | — |
| A5 | apply | short-answer | 0.4 | Give a 3×3 diagonal matrix with diagonal entries 2, −1, 4. `[verified]` | diag(2,−1,4) | — |
| A6 | apply | numeric | 0.5 | A=[[0,1],[1,0]]. Compute A applied to (5,9). `[verified: (9,5)]` | (9,5) — A swaps the two components | — |
| E2 | explain | short-answer | 0.79 | Why does Aᵀᵢⱼ=Aⱼᵢ correspond to "swapping rows and columns"? | defining Aᵀᵢⱼ=Aⱼᵢ literally relabels: what was the (j,i) entry (row j, column i) of A becomes the (i,j) entry (row i, column j) of Aᵀ — every row of A becomes the corresponding column of Aᵀ and vice versa, exactly what "swapping rows and columns" means *(required: the explicit index-relabeling argument)* | — |
| E3 | explain | short-answer | 0.89 | Why must a matrix be square for it to possibly be symmetric? | symmetry requires Aᵢⱼ=Aⱼᵢ for every valid pair of indices; if A is not square, some entries Aᵢⱼ exist without a corresponding Aⱼᵢ entry to compare — the transpose would even have different dimensions than A itself — so Aᵀ=A cannot even be evaluated, let alone hold *(required: the dimension-mismatch argument for non-square A)* | — |
| E4 | explain | derivation | 0.99 | Show Ix=x for the identity matrix I, by direct computation. | the i-th entry of Ix is Σⱼ Iᵢⱼxⱼ; since Iᵢⱼ=1 when i=j and 0 otherwise, this collapses to Iᵢᵢxᵢ=1·xᵢ=xᵢ — every entry of Ix equals the corresponding entry of x, so Ix=x exactly *(required: the explicit collapse using Iᵢⱼ's 0/1 structure)* | — |
| E5 | explain | short-answer | 1.04 | Why does knowing a matrix's columns — where the basis vectors go — let you reconstruct the whole linear transformation? | any vector x=(x₁,…,xₙ) can be written as x=Σxᵢeᵢ, a linear combination of the standard basis vectors; linearity then forces T(x)=ΣxᵢT(eᵢ)=Σxᵢ(column i of A), so the n columns completely determine T's action on every other vector, by linearity alone *(required: the explicit x=Σxᵢeᵢ decomposition and linearity step)* | — |
| T2 | transfer | short-answer | 1.29 | A covariance matrix Σ is always symmetric. Using the transpose definition, explain why Σᵢⱼ=Σⱼᵢ is guaranteed structurally, before any data is involved. | Σᵢⱼ is defined as Cov(Xᵢ,Xⱼ), and covariance is symmetric in its two arguments — Cov(Xᵢ,Xⱼ)=Cov(Xⱼ,Xᵢ); since Aᵀ=A means Aᵢⱼ=Aⱼᵢ for every i,j, and that's exactly the covariance identity, Σ's symmetry follows directly from the definition of covariance itself, with no numbers needed *(required: connects Aᵀ=A directly to covariance's own symmetry)* | — |
| T3 | transfer | short-answer | 1.39 | Why does whether an image-processing convolution kernel matrix is symmetric affect whether the resulting filter treats all rotational directions equally? | a symmetric kernel (Aᵀ=A) applies the same weighting pattern regardless of how the neighborhood is read (row-by-row versus column-by-column), tending to respond identically to features rotated by 90°; an asymmetric kernel — like a directional edge detector — deliberately breaks this symmetry so the filter responds more strongly to edges in one orientation than another *(required: connects Aᵀ=A to the rotational-symmetry behavior of the filter)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.76…1.39.*
| T2 | transfer | short-answer | 1.35 | A grayscale image is stored as a matrix of pixel intensities. Rotating the image 90° is done by multiplying coordinate vectors by a rotation matrix — a completely different operation from the pixel-intensity matrix itself. Why can both be called "a matrix" without contradiction? | the pixel matrix is data indexed by position, with no notion of acting on anything; the rotation matrix is an operator that maps position vectors to new position vectors — they coexist because one matrix (the image) is the *object being transformed* (via its coordinates) and the other (the rotation) is the *transformation itself*, and neither role depends on which one is "table-like" *(required: names both roles explicitly and why they don't conflict)* | — |
| T3 | transfer | short-answer | 1.50 | A recommendation system represents users and items as a ratings matrix R, but also uses a matrix P to project a user's rating vector onto a lower-dimensional "preference space." One of these matrices is a linear transformation, the other is a data table. Which is which, and why does that distinction matter for how each is updated during training? | R is a data table — its entries are observed or predicted ratings, updated to match data; P is a linear transformation whose columns define a change of representation, updated so that mapping through it produces a useful (e.g. low-dimensional) representation — treating R as if it were meant to "act on" vectors, or P as if it stored observations, would misdirect how each gets trained *(required: identifies which is which and gives a training-relevant consequence)* | — |
| T4 | transfer | short-answer | 1.70 | Explain why a database "join table" (an n×m matrix of 0s and 1s marking which rows relate to which columns) is naturally thought of as data, while the same-shaped 0/1 matrix used as a "selection operator" in linear algebra is naturally thought of as a transformation. | as a join table, the matrix's entries directly record a relationship between two datasets with no vector being acted upon; as a selection operator, the identical array of 0s and 1s is applied to a vector via matrix-vector multiplication to extract or zero out specific components — the numbers are identical, but only in the second case is the matrix functioning as a map between spaces *(required: uses the same shape example to show intent, not entries, distinguishes the two roles)* | — |

*Coverage: 20 items, −0.81…1.70.*

---

## Trace (`trace`)
*Prereq: Matrices · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Define trace(A), and state its linearity property. | sum of diagonal entries; trace(A+B)=trace(A)+trace(B) | — |
| R2 | recall | mcq | −0.45 | trace(AB) vs trace(BA), for compatible A, B: | always equal (the cyclic property), even when AB≠BA as matrices | assumes they must differ, since AB≠BA in general → `trace` |
| R3 | recall | short-answer | −0.30 | Is trace defined for non-square matrices? | No — trace requires a square matrix, since it sums the diagonal entries aᵢᵢ, which only exist consistently when the number of rows equals the number of columns | — |
| R4 | recall | mcq | −0.10 | trace(Aᵀ) compared to trace(A): | always equal, since transposing doesn't move diagonal entries | assumes transposing changes the trace, confusing it with an operation that reorders off-diagonal entries → `trace` |
| A1 | apply | numeric | 0.1 | A=[[1,2],[3,4]]. Compute trace(A). | 1+4=5 | — |
| A2 | apply | numeric | 0.22 | A=[[2,1,0],[0,3,1],[1,0,4]]. Compute trace(A). `[verified: 9]` | 2+3+4=9 | — |
| A3 | apply | numeric | 0.34 | A=[[1,2],[3,4]], B=[[5,−1],[0,2]]. Verify trace(A+B)=trace(A)+trace(B). `[verified: both 12]` | trace(A+B)=trace([[6,1],[3,6]])=12; trace(A)+trace(B)=5+7=12 — matches, confirming linearity | — |
| A4 | apply | numeric | 0.46 | A=[[2,1],[0,3]]. Compute trace(3A). `[verified: 15]` | trace(3A)=3·trace(A)=3·5=15 | — |
| A5 | apply | numeric | 0.58 | A=[[1,2],[0,1]], B=[[2,0],[1,1]], C=[[0,1],[1,0]]. Compute trace(ABC), trace(BCA), and trace(ACB); confirm the first two match but the third differs. `[verified: trace(ABC)=trace(BCA)=3, trace(ACB)=5]` | trace(ABC)=trace(BCA)=3 (a cyclic rotation of the product), but trace(ACB)=5 — swapping B and C (not a cyclic rotation) genuinely changes the value | assumes any reordering of a product inside trace is safe, not just cyclic rotations → `trace` |
| A6 | apply | numeric | 0.68 | A=[[2,1],[1,2]]. Compute trace(A²) directly, and compare it to the sum of A's eigenvalues squared (eigenvalues 3 and 1). `[verified: trace(A²)=10=3²+1²]` | trace(A²)=10, matching 3²+1²=10 — trace of a power equals the sum of eigenvalues raised to that power | — |
| A7 | apply | short-answer | 0.75 | For an orthogonal projection matrix P with P²=P, trace(P)=2. What does this tell you about the subspace P projects onto? | the subspace has dimension 2 — trace of an orthogonal projection always equals the dimension of the subspace being projected onto, since its eigenvalues are only 0s and 1s and trace counts the 1s | — |
| E1 | explain | derivation | 0.8 | Prove trace(AB)=trace(BA), directly from the definition. | Σᵢ(AB)ᵢᵢ=ΣᵢΣⱼAᵢⱼBⱼᵢ=ΣⱼΣᵢBⱼᵢAᵢⱼ=Σⱼ(BA)ⱼⱼ — the double sum is symmetric in how the indices are grouped `[verified: trace(AB)=trace(BA)=69 for A,B from the matmul cluster]` *(required: the double-sum reindexing, not just "it's a known identity")* | — |
| E2 | explain | derivation | 0.90 | Prove that trace is invariant under similarity: trace(P⁻¹AP)=trace(A). | by cyclicity, trace(P⁻¹AP)=trace(APP⁻¹)=trace(AI)=trace(A) — rotating the three-factor product moves P⁻¹ to the end, where it cancels with P *(required: uses cyclicity to rotate P⁻¹ to meet P)* | — |
| E3 | explain | short-answer | 1.00 | Explain why trace(A) equals the sum of A's eigenvalues, using the characteristic polynomial. | the characteristic polynomial det(A−λI) expands as (−1)ⁿ(λⁿ−trace(A)λⁿ⁻¹+…), and its roots are exactly the eigenvalues; comparing that same coefficient to the expansion Π(λᵢ−λ) shows the coefficient of λⁿ⁻¹ is also −Σλᵢ, forcing trace(A)=Σλᵢ *(required: matches the λⁿ⁻¹ coefficient from both expansions)* | — |
| E4 | explain | short-answer | 1.10 | Why is ‖A‖²_F = trace(AᵀA) a useful identity, rather than just a curiosity? | it rewrites the Frobenius norm — defined entrywise as a sum of squares — as a single trace expression, which lets all of trace's algebraic tools (linearity, cyclicity) be applied directly to norm computations, e.g. in deriving matrix-calculus gradients of squared-error loss *(required: names a concrete use, like gradient derivations, not just "it's equivalent")* | — |
| E5 | explain | short-answer | 1.20 | A common mistake in matrix-calculus derivations is writing trace(ABC)=trace(ACB). Explain concretely why this step is invalid, referencing A5's numbers. | trace only tolerates *cyclic* rotations of the product (trace(ABC)=trace(BCA)=trace(CAB)); swapping B and C is a transposition, not a rotation, and A5 showed trace(ABC)=3 while trace(ACB)=5 — a genuine numerical difference, not a typo *(required: explicitly distinguishes cyclic rotation from arbitrary reordering)* | conflates "cyclic" with "any permutation is allowed" → `trace` |
| T1 | transfer | short-answer | 1.3 | trace(Σ) equals the sum of a covariance matrix's variances, ignoring every off-diagonal covariance entirely. Why does that still give a meaningful "total spread" measure? | trace(Σ) = Σ Var(Xᵢ), so it captures exactly how much each individual component varies on its own, summed across all dimensions — a coherent aggregate measure even though it says nothing about how the components co-vary with each other *(required)* | — |
| R3 | recall | short-answer | −0.65 | Fill in the blank: trace(A) is only defined for ___ matrices. | square | — |
| R4 | recall | mcq | −0.6 | trace(cA), for scalar c, equals: | c·trace(A) | claims trace(cA) equals c²·trace(A), incorrectly squaring the scalar factor → `trace` |
| R5 | recall | mcq | −0.55 | True or false: trace(Aᵀ) = trace(A). | true | answers "false," missing that transposing leaves the diagonal (and hence the trace) untouched → `trace` |
| R6 | recall | numeric | −0.5 | What is trace(Iₙ), the trace of the n×n identity matrix? `[verified: n]` | n | — |
| R7 | recall | mcq | −0.4 | trace(A+B) equals: | trace(A) + trace(B) | claims trace(A+B) equals trace(A)·trace(B), confusing the additive linearity of trace with a multiplicative rule → `trace` |
| R8 | recall | short-answer | −0.35 | Fill in the blank: trace is the sum of a matrix's ___ entries. | diagonal | — |
| R9 | recall | mcq | −0.3 | The trace of a matrix also equals: | the sum of its eigenvalues | claims it equals the product of its eigenvalues, confusing trace with the determinant instead → `trace` |
| R10 | recall | short-answer | −0.25 | Why is "a 1×1 matrix equals its own trace" a useful fact in derivations like E[XᵀAX]? | it lets a scalar such as XᵀAX (technically a 1×1 matrix) be rewritten as its own trace, which can then be manipulated using trace's cyclic property to rearrange the matrices inside | — |
| R11 | recall | mcq | −0.2 | For an idempotent projection matrix P, trace(P) equals: | rank(P) | claims trace(P) always equals the number of rows of P, regardless of its rank → `trace` |
| R12 | recall | mcq | −0.15 | True or false: trace(AB) always equals trace(A)·trace(B). | false | answers "true," a multiplicative claim that does not hold for trace in general → `trace` |
| R13 | recall | numeric | −0.1 | What is trace(0), the trace of any zero matrix? `[verified: 0]` | 0 | — |
| R14 | recall | mcq | −0.05 | trace's cyclic property, trace(ABC)=trace(BCA)=trace(CAB), allows: | cyclically rotating the order of the matrices inside a trace | claims it allows any arbitrary permutation of A, B, C inside the trace, overgeneralizing beyond the cyclic rotations the property actually guarantees → `trace` |
| R15 | recall | short-answer | 0.0 | Why can't the matrices inside a trace be freely permuted in any order? | only cyclic rotations are guaranteed to preserve the trace; a non-cyclic rearrangement (like swapping two adjacent factors without cycling) is not covered by the property and can genuinely change the value | — |
| R16 | recall | mcq | 0.05 | trace maps a square matrix to: | a single scalar | claims trace maps a matrix to another matrix of the same size, missing that it collapses to one number → `trace` |
| A2 | apply | numeric | 0.2 | A=[[3,1],[0,−2]]. Compute trace(A). `[verified: 1]` | 3+(−2) = 1 | — |
| A3 | apply | numeric | 0.3 | A=[[2,5],[5,2]]. Compute trace(2A). `[verified: 8]` | 2×(2+2) = 8 | — |
| A4 | apply | numeric | 0.4 | A=[[1,2],[3,4]], B=[[0,1],[1,0]]. Compute trace(A+B). `[verified: 5]` | trace(A)=5, trace(B)=0, so trace(A+B)=5 | — |
| A5 | apply | numeric | 0.5 | What is trace(I₄), the trace of the 4×4 identity matrix? `[verified: 4]` | 4 | — |
| A6 | apply | numeric | 0.6 | P=[[0.5,0.5],[0.5,0.5]] (an idempotent projection, rank 1). Compute trace(P) and confirm it equals rank(P). `[verified: 1=1]` | trace(P) = 0.5+0.5 = 1 = rank(P) | — |
| E2 | explain | short-answer | 0.9 | Why does trace(cA)=c·trace(A) follow immediately from the definition, entry by entry? | trace(cA) sums the diagonal entries of cA, each of which is c times the corresponding diagonal entry of A; factoring c out of every term in that sum gives c times the sum of A's diagonal entries, exactly c·trace(A) *(required: the explicit entry-wise factoring)* | — |
| E3 | explain | short-answer | 1.0 | Why does trace(Aᵀ)=trace(A), using the transpose's definition? | transposing swaps off-diagonal entries with their mirror image across the diagonal, but leaves every diagonal entry Aᵢᵢ exactly where it is, since Aᵀᵢᵢ=Aᵢᵢ; trace only sums diagonal entries, and those are unaffected by transposing, so the trace is unchanged *(required: names that diagonal entries are fixed points of the transpose)* | — |
| E4 | explain | derivation | 1.1 | Derive trace(Iₙ)=n directly from the definition of the identity matrix. | Iₙ has 1 on every diagonal entry and 0 elsewhere; trace sums exactly the n diagonal entries, each equal to 1, giving 1+1+⋯+1 (n times) = n *(required: the explicit sum of n ones)* | — |
| E5 | explain | short-answer | 1.2 | Why is trace(AB)=trace(BA) surprising at first glance, given AB≠BA as matrices in general? | the cyclic property only claims the *scalar* trace of the two products agrees, not that the matrices AB and BA are themselves equal — they can be entirely different matrices, yet the specific number obtained by summing each product's diagonal always coincides, a genuinely nontrivial fact proved by reindexing the double sum *(required: distinguishes the matrix-level inequality from the scalar-level trace equality)* | — |
| T2 | transfer | short-answer | 1.4 | PCA's total variance is trace(Σ). Why does trace being preserved under an orthogonal change of basis matter for interpreting PCA's explained-variance percentages? | since trace equals the sum of eigenvalues (R9), and PCA's transformed covariance matrix (in the eigenbasis) is diagonal with the eigenvalues on the diagonal, trace(Σ) computed in the original basis equals the sum of eigenvalues computed in the rotated (principal-component) basis — so "percentage of variance explained by the first k components" compares like quantities regardless of the starting basis *(required: names trace-as-sum-of-eigenvalues as the connecting fact)* | — |
| T3 | transfer | short-answer | 1.5 | A neural network's weight regularization sometimes penalizes trace(WᵀW). What does this term measure, and why is trace a natural way to express it? | trace(WᵀW) equals the sum of the squares of every entry of W, since each diagonal entry of WᵀW is a column's squared norm; this is exactly the squared Frobenius norm of W — trace provides a compact way to express "total squared magnitude across every weight" as a single scalar, without writing out a double sum over all entries explicitly *(required: connects trace(WᵀW) to the squared Frobenius norm)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.65…1.5.*
| T2 | transfer | short-answer | 1.45 | In ridge regression, the "effective degrees of freedom" of the fitted model is defined as trace(H) for the hat matrix H, and this number is often not an integer. Why does trace(H) make sense as a degrees-of-freedom count even when it isn't a whole number? | for ordinary least squares H is an exact projection with eigenvalues 0 and 1, so trace(H) counts parameters exactly; ridge regression shrinks H's eigenvalues toward (but not to) 0 or 1, so trace(H) becomes a continuous, shrinkage-aware generalization of "how many parameters are effectively being fit," rather than a literal integer count *(required: connects the projection-eigenvalue picture to why shrinkage makes the count fractional)* | — |
| T3 | transfer | short-answer | 1.60 | In a neural network, the trace of the Hessian of the loss (the sum of its eigenvalues) is sometimes used as a cheap proxy for how "sharp" a minimum is. Given trace(H)=ΣΛᵢ, why is trace alone a weaker sharpness measure than the largest eigenvalue by itself? | trace sums *all* the curvature directions together, so a few very flat directions can offset one very sharp direction and still yield a small or moderate trace; the largest eigenvalue isolates the single worst-case curvature direction, which is what actually controls how easily the optimizer can be kicked out of the minimum by a large step *(required: explains how averaging across directions can hide a sharp direction)* | assumes a small trace(H) always means a flat, well-generalizing minimum → `trace` |
| T4 | transfer | short-answer | 1.80 | Hutchinson's estimator approximates trace(A) for a huge matrix A using only matrix-vector products: trace(A)≈(1/m)Σⱼ zⱼᵀAzⱼ for random ±1 vectors zⱼ. Why is this useful for a matrix so large that even forming A explicitly is infeasible? | many large operators (e.g. a neural network's Hessian) are only accessible as a function "vector in, vector out" rather than as a stored array; Hutchinson's trick needs nothing but that function applied to a handful of random vectors, turning an otherwise inaccessible quantity (the trace) into something computable with no matrix ever explicitly formed *(required: explains why matrix-vector-product access, not full storage, is the enabling fact)* | — |

*Coverage: 20 items, −0.7…1.8.*

---

## Linear Transformations (`linear-transformations`)
*Prereq: Matrices · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Define a linear transformation. | T(u+v)=T(u)+T(v) and T(cu)=cT(u), for every u, v, and scalar c | — |
| R2 | recall | mcq | −0.45 | Which is *not* a linear transformation? (a) T(x)=2x (b) T(x)=x+1 (c) T(x,y)=(y,x) | (b) — a translation is *affine*, not linear | picks (c), the coordinate-swap, mistaking a perfectly linear map for the non-linear one → `linear-transformations` |
| R3 | recall | short-answer | −0.28 | State the kernel (null space) of a linear transformation T, in terms of the equation T(x)=0. | ker(T) = {x : T(x)=0} — the set of all inputs T sends to the zero vector | — |
| R4 | recall | mcq | −0.08 | If T:Rⁿ→Rᵐ is linear and injective (one-to-one), then ker(T) is: | just {0} — no nonzero vector maps to 0 | believes an injective linear map can still have a nontrivial kernel → `linear-transformations` |
| A1 | apply | short-answer | 0.1 | Spot-check T(x,y)=(x+y, x−y) for additivity using (1,2) and (3,4). `[verified: T(sum)=(10,-2)=T(v1)+T(v2)]` | T(4,6)=(10,−2); T(1,2)+T(3,4)=(3,−1)+(7,−1)=(10,−2) — matches, consistent with linearity (a spot-check, not a full proof) | — |
| A2 | apply | short-answer | 0.24 | Check T(x,y,z)=(x+2y, y−z, 3x+z) for additivity using v₁=(1,0,2), v₂=(0,1,1). `[verified: T(v1+v2)=(3,-2,6)=T(v1)+T(v2)]` | T(1,1,3)=(3,−2,6); T(1,0,2)+T(0,1,1)=(1,−2,3)+(2,0,3)=(3,−2,6) — matches | — |
| A3 | apply | numeric | 0.36 | T(x,y)=(3x−y, x+2y). Find the matrix A representing T, and compute T(2,5) using it. `[verified: (1,12)]` | A=[[3,−1],[1,2]]; A(2,5)=(3·2−5, 2+2·5)=(1,12) | — |
| A4 | apply | numeric | 0.48 | S(x,y)=(y,x) (swap) and T(x,y)=(3x−y,x+2y). Compute S(T(1,1)). `[verified: (3,2)]` | T(1,1)=(2,3); S(2,3)=(3,2) | — |
| A5 | apply | short-answer | 0.58 | T(x,y)=(xy, x). Use v=(1,3) and the scalar c=2 to check homogeneity: compare T(2v) to 2·T(v). `[verified: T(2,6)=(12,2), 2·T(1,3)=(6,2) — mismatch]` | T(2,6)=(12,2), but 2·T(1,3)=2·(3,1)=(6,2) — these disagree, so T fails homogeneity and is not linear | assumes any formula built only from x and y (with no added constant) must be linear, missing that products of variables also break linearity → `linear-transformations` |
| A6 | apply | numeric | 0.68 | T(x,y)=(x+y, x−y). Is (1,−1) in ker(T)? Is (1,1)? `[verified: T(1,-1)=(0,2), T(1,1)=(2,0)]` | T(1,−1)=(0,2)≠0, so (1,−1) is not in ker(T); T(1,1)=(2,0)≠0 either — neither vector is in the kernel | — |
| A7 | apply | numeric | 0.75 | T(x,y)=(2x+y, −x+3y). Find T(e₁) and T(e₂), and use them to write T's matrix directly. `[verified: T(e1)=(2,-1), T(e2)=(1,3)]` | T(e₁)=(2,−1), T(e₂)=(1,3); the matrix is [[2,1],[−1,3]] — its columns are exactly these images | — |
| E1 | explain | short-answer | 0.8 | Why can every linear transformation Rⁿ→Rᵐ be represented by some matrix A, with T(x)=Ax? | T is fully determined by where it sends the basis vectors e₁,…,eₙ (linearity forces every other vector's image to be a linear combination of these); those images become exactly the columns of A *(required: the "determined by basis images" argument)* | — |
| E2 | explain | short-answer | 0.92 | Prove that ker(T) is always a subspace, for any linear transformation T. | 0∈ker(T) since T(0)=0; if T(u)=0 and T(v)=0 then T(u+v)=T(u)+T(v)=0, so ker(T) is closed under addition; if T(u)=0 then T(cu)=cT(u)=0, so it's closed under scalar multiplication — all three subspace conditions follow directly from linearity *(required: verifies all three closure conditions, not just asserts it)* | — |
| E3 | explain | short-answer | 1.02 | Why does a linear transformation T:Rⁿ→Rᵐ with n>m always have a nontrivial kernel? | the image of T lies in Rᵐ and is spanned by the images of n basis vectors, so if n>m those n images cannot all be linearly independent in the m-dimensional target — some nontrivial combination of them must vanish, giving a nonzero vector in ker(T) *(required: ties "more inputs than output dimensions" to forced linear dependence of the images)* | — |
| E4 | explain | short-answer | 1.12 | Explain why the set of all linear transformations Rⁿ→Rᵐ is itself a vector space, closed under addition and scalar multiplication. | if S and T are linear, then (S+T)(x):=S(x)+T(x) and (cT)(x):=cT(x) are themselves linear (each inherits additivity and homogeneity from S and T), and the corresponding matrices are just A+B and cA — so combining transformations this way never leaves the class of linear maps *(required: shows S+T and cT satisfy the linearity conditions, not just states the conclusion)* | — |
| E5 | explain | short-answer | 1.22 | Why is the composition of two linear transformations always linear, even before knowing anything about their specific matrices? | for S∘T, additivity holds because (S∘T)(u+v)=S(T(u+v))=S(T(u)+T(v))=S(T(u))+S(T(v)) (applying T's then S's additivity in turn), and homogeneity follows the same way — the composition inherits both properties purely from each map individually being linear *(required: chains T's linearity into S's, not just asserts closure)* | — |
| T1 | transfer | short-answer | 1.3 | Why must T(0)=0 for any linear transformation, and how does that immediately rule out T(x)=3x+5 as linear? | linearity gives T(0)=T(0·x)=0·T(x)=0 for any x; since T(0)=5≠0 for T(x)=3x+5, it cannot be linear — no further checking is needed once this one value is examined *(required: uses T(0·x)=0·T(x) as the mechanism, not just "translations aren't linear")* | — |
| R3 | recall | short-answer | −0.65 | Fill in the blank: a linear transformation satisfies T(u+v)=___ and T(cu)=___. | T(u)+T(v); cT(u) | — |
| R4 | recall | mcq | −0.6 | T(0), for any linear transformation T, must equal: | 0, the zero vector | claims T(0) can be any vector depending on T, missing that linearity forces it to be exactly 0 → `linear-transformations` |
| R5 | recall | mcq | −0.55 | True or false: every linear transformation Rⁿ→Rᵐ can be represented by some m×n matrix. | true | answers "false," missing the matrix-representability property `matrices`' own E1 relies on → `linear-transformations` |
| R6 | recall | short-answer | −0.5 | What is the identity transformation? | T(x)=x for every x — it leaves every vector unchanged | — |
| R7 | recall | mcq | −0.4 | Composing two linear transformations T₁ then T₂ corresponds to: | multiplying their matrices, in the order matching applying T₁ first | claims composing T₁ then T₂ corresponds to adding their matrices, confusing composition with the unrelated operation of matrix addition → `linear-transformations` |
| R8 | recall | short-answer | −0.35 | Fill in the blank: a linear transformation preserves ___ combinations of vectors. | linear — T(au+bv)=aT(u)+bT(v) | — |
| R9 | recall | mcq | −0.3 | Which of these is a linear transformation of R²? | a 90° rotation about the origin | picks "a translation by (1,0)," an affine map that fails T(0)=0 → `linear-transformations` |
| R10 | recall | short-answer | −0.25 | Why is a reflection across a line through the origin linear, but a reflection across an arbitrary line generally is not? | a reflection through the origin still sends the origin to itself and satisfies both linearity axioms; a reflection across a line not through the origin moves the origin itself, violating T(0)=0, so it fails to be linear — it's affine instead | — |
| R11 | recall | mcq | −0.2 | The zero transformation T(x)=0 for all x is: | a valid (if trivial) linear transformation | claims the zero transformation isn't linear because it "throws away all information," conflating a degenerate but perfectly linear map with a non-linear one → `linear-transformations` |
| R12 | recall | mcq | −0.15 | True or false: a linear transformation must map the origin to itself. | true | answers "false," directly contradicting T(0)=0 → `linear-transformations` |
| R13 | recall | short-answer | −0.1 | Which single image, if nonzero, is enough to prove T is NOT linear? | T(0), the image of the zero vector — if T(0)≠0, T cannot be linear | — |
| R14 | recall | mcq | −0.05 | Scaling every coordinate by the same factor c, T(x)=cx, is: | a linear transformation, for any fixed scalar c | claims T(x)=cx is linear only when c>0, an unnecessary restriction not part of the definition → `linear-transformations` |
| R15 | recall | short-answer | 0.0 | What must be checked to verify a candidate function T is linear, beyond spot-checking a few examples? | both defining properties — additivity and homogeneity — must hold for *every* possible u, v, and scalar c, not just the particular examples checked | — |
| R16 | recall | mcq | 0.05 | A shear transformation, like T(x,y)=(x+y,y), is: | linear | claims a shear is not linear because it "distorts shapes," confusing visual distortion with the algebraic definition of linearity → `linear-transformations` |
| A2 | apply | short-answer | 0.2 | T(x,y)=(2x,3y). Verify T(u+v)=T(u)+T(v) for u=(1,2), v=(3,1). `[verified: both (8,9)]` | T(u)=(2,6), T(v)=(6,3), sum=(8,9); u+v=(4,3), T(u+v)=(8,9) — matches | — |
| A3 | apply | short-answer | 0.3 | Is T(x,y)=(x², y) linear? Check homogeneity using u=(1,1), c=2. `[verified: T(2,2)=(4,2)≠(2,2)=2T(1,1)]` | no — T(2u)=T(2,2)=(4,2), but 2T(u)=2(1,1)=(2,2); these don't match, so T is not linear | — |
| A4 | apply | numeric | 0.4 | T(x,y,z)=(x+y, y+z). Compute T(0,0,0). `[verified: (0,0)]` | (0,0) — consistent with (though not sufficient proof of) linearity | — |
| A5 | apply | numeric | 0.5 | T(x)=5x (1-D). Verify T(3)+T(4)=T(7). `[verified: both 35]` | T(3)+T(4)=15+20=35; T(7)=35 — matches | — |
| A6 | apply | short-answer | 0.6 | T(x,y)=(x,y,1). Is T linear? Check T(0,0). `[verified: T(0,0)=(0,0,1)≠(0,0,0)]` | no — T(0,0)=(0,0,1)≠(0,0,0), so T fails T(0)=0 and cannot be linear; it's an affine embedding instead | — |
| E2 | explain | short-answer | 0.9 | Why does T(0)=0 follow automatically from the two linearity axioms, rather than being a separate axiom itself? | T(0)=T(0·x)=0·T(x)=0 for any x, using the homogeneity axiom with c=0 — T(0)=0 is a logical consequence of homogeneity, not an independent requirement *(required: the explicit T(0·x)=0·T(x) step)* | — |
| E3 | explain | short-answer | 1.0 | Why does composing two linear transformations always give another linear transformation? | if T₁ and T₂ are both linear, (T₂∘T₁)(u+v) = T₂(T₁(u+v)) = T₂(T₁(u)+T₁(v)) = T₂(T₁(u))+T₂(T₁(v)), using T₁'s additivity and then T₂'s additivity in turn; the same chaining argument works for homogeneity, so the composite inherits both linearity axioms *(required: the explicit two-step chaining through T₁ then T₂)* | — |
| E4 | explain | derivation | 1.1 | Verify T(x,y)=(2x,3y) satisfies homogeneity, T(cu)=cT(u), for general scalar c and u=(x,y). | T(cu)=T(cx,cy)=(2cx,3cy); cT(u)=c(2x,3y)=(2cx,3cy) — these match exactly for every c, x, y *(required: the explicit both-sides computation)* | — |
| E5 | explain | short-answer | 1.2 | Why must additivity and homogeneity be checked together — could a function satisfy one but not the other? | yes — the two properties are logically independent; some functions satisfy homogeneity for every scalar but fail additivity (or vice versa), so genuine linearity requires verifying both properties hold, not assuming one implies the other *(required: names that the two properties are logically independent)* | — |
| T2 | transfer | short-answer | 1.4 | A neural network layer computes y=Wx+b. Why is it called "linear" in casual ML usage despite technically violating T(0)=0? | y=Wx+b generally sends 0 to b≠0, technically making it affine rather than linear in the strict algebraic sense; ML practitioners often use "linear layer" loosely to mean "no nonlinear activation function applied," a different (weaker) sense of "linear" than the algebraic definition here, which can cause real confusion when precision matters *(required: distinguishes the affine-vs-linear technicality from the ML usage)* | — |
| T3 | transfer | short-answer | 1.5 | Why does confirming an image-compression transform is genuinely linear, not merely "looks proportional," matter for guaranteeing correct decompression? | if the forward transform T and its inverse T⁻¹ are both genuinely linear, composing them is guaranteed to itself be linear and, being the identity on the relevant space, to satisfy exact recovery for every linear combination of inputs; a transform that merely "looks proportional" on a few tested examples but secretly fails additivity or homogeneity somewhere could silently corrupt certain pixel-value combinations, even if it passes casual spot checks *(required: contrasts genuine linearity's compositional guarantee against a spot-checked approximation)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.65…1.5.*
| T2 | transfer | short-answer | 1.48 | A camera projection maps 3D world points to 2D image points. Explain, using kernel and dimension-counting, why this map cannot be injective. | the map goes from R³ to R², so by the dimension argument (n=3>m=2), the kernel must be nontrivial — concretely, an entire line of 3D points along a single camera ray all project to the same 2D pixel, which is exactly why depth information is lost in a single image *(required: connects the R³→R² dimension mismatch to the concrete "whole ray maps to one pixel" fact)* | — |
| T3 | transfer | short-answer | 1.65 | A neural network layer computes h=σ(Wx) where σ is a nonlinear activation (e.g. ReLU) and W is a matrix. Is the map x↦σ(Wx) linear? Why does the answer matter for what the network can represent? | no — even though Wx is linear, applying σ afterward destroys additivity and homogeneity in general (σ(a+b)≠σ(a)+σ(b) for a nonlinear σ); this matters because a network built purely from linear layers, however many are stacked, collapses to a single linear map, so the nonlinearity is what lets the network represent anything beyond linear functions *(required: explicitly ties the nonlinearity's necessity to expressiveness beyond linear maps)* | — |
| T4 | transfer | short-answer | 1.85 | Cryptographic hash functions are designed to look nothing like linear transformations, even though they operate on fixed-length bit vectors. Using T(0)=0 as a necessary property of any linear map, explain one concrete way a hash function must violate linearity to be useful for security. | a linear map always sends the zero input to the zero output, so an attacker who knows a function is linear immediately knows one input-output pair for free and can exploit linear structure (e.g. XOR-ing known inputs) to predict outputs of new inputs; a secure hash function must avoid this entirely — H(0) is an essentially random-looking value, and combining known inputs (e.g. hashing a+b) gives no exploitable relationship to H(a) and H(b) *(required: uses T(0)=0 as the starting point for why linearity would be exploitable)* | — |

*Coverage: 20 items, −0.7…1.85.*

---

## Matrix Calculus (Gradients & Jacobians) (`matrix-calculus`)
*Prereq: Linear Transformations, Vector Norm · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define the gradient of a scalar function f:Rⁿ→R, and the Jacobian of a vector function g:Rⁿ→Rᵐ. | ∇f = (∂f/∂x₁,…,∂f/∂xₙ), a vector; the Jacobian is the m×n matrix of all partial derivatives ∂gᵢ/∂xⱼ | — |
| R2 | recall | mcq | −0.16 | At a given point, ∇f points in the direction of: | steepest **ascent** | claims it points toward steepest descent — the exact reverse, and the reason gradient descent *subtracts* the gradient → `matrix-calculus` |
| R3 | recall | short-answer | −0.02 | What is the Hessian of a scalar function f:Rⁿ→R? | the n×n matrix of second partial derivatives, Hᵢⱼ=∂²f/∂xᵢ∂xⱼ — symmetric when the mixed partials commute | — |
| R4 | recall | mcq | 0.14 | The Jacobian of a composition g∘f, by the chain rule, is: | J_g(f(x))·J_f(x) — a matrix product of the two Jacobians, evaluated at the right points | multiplies the Jacobians in the wrong order, forgetting composition order matters just as it does for matrix multiplication in general → `matrix-calculus` |
| A1 | apply | numeric | 0.34 | f(x,y)=x²+3y². Compute ∇f at (1,2). `[verified: (2,12)]` | ∇f=(2x,6y); at (1,2): (2,12) | — |
| A2 | apply | short-answer | 0.5 | For the linear function f(x)=a·x (a a fixed vector), find ∇f. `[verified concept: gradient of a linear function is constant]` | ∇f = a — constant everywhere, foundational for computing gradients of loss functions built from linear terms | computes a gradient that still depends on x, missing that a linear function's gradient is constant → `matrix-calculus` |
| A3 | apply | numeric | 0.62 | f(x,y,z)=x²+2y²+3z². Compute ∇f at (1,−1,2). `[verified: (2,-4,12)]` | ∇f=(2x,4y,6z); at (1,−1,2): (2,−4,12) | — |
| A4 | apply | numeric | 0.72 | f(x,y)=xy. Compute ∇f at (3,4). `[verified: (4,3)]` | ∇f=(y,x); at (3,4): (4,3) | — |
| A5 | apply | numeric | 0.80 | g(x,y)=(x+y, x−y, xy). Compute the Jacobian at (2,1). `[verified: [[1,1],[1,-1],[1,2]]]` | J=[[1,1],[1,−1],[y,x]]; at (2,1): [[1,1],[1,−1],[1,2]] | — |
| A6 | apply | numeric | 0.88 | f(x,y)=x²+y²−3x. Compute ∇f at (2,5). `[verified: (1,10)]` | ∇f=(2x−3,2y); at (2,5): (1,10) | — |
| A7 | apply | numeric | 0.96 | f(x)=xᵀAx for A=[[2,1],[1,3]] (symmetric). Compute ∇f=2Ax at x=(1,1), and verify against the direct partial derivatives of f(x,y)=2x²+2xy+3y². `[verified: both (6,8)]` | 2Ax=(6,8); direct partials ∂f/∂x=4x+2y=6, ∂f/∂y=2x+6y=8 at (1,1) — matches | — |
| E1 | explain | derivation | 1.04 | For f(x)=xᵀAx with symmetric A, ∇f=2Ax. Verify this on A=diag(1,2), f(x,y)=x²+2y², by direct partial differentiation. `[verified: (2x,4y)=2Ax]` | ∂f/∂x=2x, ∂f/∂y=4y, giving ∇f=(2x,4y); 2Ax with A=diag(1,2) gives (2x,4y) — matches *(required: the direct partial-derivative computation, confirming the general formula on a concrete case)* | — |
| E2 | explain | derivation | 1.14 | Derive the gradient of f(x)=‖x‖² (i.e. xᵀx) from first principles using partial derivatives, and confirm it matches 2Ax with A=I. | f(x)=Σxᵢ², so ∂f/∂xᵢ=2xᵢ for each i, giving ∇f=2x; taking A=I in the general quadratic-form formula ∇(xᵀAx)=2Ax gives 2Ix=2x — consistent *(required: the entrywise partial-derivative derivation, not just quoting the formula)* | — |
| E3 | explain | short-answer | 1.24 | Explain why the gradient of a linear function f(x)=aᵀx is the constant vector a, with no dependence on x, and why this matters for gradient descent on linear models. | each partial derivative ∂f/∂xᵢ=aᵢ is already constant, since f is a sum of terms each depending on exactly one xᵢ linearly; for gradient descent this means the "pull" of a purely linear loss term never weakens as training proceeds, unlike quadratic terms whose gradient shrinks near the minimum *(required: contrasts the constant gradient with a quadratic term's shrinking gradient)* | — |
| E4 | explain | derivation | 1.34 | Using the chain rule for Jacobians, derive the gradient of the composition h(x)=f(g(x)) where g:Rⁿ→Rᵐ is linear (g(x)=Bx) and f:Rᵐ→R is scalar. | J_h(x)=∇f(g(x))ᵀ·B (a 1×m row vector times an m×n matrix, from the chain rule J_h=J_f·J_g and J_g=B for a linear map); transposing back to a column gives ∇h(x)=Bᵀ∇f(Bx) — exactly the form used to backpropagate a gradient through a linear layer *(required: identifies Bᵀ∇f(Bx) as the resulting gradient)* | — |
| E5 | explain | short-answer | 1.44 | Explain why ∇²f (the Hessian) being positive semidefinite at a critical point is the multivariable generalization of "f''(x)≥0" for a local minimum in single-variable calculus. | a critical point has ∇f=0, and moving in direction v changes f approximately by ½vᵀ(∇²f)v to second order; this quantity is guaranteed non-negative for every direction v exactly when ∇²f is positive semidefinite, which is the direct multivariable analogue of a nonnegative second derivative ruling out a local max in every direction *(required: connects the quadratic-form sign to the Taylor expansion argument)* | — |
| T1 | transfer | short-answer | 1.54 | Gradient descent updates θ ← θ − η∇L(θ), *subtracting* the gradient. Using R2, explain why subtraction (not addition) is correct for minimizing L, and what would happen if the sign were flipped by mistake. | the gradient points toward steepest ascent, so moving in its negative direction moves toward steeper *descent* — exactly what minimizing L requires; adding the gradient instead would move toward steeper ascent, actively *increasing* the loss at every step — a catastrophic sign bug, not a subtle one *(required: names the sign-flip consequence explicitly)* | — |
| R3 | recall | short-answer | −0.4 | Fill in the blank: the Hessian matrix of f:Rⁿ→R contains all ___ partial derivatives. | second-order | — |
| R4 | recall | mcq | −0.35 | The gradient ∇f is a special case of the Jacobian when: | f is scalar-valued (its output dimension m=1) | claims the gradient is a special case of the Jacobian when the input dimension n=1, confusing which dimension must be 1 → `matrix-calculus` |
| R5 | recall | mcq | −0.3 | True or false: at a critical point of f, ∇f=0. | true | answers "false," contradicting the very definition of a critical point → `matrix-calculus` |
| R6 | recall | short-answer | −0.25 | What is the directional derivative of f in the direction of unit vector u, in terms of ∇f? | ∇f·u | — |
| R7 | recall | mcq | −0.2 | For f(x)=xᵀAx with symmetric A, ∇f equals: | 2Ax | states ∇f=Ax, dropping the factor of 2 that comes from differentiating a quadratic form → `matrix-calculus` |
| R8 | recall | short-answer | −0.1 | Fill in the blank: the Jacobian of g:Rⁿ→Rᵐ is an ___×___ matrix. | m; n | — |
| R9 | recall | mcq | −0.05 | The (i,j) entry of the Jacobian matrix of g is: | ∂gᵢ/∂xⱼ | swaps the indices, giving ∂gⱼ/∂xᵢ, reversing which output and which input the entry corresponds to → `matrix-calculus` |
| R10 | recall | short-answer | 0.0 | Why is the gradient written as a vector, while the derivative of a single-variable function is a single number? | a multivariable function has one partial derivative per input variable, so recording the full rate of change requires one number per direction — a vector — rather than a single scalar | — |
| R11 | recall | mcq | 0.05 | The Hessian of a scalar function f:Rⁿ→R has shape: | n×n | claims the Hessian has shape n×1, confusing it with the gradient rather than the full matrix of second partials → `matrix-calculus` |
| R12 | recall | mcq | 0.1 | True or false: the Hessian matrix is generally symmetric, given continuous second partial derivatives. | true | answers "false," missing the mixed-partials theorem that guarantees Hessian symmetry → `matrix-calculus` |
| R13 | recall | short-answer | 0.15 | State the chain rule for gradients: for h(x)=f(g(x)), what is ∇h in terms of g's Jacobian and ∇f? | ∇h(x) = J_g(x)ᵀ∇f(g(x)) | — |
| R14 | recall | mcq | 0.2 | A negative-definite Hessian at a critical point indicates: | a local maximum | claims a negative-definite Hessian indicates a local minimum, swapping the two sign-definiteness conclusions → `matrix-calculus` |
| R15 | recall | short-answer | 0.25 | What does a zero gradient at a point guarantee, and what does it NOT guarantee? | it guarantees the point is a critical point (a candidate for a local min, max, or saddle); it does not guarantee the point is actually a local minimum or maximum — the Hessian is needed to distinguish these | — |
| R16 | recall | mcq | 0.3 | A positive-definite Hessian at a critical point indicates: | a local minimum | claims a positive-definite Hessian only guarantees the function is increasing everywhere, rather than correctly identifying a local minimum at that critical point → `matrix-calculus` |
| A3 | apply | numeric | 0.6 | f(x,y)=3x²+y². Compute ∇f at (2,−1). `[verified: (12,−2)]` | ∇f=(6x,2y); at (2,−1): (12,−2) | — |
| A4 | apply | numeric | 0.65 | g(x,y)=(x+y, xy). Compute the Jacobian at (1,2). `[verified: [[1,1],[2,1]]]` | J = [[1,1],[y,x]]; at (1,2): [[1,1],[2,1]] | — |
| A5 | apply | numeric | 0.7 | f(x,y,z)=x²+2y²+3z². Compute the Hessian. `[verified: constant diag(2,4,6)]` | Hessian = diag(2,4,6), constant regardless of the point | — |
| A6 | apply | short-answer | 0.75 | f(x)=aᵀx+b, for fixed vector a and constant b. Find ∇f. `[verified concept: constant gradient, extending A2's linear case]` | ∇f = a — constant, and unaffected by the additive constant b | — |
| E2 | explain | short-answer | 1.14 | Why must the Hessian matrix be symmetric, given continuous second partial derivatives? | this is Clairaut's (Schwarz's) theorem on mixed partials: ∂²f/∂xᵢ∂xⱼ = ∂²f/∂xⱼ∂xᵢ whenever both are continuous, so the (i,j) and (j,i) entries of the Hessian are automatically equal, making the matrix symmetric *(required: names the mixed-partials theorem specifically)* | — |
| E3 | explain | short-answer | 1.24 | Why is the Jacobian of g:Rⁿ→Rᵐ an m×n matrix, rather than n×m? | each of the m output components gᵢ contributes one row of partial derivatives (one row per output), and each row has n entries (one per input variable) — matching the convention that Jg(x)·Δx must be a valid matrix-vector product mapping an n-dimensional input change to an m-dimensional output change *(required: connects the shape to the matrix-vector product convention)* | — |
| E4 | explain | derivation | 1.34 | Derive the directional derivative formula D_uf=∇f·u from D_uf=lim_{h→0}[f(x+hu)−f(x)]/h, using a first-order Taylor expansion. | expanding f(x+hu) ≈ f(x) + h(∇f·u) + O(h²) to first order in h, subtracting f(x) and dividing by h gives [f(x+hu)−f(x)]/h ≈ ∇f·u + O(h); taking h→0 leaves exactly ∇f·u, since the higher-order term vanishes *(required: the explicit Taylor expansion and limit)* | — |
| E5 | explain | short-answer | 1.44 | Why does the Hessian sign test generalize the single-variable second-derivative test? | in one variable, f''(x)>0 at a critical point signals a local minimum and f''(x)<0 a local maximum; the Hessian is the multivariable analogue of f'', and sign-*definiteness* (rather than a single sign) is the right generalization, since it requires the quadratic approximation to curve upward (or downward) in *every* direction, not just along one axis *(required: names definiteness, not a single scalar sign, as the multivariable analogue)* | — |
| T2 | transfer | short-answer | 1.64 | Newton's method updates x ← x − H⁻¹∇f(x), using the Hessian rather than only the gradient. Why does this typically converge in fewer iterations near a well-behaved minimum? | the Hessian captures the curvature of f, so H⁻¹∇f(x) rescales the gradient step according to how steeply f curves in each direction — taking a bigger step where the function is flat and a smaller step where it's sharply curved; this matches Newton's method to the local quadratic shape of f directly, rather than gradient descent's fixed-direction, uniformly-scaled step *(required: names curvature-based rescaling as the mechanism)* | — |
| T3 | transfer | short-answer | 1.74 | Backpropagation computes ∇L(θ) using the chain rule for gradients (R13). Why does the Jacobian-transpose structure of that chain rule matter for making backpropagation tractable in deep networks? | the chain rule ∇h(x)=J_g(x)ᵀ∇f(g(x)) lets each layer's gradient be computed by multiplying the *next* layer's already-computed gradient by that layer's own (typically much smaller) local Jacobian transpose, propagating backward one layer at a time; this avoids ever explicitly forming the full Jacobian of the entire network, which is what makes gradient computation via backprop tractable at scale *(required: names the layer-by-layer propagation that avoids forming the full network Jacobian)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.4…1.74.*
| T2 | transfer | short-answer | 1.68 | Backpropagation computes ∂L/∂W for each weight matrix W in a network by repeatedly applying the chain rule for Jacobians. Why does this make matrix calculus, not just scalar calculus, essential to training neural networks? | each layer's output is a vector (or matrix) function of the previous layer's vector output, so the "derivative" at each step is a Jacobian matrix, not a single number; backpropagation is literally the chain rule for compositions of vector-valued functions (E4's construction, applied layer after layer), so representing and multiplying these Jacobians requires matrix calculus throughout *(required: identifies backprop as repeated Jacobian chain-rule application)* | — |
| T3 | transfer | short-answer | 1.85 | For linear regression's squared-error loss L(β)=‖y−Xβ‖², the gradient is ∇L(β)=−2Xᵀ(y−Xβ). Explain how this formula follows from treating L as a quadratic form in β, and why setting it to zero recovers the normal equations. | expanding L(β)=(y−Xβ)ᵀ(y−Xβ)=yᵀy−2yᵀXβ+βᵀXᵀXβ shows the βᵀXᵀXβ term is a quadratic form with matrix XᵀX, so its gradient is 2XᵀXβ (via the 2Ax rule), and the linear term −2yᵀXβ has constant gradient −2Xᵀy; setting the sum to zero, −2Xᵀy+2XᵀXβ=0, is exactly XᵀXβ=Xᵀy, the normal equations *(required: derives the gradient from the quadratic-form expansion, arriving at the normal equations)* | — |
| T4 | transfer | short-answer | 2.05 | A saddle point has ∇f=0 but is neither a local min nor max, because the Hessian has both positive and negative eigenvalues. Using R2 (gradient points toward steepest ascent) and this fact about the Hessian, explain why gradient descent can slow down dramatically near a saddle point without actually being stuck at a good solution. | near a saddle, the gradient itself is small (it vanishes exactly at the point) even though moving along a negative-eigenvalue eigendirection of the Hessian would decrease f further; because gradient descent's step size scales with the gradient's magnitude, it takes tiny steps in a region where the true loss landscape still has a clear escape direction — creating the illusion of near-convergence *(required: connects the vanishing-but-nonzero-curvature situation to misleadingly small gradient steps)* | assumes a near-zero gradient always signals a good (near-minimum) solution → `matrix-calculus` |

*Coverage: 20 items, −0.46…2.05.*

---

## Kronecker Product (`kronecker-product`)
*Prereq: Matrix Multiplication · ancestors 3 · b₀ = 0.19*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.81 | Describe the Kronecker product A⊗B in one sentence. | a block matrix where every entry of A is replaced by that entry times the entire matrix B | — |
| R2 | recall | mcq | −0.55 | For A (m×n) and B (p×q), A⊗B has dimensions: | (mp)×(nq) | picks (m+p)×(n+q), treating it like a block-concatenation rather than a block-multiplication → `kronecker-product` |
| R3 | recall | short-answer | −0.38 | Is the Kronecker product commutative, i.e. does A⊗B=B⊗A in general? | No — even when both products are defined and give the same overall size, A⊗B and B⊗A generally differ (they're related by a fixed permutation of rows/columns, not equal) | assumes A⊗B=B⊗A by analogy with the commutativity of scalar multiplication → `kronecker-product` |
| R4 | recall | mcq | −0.18 | trace(A⊗B) for square A, B equals: | trace(A)·trace(B) | assumes trace(A⊗B)=trace(A)+trace(B), confusing it with a block-diagonal (direct sum) construction → `kronecker-product` |
| A1 | apply | numeric | 0.0 | A=[1,2] (1×2), B=[[0,1],[1,0]] (2×2). Compute A⊗B. `[verified: [[0,1,0,2],[1,0,2,0]]]` | A⊗B = [1·B, 2·B] = [[0,1,0,2],[1,0,2,0]] | — |
| A2 | apply | numeric | 0.12 | A=[[1,2],[3,4]] (2×2), B=[[0,1]] (1×2). Compute A⊗B. `[verified: [[0,1,0,2],[0,3,0,4]]]` | A⊗B=[[0,1,0,2],[0,3,0,4]] | — |
| A3 | apply | numeric | 0.24 | A=I₂ (2×2 identity), B=[[2,3],[4,5]]. Compute I₂⊗B, and describe its structure. `[verified: block-diagonal with two copies of B]` | I₂⊗B is block-diagonal, with B repeated twice along the diagonal and zeros elsewhere — Kronecker product with an identity just replicates the other matrix into separate blocks | — |
| A4 | apply | numeric | 0.36 | A=[[2],[3]] (2×1), B=[[1,4]] (1×2). Compute A⊗B. `[verified: [[2,8],[3,12]]]` | A⊗B=[[2,8],[3,12]] — for a column times a row, the Kronecker product coincides with the ordinary outer product | — |
| A5 | apply | numeric | 0.48 | A=[[1,2],[0,1]], B=[[2,0],[1,3]]. Verify (A⊗B)ᵀ=Aᵀ⊗Bᵀ by computing both sides. `[verified: both equal [[2,1,0,0],[0,3,0,0],[4,2,2,1],[0,6,0,3]]]` | (A⊗B)ᵀ = Aᵀ⊗Bᵀ = [[2,1,0,0],[0,3,0,0],[4,2,2,1],[0,6,0,3]] — matches | — |
| A6 | apply | numeric | 0.58 | A=[[1,2],[3,4]], B=[[2,0],[1,1]]. Compute det(A⊗B) and confirm it equals det(A)²·det(B)² (using det(A⊗B)=det(A)^p·det(B)^n for A n×n, B p×p). `[verified: both 16]` | det(A⊗B)=16; det(A)²·det(B)²=(−2)²·(2)²=4·4=16 — matches | — |
| A7 | apply | numeric | 0.65 | A=[[1,2],[0,1]], X=[[1,0],[2,1]], B=[[1,1],[0,2]]. Verify vec(AXB)=(Bᵀ⊗A)vec(X) numerically (column-major vec). `[verified: both (5,2,9,4)]` | vec(AXB)=(5,2,9,4) computed directly; (Bᵀ⊗A)vec(X)=(5,2,9,4) computed via the identity — matches | — |
| E1 | explain | short-answer | 0.69 | State the vectorization identity vec(AXB)=(Bᵀ⊗A)vec(X), and explain why it's useful even without a full proof. | it converts a *matrix* equation into an ordinary *linear system* in the flattened vector vec(X), letting standard linear-solver machinery be applied to problems that were originally posed in terms of matrix products *(required: the "turns a matrix equation into a linear system" payoff)* | — |
| E2 | explain | short-answer | 0.82 | Explain why (A⊗B)(C⊗D)=AC⊗BD, at least in the case where all products are defined, and why this "mixed-product property" is the single most useful Kronecker identity. | the block structure of A⊗B means each block is aᵢⱼB, and multiplying two such block matrices causes the A-blocks and B-blocks to combine independently, giving (AC)⊗(BD); it's the key identity because it lets you manipulate huge Kronecker-structured products without ever forming the full matrices *(required: names the independent-combination structure, not just states the formula)* | — |
| E3 | explain | short-answer | 0.94 | Why does the Kronecker product's size formula (mp)×(nq) for A (m×n), B (p×q) match the fact that A⊗B represents "every entry of A times all of B"? | there are mn entries of A, and each one is replaced by an entire copy of B (p×q); arranging m block-rows of n blocks each, with each block p×q, produces exactly mp rows and nq columns overall — the size follows directly from the block-replacement construction, not from any separate rule *(required: derives the size from block-counting, not just restates it)* | — |
| E4 | explain | short-answer | 1.05 | Explain why (A⊗B)⁻¹=A⁻¹⊗B⁻¹ when A and B are both invertible, using the mixed-product property. | by the mixed-product property, (A⊗B)(A⁻¹⊗B⁻¹)=(AA⁻¹)⊗(BB⁻¹)=I⊗I=I, so A⁻¹⊗B⁻¹ is exactly the inverse of A⊗B by definition — no separate derivation is needed once the mixed-product identity is available *(required: uses the mixed-product property directly, arriving at I⊗I=I)* | — |
| E5 | explain | short-answer | 1.15 | Explain in words why vec(AXB)=(Bᵀ⊗A)vec(X) lets you solve for an unknown matrix X in the equation AXB=C using ordinary linear-algebra tools, when X appears "sandwiched" between two matrices. | AXB=C is not in the form "matrix times unknown vector," so standard linear solvers don't directly apply; flattening both sides with vec turns it into (Bᵀ⊗A)vec(X)=vec(C), a completely standard linear system in the unknown vector vec(X), solvable by any method for Ax=b *(required: explains why "sandwiched" X is the obstacle vec resolves)* | — |
| T1 | transfer | short-answer | 1.19 | Spatio-temporal data often models its joint covariance as a Kronecker product of a purely-spatial covariance and a purely-temporal one. Why is multiplying two simpler structures together a natural way to build a large, structured covariance, rather than estimating the full joint covariance directly? | the Kronecker structure assumes the spatial and temporal correlation patterns act independently/separably, drastically reducing the number of parameters to estimate compared to a fully general joint covariance matrix, while still capturing genuine two-way dependence *(required: the parameter-reduction/separability argument)* | — |
| R3 | recall | short-answer | −0.76 | Fill in the blank: A⊗B is a ___ matrix built by replacing every entry of A with that entry times B. | block | — |
| R4 | recall | mcq | −0.71 | In general, A⊗B and B⊗A are: | not equal — the Kronecker product is not commutative in general | claims A⊗B always equals B⊗A, treating the Kronecker product as if it were as commutative as ordinary scalar multiplication → `kronecker-product` |
| R5 | recall | mcq | −0.66 | True or false: (A⊗B)ᵀ = Aᵀ⊗Bᵀ. | true | answers "false," missing that the transpose distributes cleanly over the Kronecker product, unlike ordinary matrix multiplication's transpose rule → `kronecker-product` |
| R6 | recall | short-answer | −0.61 | What is trace(A⊗B), in terms of trace(A) and trace(B)? | trace(A) · trace(B) | — |
| R7 | recall | mcq | −0.5 | The eigenvalues of A⊗B (for square A, B) are: | all products λᵢμⱼ of an eigenvalue λᵢ of A and an eigenvalue μⱼ of B | claims the eigenvalues of A⊗B are the sums λᵢ+μⱼ, confusing the Kronecker product's eigenvalue rule with the (different) Kronecker sum's rule → `kronecker-product` |
| R8 | recall | short-answer | −0.45 | Fill in the blank: the mixed-product property states (A⊗B)(C⊗D) = ___. | (AC)⊗(BD) | — |
| R9 | recall | mcq | −0.4 | If A is m×m and B is n×n, then A⊗B has dimensions: | (mn)×(mn) | claims A⊗B has dimensions (m+n)×(m+n), confusing block-multiplication scaling with block-concatenation → `kronecker-product` |
| R10 | recall | short-answer | −0.35 | What identity relates vec(AXB) to a Kronecker product acting on vec(X)? | vec(AXB) = (Bᵀ⊗A)vec(X) | — |
| R11 | recall | mcq | −0.3 | det(A⊗B), for A (m×m) and B (n×n), equals: | det(A)ⁿ · det(B)ᵐ | claims det(A⊗B)=det(A)·det(B) directly, omitting the size-dependent exponents the actual formula requires → `kronecker-product` |
| R12 | recall | mcq | −0.25 | True or false: A⊗B is invertible whenever A and B are both invertible. | true | answers "false," missing that (A⊗B)⁻¹=A⁻¹⊗B⁻¹ exists exactly when both factors are invertible → `kronecker-product` |
| R13 | recall | short-answer | −0.2 | What is (A⊗B)⁻¹, when A and B are both invertible? | A⁻¹⊗B⁻¹ | — |
| R14 | recall | mcq | −0.15 | Iₘ⊗Iₙ equals: | I_{mn}, the identity matrix of size mn | claims Iₘ⊗Iₙ equals I_{m+n}, applying the wrong (additive) size rule → `kronecker-product` |
| R15 | recall | short-answer | −0.1 | Why does A⊗B generally have far more entries than A and B combined? | for A (m×n) and B (p×q), A⊗B has (mp)×(nq) entries, i.e. (mn)(pq) total — the *product* of A's and B's entry counts, growing multiplicatively rather than additively | — |
| R16 | recall | mcq | −0.05 | The Kronecker product is used to build large structured covariance matrices because: | it lets a joint covariance be expressed as the product of two much smaller factor covariances, reducing the parameter count | claims it's used because it's computationally cheaper to invert any matrix via Kronecker products, a much broader (and false) claim than the actual parameter-reduction motivation → `kronecker-product` |
| A2 | apply | numeric | 0.1 | A=[[1,0],[0,2]], B=[[1,1],[1,1]]. Compute A⊗B. `[verified]` | [[1,1,0,0],[1,1,0,0],[0,0,2,2],[0,0,2,2]] | — |
| A3 | apply | numeric | 0.2 | A=[2] (1×1), B=[[3,4],[5,6]]. Compute A⊗B. `[verified: 2B]` | 2×B = [[6,8],[10,12]] | — |
| A4 | apply | numeric | 0.3 | A is 2×2, B is 3×3. What are the dimensions of A⊗B? `[verified: 6×6]` | 6×6 | — |
| A5 | apply | numeric | 0.4 | A=[[2,0],[0,3]], B=I₂. Compute trace(A⊗B). `[verified: 10]` | trace(A)×trace(B) = 5×2 = 10 | — |
| A6 | apply | numeric | 0.5 | A, B are both 2×2 invertible with det(A)=4, det(B)=5. Compute det(A⊗B). `[verified: 400]` | det(A⊗B) = det(A)²det(B)² = 16×25 = 400 | — |
| E2 | explain | short-answer | 0.79 | Why is A⊗B generally not equal to B⊗A, even though both have the same overall size? | A⊗B arranges B-sized blocks according to A's entries as the outer structure, while B⊗A arranges A-sized blocks according to B's entries as the outer structure — genuinely different arrangements of the same numbers (related by a fixed permutation of rows and columns), so the two matrices are generally not identical entry-by-entry *(required: the explicit "outer structure comes from the first factor" argument)* | — |
| E3 | explain | short-answer | 0.89 | Why does the mixed-product property make Kronecker products convenient for computing with large structured matrices? | it lets a multiplication between two huge Kronecker-structured matrices be replaced by two much smaller matrix multiplications (AC and BD), combined afterward via ⊗ — avoiding ever forming or multiplying the full-size matrices directly, a substantial computational saving *(required: names avoiding the full-size multiplication as the payoff)* | — |
| E4 | explain | derivation | 0.94 | Using the mixed-product property, show (A⊗B)(A⁻¹⊗B⁻¹) = I when A, B are invertible. | (A⊗B)(A⁻¹⊗B⁻¹) = (AA⁻¹)⊗(BB⁻¹) = I⊗I = I, applying the mixed-product property once and then using AA⁻¹=I, BB⁻¹=I *(required: the explicit mixed-product-property application, not just citing the inverse formula)* | — |
| E5 | explain | short-answer | 1.04 | Why does the eigenvalue formula for A⊗B (products λᵢμⱼ) follow from the mixed-product property? | if Av=λv and Bw=μw, then (A⊗B)(v⊗w) = (Av)⊗(Bw) = λv⊗μw = λμ(v⊗w), applying the mixed-product property; so v⊗w is an eigenvector of A⊗B with eigenvalue λμ, confirming products of eigenvalues are exactly the eigenvalues of A⊗B *(required: the explicit (Av)⊗(Bw) computation via the mixed-product property)* | — |
| T2 | transfer | short-answer | 1.29 | A spatio-temporal model assumes Cov = Σ_space ⊗ Σ_time, with Σ_space 10×10 and Σ_time 100×100. Compare the parameter count to a fully general 1000×1000 joint covariance. `[verified: ≈5105 vs ≈500,500]` | the Kronecker model needs roughly 10×11/2 + 100×101/2 ≈ 55+5050 = 5105 free parameters, versus a fully general symmetric 1000×1000 covariance needing roughly 1000×1001/2 ≈ 500,500 — nearly two orders of magnitude fewer, illustrating why the separability assumption is so valuable when reasonable | — |
| T3 | transfer | short-answer | 1.39 | Why does solving (A⊗B)x=c often exploit vec(AXB)=(Bᵀ⊗A)vec(X) instead of forming A⊗B explicitly? | reshaping x back into a matrix X and c into a matrix C lets the system be solved via ordinary matrix operations on A and B directly (e.g. a form like AXBᵀ=C) — dramatically smaller than A⊗B itself — avoiding ever materializing the full Kronecker-product matrix, which can be enormous even when A and B are individually modest in size *(required: names avoiding materializing A⊗B as the goal)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.76…1.39.*
| T2 | transfer | short-answer | 1.38 | A multi-output regression predicts several response variables at once from the same input features. Modeling the coefficient matrix as B=B₁⊗B₂ (a Kronecker-structured coefficient) rather than a fully general matrix reduces the number of free parameters. Explain why, and what assumption this imposes on the relationships between outputs. | a general coefficient matrix mapping p features to q outputs has pq free parameters, while a Kronecker-structured B=B₁⊗B₂ needs only as many parameters as B₁ and B₂ combined (far fewer for large p,q); this imposes a separability assumption — the way features relate to outputs must factor into an independent "feature effect" and "output effect," rather than allowing arbitrary feature-output interactions *(required: quantifies the parameter reduction and names the separability assumption)* | — |
| T3 | transfer | short-answer | 1.55 | Multilevel (hierarchical) models often assume the covariance across (subject, timepoint) pairs is Σ_subject⊗Σ_time. Using the mixed-product property, explain why this makes inverting the full covariance matrix dramatically cheaper than inverting a general covariance of the same size. | since (A⊗B)⁻¹=A⁻¹⊗B⁻¹, inverting the full Kronecker-structured covariance reduces to separately inverting Σ_subject and Σ_time, each far smaller than the full matrix; a general covariance of the same overall size would require inverting the whole large matrix directly, at much higher computational cost *(required: connects the (A⊗B)⁻¹=A⁻¹⊗B⁻¹ identity to the cost reduction)* | — |
| T4 | transfer | short-answer | 1.75 | Some deep learning layers replace a full weight matrix W with a Kronecker-factored W=W₁⊗W₂ to reduce memory and compute. Using the size formula from R2, explain why this changes both how many parameters the layer stores and how a forward pass Wx should actually be computed for efficiency. | if W₁ is m×n and W₂ is p×q, W=W₁⊗W₂ has size (mp)×(nq) but is stored using only the mn+pq entries of the two factors, far fewer than the (mp)(nq) entries a dense W of that size would need; a forward pass should exploit the vec identity to compute Wx via reshaping x into a matrix and applying W₁ and W₂ separately, rather than ever materializing the full (mp)×(nq) matrix *(required: connects parameter savings to avoiding materializing the full product during the forward pass)* | — |

*Coverage: 20 items, −0.81…1.75.*

---

## Matrix Norms (`matrix-norms`)
*Prereq: Vector Norm · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Define the Frobenius norm. | ‖A‖_F = √(Σᵢⱼ Aᵢⱼ²) — the vector norm applied after flattening A into a single long vector | — |
| R2 | recall | mcq | −0.45 | The operator (spectral) norm ‖A‖₂ is defined as: | max over nonzero x of ‖Ax‖/‖x‖ | picks "the largest entry of A" — a different, much cruder quantity → `matrix-norms` |
| R3 | recall | short-answer | −0.28 | State the submultiplicative property that both the Frobenius norm and the operator norm satisfy for compatible matrices A, B. | ‖AB‖ ≤ ‖A‖‖B‖ — the norm of a product is bounded by the product of the norms, for either norm | — |
| R4 | recall | mcq | −0.08 | The nuclear norm ‖A‖_* is defined as: | the sum of A's singular values, Σᵢσᵢ | confuses it with the Frobenius norm, which sums squared singular values before a square root, not the singular values directly → `matrix-norms` |
| A1 | apply | numeric | 0.1 | A=diag(3,4). Compute ‖A‖_F. `[verified: 5]` | √(9+16)=5 | — |
| A2 | apply | short-answer | 0.2 | Same A=diag(3,4): what is the operator norm ‖A‖₂? `[verified: 4]` | 4 — the largest diagonal entry, since A stretches the (0,1) direction by exactly 4, the maximum possible stretch for this diagonal matrix | reports the operator norm as equal to the Frobenius norm (5), not distinguishing the two → `matrix-norms` |
| A3 | apply | numeric | 0.32 | A=[[1,2],[2,1]]. Compute ‖A‖_F. `[verified: √10≈3.16]` | ‖A‖_F=√(1+4+4+1)=√10≈3.16 | — |
| A4 | apply | numeric | 0.44 | A=[[1,2],[2,1]] (symmetric, eigenvalues 3 and −1). Compute ‖A‖₂. `[verified: 3]` | ‖A‖₂=max(abs(eigenvalue))=3, since for a symmetric matrix the operator norm equals the largest absolute eigenvalue | assumes ‖A‖₂ equals the largest eigenvalue even when it's negative and larger in magnitude the other way, without taking the absolute value → `matrix-norms` |
| A5 | apply | numeric | 0.56 | A=[[2,0,0],[0,1,0],[0,0,3]]. Compute ‖A‖_F and ‖A‖₂. `[verified: F=√14≈3.74, op=3]` | ‖A‖_F=√(4+1+9)=√14≈3.74; ‖A‖₂=3, the largest diagonal entry (the largest singular value for a diagonal matrix) | — |
| A6 | apply | numeric | 0.66 | A=[[1,2],[3,4]], B=[[0,1],[1,0]]. Compute ‖AB‖₂ and ‖A‖₂‖B‖₂; confirm submultiplicativity holds. `[verified: ‖AB‖₂≈5.46, ‖A‖₂‖B‖₂≈5.46]` | ‖AB‖₂≈5.46, and ‖A‖₂‖B‖₂≈5.46 — here they coincide to the precision shown, still consistent with ‖AB‖≤‖A‖‖B‖ | — |
| A7 | apply | numeric | 0.74 | A=I₂, B=[[0,1],[1,0]] (a swap). Compute ‖A+B‖_F and confirm it's less than ‖A‖_F+‖B‖_F, illustrating the triangle inequality. `[verified: ‖A+B‖_F=2, ‖A‖_F+‖B‖_F=2√2≈2.83]` | ‖A+B‖_F=2 < ‖A‖_F+‖B‖_F=2√2≈2.83 — the triangle inequality holds with room to spare here | — |
| E1 | explain | short-answer | 0.8 | Why do Frobenius and operator norms give *different* numbers for the same matrix, as A1/A2 show (5 vs 4)? | Frobenius treats the matrix as one long vector of entries — a "total energy" measure across every direction at once; the operator norm asks only about the single worst-case direction — these are fundamentally different quantities, and the largest singular value (a preview of SVD) turns out to equal the operator norm exactly *(required: the "total energy vs. worst-case direction" distinction)* | — |
| E2 | explain | short-answer | 0.92 | Explain why ‖A‖₂ (the operator norm) is always less than or equal to ‖A‖_F (the Frobenius norm), for any matrix A. | ‖A‖₂ picks out the single largest singular value, while ‖A‖_F equals the square root of the *sum of squares* of all the singular values; since one term of a sum of nonnegative squares can never exceed the square root of the whole sum (unless there's only one nonzero singular value), ‖A‖₂≤‖A‖_F always *(required: connects both norms to singular values and compares "max term" to "sum of squares")* | — |
| E3 | explain | short-answer | 1.02 | Explain why the operator norm ‖A‖₂ equals A's largest singular value, rather than its largest eigenvalue, in general. | ‖A‖₂=max‖x‖=1‖Ax‖ measures the largest possible stretch factor A can apply to *any* unit vector, which is exactly what the singular values (from the SVD) are built to capture; eigenvalues only describe stretching along A's own eigenvectors and can even be complex or undefined for non-square A, so they coincide with the operator norm's defining quantity only in the special case of a symmetric (or normal) matrix *(required: distinguishes singular values as "worst-case stretch over all directions" from eigenvalues)* | — |
| E4 | explain | short-answer | 1.12 | Why is the nuclear norm ‖A‖_*=Σσᵢ used as a convex relaxation of rank(A) in low-rank matrix recovery, rather than using rank directly? | rank counts *how many* singular values are nonzero, a discrete, non-convex quantity that makes minimizing rank subject to constraints computationally intractable in general; the nuclear norm sums the actual singular values instead, giving a convex function that still favors solutions with many singular values at (or near) zero — the matrix analogue of using the ℓ1 norm as a convex relaxation of counting nonzero entries *(required: draws the explicit analogy to ℓ1-vs-count sparsity)* | — |
| E5 | explain | short-answer | 1.22 | Two weight matrices in a neural network have the same Frobenius norm but very different operator norms. Explain what this discrepancy implies about how each matrix could affect the stability of a forward pass through many layers. | Frobenius norm only reflects the "total energy" spread across all entries and gives no guarantee about any single direction; a matrix with a large operator norm relative to its Frobenius norm can still stretch some particular input direction a great deal, and composing many such layers can cause activations to blow up exponentially even though each layer's Frobenius norm looks moderate *(required: connects the norm discrepancy to compounding instability across layers)* | — |
| T1 | transfer | short-answer | 1.3 | An iterative algorithm stops when ‖A_new−A_old‖ is small. Why might the wrong norm choice give a misleadingly early "converged" signal? | a small Frobenius-norm change means every individual entry has moved only a little on average, but that doesn't guarantee the matrix's *worst-case* behavior (its operator norm, governing how much it can stretch some vector) has also stabilized — a few entries could still be changing in a direction that matters a great deal *(required: connects the two norms' different meanings to a concrete convergence-monitoring failure)* | — |
| R3 | recall | short-answer | −0.65 | Fill in the blank: by definition of the operator norm, ‖Ax‖ ≤ ___. | ‖A‖‖x‖ | — |
| R4 | recall | mcq | −0.6 | Matrix norms satisfy the same three axioms as vector norms: | nonnegativity/definiteness, homogeneity, and the triangle inequality | claims matrix norms additionally require commutativity with matrix multiplication, an extra condition not part of the axioms → `matrix-norms` |
| R5 | recall | mcq | −0.55 | True or false: ‖AB‖ ≤ ‖A‖‖B‖ (submultiplicativity) holds for both the Frobenius and operator norms. | true | answers "false," missing that submultiplicativity holds for both norms discussed here → `matrix-norms` |
| R6 | recall | numeric | −0.5 | What is ‖I‖₂, the operator norm of the identity matrix? `[verified: 1]` | 1 | — |
| R7 | recall | mcq | −0.4 | The operator (spectral) norm ‖A‖₂ equals: | the largest singular value of A | claims it equals the largest eigenvalue of A, which is only correct for certain matrices (e.g. symmetric PSD) and not the general definition → `matrix-norms` |
| R8 | recall | short-answer | −0.35 | Fill in the blank: ‖A‖_F is computed by treating A as one long ___ and applying the ordinary Euclidean vector norm. | vector | — |
| R9 | recall | mcq | −0.3 | Which inequality relates the Frobenius and operator norms of the same matrix? | ‖A‖₂ ≤ ‖A‖_F | claims ‖A‖_F ≤ ‖A‖₂, reversing the actual inequality E2 proves | — |
| R10 | recall | short-answer | −0.25 | What does "submultiplicative" mean for a matrix norm? | ‖AB‖ ≤ ‖A‖‖B‖ — the norm of a product is bounded by the product of the norms | — |
| R11 | recall | numeric | −0.2 | What is ‖Iₙ‖_F, the Frobenius norm of the n×n identity matrix? `[verified: √n]` | √n | — |
| R12 | recall | mcq | −0.15 | True or false: the Frobenius norm is an "entrywise" norm, while the operator norm is an "induced" norm. | true | answers "false," missing this categorical distinction between the two norms → `matrix-norms` |
| R13 | recall | short-answer | −0.1 | What role does ‖A‖ play in bounding how much A can stretch any vector x? | ‖Ax‖ ≤ ‖A‖‖x‖ holds for every x, so ‖A‖ is the tightest possible such bound — the largest possible stretching factor | — |
| R14 | recall | mcq | −0.05 | A matrix's condition number is typically defined using: | the operator norm, as ‖A‖₂‖A⁻¹‖₂ | claims the condition number is defined using the Frobenius norm instead, missing that the operator norm's worst-case-stretch interpretation is what condition number needs → `matrix-norms` |
| R15 | recall | short-answer | 0.0 | Why is the operator norm called an "induced" norm? | it is defined directly from the underlying vector norm, via max over nonzero x of ‖Ax‖/‖x‖, rather than being defined independently by a separate formula on the matrix's entries | — |
| R16 | recall | mcq | 0.05 | Which norm registers a large change if a single entry of A is changed drastically, even if every other entry stays the same? | the Frobenius norm — since it sums every entry's squared value directly | claims the operator norm would react more strongly to a single entry's change, when in fact it depends on the whole structure and may barely move for an isolated entry change → `matrix-norms` |
| A3 | apply | numeric | 0.3 | A=[[0,2],[2,0]]. Compute ‖A‖_F. `[verified: 2√2 ≈ 2.828]` | √(0+4+4+0) = √8 = 2√2 | — |
| A4 | apply | numeric | 0.4 | A=3I₃ (3 times the 3×3 identity). Compute ‖A‖₂ and ‖A‖_F. `[verified: 3 and 3√3 ≈ 5.196]` | ‖A‖₂=3 (every singular value is 3); ‖A‖_F=√(9+9+9)=√27=3√3 | — |
| A5 | apply | numeric | 0.5 | A=[[1,0],[0,0]] (a rank-1 projection). Compute ‖A‖₂ and ‖A‖_F. `[verified: both 1]` | singular values are 1 and 0, so ‖A‖₂=1; ‖A‖_F=√(1²)=1 — equal here, consistent with E2's equality condition for rank ≤ 1 | — |
| A6 | apply | numeric | 0.6 | A=diag(2,3,6). Verify submultiplicativity ‖A²‖₂ ≤ ‖A‖₂² using the operator norm. `[verified: 36 ≤ 36]` | ‖A‖₂=6 (largest diagonal entry); A²=diag(4,9,36), so ‖A²‖₂=36; ‖A‖₂²=36 — equality holds here | — |
| E2 | explain | short-answer | 0.9 | Why does ‖A‖₂ ≤ ‖A‖_F always hold? | ‖A‖₂ picks out only the single largest singular value of A, while ‖A‖_F² equals the sum of the squares of *all* singular values; since the largest singular value squared can be at most the sum of all singular values squared (the rest being nonnegative), ‖A‖₂ ≤ ‖A‖_F follows, with equality exactly when A has rank ≤ 1 *(required: names the "largest term vs. sum of all terms" argument and the rank-1 equality condition)* | — |
| E3 | explain | short-answer | 1.0 | Why is the operator norm particularly meaningful for measuring how much a matrix can distort a vector, compared to the Frobenius norm? | the operator norm is defined as exactly the worst-case stretching factor max‖Ax‖/‖x‖, so it directly answers "how much can this transformation amplify any single input vector," while the Frobenius norm aggregates information about all of A's entries at once without a direct worst-case-stretching interpretation *(required: the explicit worst-case-stretch framing)* | — |
| E4 | explain | derivation | 1.1 | Verify ‖Iₙ‖_F=√n and ‖Iₙ‖₂=1 are consistent with ‖A‖₂≤‖A‖_F. `[verified: 1 ≤ √n for n≥1]` | √n ≥ 1 for every n≥1, so ‖Iₙ‖₂=1 ≤ √n=‖Iₙ‖_F is indeed satisfied; the gap between them grows with n, illustrating the two norms can diverge substantially for larger matrices even here *(required: the explicit numeric check confirming the inequality)* | — |
| E5 | explain | short-answer | 1.2 | Why does submultiplicativity matter for bounding the error introduced by composing multiple linear transformations (e.g. neural network layers)? | if each layer's weight matrix has operator norm at most c, the composed transformation through k layers has operator norm at most cᵏ by repeated submultiplicativity — a concrete, provable bound on how much the whole network can amplify its input (or an error in it), exactly the kind of guarantee used in network stability and robustness analyses *(required: the explicit cᵏ bound from repeated application)* | — |
| T2 | transfer | short-answer | 1.4 | Weight clipping in some GAN training methods constrains each layer's weight matrix to ‖W‖₂ ≤ c. Why does bounding the operator norm, specifically, control the network's overall Lipschitz constant? | the network's overall sensitivity to input perturbations is bounded by the product of each layer's operator norm, per E5's submultiplicativity argument, so capping every individual ‖W‖₂ at c directly caps the network's end-to-end Lipschitz constant at roughly c raised to the number of layers — a guarantee the Frobenius norm would not give as tightly, since it doesn't directly bound worst-case stretching *(required: connects the per-layer cap to the end-to-end Lipschitz bound via submultiplicativity)* | — |
| T3 | transfer | short-answer | 1.5 | A numerically unstable system Ax=b has a large condition number κ(A)=‖A‖₂‖A⁻¹‖₂. Using R13's interpretation, what does a large κ(A) mean for how errors in b propagate to errors in x? | ‖A‖₂ bounds how much A can stretch any vector, while ‖A⁻¹‖₂ bounds how much A⁻¹ can stretch any vector; their product bounds how much a relative error in b can be amplified into a relative error in x=A⁻¹b — a large κ(A) means some directions are stretched enormously by A while others barely move, so small perturbations in b can translate into wildly disproportionate errors in x *(required: connects κ(A) as a product of two worst-case stretch factors to error amplification)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.65…1.5.*
| T2 | transfer | short-answer | 1.45 | Weight decay (L2 regularization) penalizes the Frobenius norm of a network's weight matrices, ‖W‖²_F. Explain why this doesn't directly control the operator norm ‖W‖₂, and why that gap matters for guaranteeing Lipschitz-bounded (stable) network behavior. | penalizing ‖W‖_F only discourages the *total* size of all entries combined, and E2 showed ‖W‖₂≤‖W‖_F is the only guaranteed relationship — a matrix can have small Frobenius norm while still having an operator norm close to it (e.g. a near-rank-1 matrix), so weight decay gives no tight control over the network's worst-case stretch factor, which is what actually bounds how much a small input perturbation can be amplified *(required: uses the E2 inequality direction to explain the lack of tight control)* | — |
| T3 | transfer | short-answer | 1.62 | An iterative algorithm monitors convergence via ‖A_new−A_old‖_F < ε and declares convergence early. A practitioner switches the check to the operator norm and finds the algorithm needs many more iterations to "converge." Explain what this reveals about the algorithm's actual behavior. | since ‖A‖₂≤‖A‖_F always (E2), a small Frobenius-norm change can mask a still-significant operator-norm change — meaning some direction of the matrix was still shifting by a meaningful amount even while the entrywise-averaged Frobenius change looked small; the Frobenius-based stopping criterion was declaring victory before the matrix's worst-case behavior had actually stabilized *(required: uses the E2 inequality to explain how Frobenius convergence can mask operator-norm instability)* | — |
| T4 | transfer | short-answer | 1.82 | In low-rank matrix completion (e.g. recommender systems filling in missing ratings), reconstruction error is often reported using the Frobenius norm even though users may care most about a few large, visible errors. Explain the mismatch, and why an operator-norm-based bound would tell a different story. | Frobenius error averages the squared error across every entry, so it can be small overall even if a handful of highly visible entries (e.g. a popular item's rating) are badly wrong, as long as most entries are predicted well; an operator-norm bound instead controls the worst-case behavior of the error matrix as an operator, which is more sensitive to a few large, structured errors than to many small scattered ones — the two norms answer genuinely different questions about "how wrong" a reconstruction is *(required: explains why Frobenius can hide a few large errors that operator norm would flag)* | — |

*Coverage: 20 items, −0.7…1.82.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| matrix multiplication assumed commutative | `matrix-multiplication` |
| domain/codomain of an m×n matrix reversed | `matrices` |
| square matrix assumed to map to a different space than it maps from | `matrices` |
| trace(AB)=trace(BA) doubted despite AB≠BA | `trace` |
| transpose assumed to change the trace | `trace` |
| any reordering inside trace (not just cyclic) assumed safe | `trace` |
| "cyclic" conflated with "any permutation allowed" | `trace` |
| small trace(Hessian) assumed to always mean a flat, well-generalizing minimum | `trace` |
| affine maps (with a constant shift) mistaken for linear | `linear-transformations` |
| injective linear map assumed to still allow a nontrivial kernel | `linear-transformations` |
| a formula with no added constant assumed automatically linear (products of variables missed) | `linear-transformations` |
| gradient direction (ascent vs descent) reversed | `matrix-calculus` |
| Jacobian chain rule multiplied in the wrong order | `matrix-calculus` |
| near-zero gradient assumed to always signal a good (near-minimum) solution | `matrix-calculus` |
| Kronecker product dimensions computed by addition, not multiplication | `kronecker-product` |
| Kronecker product assumed commutative | `kronecker-product` |
| trace(A⊗B) assumed additive rather than multiplicative | `kronecker-product` |
| Frobenius and operator norms conflated | `matrix-norms` |
| transpose-of-a-product order reversed, i.e. (AB)ᵀ treated as AᵀBᵀ | `matrix-multiplication` |
| squareness alone assumed sufficient for symmetry | `matrices` |
| trace(cA) scaled by c² instead of c | `trace` |
| T(0)=0 dropped as a required consequence of homogeneity | `linear-transformations` |
| Hessian sign-definiteness conclusions (min vs max) swapped | `matrix-calculus` |
| Kronecker product assumed commutative | `kronecker-product` |
| condition number's worst-case-stretch reading applied to the wrong norm | `matrix-norms` |

**Cluster total: 210 items across 7 concepts, 30 per concept.** All numeric claims verified by
script, including the non-trivial trace(AB)=trace(BA)=69 identity holding despite AB≠BA as matrices,
the gradient of a quadratic form checked by direct partial differentiation against the general 2Ax
formula, the Kronecker determinant/trace identities, and the Frobenius-vs-operator-norm inequality
confirmed on both a rank-1 and a scaled-identity example.
| nuclear norm confused with the Frobenius norm | `matrix-norms` |
| operator norm equated with the largest eigenvalue without taking absolute value | `matrix-norms` |

**Cluster total: 140 items across 7 concepts.** All numeric claims verified by script (NumPy), including
the non-trivial trace(AB)=trace(BA)=69 identity holding despite AB≠BA as matrices, the gradient of a
quadratic form checked by direct partial differentiation against the general 2Ax formula, the
trace(ABC)=trace(BCA)=3≠trace(ACB)=5 cyclic-vs-noncyclic distinction, the Kronecker mixed-product and
vec(AXB)=(Bᵀ⊗A)vec(X) identities, and every Frobenius/operator/nuclear-norm example.
