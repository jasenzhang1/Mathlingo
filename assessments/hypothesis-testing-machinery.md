# Cluster 10 — Hypothesis Testing Machinery

Sampling Distribution → Confidence Interval (9 concepts). Same format as
[Cluster 1](foundations-of-probability.md).

**Scope note.** `sampling-distribution`'s official prerequisite includes `central-limit-theorem`,
which lives in the `multivariate-probability` domain and is out of scope for this probability+statistics
sweep. Its content is folded into `sampling-distribution` directly below rather than assumed — the
CLT's statement is this concept's own central fact, not borrowed machinery.

This cluster carries the two most consequential misinterpretations in applied statistics —
p-values are not P(H₀ true), and a 95% CI is not "95% probability the parameter is in this interval"
— and it deliberately builds toward `hypothesis-test`'s multiple-comparisons item and `power`'s
capstone, both of which require every earlier concept in the cluster at once.

---

## Sampling Distribution (`sampling-distribution`)
*Prereq: Sample Variance, Central Limit Theorem · ancestors 21 · b₀ = 1.05*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.05 | State the Central Limit Theorem's conclusion about X̄'s sampling distribution for large n. | X̄ is approximately Normal(μ, σ²/n) for large n, **regardless of the population's original shape**, provided it has finite variance | — |
| R2 | recall | mcq | 0.35 | The remarkable, universal part of CLT is that it: | works for *any* population distribution with finite variance | claims CLT "only works if the population is already Normal" — missing the entire point of the theorem → `sampling-distribution` |
| A1 | apply | short-answer | 0.85 | A population is heavily right-skewed. Is X̄'s sampling distribution likely Normal-looking at n=5? At n=200? | at n=5, likely not yet — the original skewness persists strongly for tiny samples; at n=200, much closer to Normal per CLT — the *rate* of convergence depends on how non-normal the population is *(required: both regimes, not just "large n is better")* | — |
| A2 | apply | numeric | 1.0 | A population (shape unspecified) has mean 500, SD 100. For n=100, state X̄'s approximate sampling distribution and use it to approximate P(X̄>520). `[verified: 0.0228]` | X̄ ≈ Normal(500, 100/√100=10); P(X̄>520) = P(Z>2) ≈ 0.0228 — the identical Z=2 tail probability from `normal-distribution`'s A1 in Cluster 4 | forgets to divide σ by √n before standardizing → `sampling-distribution` |
| E1 | explain | short-answer | 1.55 | Explain why CLT is such a practically powerful result — connect to how rarely we actually know a population's true shape. | in real applications the population's shape is almost never known, yet CLT licenses Normal-based methods for the sample mean anyway, as long as n is reasonably large — arguably the single most practically important theorem in applied statistics *(required)* | — |
| E2 | explain | short-answer | 1.7 | Why does the rate of convergence to normality depend on the population's shape? | heavily skewed or heavy-tailed populations need larger n before X̄'s distribution looks convincingly Normal, while an already-symmetric population's X̄ looks Normal almost immediately, even at small n *(required, connecting back to A1)* | — |
| T1 | transfer | short-answer | 2.05 | Customer satisfaction scores are heavily skewed (most 9–10, a long left tail of 1–2s) — clearly not Normal. Is a Normal-based 95% CI for the *true average* score justified with n=500? | yes — CLT applies to the *sampling distribution of X̄*, not to the raw data's distribution; even with heavily skewed individual scores, X̄'s own distribution is close to Normal at n=500, making Normal-based CI methods for the mean valid despite the raw data being nowhere close to Normal itself *(required: explicitly distinguishes the data's distribution from X̄'s distribution)* — the single most common point of confusion about CLT | concludes Normal-based methods are invalid because the raw data isn't Normal → `sampling-distribution` |
| R3 | recall | short-answer | −1.4 | State the two parameters of X̄'s approximate CLT sampling distribution, in terms of the population's μ and σ. | mean μ, variance σ²/n (so X̄ ≈ Normal(μ, σ²/n)) | — |
| R4 | recall | mcq | −1.25 | The Central Limit Theorem describes the sampling distribution of: | the sample mean X̄ | describes it as "the distribution of a single random observation Xᵢ," confusing CLT's actual subject (X̄) with the raw population distribution → `sampling-distribution` |
| R5 | recall | short-answer | −1.15 | True or false: CLT requires the population distribution to already be approximately Normal for it to apply. | false — CLT's whole point is that it works for *any* population shape with finite variance, given a large enough n | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: CLT requires the population to have a finite ____, or the theorem's conclusion can fail. | variance | — |
| R7 | recall | mcq | −0.95 | Which condition is required for CLT's approximate-normality conclusion to apply to X̄? | a sufficiently large sample size n | claims CLT requires the sample to be drawn without replacement, an unrelated condition not part of the theorem's requirements → `sampling-distribution` |
| R8 | recall | short-answer | −0.85 | Define "sampling distribution" in general (not specific to X̄). | the probability distribution of a statistic (any function of the sample data) across hypothetically repeated samples of the same size from the same population | — |
| R9 | recall | mcq | −0.75 | For a population that is *already* exactly Normal, X̄'s sampling distribution is: | exactly Normal for every n, including small n | claims X̄'s distribution is only "approximately" Normal in this case too, missing that Normal population data makes X̄ Normal *exactly*, at any n, with no CLT approximation needed → `sampling-distribution` |
| R10 | recall | short-answer | −0.65 | True or false: a larger n always brings X̄'s sampling distribution closer to Normal, regardless of the population's shape. | true — this is the general convergence CLT guarantees, though the *rate* of convergence depends on the population's shape | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: the practical rule of thumb sometimes used for "n large enough" for CLT to kick in for a moderately-skewed population is n ≥ ____. | 30 (a common rule of thumb, though the true required n depends on how skewed the population is) | — |
| R12 | recall | mcq | −0.45 | Which population shape would you expect to need the *largest* n before X̄ looks convincingly Normal? | a heavily skewed, heavy-tailed population | picks "a population that is already symmetric and Normal," which needs the smallest n (effectively n=1), not the largest → `sampling-distribution` |
| R13 | recall | short-answer | −0.35 | State what CLT says happens to the *shape* of X̄'s distribution as n grows, independent of the population's original shape. | it becomes increasingly close to a Normal (bell-shaped) curve | — |
| R14 | recall | mcq | −0.25 | CLT is sometimes called "remarkable" specifically because: | its conclusion (approximate normality of X̄) holds regardless of the population's own distributional shape | calls it remarkable because "it lets you compute the population mean exactly," which CLT does not do — it only describes X̄'s sampling distribution shape, not the value of μ → `sampling-distribution` |
| R15 | recall | short-answer | −0.15 | True or false: CLT applies only to means, never to sums of iid random variables. | false — since a sum is just n×X̄, CLT equivalently implies sums of iid random variables are also approximately Normal for large n | — |
| R16 | recall | short-answer | 0.0 | Fill in the blank: CLT's conclusion about X̄'s approximate distribution shape is Normal, regardless of the population's own shape, as long as n is ____ and the population variance is finite. | large (enough) | — |
| A3 | apply | short-answer | 0.9 | A population is bimodal (two separate humps, e.g. heights of a mixed adult/child population). Would you expect X̄'s sampling distribution at n=10 to already look unimodal and bell-shaped? | not necessarily — for a strongly bimodal population, small n may still show visible traces of that bimodality in X̄'s distribution; CLT's convergence isn't instantaneous, and how quickly it "kicks in" depends on how far the population's shape is from Normal to begin with, per A1/E2's rate argument | — |
| A4 | apply | numeric | 0.95 | A population has mean 40, SD 8. For n=64, state X̄'s approximate sampling distribution and use it to approximate P(X̄<38). `[verified: 0.0228]` | X̄ ≈ Normal(40, 8/√64=1); P(X̄<38) = P(Z<−2) ≈ 0.0228 | forgets to divide σ by √n, using SD=8 directly instead of SE=1 → `sampling-distribution` |
| A5 | apply | short-answer | 1.0 | A survey collects only n=8 responses from a population believed to be roughly symmetric (not skewed). Is Normal-based reasoning about X̄ likely reasonable here despite the small n? | plausibly yes — since the population itself is already close to symmetric, X̄'s sampling distribution converges to Normal quickly, so even a small n like 8 may already be a reasonable approximation, unlike the heavily-skewed case in A1 which needed much larger n | — |
| A6 | apply | numeric | 1.05 | A population has mean 100, SD 15, n=225. Approximate P(97 < X̄ < 103). `[verified: ≈0.9973]` | SE=15/√225=1; X̄ ≈ Normal(100,1); P(97<X̄<103)=P(−3<Z<3)≈0.9973 | — |
| E3 | explain | short-answer | 1.75 | Explain why CLT is considered a statement about the *shape* of a distribution converging, rather than a statement that X̄ becomes a constant as n grows. | X̄'s variance (σ²/n) does shrink toward 0 as n grows (so X̄ concentrates near μ), but CLT's specific content is about the *shape* the remaining, shrinking spread of X̄ takes — namely, increasingly Normal — a separate claim from the fact (from `sample-mean`) that the spread itself is shrinking *(required: distinguishes the shrinking-spread fact from the shape-convergence claim)* | — |
| E4 | explain | short-answer | 1.8 | Explain why CLT is essential for constructing confidence intervals and hypothesis tests on μ even when the underlying population's distribution is completely unknown. | Normal-based inference (Z or t procedures) requires knowing X̄'s sampling distribution; without CLT, this would only be possible if the population's exact distribution were known and Normal — CLT guarantees the needed approximate normality of X̄ regardless, which is what makes standard inference procedures broadly applicable in practice *(required)* | — |
| E5 | explain | short-answer | 1.85 | Contrast what CLT says about a *sum* of n iid variables versus what it says about their *mean*, and explain why both statements carry the same essential content. | a sum ΣXᵢ = nX̄ is just a rescaled version of X̄, so if X̄ is approximately Normal(μ,σ²/n), the sum is approximately Normal(nμ, nσ²) — the same convergence result, restated in different units; neither is more fundamental than the other, since a linear rescaling of a Normal random variable is still Normal *(required: the explicit rescaling argument)* | — |
| T2 | transfer | short-answer | 2.1 | A manufacturing process's individual part measurements are known to be strongly right-skewed (occasional very large defects). Quality control averages batches of n=4 parts to make pass/fail decisions using a Normal-based rule. Critique this practice. | at n=4, CLT's convergence to normality is unlikely to be far enough along for a strongly skewed population — this is exactly A1's "n=5, likely not yet" regime; a Normal-based rule built on an assumption of approximate normality for X̄ could be substantially miscalibrated at such a small n, and the team should either use a much larger batch size or a non-Normal-based method *(required: explicitly identifies n=4 as too small given the skew, echoing A1's reasoning)* | assumes CLT justifies Normal-based methods at any n, regardless of how skewed the underlying population is or how small the sample → `sampling-distribution` |
| T3 | transfer | numeric | 2.2 | Daily website error counts are Poisson-distributed with rate λ=9 (mean 9, variance 9). For a week's worth of daily counts (n=7), approximate P(X̄>10.5) using CLT. `[verified: ≈0.0929]` | SE=√(9/7)≈1.134; Z=(10.5−9)/1.134≈1.323; P(Z>1.323)≈0.093 — CLT applies even though the underlying Poisson population is discrete and only approximately symmetric, illustrating the theorem's reach beyond continuous, obviously-skewed textbook examples *(required: sets up the Normal approximation to X̄ correctly using √(λ/n) as SE)* | assumes CLT cannot apply because the underlying population (Poisson) is discrete rather than continuous → `sampling-distribution` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Standard Error (`standard-error`)
*Prereq: Sampling Distribution · ancestors 22 · b₀ = 1.07*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.07 | Define SE(X̄), distinguishing it clearly from the SD of the raw data. | SE(X̄)=σ/√n is the SD of X̄'s *sampling* distribution — how much X̄ itself varies across repeated samples — not σ itself, the spread of individual data points | — |
| R2 | recall | mcq | 0.37 | When σ is unknown (the usual case), the estimated SE of X̄ is: | s/√n, plugging in the sample SD | uses s with no n-adjustment at all, treating SE as identical to SD → `standard-error` |
| A1 | apply | numeric | 0.87 | A sample of n=64 has s=16. Find the estimated SE of X̄. `[verified: 2]` | SE ≈ 16/√64 = 2 | — |
| A2 | apply | short-answer | 1.0 | "Average commute is 25 minutes (SD=10, n=100)." A reader uses the *full SD* (10) instead of the SE for a margin of error. Compute both and explain the consequence. `[verified: SD=10, SE=1]` | SD=10 describes individual commute-time spread; SE=10/√100=1 describes how much the *sample mean itself* would vary across repeated studies; using 10 instead of 1 makes the resulting interval **ten times too wide**, badly overstating uncertainty about the true average *(required: the explicit 10× factor)* | — |
| E1 | explain | short-answer | 1.57 | Precisely distinguish the two questions SD and SE each answer. | SD answers "how spread out is a typical single data point?"; SE answers "how much would *my estimate of the mean* vary if I repeated this whole study?" — confusing them either drastically overstates or understates the precision of an estimate *(required: both questions stated explicitly)* | — |
| E2 | explain | short-answer | 1.7 | Why does SE shrink as n grows while SD does not? | SE=σ/√n depends on n directly; SD is a fixed property of the population, entirely unrelated to how large a sample happens to be taken — exactly `sample-mean`'s earlier 1/√n scaling result, now named as SE specifically *(required)* | — |
| T1 | transfer | short-answer | 2.07 | A study reports "SD=15 mmHg" from n=1000 patients. A journalist writes "very precise — patients vary by only about 15 points." Is this interpretation of what 15 mmHg *measures* correct? What quantity actually describes the precision of the estimated *average*? `[verified: SE≈0.474]` | the journalist correctly describes individual patient variability (SD), which has nothing to do with sample size or estimation precision; the actual precision of the estimated average is the SE, 15/√1000≈0.47 mmHg — much smaller, reflecting that the average is known very precisely even though individuals vary a lot *(required: computes the actual SE and contrasts it with SD)* | conflates "the study is precise" (about the mean) with "individuals don't vary much" (about SD) → `standard-error` |
| R3 | recall | short-answer | −1.4 | State the formula for SE(X̄) when σ is known. | SE(X̄) = σ/√n | — |
| R4 | recall | mcq | −1.25 | The "standard error" is, by definition, a standard deviation of: | a statistic's sampling distribution (e.g. X̄'s) | defines it as "the standard deviation of the raw population data," which is just σ, not SE → `standard-error` |
| R5 | recall | short-answer | −1.15 | True or false: SE and SD are measured in the same units as the original data. | true — both SE and SD carry the same units as the raw measurements (e.g. minutes, mmHg), unlike variance, which is in squared units | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: when σ is unknown, the estimated SE of X̄ uses ____ in place of σ. | s (the sample standard deviation) | — |
| R7 | recall | mcq | −0.95 | Which of these decreases as n increases? | SE, but not SD | claims both SE and SD decrease as n grows, missing that SD is a fixed population property unrelated to sample size → `standard-error` |
| R8 | recall | short-answer | −0.85 | Define "margin of error" in relation to SE. | typically, margin of error = (a critical value) × SE — a multiple of the standard error used to build an interval around a point estimate | — |
| R9 | recall | mcq | −0.75 | Estimated SE using s=20 and n=100 is: | 2 | computes 20/100=0.2, forgetting to take the square root of n before dividing → `standard-error` |
| R10 | recall | short-answer | −0.65 | True or false: SE can be computed even when only a sample (not the full population) is available. | true — the *estimated* SE (s/√n) uses only sample quantities, which is exactly why it's the practically useful version | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: SE stands for ____. | standard error (of the mean, or of whatever statistic is being estimated) | — |
| R12 | recall | mcq | −0.45 | For n=1, SE(X̄) equals: | SD itself (σ/√1 = σ) | claims SE(X̄) is always strictly smaller than SD, missing that at n=1 they are exactly equal → `standard-error` |
| R13 | recall | short-answer | −0.35 | State what happens to SE as n → ∞. | SE → 0 — the estimate of the mean becomes arbitrarily precise as sample size grows without bound | — |
| R14 | recall | mcq | −0.25 | Which of these is a correct verbal description of what SE measures? | how much the estimate of the mean would vary across repeated samples of the same size | describes SE as "the typical distance of one data point from the mean," which is what SD measures, not SE → `standard-error` |
| R15 | recall | short-answer | −0.15 | True or false: two studies with identical SD but different n will have identical SE. | false — SE=SD/√n, so studies with the same SD but different n have different SE (the larger-n study has smaller SE) | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: SE is used to quantify the precision of an ____, while SD quantifies the spread of the ____. | estimate (e.g. of the mean); raw individual data | — |
| A3 | apply | numeric | 0.9 | A sample of n=100 has s=25. Find the estimated SE of X̄. `[verified: 2.5]` | SE ≈ 25/√100 = 2.5 | — |
| A4 | apply | numeric | 0.95 | A sample of n=49 has s=21. Find the estimated SE of X̄. `[verified: 3]` | SE ≈ 21/√49 = 21/7 = 3 | — |
| A5 | apply | short-answer | 1.0 | A report states "average delivery time is 40 minutes (s=12, n=36)." A reader mistakenly uses s=12 directly as the "typical uncertainty" of the 40-minute average. What's the correct SE, and by what factor does using s instead overstate the uncertainty? `[verified: SE=2, factor=6]` | SE=12/√36=2; using 12 instead overstates the average's uncertainty by a factor of 6 (12/2) *(required: the explicit factor)* | — |
| A6 | apply | short-answer | 1.05 | Two labs measure the same quantity: Lab A has s=8, n=16; Lab B has s=8, n=64. Which lab's estimate of the mean is more precise, and by what factor is its SE smaller? `[verified: Lab B, factor 2]` | Lab A: SE=8/4=2; Lab B: SE=8/8=1 — Lab B's estimate is twice as precise (its SE is half of Lab A's), despite identical SD, purely because of its larger n | — |
| E3 | explain | short-answer | 1.75 | Explain why SE is the more relevant quantity than SD when reporting how trustworthy an estimated average is, even though SD is often more intuitive to non-statisticians. | SD describes variability among individual data points, which is a property of the population and doesn't change with study design; SE specifically describes how much the *estimate itself* (the number being reported) would bounce around across repeated studies — the quantity a reader actually needs to judge whether to trust the reported average *(required: explicitly names SE as the estimate-trustworthiness quantity)* | — |
| E4 | explain | short-answer | 1.8 | Explain why increasing n reduces SE but does not reduce SD, using the underlying formulas. | SD (σ) is a fixed population parameter with no n in its definition at all; SE=σ/√n has n explicitly in the denominator, so as n grows, more data is averaged together and the *estimate's* variability shrinks — while the *population's* inherent spread (σ) is unaffected by how much data happens to be collected *(required: the explicit "n only appears in SE's formula" argument)* | — |
| E5 | explain | short-answer | 1.85 | Explain why SE is a property of the *estimation procedure* (n, and the statistic being computed) rather than a property of any single computed estimate. | SE=σ/√n depends only on the population's SD and the chosen sample size — not on which specific numbers happened to be observed; two different samples of the same size n from the same population would have the same SE (in the theoretical/known-σ sense) despite producing different X̄ values, showing SE characterizes the *procedure's* precision, not any one outcome *(required)* | — |
| T2 | transfer | short-answer | 2.1 | A/B test dashboards often show a metric's SD prominently but bury or omit its SE. Explain the practical risk of this design choice for a product manager deciding whether to trust a reported lift. | showing only SD (individual-user variability) with no SE gives no direct signal about how much the *reported average lift* itself might vary if the test were rerun; a manager could mistake a large, stable-looking SD for "the data is noisy so ignore small differences" without realizing the actual estimate could be quite precise (small SE) if n is large, or dangerously imprecise (large SE) if n is small — SD alone answers the wrong question for a go/no-go decision on the average *(required: explicitly connects the missing-SE problem to a concrete bad decision-making consequence)* | — |
| T3 | transfer | numeric | 2.2 | A researcher wants SE(X̄) ≤ 1.5 for a population with σ=18. What is the minimum sample size required? `[verified: n≥144]` | 18/√n ≤ 1.5 ⟹ √n ≥ 12 ⟹ n ≥ 144 — the same square-root-law budgeting problem as `sample-mean`'s earlier T1, now framed explicitly in SE language | assumes a linear (rather than square-root) relationship between the SE target and the required n → `standard-error` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Test Statistic (`test-statistic`)
*Prereq: Sampling Distribution · ancestors 22 · b₀ = 1.07*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.07 | Define a test statistic, emphasizing its purpose. | a number computed from sample data, constructed to have a *known* distribution when a specific (usually null) hypothesis is true — used to measure how unusual the data would be under that hypothesis | — |
| R2 | recall | mcq | 0.37 | For H₀: μ=μ₀, Z=(X̄−μ₀)/SE measures: | how many standard errors X̄ is from the hypothesized value μ₀ | reads Z as "how far X̄ is from zero," ignoring μ₀ entirely → `test-statistic` |
| A1 | apply | numeric | 0.87 | X̄=52, SE=2, H₀ claims μ₀=50. Compute Z. `[verified: 1]` | Z=(52−50)/2=1 | — |
| A2 | apply | numeric | 1.0 | Same setup with X̄=58. Compute Z, and compare its evidential weight against A1's. `[verified: 4]` | Z=(58−50)/2=4 — far larger than A1's Z=1, indicating data much less consistent with the null (Z=4 is extremely rare under standard normal) *(required: the comparative statement, not just the number)* | — |
| E1 | explain | short-answer | 1.57 | Why standardize (divide by SE) rather than use the raw difference X̄−μ₀ directly? | a raw difference of "2 units" means something different depending on how precisely X̄ is estimated (i.e. on SE); standardizing puts the difference in universal units (standard errors), comparable against a known reference distribution regardless of the original measurement's units or precision *(required)* | — |
| E2 | explain | short-answer | 1.7 | State the full logical chain connecting a test statistic's value to "evidence against H₀." | the test statistic's distribution under H₀ is known; an observed value in a region that would be very unlikely under that known distribution is treated as evidence against H₀ — this compute/compare/judge structure is the entire mechanical core of hypothesis testing, foreshadowing `rejection-region` and `p-value` directly *(required)* | — |
| T1 | transfer | short-answer | 2.07 | Two entirely different studies (different claims, sample means, SEs, units) both produce Z=0.3. Explain why both represent "similarly weak evidence" despite the very different raw numbers involved. | standardization (E1) is exactly what makes this possible — both Z values are measured in the same universal units (SEs from the hypothesized value), referenced against the same standard normal distribution, so Z=0.3 represents the same *degree* of surprise regardless of the wildly different raw quantities behind each case *(required)* | — |
| R3 | recall | short-answer | −1.4 | State the general formula for a Z-type test statistic, using X̄, μ₀, and SE. | Z = (X̄ − μ₀) / SE | — |
| R4 | recall | mcq | −1.25 | A test statistic is constructed so that, under H₀, it has: | a fully known probability distribution | claims it's constructed so that its value is always exactly 0 under H₀, confusing "has a known distribution" with "always equals a fixed number" → `test-statistic` |
| R5 | recall | short-answer | −1.15 | True or false: a test statistic is computed using only the observed sample data and the hypothesized parameter value, never the true unknown parameter. | true — since the true parameter is unknown, only μ₀ (the hypothesized value under H₀) and the sample data are used in the computation | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: a test statistic near 0 suggests the data is ____ with H₀. | consistent (i.e. not surprising under H₀) | — |
| R7 | recall | mcq | −0.95 | A test statistic's magnitude reflects: | how far the observed data is from what H₀ predicts, measured in standardized units | reflects "the sample size alone," missing that the test statistic combines the observed discrepancy *and* its precision (via SE), not sample size in isolation → `test-statistic` |
| R8 | recall | short-answer | −0.85 | Define, in one sentence, why a test statistic needs a *known* reference distribution under H₀. | so that an observed value can be judged as "typical" or "surprising" by comparing it against the full range of values that distribution says are plausible under H₀ | — |
| R9 | recall | mcq | −0.75 | Which best describes what a very large \|Z\| indicates? | data that would be quite unusual if H₀ were actually true | describes a large \|Z\| as "strong evidence H₀ is definitely false," overstating what a test statistic alone establishes (it's evidence *against*, not proof) → `test-statistic` |
| R10 | recall | short-answer | −0.65 | True or false: the same raw difference X̄−μ₀ always produces the same Z value, regardless of SE. | false — Z=(X̄−μ₀)/SE depends on SE as well; the same raw difference divided by a different SE gives a different Z | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: Z is often called a "standardized" statistic because it is measured in units of ____. | standard errors | — |
| R12 | recall | mcq | −0.45 | If X̄=μ₀ exactly, the test statistic Z equals: | 0 | claims Z would be undefined whenever X̄=μ₀, rather than simply 0 (a perfectly valid, meaningful value) → `test-statistic` |
| R13 | recall | short-answer | −0.35 | State what "evidence against H₀" means in terms of the test statistic's value. | an observed test-statistic value that would be unlikely (far in the tail) under H₀'s known reference distribution | — |
| R14 | recall | mcq | −0.25 | A test statistic of Z=0.05 (very close to 0) is generally interpreted as: | data highly consistent with H₀, essentially no evidence against it | interpreted as "borderline significant," misreading a near-zero test statistic (strong support for H₀) as if it were close to a rejection threshold → `test-statistic` |
| R15 | recall | short-answer | −0.15 | True or false: a test statistic can, in principle, be negative. | true — e.g. Z=(X̄−μ₀)/SE is negative whenever X̄ < μ₀ | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: comparing an observed test statistic against its known distribution under H₀ is the mechanical core of a ____. | hypothesis test | — |
| A3 | apply | numeric | 0.9 | X̄=45, SE=3, H₀ claims μ₀=45. Compute Z. `[verified: 0]` | Z=(45−45)/3=0 — data lands exactly at the hypothesized value | — |
| A4 | apply | numeric | 0.95 | X̄=61, SE=4, H₀ claims μ₀=50. Compute Z. `[verified: 2.75]` | Z=(61−50)/4=2.75 | — |
| A5 | apply | short-answer | 1.0 | Two samples produce the same raw gap X̄−μ₀=6, but Sample A has SE=2 and Sample B has SE=12. Compute each Z and say which represents stronger evidence against H₀. `[verified: A: Z=3, B: Z=0.5]` | Sample A: Z=6/2=3; Sample B: Z=6/12=0.5 — Sample A's Z is far larger, representing much stronger evidence against H₀ despite the identical raw gap, because its estimate is far more precise (smaller SE) *(required: names precision/SE as the reason, not just the raw gap)* | — |
| A6 | apply | numeric | 1.05 | X̄=18, SE=5, H₀ claims μ₀=25. Compute Z, noting its sign. `[verified: −1.4]` | Z=(18−25)/5=−1.4 — negative, since X̄ fell below the hypothesized value | — |
| E3 | explain | short-answer | 1.75 | Explain why a test statistic's known distribution under H₀ must be derived (not just assumed), and what could go wrong if the assumed reference distribution were actually incorrect. | the reference distribution comes from mathematical derivation (e.g. via CLT, or an exact result for Normal data) grounded in specific assumptions about the data-generating process; if those assumptions don't hold (e.g. non-independence, wrong variance structure) the assumed reference distribution may not actually describe the test statistic's true behavior under H₀, silently invalidating the test's claimed error rates *(required: names a concrete assumption whose failure would break the reference distribution)* | — |
| E4 | explain | short-answer | 1.8 | Explain why constructing a test statistic is described as "measuring surprise," and why surprise needs a reference point (H₀) to be meaningful. | "surprising" is inherently relative — a value is only surprising *compared to* what was expected; the test statistic quantifies the gap between what was observed and what H₀ specifically predicts, scaled by how much variability is expected under that same H₀, making "surprise" a precise, computable quantity rather than a vague impression *(required)* | — |
| E5 | explain | short-answer | 1.85 | Explain the logical distinction between "the test statistic is large" and "H₀ is false" — why is the first only evidence for, not proof of, the second. | a large test statistic says the data would be unlikely *if* H₀ were true — but unlikely events do still happen sometimes even when H₀ is true (that's exactly what α controls), and the data could also be explained by some other departure from the test's assumptions rather than H₀ specifically being false; a large test statistic is probabilistic evidence, not a logical proof *(required: the explicit "unlikely ≠ impossible" argument)* | — |
| T2 | transfer | short-answer | 2.1 | A trading algorithm computes a "signal strength" score for identifying mispriced assets, standardized to have known behavior under a "market is efficient" null. Explain how this is structurally identical to the test-statistic concept, even though it's not labeled "hypothesis test" in that context. | it plays exactly the same role: a number computed from observed data (prices), constructed to have a known distribution under a specific null hypothesis (market efficiency), used to judge how surprising the observed data would be if that null were true — the same compute/compare/judge structure this concept defines, applied outside an explicitly statistical-textbook framing *(required: explicitly maps "signal strength" onto the test-statistic definition's three components)* | — |
| T3 | transfer | numeric | 2.2 | A hospital tracks a Z-type quality score for infection rates: X̄=0.08 (observed rate), μ₀=0.05 (target rate), SE=0.01. Compute Z, and explain what it means for the hospital's quality relative to target. `[verified: Z=3]` | Z=(0.08−0.05)/0.01=3 — a substantially large positive Z, indicating the observed infection rate is 3 standard errors above the target, which would be quite unusual if the true rate matched the target, i.e. meaningful evidence the hospital's true rate exceeds target *(required: interprets the sign and magnitude correctly, not just computes the number)* | interprets a large positive Z as automatically meaning "definitely above target with certainty," rather than as strong but probabilistic evidence → `test-statistic` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Rejection Region (`rejection-region`)
*Prereq: Test Statistic · ancestors 23 · b₀ = 1.09*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.09 | Define the rejection region, and what determines its boundary. | the set of test-statistic values for which H₀ is rejected; its boundary (critical value) is set by the chosen significance level α and by whether the test is one- or two-tailed | — |
| R2 | recall | mcq | 0.39 | For a two-tailed Z-test at α=0.05, the rejection region is: | \|Z\| > 1.96 | picks Z>1.645 — the one-tailed critical value, applied to a two-tailed test → `rejection-region` |
| A1 | apply | short-answer | 0.89 | For a one-tailed test (H₀:μ≤μ₀ vs H₁:μ>μ₀) at α=0.05, state the rejection region. | Z > 1.645 — all of α's mass in one tail, rather than split 0.025/0.025 | uses the two-tailed critical value 1.96 for a one-tailed test → `rejection-region` |
| A2 | apply | numeric | 1.0 | From `test-statistic`'s A2 (Z=4), using the two-tailed rejection region \|Z\|>1.96 at α=0.05, is H₀ rejected? `[verified: yes]` | yes — \|4\|=4 > 1.96, clearly inside the rejection region | — |
| E1 | explain | short-answer | 1.59 | Why must α (and hence the rejection region) be fixed *before* looking at the data? What goes wrong if a researcher picks the boundary after computing their result to make it "significant"? | this is a form of p-hacking/data-dredging — the test's actual guaranteed error rate is α only if the threshold was fixed in advance; choosing it after seeing the data means effectively searching for a threshold that "works," which inflates the true false-rejection rate far above the nominal α *(required)* | — |
| E2 | explain | short-answer | 1.75 | Why is a one-tailed rejection region more powerful for detecting an effect in a specific direction, but only valid when there's a genuine, pre-specified reason to rule out the other direction? | putting all of α in one tail lowers the critical value needed for that direction, increasing sensitivity to it — but this is legitimate only if the other direction was ruled out *before* seeing the data on genuine substantive grounds, not chosen for convenience *(required: names the pre-specification requirement)* | — |
| T1 | transfer | short-answer | 2.09 | A researcher gets a two-tailed p=0.06 (narrowly non-significant), then switches to a one-tailed test on the same data, getting p=0.03 ("significant"), justified by "I always expected this direction." Why is this switch, done *after* seeing the result, a serious problem — even though a genuinely pre-registered one-tailed test would have been fine? | switching test types after seeing the near-miss result is p-hacking — the decision to use a one-tailed test must be made and justified *before* seeing the data; doing it retroactively because it produces the desired outcome inflates the true false-positive rate far above 0.05, exactly the mechanism E1 warned about *(required: names the after-the-fact timing as the actual problem, not the one-tailed test itself)* | argues the one-tailed test is legitimate because "the direction really was expected" without acknowledging the after-the-fact timing problem → `rejection-region` |
| R3 | recall | short-answer | −1.4 | State the critical value(s) for a one-tailed Z-test at α=0.01. | Z > 2.326 (all of α's mass in one tail) | — |
| R4 | recall | mcq | −1.25 | The rejection region's boundary (critical value) grows farther from 0 when α: | decreases | claims a smaller α produces a *smaller* critical value, backwards from the actual relationship (smaller α means a more extreme, harder-to-reach threshold) → `rejection-region` |
| R5 | recall | short-answer | −1.15 | True or false: the rejection region is a set of test-statistic values, not a set of possible data outcomes directly. | true — it's defined on the scale of the computed test statistic (e.g. Z values), not the raw data itself | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: a test statistic falling in the rejection region leads to a decision to ____ H₀. | reject | — |
| R7 | recall | mcq | −0.95 | For a two-tailed test at α=0.10, the rejection region is: | \|Z\| > 1.645 | uses the one-tailed 1.28 critical value for α=0.10, rather than the correctly-split two-tailed 1.645 → `rejection-region` |
| R8 | recall | short-answer | −0.85 | Define "critical value" in one sentence. | the specific boundary value of the test statistic that separates the rejection region from the region where H₀ is not rejected | — |
| R9 | recall | mcq | −0.75 | The "acceptance region" (or non-rejection region) is: | every test-statistic value *not* in the rejection region | describes it as "the region proving H₀ is true," misreading failure to reject as proof, the same misconception `hypothesis-test`'s R2 targets → `rejection-region` |
| R10 | recall | short-answer | −0.65 | True or false: the rejection region's shape (which tail(s)) depends on how H₁ is stated. | true — a one-sided H₁ produces a one-tailed rejection region; a two-sided H₁ produces a two-tailed one | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: for a left-tailed test (H₁: μ<μ₀) at α=0.05, the rejection region is Z < ____. | −1.645 | — |
| R12 | recall | mcq | −0.45 | Increasing α (say from 0.01 to 0.10) makes the rejection region: | larger (easier to reject H₀) | claims increasing α shrinks the rejection region, backwards from the actual relationship → `rejection-region` |
| R13 | recall | short-answer | −0.35 | State the two pieces of information needed to fully specify a rejection region. | the significance level α, and whether the test is one-tailed or two-tailed (and if one-tailed, which direction) | — |
| R14 | recall | mcq | −0.25 | \|Z\|>2.576 is the two-tailed rejection region for which α? | α = 0.01 | picks α=0.05, confusing the 0.05-level critical value (1.96) with the 0.01-level one (2.576) → `rejection-region` |
| R15 | recall | short-answer | −0.15 | True or false: the rejection region is determined entirely before the test statistic is computed. | true — α and the tail direction are chosen in advance, so the rejection region's boundaries are fixed before the data-derived test statistic is even computed | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: a Z value that falls in the rejection region is described as ____, in common statistical language. | statistically significant | — |
| A3 | apply | short-answer | 0.9 | For a left-tailed test (H₁:μ<μ₀) at α=0.01, state the rejection region. | Z < −2.326 | — |
| A4 | apply | numeric | 0.95 | A two-tailed test at α=0.05 produces Z=−2.3. Is H₀ rejected? `[verified: yes]` | yes — \|−2.3\|=2.3 > 1.96, inside the rejection region | — |
| A5 | apply | numeric | 1.0 | A one-tailed test (H₁:μ>μ₀) at α=0.05 produces Z=1.5. Is H₀ rejected? `[verified: no]` | no — 1.5 < 1.645, not in the (one-tailed) rejection region, even though it would have been closer to significance under a smaller critical value | — |
| A6 | apply | short-answer | 1.05 | A researcher wants a stricter standard of evidence than the usual 0.05 and sets α=0.001 for a two-tailed test. Is the resulting critical value larger or smaller than 1.96 in magnitude? | larger — a stricter (smaller) α requires a more extreme test statistic before rejecting, so the critical value moves further from 0 (to about 3.29 for α=0.001 two-tailed) | — |
| E3 | explain | short-answer | 1.75 | Explain why the rejection region's boundary is derived from the test statistic's *known distribution under H₀*, not from the data actually observed. | the boundary must be set based on what values would be rare *if H₀ were true* — a property of the reference distribution — computed independent of which specific data eventually get collected; if the boundary depended on the observed data itself, it wouldn't provide a valid, pre-committed standard of evidence at all *(required)* | — |
| E4 | explain | short-answer | 1.8 | Explain why a two-tailed rejection region splits α into two halves (α/2 each) rather than using all of α in a single tail. | a two-tailed test treats deviations in *either* direction as evidence against H₀, so the total false-rejection probability α must be the *combined* probability across both tails; splitting it α/2 and α/2 keeps the overall Type I error rate at exactly α rather than doubling it *(required: the explicit combined-probability argument)* | — |
| E5 | explain | short-answer | 1.85 | Explain why choosing a one-tailed test when a two-tailed test was actually appropriate (i.e., without genuine prior justification for ruling out one direction) inflates the effective Type I error rate for that direction. | putting all of α on one side makes it easier to reject H₀ for effects in that direction than a genuinely justified two-tailed α would allow, effectively testing at a more lenient standard than advertised whenever the researcher's true prior beliefs didn't actually rule out the other direction — an unjustified one-tailed test borrows significance it hasn't earned *(required)* | — |
| T2 | transfer | short-answer | 2.15 | A pharmaceutical company sets its rejection region based on α=0.05 before a trial, then, on seeing the trial results fall just short, argues after the fact that "we should really have used α=0.10 for this indication." Evaluate this argument using the timing principle from E1/T1. | this is the identical fallacy as T1's test-switching example — the significance level (and hence rejection region) must be fixed before the data are seen; proposing a laxer α specifically because the pre-specified stricter one failed to reject is exactly the kind of post-hoc threshold-searching that inflates the true false-rejection rate, regardless of whether α=0.10 might have been defensible if chosen in advance *(required: explicitly identifies the after-the-fact timing as the violation, independent of whether α=0.10 is "reasonable" in isolation)* | argues the change is fine because α=0.10 is itself a commonly used significance level, ignoring that it was chosen only after seeing the inconvenient result → `rejection-region` |
| T3 | transfer | numeric | 2.2 | A biotech firm requires α=0.001 (very strict) for any two-tailed genomic association claim, given the huge number of genes tested. For an observed Z=3.0, would H₀ be rejected at this strict threshold, given the two-tailed critical value for α=0.001 is approximately 3.29? `[verified: no, since 3.0<3.29]` | no — \|3.0\|=3.0 < 3.29, so despite Z=3.0 easily clearing the usual 1.96 threshold, it falls short of this much stricter, multiple-testing-motivated boundary; illustrates why the choice of α (and hence the rejection region) can completely change a decision even with the same computed test statistic *(required: explicitly notes the same Z would have been "significant" at the ordinary α=0.05 threshold)* | assumes any Z above the familiar 1.96 threshold is automatically significant, regardless of the actual α being used → `rejection-region` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Hypothesis Test (`hypothesis-test`)
*Prereq: Rejection Region · ancestors 24 · b₀ = 1.11*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.11 | List the basic steps of a hypothesis test. | (1) state H₀, H₁ (2) choose α (3) compute the test statistic (4) determine the rejection region / p-value (5) decide and interpret in context | — |
| R2 | recall | mcq | 0.41 | "Failing to reject H₀" means: | there wasn't enough evidence in this sample to conclude H₀ is false — but H₀ could still be false | treats failing to reject as "H₀ has been proven true" — the central, most commonly violated interpretive point in hypothesis testing → `hypothesis-test` |
| A1 | apply | short-answer | 0.91 | A drug trial fails to reject H₀ (no effect) at α=0.05. A headline claims "Study PROVES the drug doesn't work." Critique this using R2. | wrong — failing to reject doesn't prove no effect; it could mean the drug genuinely has none, or the sample was too small to detect a real but modest effect (a Type II error possibility) — "no evidence of an effect" ≠ "evidence of no effect" *(required)* | — |
| A2 | apply | short-answer | 1.0 | State H₀ and H₁ for testing whether a coin is biased *toward heads specifically* (not just "biased"). | H₀: p=0.5; H₁: p>0.5 — a one-sided alternative, since the question specifically concerns bias toward heads, connecting directly to `rejection-region`'s one/two-tailed distinction | states a two-sided H₁ (p≠0.5) despite the question specifying a direction → `hypothesis-test` |
| E1 | explain | short-answer | 1.61 | Why is H₀ typically the "no effect / status quo" hypothesis rather than the researcher's actual hypothesis of interest? | the procedure assumes H₀ and asks whether the data would be surprising under it — this only works if H₀ is specific enough to compute an exact sampling distribution from (which "no effect" usually is, a single value); "there is *some* effect" is typically too vague — an entire range of possible effect sizes — to yield one sampling distribution *(required)* | — |
| E2 | explain | short-answer | 1.75 | Narrate the full logical chain of a hypothesis test, integrating test statistic, rejection region, and decision. | compute a test statistic with a known distribution under H₀; check whether it falls in a *pre-specified* rejection region; if so, treat the data as sufficiently inconsistent with H₀ to reject it, while acknowledging this conclusion could still be wrong with controlled probability α *(required: the chain must connect coherently end to end)* | — |
| T1 | transfer | short-answer | 2.11 | A company runs 20 separate α=0.05 tests looking for *any* significant side effect among 20 outcomes, finds one significant hit, and announces "we found a significant side effect." Why is this misleading, even if every null hypothesis is actually true? `[verified: 1−0.95²⁰≈0.64]` | if all 20 nulls are true, P(at least one false positive across 20 independent tests) = 1−0.95²⁰ ≈ **64%**, not the nominal 5% — reporting only the one "hit" while ignoring the other 19 badly misrepresents the actual evidence *(required: the explicit 64% computation)* — the multiple-comparisons problem, and directly reachable via `pie-boole`'s Boole's inequality as a (looser, 100%-capped) bound | assumes each test's 5% rate applies unchanged to the batch of 20 → `hypothesis-test` |
| R3 | recall | short-answer | −1.4 | Name the two hypotheses every hypothesis test states, and which one is assumed true for computing the test statistic's distribution. | H₀ (null hypothesis) and H₁ (alternative hypothesis); H₀ is the one assumed true when deriving the test statistic's reference distribution | — |
| R4 | recall | mcq | −1.25 | The significance level α is chosen: | before the data is collected/analyzed | claims α is chosen "after seeing how significant the result looks," which is exactly the p-hacking behavior `rejection-region`'s E1 warns against → `hypothesis-test` |
| R5 | recall | short-answer | −1.15 | True or false: a hypothesis test can, by itself, tell you the *size* of an effect, not just whether to reject H₀. | false — a basic hypothesis test only yields a reject/fail-to-reject decision (and a p-value); estimating effect size requires a separate point estimate and confidence interval | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: the hypothesis that represents "no effect" or "no difference" is conventionally called ____. | the null hypothesis (H₀) | — |
| R7 | recall | mcq | −0.95 | H₁ (the alternative hypothesis) typically represents: | the claim the researcher suspects or wants to find evidence for | describes H₁ as "the hypothesis assumed true when computing the test statistic," which is actually H₀'s role, not H₁'s → `hypothesis-test` |
| R8 | recall | short-answer | −0.85 | Define "statistical significance" in the context of a hypothesis test. | a result is "statistically significant" at level α if the test statistic falls in the rejection region, i.e. the p-value is less than α | — |
| R9 | recall | mcq | −0.75 | Which of these is a valid pair of hypotheses for testing whether a coin is fair? | H₀: p=0.5, H₁: p≠0.5 | states H₀: p≠0.5, H₁: p=0.5 — swapping which hypothesis is the specific, testable "status quo" claim and which is the alternative → `hypothesis-test` |
| R10 | recall | short-answer | −0.65 | True or false: "rejecting H₀" and "H₀ is false" are logically identical statements. | false — rejecting H₀ is a decision made with controlled error rate α; it remains possible (with probability α) that H₀ was actually true and this was a Type I error | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: the five basic steps of a hypothesis test are: state hypotheses, choose α, compute the ____, determine the rejection region or p-value, and decide. | test statistic | — |
| R12 | recall | mcq | −0.45 | A "two-sided" hypothesis test refers to: | a test where H₁ allows for a difference in either direction (e.g. μ≠μ₀) | describes it as "a test that computes two separate test statistics," misreading "two-sided" as about the number of statistics rather than the directionality of H₁ → `hypothesis-test` |
| R13 | recall | short-answer | −0.35 | State the specific claim H₀ makes in a test of whether a new website design changes the click-through rate. | H₀: the new design's true click-through rate equals the old design's (no difference) | — |
| R14 | recall | mcq | −0.25 | If a hypothesis test rejects H₀, the appropriate conclusion is: | there is evidence, at the chosen α, that H₀ is false | claims rejecting H₀ "proves H₁ is true with certainty," overstating what a hypothesis test's controlled-error-rate decision actually establishes → `hypothesis-test` |
| R15 | recall | short-answer | −0.15 | True or false: every hypothesis test must ultimately compute some kind of test statistic. | true — this is the mechanical core connecting `hypothesis-test` back to `test-statistic` | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: the final step of a hypothesis test is to decide whether to reject H₀ and to ____ the conclusion in the context of the original question. | interpret | — |
| A3 | apply | short-answer | 0.9 | State H₀ and H₁ for testing whether a new manufacturing process changes (in either direction) the average part weight from the historical 50g. | H₀: μ=50; H₁: μ≠50 — a two-sided alternative, since the question allows for change in either direction | — |
| A4 | apply | short-answer | 0.95 | A researcher's actual question is "does the new drug work *better* than the old one?" State the correctly-directed H₀ and H₁. | H₀: μ_new ≤ μ_old (or μ_new=μ_old, a common simplification); H₁: μ_new > μ_old — a one-sided alternative matching the specific direction of interest | — |
| A5 | apply | short-answer | 1.0 | A test rejects H₀ at α=0.05. A colleague asks "does this mean there's only a 5% chance we're wrong about this specific conclusion?" Correct this using R10's distinction. | no — α is the *long-run* probability of a Type I error across many hypothetical repetitions when H₀ is true, not the probability that this specific decision is wrong; that latter quantity would require a posterior probability, which a frequentist hypothesis test does not compute *(required)* | — |
| A6 | apply | short-answer | 1.05 | Walk through the five steps of a hypothesis test for: "is average daily screen time among teens different from the widely-cited 5 hours?" | (1) H₀: μ=5, H₁: μ≠5 (2) choose α, e.g. 0.05 (3) compute Z or T from the sample (4) find rejection region or p-value (5) decide and interpret in terms of screen time specifically *(required: all five steps present and correctly ordered)* | — |
| E3 | explain | short-answer | 1.75 | Explain why a hypothesis test's five-step structure (state hypotheses, choose α, compute statistic, determine region, decide) must be followed strictly in this order, and what goes wrong if step 2 happens after step 3. | choosing α after seeing the test statistic's value lets the researcher pick whichever α makes the desired conclusion "significant," which is exactly the p-hacking failure mode `rejection-region`'s E1 names; the order enforces that the standard of evidence is committed to before it's influenced by the specific result *(required: explicitly ties the ordering requirement to the p-hacking mechanism)* | — |
| E4 | explain | short-answer | 1.8 | Explain why H₀ is chosen to be a hypothesis specific enough to yield an exact (or approximate) sampling distribution, using a concrete example of a *bad* H₀ choice. | H₀ must pin down a single value (e.g. μ=50, or p=0.5) so that the test statistic's distribution under it is fully computable; a "bad" H₀ like "the drug has *some* effect" doesn't specify a single distribution — it's compatible with infinitely many different effect sizes, each implying a different sampling distribution, so no single reference distribution could be derived *(required: a concrete example of an under-specified H₀)* | — |
| E5 | explain | short-answer | 1.85 | Explain the asymmetry in how "reject H₀" and "fail to reject H₀" should be interpreted, and why the procedure isn't symmetric despite superficially looking like a coin flip between two conclusions. | rejecting H₀ is a positive, controlled-error-rate claim (the data would be quite unlikely under H₀, at rate α); failing to reject is not a positive claim about H₀ being true at all — it just means the evidence wasn't strong enough to clear that bar, which could be because H₀ is true, or because the study lacked power (`power`'s later territory) — the two outcomes are not treated with equal evidential weight *(required: explicitly explains why the interpretations are asymmetric, not just restates R2)* | — |
| T2 | transfer | short-answer | 2.15 | A/B testing platforms sometimes let users "peek" at results daily and stop the test as soon as it becomes significant, rather than fixing the sample size/duration in advance. Explain why this practice, even with a correctly-chosen α=0.05 and correctly-stated hypotheses, still inflates the true false-positive rate. | this is a subtler version of the timing violations seen throughout this cluster (E3, and `rejection-region`'s post-hoc threshold problem): repeatedly checking for significance and stopping at the first "hit" gives the test many chances to cross the threshold by chance alone, even under a true H₀ — each daily peek is effectively another opportunity for a false rejection, so the *actual* probability of eventually declaring significance under H₀ is far higher than the nominal 5% for any single fixed-sample test *(required: connects the repeated-peeking behavior to an inflated overall error rate, not just "it's against the rules")* | assumes daily peeking is harmless as long as the final test itself uses the correct α=0.05 formula → `hypothesis-test` |
| T3 | transfer | short-answer | 2.2 | A researcher tests 5 different possible H₀'s for the same dataset (e.g. "μ=45," "μ=50," "μ=55," ...) until one is rejected, then reports only that one result as "the finding." Critique this using the five-step structure and the multiple-comparisons logic from T1. | the five-step structure assumes H₀ is stated *once*, in advance, based on a substantive research question — not searched over after the fact among several candidate values until one happens to be rejectable; this is structurally the same multiple-comparisons inflation as T1's 20-test example, now applied to a search over hypotheses rather than over outcomes, and reporting only the rejected one while hiding the other four searched-and-failed attempts badly misrepresents the true strength of evidence *(required: explicitly draws the parallel to T1's multiple-comparisons mechanism)* | treats testing multiple candidate H₀ values and reporting only the significant one as equivalent to a single, pre-specified, valid hypothesis test → `hypothesis-test` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Type I and Type II Error (`type-i-ii-error`)
*Prereq: Hypothesis Test · ancestors 25 · b₀ = 1.13*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.13 | Define Type I and Type II error, with their associated probabilities. | Type I: rejecting a *true* H₀ (false positive), probability α; Type II: failing to reject a *false* H₀ (false negative), probability β | — |
| R2 | recall | mcq | 0.43 | For fixed sample size, decreasing α generally: | increases β | assumes decreasing α is unambiguously "safer," missing the tradeoff with β entirely → `type-i-ii-error` |
| A1 | apply | short-answer | 0.93 | In a criminal trial analogy (H₀=innocent), what do a Type I and a Type II error each correspond to? | Type I: convicting an innocent person; Type II: acquitting a guilty person | — |
| A2 | apply | short-answer | 1.05 | A company sets α=0.01 (very strict) for a quality-control defect test. What's the likely consequence for detecting genuine defects? | with such a strict α, β (missing a genuine defect) will be higher than with a more lenient α — the company has traded fewer false alarms for more missed real defects *(required: names the tradeoff explicitly, using R2)* | — |
| E1 | explain | short-answer | 1.63 | Why does decreasing α tend to increase β? Use the rejection-region mechanism. | shrinking the rejection region (to reduce false rejections under a true H₀) simultaneously makes it *harder* to reject H₀ even when H₁ is actually true, mechanically increasing the Type II error rate — a "shrinking a region grows its complement's failure mode" argument, not just a restated tradeoff *(required)* | — |
| E2 | explain | short-answer | 1.78 | Why should the choice of which error to prioritize depend on real-world consequences? Contrast a smoke detector with a spam filter. | a smoke detector should tolerate more Type I errors (false alarms) because a Type II error (missed real fire) is catastrophic; a spam filter's Type I error (flagging a legitimate email as spam) is often considered worse than its Type II error (letting spam through), since missing an important email costs most users more *(required: both contrasting examples, with the direction of preference justified)* | assumes one error type is universally "worse" across all applications → `type-i-ii-error` |
| T1 | transfer | short-answer | 2.13 | For COVID testing (H₀="patient does not have the disease"), describe Type I and Type II errors specifically, and explain why public health authorities during a pandemic might deliberately accept a *higher* Type I rate for a *lower* Type II rate — contrary to the usual α=0.05 convention. | Type I: telling a healthy person they're infected (unnecessary quarantine, anxiety); Type II: telling an infected person they're healthy (continued spread of a contagious disease) — during a pandemic, a missed case (Type II) is often judged far more costly to society than an unnecessary quarantine (Type I), justifying a deliberate shift away from the conventional default *(required: both error definitions specific to this context, plus the direction-and-reason for the shift)* | applies the academic α=0.05 default uncritically without considering the asymmetric real-world costs → `type-i-ii-error` |
| R3 | recall | short-answer | −1.4 | State the symbol conventionally used for the Type II error probability. | β | — |
| R4 | recall | mcq | −1.25 | A Type I error occurs specifically when: | H₀ is actually true, but the test rejects it | occurs when "H₀ is actually false and the test fails to reject it" — this is the description of a Type II error, not a Type I → `type-i-ii-error` |
| R5 | recall | short-answer | −1.15 | True or false: α and β can both be driven simultaneously to 0 by choosing a stricter test, with everything else held fixed. | false — for fixed n, tightening α (to reduce Type I errors) generally increases β (more Type II errors); both can be reduced together only by other means, like increasing n | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: a Type II error occurs when the test fails to reject H₀, but H₀ is actually ____. | false | — |
| R7 | recall | mcq | −0.95 | Which is a Type II error in a courtroom analogy (H₀=innocent)? | acquitting a guilty defendant | picks "convicting an innocent defendant," which is the Type I error, not Type II → `type-i-ii-error` |
| R8 | recall | short-answer | −0.85 | Define α in relation to Type I error. | α is the pre-chosen probability of committing a Type I error — rejecting H₀ when it is actually true | — |
| R9 | recall | mcq | −0.75 | β is generally described as: | not directly set by the researcher in advance the way α is; it depends on the true effect size, n, and α | described as "always fixed at 0.20 by convention," confusing a common *target* used in power analysis with a fact about how β behaves in general → `type-i-ii-error` |
| R10 | recall | short-answer | −0.65 | True or false: it is possible for a test to have both a Type I and a Type II error simultaneously on the same decision. | false — for any single hypothesis test decision, only one of the two error types is even *possible*, depending on whether H₀ is actually true or false (which is unknown but fixed); you cannot make both mistakes on the same test at once | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: a "false positive" in common language corresponds to a ____ error in hypothesis-testing language. | Type I | — |
| R12 | recall | mcq | −0.45 | A "false negative" in common language corresponds to: | a Type II error | matches "false negative" to Type I error, swapping the two error types → `type-i-ii-error` |
| R13 | recall | short-answer | −0.35 | State the four possible outcome combinations of (H₀ true/false) × (reject/fail to reject H₀). | H₀ true & fail to reject (correct); H₀ true & reject (Type I error); H₀ false & fail to reject (Type II error); H₀ false & reject (correct, i.e. power) | — |
| R14 | recall | mcq | −0.25 | Which quantity is *not* directly one of α, β, or power? | the p-value of a specific observed dataset | picks "1−β," which actually IS one of these quantities (it's the definition of power), rather than correctly identifying the p-value (an outcome-specific quantity, unlike the other three which are properties of the test procedure) as the one that doesn't belong → `type-i-ii-error` |
| R15 | recall | short-answer | −0.15 | True or false: α is a property of the test procedure chosen in advance, while whether a Type I error actually occurred on a given dataset is unknowable in practice. | true — you never actually know, for a specific dataset, whether you made a Type I error, since that would require knowing the true state of H₀; α only describes the long-run rate across hypothetical repetitions | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: the trade-off between Type I and Type II error rates, for a fixed sample size, is often summarized as α and β moving in ____ directions. | opposite | — |
| A3 | apply | short-answer | 0.9 | A spam filter's H₀ is "this email is not spam." Identify its Type I and Type II errors in plain language. | Type I: a legitimate email is incorrectly flagged as spam; Type II: an actual spam email is incorrectly let through to the inbox | — |
| A4 | apply | short-answer | 0.95 | An airport security screening's H₀ is "this passenger is not carrying a prohibited item." Identify the Type I and Type II errors, and which one has the more severe real-world consequence. | Type I: an innocent passenger is flagged/searched unnecessarily (inconvenience); Type II: a passenger carrying a prohibited item passes through undetected (potentially catastrophic) — Type II is far more severe here, which is why airport security deliberately tolerates a high Type I (false alarm) rate *(required: names both errors and correctly judges Type II as more severe in this context)* | — |
| A5 | apply | short-answer | 1.0 | A factory quality test currently has α=0.05, β=0.30. If the factory tightens to α=0.01 with the same sample size, what happens to β qualitatively? | β increases (becomes worse) — tightening α, at fixed n, mechanically raises β by shrinking the rejection region, per R2/E1's tradeoff | — |
| A6 | apply | short-answer | 1.05 | A researcher wants to reduce β without changing α. Name one lever, other than sample size, that could help (hint: connects forward to `power`). | increasing the true effect size being tested is out of the researcher's control, but improving measurement precision (reducing measurement noise, which lowers effective SE) can reduce β at fixed α and n — a preview of the levers `power` explores more fully | — |
| E3 | explain | short-answer | 1.75 | Explain why it is impossible, for a fixed sample size, to reduce both α and β to arbitrarily small values simultaneously. | the two error rates are governed by overlapping regions of the same test statistic's distribution under H₀ versus H₁; shrinking the rejection region (to lower α) mechanically expands the region where a true H₁ still fails to trigger rejection (raising β) — the only way to reduce *both* without limit is to add more information (larger n), which shrinks the overlap between the two sampling distributions itself *(required: the explicit "same distributions, same statistic" mechanism, and that n is the escape valve)* | — |
| E4 | explain | short-answer | 1.8 | Explain, using the criminal-trial analogy, why the justice system is explicitly designed to prioritize avoiding Type I errors over Type II errors ("innocent until proven guilty"). | the system treats convicting an innocent person (Type I) as a far graver injustice than acquitting a guilty one (Type II), so it deliberately sets a high standard of evidence ("beyond reasonable doubt," a very small effective α) even though this necessarily lets more guilty people go free (higher β) than a more lenient standard would — an explicit, values-driven asymmetric tradeoff choice, not an oversight *(required: names the deliberate, values-based nature of the tradeoff)* | — |
| E5 | explain | short-answer | 1.85 | Explain why "the test's error rates (α, β)" are properties of the *procedure*, established before data collection, rather than facts that can be determined by examining a single completed study's results. | α and β describe the long-run behavior of the test procedure across hypothetical repetitions under each hypothesis being true — they are chosen or calculated before running the study; once a single dataset is collected and a decision is made, that decision is simply right or wrong (unknowably, since the truth is unknown), not itself an "instance" of α or β occurring with some observable frequency *(required: the explicit "procedure property vs. single-outcome fact" distinction)* | — |
| T2 | transfer | short-answer | 2.15 | A medical device company must choose a decision threshold for flagging a scan as "abnormal" (H₀="scan is normal"). Regulators require a *very* low Type I rate (to avoid unnecessary alarming/costly follow-ups), while doctors want a very low Type II rate (to avoid missing real disease). Explain why this is fundamentally a single-threshold tradeoff, not two independently solvable problems, and what would actually let both groups get closer to what they want. | for a fixed threshold and fixed data quality, α and β are mechanically linked (E1) — you cannot independently satisfy "very low α" and "very low β" by threshold choice alone, since moving the threshold to help one necessarily hurts the other; the only way to genuinely improve both simultaneously is to improve the underlying test itself (better imaging resolution, a more discriminating biomarker) — analogous to `power`'s upcoming point that only n (or effect-size/measurement quality) breaks the tradeoff, not the threshold *(required: explicitly states the threshold-alone limitation and names an actual escape valve)* | assumes the threshold can simply be tuned to satisfy both groups' demands simultaneously, without any change to the underlying measurement quality → `type-i-ii-error` |
| T3 | transfer | short-answer | 2.2 | An email platform's spam filter and a bank's fraud-detection system both face Type I/II tradeoffs, but the platform is tuned to minimize Type I (favor letting emails through) while the bank is tuned to minimize Type II (favor flagging transactions). Using A3/A4's reasoning, explain why the "right" tradeoff is inherently application-specific rather than something a single universal α=0.05 default could ever get right for both. | the correct balance between α and β depends entirely on the relative real-world *cost* of each error type in that specific application — an email platform judges a missed important message (Type I) as more costly to the average user than an occasional spam email getting through (Type II), while a bank judges missing real fraud (Type II) as catastrophically more costly than occasionally freezing a legitimate transaction (Type I); no single fixed α can be "correct" across such different cost structures, which is exactly why E2's smoke-detector/spam-filter contrast and this item's platform/bank contrast both exist *(required: explicitly connects the different tunings to differing relative costs of the two error types, not just restates that they're tuned differently)* | assumes there is one universally "correct" α/β tradeoff that should apply the same way across every application → `type-i-ii-error` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Power (`power`)
*Prereq: Type I and Type II Error · ancestors 26 · b₀ = 1.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.15 | Define statistical power. | Power = 1−β = P(correctly rejecting H₀ \| H₁ is true) — the probability of detecting a real effect when one exists | — |
| R2 | recall | mcq | 0.45 | Which increases power, all else equal? | a larger sample size | picks "a smaller true effect size" — backwards; smaller effects are *harder* to detect → `power` |
| A1 | apply | short-answer | 0.95 | A study with 40% power finds a non-significant result. Is this strong evidence the effect doesn't exist? | no — even if the true effect is real, there's a 60% chance (β=0.6) the study fails to detect it purely from low power; a non-significant result from an underpowered study is only weak evidence against the effect *(required)* | treats any non-significant result as equally strong evidence against an effect, regardless of the study's power → `power` |
| A2 | apply | short-answer | 1.1 | A study currently has 55% power but the researcher wants 80%. Should n increase or decrease, and why? | increase — larger n directly increases power (R2); a formal power analysis would compute the specific n needed to reach exactly 80% for this effect size | — |
| E1 | explain | short-answer | 1.65 | Using the same rejection-region-shrinking logic as `type-i-ii-error`'s E1 (now reversed), explain why *increasing* α increases power. | increasing α *grows* the rejection region, making it easier to reject H₀ whenever H₁ is actually true — at the direct cost of more false rejections when H₀ is true; this completes the α/β/power triangle from the previous two concepts *(required: explicitly the mirror-image argument of the earlier item)* | — |
| E2 | explain | short-answer | 1.8 | Why does a larger true effect size increase power, with no change to n or α? | the test statistic's sampling distributions under H₀ and under the specific H₁ overlap *less* when the true effect is larger, making it easier to distinguish the two from a given sample *(required: the overlap/distinguishability mechanism)* | — |
| T1 | transfer | short-answer | 2.15 | A trial with only 30 of a recommended 500 patients finds no significant drug effect. A competitor claims this proves the drug doesn't work. Critique this using `hypothesis-test`, `type-i-ii-error`, and `power` together. | failing to reject H₀ never proves it true (`hypothesis-test`); with only 30 of 500 recommended patients, the study almost certainly had very low power to detect the expected effect size, making a Type II error highly likely regardless of whether the drug actually works; the claim conflates "no significant evidence found" with "evidence of no effect," compounded by a design unlikely to detect a real effect even if one existed *(required: draws on all three concepts explicitly, not just one)* | critiques the trial on only one of the three grounds (e.g. sample size alone, without the "fail to reject ≠ proof" point) → `power` |
| R3 | recall | short-answer | −1.4 | State the formula relating power to β. | Power = 1 − β | — |
| R4 | recall | mcq | −1.25 | Power is defined as the probability of: | correctly rejecting H₀ when H₁ is actually true | defines power as "the probability of correctly failing to reject H₀ when H₀ is true," which is actually 1−α, an entirely different quantity → `power` |
| R5 | recall | short-answer | −1.15 | True or false: power is a single fixed number that doesn't depend on the true effect size. | false — power depends on the (unknown) true effect size; it's typically computed *for* a specific hypothesized effect size during study planning | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: power ranges between ____ and ____ (as a probability). | 0 and 1 | — |
| R7 | recall | mcq | −0.95 | Which of these decreases power, all else equal? | a smaller sample size | picks "a larger true effect size," backwards — larger effects are *easier* to detect, increasing power, not decreasing it → `power` |
| R8 | recall | short-answer | −0.85 | Define "underpowered study" in one sentence. | a study whose power to detect the effect size of actual interest is low, meaning it has a high chance of missing a real effect (a high β) | — |
| R9 | recall | mcq | −0.75 | A "power analysis," conducted before running a study, is used to: | determine the sample size needed to achieve a target power (e.g. 80%) for a hypothesized effect size | describes it as "computing the actual power achieved after the study is done," missing that power analysis is fundamentally a *planning* tool used in advance | — |
| R10 | recall | short-answer | −0.65 | True or false: 80% power is a common conventional target in study design. | true — 80% power (β=0.20) is a widely used, if somewhat arbitrary, planning convention, echoing α=0.05's role for Type I error | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: increasing α, holding everything else fixed, ____ power. | increases | — |
| R12 | recall | mcq | −0.45 | Which of these increases power, all else equal? | reducing measurement noise/variability in the data | picks "using a stricter (smaller) α," which actually *decreases* power (per R11's reverse direction), not increases it → `power` |
| R13 | recall | short-answer | −0.35 | State what a "power curve" typically plots. | power as a function of the true (hypothesized) effect size, for a fixed n and α — showing how detection probability rises as the true effect grows | — |
| R14 | recall | mcq | −0.25 | A study reporting "power = 0.95" for a given effect size means: | if that effect size is truly present, there's a 95% chance the study would detect it (reject H₀) | interprets it as "there's a 95% chance the effect actually exists," conflating a pre-study design property (power) with a post-hoc probability statement about reality → `power` |
| R15 | recall | short-answer | −0.15 | True or false: power can be computed without specifying a particular effect size to detect. | false — power is always computed *for* a specific hypothesized effect size (or a range of them, as in a power curve); there is no single "power" number independent of the effect size in question | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: the four levers that jointly determine power are: sample size, α, the true effect size, and ____. | the underlying variability/noise in the data (σ or measurement precision) | — |
| A3 | apply | short-answer | 0.9 | A study is designed with 80% power to detect a 5-point difference. Interpret this number for a colleague unfamiliar with the term. | if the true difference really is 5 points, this study design has an 80% chance of correctly detecting it (rejecting H₀) — and correspondingly a 20% chance of missing it (Type II error), even though the effect is genuinely real | — |
| A4 | apply | short-answer | 0.95 | A grant reviewer notices a proposed study has only 35% power for the smallest effect size the researchers consider practically meaningful. What should the reviewer recommend? | recommend increasing the planned sample size (or otherwise improving precision) before funding, since a study this underpowered has a very high chance (65%) of failing to detect even a practically meaningful real effect, wasting resources on an inconclusive result | — |
| A5 | apply | short-answer | 1.0 | Two proposed studies test the same H₀ with the same n and α, but Study A looks for a small effect (Cohen's d=0.2) and Study B looks for a large effect (d=0.8). Which has higher power? | Study B — larger true effect sizes are inherently easier to detect at the same n and α, giving it higher power than Study A's search for a subtler effect | — |
| A6 | apply | short-answer | 1.05 | A team increases their planned sample size from n=50 to n=200 to raise power. Name one *cost* of this decision that a purely statistical power calculation doesn't capture. | e.g. greater expense, longer time to complete the study, more participant burden/risk (in a clinical context), or delayed decision-making — power calculations optimize statistical detection but don't account for these practical tradeoffs | — |
| E3 | explain | short-answer | 1.75 | Explain why power is considered a property of the *study design*, decided before data collection, rather than something computed after seeing the results. | power depends on n, α, and a *hypothesized* effect size — all quantities available (or chosen) before running the study; computing it after the fact using the *observed* effect size ("post-hoc power") is a well-known statistical malpractice, since it's mathematically just a rescaling of the p-value and provides no genuinely new information *(required: names the "power should be planned, not computed post-hoc from observed results" point)* | — |
| E4 | explain | short-answer | 1.8 | Explain why a well-designed study should target adequate power for the *smallest effect size considered practically meaningful*, not for the effect size the researchers secretly hope is true. | power is effect-size-specific (R13); designing only for an optimistically large hypothetical effect risks a severely underpowered study if the true effect turns out to be smaller (but still practically important) — targeting the smallest meaningful effect size ensures the study can detect anything worth detecting, rather than only detecting the best-case scenario *(required: the explicit "design for the smallest meaningful effect, not the hoped-for one" argument)* | — |
| E5 | explain | short-answer | 1.85 | Explain the relationship between power and the overlap of the two sampling distributions (under H₀ and under the specific H₁), connecting to `type-i-ii-error`'s E2 and this concept's own E2. | power is the area of the H₁ sampling distribution that falls *outside* the rejection region's complement — equivalently, 1 minus the overlap area counted as β; anything that reduces the overlap between the two distributions (larger true effect, smaller variance/larger n) directly increases power, which is the same overlap-based mechanism `type-i-ii-error`'s tradeoff argument and this concept's E2 both rely on *(required: explicitly frames power via the overlap area, tying together the three related concepts)* | — |
| T2 | transfer | short-answer | 2.2 | A startup runs an A/B test for one week, finds no significant lift, and kills the feature. A data scientist later points out the test only had about 15% power to detect the lift size the product team actually expected. Explain what should have happened differently, and why "no significant lift" from this particular test carries little informational value. | with only 15% power, the test had an 85% chance of failing to detect the exact effect size the team expected, even if that effect were genuinely present — the non-significant result is thus almost uninformative about whether the feature actually helps; the team should have run a power analysis *before* the test to determine an adequate sample size/duration for the expected effect, and, having not done so, should treat "no significant lift" as inconclusive rather than as evidence the feature doesn't work *(required: computes the practical implication of 15% power — an 85% chance of a false negative even if the effect is real — and recommends pre-study power analysis)* | treats a low-power test's non-significant result as strong evidence the feature has no effect → `power` |
| T3 | transfer | numeric | 2.3 | Two candidate study designs both target the same effect size and α: Design A has power 0.50; Design B has power 0.90. If the true effect is genuinely present, compare the probability each design *fails* to detect it. `[verified: A: 0.50, B: 0.10]` | Design A's β = 1−0.50 = 0.50 (a coin-flip chance of missing a real effect); Design B's β = 1−0.90 = 0.10 — Design B is five times less likely to miss the same real effect, a difference with serious practical consequences for how much weight a non-significant result from each design should be given *(required: computes both β values and interprets the five-fold difference)* | treats "power=0.50" and "power=0.90" as similarly acceptable design choices without computing the very different miss rates each implies → `power` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.3. T1 is a deliberate capstone requiring the whole error-rate sub-arc at once.*

---

## P-Value (`p-value`)
*Prereq: Hypothesis Test · ancestors 25 · b₀ = 1.13*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.13 | Define the p-value precisely. | P(test statistic as extreme or more extreme than observed \| H₀ is true) — a probability computed *assuming* H₀ | — |
| R2 | recall | mcq | 0.43 | The p-value is: | the probability of the data (or more extreme), given H₀ is true | picks "the probability H₀ is true" — arguably the single most important misconception in this entire bank → `p-value` |
| A1 | apply | numeric | 0.93 | A two-tailed Z-test gives Z=2.5. Find the p-value. `[verified: 0.0124]` | p = 2·P(Z>2.5) ≈ 0.0124 | forgets to double the one-tailed probability for a two-tailed test → `p-value` |
| A2 | apply | short-answer | 1.05 | At α=0.05, using A1's p≈0.0124, is H₀ rejected? Confirm this matches the rejection-region approach for Z=2.5. `[verified: consistent, |2.5|>1.96]` | reject, since p=0.0124<0.05; and \|2.5\|=2.5>1.96, so the rejection-region approach agrees — confirming the two methods are equivalent | — |
| E1 | explain | short-answer | 1.63 | Explain precisely why "the p-value is the probability H₀ is true" is wrong, using the actual definition. | the p-value is computed by *assuming* H₀ from the start — it's a probability conditional *on* H₀ — so it cannot simultaneously mean "the probability H₀ is true," which would require a posterior probability via Bayes' rule with a prior on H₀ *(required: the explicit conditional-direction argument)* — the same category error `likelihood-vs-probability` warned about earlier | — |
| E2 | explain | short-answer | 1.78 | Why do the p-value and rejection-region approaches always give the identical decision at a given α? | "test statistic in the rejection region" and "p-value < α" are logically equivalent statements — both say the observed data is among the most extreme α-fraction of outcomes possible under H₀ *(required)* | — |
| T1 | transfer | short-answer | 2.13 | A study reports p=0.03 for a new teaching method. A teacher tells colleagues "this means there's a 97% chance the method actually works." Why is this a misinterpretation? | p=0.03 means: if the method had no real effect, there'd be only a 3% chance of seeing data this extreme by chance — it says nothing directly about the probability the method works, which would require Bayesian reasoning with a prior the calculation never used *(required: connects to the actual definition, not just "that's wrong")* — the single most consequential statistical misinterpretation in practice | restates the teacher's claim as "basically correct, just imprecisely worded" → `p-value` |
| R3 | recall | short-answer | −1.4 | State the decision rule linking the p-value to α. | reject H₀ if p < α; fail to reject if p ≥ α | — |
| R4 | recall | mcq | −1.25 | A smaller p-value indicates: | data that would be more surprising/extreme under H₀ | indicates "a larger true effect size for certain," overstating what a small p-value alone establishes (it reflects both effect size and precision/n, not effect size in isolation) → `p-value` |
| R5 | recall | short-answer | −1.15 | True or false: the p-value is computed assuming H₁ is true. | false — the p-value is computed *assuming H₀ is true*; that's the whole basis for calling an observed result "surprising" | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: a p-value less than α leads to a decision to ____ H₀. | reject | — |
| R7 | recall | mcq | −0.95 | For a one-tailed test, the p-value is computed as: | the probability of a result at least as extreme in the specified direction only | computes it "the same as the two-tailed p-value, just using a different critical value," missing that the p-value calculation itself differs (single tail vs. doubled) between one- and two-tailed tests → `p-value` |
| R8 | recall | short-answer | −0.85 | Define "extreme," precisely, in the p-value's definition. | as far or farther from what H₀ predicts as the observed test statistic, in the direction(s) relevant to H₁ | — |
| R9 | recall | mcq | −0.75 | p=0.5 for a test statistic indicates: | data very consistent with H₀ — about as typical as data under H₀ could be | indicates "the test is inconclusive and more data is needed," a plausible-sounding but incorrect gloss; p=0.5 specifically signals the observed result is entirely unremarkable under H₀, not merely "inconclusive" → `p-value` |
| R10 | recall | short-answer | −0.65 | True or false: p-values range between 0 and 1. | true — a p-value is a probability, so it is always in [0,1] | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: p < 0.05 is a common, but ultimately ____, convention for "statistical significance." | arbitrary (a convention, not a mathematical law) | — |
| R12 | recall | mcq | −0.45 | p=0.001 versus p=0.04, both below α=0.05, means: | both lead to the same "reject H₀" decision, though p=0.001 reflects more extreme data relative to H₀ | claims "p=0.001 means the effect is 40 times larger than the effect for p=0.04," confusing the p-value's magnitude with a direct, linearly-scaled measure of effect size → `p-value` |
| R13 | recall | short-answer | −0.35 | State what "p-hacking" refers to, in one sentence. | manipulating data collection or analysis choices (after peeking at results) specifically to push a p-value below the significance threshold | — |
| R14 | recall | mcq | −0.25 | The p-value and the significance level α are: | conceptually different: α is chosen in advance as a threshold; p is computed from the data afterward and compared to it | described as "the same quantity, just computed at different points in the analysis," collapsing the meaningful distinction between a pre-chosen threshold and a data-derived quantity → `p-value` |
| R15 | recall | short-answer | −0.15 | True or false: a p-value close to 0 automatically means the effect is practically important, not just statistically detectable. | false — a tiny p-value can arise from a trivially small effect if n is very large; statistical significance and practical importance are separate questions | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: the p-value is computed based on the observed test statistic and ____ or more extreme values under H₀. | equal to it | — |
| A3 | apply | numeric | 0.9 | A one-tailed Z-test (H₁: μ>μ₀) gives Z=1.8. Find the p-value. `[verified: ≈0.0359]` | p = P(Z>1.8) ≈ 0.0359 | doubles this one-tailed probability unnecessarily, as if the test were two-tailed → `p-value` |
| A4 | apply | numeric | 0.95 | A two-tailed Z-test gives Z=1.5. Find the p-value. `[verified: ≈0.1336]` | p = 2·P(Z>1.5) ≈ 2(0.0668) = 0.1336 | — |
| A5 | apply | short-answer | 1.0 | At α=0.01, using A4's p≈0.1336, is H₀ rejected? | fail to reject, since p=0.1336 > 0.01 — even though the same result might have been rejected at a more lenient α=0.05 in some other scenario, this specific result clearly does not clear the α=0.01 bar | — |
| A6 | apply | numeric | 1.05 | A one-tailed Z-test (H₁: μ<μ₀) gives Z=−2.1. Find the p-value. `[verified: ≈0.0179]` | p = P(Z<−2.1) ≈ 0.0179 | computes P(Z>2.1) instead of P(Z<−2.1), using the wrong tail for a left-tailed test → `p-value` |
| E3 | explain | short-answer | 1.75 | Explain why the p-value is *not* the same as "the probability the observed result happened by chance." | every result "happens by chance" in the sense of sampling variability, whether H₀ is true or not; the p-value specifically measures how *unusual* the observed result (or more extreme) would be, conditional on H₀ being the true state of the world — a much more precise claim than the vague "happened by chance" phrasing, which doesn't specify what it's conditioning on *(required)* | — |
| E4 | explain | short-answer | 1.8 | Explain why a p-value is a function of both the true effect size *and* the sample size, and why this matters for interpreting a single p-value in isolation. | for a fixed true effect, a larger n produces smaller SE, which pushes the test statistic (and hence the p-value) further from what H₀ predicts even for the same underlying effect; conversely a huge n can produce a tiny p-value for a practically negligible effect, while a small n can fail to produce a small p-value even for a substantively large effect — the p-value alone conflates "how big is the effect" with "how much data do we have," which is why it should be reported alongside an effect size estimate *(required: explicitly separates the effect-size and sample-size contributions)* | — |
| E5 | explain | short-answer | 1.85 | Explain why treating p=0.049 as "significant" and p=0.051 as "not significant" is treating a somewhat arbitrary line as if it were a meaningful cliff edge. | the underlying evidence at p=0.049 and p=0.051 is nearly identical — both reflect data almost equally (un)surprising under H₀ — yet the binary significant/non-significant framing forces them into different categorical buckets; this reflects a property of the α=0.05 convention (a chosen threshold), not a genuine qualitative difference in the strength of evidence the two results actually provide *(required: explicitly states the near-identical evidential content despite the different bucket)* | — |
| T2 | transfer | short-answer | 2.2 | A pharmaceutical company runs a large trial (n=50,000) and reports p<0.0001 for a drug's effect on a biomarker, with the actual measured difference being clinically negligible (e.g. a 0.2% change). Critique the practice of reporting only the p-value here, using E4's reasoning. | at n=50,000, even a tiny, clinically irrelevant true effect will very likely produce an extremely small p-value, since SE shrinks enormously with such a large n — the p-value's extremeness here reflects the sample size, not necessarily a meaningful effect; reporting p<0.0001 alone, without the actual effect size (0.2%) and its clinical relevance, risks the reader inferring a dramatic, important finding from what may be a practically inconsequential one *(required: explicitly names sample size as the driver of the extreme p-value here, not a large true effect)* | treats an extremely small p-value as automatic evidence of a large, practically important effect → `p-value` |
| T3 | transfer | short-answer | 2.3 | A journal historically only publishes "statistically significant" (p<0.05) results. Explain the population-level distortion this creates in the published scientific record, even if every individual study's p-value is computed correctly. | if studies with p≥0.05 are systematically never published ("the file drawer problem"), the published literature becomes a biased sample of all studies actually conducted — genuine null results (correctly computed, honest p-values above 0.05) are hidden from view, while a disproportionate share of published "significant" findings may themselves be false positives (recall the α-rate of Type I errors occurring across many attempted studies, echoing `hypothesis-test`'s T1 multiple-comparisons logic, now playing out across an entire field rather than one team's 20 tests) *(required: connects individual-study correctness to a systemic, publication-driven distortion, and references the same underlying false-positive-accumulation mechanism)* | assumes that because each individual p-value is computed correctly, the resulting published literature as a whole must also be a trustworthy, unbiased picture of the evidence → `p-value` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.3.*

---

## Confidence Interval (`confidence-interval`)
*Prereq: Standard Error · ancestors 23 · b₀ = 1.09*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.09 | State the general CI formula, and what "95% confidence" correctly means. | X̄ ± (critical value)·SE; "95% confidence" describes the *procedure*: if repeated across many samples, about 95% of the resulting intervals would contain the true parameter — not a probability statement about one specific interval | — |
| R2 | recall | mcq | 0.39 | For a 95% CI [45,55], the correct interpretation is: | if this sampling procedure were repeated many times, about 95% of the resulting intervals would contain the true parameter | picks "there's a 95% probability the true parameter is between 45 and 55" — the central, defining subtlety this whole concept exists to correct → `confidence-interval` |
| A1 | apply | numeric | 0.89 | X̄=50, SE=2. Construct an approximate 95% CI using critical value 1.96. `[verified: [46.08, 53.92]]` | 50 ± 1.96(2) = [46.08, 53.92] | — |
| A2 | apply | short-answer | 1.0 | A 95% CI for a drug's effect is [−2, 8], which includes 0. What does this say about rejecting H₀:effect=0 at α=0.05? | fail to reject — since the CI includes 0, the equivalent hypothesis test would not reject H₀ at this α *(required: uses the CI-test equivalence, not a fresh calculation)* | — |
| E1 | explain | short-answer | 1.59 | Why does a 95% CI excluding μ₀ correspond exactly to rejecting H₀:μ=μ₀ at α=0.05? | both procedures are built from the same test-statistic/rejection-region logic, algebraically rearranged — both ultimately ask "is μ₀ more than (critical value)×SE away from X̄?", just phrased as an interval containing/excluding a value versus a statistic falling in/out of a region *(required: the shared underlying question, not just "they happen to agree")* | — |
| E2 | explain | short-answer | 1.75 | Why is it technically wrong (in the frequentist framework) to say "there's a 95% probability the true parameter is in this specific, already-computed interval"? | the true parameter is a fixed, unknown number, not a random variable, in this framework — once the interval is computed, the parameter either is or isn't in it (probability 1 or 0, just unknown which); "95%" describes the *method's* reliability across hypothetical repetitions, not a probability statement about this one fixed interval *(required: the fixed-parameter argument specifically)* | — |
| T1 | transfer | short-answer | 2.09 | A poll reports "52% support, 95% CI [48%, 56%]." A commentator says "there's a 95% chance the true support is between 48% and 56%." What's wrong, and what's the correct interpretation? | this is exactly R2/E2's misinterpretation, applied to its most common real-world venue; the correct statement: the *procedure* used to generate such intervals would capture the true support level in about 95% of hypothetical repeated polls — not that this specific interval has a 95% probability of containing it *(required: restates the correct procedural interpretation explicitly)*; worth noting Bayesian credible intervals *do* support the more intuitive reading, which is part of why this confusion is so persistent | restates the commentator's claim as basically fine, just informally phrased → `confidence-interval` |
| R3 | recall | short-answer | −1.4 | State the critical value used for a 90% confidence interval (two-tailed, using the Normal approximation). | 1.645 | — |
| R4 | recall | mcq | −1.25 | Increasing the confidence level (e.g. from 90% to 99%), holding n fixed, makes the interval: | wider | claims a higher confidence level always produces a *narrower* interval, backwards — more confidence requires a wider net to keep the same coverage guarantee → `confidence-interval` |
| R5 | recall | short-answer | −1.15 | True or false: a confidence interval is centered on the sample statistic (e.g. X̄), not on the unknown population parameter. | true — the interval is X̄ ± margin, built around the *observed* statistic, since the true parameter is unknown | — |
| R6 | recall | short-answer | −1.05 | Fill in the blank: the general form of a confidence interval is (point estimate) ± (critical value) × ____. | SE (the standard error) | — |
| R7 | recall | mcq | −0.95 | For a 99% CI (two-tailed, Normal approximation), the critical value is approximately: | 2.576 | uses 1.96, the 95% critical value, for a 99% interval → `confidence-interval` |
| R8 | recall | short-answer | −0.85 | Define "margin of error" in the context of a confidence interval. | the amount added to and subtracted from the point estimate to form the interval — (critical value) × SE | — |
| R9 | recall | mcq | −0.75 | A wider confidence interval, at the same confidence level, generally results from: | a smaller sample size (larger SE) | claims a wider interval results from "a higher-quality, more precise sample," which would actually produce a *narrower* interval, not wider → `confidence-interval` |
| R10 | recall | short-answer | −0.65 | True or false: as sample size increases, a confidence interval at a fixed confidence level tends to narrow. | true — larger n reduces SE, shrinking the margin of error for the same critical value | — |
| R11 | recall | short-answer | −0.55 | Fill in the blank: a confidence interval that includes the hypothesized value μ₀ corresponds to ____ H₀ at the matching α. | failing to reject | — |
| R12 | recall | mcq | −0.45 | The confidence level (e.g. 95%) describes: | the long-run success rate of the interval-construction *procedure* across repeated samples | describes it as "the probability that this specific interval, once computed, contains the true parameter," the exact misinterpretation this concept's R2 warns against → `confidence-interval` |
| R13 | recall | short-answer | −0.35 | State the relationship between a two-tailed test's α and the confidence level of the corresponding CI. | confidence level = 1 − α (e.g. α=0.05 two-tailed test pairs with a 95% CI) | — |
| R14 | recall | mcq | −0.25 | A 95% CI for a mean difference that is entirely above 0 (e.g. [2, 8]) implies: | the corresponding two-tailed test at α=0.05 would reject H₀: difference=0 | implies "we are 95% certain the true difference is exactly 5" (the interval's midpoint), collapsing an entire interval of plausible values into one falsely-precise point claim → `confidence-interval` |
| R15 | recall | short-answer | −0.15 | True or false: a confidence interval provides a single best-guess point estimate with no accompanying uncertainty information. | false — the entire purpose of a CI is to pair a point estimate with a *quantified range* reflecting sampling uncertainty, unlike a bare point estimate | — |
| R16 | recall | short-answer | −0.05 | Fill in the blank: the general CI formula X̄ ± (critical value)·SE mirrors the same structure used to build a ____ region for a hypothesis test. | rejection | — |
| A3 | apply | numeric | 0.9 | X̄=100, SE=5. Construct an approximate 90% CI using critical value 1.645. `[verified: [91.775, 108.225]]` | 100 ± 1.645(5) = [91.775, 108.225] | — |
| A4 | apply | numeric | 0.95 | X̄=200, SE=4. Construct an approximate 99% CI using critical value 2.576. `[verified: [189.696, 210.304]]` | 200 ± 2.576(4) = [189.696, 210.304] | — |
| A5 | apply | short-answer | 1.0 | A 95% CI for a new policy's effect on satisfaction is [−1.5, 0.5], straddling 0. What does this say about rejecting H₀:effect=0 at α=0.05? | fail to reject — since the interval includes 0, the equivalent test would not reject H₀ at α=0.05 | — |
| A6 | apply | short-answer | 1.05 | A sample's 95% CI is [40, 60] (width 20). If the sample size were quadrupled with everything else the same, roughly how wide would the new 95% CI be? | roughly half as wide, about 10 (width ≈ 20) — since quadrupling n halves SE (the √n scaling from `sample-mean`), and the margin of error scales directly with SE at a fixed critical value | — |
| E3 | explain | short-answer | 1.75 | Explain why increasing the confidence level (e.g. 95%→99%) necessarily widens the interval, using the critical-value mechanism. | a higher confidence level requires the interval to succeed in capturing the true parameter more often across repeated samples, which requires a larger critical value (more standard errors on each side) to maintain that higher hit rate — the tradeoff mirrors decreasing α in `rejection-region`, which likewise pushes critical values further from center *(required: the explicit critical-value-size mechanism)* | — |
| E4 | explain | short-answer | 1.8 | Explain the tension between wanting a *narrow* (precise) interval and wanting a *high* confidence level, and why you can't maximize both simultaneously at fixed n. | narrowing the interval (smaller critical value) necessarily lowers the confidence level, since a tighter net around X̄ is less likely to capture the true parameter across repeated samples; raising confidence (larger critical value) necessarily widens the interval — precision and confidence trade off directly against each other unless n itself is increased, which is the only lever that can improve both simultaneously *(required: explicitly states that increasing n is the escape valve, echoing `power`'s escape-valve argument for α/β)* | — |
| E5 | explain | short-answer | 1.85 | Explain why the CI-hypothesis-test duality (E1 from before) means a CI can be used to test *many* different H₀ values at once, not just one. | a single computed CI, e.g. [46, 54], simultaneously tells you the outcome of a two-tailed test at α=0.05 for *every* possible μ₀: any μ₀ inside the interval would fail to be rejected, and any μ₀ outside would be rejected — one interval efficiently summarizes the results of infinitely many individual hypothesis tests, all at the matching α, which a single hypothesis test targeting one specific μ₀ cannot do *(required: explicitly frames the CI as summarizing many simultaneous tests)* | — |
| T2 | transfer | short-answer | 2.15 | A retailer's dashboard shows a 95% CI for weekly revenue lift from a new feature: [−$2,000, $18,000]. A manager says "great, the average case is an $8,000 gain, let's ship it." Critique this reading of the interval. | the manager is treating the interval's midpoint as if it were a confidently-known "average case," when the actual message of a CI this wide is substantial uncertainty — the true lift could plausibly be anywhere from a $2,000 *loss* to an $18,000 gain, and the interval including 0 (well within this wide range) means the data doesn't even clearly rule out no effect at all; picking out the midpoint as "the" expected outcome discards exactly the uncertainty information the interval exists to convey *(required: explicitly notes the interval spans a loss to a large gain, and that 0 is well within range)* | reads the interval's midpoint as a confident point estimate, ignoring the width of the interval entirely → `confidence-interval` |
| T3 | transfer | numeric | 2.2 | A study currently reports a 95% CI of width 8 (e.g. [46,54]) from n=100. Using the √n scaling, estimate the width of a 95% CI from the same population at n=400. `[verified: width ≈ 4]` | quadrupling n halves SE (per `sample-mean`'s square-root law), so the margin of error — and hence the interval's width — also roughly halves, from 8 to about 4, at the same 95% confidence level *(required: the explicit halving via the √n scaling, not a guess)* | assumes quadrupling the sample size would also roughly quadruple or halve the width by the same factor as n itself, rather than by √n → `confidence-interval` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.2.*

---

## Cluster 10 misconception index

| Tag | Blame |
|---|---|
| Normal-based methods rejected because raw data isn't Normal | `sampling-distribution` |
| CLT's population-shape-independence misunderstood or its data requirements confused | `sampling-distribution` |
| SD and SE conflated in either direction | `standard-error` |
| SE incorrectly assumed insensitive to sample size | `standard-error` |
| test-statistic standardization purpose missed | `test-statistic` |
| large test-statistic magnitude treated as proof rather than evidence | `test-statistic` |
| one-/two-tailed critical values swapped, or test type switched post hoc | `rejection-region` |
| rejection-region boundary direction confused with α direction | `rejection-region` |
| "fail to reject" read as "proven true" | `hypothesis-test` |
| multiple-comparisons inflation ignored | `hypothesis-test` |
| H₀/H₁ roles or directionality reversed | `hypothesis-test` |
| α/β tradeoff treated as one-directional ("lower is always better") | `type-i-ii-error` |
| Type I and Type II error definitions swapped | `type-i-ii-error` |
| low-power non-significance treated as evidence of no effect | `power` |
| post-hoc ("observed") power treated as informative | `power` |
| **p-value read as P(H₀ true)** | `p-value` |
| p-value's dependence on sample size, not just effect size, ignored | `p-value` |
| **95% CI read as a probability statement about one fixed interval** | `confidence-interval` |
| CI width scaling with n confused with a linear (not √n) relationship | `confidence-interval` |

**Cluster total: 270 items across 9 concepts (30 each).** All numeric claims verified, including the
exact multiple-comparisons inflation figure (1−0.95²⁰≈64%), the two-tailed p-value at Z=2.5, and every
newly added Z/p-value/CI computation. This cluster carries the two most consequential misreadings in
all of applied statistics, and both get dedicated, unambiguous treatment: `p-value`'s R2/T1 and
`confidence-interval`'s R2/E2/T1.
