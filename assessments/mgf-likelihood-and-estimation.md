# Cluster 6 — MGF, Likelihood & Estimation

MGF → Exponential Family (8 concepts). Same format as [Cluster 1](foundations-of-probability.md).

This cluster closes several loops opened earlier in the bank on purpose: `mgf-properties`' T1
re-derives the very first pilot file's core fact (sum of iid Bernoullis is Binomial) via MGF-matching
instead of combinatorics; `mle`'s T1 and `exponential-family`'s E1 both turn on the same surprising
fact about Uniform(0,θ); and `distribution-transformations`' E1 makes explicit that Cluster 4's Normal
standardization proof was secretly a special case of this cluster's general theorem, proved before the
theorem existed.

---

## Moment Generating Function (`mgf`)
*Prereq: Expectation · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | Define M_X(t) = E[e^{tX}], and state what M_X(0) always equals. | M_X(0) = E[e⁰] = E[1] = 1, for *every* distribution | — |
| R2 | recall | mcq | 0.0 | M_X′(0) equals: | E[X] | picks Var(X) — conflating the first derivative with the second → `mgf` |
| A1 | apply | short-answer | 0.5 | X = 1 with probability p, 0 with probability 1−p. Find M_X(t) directly from the definition. `[verified: M(0)=1]` | M(t) = (1−p)e⁰ + pe^t = (1−p) + pe^t; check M(0)=(1−p)+p=1 ✓ | — |
| A2 | apply | numeric | 0.65 | Using A1's MGF, differentiate to recover E[X] and check it matches p. `[verified]` | M′(t) = pe^t; M′(0) = p = E[X], matching directly | — |
| E1 | explain | derivation | 1.2 | Derive M_X^{(k)}(0) = E[X^k] — the k-th derivative at 0 gives the k-th raw moment. | differentiating e^{tX} k times with respect to t gives X^k e^{tX}; evaluating at t=0 leaves X^k; taking E[·] gives E[X^k] *(required: the differentiate-then-evaluate-then-expect chain, not just the stated result)* | — |
| E2 | explain | short-answer | 1.4 | Explain why the MGF, when it exists in an interval around 0, uniquely determines the distribution — and why that makes it a useful *proof tool*. | the uniqueness theorem: two distributions with matching MGFs (on a common interval containing 0) are the same distribution; this means showing two MGFs match is *sufficient* to prove two random variables have the same distribution, without ever comparing pmfs or pdfs directly *(required)* | — |
| T1 | transfer | short-answer | 1.7 | Why is the MGF sometimes called a distribution's "fingerprint"? Give a scenario where comparing two MGFs is easier than comparing two pmfs or pdfs directly. | sums of independent random variables, and distributions defined only as limits, are both cases where algebra on MGFs (multiplying, or taking a limit of a single function of t) is far simpler than manipulating pmf/pdf expressions directly — this is exactly the machinery `mgf-properties` builds next | — |
| R3 | recall | mcq | −0.28 | M_X″(0) equals: | E[X²], the second raw moment | picks Var(X) directly, skipping that the second *derivative* gives the raw moment E[X²], not the centered variance → `mgf` |
| R4 | recall | short-answer | −0.24 | State Var(X) in terms of M_X′(0) and M_X″(0). | Var(X) = M″(0) − [M′(0)]² | — |
| R5 | recall | mcq | −0.2 | True or false: every random variable has an MGF that exists (is finite) for all real t. | False — the MGF may exist only on a bounded interval around 0, or not at all beyond t=0 (e.g. Cauchy) | claims every distribution has an MGF defined for all t, missing that heavy tails can make E[e^{tX}] diverge → `mgf` |
| R6 | recall | short-answer | −0.16 | Fill in the blank: M_X(t) is defined as E[___]. | e^{tX} | — |
| R7 | recall | mcq | −0.12 | Every MGF, when it exists, takes what value at t=0? | 1, since M_X(0) = E[e⁰] = E[1] = 1 for any distribution | claims the value at t=0 depends on the distribution's mean or variance → `mgf` |
| R8 | recall | mcq | −0.08 | True or false: if two random variables have MGFs that agree on some open interval containing 0, the two random variables have the same distribution. | True — this is the MGF uniqueness theorem, the basis of E2's "proof tool" use | claims matching MGFs only guarantees matching means, not full distributional equality → `mgf` |
| R9 | recall | short-answer | −0.04 | State the general relationship between the k-th derivative of the MGF at 0 and the moments of X. | M_X^{(k)}(0) = E[X^k], the k-th raw moment | — |
| R10 | recall | mcq | 0.02 | M_X′(0) gives which quantity? | E[X], the mean | gives Var(X), swapping which derivative order corresponds to which moment → `mgf` |
| R11 | recall | short-answer | 0.06 | Fill in the blank: the word "moment" in "moment generating function" refers to the ___ of X. | raw moments, E[X^k] for k=1,2,3,... | — |
| R12 | recall | mcq | 0.1 | True or false: an MGF, wherever it exists, is always a monotonically increasing function of t. | False — an MGF need not be monotonic in t; only its value and derivatives at t=0 carry the moment information used in this cluster | claims monotonicity is a required property of every MGF, over-generalizing from a few worked examples → `mgf` |
| R13 | recall | short-answer | 0.14 | Write the Taylor series of e^{tX} in powers of t, and explain in one line why it justifies M_X^{(k)}(0) = E[X^k]. | e^{tX} = 1 + tX + (t²X²)/2! + (t³X³)/3! + ...; taking E[·] termwise gives M_X(t) = 1 + tE[X] + (t²/2!)E[X²] + ..., so the coefficient of tᵏ/k! is exactly E[X^k], recovered by the k-th derivative at t=0 | — |
| R14 | recall | mcq | 0.18 | For a constant c (a degenerate random variable that always equals c), M_c(t) equals: | e^{ct} | claims M_c(t) = c for all t, treating the MGF as if it just returned the constant itself rather than E[e^{tc}] | — |
| R15 | recall | short-answer | 0.22 | State the condition for an MGF to "exist" in the sense this cluster uses. | E[e^{tX}] must be finite for t in some open interval containing 0 | — |
| R16 | recall | mcq | 0.26 | M_X(t) is a function of which variable? | t, the dummy transform variable — X itself is integrated/summed out inside the expectation | claims M_X(t) is a function of X, missing that X has already been averaged over inside the expectation → `mgf` |
| A3 | apply | numeric | 0.44 | X~Poisson(3), with M_X(t)=exp(3(e^t−1)). Differentiate to find E[X]. `[verified: 3]` | M′(t) = 3e^t·exp(3(e^t−1)); M′(0) = 3·1·1 = 3 = E[X], matching the known Poisson mean λ=3 | — |
| A4 | apply | numeric | 0.56 | X~Exponential(2), with M_X(t)=2/(2−t) for t<2. Differentiate to find E[X]. `[verified: 0.5]` | M′(t) = 2/(2−t)²; M′(0) = 2/4 = 0.5 = E[X], matching the known Exponential mean 1/λ=0.5 | — |
| A5 | apply | short-answer | 0.58 | A distribution has M_X(t) = exp(2t + 1.5t²). Without deriving from a density, read off E[X] and Var(X) directly from this Normal-family MGF form exp(μt + σ²t²/2). `[verified: E[X]=2, Var(X)=3]` | matching exp(μt+σ²t²/2) to exp(2t+1.5t²) gives μ=2 and σ²/2=1.5 so σ²=3; E[X]=2, Var(X)=3 | reads off σ²=1.5 directly instead of solving σ²/2=1.5, off by a factor of 2 → `mgf` |
| A6 | apply | numeric | 0.62 | X takes value 1 with probability 0.7, 0 with probability 0.3 (from A1's form with p=0.7). Evaluate M_X(1) numerically. `[verified: ≈2.2028]` | M(1) = 0.3 + 0.7e¹ ≈ 0.3 + 0.7(2.71828) ≈ 2.2028 | evaluates M(t) at t=1 by plugging t=1 into E[X] instead of into e^{tX}, e.g. reports 0.7 → `mgf` |
| E3 | explain | derivation | 1.28 | Derive Var(X) = M″(0) − [M′(0)]², starting from Var(X) = E[X²] − (E[X])². | by R9, M′(0)=E[X] and M″(0)=E[X²]; substituting directly into Var(X)=E[X²]−(E[X])² gives Var(X)=M″(0)−[M′(0)]² *(required: the explicit substitution of both derivative identities)* | — |
| E4 | explain | short-answer | 1.42 | Using the Taylor series of e^{tX} from R13, explain intuitively why the MGF "generates" all the moments at once, rather than needing a separate formula per moment. | expanding e^{tX} as a single power series in t packages every power X^k into one function, with E[X^k] sitting as the coefficient of tᵏ/k!; differentiating k times and evaluating at t=0 is just the standard way to extract a single Taylor coefficient — one object (M_X(t)) therefore encodes the entire sequence of moments *(required: ties the coefficient-extraction idea to differentiation, not just asserting the MGF is "convenient")* | — |
| E5 | explain | short-answer | 1.5 | Explain why matching a *finite* number of moments between two distributions does not generally prove they're the same distribution, but matching the *entire* MGF on an interval around 0 does. | a finite set of moments (say, mean and variance) leaves infinitely many distributions consistent with those two numbers alone — moments beyond that set can still differ; the MGF, when it exists on an interval, packages *every* moment simultaneously (R13), so two MGFs agreeing there forces agreement of every single moment, which the uniqueness theorem (R8) upgrades to full distributional equality *(required: the finite-vs-all-moments distinction)* | — |
| T2 | transfer | short-answer | 1.8 | An actuary needs the mean and variance of total claims across a large book of policies, but the exact density of the total is intractable to write down. Explain how working with the MGF sidesteps needing that density at all. | the actuary only needs M_S(t) for the total S (obtainable, as `mgf-properties` will show, from the individual policies' MGFs) and its low-order derivatives at 0 — by R9/E3, those derivatives hand back E[S] and Var(S) directly, without ever writing down or integrating S's actual density *(required: names that the derivatives at 0, not the full density, supply the needed moments)* | insists the exact density of S must be derived before any moment of S can be computed → `mgf` |
| T3 | transfer | short-answer | 1.9 | A risk analyst finds that a stock's log-return MGF exp(tX) has E[e^{tX}] = ∞ for any t beyond some threshold t₀ > 0. Explain what this tells the analyst about the tail risk of the return distribution, and why it's a warning sign for risk measures that use exponential weighting. | E[e^{tX}] diverging for t beyond t₀ means the distribution's upper tail decays too slowly for the exponential weight e^{tX} to be tamed — a signature of heavy (fat) tails; any risk measure built on exponential weighting (e.g. exponential utility, certain large-deviation bounds) implicitly assumes the MGF exists in a neighborhood it needs, so this divergence is a direct warning that such tools will silently fail or give misleading answers here *(required: connects the divergence to heavy tails and names a concrete consequence)* | treats the MGF's non-existence beyond t₀ as a technicality with no implication for the distribution's actual tail behavior → `mgf` |

*Coverage: 16/6/5/3 — 30 items, −0.3…1.9.*

---

## MGF Properties and Applications (`mgf-properties`)
*Prereq: MGF · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State the MGF of X+Y for independent X, Y, and of aX+b in terms of M_X. | M_{X+Y}(t) = M_X(t)M_Y(t) (independent only); M_{aX+b}(t) = e^{bt}M_X(at) | — |
| R2 | recall | mcq | 0.04 | The product rule M_{X+Y}(t) = M_X(t)M_Y(t) requires: | X, Y independent | claims it holds "always" — a genuinely easy over-generalization to make → `mgf-properties` |
| A1 | apply | short-answer | 0.54 | Given M_Poisson(λ)(t) = exp(λ(e^t−1)), find M_{X+Y}(t) for independent X~Poisson(λ), Y~Poisson(μ), and identify the resulting distribution. `[verified: matches Poisson(λ+μ)]` | M_{X+Y}(t) = exp(λ(e^t−1))·exp(μ(e^t−1)) = exp((λ+μ)(e^t−1)) — exactly Poisson(λ+μ)'s MGF, re-deriving `poisson-distribution`'s convolution result in one line | — |
| A2 | apply | short-answer | 0.65 | Y₁,…,Yₙ are iid Bernoulli(p) with MGF (1−p)+pe^t (from `mgf`'s A1). Find the MGF of S=ΣYᵢ and identify its distribution. `[verified: matches known Binomial MGF]` | M_S(t) = [(1−p)+pe^t]ⁿ — exactly the Binomial(n,p) MGF; by uniqueness, S ~ Binomial(n,p), reproducing the very first combinatorial fact in this bank without any counting argument at all | — |
| E1 | explain | derivation | 1.24 | Derive M_{X+Y}(t)=M_X(t)M_Y(t) for independent X,Y from the definition. | M_{X+Y}(t) = E[e^{t(X+Y)}] = E[e^{tX}e^{tY}]; since X,Y are independent, so are e^{tX} and e^{tY} (functions of independent variables), so E[e^{tX}e^{tY}] = E[e^{tX}]E[e^{tY}] = M_X(t)M_Y(t) *(required: names explicitly why independence of X,Y transfers to independence of the transformed quantities)* | — |
| E2 | explain | short-answer | 1.4 | Why doesn't *every* distribution have an MGF? Give the intuition using a heavy-tailed example. | e^{tX} grows exponentially in X; if a distribution's tails decay too slowly (e.g. only polynomially, as with Cauchy), E[e^{tX}] diverges to infinity for any t≠0, so the MGF simply doesn't exist near 0 *(required: connects tail behavior to the divergence, not just "some distributions don't have one")* | — |
| T1 | transfer | derivation | 1.74 | Using only the MGF product rule (not combinatorics), re-derive that the sum of n iid Bernoulli(p) is Binomial(n,p) — the fact this whole bank started with. | M_S(t) = [(1−p)+pe^t]ⁿ (from A2); recognizing this as exactly the Binomial(n,p) MGF (itself obtainable from `binomial-theorem`: Σ C(n,k)(pe^t)^k(1−p)^{n−k} = ((1−p)+pe^t)ⁿ) means, by uniqueness, S must be Binomial(n,p) *(required: closes the loop explicitly — this is the same fact as the pilot file's opening PMF, now proved a second, very different way)* | re-derives the combinatorial proof instead of using the MGF machinery this item is testing → `mgf-properties` |
| R3 | recall | short-answer | −0.22 | Fill in the blank: M_{aX}(t) = ___. | M_X(at) | — |
| R4 | recall | mcq | −0.18 | M_{X+b}(t), for constant b, equals: | e^{bt} M_X(t) | claims M_{X+b}(t) = M_X(t) + b, treating the shift as additive on the MGF instead of multiplicative → `mgf-properties` |
| R5 | recall | mcq | −0.14 | True or false: for independent X, Y, Var(X+Y) can be recovered by taking the second derivative of the product M_X(t)M_Y(t) at t=0, exactly as `mgf`'s R9 does for a single variable. | True | claims the product rule only helps recover the mean, not higher moments, of the sum → `mgf-properties` |
| R6 | recall | short-answer | −0.1 | State the general n-variable version of the MGF product rule, for independent X₁,...,Xₙ. | M_{ΣXᵢ}(t) = ∏ᵢ M_{Xᵢ}(t) | — |
| R7 | recall | mcq | −0.06 | Poisson distributions are "closed under summation" for independent variables because: | the product of two Poisson MGFs, exp(λ(e^t−1))·exp(μ(e^t−1)), is itself exactly a Poisson MGF, with parameter λ+μ | claims closure under summation is a property every distribution family automatically has, not something that must be checked via the MGF product → `mgf-properties` |
| R8 | recall | mcq | −0.02 | True or false: for *dependent* X, Y, M_{X+Y}(t) still factors as M_X(t)M_Y(t). | False — the factorization step in E1 explicitly used independence to split E[e^{tX}e^{tY}] into a product of expectations; dependence breaks that step | claims the product rule holds regardless of dependence, missing R2/E1's independence requirement → `mgf-properties` |
| R9 | recall | short-answer | 0.02 | Fill in the blank: M_{aX+b}(t) = ___. | e^{bt} M_X(at) — combining R3 and R4's two separate rules | — |
| R10 | recall | mcq | 0.06 | The key algebraic fact that makes M_{X+Y}(t)=M_X(t)M_Y(t) true for independent X, Y is: | E[e^{tX}e^{tY}] = E[e^{tX}]·E[e^{tY}] — expectation of a product of independent quantities factors | claims it's true because e^{t(X+Y)} always factors as e^{tX}e^{tY}, missing that the *expectation* factoring (not just the exponent) is what needs independence → `mgf-properties` |
| R11 | recall | short-answer | 0.1 | Name one named distribution family where the MGF product of two independent members is generally NOT itself a member of the same family with updated parameters. | Uniform — the sum of two independent Uniforms is not Uniform (it's a triangular-shaped distribution instead) | — |
| R12 | recall | mcq | 0.14 | If X~N(μ₁,σ₁²) and Y~N(μ₂,σ₂²) are independent, X+Y is distributed as: | N(μ₁+μ₂, σ₁²+σ₂²) | claims the variances subtract or the means multiply, misapplying the general sum rule to the specific Normal case → `mgf-properties` |
| R13 | recall | mcq | 0.18 | True or false: for M_{X+Y}(t)=M_X(t)M_Y(t) to even make sense, both M_X and M_Y need to exist on a common interval around 0. | True | claims the product rule works even if one of the two individual MGFs doesn't exist near 0 → `mgf-properties` |
| R14 | recall | short-answer | 0.22 | Fill in the blank: for independent X, Y, ln M_{X+Y}(t) = ___ + ___. | ln M_X(t) + ln M_Y(t) | — |
| R15 | recall | short-answer | 0.26 | What does raising a single distribution's MGF to the n-th power, M_X(t)ⁿ, correspond to? | the MGF of the sum of n independent copies of X (an iid sample of size n) | — |
| R16 | recall | mcq | 0.3 | Using the MGF product rule, the sum of two independent Poisson random variables with parameters λ and μ is distributed as: | Poisson(λ+μ) | claims the sum is Poisson(λμ) or Poisson(max(λ,μ)), misremembering how the parameters combine → `mgf-properties` |
| A3 | apply | short-answer | 0.46 | X~Gamma(2,1), Y~Gamma(3,1) independent, with M_Gamma(α,β)(t)=(β/(β−t))^α. Find M_{X+Y}(t) and identify the resulting distribution. `[verified: exponent adds to 5]` | M_{X+Y}(t) = (1/(1−t))² · (1/(1−t))³ = (1/(1−t))⁵ — exactly the Gamma(5,1) MGF, so X+Y ~ Gamma(5,1) | multiplies the α exponents instead of adding them, e.g. reports (1/(1−t))⁶ → `mgf-properties` |
| A4 | apply | short-answer | 0.58 | X~N(2,3), Y~N(−1,4), independent. Using the MGF product rule, identify the distribution of X+Y. `[verified: N(1,7)]` | mean = 2+(−1) = 1; variance = 3+4 = 7; X+Y ~ N(1,7) | adds the standard deviations instead of the variances to get the combined spread → `mgf-properties` |
| A5 | apply | numeric | 0.6 | Var(X)=4, Var(Y)=9, X and Y independent. Using the fact that the second derivative of ln M_{X+Y}(t) at 0 gives Var(X+Y), find Var(X+Y). `[verified: 13]` | Var(X+Y) = Var(X)+Var(Y) = 4+9 = 13, consistent with R5/R14's log-MGF-additivity fact | subtracts the variances (4−9 or 9−4) instead of adding them for an independent sum → `mgf-properties` |
| A6 | apply | numeric | 0.68 | X₁~Bernoulli(0.3), X₂~Bernoulli(0.6), independent. Find Var(X₁+X₂) via the MGF product rule's implied additivity. `[verified: 0.45]` | Var(X₁)=0.3(0.7)=0.21; Var(X₂)=0.6(0.4)=0.24; Var(X₁+X₂)=0.21+0.24=0.45 | computes Var(X₁+X₂) as Var(X₁)·Var(X₂) instead of the sum, confusing the additive rule with a multiplicative one → `mgf-properties` |
| E3 | explain | derivation | 1.32 | Derive ln M_{X+Y}(t) = ln M_X(t) + ln M_Y(t) for independent X, Y, and explain what this implies about how *cumulants* (derivatives of the log-MGF at 0) behave for independent sums. | taking logs of both sides of M_{X+Y}(t)=M_X(t)M_Y(t) turns the product into a sum: ln M_{X+Y}(t) = ln M_X(t) + ln M_Y(t); since cumulants are derivatives of the log-MGF at 0, and derivatives of a sum are the sum of derivatives, every cumulant of X+Y is the sum of the corresponding cumulants of X and Y — a stronger, cleaner statement than "variances add," which is just the second cumulant case *(required: the log step and the cumulant-additivity consequence)* | — |
| E4 | explain | derivation | 1.42 | Derive M_{aX+b}(t) = e^{bt}M_X(at) directly from the definition M_{aX+b}(t)=E[e^{t(aX+b)}]. | E[e^{t(aX+b)}] = E[e^{atX}·e^{tb}] = e^{tb}·E[e^{(at)X}] = e^{bt}·M_X(at), pulling the constant e^{tb} out of the expectation and recognizing E[e^{(at)X}] as M_X evaluated at at *(required: both the factoring-out and the re-evaluation-at-at steps)* | — |
| E5 | explain | short-answer | 1.52 | Explain why knowing M_{X+Y}(t) matches some target MGF does NOT, by itself, tell you what the individual M_X(t) and M_Y(t) are. | a single product M_X(t)M_Y(t) can be factored into two functions in more than one way — infinitely many pairs of valid MGFs can multiply to the same product (e.g. shifting mass between the two "component" distributions while keeping the total fixed), so the sum's MGF alone under-determines the two individual pieces without further information about each variable separately *(required: the non-unique-factorization point, not just "you'd need more information")* | — |
| T2 | transfer | short-answer | 1.85 | An actuary has a portfolio of 10,000 independent policies, each with its own MGF, and needs the mean and variance of total claims without ever writing down the total's joint distribution. Explain how the MGF product rule solves this. | by R6, the total's MGF is the product of the 10,000 individual MGFs; taking the log (E3) turns this into a sum of individual log-MGFs, so the total's mean and variance are just the sum of each policy's mean and variance (R5) — computable policy-by-policy without ever constructing or integrating a 10,000-dimensional joint distribution *(required: names that only per-policy quantities, summed, are needed)* | insists the full joint distribution of all 10,000 policies must be derived before any moment of the total can be found → `mgf-properties` |
| T3 | transfer | short-answer | 1.95 | A signal-processing engineer models sensor noise as a sum of many independent noise sources and uses the MGF product rule to derive properties of the combined noise. A colleague suggests modeling two of the sources as dependent instead (say, both driven by the same temperature fluctuation) without changing the analysis. Explain why this would break the technique. | the MGF product rule's derivation (E1) explicitly needs E[e^{tX}e^{tY}] = E[e^{tX}]E[e^{tY}], which only holds under independence (R8); if two sources share a common driver like temperature, they become dependent, the factorization step fails, and M_{X+Y}(t) ≠ M_X(t)M_Y(t) in general — the combined-noise moments computed from the product would then be wrong *(required: ties the failure specifically to the independence step in the product-rule proof)* | assumes the product rule still applies to dependent sources as long as each one individually has a well-defined MGF → `mgf-properties` |

*Coverage: 16/6/5/3 — 30 items, −0.26…1.95. A2/T1 are the standout pair — this is the moment the whole MGF apparatus pays for itself.*

---

## Likelihood vs Probability (`likelihood-vs-probability`)
*Prereq: PMF, PDF · ancestors 9 · b₀ = 0.65*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.35 | Define the likelihood L(θ) given observed data x, and state the key distinction from the probability P(x\|θ). | same formula p(x\|θ) or f(x\|θ); probability varies x for *fixed* θ, likelihood varies θ for *fixed* (observed) x | — |
| R2 | recall | mcq | −0.05 | Viewed as a function of θ, the likelihood L(θ): | is not generally a probability distribution over θ at all | claims L(θ) "always integrates to 1 over θ" — the single most consequential confusion in this topic → `likelihood-vs-probability` |
| A1 | apply | short-answer | 0.45 | For n iid Bernoulli(θ) observations with k successes, write L(θ). | L(θ) = θᵏ(1−θ)ⁿ⁻ᵏ | writes θᵏ(1−θ)ᵏ, reusing k in both exponents → `likelihood-vs-probability` |
| A2 | apply | numeric | 0.6 | For n=10, k=7, compare L(0.5) and L(0.7) numerically — which θ is "more likely" given this data? `[verified: L(0.5)≈0.000977, L(0.7)≈0.002224]` | L(0.7) > L(0.5) — θ=0.7 is more likely, sensibly matching the observed proportion 7/10 | — |
| E1 | explain | derivation | 1.15 | Using A1's likelihood, show it is maximized at θ=k/n by differentiating log L(θ). | log L(θ) = k ln θ + (n−k) ln(1−θ); derivative k/θ − (n−k)/(1−θ) = 0 ⟹ k(1−θ) = (n−k)θ ⟹ θ = k/n *(required: the full derivative-and-solve chain)* — this is `mle` before that concept has a name | — |
| E2 | explain | short-answer | 1.3 | Why do we usually maximize the *log*-likelihood rather than the likelihood directly? | log turns a product of many small terms into a sum (avoiding numerical underflow with many data points, and simplifying differentiation); since log is strictly increasing, the maximizing θ is identical either way *(required: both the numerical and the "doesn't change the maximizer" points)* | — |
| T1 | transfer | short-answer | 1.65 | A headline claims: "Scientists find θ=0.3 is the most probable value, with probability 0.6, based on the likelihood function." What's wrong with this statement? | it treats L(θ) as a probability distribution over θ, which R2 already showed it generally is not — L(θ) need not integrate to 1 over θ, so "probability 0.6" is meaningless as stated; a genuine probability statement about θ requires Bayesian machinery (a prior, then Bayes' rule) to convert the likelihood into an actual posterior *(required: names what additional ingredient — a prior — would actually be needed)* | accepts the headline's framing and tries to compute what "probability 0.6" might mean rather than identifying the category error → `likelihood-vs-probability` |
| R3 | recall | short-answer | −0.32 | Fill in the blank: for continuous data, the likelihood L(θ) uses the ___ f(x\|θ) rather than a PMF. | density | — |
| R4 | recall | mcq | −0.28 | The likelihood for n iid observations x₁,...,xₙ is: | L(θ) = ∏ᵢ f(xᵢ\|θ) (or ∏ᵢ p(xᵢ\|θ) in the discrete case) — the product of the individual densities/PMFs | sums the individual densities instead of multiplying them, treating iid likelihood like a sum rather than a product → `likelihood-vs-probability` |
| R5 | recall | short-answer | −0.24 | Define the log-likelihood ℓ(θ). | ℓ(θ) = ln L(θ) | — |
| R6 | recall | mcq | −0.2 | In the likelihood function L(θ), which quantity stays fixed while θ varies? | x, the observed data | claims θ is held fixed and x varies, exactly reversing likelihood's defining distinction from probability → `likelihood-vs-probability` |
| R7 | recall | mcq | −0.16 | True or false: viewed as a function of x for *fixed* θ, p(x\|θ) sums (or integrates) to 1 over x. | True — this is the ordinary probability reading, in contrast to the likelihood reading where θ varies and x is fixed | claims p(x\|θ) also sums to 1 over x when read as a likelihood, conflating the two readings → `likelihood-vs-probability` |
| R8 | recall | short-answer | −0.12 | Fill in the blank: the likelihood ratio L(θ₁)/L(θ₂) compares ___. | how much better θ₁ explains the observed data compared to θ₂ — the relative support the data gives to the two parameter values | — |
| R9 | recall | mcq | −0.08 | Which of these correctly describes "probability" as opposed to "likelihood," using the same formula p(x\|θ)? | probability varies x for fixed θ; likelihood varies θ for fixed (observed) x | states probability varies θ and likelihood varies x, exactly the reversed pairing → `likelihood-vs-probability` |
| R10 | recall | short-answer | −0.04 | For continuous data, explain briefly why L(θ) can take values greater than 1, even though probabilities never exceed 1. | L(θ) is built from a *density*, and densities are not themselves bounded by 1 (only their integral over x is required to equal 1) — so a density value, and hence a likelihood value, can exceed 1 | — |
| R11 | recall | mcq | 0.0 | For n iid Poisson(θ) observations x₁,...,xₙ, the likelihood is: | L(θ) = ∏ᵢ e^{−θ}θ^{xᵢ}/xᵢ! | writes L(θ) = e^{−θ}θ^{Σxᵢ}/Σ(xᵢ!), incorrectly summing the xᵢ! terms inside a single factorial instead of leaving them as a product | — |
| R12 | recall | mcq | 0.06 | True or false: the likelihood function is a function of the parameter θ, with the observed data held fixed. | True — this is the core definition this whole concept exists to establish | claims the likelihood is a function of the data with θ held fixed, again reversing the defining roles → `likelihood-vs-probability` |
| R13 | recall | short-answer | 0.1 | Fill in the blank: L(θ) is given by exactly the same formula as p(x\|θ) or f(x\|θ) — what changes is ___. | which variable is treated as fixed and which is treated as varying, not the formula itself | — |
| R14 | recall | mcq | 0.16 | Which of these is NOT a required property of a valid likelihood function L(θ)? | that it integrates to 1 over θ | picks "evaluated at the observed data" or "same formula as p(x\|θ)" as not required, when those genuinely are part of the definition → `likelihood-vs-probability` |
| R15 | recall | short-answer | 0.2 | In one sentence, what does the likelihood L(θ) measure? | how well a given parameter value θ explains (makes probable) the data that was actually observed | — |
| R16 | recall | mcq | 0.26 | Which of the following pairs correctly matches "probability" and "likelihood" to what varies in each? | probability: x varies, θ fixed; likelihood: θ varies, x fixed | matches probability to "θ varies, x fixed" and likelihood to "x varies, θ fixed" — the defining reversal this whole concept exists to correct → `likelihood-vs-probability` |
| A3 | apply | numeric | 0.4 | A single observation X~Exponential(θ), x=3. Compare L(0.5) and L(1). `[verified: L(0.5)≈0.1116, L(1)≈0.0498]` | L(θ)=θe^{−3θ}; L(0.5)=0.5e^{−1.5}≈0.1116; L(1)=1·e^{−3}≈0.0498 — θ=0.5 is more likely given this single observation | plugs θ into e^{−θx} without the leading factor of θ, dropping a multiplicative term from the density → `likelihood-vs-probability` |
| A4 | apply | numeric | 0.52 | 4 iid Normal(μ,1) observations: x=(1,2,3,4). Compare the (unnormalized) exponent terms Σ(xᵢ−μ)² for μ=2 and μ=3, and say which likelihood is larger. `[verified: both sums equal 6]` | Σ(xᵢ−2)²=1+0+1+4=6; Σ(xᵢ−3)²=4+1+0+1=6 — the two sums are *equal*, so L(2)=L(3) exactly, since both μ values sit equally far (0.5) from the true sample mean 2.5 | assumes the closer-looking integer value must have strictly higher likelihood without actually computing both sums → `likelihood-vs-probability` |
| A5 | apply | numeric | 0.58 | n=10 iid Bernoulli(θ), k=3 successes. Compare L(0.2) and L(0.4). `[verified: L(0.2)≈0.001678, L(0.4)≈0.001792]` | L(θ)=θ³(1−θ)⁷; L(0.2)=0.2³(0.8)⁷≈0.001678; L(0.4)=0.4³(0.6)⁷≈0.001792 — θ=0.4 is (slightly) more likely than θ=0.2, though neither is as likely as θ=0.3 (the observed proportion) | uses θ^{n−k}(1−θ)^k instead of θᵏ(1−θ)^{n−k}, swapping which exponent goes with successes vs. failures → `likelihood-vs-probability` |
| A6 | apply | short-answer | 0.62 | n iid Uniform(0,θ) observations with maximum observed value 7. Write L(θ) as a function of θ. | L(θ) = 1/θⁿ for θ ≥ 7, and L(θ) = 0 for θ < 7 — any θ smaller than the largest observed value is impossible under this model, and larger θ makes each observation's density smaller | writes L(θ)=1/θⁿ for all θ>0, missing that θ<7 is impossible given the data (zero likelihood there) → `likelihood-vs-probability` |
| E3 | explain | derivation | 1.2 | For n iid Normal(μ, σ²) observations with σ² known, derive ℓ(μ) and show that maximizing it is equivalent to *minimizing* the sum of squared residuals Σ(xᵢ−μ)². | ℓ(μ) = Σᵢ ln[(1/√(2πσ²)) e^{−(xᵢ−μ)²/(2σ²)}] = −(n/2)ln(2πσ²) − (1/(2σ²))Σ(xᵢ−μ)²; the first term doesn't depend on μ, so maximizing ℓ(μ) over μ is exactly minimizing Σ(xᵢ−μ)² (since it's subtracted with a positive coefficient) *(required: isolates the μ-dependent term and states the sign flip that turns maximization into minimization)* — this is the reason least-squares regression is a special case of maximum likelihood | — |
| E4 | explain | short-answer | 1.32 | Explain why the statement "L(θ₁) > L(θ₂)" is a meaningful, valid comparison, even though neither L(θ₁) nor L(θ₂) is individually a probability of θ. | likelihood comparisons are *relative*: L(θ₁)>L(θ₂) says the observed data was more probable to occur under θ₁ than under θ₂, which is a well-defined statement built entirely from ordinary probabilities (or densities) at the fixed observed data point — it never requires L to be a distribution over θ, only that the two values being compared use the same fixed data and the same formula *(required: explains why the comparison sidesteps the "not a distribution over θ" issue entirely)* | — |
| E5 | explain | derivation | 1.45 | Show that a multiplicative constant in the density that does *not* depend on θ cancels out of the likelihood ratio L(θ)/L(θ₀), and explain why this justifies dropping such constants when maximizing. | if f(x\|θ) = c(x)·g(x,θ) where c(x) doesn't depend on θ, then L(θ)/L(θ₀) = [c(x)g(x,θ)]/[c(x)g(x,θ₀)] = g(x,θ)/g(x,θ₀) — the c(x) factor cancels exactly *(required: the explicit cancellation)*; since the maximizer of L(θ) only depends on *ratios* L(θ)/L(θ₀) being compared across θ, any θ-free constant can be dropped from L(θ) before maximizing without changing θ̂ | — |
| T2 | transfer | short-answer | 1.75 | A clinical trial report says "the likelihood function peaks at a 62% success rate for this drug" and a journalist writes "there is a 62% probability the drug's true success rate is 62%." What's the category error, and what would actually be needed to make a genuine probability statement about the true rate? | the likelihood's peak (the MLE) is the θ value that makes the *observed trial data* most probable — it is not itself a probability statement about θ, exactly R2/T1's point applied to medicine; a genuine probability statement about the true success rate (a credible interval, say "there's a 95% chance the true rate is between 55% and 68%") requires Bayesian machinery — a prior distribution over rates, updated via Bayes' rule — which a bare likelihood peak does not supply *(required: names the Bayesian ingredient needed, echoing T1)* | treats "the likelihood peaks at 62%" and "there's a 62% probability the rate is 62%" as saying the same thing → `likelihood-vs-probability` |
| T3 | transfer | short-answer | 1.85 | A forensic expert testifies: "the likelihood of this DNA pattern, given the suspect is guilty, is 99% — so there's a 99% likelihood the suspect is guilty, given the match." Identify the two distinct errors in this statement. | first, "the likelihood of the pattern given guilt is 99%" is P(evidence \| guilt) — a probability statement about the *evidence*, correctly a likelihood reading; second, restating this as "99% likelihood the suspect is guilty given the match" swaps it into P(guilt \| evidence), an entirely different quantity that additionally requires the base rate of guilt (a prior) to compute via Bayes' rule — conflating the two is the classic prosecutor's fallacy, and even the word "likelihood" is being used loosely in the second half, no longer meaning the technical L(θ) at all *(required: names both the P(evidence\|guilt) vs P(guilt\|evidence) swap and the missing base rate)* | accepts the final "99% likelihood of guilt" figure as a direct, valid restatement of the first number → `likelihood-vs-probability` |

*Coverage: 16/6/5/3 — 30 items, −0.35…1.85.*

---

## Method of Moments Estimation (`method-of-moments`)
*Prereq: Expectation · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | Describe the method of moments procedure in one sentence. | set the first (or first few) sample moment(s) equal to the corresponding theoretical moment(s), expressed in terms of the unknown parameter(s), and solve | — |
| R2 | recall | mcq | 0.0 | For a distribution with 2 unknown parameters, method of moments generally requires: | 2 equations (matching the first two moments) | matches only the mean, treating one equation as always sufficient regardless of how many parameters are unknown → `method-of-moments` |
| A1 | apply | short-answer | 0.5 | X~Exponential(λ), so E[X]=1/λ. Given sample mean X̄=4, find λ̂. `[verified: 0.25]` | λ̂ = 1/4 = 0.25 | — |
| A2 | apply | short-answer | 0.65 | X~Uniform(0,θ), so E[X]=θ/2. Given X̄=6, find θ̂. `[verified: 12]` | θ̂ = 12 | — |
| E1 | explain | derivation | 1.2 | Method-of-moments estimators aren't unique in *how* you match moments. For Uniform(0,θ), derive the estimator you'd get by matching the *second* moment instead of the first, and show it's a genuinely different formula. `[verified: E[X²]=θ²/3]` | E[X²] = Var(X) + E[X]² = θ²/12 + θ²/4 = θ²/3; setting the sample second moment m₂ = θ²/3 gives θ̂ = √(3m₂) — a different closed form from θ̂=2X̄, and generally a *different number* on the same finite sample *(required: derives θ²/3 from Var+mean², not just asserts a different formula exists)* | — |
| E2 | explain | short-answer | 1.35 | Why are method-of-moments estimators usually easy to compute in closed form, even when MLE requires numerical optimization for the same distribution? | moments are simple functions of the data (sample averages) set equal to typically simple algebraic expressions in θ — often solvable by hand; MLE instead requires maximizing a likelihood that can be algebraically messy *(required)* | — |
| T1 | transfer | short-answer | 1.7 | For Gamma(α,β) (2 unknown parameters), set up the two method-of-moments equations using E[X]=α/β and E[X²]=α/β²+(α/β)². Why does having two unknowns require exactly two equations — no more, no fewer? | E[X]=α/β=X̄ and E[X²]=α/β²+(α/β)²=m₂ give two equations in two unknowns (α,β), solvable simultaneously; one equation per unknown parameter is required for the system to have (generically) a unique solution — fewer leaves parameters undetermined, more over-determines the system *(required)* | writes only the mean equation and tries to solve for both α and β from it alone → `method-of-moments` |
| R3 | recall | short-answer | −0.26 | Fill in the blank: the k-th sample moment is ___. | (1/n) Σᵢ xᵢᵏ | — |
| R4 | recall | mcq | −0.22 | Method of moments equates sample moments to: | the theoretical (population) moments, expressed as functions of the unknown parameter(s) | equates sample moments to the MLE's log-likelihood derivative instead, mixing up two different estimation procedures → `method-of-moments` |
| R5 | recall | mcq | −0.18 | True or false: for a given distribution, the method-of-moments estimator is always unique, regardless of which moments you choose to match. | False — E1 shows matching the first vs. second moment for Uniform(0,θ) gives two genuinely different formulas | claims uniqueness holds always, missing E1's explicit counterexample → `method-of-moments` |
| R6 | recall | short-answer | −0.14 | State the formula for the first sample moment. | m₁ = X̄ = (1/n) Σᵢ xᵢ, the sample mean | — |
| R7 | recall | mcq | −0.1 | For a distribution with a single unknown parameter, method of moments typically uses: | just the first moment equation (E[X]=X̄), solved for the one unknown | uses both the first and second moment equations even when there's only one unknown, over-determining a system that needs just one equation → `method-of-moments` |
| R8 | recall | mcq | −0.06 | True or false: a method-of-moments estimate can sometimes fall outside the parameter's valid range (e.g. a negative variance estimate). | True — this is a known weakness of the method, since nothing in the moment-matching algebra enforces the parameter's valid range | claims MoM estimates are guaranteed to always be valid parameter values, missing a genuine limitation of the method → `method-of-moments` |
| R9 | recall | short-answer | −0.02 | Fill in the blank: for X~Bernoulli(p), E[X] = ___, so the method-of-moments estimator is p̂ = ___. | p; p̂ = X̄ | — |
| R10 | recall | mcq | 0.02 | Asymptotically, how does method of moments generally compare to MLE in terms of efficiency (estimator variance)? | MoM is generally less efficient (has higher variance) than MLE | claims MoM is always at least as efficient as MLE, missing the well-known asymptotic efficiency gap between the two methods → `method-of-moments` |
| R11 | recall | short-answer | 0.06 | Define, in one sentence, what "moment" means in "method of moments." | a moment is E[X^k] for some power k — a raw moment of the distribution, matched here between the sample and the theoretical distribution | — |
| R12 | recall | mcq | 0.1 | The number of moment equations needed in a method-of-moments fit equals: | the number of unknown parameters being estimated | equals the sample size n, confusing the number of equations needed with the number of data points | — |
| R13 | recall | mcq | 0.14 | True or false: method of moments requires knowing the distribution family's theoretical moments (as closed-form functions of θ) in advance. | True — without a formula for E[X], E[X²], etc. in terms of θ, there's nothing to set the sample moments equal to | claims MoM can be applied without knowing the distribution family, treating it as fully nonparametric | — |
| R14 | recall | short-answer | 0.18 | Fill in the blank: the second sample moment is m₂ = ___. | (1/n) Σᵢ xᵢ² | — |
| R15 | recall | short-answer | 0.22 | Give one practical advantage of method of moments over MLE. | it's typically simple, closed-form algebra (solve for θ from a moment equation) rather than requiring numerical optimization, which MLE often needs | — |
| R16 | recall | mcq | 0.26 | Which of these correctly orders the general method-of-moments procedure? | (1) write the theoretical moments as functions of θ, (2) set them equal to the corresponding sample moments, (3) solve for θ | solves for θ before setting up the equations, or skips writing the theoretical moment formulas entirely → `method-of-moments` |
| A3 | apply | numeric | 0.42 | X~Geometric(p) (number of failures before the first success), so E[X]=(1−p)/p. Given sample mean X̄=3, find p̂. `[verified: 0.25]` | (1−p̂)/p̂ = 3 ⟹ 1 = 4p̂ ⟹ p̂ = 0.25 | sets p̂/(1−p̂)=3 instead, inverting which ratio the mean formula gives → `method-of-moments` |
| A4 | apply | numeric | 0.5 | X~Poisson(λ), so E[X]=λ. Given sample mean X̄=3.2, find λ̂. `[verified: 3.2]` | λ̂ = X̄ = 3.2, the simplest possible method-of-moments case since the parameter equals the mean directly | applies a transformation to X̄ (e.g. λ̂=1/X̄) instead of using the mean directly, confusing this with the Exponential case from A1 → `method-of-moments` |
| A5 | apply | numeric | 0.58 | X~Gamma(α,2) with rate β=2 known, so E[X]=α/2. Given sample mean X̄=7, find α̂. `[verified: 14]` | α̂/2 = 7 ⟹ α̂ = 14 | divides X̄ by 2 instead of multiplying, reporting α̂=3.5 → `method-of-moments` |
| A6 | apply | short-answer | 0.68 | X~Gamma(α,β), both unknown, with E[X]=α/β and E[X²]=α/β²+(α/β)². Given X̄=4 and m₂=24, solve the two-equation system for α̂, β̂. `[verified: β̂=0.5, α̂=2]` | α/β=4 and α/β²+16=24 ⟹ α/β²=8 ⟹ (α/β)/β=8 ⟹ 4/β=8 ⟹ β̂=0.5; α̂=4(0.5)=2 | plugs the mean equation's ratio directly into the second equation without isolating β first, e.g. solves α/β²=8 as if α/β²=α/β directly → `method-of-moments` |
| E3 | explain | derivation | 1.38 | Prove that the Poisson method-of-moments estimator λ̂=X̄ is *consistent* — that it converges in probability to the true λ as n→∞ — using the (weak) law of large numbers. | the weak law of large numbers states that X̄ₙ →ᵖ E[X] as n→∞, for iid Xᵢ with finite mean; since E[X]=λ for Poisson(λ), applying the law directly gives X̄ₙ →ᵖ λ, i.e. λ̂ₙ = X̄ₙ is consistent for λ *(required: the explicit substitution of E[X]=λ into the LLN statement)* — a first, concrete preview of what `law-of-large-numbers` proves in general | — |
| E4 | explain | short-answer | 1.48 | Explain intuitively why matching a *higher-order* sample moment (like m₂ or m₃) instead of the sample mean, when both are valid choices for the same single parameter, generally produces a noisier (higher-variance) estimator. | higher-order sample moments involve averaging powers of the data (xᵢ², xᵢ³, ...), which amplifies the influence of extreme observations far more than a simple average does — a single large xᵢ contributes disproportionately more to m₂ than to m₁, so the resulting estimator inherits more sampling variability from whichever few observations happen to be largest *(required: the amplification-of-extremes mechanism, not just "higher moments are noisier")* | — |
| E5 | explain | short-answer | 1.6 | For a two-parameter family, explain in general terms why the method-of-moments system {E[X]=g₁(θ₁,θ₂)=X̄, E[X²]=g₂(θ₁,θ₂)=m₂} might fail to have a solution, or have more than one. | the system reduces to inverting the map (θ₁,θ₂) ↦ (g₁(θ₁,θ₂), g₂(θ₁,θ₂)); if this map is not one-to-one (invertible) over the valid parameter space, either no (θ₁,θ₂) achieves the observed (X̄,m₂) exactly, or multiple parameter pairs do — solvability and uniqueness of method-of-moments estimates are therefore properties of the specific moment map, not guaranteed in general *(required: names the invertibility-of-the-moment-map issue)* | — |
| T2 | transfer | short-answer | 1.85 | An engineer modeling component lifetimes with a Weibull distribution needs a starting guess for an overnight numerical MLE optimization. Explain why computing the method-of-moments estimate first, and feeding it in as the optimizer's starting point, is a common and sensible workflow. | method-of-moments estimates are cheap, closed-form (or nearly so), and consistent (E3), so they land in the right neighborhood of the true parameter with very little computation; starting the MLE's numerical search from a value already close to the answer sharply reduces the number of iterations needed and lowers the risk of the optimizer converging to a poor local optimum — MoM here serves purely as a fast initializer, not a replacement for MLE's better asymptotic efficiency (R10) *(required: names both the "fast starting point" role and that it isn't a replacement for MLE)* | assumes the method-of-moments estimate is only useful as a final answer, missing its common role purely as an optimizer starting point → `method-of-moments` |
| T3 | transfer | short-answer | 1.95 | A survey team fits a Gamma distribution to household income data using method of moments (as in A6) instead of waiting for a slower MLE fit, in order to get a policy report out by a deadline. Discuss the tradeoff being made. | the team gains speed and a simple, transparent closed-form calculation, at the cost of statistical efficiency — the MoM fit will generally have higher variance (R10) than an MLE fit would, meaning the fitted Gamma parameters are noisier estimates of the true income distribution than a more careful analysis would produce; whether that tradeoff is acceptable depends on how much the deadline is worth relative to the added uncertainty the report's policy conclusions will carry *(required: names both sides of the tradeoff explicitly, not just "MoM is faster")* | treats the MoM fit as equivalent in quality to an MLE fit, since "both estimate the same parameters" → `method-of-moments` |

*Coverage: 16/6/5/3 — 30 items, −0.3…1.95.*

---

## Maximum Likelihood Estimation (`mle`)
*Prereq: Likelihood vs Probability · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | Define the MLE θ̂ in one sentence. | the value of θ that maximizes the likelihood L(θ) (equivalently, log L(θ)) given the observed data | — |
| R2 | recall | mcq | 0.0 | *Invariance property*: if θ̂ is the MLE of θ, the MLE of g(θ) (any function g) is: | g(θ̂) | claims the MLE of g(θ) is "unrelated to θ̂ in general" — missing one of MLE's most useful properties → `mle` |
| A1 | apply | numeric | 0.5 | From `likelihood-vs-probability`'s derivation, θ̂=0.7 for n=10, k=7. Use *invariance* (not a fresh derivation) to find the MLE of the odds θ/(1−θ). `[verified: 2.333]` | MLE of odds = θ̂/(1−θ̂) = 0.7/0.3 = 7/3 ≈ 2.333 | re-derives the MLE from scratch for the odds parameter instead of applying invariance directly → `mle` |
| A2 | apply | derivation | 0.65 | For a single X~Exponential(λ), find the MLE of λ by maximizing L(λ)=λe^{−λx}. `[verified: λ̂=1/x]` | log L = ln λ − λx; derivative 1/λ − x = 0 ⟹ λ̂ = 1/x | — |
| E1 | explain | derivation | 1.2 | For n iid Exponential(λ) observations, derive λ̂ = 1/X̄, and note which other concept's estimator this exactly matches. `[verified: matches Method of Moments' A1 answer]` | L(λ)=λⁿexp(−λΣxᵢ); log L = n ln λ − λΣxᵢ; derivative n/λ − Σxᵢ = 0 ⟹ λ̂ = n/Σxᵢ = 1/X̄ — identical to `method-of-moments`'s estimator for this same distribution *(required: the full derivation, plus noticing the match)* | — |
| E2 | explain | short-answer | 1.35 | Argue informally why the invariance property holds for a *monotonic* g: if θ̂ maximizes L(θ), why must g(θ̂) maximize the likelihood re-expressed in terms of φ=g(θ)? | g monotonic means it's one-to-one, so maximizing over θ is exactly equivalent to maximizing over φ=g(θ) — it's the same search, just relabeled; the maximizing φ is therefore g applied to the maximizing θ, i.e. g(θ̂) *(required: the relabeling argument, not just restating the property)* | — |
| T1 | transfer | short-answer | 1.7 | MLE and Method of Moments give the *identical* estimator (1/X̄) for Exponential's rate. Is this a coincidence, or does it always happen? Give a distribution where the two methods disagree, and say why. `[verified: Uniform MLE is max(xᵢ)]` | not general — for Uniform(0,θ), the MLE is θ̂ = max(x₁,…,xₙ) (the smallest θ consistent with every observed point; L(θ)=1/θⁿ for θ≥max(xᵢ) is *decreasing* in θ, so the max is achieved at the smallest feasible θ), which is nothing like Method of Moments' θ̂=2X̄ *(required: derives *why* the Uniform MLE is a max, not just states the fact)* | assumes the two methods must always agree because they agreed for Exponential → `mle` |
| R3 | recall | short-answer | −0.26 | Fill in the blank: the MLE θ̂ is the value of θ that ___. | maximizes the likelihood L(θ) (equivalently, the log-likelihood ln L(θ)) given the observed data | — |
| R4 | recall | mcq | −0.22 | To find an MLE via calculus, the standard approach is to: | set d/dθ ln L(θ) = 0 and solve for θ | sets L(θ) itself (not the log or the derivative) equal to zero, confusing the equation to solve with the function to maximize → `mle` |
| R5 | recall | mcq | −0.18 | True or false: the MLE always has a closed-form algebraic solution. | False — many models (e.g. logistic regression) require numerical optimization because the maximizing equation has no closed form | claims a closed form always exists, missing that many real MLE problems require iterative numerical methods → `mle` |
| R6 | recall | short-answer | −0.14 | State the second-derivative condition that confirms a critical point θ̂ is a maximum (not a minimum) of ln L(θ). | d²/dθ² ln L(θ) < 0 at θ=θ̂ — the log-likelihood is concave there | — |
| R7 | recall | mcq | −0.1 | MLE is a method for: | estimating unknown parameters of an assumed distribution family, given observed data | describes MLE as a method for testing whether a distributional assumption is correct, confusing estimation with model checking → `mle` |
| R8 | recall | mcq | −0.06 | True or false: MLE estimators are always unbiased. | False — the classic counterexample is the naive (n-divisor) variance estimator from `unbiased-estimator`, which is the MLE for σ² under Normality but is biased | claims MLE guarantees unbiasedness, a common and consequential over-generalization → `mle` |
| R9 | recall | short-answer | −0.02 | Fill in the blank: by the invariance property, the MLE of g(θ) is ___. | g(θ̂) — apply g directly to the MLE of θ | — |
| R10 | recall | mcq | 0.02 | Which of these is NOT part of the standard analytic MLE procedure? | integrating L(θ) over θ | picks "differentiate and set to 0" or "take the log" as not part of the procedure, when those are the actual standard steps → `mle` |
| R11 | recall | short-answer | 0.06 | Define the "score function" used in MLE theory. | the derivative of the log-likelihood with respect to θ, d/dθ ln L(θ) | — |
| R12 | recall | mcq | 0.1 | True or false: under standard regularity conditions, MLE is asymptotically efficient — its variance approaches the Cramér–Rao lower bound as n→∞. | True — this is one of MLE's most important large-sample properties | claims MLE's variance grows without bound as n increases, the opposite of the efficiency property | — |
| R13 | recall | short-answer | 0.14 | What does the acronym MLE stand for? | Maximum Likelihood Estimation (or Estimator) | — |
| R14 | recall | mcq | 0.18 | MLE being "consistent" (under standard regularity conditions) means: | θ̂ converges in probability to the true θ as the sample size n → ∞ | claims consistency means θ̂ is unbiased for every n, conflating a finite-sample property with the large-sample consistency property → `mle` |
| R15 | recall | short-answer | 0.22 | Why must you check that a critical point of ln L(θ) is a *global* maximum, not just verify it's a local one via the second-derivative test? | ln L(θ) can, in principle, have multiple local maxima (or the boundary of θ's valid range can beat any interior critical point), so a local check alone doesn't guarantee θ̂ is the value that truly maximizes the likelihood overall | — |
| R16 | recall | mcq | 0.26 | For MLE, we choose θ̂ to: | maximize L(θ) (or ln L(θ)) over all valid values of θ | chooses θ̂ to minimize L(θ), inverting the entire objective of the method → `mle` |
| A3 | apply | numeric | 0.42 | n=8 iid Bernoulli(θ) observations, k=5 successes. Find θ̂. `[verified: 0.625]` | using `likelihood-vs-probability`'s E1 result θ̂=k/n: θ̂ = 5/8 = 0.625 | computes θ̂ = n/k = 8/5 = 1.6, an impossible value outside [0,1], by inverting the ratio → `mle` |
| A4 | apply | derivation | 0.55 | A single observation X~Poisson(θ), x=6. Find the MLE θ̂ by maximizing L(θ)=e^{−θ}θ⁶/6!. `[verified: θ̂=6]` | ln L(θ) = −θ + 6 ln θ − ln 6!; d/dθ = −1 + 6/θ = 0 ⟹ θ̂ = 6 | differentiates θ⁶ as if it were 6θ (losing the log first), getting a linear equation with the wrong solution → `mle` |
| A5 | apply | numeric | 0.6 | n=5 iid Normal(μ, σ²=4 known) observations x=(2,4,6,8,10). Find the MLE μ̂. `[verified: 6]` | for Normal with known σ², maximizing the likelihood in μ is equivalent to minimizing Σ(xᵢ−μ)² (per `likelihood-vs-probability`'s E3), whose minimizer is the sample mean: μ̂ = (2+4+6+8+10)/5 = 6 | uses the sample median instead of the sample mean, confusing which summary statistic maximizes the Normal likelihood → `mle` |
| A6 | apply | numeric | 0.5 | Using A3's θ̂=0.625, apply the invariance property to find the MLE of θ² (a variance-like transform of θ). `[verified: 0.390625]` | MLE of θ² = (θ̂)² = 0.625² = 0.390625 | re-derives the MLE of θ² from scratch via a fresh likelihood maximization instead of applying invariance directly to θ̂, echoing A1's original misconception → `mle` |
| E3 | explain | derivation | 1.28 | For n iid Poisson(θ) observations, derive the general MLE formula θ̂ = X̄. | L(θ)=∏ᵢ e^{−θ}θ^{xᵢ}/xᵢ!; ln L(θ) = −nθ + (Σxᵢ) ln θ − Σ ln(xᵢ!); d/dθ = −n + (Σxᵢ)/θ = 0 ⟹ θ̂ = (Σxᵢ)/n = X̄ *(required: the full log-likelihood, differentiation, and solving steps)* | — |
| E4 | explain | short-answer | 1.42 | In plain language, what do MLE's "regularity conditions" broadly ensure, and why do consistency and asymptotic efficiency depend on them? | regularity conditions (roughly: the support of the distribution doesn't depend on θ, the likelihood is smooth/differentiable enough, and the true θ lies in the interior of the parameter space, among others) rule out pathological cases — like Uniform(0,θ)'s θ-dependent support from `exponential-family` — where the usual calculus-based (derivative-equals-zero) machinery behind MLE's nice asymptotic properties simply doesn't apply; without them, the theorems guaranteeing consistency and efficiency are not automatically available *(required: names at least one concrete regularity condition and connects its absence to a real counterexample)* | — |
| E5 | explain | derivation | 1.55 | Prove that the score function has expectation zero at the true parameter value: E[d/dθ ln L(θ)]|_{θ=θ_true} = 0, for a single observation with density f(x\|θ). | E[d/dθ ln f(X\|θ)] = ∫ [f'(x\|θ)/f(x\|θ)] f(x\|θ) dx = ∫ f'(x\|θ) dx = d/dθ ∫ f(x\|θ) dx = d/dθ (1) = 0, using that f(·\|θ) integrates to 1 for every θ and swapping the order of differentiation and integration *(required: the full chain, including the swap and the integrates-to-1 fact)* — this identity is the starting point for `fisher-information`'s definition | — |
| T2 | transfer | short-answer | 1.8 | An A/B test's MLE conversion rate is θ̂=0.08 (8%). The business wants the MLE of expected revenue per visitor, where each conversion is worth a fixed $50. Using invariance, find this MLE and explain why no separate derivation is needed. `[verified: $4]` | expected revenue per visitor = θ × $50 is a deterministic function g(θ)=50θ of θ; by invariance (R9), the MLE of g(θ) is simply g(θ̂) = 50 × 0.08 = $4 — no fresh likelihood maximization over "revenue" is needed, since g is just a rescaling of the same parameter already estimated *(required: the numeric answer and the explicit invariance justification)* | sets up a brand-new likelihood function in terms of revenue and re-maximizes from scratch, duplicating work invariance makes unnecessary → `mle` |
| T3 | transfer | short-answer | 1.9 | A machine learning engineer says "training a logistic regression model by minimizing cross-entropy loss" while a statistician says "fitting a logistic regression model by maximum likelihood." Explain why these are, in fact, describing the exact same procedure. | logistic regression models each label as Bernoulli(θ(x)) with θ(x) a function of the inputs and the model's weights; the Bernoulli log-likelihood for one observation is y ln θ + (1−y) ln(1−θ) — exactly the negative of the cross-entropy loss for that observation; summing over the training set and maximizing the total log-likelihood is therefore identical to summing the cross-entropy losses and minimizing them (maximizing a quantity and minimizing its negative are the same optimization) *(required: the explicit sign-flip correspondence between the two objectives, not just "they're related")* | treats "minimizing cross-entropy" and "maximum likelihood" as two different techniques that happen to give similar answers, rather than the same objective viewed with opposite signs → `mle` |

*Coverage: 16/6/5/3 — 30 items, −0.3…1.9. T1's Uniform result is reused directly by `exponential-family`'s E1.*

---

## Unbiased Estimator (`unbiased-estimator`)
*Prereq: MLE, Method of Moments · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.18 | Define an unbiased estimator. | θ̂ is unbiased if E[θ̂] = θ for *every* possible true value of θ | — |
| R2 | recall | mcq | 0.12 | Which is TRUE? (a) MLE is always unbiased (b) the sample mean is always unbiased for the population mean (c) unbiased estimators are always the best choice. | (b) | picks (a) — `mle`'s Exponential estimator happens to be unbiased-adjacent in some cases, but MLE is *not* unbiased in general (the classic counterexample is next, in A2/E1) → `unbiased-estimator` |
| A1 | apply | derivation | 0.62 | Show X̄ = (1/n)ΣXᵢ is unbiased for μ=E[Xᵢ], using linearity of expectation. | E[X̄] = (1/n)ΣE[Xᵢ] = (1/n)(nμ) = μ *(required, one line but must be shown)* | — |
| A2 | apply | numeric | 0.75 | The "naive" variance estimator (1/n)Σ(Xᵢ−X̄)² has E[·] = ((n−1)/n)σ². For n=5, σ²=10, find its bias. `[verified by simulation: ≈8.0 vs true 10]` | bias = ((n−1)/n)σ² − σ² = −σ²/n = −10/5 = −2 — the naive estimator *underestimates* σ² by 2 on average at this sample size | assumes any "reasonable-looking" estimator built from an average must be unbiased → `unbiased-estimator` |
| E1 | explain | derivation | 1.32 | Prove E[(1/n)Σ(Xᵢ−X̄)²] = ((n−1)/n)σ², using the identity Σ(Xᵢ−X̄)² = Σ(Xᵢ−μ)² − n(X̄−μ)². | E[Σ(Xᵢ−μ)²] = nσ²; E[n(X̄−μ)²] = n·Var(X̄) = n·(σ²/n) = σ²; subtracting gives E[Σ(Xᵢ−X̄)²] = nσ²−σ² = (n−1)σ²; dividing by n gives the result `[verified: matches simulation to 3 sig figs]` *(required: the algebraic identity must be used, not assumed)* — this is exactly why the (n−1) (Bessel's) correction exists | — |
| E2 | explain | short-answer | 1.5 | Explain *intuitively* why using X̄ (an estimate) instead of the true μ systematically makes the naive estimator underestimate spread. | X̄ is, by construction, the value of c that *minimizes* Σ(Xᵢ−c)² over all choices of c — so plugging in the true (unknown) μ instead would give a sum of squared deviations at least as large; using the data's own best-fit center systematically produces a smaller apparent spread than the true center would *(required: connects to X̄ minimizing the sum of squares, the actual mechanism)* | — |
| T1 | transfer | short-answer | 1.82 | A junior analyst divides by n "because that's literally the definition of an average"; their boss insists on n−1. Who's right, and does it matter much for n=1000 vs n=5? `[verified: (n-1)/n = 0.8 at n=5, 0.999 at n=1000]` | the boss is right for unbiasedness (E1); the practical gap shrinks as n grows — (n−1)/n = 0.8 at n=5 (a 20% average underestimate) but 0.999 at n=1000 (negligible) *(required: both directions of the comparison, with the actual factor)* | treats the correction as "always negligible" or "always essential," without the sample-size-dependence → `unbiased-estimator` |
| R3 | recall | short-answer | −0.14 | Fill in the blank: bias(θ̂) = E[θ̂] − ___. | θ, the true parameter value | — |
| R4 | recall | mcq | −0.1 | An estimator θ̂ with bias(θ̂) = 0 for every possible true value of θ is called: | unbiased | calls it "consistent" instead, conflating a finite-sample property (unbiasedness) with a large-sample one → `unbiased-estimator` |
| R5 | recall | mcq | −0.06 | True or false: an unbiased estimator is always the best (lowest-variance) choice available. | False — a biased estimator can have lower mean squared error (MSE) by trading a small bias for a large variance reduction, as E5/T3 will show | claims unbiasedness alone guarantees the best estimator, ignoring the variance side of the tradeoff entirely → `unbiased-estimator` |
| R6 | recall | short-answer | −0.02 | State the mean squared error (MSE) of an estimator, and its bias-variance decomposition. | MSE(θ̂) = E[(θ̂−θ)²] = Var(θ̂) + bias(θ̂)² | — |
| R7 | recall | mcq | 0.02 | Which scenario illustrates a *biased* estimator that is nonetheless widely used because of a favorable MSE tradeoff? | ridge regression's shrunken coefficient estimates, which trade a small bias for a large variance reduction | claims no biased estimator is ever preferable to an unbiased one, missing R5's point and its main practical example → `unbiased-estimator` |
| R8 | recall | mcq | 0.06 | True or false: the sample proportion p̂ = k/n (successes out of n trials) is unbiased for the true probability p. | True — E[p̂] = E[k]/n = np/n = p | claims p̂ is only unbiased for large n, missing that unbiasedness here holds exactly for every n | — |
| R9 | recall | short-answer | 0.1 | Fill in the blank: the sample variance S² using the (n−1) divisor is ___ for σ², unlike the naive (n-divisor) version. | unbiased | — |
| R10 | recall | mcq | 0.14 | Bias is a property of an estimator computed by averaging over: | repeated sampling — all the different samples the estimator could see, not any single observed sample | claims bias describes how far off a single specific computed estimate is from θ, rather than the long-run average behavior across samples → `unbiased-estimator` |
| R11 | recall | short-answer | 0.18 | Write the MSE decomposition formula, naming each term. | MSE(θ̂) = Var(θ̂) + [bias(θ̂)]² — the estimator's own sampling variance, plus the square of its systematic bias | — |
| R12 | recall | mcq | 0.22 | True or false: unbiasedness, unlike consistency, is a finite-sample property — it must hold exactly for every n, not just in the limit. | True | claims unbiasedness is only meaningful as n→∞, conflating it with the large-sample notion of consistency → `unbiased-estimator` |
| R13 | recall | short-answer | 0.26 | Fill in the blank: an estimator can be biased for every finite n, yet still be ___, meaning its bias vanishes as n→∞. | consistent | — |
| R14 | recall | mcq | 0.3 | Which is a real example of an estimator that is biased for finite n but consistent? | the naive (n-divisor) MLE variance estimator from A2/E1, whose bias −σ²/n shrinks to 0 as n→∞ | claims no biased estimator can ever be consistent, treating the two properties as mutually exclusive → `unbiased-estimator` |
| R15 | recall | short-answer | 0.34 | In one sentence, why isn't "is it unbiased?" the only question statisticians ask when judging an estimator? | because an estimator's overall quality also depends on its variance (and hence its MSE) — a low-bias, high-variance estimator can be worse in practice than a small-bias, low-variance one | — |
| R16 | recall | mcq | 0.38 | The definition "θ̂ is unbiased" requires E[θ̂] = θ to hold for: | every possible true value of θ, not just one specific value | requires the equality to hold only at the true θ that happens to occur in a given dataset, missing that unbiasedness is a property of the estimator across the whole parameter space → `unbiased-estimator` |
| A3 | apply | numeric | 0.5 | An estimator θ̂ satisfies E[θ̂] = θ + 2 for every θ. Find bias(θ̂). `[verified: 2]` | bias(θ̂) = E[θ̂] − θ = (θ+2) − θ = 2 | reports the bias as 0, treating "E[θ̂] depends on θ" as automatically meaning the extra +2 term isn't a genuine bias → `unbiased-estimator` |
| A4 | apply | derivation | 0.58 | X₁,...,Xₙ are iid with E[Xᵢ]=μ. Show the estimator θ̂=(X₁+Xₙ)/2 (average of just the first and last observations) is unbiased for μ. `[verified]` | E[θ̂] = E[(X₁+Xₙ)/2] = (E[X₁]+E[Xₙ])/2 = (μ+μ)/2 = μ, by linearity — unbiased, though it wastefully ignores most of the sample | assumes only the full-sample mean X̄ can ever be unbiased for μ, missing that many other linear combinations are too (just less efficient) → `unbiased-estimator` |
| A5 | apply | numeric | 0.68 | Var(θ̂)=3, bias(θ̂)=2. Find MSE(θ̂). `[verified: 7]` | MSE = Var + bias² = 3 + 2² = 7 | computes MSE = Var + bias = 5, forgetting to square the bias term | — |
| A6 | apply | numeric | 0.72 | Estimator θ̂₁ is unbiased with Var(θ̂₁)=10. Estimator θ̂₂ has bias(θ̂₂)=1 and Var(θ̂₂)=2. Compute both MSEs and say which estimator is preferable by the MSE criterion. `[verified: MSE₁=10, MSE₂=3]` | MSE(θ̂₁) = 10+0² = 10; MSE(θ̂₂) = 2+1² = 3; θ̂₂ has lower MSE despite being biased, so it's preferable by this criterion | picks θ̂₁ automatically because it's unbiased, without computing and comparing the actual MSE values → `unbiased-estimator` |
| E3 | explain | derivation | 1.38 | Derive the bias-variance decomposition MSE(θ̂) = Var(θ̂) + bias(θ̂)², starting from MSE(θ̂)=E[(θ̂−θ)²]. | write θ̂−θ = (θ̂−E[θ̂]) + (E[θ̂]−θ); squaring and taking expectation, E[(θ̂−θ)²] = E[(θ̂−E[θ̂])²] + 2(E[θ̂]−θ)E[θ̂−E[θ̂]] + (E[θ̂]−θ)²; the middle cross term vanishes because E[θ̂−E[θ̂]]=0; the first term is Var(θ̂) by definition and the last is bias(θ̂)², giving MSE(θ̂)=Var(θ̂)+bias(θ̂)² *(required: the add-and-subtract split and the vanishing cross term, not just citing the formula)* | — |
| E4 | explain | short-answer | 1.52 | Explain, using the bias-variance decomposition, why deliberately introducing a small bias can sometimes lower an estimator's MSE below what any unbiased estimator can achieve. | MSE is the *sum* of variance and squared bias; an estimator that accepts a small bias² penalty can sometimes buy a much larger reduction in variance in exchange (for instance, by shrinking an estimate toward a fixed value, damping how much it swings across different samples), and if the variance drop outweighs the added bias² term, the total MSE goes down — unbiasedness alone doesn't optimize this sum, it only forces the bias² term to be exactly zero, which is not the same as minimizing the total *(required: the tradeoff mechanism, not just "sometimes bias helps")* | — |
| E5 | explain | derivation | 1.62 | For two independent unbiased estimators θ̂₁, θ̂₂ of the same θ, show that the weighted average θ̂_w = wθ̂₁+(1−w)θ̂₂ is unbiased for *any* weight w ∈ [0,1]. | E[θ̂_w] = wE[θ̂₁] + (1−w)E[θ̂₂] = wθ + (1−w)θ = [w+(1−w)]θ = θ, by linearity and both estimators' unbiasedness *(required: the linearity step and the algebraic simplification to θ)* — note this only needs each piece to be unbiased, not independence, though independence is what makes the resulting variance well-behaved | — |
| T2 | transfer | short-answer | 1.9 | A pollster uses an unbiased estimator of a candidate's true support based on a sample of just 5 voters. A rival pollster says "unbiased means trustworthy — this poll is fine." Using MSE, explain what's missing from that claim. | unbiasedness only guarantees E[θ̂]=θ on average across many hypothetical samples of size 5 — it says nothing about how much any *single* poll's estimate can swing from that average; with only 5 voters, Var(θ̂) is enormous, so MSE(θ̂)=Var(θ̂)+0² is large purely from the variance term, meaning this particular poll's number could be wildly off even though the estimator is, in principle, unbiased *(required: names that MSE, not just bias, determines practical reliability, and ties it to the small-n variance)* | accepts "unbiased" as sufficient grounds to trust the specific 5-voter poll's number → `unbiased-estimator` |
| T3 | transfer | short-answer | 2.0 | Ridge regression deliberately shrinks coefficient estimates toward zero, making them biased, yet it's standard practice in machine learning, especially with many correlated predictors. Explain, using the bias-variance decomposition, why abandoning unbiasedness here is often the right call. | with many (especially correlated) predictors, the ordinary unbiased least-squares estimator can have very high variance — small changes in the training data swing the fitted coefficients wildly; ridge regression's shrinkage introduces a controlled bias in exchange for a substantial variance reduction, and when predictors are strongly correlated that variance reduction typically outweighs the added bias², lowering total MSE below what the unbiased estimator achieves (E4's mechanism, applied at model-fitting scale) *(required: names the high-variance setting where the tradeoff pays off, not just "bias can help")* | treats ridge regression's bias as a pure downside, missing that it's accepted deliberately because it lowers overall MSE → `unbiased-estimator` |

*Coverage: 16/6/5/3 — 30 items, −0.18…2.0. This whole section sets up `sample-variance`'s (n−1) denominator before that concept exists.*

---

## Distribution Transformations (`distribution-transformations`)
*Prereq: PDF, PMF · ancestors 9 · b₀ = 0.65*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.35 | State the 1-D change-of-variables formula for Y=g(X), g strictly monotonic and differentiable. | f_Y(y) = f_X(g⁻¹(y))·\|d/dy g⁻¹(y)\| | — |
| R2 | recall | mcq | −0.05 | For *non*-monotonic g (e.g. g(x)=x²), the formula: | needs to sum contributions from every x-branch mapping to the same y | assumes the single-branch formula still applies unmodified → `distribution-transformations` |
| A1 | apply | derivation | 0.45 | X~Uniform(0,1), Y=−ln(X). Find f_Y(y) and identify the distribution. `[verified: E[Y]≈1 by simulation, matching Exponential(1)]` | g⁻¹(y)=e⁻ʸ; \|d/dy e⁻ʸ\|=e⁻ʸ; f_X(e⁻ʸ)=1 for 0<e⁻ʸ<1 i.e. y>0; f_Y(y)=e⁻ʸ for y>0 — **exactly Exponential(1)** | forgets the support restriction (y>0) that comes from X's own support | 
| A2 | apply | derivation | 0.6 | X~N(0,1), Y=X². Using the two-branch rule from R2, set up f_Y(y) for y>0 (no need to simplify to the named χ²₁ constant). `[verified: reduces to φ(√y)/√y, matching χ²₁]` | f_Y(y) = φ(√y)·(1/(2√y)) + φ(−√y)·(1/(2√y)) = φ(√y)/√y (using φ even) — this matches `chi-square-distribution`'s χ²₁ density, closing the loop from that cluster | uses only one branch (x=√y), missing x=−√y entirely, halving the density → `distribution-transformations` |
| E1 | explain | derivation | 1.15 | Prove the change-of-variables formula for monotonic *increasing* g, via the CDF. | F_Y(y)=P(g(X)≤y)=P(X≤g⁻¹(y))=F_X(g⁻¹(y)); differentiate both sides in y via the chain rule to get f_Y(y)=f_X(g⁻¹(y))·(g⁻¹)′(y) *(required: the CDF-then-differentiate chain)* — this is the exact technique `normal-distribution`'s E1 used to standardize Z, proved here as the general theorem it was secretly a special case of | — |
| E2 | explain | short-answer | 1.3 | Why does the formula need \|·\| around the derivative, rather than the signed derivative? | a density must be nonnegative; for g *decreasing*, the CDF derivation flips a direction (P(X≤g⁻¹(y)) becomes P(X≥g⁻¹(y))), introducing a sign that the absolute value correctly absorbs regardless of whether g is increasing or decreasing *(required)* | — |
| T1 | transfer | short-answer | 1.65 | Computers generate Uniform(0,1) numbers natively but need other distributions for simulation. Using A1, explain how a computer can produce an Exponential(1) draw from a Uniform(0,1) draw — and why this trick (inverse transform sampling) generalizes to *any* target distribution with an invertible CDF. | generate U~Uniform(0,1), output Y=−ln(U) (exactly A1, run in reverse); more generally, if U~Uniform(0,1), then Y=F⁻¹(U) has CDF F for *any* invertible target CDF F — the general statement this specific trick is an instance of *(required: states the general F⁻¹(U) fact, not just the Exponential special case)* — this is a real building block of random-number-generation libraries | treats A1's trick as specific to Exponential, without generalizing to F⁻¹(U) → `distribution-transformations` |
| R3 | recall | short-answer | −0.3 | State the change-of-variables formula for Y=g(X) when g is monotonic *decreasing* (as opposed to increasing). | f_Y(y) = f_X(g⁻¹(y))·\|d/dy g⁻¹(y)\| — the same formula as the increasing case; the absolute value is exactly what makes it work regardless of the direction of monotonicity | — |
| R4 | recall | mcq | −0.26 | The 1-D change-of-variables formula requires g to be: | differentiable and (locally) monotonic/invertible over the relevant domain | requires only that g be differentiable, omitting the invertibility requirement that makes g⁻¹ well-defined at all → `distribution-transformations` |
| R5 | recall | mcq | −0.22 | True or false: for a linear transformation Y=aX+b (a≠0), the Jacobian factor \|d/dy g⁻¹(y)\| in the change-of-variables formula is a constant. | True — it equals 1/\|a\|, not depending on y at all | claims the Jacobian factor for a linear transform still varies with y, missing that linear transforms give a constant Jacobian | — |
| R6 | recall | short-answer | −0.18 | State the density of Y=aX+b in terms of f_X. | f_Y(y) = (1/\|a\|) f_X((y−b)/a) | — |
| R7 | recall | mcq | −0.14 | Any linear transformation Y=aX+b of a Normal random variable X is itself: | Normal — with mean aμ+b and variance a²σ² | claims a linear transform of a Normal is only approximately Normal, missing that Normality is *exactly* preserved under linear transforms | — |
| R8 | recall | mcq | −0.1 | True or false: applying a strictly increasing transformation to a set of random variables preserves the ranking (order) of their realized values. | True — if g is strictly increasing, x₁<x₂ implies g(x₁)<g(x₂), so order statistics' ranks are unaffected | claims any transformation preserves rank order, without the strictly-increasing requirement — a decreasing or non-monotonic g would not → `distribution-transformations` |
| R9 | recall | short-answer | −0.06 | Fill in the blank: for Y=X² (a two-branch transform), the general formula sums contributions from x=___ and x=___. | √y and −√y | — |
| R10 | recall | mcq | −0.02 | Inverse transform sampling relies on which fact? | if U~Uniform(0,1), then Y=F⁻¹(U) has CDF F, for any invertible target CDF F | relies on the fact that F(U) (rather than F⁻¹(U)) has CDF F, inverting which function needs to be applied to U → `distribution-transformations` |
| R11 | recall | short-answer | 0.02 | In the 1-D change-of-variables formula, what does the factor \|d/dy g⁻¹(y)\| represent, informally? | it's the (1-D) "Jacobian" — how much a small interval of y-values gets stretched or compressed when mapped back to x-values via g⁻¹, which corrects the density for that local change of scale | — |
| R12 | recall | mcq | 0.06 | For g(x)=x³ (strictly monotonic increasing over all of ℝ), the change-of-variables formula for f_Y(y) requires: | only a single branch, since g is monotonic (one-to-one) everywhere, unlike g(x)=x² | requires the two-branch treatment from R9 even though x³ is monotonic and has no second branch to sum | — |
| R13 | recall | mcq | 0.1 | True or false: the density-based change-of-variables formula (with the Jacobian factor) applies to discrete random variables in the same form as continuous ones. | False — for discrete X, transforming to Y=g(X) (one-to-one) is a direct relabeling of probabilities, p_Y(y)=p_X(g⁻¹(y)), with no Jacobian/derivative correction needed | applies the continuous Jacobian formula to a discrete transformation, introducing a derivative term that has no meaning for a PMF → `distribution-transformations` |
| R14 | recall | short-answer | 0.14 | For discrete X with Y=g(X) one-to-one, state p_Y(y) in terms of p_X. | p_Y(y) = p_X(g⁻¹(y)) | — |
| R15 | recall | short-answer | 0.18 | Name the two structural ingredients the 1-D continuous change-of-variables formula needs from g. | g must be invertible (so g⁻¹ exists) over the relevant domain, and g⁻¹ must be differentiable (so the Jacobian factor is well-defined) | — |
| R16 | recall | mcq | 0.22 | The correct general formula for f_Y(y) when g is not globally monotonic is: | f_Y(y) = Σ over all x-branches mapping to y of f_X(x)·\|dx/dy\| | uses only the single-branch formula regardless of how many x values map to the same y, dropping the summation R9 requires → `distribution-transformations` |
| A3 | apply | derivation | 0.42 | X~Uniform(0,1), Y=X³. Find f_Y(y) and verify it integrates to 1. `[verified: ≈1 by numerical check]` | g⁻¹(y)=y^{1/3}; \|d/dy y^{1/3}\| = (1/3)y^{−2/3}; f_X(y^{1/3})=1 for 0<y<1; f_Y(y) = (1/3)y^{−2/3} for 0<y<1 | forgets to apply the chain rule to g⁻¹(y)=y^{1/3}, treating the derivative as simply 1 → `distribution-transformations` |
| A4 | apply | short-answer | 0.5 | X~Exponential(1), Y=2X+3. Using R6's linear-transform formula, find f_Y(y). `[verified: support y>3]` | f_Y(y) = (1/2) f_X((y−3)/2) = (1/2)e^{−(y−3)/2}, for y>3 (since X>0 requires (y−3)/2>0) | omits the shifted support y>3, writing the density as valid for all y>0 instead → `distribution-transformations` |
| A5 | apply | numeric | 0.46 | X is a fair die roll, Uniform on {1,...,6}. Y=2X (a one-to-one discrete transform). Find p_Y(6). `[verified: 1/6]` | p_Y(6) = p_X(g⁻¹(6)) = p_X(3) = 1/6 | applies a Jacobian-style correction to the discrete PMF, e.g. dividing 1/6 by 2, treating this like a continuous density transform → `distribution-transformations` |
| A6 | apply | short-answer | 0.55 | X~N(0,1), Y=3X−2. Using the linear-transform rule for Normals (R7), identify the distribution of Y. `[verified: N(−2,9)]` | Y ~ N(3(0)−2, 3²(1)) = N(−2, 9) | scales the mean by 3² instead of the variance, e.g. reports N(−18,1) | — |
| E3 | explain | derivation | 1.22 | Derive the discrete change-of-variables formula p_Y(y)=p_X(g⁻¹(y)) for one-to-one g, directly from the definition of probability. | P(Y=y) = P(g(X)=y); since g is one-to-one, g(X)=y exactly when X=g⁻¹(y), so P(g(X)=y) = P(X=g⁻¹(y)) = p_X(g⁻¹(y)) *(required: the "g(X)=y iff X=g⁻¹(y)" equivalence, using one-to-one-ness explicitly)* | — |
| E4 | explain | short-answer | 1.36 | Explain why the discrete change-of-variables formula needs no Jacobian (derivative) correction term, unlike the continuous formula. | in the discrete case, probability is concentrated at individual points, and relabeling a point x as y=g(x) doesn't spread or compress any probability mass — the mass P(X=x) simply moves wholesale to P(Y=y); in the continuous case, density describes how mass is spread over an interval, and stretching or compressing that interval (via g) changes how concentrated the density is, which is exactly what the Jacobian factor corrects for *(required: the mass-vs-density distinction driving the difference)* | — |
| E5 | explain | derivation | 1.48 | Derive f_Y(y) = (1/\|a\|) f_X((y−b)/a) for Y=aX+b as a special case of the general 1-D change-of-variables formula. | g(x)=ax+b, so g⁻¹(y)=(y−b)/a; d/dy g⁻¹(y) = 1/a, so \|d/dy g⁻¹(y)\| = 1/\|a\|; substituting into the general formula f_Y(y)=f_X(g⁻¹(y))·\|d/dy g⁻¹(y)\| gives f_Y(y) = f_X((y−b)/a) · (1/\|a\|) *(required: the explicit g⁻¹ and its derivative, substituted into the general formula)* | — |
| T2 | transfer | short-answer | 1.75 | Standardized test scores are computed as Z=(X−μ)/σ, a linear transformation of the raw score X. Using this concept's linear-transform result, explain why Z-scores from two *different* tests (with different μ, σ) can be meaningfully compared on a common scale. | Z=(X−μ)/σ is exactly the linear form Y=aX+b with a=1/σ, b=−μ/σ; by R6/E5, this rescales and re-centers whatever distribution X had, so regardless of the original test's raw scale, Z always has mean 0 and variance 1 (or, if X is Normal, Z is exactly N(0,1)) — the linear-transform machinery is precisely what guarantees the two tests' Z-scores land on the same standardized scale, even though their raw score distributions differ completely *(required: ties the standardization formula explicitly to the Y=aX+b transform rule)* | assumes raw scores from two different tests can be compared directly without any transformation, missing why standardization is needed at all → `distribution-transformations` |
| T3 | transfer | short-answer | 1.85 | A programmer generates Exponential(1) draws using Y=−ln(1−U) instead of A1's Y=−ln(U), where U~Uniform(0,1). Explain why this variant also works, and what general principle (beyond this one example) guarantees infinitely many such valid transformations exist. | since U~Uniform(0,1) implies 1−U~Uniform(0,1) as well (Uniform(0,1) is symmetric about 0.5), substituting 1−U for U inside A1's formula gives another draw with the identical Exponential(1) distribution; more generally, T1's inverse-transform principle Y=F⁻¹(U) is one canonical construction, but *any* transformation g with g(U) matching the target CDF works — g(u)=F⁻¹(u) and g(u)=F⁻¹(1−u) are just two of infinitely many valid choices built from the same underlying Uniform symmetry *(required: the 1−U symmetry argument and the "infinitely many valid transforms" generalization)* | assumes the specific formula Y=−ln(U) from A1 is the *only* way to generate an Exponential(1) draw from a Uniform(0,1) draw → `distribution-transformations` |

*Coverage: 16/6/5/3 — 30 items, −0.35…1.85.*

---

## Exponential Family (`exponential-family`)
*Prereq: MGF · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State the canonical one-parameter exponential family form. | f(x;θ) = h(x)·exp(η(θ)T(x) − A(θ)) | — |
| R2 | recall | mcq | 0.04 | Which of these is *not* a member of the exponential family in its usual form? | Uniform(0,θ) — because its support depends on θ | picks Poisson or Normal, not recognizing they *do* fit the canonical form → `exponential-family` |
| A1 | apply | derivation | 0.54 | Write Bernoulli(θ) in exponential family form, identifying h(x), η(θ), T(x), A(θ). | θˣ(1−θ)¹⁻ˣ = exp(x·ln(θ/(1−θ)) + ln(1−θ)); h(x)=1, η(θ)=ln(θ/(1−θ)) (the *logit*), T(x)=x, A(θ)=−ln(1−θ) *(required: the logit must appear as η(θ), not just "some function of θ")* | — |
| A2 | apply | derivation | 0.65 | Write Poisson(λ) in exponential family form. | e⁻ᵠλˣ/x! = (1/x!)exp(x ln λ − λ); h(x)=1/x!, η(λ)=ln λ, T(x)=x, A(λ)=λ | — |
| E1 | explain | short-answer | 1.24 | Explain why Uniform(0,θ) fails to be exponential family, connecting it to `mle`'s earlier finding about its MLE. | exponential family densities require the support to *not* depend on θ; Uniform(0,θ)'s support [0,θ] depends on θ directly, disqualifying it — and this is exactly why its MLE (θ̂=max(xᵢ), from `mle`'s T1) is a boundary/max solution rather than the smooth calculus solution (derivative=0) that exponential-family MLEs typically produce *(required: the explicit connection to the earlier MLE finding, not just restating the support rule)* | — |
| E2 | explain | short-answer | 1.4 | What does T(x) represent inside the exponential form, informally (without yet defining sufficiency formally)? | for Bernoulli, T(x)=x itself; for a sample of n iid Bernoullis, the sum Σxᵢ turns out to be the *only* thing about the data that ends up mattering for inference about θ — a first, informal look at what `sufficient-statistic` will name precisely | — |
| T1 | transfer | short-answer | 1.74 | Nearly every named distribution in this bank (Normal, Bernoulli, Binomial, Poisson, Gamma, Beta, Exponential) is exponential family — one reason it's studied as a unifying topic. Give one *practical* benefit of knowing this, beyond tidiness. | moments can be computed uniformly via derivatives of A(θ) (the log-partition function) instead of a fresh calculation per distribution; a single generic fitting algorithm can handle *any* exponential-family member without distribution-specific code; conjugate Bayesian priors exist automatically for the whole family *(required: at least one concrete practical payoff, not just "it's elegant")* | states only that it's "mathematically elegant" without naming a concrete downstream use → `exponential-family` |
| R3 | recall | short-answer | −0.22 | In the canonical form f(x;θ)=h(x)exp(η(θ)T(x)−A(θ)), what is A(θ) called? | the log-partition function (also called the cumulant generating function) | — |
| R4 | recall | mcq | −0.18 | Which of these IS a member of the exponential family in its usual form? | Normal(μ, σ²) with σ² known | picks Cauchy, not recognizing that its heavy tails and lack of a clean exp(η·T−A) form disqualify it → `exponential-family` |
| R5 | recall | mcq | −0.14 | True or false: Binomial(n,p) with n known and p unknown is exponential family. | True — its support {0,...,n} doesn't depend on p, and it can be written in the canonical exp(η(p)T(x)−A(p)) form | claims Binomial is never exponential family, missing that with n fixed and known, its support condition is satisfied | — |
| R6 | recall | short-answer | −0.1 | Define the "natural" (or canonical) parameter η(θ) in the exponential family form. | the reparametrization of θ that appears linearly inside the exponent multiplying T(x) — the parameter the canonical form is written in terms of, rather than θ directly | — |
| R7 | recall | mcq | −0.06 | In the canonical form, h(x) is called the: | base measure (or carrier density) | calls h(x) the sufficient statistic, confusing it with T(x) | — |
| R8 | recall | mcq | −0.02 | True or false: the role of A(θ) in the canonical form is to ensure the density integrates (or sums) to 1. | True — A(θ) = ln ∫ h(x)exp(η(θ)T(x)) dx is exactly chosen to make normalization hold | claims A(θ) has no normalization role and is purely a notational convenience → `exponential-family` |
| R9 | recall | short-answer | 0.02 | Fill in the blank: T(x) in the canonical form is called the ___. | sufficient statistic | — |
| R10 | recall | mcq | 0.06 | Which of these does NOT belong to the exponential family in general form? | Cauchy — its heavy tails and lack of finite moments prevent the canonical exp(η·T−A) representation | picks Gamma or Beta as non-members, missing that both do fit the canonical form → `exponential-family` |
| R11 | recall | short-answer | 0.1 | State the general formula for E[T(X)] in terms of the log-partition function A, as a function of the natural parameter η. | E[T(X)] = dA/dη | — |
| R12 | recall | mcq | 0.14 | True or false: Gamma(α,β) with the shape α known and the rate β unknown is exponential family. | True — its support (0,∞) doesn't depend on β, satisfying the key structural requirement | claims Gamma can never be exponential family regardless of which parameter is held fixed, missing that this specific case does satisfy the support condition | — |
| R13 | recall | short-answer | 0.18 | Fill in the blank: the multi-parameter exponential family form generalizes T(x) and η(θ) from scalars to ___. | vectors — T(x) becomes a vector of sufficient statistics, and η(θ) a corresponding vector of natural parameters | — |
| R14 | recall | mcq | 0.22 | Var(T(X)) in the exponential family relates to the log-partition function A via: | the second derivative, d²A/dη² | relates Var(T(X)) to A(η) itself rather than to any of its derivatives, missing the moment-generating role A plays | — |
| R15 | recall | short-answer | 0.26 | Name two named distributions, besides Bernoulli and Poisson, that belong to the exponential family. | any two of: Normal (known variance), Gamma (one parameter known), Exponential, Beta | — |
| R16 | recall | mcq | 0.3 | Fill in the blank: for exponential family membership, the support of f(x;θ) must not depend on ___. | θ, the parameter — this is exactly the condition that disqualifies Uniform(0,θ) (E1) | claims the support must not depend on x, garbling which variable the support-independence condition is about → `exponential-family` |
| A3 | apply | derivation | 0.4 | Write Exponential(λ) in exponential family form, identifying h(x), η(λ), T(x), A(λ). | f(x;λ)=λe^{−λx} = exp(x(−λ) − (−ln λ)); h(x)=1, η(λ)=−λ, T(x)=x, A(λ)=−ln λ | writes η(λ)=λ (dropping the sign), which flips the role of A and produces the wrong normalization term → `exponential-family` |
| A4 | apply | derivation | 0.5 | Write Normal(μ, σ²=1 known) in exponential family form, identifying h(x), η(μ), T(x), A(μ). | f(x;μ)=(1/√(2π))e^{−(x−μ)²/2} = (1/√(2π))e^{−x²/2}·exp(μx − μ²/2); h(x)=(1/√(2π))e^{−x²/2}, η(μ)=μ, T(x)=x, A(μ)=μ²/2 | folds the e^{−x²/2} factor into A(μ) instead of h(x), letting the "constant" A depend on the data x → `exponential-family` |
| A5 | apply | numeric | 0.46 | Using A4's A(η)=η²/2 for the Normal case, find E[T(X)] at η=3 via R11's formula, and confirm it matches the known mean. `[verified: 3]` | E[T(X)] = dA/dη = η = 3, at η=3 this gives 3 — matching μ=3 directly, since η=μ for this parametrization | computes dA/dη as η²/2 itself instead of differentiating it, reporting 4.5 → `exponential-family` |
| A6 | apply | numeric | 0.6 | For Exponential(λ=2) with η=−λ=−2 and A(η)=−ln(−η) (from A3), find Var(T(X)) via R14's formula and confirm it matches the known Exponential variance 1/λ². `[verified: 0.25]` | dA/dη = −1/η; d²A/dη² = 1/η² = 1/(−2)² = 0.25 — matching 1/λ² = 1/4 = 0.25 exactly | evaluates only the first derivative (getting E[T(X)]) and mistakes it for Var(T(X)), skipping the required second differentiation → `exponential-family` |
| E3 | explain | derivation | 1.3 | Derive E[T(X)] = dA/dη, starting from the normalization identity ∫h(x)exp(η T(x)−A(η)) dx = 1. | differentiating both sides with respect to η: ∫h(x)exp(ηT(x)−A(η))·(T(x)−A′(η)) dx = 0; splitting the integral, ∫T(x)f(x;η)dx − A′(η)∫f(x;η)dx = 0; the second integral is 1 (normalization again), so E[T(X)] − A′(η) = 0, giving E[T(X)]=A′(η)=dA/dη *(required: the differentiate-under-the-integral step and the split into two terms)* | — |
| E4 | explain | short-answer | 1.44 | Explain why the exponential family's sufficient statistic T(x) being low-dimensional (often just one or two numbers, regardless of sample size) is practically valuable for inference on large datasets. | if T(x) captures everything the likelihood needs from the data (a preview of what `sufficient-statistic` formalizes), then storing or transmitting T(x) — a fixed small number of values — is exactly as informative for inference as keeping the entire raw dataset; this means a dataset of any size can be compressed down to (typically) just a sum or count, with no loss of information for fitting the exponential-family model, a major practical saving in memory and computation *(required: names the no-loss-of-information compression, not just "it's a smaller number")* | — |
| E5 | explain | derivation | 1.54 | Derive Var(T(X)) = d²A/dη², by differentiating E[T(X)]=dA/dη a second time. | starting from E3's identity ∫T(x)f(x;η)dx = A′(η), differentiate both sides in η again: ∫T(x)·f(x;η)·(T(x)−A′(η)) dx = A″(η); the left side expands to E[T(X)²] − A′(η)E[T(X)] = E[T(X)²] − (A′(η))², which is exactly Var(T(X)) by the shortcut variance formula, since E[T(X)]=A′(η) *(required: the second differentiation and the identification with the variance shortcut formula)* | — |
| T2 | transfer | short-answer | 1.85 | Generalized linear models (GLMs) assume the response variable's distribution belongs to the exponential family, whether it's Binomial (logistic regression), Poisson (count regression), or Normal (ordinary regression). Explain why this shared assumption is what allows one unified fitting algorithm to handle such different-looking response types. | because every exponential-family member shares the same canonical structure (h(x), η(θ), T(x), A(θ)), a single fitting procedure that only needs to know how to differentiate A(θ) (to get E[T(X)] and Var(T(X)), by E3/E5) can be written once and reused for any member — the algorithm never needs distribution-specific code, only the specific A(θ) plugged in, which is exactly why one iterative fitting routine can train a logistic, Poisson, or linear regression model with the same underlying machinery *(required: ties the shared canonical form directly to the reusability of one algorithm)* | assumes GLMs need a separate, custom-built fitting algorithm for each response distribution family, missing what the shared exponential-family structure buys | — |
| T3 | transfer | short-answer | 1.95 | In Bayesian statistics, a "conjugate prior" is one that, combined with the likelihood, gives a posterior in the same distributional family as the prior. Conjugate priors are guaranteed to exist for any exponential-family likelihood. Explain, at a conceptual level, why the canonical exponential form is exactly the structural property that makes this guarantee possible. | the canonical form's exponent η(θ)T(x)−A(θ) is *linear* in the sufficient statistic T(x) for fixed θ, and correspondingly linear in some function of θ for fixed T(x); a prior built with the same exp(η(θ)·s − c·A(θ)) shape (for tunable constants s, c) combines with the likelihood by simply adding exponents, landing back in that same family with updated constants — it's precisely the shared exponential structure between likelihood and prior that lets multiplication (Bayes' rule) stay inside one family, which a likelihood outside the exponential family generally cannot guarantee *(required: connects the linear-exponent structure to why multiplying likelihood × prior stays in-family)* | treats conjugacy as a lucky coincidence specific to a few named distributions rather than a structural consequence of exponential-family form | — |

*Coverage: 16/6/5/3 — 30 items, −0.26…1.95.*

---

## Cluster 6 misconception index

| Tag | Blame |
|---|---|
| MGF derivative order confused (mean vs variance) | `mgf` |
| MGF product rule applied without checking independence | `mgf-properties` |
| likelihood treated as a probability distribution over θ | `likelihood-vs-probability` |
| one moment equation used for a multi-parameter family | `method-of-moments` |
| MLE assumed always unbiased, or invariance property missed | `mle`, `unbiased-estimator` |
| naive (n-divisor) variance assumed unbiased | `unbiased-estimator` |
| single-branch change of variables on a non-monotonic transform | `distribution-transformations` |
| exponential-family support rule missed (Uniform's disqualification) | `exponential-family` |
| MGF believed bounded, monotonic, or defined for all t regardless of tail weight | `mgf` |
| shift and scale rules for M_{aX+b}(t) applied with the wrong exponent placement | `mgf-properties` |
| probability-vs-likelihood roles reversed (which variable is "fixed") | `likelihood-vs-probability` |
| method-of-moments estimate assumed unique regardless of which moment is matched | `method-of-moments` |
| MLE invariance property re-derived from scratch instead of applied directly | `mle` |
| MSE's bias-variance tradeoff ignored in favor of "unbiased is always best" | `unbiased-estimator` |
| discrete relabeling (p_Y(y)=p_X(g⁻¹(y))) confused with the continuous Jacobian formula | `distribution-transformations` |
| exponential-family log-partition function A(θ) mistaken for the sufficient statistic or vice versa | `exponential-family` |

**Cluster total: 240 items across 8 concepts (30 each).** All numeric and simulation-based claims verified,
including a 500,000-trial check of the classic (n−1)/n variance-bias factor and a 2-million-sample
check of the inverse-transform-sampling identity. This cluster contains the densest web of
cross-references in the bank so far — MGF-matching re-proving the pilot file's opening fact, the
Uniform MLE explaining its own exponential-family exclusion, and Cluster 4's Normal-standardization
proof turning out to be a special case of this cluster's general transformation theorem.
