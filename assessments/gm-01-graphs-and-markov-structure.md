# Graphical Models Cluster 1 — Graphs & Markov Structure

Graphs, Directed vs Undirected Graphs, Conditional Independence and D-Separation, Markov Random
Fields, Markov Chains, Hidden Markov Models (6 concepts). Same format as
[foundations-of-probability.md](foundations-of-probability.md).

`markov-chains`' R2 reuses `eigenvalues-eigenvectors`' original T1 example (the stationary
distribution as an eigenvector) directly, and its E1 connects long-run mixing speed to
`matrix-stability`'s spectral-radius argument from the linear-algebra sweep.

---

## Graphs (`graphs`)
*Prereq: Set Theory · ancestors 1 · b₀ = −0.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −1.15 | Define a graph, and what nodes/edges represent in a graphical model. | a set of nodes (vertices) and edges connecting pairs of them; in graphical models, nodes are random variables and edges are dependence relationships | — |
| R2 | recall | mcq | −0.9 | A graphical model's primary purpose is to: | compactly represent the conditional independence structure among many variables, avoiding writing out the full joint distribution | claims it's "to visualize data points directly (like a scatterplot)" — a graphical model encodes dependence structure, not raw data → `graphs` |
| A1 | apply | short-answer | −0.35 | A joint over 10 binary variables needs up to 1023 free parameters with no independence assumed. How does encoding independence via a graph reduce this? | if each variable depends directly only on a few others (its graph neighbors), the joint factors into small local pieces, each needing far fewer parameters than the full joint table — the graph makes this sparsity of dependence explicit *(required: the factorization-from-sparsity argument)* | — |
| E1 | explain | short-answer | 0.35 | Why is a graph structure a natural fit for specifying high-dimensional joint distributions? | each variable typically depends directly on only a few others (its "neighbors"), and the graph makes this sparsity of dependence explicit and visually clear, rather than leaving it implicit in a giant table *(required)* | — |
| T1 | transfer | short-answer | 0.85 | Why is a graphical model said to sit at the intersection of graph theory and probability theory? | the graph provides a purely combinatorial/structural object (nodes, edges), while the probabilistic interpretation (conditional independence, factorization of the joint) gives that structure its statistical meaning — neither half alone captures what a graphical model is *(required: names both halves as jointly necessary)* | — |
| R3 | recall | mcq | −1.05 | In a graphical model, a node represents: | a random variable | claims a node represents a single data point rather than a random variable → `graphs` |
| R4 | recall | mcq | −1.0 | In a graphical model, an edge represents: | a direct dependence relationship between two variables | claims an edge represents a causal effect in every case, even in undirected graphs where edges are symmetric → `graphs` |
| R5 | recall | short-answer | −0.85 | Fill in the blank: the degree of a node is ___. | the number of edges incident to it | — |
| R6 | recall | short-answer | −0.8 | True or false: a complete graph (every pair of nodes connected) encodes no conditional independence assumptions. | True | — |
| R7 | recall | short-answer | −0.75 | Define a "clique" in a graph. | a subset of nodes that are all pairwise connected to each other | — |
| R8 | recall | mcq | −0.7 | A "path" in a graph is: | a sequence of edges connecting a sequence of distinct nodes | claims a path requires the edges to all point in the same direction, which conflates paths with directed paths specifically → `graphs` |
| R9 | recall | short-answer | −0.65 | Fill in the blank: a graph with no cycles is called ___. | acyclic | — |
| R10 | recall | short-answer | −0.6 | True or false: two nodes with no edge directly between them are always marginally independent. | False — they can still be dependent through another path in the graph; lacking a direct edge only rules out a direct dependence | — |
| R11 | recall | mcq | −0.55 | A "subgraph" is: | a graph formed from a subset of the original graph's nodes and edges | claims a subgraph must contain every node of the original graph, only removing edges → `graphs` |
| R12 | recall | short-answer | −0.5 | For n binary random variables with no independence assumed, how many free parameters does the full joint distribution need? | 2ⁿ−1 | — |
| R13 | recall | short-answer | −0.45 | Fill in the blank: a graph is "connected" if ___. | there is a path between every pair of nodes | — |
| R14 | recall | mcq | −0.4 | The two broad probabilistic model families graphs give rise to are: | Bayesian networks (directed) and Markov random fields (undirected) | names "Bayesian networks and neural networks" — neural networks are not the undirected graphical model family → `graphs` |
| R15 | recall | short-answer | −0.3 | True or false: a graphical model's edges are always drawn purely from expert knowledge, never learned from data. | False — graph structure can also be learned from data ("structure learning") | — |
| R16 | recall | mcq | −0.25 | Why is a "sparse" graph (few edges relative to the number of possible pairs) desirable in a graphical model? | it corresponds to fewer direct dependencies, hence fewer parameters needed to specify the joint distribution | claims sparsity is desirable purely because it makes the diagram easier to draw, missing the parameter-count reason → `graphs` |
| A2 | apply | numeric | −0.28 | 4 binary random variables, no independence assumed. Free parameters needed for the full joint? `[verified: 15]` | 2⁴−1=15 | — |
| A3 | apply | numeric | −0.22 | 5 binary random variables, no independence assumed. Free parameters needed? `[verified: 31]` | 2⁵−1=31 | — |
| A4 | apply | numeric | −0.18 | A graph on nodes A,B,C,D,E has edges A–B, A–C, B–C, C–D, D–E. What is the degree of node C? `[verified: 3]` | edges touching C: A–C, B–C, C–D → degree 3 | — |
| A5 | apply | numeric | −0.12 | How many edges does a complete graph on 4 nodes have? `[verified: 6]` | C(4,2)=6 | — |
| A6 | apply | numeric | −0.05 | A tree has 6 nodes. How many edges must it have? `[verified: 5]` | a tree on n nodes has n−1 edges: 6−1=5 | — |
| E2 | explain | short-answer | 0.4 | Why does a tree-structured graph (n nodes, n−1 edges) allow especially efficient inference? | a tree has no cycles, so message-passing/inference algorithms can propagate exactly along a single path between any two nodes without needing to account for multiple competing paths of information, unlike a general graph with cycles *(required: names the no-cycles/single-path property)* | — |
| E3 | explain | short-answer | 0.5 | Why does the number of free parameters in an unconstrained joint distribution grow exponentially with the number of variables, while a sparse graphical model's does not? | the full joint table has one free parameter per combination of variable values (2ⁿ−1 for n binary variables), but a sparse graph factors the joint into small local pieces, one per node's neighborhood, so the parameter count grows with the size of each node's neighborhood rather than with the total number of variables *(required: contrasts the exponential full-joint count with the local factorization)* | — |
| E4 | explain | short-answer | 0.6 | Why is checking whether a specific pair of nodes is connected by any path not the same as checking whether they are conditionally independent? | path-connectivity alone doesn't determine independence; the actual independence question depends on the graph's specific separation rules (blocked or unblocked paths given what is conditioned on), which the later d-separation/Markov-blanket machinery formalizes *(required: names that raw connectivity is not equivalent to the independence criterion)* | — |
| E5 | explain | short-answer | 0.7 | Why might two different graphs encode the exact same set of conditional independencies? | several distinct edge structures can imply the identical set of separation statements — e.g. different equivalent DAGs in the same Markov equivalence class — so the graph is not always uniquely recoverable from the independencies alone *(required: names Markov equivalence or an equivalent argument)* | — |
| T2 | transfer | short-answer | 0.9 | Why does adding a redundant edge to an already-correct graph never make the encoded independence structure wrong, only less informative? | an extra edge can only remove an independence claim, never add a false one — the graph with the extra edge is still consistent with any distribution the sparser, correct graph was consistent with, just less efficient/specific about which independencies actually hold *(required: names the one-directional consequence of adding edges)* | — |
| T3 | transfer | short-answer | 1.0 | A learner builds a graphical model but leaves out an edge for two variables that, on reflection, really do have a direct dependence. What failure mode results? | this understates the true dependence structure, meaning the model will implicitly claim an independence that does not actually hold, leading to systematically wrong marginal or conditional probability calculations even though the model looks simpler *(required: names the false-independence-claim consequence of omitting a needed edge)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.15…1.0.*

---

## Directed vs Undirected Graphs (`directed-vs-undirected-graphs`)
*Prereq: Graphs · ancestors 2 · b₀ = 0.05*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.95 | Distinguish directed and undirected graphs, and their probabilistic model names. | directed: edges have direction, often causal/generative — "Bayesian networks"; undirected: edges are symmetric — "Markov random fields" | — |
| R2 | recall | mcq | −0.65 | A directed edge A→B typically suggests: | B's distribution depends on (is conditioned on) A — A is a "parent" of B | claims "A and B are statistically independent" — the opposite of what a directed edge indicates → `directed-vs-undirected-graphs` |
| A1 | apply | short-answer | −0.1 | A graph has Rain→WetGrass and Sprinkler→WetGrass. What does this imply about computing P(WetGrass)? | it must be conditioned on both parents: P(WetGrass\|Rain, Sprinkler) — the graph structure specifies exactly which variables the conditional distribution depends on | — |
| E1 | explain | short-answer | 0.6 | Why must directed graphical models be acyclic (DAGs) for a sensible probabilistic interpretation? | a directed cycle (A causes B causes A) would make it impossible to write the joint as a well-defined product of conditionals — you'd need to condition on something that itself depends on what you're computing; acyclicity is exactly what guarantees such a factorization exists *(required: the circular-conditioning problem specifically)* | — |
| T1 | transfer | short-answer | 1.1 | Describe the "v-structure" issue in converting between directed and undirected representations. | two parents A, B pointing to the same child C, with no direct A–B edge, encodes that A and B are marginally independent but become dependent once C is conditioned on ("explaining away") — a subtlety undirected graphs, which only encode symmetric-looking relationships, cannot represent as naturally *(required: names the marginal-independence/conditional-dependence asymmetry)* — foreshadows `conditional-independence-d-separation` directly | — |
| R3 | recall | mcq | −0.55 | In a directed graph, if A→B, A is called B's: | parent | calls A "B's ancestor," which is correct but more general than the precise term "parent" for a direct edge → `directed-vs-undirected-graphs` |
| R4 | recall | mcq | −0.5 | In a directed graph, if A→B, B is called A's: | child | calls B "A's descendant," the more general term, rather than the precise term "child" for a direct edge → `directed-vs-undirected-graphs` |
| R5 | recall | short-answer | −0.45 | Fill in the blank: the probabilistic model built on a DAG is called a ___. | Bayesian network | — |
| R6 | recall | short-answer | −0.4 | Fill in the blank: the probabilistic model built on an undirected graph is called a ___. | Markov Random Field | — |
| R7 | recall | short-answer | −0.35 | True or false: an undirected graph can contain cycles. | True — unlike a Bayesian network's DAG, undirected graphs have no acyclicity requirement | — |
| R8 | recall | mcq | −0.3 | Converting a DAG into an equivalent undirected graph by connecting all parents of each common child is called: | moralization | calls this process "factorization," confusing it with the general product-of-conditionals decomposition → `directed-vs-undirected-graphs` |
| R9 | recall | short-answer | −0.25 | State the factorization formula for a Bayesian network's joint distribution. | P(X₁,...,Xₙ) = ∏ᵢ P(Xᵢ \| parents(Xᵢ)) | — |
| R10 | recall | short-answer | −0.2 | State the factorization formula for a Markov random field's joint distribution. | P(X) = (1/Z) ∏_c ψ_c(X_c), a product of potential functions over cliques, normalized by the partition function Z | — |
| R11 | recall | short-answer | −0.15 | True or false: the factors in a Markov random field's factorization must each be valid conditional probabilities. | False — they are potential functions, non-negative but not individually normalized to sum to 1 | — |
| R12 | recall | mcq | −0.05 | An "ancestor" of node B in a DAG is: | any node with a directed path leading to B | restricts "ancestor" to mean only B's direct parent, missing the multi-hop generalization → `directed-vs-undirected-graphs` |
| R13 | recall | short-answer | 0.0 | Fill in the blank: a node with no parents in a DAG is called a ___ node. | root | — |
| R14 | recall | mcq | 0.1 | Which structure most naturally encodes a genuinely asymmetric relationship, such as "A causes B" but not vice versa? | a directed graph | claims an undirected graph encodes this equally naturally, missing that its edges are inherently symmetric → `directed-vs-undirected-graphs` |
| R15 | recall | short-answer | 0.15 | True or false: the stated prerequisite concept for Directed vs Undirected Graphs is Graphs. | True | — |
| R16 | recall | short-answer | 0.2 | What does the normalizing constant Z (the "partition function") in an MRF's factorization ensure? | that the product of potential functions sums (or integrates) to 1 over all configurations, making it a valid probability distribution | — |
| A2 | apply | short-answer | 0.0 | A DAG has edges A→C, B→C, C→D. How many parents does node D have? `[verified: 1]` | only C points directly to D | — |
| A3 | apply | short-answer | 0.05 | In the same DAG (A→C, B→C, C→D), how many parents does node C have? `[verified: 2]` | A and B both point directly to C | — |
| A4 | apply | short-answer | 0.1 | A DAG is a chain X₁→X₂→X₃→X₄→X₅. How many edges does it have? `[verified: 4]` | one edge between each consecutive pair: 4 edges for 5 nodes | — |
| A5 | apply | short-answer | 0.2 | Moralizing a DAG where node C has two parents A, B with no existing A–B edge. How many new edges does moralization add here? `[verified: 1]` | connecting the previously-unconnected parents A and B adds exactly 1 edge | — |
| A6 | apply | numeric | 0.3 | An MRF has cliques {A,B}, {B,C}, {C,D} with potentials ψ(A,B)=2, ψ(B,C)=3, ψ(C,D)=1 for one configuration. Unnormalized joint value (before dividing by Z)? `[verified: 6]` | 2·3·1=6 | — |
| E2 | explain | short-answer | 0.65 | Why must a Bayesian network's graph be acyclic, given the factorization P(X)=∏P(Xᵢ\|parents(Xᵢ))? | this factorization requires evaluating each node's conditional given only its parents; a cycle would create a variable that is (indirectly) its own ancestor, so no valid topological order for the factorization could exist *(required: names the topological-order requirement)* | — |
| E3 | explain | short-answer | 0.7 | Why does moralization sometimes lose independence information that the original DAG expressed? | the v-structure at a common child — two independent parents pointing to it — becomes an undirected edge between the parents once moralized, making them look directly connected even though they were marginally independent before conditioning on the child *(required: connects moralization to the v-structure/explaining-away loss)* | — |
| E4 | explain | short-answer | 0.75 | Why can undirected potentials ψ_c(X_c) take any non-negative value, rather than being constrained like conditional probabilities? | the normalizing constant Z is computed globally, after multiplying all the potentials together, so the individual factors only need to be non-negative; local normalization is not required, unlike each P(Xᵢ\|parents) in a Bayesian network, which must be a valid conditional distribution on its own *(required: contrasts local normalization in DAGs with global normalization in MRFs)* | — |
| E5 | explain | short-answer | 0.8 | Why is computing the partition function Z often the computational bottleneck of working with Markov random fields? | Z requires summing (or integrating) the product of potentials over every possible joint configuration of all variables, a sum whose size grows exponentially with the number of variables, unlike a Bayesian network where each local conditional is already normalized and no global sum is needed *(required: names the exponential-sum nature of Z)* | — |
| T2 | transfer | short-answer | 1.15 | Why might a researcher prefer an undirected representation for spatial data (like pixel grids) even though it can't express directionality? | spatial neighbor relationships (e.g. pixel adjacency) have no natural causal order — no pixel "causes" its neighbor — so forcing a directed structure would require an arbitrary choice of order, whereas undirected edges naturally express the symmetric dependency actually present *(required: connects back to the symmetric-relationship motivation, distinct from any expressiveness claim)* | — |
| T3 | transfer | short-answer | 1.2 | Two learners debate whether a Bayesian network or an MRF is "strictly more powerful." Resolve the disagreement. | neither strictly dominates: a DAG can express asymmetric v-structure independencies (explaining away) that moralization loses when converted to an undirected form, while an MRF can naturally express cyclic symmetric dependencies (like a grid of mutually adjacent pixels) that a DAG cannot represent without an arbitrary acyclic ordering — each has independence structures the other cannot represent exactly *(required: names structures unique to each side, not just a vague "different strengths" claim)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.95…1.2.*

---

## Conditional Independence and D-Separation (`conditional-independence-d-separation`)
*Prereq: Directed vs Undirected Graphs, Independence (Set Theory), Conditional Probability · ancestors 8 · b₀ = 0.60*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.4 | Describe d-separation. | a graphical criterion for reading conditional independence directly off a DAG's structure, with no numerical computation — d-separated nodes are conditionally independent given the separating set | — |
| R2 | recall | mcq | −0.1 | The "explaining away" pattern (two parents A, B of child C, no A–B edge) means: | A and B are marginally independent but become dependent once C is observed | resolves `directed-vs-undirected-graphs`'s T1 cliffhanger incorrectly, e.g. claiming "A and B are always independent regardless of whether C is observed" → `conditional-independence-d-separation` |
| A1 | apply | short-answer | 0.4 | Burglary (A) and Earthquake (B) can each independently trigger an Alarm (C). Given the alarm rang and you then learn there was an earthquake, does burglary become more or less likely? | less likely — the earthquake "explains away" the alarm, making burglary a less necessary explanation, a vivid illustration of dependence induced between A and B once C is observed, despite A and B being marginally independent *(required: the explicit "less likely" direction with the explaining-away reasoning)* | — |
| E1 | explain | short-answer | 1.1 | State the d-separation rule for a "collider" (A→C←B), contrasting it with a simple chain (A→C→B). | A and B are d-connected (dependent) when conditioning on C (or any descendant of C), but d-separated when C is not conditioned on — the exact opposite of a chain, where conditioning on C *blocks* dependence between A and B rather than creating it *(required: both structures and their opposite conditioning behavior)* | — |
| T1 | transfer | short-answer | 1.6 | Why can conditioning on the wrong variable actually introduce a spurious association between originally independent variables? | conditioning on a collider (rather than a genuine confounder) induces exactly the dependence described in A1/E1 between variables that were marginally independent — known as collider bias or Berkson's paradox, a genuinely important and often-violated principle in observational data analysis *(required: names collider bias/Berkson's paradox and the collider-vs-confounder distinction)* | — |
| R3 | recall | mcq | −0.35 | In a "chain" structure A→C→B, conditioning on C: | blocks the path, making A and B conditionally independent given C | claims conditioning on C in a chain creates dependence between A and B, the opposite of the actual blocking effect → `conditional-independence-d-separation` |
| R4 | recall | mcq | −0.3 | In a "fork" (common-cause) structure A←C→B, conditioning on C: | blocks the path, making A and B conditionally independent given C | claims conditioning on the common cause C makes A and B more dependent, the opposite of the actual blocking effect → `conditional-independence-d-separation` |
| R5 | recall | short-answer | −0.25 | Fill in the blank: the three canonical three-node structures in d-separation are the chain, the fork, and the ___. | collider | — |
| R6 | recall | short-answer | −0.2 | True or false: a collider structure A→C←B is d-connected only if C, or a descendant of C, is conditioned on. | True | — |
| R7 | recall | short-answer | −0.15 | Define a "blocked path" in the context of d-separation. | a path is blocked if it contains a chain or fork node that is conditioned on, or a collider node that is not conditioned on and has no conditioned descendant | — |
| R8 | recall | mcq | −0.05 | Two nodes are d-separated given a set Z if: | every path between them is blocked given Z | claims d-separation only requires at least one path to be blocked, rather than every path → `conditional-independence-d-separation` |
| R9 | recall | short-answer | 0.0 | Define the Markov blanket of a node in a Bayesian network. | the minimal set of nodes — its parents, children, and children's other parents (co-parents) — that renders it conditionally independent of all other nodes in the network | — |
| R10 | recall | short-answer | 0.05 | Fill in the blank: in a fork A←C→B, without conditioning on C, A and B are ___ dependent. | marginally | — |
| R11 | recall | short-answer | 0.1 | True or false: d-separation gives a sufficient condition for conditional independence, and under the "faithfulness" assumption, also a necessary one. | True | — |
| R12 | recall | mcq | 0.15 | The stated prerequisites for Conditional Independence and D-Separation are: | Directed vs Undirected Graphs, Independence (Set Theory), and Conditional Probability | names only "Directed vs Undirected Graphs," omitting the other two stated prerequisites → `conditional-independence-d-separation` |
| R13 | recall | short-answer | 0.2 | In a chain A→C→B, before conditioning on C, are A and B dependent or independent? | dependent — information flows unblocked through C | — |
| R14 | recall | mcq | 0.25 | A collider's other common name in the literature is: | a "v-structure" | calls a collider a "moral edge," confusing the collider structure itself with the edge added when moralizing it → `conditional-independence-d-separation` |
| R15 | recall | short-answer | 0.3 | Fill in the blank: the phenomenon where conditioning on a collider creates spurious dependence is called collider bias, or ___. | Berkson's paradox | — |
| R16 | recall | short-answer | 0.35 | True or false: d-separation requires actually computing numerical probabilities to determine independence. | False — it is a purely graphical/structural criterion, with no numerical computation needed | — |
| A2 | apply | short-answer | 0.45 | A chain Diet→Cholesterol→HeartDisease. Conditioning on Cholesterol, are Diet and HeartDisease d-separated? | yes — the chain is blocked once the middle node is conditioned on | — |
| A3 | apply | short-answer | 0.5 | A fork Genotype→EyeColor and Genotype→HairColor. Without conditioning on Genotype, are EyeColor and HairColor dependent or independent? | dependent — the fork's shared cause is unblocked, so information flows between the two effects | — |
| A4 | apply | short-answer | 0.55 | A collider Talent→Success←Connections. Without conditioning on Success, are Talent and Connections d-separated? | yes — an unconditioned collider blocks the path by default | — |
| A5 | apply | short-answer | 0.6 | The same collider Talent→Success←Connections, now conditioning on Success. Are Talent and Connections d-separated? | no — conditioning on the collider opens the path, inducing dependence (explaining away) | — |
| A6 | apply | numeric | 0.65 | A node has 2 parents, 3 children, and each child has 1 other parent besides this node. Size of its Markov blanket? `[verified: 8]` | 2 parents + 3 children + 3 co-parents = 8 | — |
| E2 | explain | short-answer | 1.15 | Why does the same node C behave oppositely in a chain versus a collider when conditioned on? | in a chain or fork, C sits "in the way" of the only path connecting A and B, so learning C blocks the flow of information between them; in a collider, C is instead the common effect both A and B independently influence, so learning C creates a new explanatory competition between A and B that wasn't there before *(required: names the "in-the-way" vs "common-effect" structural difference)* | — |
| E3 | explain | short-answer | 1.25 | Why does the Markov blanket give exactly the right conditioning set to make a node independent of everything else? | conditioning on parents blocks all chain/fork paths going up, conditioning on children blocks paths going down, and conditioning on children's other parents blocks the collider paths those children create — together these are exactly the paths that could carry information to the node from elsewhere *(required: names all three components and what each blocks)* | — |
| E4 | explain | short-answer | 1.35 | Why is "faithfulness" a nontrivial assumption rather than an automatic property of any distribution consistent with a graph? | a distribution could, by an exact numerical coincidence (canceling paths), be independent even though the graph's structure implies dependence; faithfulness explicitly rules out such coincidences, assuming every independence present in the distribution is also implied structurally by the graph *(required: names the coincidental-cancellation counterexample to faithfulness)* | — |
| E5 | explain | short-answer | 1.45 | Why can adjusting for a variable in an observational study sometimes make a causal estimate worse rather than better? | if the adjusted-for variable is a collider, or a descendant of one, on the causal path being studied, conditioning on it opens a spurious association per collider bias, rather than closing a genuine confounding path the way adjusting for a true confounder would *(required: distinguishes conditioning-opens vs conditioning-closes depending on structure)* | — |
| T2 | transfer | short-answer | 1.65 | A researcher estimates the causal effect of Exercise on HeartHealth but also controls for Fitness, a collider caused by both Exercise and Genetics. What mistake does this introduce? | conditioning on Fitness — a collider on the Exercise→Fitness←Genetics path — opens a spurious association between Exercise and Genetics, contaminating the Exercise→HeartHealth estimate with a genetics-driven confound that adjusting for Fitness was never intended to introduce *(required: identifies Fitness specifically as the problematic collider control)* | — |
| T3 | transfer | short-answer | 1.7 | When can two graphically d-connected variables still be genuinely independent in the actual distribution? | only through an exact numerical coincidence, e.g. two paths' effects cancelling exactly, which violates faithfulness; under the standard faithfulness assumption typically made in practice, d-connection is treated as implying genuine dependence, but this is an assumption about the distribution, not a guarantee from the graph alone *(required: names the coincidental-cancellation exception and that faithfulness is an assumption, not a guarantee)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.4…1.7.*

---

## Markov Random Fields (`markov-random-fields`)
*Prereq: Conditional Independence and D-Separation · ancestors 9 · b₀ = 0.65*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.35 | Describe a Markov Random Field's defining property. | a node is conditionally independent of all other nodes given its immediate neighbors — the "Markov blanket" for an undirected graph is exactly its neighbor set | — |
| R2 | recall | mcq | −0.05 | MRFs are often preferred for spatial/symmetric dependence (neighboring pixels) because: | spatial relationships have no natural direction — undirected edges capture this symmetry naturally | claims "MRFs always require fewer parameters" — not a general guarantee, and not the actual motivating reason → `markov-random-fields` |
| A1 | apply | short-answer | 0.45 | Why would modeling neighboring image pixels with a *directed* graph feel awkward? | it would force an arbitrary choice of which pixel "causes" which, when spatial adjacency has no genuine directionality — directly the lack-of-natural-direction point from R2 *(required: connects explicitly to R2)* | — |
| E1 | explain | short-answer | 1.15 | State the Hammersley-Clifford theorem informally. | an MRF's joint distribution factors as a product of "potential functions" over cliques (fully-connected subsets) of the graph — the undirected analog of a Bayesian network's product-of-conditionals factorization, without any directional/causal interpretation attached to each factor *(required: names cliques and the lack of causal interpretation)* | — |
| T1 | transfer | short-answer | 1.65 | Why is a Markov chain a special, simple case of an MRF? | a chain-structured graph (X₁–X₂–X₃–⋯–Xₙ, each connected only to immediate neighbors in a linear sequence) has the Markov property (future depends on past only through the present) exactly matching MRFs' "conditionally independent given neighbors" property, applied to this specific linear graph structure *(required: the explicit graph-structure-to-Markov-property mapping)* | — |
| R3 | recall | mcq | −0.3 | The Markov blanket of a node in an MRF is exactly: | its immediate neighbors in the graph | claims the Markov blanket also always includes every node's neighbors-of-neighbors, confusing it with reachability rather than direct adjacency → `markov-random-fields` |
| R4 | recall | short-answer | −0.25 | Fill in the blank: the Hammersley-Clifford theorem requires the joint distribution to be ___ for the clique-factorization equivalence to hold. | positive (strictly nonzero everywhere) | — |
| R5 | recall | short-answer | −0.2 | True or false: in an MRF, a node is conditionally independent of all non-neighbors given its neighbors. | True | — |
| R6 | recall | mcq | −0.15 | A "pairwise MRF" restricts clique potentials to: | cliques of size at most 2 (single nodes and pairs) | claims a pairwise MRF restricts the graph itself to have at most 2 nodes total, confusing clique size with graph size → `markov-random-fields` |
| R7 | recall | short-answer | −0.1 | Define the Gibbs distribution form used to write an MRF's joint density. | P(x) = (1/Z) exp(−E(x)), where E(x) is an "energy function" summing local potentials over cliques | — |
| R8 | recall | short-answer | 0.0 | Fill in the blank: in the Gibbs form P(x)=(1/Z)exp(−E(x)), lower-energy configurations have ___ probability. | higher | — |
| R9 | recall | mcq | 0.05 | The Ising model, a classic MRF example, was originally developed to model: | magnetic spin interactions in physics | claims the Ising model was originally developed for image segmentation, which is a later application borrowed from its physics origin, not its origin itself → `markov-random-fields` |
| R10 | recall | short-answer | 0.1 | State the "global Markov property" for an MRF. | any two sets of nodes A and B are conditionally independent given a separating set S, if S separates A from B in the graph — removing S disconnects A from B | — |
| R11 | recall | short-answer | 0.15 | True or false: MRFs require each clique potential to be an individually normalized probability, similar to a Bayesian network's local conditional. | False — clique potentials need only be non-negative; normalization happens globally via Z | — |
| R12 | recall | mcq | 0.2 | A common application domain for MRFs is: | image segmentation and denoising, exploiting neighboring-pixel dependence | claims MRFs are used exclusively for time-series forecasting, missing their classic spatial-data applications → `markov-random-fields` |
| R13 | recall | short-answer | 0.25 | Fill in the blank: MRFs are also known by the alternative name ___, particularly in the spatial-statistics literature. | Markov networks | — |
| R14 | recall | short-answer | 0.3 | True or false: MRFs can be equivalently represented as "factor graphs," making explicit which variables each potential function depends on. | True | — |
| R15 | recall | mcq | 0.35 | The stated prerequisite concept for Markov Random Fields is: | Conditional Independence and D-Separation | names Hidden Markov Models, which is not this concept's stated prerequisite → `markov-random-fields` |
| R16 | recall | short-answer | 0.4 | Why is Z, the partition function, generally hard to compute for a large MRF? | summing over all possible joint configurations grows exponentially with the number of variables | — |
| A2 | apply | short-answer | 0.5 | A pixel grid MRF where each interior pixel has 4 neighbors (up/down/left/right). Markov blanket size of an interior pixel? `[verified: 4]` | its 4 grid neighbors | — |
| A3 | apply | short-answer | 0.55 | In the same grid, an edge pixel (not a corner) has 3 neighbors. Markov blanket size? `[verified: 3]` | its 3 grid neighbors | — |
| A4 | apply | short-answer | 0.6 | In the same grid, a corner pixel has 2 neighbors. Markov blanket size? `[verified: 2]` | its 2 grid neighbors | — |
| A5 | apply | short-answer | 0.65 | Configuration A has energy 3; configuration B has energy 1, same Z. Which has higher probability under the Gibbs distribution? `[verified: B]` | lower energy means higher probability; 1<3, so B is more probable | — |
| A6 | apply | numeric | 0.7 | Energies E_A=2, E_B=5 (same Z). Ratio P(A)/P(B) under the Gibbs distribution? `[verified: 20.09]` | P(A)/P(B)=exp(−(E_A−E_B))=exp(3)≈20.09 | — |
| E2 | explain | short-answer | 1.2 | Why must the Hammersley-Clifford theorem require strict positivity of the distribution? | without positivity, a distribution can display conditional independencies satisfying the local Markov property but that cannot be written as a product of clique potentials — the theorem's equivalence between the graphical Markov property and clique factorization only holds once every configuration has nonzero probability *(required: names positivity as the condition closing the gap)* | — |
| E3 | explain | short-answer | 1.3 | Why do the local Markov property and the global Markov property turn out to be equivalent under Hammersley-Clifford? | both are consequences of the same underlying clique factorization: once the joint factors over cliques, any path from a node to a non-neighbor must pass through some neighbor, so conditioning on all neighbors necessarily blocks every such path — the same mechanism underlying the more general statement about separated sets *(required: connects both properties to the shared clique-factorization root)* | — |
| E4 | explain | short-answer | 1.4 | Why does lowering the "temperature" parameter in a Gibbs/Boltzmann distribution increasingly concentrate probability on the lowest-energy configuration(s)? | the Gibbs form is P(x)∝exp(−E(x)/T); as T→0, the exponent for any configuration other than the global minimum diverges to −∞ relative to the minimum, so its relative probability vanishes, concentrating essentially all mass on the minimum-energy configuration(s) *(required: the explicit T→0 limiting argument)* | — |
| E5 | explain | short-answer | 1.5 | Why is exact inference in a general, cyclic MRF computationally intractable while inference in a tree-structured MRF is efficient? | in a tree, message-passing can propagate information exactly along the single unique path between any two nodes; in a graph with cycles, there is no single path, so approximate methods (loopy belief propagation, sampling) or exact but exponential methods (junction tree) are needed instead *(required: contrasts the single-path tree case with the cyclic general case)* | — |
| T2 | transfer | short-answer | 1.7 | How does an MRF's clique-potential parameterization relate to a convolutional neural network's local receptive field? | both encode the same locality assumption — that a unit's relevant information comes from a small neighborhood rather than the whole input; a convolution's receptive field is essentially a fixed local clique-like dependency structure, though it computes a feature rather than a probability directly *(required: names the shared locality assumption while noting the feature-vs-probability distinction)* | — |
| T3 | transfer | short-answer | 1.75 | Why might a practitioner prefer a directed Bayesian network over an MRF for a generative sequence model, even if both could represent similar dependencies? | a Bayesian network's local conditionals are already individually normalized probabilities, so sampling is straightforward via ancestral sampling in topological order; an MRF's clique potentials generally require the global partition function Z to sample correctly, which is often intractable, making the directed model more practical *(required: names the ancestral-sampling advantage of normalized local conditionals)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.35…1.75.*

---

## Markov Chains (`markov-chains`)
*Prereq: Directed vs Undirected Graphs, Conditional Probability · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | State the Markov property. | P(X_{n+1}\|Xₙ,…,X₁) = P(X_{n+1}\|Xₙ) — the future depends on the past only through the present state | — |
| R2 | recall | mcq | −0.16 | The stationary distribution π (if it exists) satisfies: | π = πP — π is a left eigenvector of the transition matrix P with eigenvalue 1 | claims "π is always uniform" — only true for special transition matrices, not in general → `markov-chains` |
| A1 | apply | short-answer | 0.34 | Why is finding a Markov chain's stationary distribution a direct application of `eigenvalues-eigenvectors`? | this is the exact fact that concept's own T1 introduced: finding π is finding the left eigenvector of the transition matrix with eigenvalue 1, a direct reuse rather than a fresh technique *(required: the explicit callback to that earlier item)* | — |
| E1 | explain | short-answer | 1.04 | How does `matrix-stability`'s spectral-radius logic govern a Markov chain's convergence to its stationary distribution? | eigenvalues of the transition matrix other than the guaranteed eigenvalue 1 determine how fast the chain converges — eigenvalues close to 1 in magnitude mean slow "mixing," eigenvalues far from 1 mean fast convergence, the same eigenvalue-magnitude-governs-long-run-behavior logic from `matrix-stability` *(required: the direct callback to that concept's spectral-radius argument)* | — |
| T1 | transfer | short-answer | 1.54 | How does MCMC connect the abstract theory of stationary distributions to a practical computational technique? | MCMC constructs a Markov chain whose stationary distribution is exactly the target posterior one wants to sample from, converting the abstract R1/R2 theory into a genuinely practical, widely-used tool for sampling from complex distributions *(required: names that the chain's stationary distribution is engineered to match the target)* | — |
| R3 | recall | mcq | −0.41 | A Markov chain's transition matrix P has rows that: | sum to 1 — each row is a valid probability distribution over the next state | claims the columns, not the rows, must sum to 1, reversing the actual convention → `markov-chains` |
| R4 | recall | short-answer | −0.36 | Fill in the blank: the n-step transition probability matrix is given by ___. | Pⁿ, P raised to the n-th power | — |
| R5 | recall | short-answer | −0.31 | True or false: a Markov chain is "time-homogeneous" if its transition probabilities don't change over time. | True | — |
| R6 | recall | mcq | −0.26 | A state that, once entered, can never be left is called: | an absorbing state | calls it a "transient state," which is the opposite behavior — a transient state is eventually left, not permanently entered → `markov-chains` |
| R7 | recall | short-answer | −0.21 | Define "irreducibility" for a Markov chain. | every state can be reached from every other state — the chain has a single communicating class | — |
| R8 | recall | short-answer | −0.11 | Fill in the blank: a Markov chain is "aperiodic" if the greatest common divisor of its possible return times to any state is ___. | 1 | — |
| R9 | recall | short-answer | −0.06 | True or false: an "ergodic" Markov chain is both irreducible and aperiodic. | True | — |
| R10 | recall | mcq | −0.01 | The Chapman-Kolmogorov equations describe: | how n-step transition probabilities can be computed by summing over all possible intermediate states | claims they describe how to compute the stationary distribution directly, which is a different (eigenvector) calculation → `markov-chains` |
| R11 | recall | short-answer | 0.04 | Distinguish the "stationary distribution" from the "limiting distribution" of a Markov chain. | the stationary distribution π satisfies π=πP, a fixed point under one transition; the limiting distribution is what the chain's distribution converges to as t→∞ — they coincide for an ergodic chain, but a stationary distribution can exist without being reached in the limit | — |
| R12 | recall | short-answer | 0.09 | Fill in the blank: "detailed balance" for a Markov chain requires π_i P_ij = ___ for all i,j. | π_j P_ji | — |
| R13 | recall | mcq | 0.14 | The stated prerequisites for Markov Chains are: | Directed vs Undirected Graphs and Conditional Probability | names Hidden Markov Models as a prerequisite, when HMM is actually built on top of Markov Chains, not the reverse → `markov-chains` |
| R14 | recall | short-answer | 0.19 | What does it mean for a Markov chain to have a "random walk" structure? | the next state is reached by adding a random increment (step) to the current state — a common concrete example of a Markov chain | — |
| R15 | recall | short-answer | 0.24 | True or false: the Markov property implies the entire past history has no influence on the future, given the present. | True | — |
| R16 | recall | mcq | 0.29 | First-step analysis is a technique used to: | compute quantities like expected hitting times or absorption probabilities by conditioning on the very first transition | claims first-step analysis is used to find the stationary distribution directly, which is instead solved via the π=πP eigenvector equation → `markov-chains` |
| A2 | apply | numeric | 0.39 | Transition matrix P=[[0.7,0.3],[0.4,0.6]]. Probability of being in state 1 after 2 steps, starting in state 1? `[verified: 0.61]` | (P²)₁₁=0.7·0.7+0.3·0.4=0.49+0.12=0.61 | — |
| A3 | apply | numeric | 0.44 | Same P=[[0.7,0.3],[0.4,0.6]]. Solve π=πP for π₁ (the stationary probability of state 1). `[verified: 0.571]` | π₁·0.3=π₂·0.4 and π₁+π₂=1 → π₁=0.4/0.7≈0.571 | — |
| A4 | apply | short-answer | 0.49 | A chain has states A,B,C,D with A↔B, C absorbing, D absorbing, and no other connections. How many communicating classes does it have? `[verified: 3]` | {A,B}, {C}, {D} | — |
| A5 | apply | numeric | 0.54 | Transition matrix P=[[0.5,0.5],[0.2,0.8]]. Probability of being in state 2 after 2 steps, starting in state 1? `[verified: 0.65]` | (P²)₁₂=0.5·0.5+0.5·0.8=0.25+0.4=0.65 | — |
| A6 | apply | short-answer | 0.59 | A state's possible return times are {2,4,6,8,...}. Is the chain aperiodic at this state? `[verified: no, period 2]` | gcd of {2,4,6,8,...}=2≠1, so the state has period 2 and the chain is not aperiodic | — |
| E2 | explain | short-answer | 1.09 | Why does an absorbing state make an otherwise irreducible chain no longer irreducible? | irreducibility requires every state to be reachable from every other state; once a state can never be left, no other state can be reached from it again, breaking the two-way reachability irreducibility requires *(required: names the two-way-reachability failure caused by the absorbing state)* | — |
| E3 | explain | short-answer | 1.19 | Why can a stationary distribution exist for a periodic chain without ever being reached as a limiting distribution? | periodicity means the chain's distribution cycles through a fixed pattern rather than converging to a single limit as t→∞, even though the same π=πP fixed-point distribution still satisfies stationarity and describes the chain's long-run average behavior *(required: separates the fixed-point equation from actual convergence)* | — |
| E4 | explain | short-answer | 1.29 | Why does the Chapman-Kolmogorov equation P^(m+n)=PᵐPⁿ follow directly from the Markov property? | conditioning on the state at intermediate time m and using the Markov property lets the (m+n)-step transition probability be decomposed exactly into a sum over intermediate states of the product of the two shorter-step transition probabilities *(required: names the Markov-property-driven decomposition through the intermediate state)* | — |
| E5 | explain | short-answer | 1.39 | Why does detailed balance, when it holds, guarantee the resulting distribution is stationary, even though it's a stronger condition than stationarity itself? | detailed balance requires the probability flow between every individual pair of states to balance (π_i P_ij=π_j P_ji), a stronger, local condition than merely requiring total inflow and outflow at each state to balance — the weaker global condition stationarity actually needs — but the stronger local condition trivially implies the weaker global one *(required: names detailed balance as a sufficient, stronger-than-necessary condition)* | — |
| T2 | transfer | short-answer | 1.59 | Why is checking a chain's irreducibility and aperiodicity (ergodicity) a necessary first step before running MCMC? | MCMC relies on a Markov chain's distribution converging to a unique stationary distribution regardless of starting point; a reducible chain may get stuck in a subset of states, and a periodic chain may never converge (only cycle), so ergodicity guarantees the simulation actually estimates the intended target distribution *(required: connects ergodicity to the convergence guarantee a nonergodic chain lacks)* | — |
| T3 | transfer | short-answer | 1.64 | Contrast the assumptions a Markov chain makes about memory with those of an HMM, previewing the next concept in this cluster. | a plain Markov chain assumes the observed state sequence itself has the Markov property; an HMM instead assumes an unobserved hidden state sequence has the Markov property, while observed emissions depend only on the corresponding hidden state — the memorylessness assumption moves from the observed layer to a hidden layer *(required: names the shift from observed-state to hidden-state memorylessness)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.46…1.64.*

---

## Hidden Markov Models (`hmm`)
*Prereq: Markov Chains, Joint Distribution · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | Describe an HMM's structure. | a Markov chain of hidden states, where each state probabilistically generates an observed "emission"; the observer sees only emissions, not the hidden states | — |
| R2 | recall | mcq | 0.0 | "Hidden" in HMM refers to: | the underlying state sequence being unobserved — only emissions are directly observed | claims "the emissions being hidden" — backwards; emissions are exactly what's observed → `hmm` |
| A1 | apply | short-answer | 0.5 | A person's true mood (hidden) influences observed activities (emissions). Why is inferring the mood sequence from only the activity sequence the central HMM inference problem? | this is exactly the "decoding" problem — recovering the most likely hidden state sequence given only observed emissions — solved by the Viterbi algorithm; it's the defining inference task an HMM is built to answer *(required: names decoding/Viterbi specifically)* | — |
| E1 | explain | short-answer | 1.2 | Why does an HMM's joint distribution factor into transition and emission probabilities? | this is a direct application of `joint-distribution`'s factorization ideas, structured according to the specific dependence pattern an HMM assumes: each hidden state depends only on the previous hidden state (transition), and each emission depends only on its own hidden state (emission) *(required: names both factor types and their dependence pattern)* | — |
| T1 | transfer | short-answer | 1.7 | Why are HMMs considered a general-purpose modeling tool rather than a speech-specific technique? | the same "latent state generates a noisy observation" structure appears across many applications — gene sequence analysis, financial regime-switching models, part-of-speech tagging — all sharing the identical hidden/emission structure that made HMMs foundational for speech recognition originally *(required: at least two concrete non-speech applications)* | — |
| R3 | recall | mcq | −0.25 | The three canonical problems solved for an HMM are: | evaluation (likelihood of an observation sequence), decoding (most likely hidden state sequence), and learning (estimating model parameters) | lists "evaluation, decoding, and clustering" — clustering is not one of the three canonical HMM problems → `hmm` |
| R4 | recall | short-answer | −0.2 | Fill in the blank: the algorithm used to solve the "evaluation" problem efficiently is the ___ algorithm. | forward | — |
| R5 | recall | short-answer | −0.15 | Fill in the blank: the algorithm used to solve the "decoding" problem is the ___ algorithm. | Viterbi | — |
| R6 | recall | short-answer | −0.1 | True or false: the Viterbi algorithm uses dynamic programming, replacing a sum with a max at each step compared to the forward algorithm. | True | — |
| R7 | recall | mcq | −0.05 | The algorithm used to solve the "learning" problem — estimating HMM parameters from data — is: | Baum-Welch, a special case of EM | names "Viterbi" as the parameter-learning algorithm, confusing it with the decoding algorithm → `hmm` |
| R8 | recall | short-answer | 0.05 | What are the three sets of parameters that fully specify an HMM? | the initial state distribution, the transition probability matrix between hidden states, and the emission probability matrix from hidden states to observations | — |
| R9 | recall | short-answer | 0.1 | Fill in the blank: the Baum-Welch algorithm is a special case of the ___ algorithm. | EM (Expectation-Maximization) | — |
| R10 | recall | short-answer | 0.15 | True or false: computing the exact likelihood of an observation sequence by brute-force summing over all hidden state sequences takes time exponential in the sequence length. | True — this is exactly what the forward algorithm's dynamic programming avoids | — |
| R11 | recall | mcq | 0.2 | The emission probability P(observation\|hidden state) describes: | how likely each possible observation is, given the current hidden state | describes it as "how likely each hidden state is, given the observation" — this reverses the direction of the emission probability, which is actually a posterior computed from it → `hmm` |
| R12 | recall | short-answer | 0.25 | State the two conditional independence assumptions an HMM makes. | the hidden state at time t depends only on the hidden state at time t−1 (Markov property on the hidden layer), and the emission at time t depends only on the hidden state at time t, not on any other hidden states or emissions | — |
| R13 | recall | short-answer | 0.3 | Fill in the blank: the prerequisite concepts for Hidden Markov Models are ___ and ___. | Markov Chains and Joint Distribution | — |
| R14 | recall | mcq | 0.35 | In part-of-speech tagging as an HMM application, the hidden states are: | the grammatical tags (noun, verb, etc.), and the emissions are the observed words | reverses the roles, claiming the words are hidden and the tags are observed → `hmm` |
| R15 | recall | short-answer | 0.4 | True or false: in an HMM, the hidden state sequence is assumed to be directly observable during training. | False — it is precisely what's unobserved; that's why it's called "hidden" | — |
| R16 | recall | short-answer | 0.45 | What does the forward algorithm compute at each time step, using dynamic programming? | the joint probability of the observations up to that time and being in each possible hidden state at that time, computed efficiently by reusing the previous time step's values | — |
| A2 | apply | numeric | 0.55 | An HMM has 3 hidden states and 4 possible observations. How many free parameters does the emission matrix have (each state's distribution over 4 outcomes)? `[verified: 9]` | each state's emission distribution has 4−1=3 free parameters; 3 states × 3 = 9 | — |
| A3 | apply | numeric | 0.6 | Same HMM, 3 hidden states. How many free parameters does the transition matrix have? `[verified: 6]` | each row has 3−1=2 free parameters; 3 rows × 2 = 6 | — |
| A4 | apply | numeric | 0.65 | Same HMM, 3 hidden states. How many free parameters does the initial state distribution have? `[verified: 2]` | 3−1=2 | — |
| A5 | apply | numeric | 0.7 | Same HMM (3 hidden states, 4 observations). Total free parameters across initial distribution, transition matrix, and emission matrix? `[verified: 17]` | 2+6+9=17 | — |
| A6 | apply | numeric | 0.75 | A sequence has length T=5 with K=3 possible hidden states at each position. How many distinct hidden state sequences would brute-force summation need to consider? `[verified: 243]` | Kᵀ=3⁵=243 | — |
| E2 | explain | short-answer | 1.25 | Why does the forward algorithm avoid the exponential blowup of brute-force summing over all hidden state sequences? | it reuses partial sums computed at time t−1 to compute the values at time t, rather than recomputing sums over the exponentially many full-length sequences from scratch — a dynamic-programming reuse that reduces complexity from exponential in T to polynomial *(required: names the reuse-of-partial-computation mechanism)* | — |
| E3 | explain | short-answer | 1.35 | Why does the Viterbi algorithm use a max rather than a sum at each step, unlike the forward algorithm? | the forward algorithm wants the total probability of the observations, summed over every possible hidden path (an evaluation question), while Viterbi wants the single most likely hidden path (a decoding question) — replacing sum with max correctly tracks the best path's probability rather than the total probability over all paths *(required: connects the sum-vs-max choice to the evaluation-vs-decoding distinction)* | — |
| E4 | explain | short-answer | 1.45 | Why is Baum-Welch, like other EM applications, only guaranteed to find a local rather than global optimum of the likelihood? | the HMM's parameters and the posterior over hidden states are updated alternately, each step improving the likelihood given the other fixed, but this coordinate-ascent-style procedure can settle into any of several local maxima depending on initialization — the same local-optimum limitation general EM inherits *(required: connects to EM's general local-optimum limitation, not something HMM-specific)* | — |
| E5 | explain | short-answer | 1.55 | Why does the HMM's Markov assumption on the hidden layer not require the observed emission sequence itself to have the Markov property? | the emissions are a noisy, indirect view of the hidden states; even though the hidden chain is Markov, marginalizing out the hidden states to describe the observed sequence alone generally produces a sequence with longer-range dependence than a simple first-order Markov chain would have *(required: names that marginalizing over hidden states breaks the simple Markov property at the observed layer)* | — |
| T2 | transfer | short-answer | 1.75 | Why does an HMM's assumption that observations depend only on the current hidden state sometimes fail in real speech recognition, and what does this motivate? | in real speech, an acoustic observation can be influenced by coarticulation with neighboring phonemes, not just the current phoneme's hidden state, violating the strict per-timestep emission independence; this motivates richer sequence models (e.g. those using attention or explicit context windows) that relax the single-timestep dependence assumption *(required: names a concrete violation of the emission-independence assumption and what it motivates)* | — |
| T3 | transfer | short-answer | 1.8 | Contrast an HMM's hidden-state inference with a Kalman filter's, as two members of the same general state-space-model family. | both assume a Markov hidden state generating noisy observations, differing mainly in whether the state space is discrete (HMM, solved by Viterbi/forward-backward) or continuous with Gaussian noise (Kalman filter, solved by an analytic recursive update) — the same generative structure, specialized to different state-space assumptions *(required: names the discrete-vs-continuous-Gaussian distinction within the shared state-space-model structure)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.3…1.8.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| graphical models mistaken for data visualization tools | `graphs` |
| directed edge direction misread as independence | `directed-vs-undirected-graphs` |
| explaining-away resolved in the wrong direction | `conditional-independence-d-separation` |
| collider bias unrecognized when conditioning on the wrong variable | `conditional-independence-d-separation` |
| MRF's motivation reduced to "fewer parameters" rather than the lack of natural direction | `markov-random-fields` |
| stationary distribution assumed uniform | `markov-chains` |
| HMM's "hidden" label misapplied to emissions rather than states | `hmm` |
| node/edge semantics confused (node as data point, edge as unconditional causation) | `graphs` |
| subgraph or connectivity terms defined incorrectly | `graphs` |
| sparsity's parameter-saving motivation missed | `graphs` |
| parent/child conflated with the more general ancestor/descendant | `directed-vs-undirected-graphs` |
| moralization confused with generic factorization | `directed-vs-undirected-graphs` |
| MRF potentials wrongly assumed to need local normalization like DAG conditionals | `directed-vs-undirected-graphs` |
| chain/fork blocking behavior reversed | `conditional-independence-d-separation` |
| d-separation's "every path" requirement weakened to "some path" | `conditional-independence-d-separation` |
| faithfulness treated as automatic rather than an assumption | `conditional-independence-d-separation` |
| Markov blanket in an MRF confused with multi-hop reachability | `markov-random-fields` |
| clique-size restriction confused with graph-size restriction | `markov-random-fields` |
| Ising model's physics origin confused with a later application | `markov-random-fields` |
| transition matrix row/column normalization convention reversed | `markov-chains` |
| absorbing and transient states confused | `markov-chains` |
| Chapman-Kolmogorov confused with the stationary-distribution equation | `markov-chains` |
| first-step analysis confused with stationary-distribution computation | `markov-chains` |
| HMM's three canonical problems conflated with unrelated tasks (e.g. clustering) | `hmm` |
| Viterbi and Baum-Welch's roles (decoding vs. learning) swapped | `hmm` |
| emission probability's conditioning direction reversed | `hmm` |

**Cluster total: 180 items across 6 concepts (30 each), expanded from the original 30-item design-doc
skeleton.** This cluster is conceptually dense (structural graph reasoning) rather than numerically
heavy, though the added apply items verify small counting/matrix-power computations (parameter counts,
Markov-blanket sizes, n-step transition probabilities, Gibbs-distribution energy ratios) by hand. The
classic burglar-alarm "explaining away" example is Pearl's standard illustration, reused verbatim as
it's the clearest concrete instance of the phenomenon.
