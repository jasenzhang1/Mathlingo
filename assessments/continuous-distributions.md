# Cluster 4 — Continuous Distributions

Normal → F-Distribution (8 concepts). Same format as [Cluster 1](foundations-of-probability.md).

**One closure note.** `f-distribution`'s only official prerequisite is `chi-square-distribution` — it
does *not* list `t-distribution`, even though the cleanest possible fact about F (T² ~ F₁,ₖ) needs
both. That item is kept, because it is too good to drop, but explicitly flagged ⚠ as needing
`t-distribution` unlocked too; everything else in the F section stays inside the concept's real
closure. This is the same discipline the pilot file used for its own downstream-leaking items.

---

## Normal Distribution (`normal-distribution`)
*Prereq: PDF, Expectation, Variance · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State the density of Normal(μ, σ²) and what μ, σ² each control. | f(x) = 1/(σ√(2π)) · exp(−(x−μ)²/(2σ²)); μ shifts location, σ controls spread | — |
| R2 | recall | mcq | 0.1 | Which is FALSE? (a) Normal is symmetric about μ (b) ≈95% of its mass lies within 2σ of μ (c) Normal is the *only* distribution with mean = median = mode. | (c) is false | picks (c) as true — misses that *any* symmetric, unimodal distribution (e.g. the t-distribution) shares this property; it isn't special to Normal → `normal-distribution` |
| A1 | apply | numeric | 0.54 | IQ scores ~ N(100, 15²). Find P(score > 130). `[verified: 0.0228]` | Z = (130−100)/15 = 2; P(Z>2) ≈ 0.0228 | forgets to standardize and looks up P(X>130) directly on a Z-table → `normal-distribution` |
| A2 | apply | numeric | 0.7 | X ~ N(50, 10²). Find the 90th percentile of X. `[verified: 62.82]` | z₀.₉₀ ≈ 1.2816; x = 50 + 10(1.2816) ≈ 62.82 | applies the z-score formula backwards, computing 50 − 10(1.2816) → `normal-distribution` |
| E1 | explain | derivation | 1.24 | Show directly (by differentiating the CDF, not by citing a named transformation rule) that Z=(X−μ)/σ is standard normal when X~N(μ,σ²). | F_Z(z) = P(Z≤z) = P(X≤μ+σz) = F_X(μ+σz); differentiate: f_Z(z) = σ·f_X(μ+σz); substitute the Normal density and simplify the exponent to −z²/2, leaving the standard normal density *(required: the full substitution, not just citing that standardizing "works")* | — |
| E2 | explain | short-answer | 1.4 | Using only the symmetry f(μ+t) = f(μ−t), argue (don't just assert) that the mean, median, and mode of Normal(μ,σ²) all equal μ. | symmetry about μ makes μ the median directly (half the mass on each side by reflection); it makes μ the mode since f is maximized where the exponent's magnitude is smallest, i.e. at x=μ; and it makes μ the mean since ∫(x−μ)f(x)dx integrates an odd function around μ to 0 *(required: an argument for each of the three, not just one)* | — |
| T1 | transfer | short-answer | 1.74 | Give two real quantities well-approximated by Normal, and one quantity often *assumed* Normal that isn't — say specifically what property of Normal it violates. | good fits: measurement error, standardized test scores away from floor/ceiling effects. Common bad fit: household income (or reaction times) — right-skewed and bounded below by 0, while Normal is symmetric with support on all of ℝ; a Normal model would assign real probability to impossible negative values *(required: names the specific violated property, not just "it's not Normal")* | picks a skewed example but says only "it doesn't look bell-shaped," without naming *which* Normal property (symmetry, unbounded support) actually fails → `normal-distribution` |
| R3 | recall | mcq | −0.7 | Under the empirical (68-95-99.7) rule, roughly what percent of Normal mass lies within 1σ of μ? | 68% | picks 95%, mixing up the 1σ and 2σ figures → `normal-distribution` |
| R4 | recall | short-answer | −0.62 | Fill in the blank: the z-score formula is z = ___. | (x−μ)/σ | — |
| R5 | recall | short-answer | −0.54 | True or false: the standard normal distribution has mean 0 and variance 1. | true | — |
| R6 | recall | short-answer | −0.46 | Define "standardizing" a normal random variable in one line. | converting X~N(μ,σ²) into Z=(X−μ)/σ, which is N(0,1) | — |
| R7 | recall | mcq | −0.38 | Under the empirical rule, roughly what percent of Normal mass lies within 3σ of μ? | 99.7% | picks 99.9% → `normal-distribution` |
| R8 | recall | short-answer | −0.3 | True or false: the Normal density has inflection points at exactly μ±σ. | true | — |
| R9 | recall | short-answer | −0.22 | Fill in the blank: Normal notation is written X ~ N(___, ___). | μ, σ² | — |
| R10 | recall | mcq | −0.14 | If X ~ N(μ,σ²), then aX+b (for constants a,b) is: | still Normal, with mean aμ+b and variance a²σ² | picks "Normal only if a=1" — any nonzero a preserves normality → `normal-distribution` |
| R11 | recall | short-answer | −0.06 | True or false: the Normal density is symmetric about μ. | true | — |
| R12 | recall | mcq | 0.02 | Which parameter controls the "width" (spread) of the bell curve? | σ | picks μ → `normal-distribution` |
| R13 | recall | short-answer | 0.1 | Fill in the blank: the standard normal is denoted Z ~ N(___, ___). | 0, 1 | — |
| R14 | recall | short-answer | 0.18 | True or false: as σ increases (μ fixed), the peak height of the density decreases. | true — the curve must spread out while still integrating to 1 | — |
| R15 | recall | mcq | 0.26 | The parameter μ represents: | the location of the peak/center of the distribution | picks "the spread of the distribution" — that's σ → `normal-distribution` |
| R16 | recall | short-answer | 0.34 | What does Φ(z) conventionally denote? | the CDF of the standard normal distribution, evaluated at z | — |
| A3 | apply | numeric | 0.7 | X ~ N(70, 10²) (a test score). Find P(X<55). `[verified: 0.0668]` | z=(55−70)/10=−1.5; P(Z<−1.5)≈0.0668 | forgets the sign of z and looks up P(Z<1.5)≈0.9332 instead → `normal-distribution` |
| A4 | apply | numeric | 0.76 | X ~ N(500, 100²). Find the 95th percentile of X. `[verified: 664.49]` | z₀.₉₅≈1.6449; x=500+100(1.6449)≈664.49 | uses z=1.96 (the 97.5th-percentile value) instead of the 95th-percentile z-score → `normal-distribution` |
| A5 | apply | numeric | 0.82 | X ~ N(20, 4²). Find the z-score of x=26. `[verified: 1.5]` | z=(26−20)/4=1.5 | divides by σ²=16 instead of σ=4, getting z=0.375 → `normal-distribution` |
| A6 | apply | numeric | 0.88 | X ~ N(50, 5²). Find P(45<X<60). `[verified: 0.8186]` | z₁=(45−50)/5=−1, z₂=(60−50)/5=2; P(−1<Z<2)=Φ(2)−Φ(−1)≈0.9772−0.1587=0.8186 | computes Φ(2)−Φ(1) instead of Φ(2)−Φ(−1), mishandling the negative z | 
| E3 | explain | derivation | 1.3 | Derive the empirical rule's 68% figure — show P(−1<Z<1) = 2Φ(1)−1 for standard normal Z, using only symmetry (not a table lookup for the final numeric step, which you may state as ≈0.6827). | by symmetry, P(Z<−1)=1−Φ(1); so P(−1<Z<1)=Φ(1)−P(Z≤−1)=Φ(1)−(1−Φ(1))=2Φ(1)−1; using Φ(1)≈0.8413, this gives ≈0.6827 *(required: the symmetry step deriving P(Z<−1)=1−Φ(1), not just quoting 68%)* | quotes 2Φ(1)−1 as a known formula without deriving the P(Z<−1)=1−Φ(1) symmetry step → `normal-distribution` |
| E4 | explain | short-answer | 1.45 | Explain why the Normal density includes the constant 1/(σ√(2π)) in front of the exponential term. | this constant is exactly what makes ∫f(x)dx=1 — the "raw" exponential exp(−(x−μ)²/(2σ²)) integrates to σ√(2π) over all of ℝ, so dividing by that value is what normalizes the density to a valid probability distribution *(required)* | — |
| E5 | explain | short-answer | 1.55 | Explain, intuitively (not by full derivation), why an affine transformation aX+b of a Normal X remains Normal rather than producing some other shape. | the Normal density's defining feature is that its log is a quadratic function of x; substituting a linear function ax+b for x inside that quadratic still produces a quadratic in the new variable, which is exactly the algebraic signature of a (rescaled, shifted) Normal density — affine transforms preserve the "log-density is quadratic" structure *(required)* | — |
| T2 | transfer | short-answer | 1.8 | A manufacturing process produces bolts with length N(50mm, 0.5²mm). Specs require length between 49mm and 51mm. Find the fraction of bolts meeting spec, and explain what would happen to this fraction if σ were cut in half through better process control. `[verified: 0.9545 at σ=0.5]` | z=±2 at the spec limits; P(49<X<51)=Φ(2)−Φ(−2)≈0.9545; halving σ to 0.25 would push the spec limits out to ±4σ, driving the in-spec fraction extremely close to 1 — tighter process control (smaller σ) directly increases the fraction meeting spec for a fixed tolerance window | assumes reducing σ has no effect on the in-spec fraction since the spec window (49-51mm) itself doesn't change → `normal-distribution` |
| T3 | transfer | short-answer | 1.85 | Two students took different exams: Student A scored 82 on an exam with N(75, 8²), Student B scored 88 on an exam with N(80, 10²). Use z-scores to determine who performed better *relative to their own exam's distribution*. `[verified: zA=0.875, zB=0.8]` | zA=(82−75)/8=0.875; zB=(88−80)/10=0.8; Student A's z-score is slightly higher, so A performed better relative to their exam's distribution, despite B's higher raw score | compares the raw scores (88>82) directly, concluding B did better without standardizing for each exam's own mean and spread → `normal-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.70…1.85 (2.55 logits).*

---

## Uniform Distribution (`uniform-distribution`)
*Prereq: PDF, Expectation, Variance · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State the density, E[X], and Var(X) for Uniform(a,b). | f(x)=1/(b−a) on [a,b]; E[X]=(a+b)/2; Var(X)=(b−a)²/12 | — |
| R2 | recall | mcq | 0.1 | Uniform(a,b) is called the "maximum-entropy" distribution on [a,b] because: | it assumes nothing beyond the support itself — no other information is encoded | picks "it has the highest variance among all distributions on [a,b]" — actually false: a two-point distribution at the endpoints has variance (b−a)²/4, larger than Uniform's (b−a)²/12 → `uniform-distribution` |
| A1 | apply | numeric | 0.54 | X ~ Uniform(2,10). Find P(3<X<7), E[X], Var(X). `[verified: 0.5, 6, 5.333]` | P=4/8=0.5; E[X]=6; Var=64/12≈5.333 | — |
| A2 | apply | numeric | 0.7 | Buses run every 15 minutes with no other information; you arrive at a random time. Find your expected wait and P(wait < 5 min). `[verified: 7.5, 0.333]` | Uniform(0,15): E=7.5; P(X<5)=5/15=1/3 | — |
| E1 | explain | derivation | 1.24 | Derive Var(Uniform(a,b)) = (b−a)²/12 from E[X²] − (E[X])². | E[X]=(a+b)/2; E[X²]=∫ₐᵇ x²/(b−a) dx = (a²+ab+b²)/3; subtract (a+b)²/4 and simplify to (b−a)²/12 *(required: the full algebra, not the formula quoted from memory)* | — |
| E2 | explain | short-answer | 1.4 | Show, with a concrete counterexample on Uniform(0,10), that Uniform is *not* memoryless the way Exponential is. `[verified]` | P(X>7\|X>5) = (3/10)/(5/10) = 0.6, but P(X>2) = 0.8 — the two differ, so knowing you've survived past 5 *does* change the outlook (it doesn't here — it improves it, since the interval is bounded and running out) | claims Uniform is memoryless by analogy to Exponential without checking → `uniform-distribution` |
| T1 | transfer | short-answer | 1.74 | Give two real scenarios well-modeled as Uniform. Then: why *can't* "the wait for the next bus if buses arrive as a truly random (Poisson) process averaging one every 10 minutes" be modeled as Uniform(0,10), even though the average gap is 10 minutes? Hint: what does "no memory" mean for a Poisson process? | good fits: rounding error, a fair spinner's angle, a random-number generator's output. The Poisson wait time is **Exponential**, not Uniform: a Poisson process has no upper bound on the gap between events (Uniform(0,10) implicitly guarantees the bus arrives by minute 10, which a memoryless process does not promise), and its hazard is constant rather than rising toward a deadline the way E2 showed Uniform's effectively does | assumes "average gap of 10 minutes" is enough to justify Uniform(0,10) without checking the process's memory structure → `uniform-distribution` |
| R3 | recall | mcq | −0.7 | For a≤x≤b, the CDF of Uniform(a,b) is: | (x−a)/(b−a) | picks x/(b−a), forgetting to shift by a → `uniform-distribution` |
| R4 | recall | short-answer | −0.62 | Fill in the blank: the density of Uniform(a,b) is f(x) = ___ for a≤x≤b. | 1/(b−a) | — |
| R5 | recall | short-answer | −0.54 | True or false: Uniform(0,1) is called the "standard uniform" distribution. | true | — |
| R6 | recall | short-answer | −0.46 | Fill in the blank: E[X] for Uniform(a,b) = ___. | (a+b)/2 | — |
| R7 | recall | mcq | −0.38 | Uniform(a,b) is fully specified by: | its two endpoints, a and b | picks "its mean and variance" — the endpoints are the primitive parameters, mean/variance are derived → `uniform-distribution` |
| R8 | recall | short-answer | −0.3 | True or false: Var(X) = (b−a)²/12 for Uniform(a,b). | true | — |
| R9 | recall | short-answer | −0.22 | Define the support of Uniform(a,b). | the interval [a,b] | — |
| R10 | recall | mcq | −0.14 | The density of Uniform(a,b) is: | constant over [a,b], and zero elsewhere | picks "linearly increasing over [a,b]" → `uniform-distribution` |
| R11 | recall | short-answer | −0.06 | True or false: the CDF of Uniform(a,b) is a straight line between a and b. | true | — |
| R12 | recall | mcq | 0.02 | As (b−a) increases (the interval widens), the density's constant height: | decreases | picks "increases," backwards → `uniform-distribution` |
| R13 | recall | short-answer | 0.1 | Fill in the blank: F(a)=0 and F(b) = ___. | 1 | — |
| R14 | recall | short-answer | 0.18 | True or false: Uniform(a,b) is a continuous distribution. | true | — |
| R15 | recall | mcq | 0.26 | A common real source of (approximately) standard uniform values is: | a random-number generator's raw [0,1) output | picks "a fair six-sided die roll" — that's discrete uniform, a different distribution → `uniform-distribution` |
| R16 | recall | short-answer | 0.34 | True or false: for Uniform(a,b), any two sub-intervals of [a,b] with the same length have the same probability. | true — this "equal probability for equal length" property is the defining feature | — |
| A3 | apply | numeric | 0.7 | X ~ Uniform(0,1). Find P(X>0.7). `[verified: 0.3]` | 1−0.7 = 0.3 | computes P(X>0.7) as 0.7 itself, reading the threshold as the answer → `uniform-distribution` |
| A4 | apply | numeric | 0.76 | X ~ Uniform(5,15). Find F(8), the CDF at x=8. `[verified: 0.3]` | (8−5)/(15−5) = 0.3 | computes 8/15 instead of shifting by the lower endpoint 5 first → `uniform-distribution` |
| A5 | apply | numeric | 0.82 | X ~ Uniform(−3,3). Find Var(X). `[verified: 3]` | (3−(−3))²/12 = 36/12 = 3 | computes (3-3)²/12=0, mishandling the negative lower endpoint → `uniform-distribution` |
| A6 | apply | numeric | 0.88 | X ~ Uniform(0,20). Find P(5<X<12). `[verified: 0.35]` | (12−5)/20 = 0.35 | computes 12/20−5/20 correctly in principle but instead computes (12−5)/(20−0)² by squaring the range → `uniform-distribution` |
| E3 | explain | derivation | 1.3 | Derive the CDF F(x)=(x−a)/(b−a) for a≤x≤b by integrating the density. | F(x)=∫ₐˣ 1/(b−a) dt = (x−a)/(b−a), a direct integral of a constant *(required: the explicit integral, not just quoting the linear formula)* | states F(x) is linear "by inspection" without performing the integral | 
| E4 | explain | short-answer | 1.45 | Every individual point in [a,b] has probability exactly 0, yet we say "every value is equally likely." Explain how these two facts are consistent. | "equally likely" for a continuous distribution is properly a statement about the *density* being constant, not about individual point probabilities (which are always 0 for any continuous distribution); what it really means operationally is that any two equal-length sub-intervals have equal probability — the density's constancy is what licenses calling the distribution "uniform" *(required)* | — |
| E5 | explain | short-answer | 1.55 | Beyond "it has the highest variance" (which R2 of the base concept shows is actually false), explain the sense in which Uniform(a,b) is the "least informative" or "maximum entropy" distribution on [a,b]. | among all distributions supported on [a,b], Uniform is the one that encodes no additional structure or preference for any sub-region — it assumes only the boundaries themselves and nothing else, which is exactly the formal content of "maximum entropy given only a bounded support" *(required)* | — |
| T2 | transfer | short-answer | 1.65 | A Monte Carlo simulation needs to generate a random angle for a spinner, uniformly distributed on [0°, 360°). Explain how you'd use a standard Uniform(0,1) random number generator to produce this, and verify the resulting distribution is correct. | multiply the Uniform(0,1) output by 360: if U~Uniform(0,1), then 360U~Uniform(0,360), since an affine transform of a uniform variable is uniform over the correspondingly transformed interval — this is the standard "rescale a standard uniform" trick used throughout simulation | assumes the standard uniform output can be used directly as the angle without rescaling to [0,360) → `uniform-distribution` |
| T3 | transfer | short-answer | 1.7 | A digital scale reports weight rounded to the nearest 0.1g. The true rounding error (true weight minus reported weight) is modeled as Uniform(−0.05, 0.05). Explain why this is a reasonable model, and find the probability the true error exceeds 0.02g in magnitude. `[verified: 0.6]` | reasonable because rounding to the nearest 0.1g means the true value could be anywhere within ±0.05g of the reported value with no particular sub-region favored; P(\|error\|>0.02) = 1 − P(−0.02<error<0.02) = 1 − (0.04/0.1) = 0.6 | computes P(\|error\|>0.02) as just P(error>0.02)=0.3, forgetting to also include the symmetric negative tail → `uniform-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.70…1.70 (2.4 logits). E2/T1 deliberately set up the Exponential section's defining property by contrast.*

---

## Exponential Distribution (`exponential-distribution`)
*Prereq: Poisson Distribution · ancestors 17 · b₀ = 0.95*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.05 | State the density, E[X], and Var(X) for Exponential(λ). | f(x)=λe⁻ᵠˣ, x≥0; E[X]=1/λ; Var(X)=1/λ² | — |
| R2 | recall | mcq | 0.3 | How does Exponential(λ) relate to Poisson(λ)? | Exponential models the *waiting time* between Poisson events; Poisson models the *count* of events in a fixed interval | claims they're "the same distribution" in different notation → `exponential-distribution` |
| A1 | apply | numeric | 0.75 | A bulb's lifetime is Exponential with mean 1000 hours. Find P(lasts > 1500 hours). `[verified: 0.2231]` | λ=1/1000; P(X>1500)=e⁻¹·⁵ ≈ 0.2231 | — |
| A2 | apply | numeric | 0.9 | Same bulb: find P(lasts > 2500 hours \| already lasted 1000 hours), using memorylessness, and compare to A1. `[verified: equal, 0.2231]` | by memorylessness this equals P(X>1500) from A1 exactly — the bulb "forgets" it has already survived 1000 hours | computes P(X>2500) unconditionally (e⁻²·⁵) instead of applying memorylessness → `exponential-distribution` |
| E1 | explain | derivation | 1.45 | Prove memorylessness: P(X>s+t \| X>s) = P(X>t). | P(X>x)=e⁻ᵠˣ; ratio e⁻ᵠ⁽ˢ⁺ᵗ⁾/e⁻ᵠˢ = e⁻ᵠᵗ = P(X>t) *(required, full chain)* | — |
| E2 | explain | short-answer | 1.7 | Exponential is the *unique* continuous memoryless distribution, the way Geometric is the unique discrete one. Explain the connection: how is Exponential a continuous-time limit of Geometric? | chop time into tiny intervals, treat each as a Bernoulli trial with a tiny success probability; the waiting time (in units of intervals) is Geometric, and letting the interval width shrink to 0 while the rate stays fixed produces Exponential in the limit *(required)* — the same discretize-and-take-a-limit move as the Poisson-from-Binomial derivation earlier in this bank | — |
| T1 | transfer | short-answer | 1.95 | Give two real waiting times well-modeled by Exponential. Then: *why can't* the time until a car engine fails be modeled as Exponential, even though "time until failure" sounds like exactly the right setup? Hint: think about wear and aging. | good fits: gaps between radioactive decays; gaps between independent website hits. Engine failure: mechanical wear increases the *hazard rate* with age — a 10-year-old engine is more likely to fail soon than a new one, which directly violates memorylessness (Exponential "forgets" its age entirely); the right family has an *increasing* hazard rate over time, unlike Exponential's constant one | attributes the failure to "engines don't fail randomly" rather than naming the specific violated property (constant hazard / memorylessness) → `exponential-distribution` |
| R3 | recall | mcq | −0.4 | The CDF of Exponential(λ) is: | F(x) = 1 − e⁻ᵠˣ, x≥0 | picks F(x)=e⁻ᵠˣ, mistaking the survival function for the CDF → `exponential-distribution` |
| R4 | recall | short-answer | −0.32 | Fill in the blank: the density of Exponential(λ) is f(x) = ___. | λe⁻ᵠˣ, x≥0 | — |
| R5 | recall | short-answer | −0.24 | True or false: Var(X) = 1/λ² for Exponential(λ). | true | — |
| R6 | recall | short-answer | −0.16 | Fill in the blank: E[X] = ___ for Exponential(λ). | 1/λ | — |
| R7 | recall | mcq | −0.08 | The support of Exponential(λ) is: | x ≥ 0 | picks all of ℝ, forgetting waiting times can't be negative → `exponential-distribution` |
| R8 | recall | short-answer | 0.0 | True or false: the hazard rate of Exponential(λ) is constant, equal to λ, at every point in time. | true | — |
| R9 | recall | short-answer | 0.08 | Fill in the blank: the median of Exponential(λ) is ___. | ln(2)/λ | — |
| R10 | recall | mcq | 0.16 | Exponential(λ) is fully determined by: | the single rate parameter λ | picks "λ and the mean, as two separate parameters" — the mean is 1/λ, not free → `exponential-distribution` |
| R11 | recall | short-answer | 0.24 | True or false: Exponential is the unique continuous memoryless distribution. | true | — |
| R12 | recall | mcq | 0.32 | As λ increases, E[X] = 1/λ: | decreases | picks "increases," backwards → `exponential-distribution` |
| R13 | recall | short-answer | 0.4 | Fill in the blank: P(X>x) = ___. | e⁻ᵠˣ | — |
| R14 | recall | short-answer | 0.48 | True or false: Exponential models the waiting time between consecutive events of a Poisson process. | true | — |
| R15 | recall | mcq | 0.56 | The parameter λ is best described as a: | rate — events (or occurrences) per unit time | picks "a probability," confusing λ with a bounded [0,1] quantity → `exponential-distribution` |
| R16 | recall | short-answer | 0.64 | Define "hazard rate" in one line. | the instantaneous rate at which the event occurs, given it hasn't occurred yet | — |
| A3 | apply | numeric | 0.8 | A light bulb's lifetime is Exponential with mean 500 hours. Find P(the bulb fails before 300 hours). `[verified: 0.4512]` | λ=1/500; P(X<300)=1−e^(−300/500)=1−e^(−0.6)≈0.4512 | computes 1−e^(−500/300), inverting which value is the rate and which is the time → `exponential-distribution` |
| A4 | apply | numeric | 0.9 | X ~ Exponential(λ=0.01). Find the median of X. `[verified: ≈69.31]` | ln(2)/0.01 ≈ 69.31 | computes 1/(2×0.01)=50, using the mean-halving intuition instead of the actual median formula → `exponential-distribution` |
| A5 | apply | numeric | 1.0 | Calls arrive at a helpdesk at a Poisson rate of 2/minute, so the gap between calls is Exponential(2). Find P(the wait for the next call is less than 30 seconds = 0.5 minutes). `[verified: 0.6321]` | 1−e^(−2×0.5) = 1−e⁻¹ ≈ 0.6321 | plugs in 30 (seconds) directly instead of converting to 0.5 minutes to match the rate's time unit → `exponential-distribution` |
| A6 | apply | numeric | 1.1 | X ~ Exponential with mean 4. Find Var(X). `[verified: 16]` | λ=1/4, so Var(X)=1/λ²=16 | computes Var(X)=1/λ=4, forgetting to square λ in the denominator → `exponential-distribution` |
| E3 | explain | derivation | 1.55 | Derive the CDF F(x)=1−e⁻ᵠˣ by integrating the density. | F(x)=∫₀ˣ λe⁻ᵠᵗ dt = [−e⁻ᵠᵗ]₀ˣ = −e⁻ᵠˣ−(−1) = 1−e⁻ᵠˣ *(required: the explicit antiderivative evaluated at both limits)* | states the CDF from memory without performing the integral | 
| E4 | explain | short-answer | 1.7 | Derive the median formula ln(2)/λ by solving F(m)=0.5 directly. | 1−e⁻ᵠᵐ=0.5 ⟹ e⁻ᵠᵐ=0.5 ⟹ −λm=ln(0.5) ⟹ m=−ln(0.5)/λ=ln(2)/λ *(required: the algebra shown, including the sign flip from ln(0.5)=−ln(2))* | — |
| E5 | explain | short-answer | 1.85 | Explain why "constant hazard rate" and "memorylessness" are really the same statement about Exponential, phrased two ways. | a constant hazard rate λ means the instantaneous chance of the event happening *right now*, given it hasn't happened yet, never depends on how long you've already waited — which is exactly what memorylessness says (P(X>s+t\|X>s)=P(X>t)); a hazard rate that *rose* with age would mean the event becomes more likely to happen soon the longer you've waited, breaking memorylessness *(required: connects the two framings explicitly)* | — |
| T2 | transfer | short-answer | 2.0 | A server's uptime between crashes is modeled as Exponential with mean 200 hours. An engineer proposes scheduling preventive maintenance every 100 hours, reasoning "since we're halfway to the average crash time, risk is building up." Evaluate this reasoning using memorylessness. | wrong, by memorylessness: having survived 100 hours crash-free says nothing about how much longer the server will last — the expected *remaining* uptime is still 200 hours, not 100; "risk building up" is exactly the kind of intuition memorylessness rules out for a true Exponential process | agrees that surviving 100 hours means the server is now "due" for a crash sooner than a fresh server would be → `exponential-distribution` |
| T3 | transfer | short-answer | 2.05 | Contrast Exponential(λ) and Uniform(0,b) as models for "time until an event": both are supported on non-negative values, but only one is memoryless. Explain, in terms of hazard rate, why Uniform *can't* be memoryless the way Exponential is. | Uniform(0,b) has a hazard rate that *increases* as x approaches b (the remaining probability mass shrinks toward a hard deadline, per `uniform-distribution`'s E2), while Exponential's hazard rate is flat forever — Uniform's built-in upper bound b is precisely what forces its hazard to rise, which a truly memoryless distribution's hazard cannot do | assumes any distribution on non-negative values could in principle be memoryless if its mean matches, without connecting the bounded support to a necessarily rising hazard → `uniform-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.40…2.05 (2.45 logits).*

---

## Gamma Distribution (`gamma-distribution`)
*Prereq: Exponential Distribution · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | State E[X], Var(X) for Gamma(α, β), and identify which parameter is "shape" and which is "rate." | E[X]=α/β; Var(X)=α/β²; α = shape, β = rate | — |
| R2 | recall | mcq | 0.32 | Gamma(1, β) is exactly: | Exponential(β) | picks Normal or Uniform, not recognizing Exponential as Gamma's α=1 special case → `gamma-distribution` |
| A1 | apply | numeric | 0.77 | Customers arrive as Poisson(3/hour). Find E[time to the 5th customer] and its variance, via Gamma(5,3). `[verified: 1.667, 0.556]` | E[X]=5/3≈1.667 hours; Var=5/9≈0.556 | — |
| A2 | apply | numeric | 0.9 | Same setup: find P(the 5th customer arrives within 1 hour) — *without* the Gamma pdf, using the count/waiting-time duality instead. `[verified: 0.1847]` | "5th arrival before time 1" ⟺ "at least 5 arrivals by time 1"; N(1)~Poisson(3), so P = P(N(1)≥5) = 1−P(N(1)≤4) ≈ 0.1847 | tries to integrate the Gamma density directly and mishandles Γ(5) → `gamma-distribution` |
| E1 | explain | derivation | 1.47 | Show the sum of α iid Exponential(β) waiting times is Gamma(α,β), using the waiting-time interpretation directly (no MGFs). | the gap to the 1st Poisson event, plus the (memoryless, hence fresh) gap from the 1st to the 2nd, plus … α such gaps, sum to the total wait for the α-th event; each gap is Exponential(β) by `exponential-distribution`'s memorylessness *(required — explicitly invokes memorylessness as the reason each gap restarts fresh)* — the same structure as `negative-binomial-distribution`'s sum-of-geometrics proof | — |
| E2 | explain | short-answer | 1.6 | Explain the *Erlang–Poisson duality* used in A2: why does "waiting time to the α-th event exceeds t" have exactly the same probability as "fewer than α events have occurred by time t"? | these describe the identical event two ways: the α-th event hasn't happened by time t *if and only if* strictly fewer than α events have occurred by then *(required: an "if and only if" argument, not just restating both sides)* | — |
| T1 | transfer | short-answer | 1.97 | Give a real process better modeled by Gamma than Exponential (hint: "time until the *k*-th something," k>1). Then use the duality from E2 to find P(a store's 10th customer of the day arrives within the first hour, given customers arrive as Poisson(8/hour)) — without writing down the Gamma density or Γ(10). | good examples: time until a machine's 3rd failure needing replacement; time until a store's 10th customer. Computation: P(T₁₀<1) = P(N(1)≥10), N(1)~Poisson(8) — computed as a Poisson tail sum, demonstrating the practical payoff of never needing Γ(10) for an integer-shape Gamma | tries to evaluate the Gamma CDF directly with the Gamma function → `gamma-distribution` |
| R3 | recall | mcq | −0.38 | The support of Gamma(α,β) is: | x > 0 | picks all of ℝ, forgetting Gamma models a positive quantity (a sum of waiting times) → `gamma-distribution` |
| R4 | recall | short-answer | −0.3 | Fill in the blank: E[X] = ___ for Gamma(α,β). | α/β | — |
| R5 | recall | short-answer | −0.22 | True or false: Var(X) = α/β² for Gamma(α,β). | true | — |
| R6 | recall | short-answer | −0.14 | State the density formula for Gamma(α,β). | f(x) = βᵅxᵅ⁻¹e⁻ᵝˣ/Γ(α), x>0 | — |
| R7 | recall | mcq | −0.06 | In Gamma(α,β), the parameter α is called the: | shape parameter | picks "the rate parameter" — that's β → `gamma-distribution` |
| R8 | recall | short-answer | 0.02 | True or false: Gamma(1,β) equals Exponential(β). | true | — |
| R9 | recall | short-answer | 0.1 | Fill in the blank: the sum of n iid Exponential(β) waiting times is Gamma(___, β). | n | — |
| R10 | recall | mcq | 0.18 | In Gamma(α,β), the parameter β is called the: | rate parameter | picks "the shape parameter" → `gamma-distribution` |
| R11 | recall | short-answer | 0.26 | True or false: Gamma distribution requires α to be a positive integer. | false — α can be any positive real number, not just an integer | — |
| R12 | recall | mcq | 0.34 | For a positive integer n, Γ(n) equals: | (n−1)! | picks n!, off by one in the factorial shift → `gamma-distribution` |
| R13 | recall | short-answer | 0.42 | Fill in the blank: the Gamma function recursion is Γ(α+1) = ___ · Γ(α). | α | — |
| R14 | recall | short-answer | 0.5 | True or false: holding β fixed, increasing α increases E[X] = α/β. | true | — |
| R15 | recall | mcq | 0.58 | The square of a standard normal, Z², is distributed as: | Gamma(1/2, 1/2), which is also called χ²₁ | picks Gamma(1,1) → `gamma-distribution` |
| R16 | recall | short-answer | 0.66 | In one line, what does the shape parameter α control about the density's form? | how many "stages" of waiting are being summed — it controls the skewness and peak location of the curve, distinguishing e.g. a sharply decreasing shape (α=1) from a more bell-like shape (large α) | — |
| A3 | apply | numeric | 1.02 | X ~ Gamma(3, 2). Find E[X] and Var(X). `[verified: E=1.5, Var=0.75]` | E[X]=3/2=1.5; Var(X)=3/4=0.75 | computes Var(X)=α/β=1.5, forgetting to square β → `gamma-distribution` |
| A4 | apply | numeric | 1.1 | The sum of 4 iid Exponential(0.5) waiting times is Gamma(4, 0.5). Find its mean and variance. `[verified: E=8, Var=16]` | E[X]=4/0.5=8; Var(X)=4/0.25=16 | uses the Exponential formulas (1/β, 1/β²) directly without multiplying by the shape parameter 4 → `gamma-distribution` |
| A5 | apply | numeric | 1.2 | Using the Poisson-count duality (not the Gamma density), find P(X≤3) for X ~ Gamma(2,1). `[verified: 0.8009]` | "2nd event by time 3" ⟺ "at least 2 events by time 3"; N(3)~Poisson(3); P(N≥2)=1−P(0)−P(1)=1−e⁻³(1+3)≈0.8009 | computes P(N(3)≥2) as 1−P(N=2), confusing "at least 2" with "not exactly 2" → `gamma-distribution` |
| A6 | apply | numeric | 1.3 | Using Gamma(1,3) = Exponential(3), find P(X>1) two ways: via the Exponential tail formula and by recognizing the Gamma(1,β) special case. `[verified: 0.0498]` | e⁻³ˣ¹⁽¹⁾ = e⁻³ ≈ 0.0498, matching Exponential(3)'s P(X>1)=e⁻³ exactly | treats Gamma(1,3) as needing the full Gamma tail machinery instead of recognizing it collapses to the simple Exponential formula → `gamma-distribution` |
| E3 | explain | derivation | 1.6 | Derive Var(X)=α/β² for Gamma(α,β) using the sum-of-Exponentials structure from E1, rather than integrating the Gamma density directly. | X = W₁+⋯+Wα where each Wᵢ~Exponential(β) is independent (by memorylessness, per E1); Var(Wᵢ)=1/β² for each; independence of the gaps gives Var(X)=ΣVar(Wᵢ)=α/β² directly, with no Gamma-density integral needed *(required: explicitly reuses the sum-of-exponentials structure and independence)* | integrates the Gamma density directly for Var(X) instead of using the much shorter sum-of-independent-exponentials route this concept sets up | 
| E4 | explain | short-answer | 1.75 | Explain why the Gamma function recursion Γ(α+1)=αΓ(α) is the natural generalization of the factorial recursion n!=n·(n−1)!. | for a positive integer n, Γ(n)=(n−1)!, so Γ(n+1)=nΓ(n) becomes n!=n·(n−1)! exactly — the Gamma function extends factorial to non-integer arguments while preserving the same multiplicative recursion *(required)* | — |
| E5 | explain | short-answer | 1.9 | Explain why allowing α to be any positive real (not just an integer) genuinely extends what Gamma can model, beyond "waiting for the α-th Poisson event." | with non-integer α, Gamma can model continuous accumulation processes that don't correspond to counting a whole number of discrete events — e.g. total rainfall accumulated (a continuously accruing quantity) rather than "time until the 5th raindrop," letting the shape parameter be fit to real data without being artificially rounded to an integer *(required)* | assumes Gamma is only meaningful for integer α since the Poisson-duality interpretation requires whole event counts → `gamma-distribution` |
| T2 | transfer | short-answer | 2.05 | An insurer models total claims from a group of 5 independent policyholders, where each policyholder's individual claim severity (given a claim occurs) is Exponential(β=0.001). If exactly 5 claims occur, explain why the total claim amount is Gamma(5, 0.001), and find its expected value. `[verified: 5000]` | the total is a sum of 5 iid Exponential(0.001) severities, which by the sum-of-exponentials fact (E1) is exactly Gamma(5,0.001); E[total]=5/0.001=5000 | treats the total as Exponential(0.001) itself (using a single severity's distribution for the whole sum) rather than recognizing the sum is Gamma → `gamma-distribution` |
| T3 | transfer | short-answer | 2.1 | In Bayesian statistics, Gamma distributions are commonly used as prior beliefs about an unknown Poisson rate λ. Explain, without doing any calculation, why a distribution supported only on (0,∞) is a sensible choice for this purpose, connecting back to what a Poisson rate parameter must satisfy. | a Poisson rate λ must be strictly positive (a negative or zero "average rate" makes no sense); Gamma's support is exactly (0,∞), matching this constraint precisely, unlike e.g. Normal which would assign positive prior probability to impossible negative rates — the same support-matching argument `beta-distribution` makes for modeling a probability on [0,1] | picks a Gamma prior "because it's a well-known distribution" without connecting its support to the actual constraint λ>0 must satisfy → `gamma-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.38…2.10 (2.48 logits).*

---

## Beta Distribution (`beta-distribution`)
*Prereq: Gamma Distribution · ancestors 19 · b₀ = 1.00*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.0 | What kind of quantity does Beta(α,β) model, and what does Beta(1,1) reduce to? | a random *probability or proportion* on [0,1]; Beta(1,1) = Uniform(0,1) | — |
| R2 | recall | mcq | 0.3 | Beta(1,1) equals: | Uniform(0,1) | picks "a point mass at 0.5," confusing "flat/uninformative" with "concentrated at the center" → `beta-distribution` |
| A1 | apply | numeric | 0.8 | X ~ Beta(2,3). Find E[X] and Var(X). `[verified: 0.4, 0.04]` | E[X]=2/5=0.4; Var(X)=2·3/(5²·6)=0.04 | — |
| A2 | apply | short-answer | 0.95 | If α and β both grow large while α/(α+β) stays fixed, what happens to the shape of Beta(α,β)? Hint: think about the variance formula. | Var = αβ/[(α+β)²(α+β+1)] shrinks toward 0 as α,β→∞ with their ratio fixed, since the denominator grows faster (order (α+β)³) than the numerator (order (α+β)²); the distribution concentrates sharply around its mean *(required: the growth-rate comparison, not just "variance goes down")* | — |
| E1 | explain | derivation | 1.5 | Verify E[X] = α/(α+β) using ∫x^α(1−x)^(β−1)dx = B(α+1,β) and the Gamma recursion Γ(n+1)=nΓ(n). | E[X] = B(α+1,β)/B(α,β); expand each Beta function via Gammas, use Γ(α+1)=αΓ(α) to cancel, leaving α/(α+β) *(required: the recursion step named explicitly)* | — |
| E2 | explain | short-answer | 1.65 | Why is Beta a natural choice specifically for modeling "a random probability," in a way Gamma or Exponential aren't? | Beta's support is exactly [0,1], matching the constraint that a probability must lie in that range; Gamma/Exponential have unbounded support on (0,∞) and would assign probability to nonsensical values like p=1.3 *(required)*; Beta is also flexible enough to be flat, U-shaped, or peaked depending on α,β | — |
| T1 | transfer | short-answer | 2.0 | Before seeing any A/B test data, you believe a website's conversion rate p is "probably around 5%, but I'm not very sure." Without doing any calculation, explain why Beta is a far more natural choice than Normal to represent this belief about p. | p is a probability, constrained to [0,1]; Normal has support on all of ℝ and would assign nonzero probability to impossible values like p=−0.1 or p=1.3; Beta's support matches the constraint exactly, and its shape can be tuned (via α,β) to peak near 0.05 with an appropriate spread *(required: names the support mismatch specifically, not just "Beta is designed for this")* | picks Normal because "the sample size will be large so CLT applies" — true of the *estimator's* sampling distribution later on, but not a reason to use Normal for a *prior belief about p itself* → `beta-distribution` |
| R3 | recall | mcq | −0.4 | For α,β>1, the mode of Beta(α,β) is: | (α−1)/(α+β−2) | picks α/(α+β), which is the *mean*, not the mode → `beta-distribution` |
| R4 | recall | short-answer | −0.32 | Fill in the blank: E[X] = ___ for Beta(α,β). | α/(α+β) | — |
| R5 | recall | short-answer | −0.24 | True or false: Var(X) for Beta(α,β) has (α+β)²(α+β+1) in its denominator. | true | — |
| R6 | recall | short-answer | −0.16 | State the density formula for Beta(α,β). | f(x) = xᵅ⁻¹(1−x)ᵝ⁻¹/B(α,β), 0≤x≤1 | — |
| R7 | recall | mcq | −0.08 | Beta distributions are most commonly used as a prior for: | an unknown Bernoulli/Binomial success probability p | picks "an unknown Poisson rate λ" — that's Gamma's role, per `gamma-distribution`'s T3 → `beta-distribution` |
| R8 | recall | short-answer | 0.0 | True or false: the support of Beta(α,β) is [0,1]. | true | — |
| R9 | recall | short-answer | 0.08 | What is the normalizing constant in the Beta density formula called? | the Beta function, B(α,β) | — |
| R10 | recall | mcq | 0.16 | Beta(α,β) is symmetric about 0.5 exactly when: | α = β | picks α+β=1 → `beta-distribution` |
| R11 | recall | short-answer | 0.24 | True or false: Beta(2,2) is symmetric about 0.5. | true — since α=β=2 | — |
| R12 | recall | mcq | 0.32 | Beta(α,β) has a U-shaped (bimodal at the endpoints) density when: | both α<1 and β<1 | picks α=β=1, which is actually flat (Uniform), not U-shaped → `beta-distribution` |
| R13 | recall | short-answer | 0.4 | Fill in the blank: as α=β both increase together, the distribution becomes more ___ around 0.5. | concentrated (peaked) | — |
| R14 | recall | short-answer | 0.48 | True or false: the parameters α, β of a Beta distribution must be positive integers. | false — they can be any positive real numbers | — |
| R15 | recall | mcq | 0.56 | Beta(α,β) can be constructed from two independent Gamma variables X~Gamma(α,·), Y~Gamma(β,·) via: | X/(X+Y) | picks X−Y → `beta-distribution` |
| R16 | recall | short-answer | 0.64 | In one line, what makes Beta a natural "conjugate prior" for a Bernoulli/Binomial likelihood? | combining a Beta prior with Bernoulli/Binomial data via Bayes' rule produces a posterior that is again Beta, just with updated parameters — the family is closed under this particular Bayesian update | — |
| A3 | apply | numeric | 1.0 | X ~ Beta(3,2). Find E[X] and Var(X). `[verified: 0.6, 0.04]` | E[X]=3/5=0.6; Var(X)=3·2/(5²·6)=6/150=0.04 | swaps the roles of α and β, computing E[X]=2/5=0.4 instead → `beta-distribution` |
| A4 | apply | numeric | 1.05 | X ~ Beta(5,5). Find E[X] and, using symmetry, the mode. `[verified: E=0.5, mode=0.5]` | E[X]=5/10=0.5; by symmetry (α=β), the mode is also 0.5 | computes the mode using the formula (α−1)/(α+β−2)=4/8=0.5 but then second-guesses it should differ from the mean, missing that symmetry forces them equal here → `beta-distribution` |
| A5 | apply | numeric | 1.1 | X ~ Beta(2,5). Find the mode using (α−1)/(α+β−2). `[verified: 0.2]` | (2−1)/(2+5−2) = 1/5 = 0.2 | computes (2−1)/(2+5) = 1/7, forgetting to subtract 2 from the denominator → `beta-distribution` |
| A6 | apply | short-answer | 1.15 | Describe qualitatively the shape of Beta(0.5, 0.5) (the "arcsine distribution"), and explain why it looks so different from Beta(5,5). | U-shaped: density is high near both 0 and 1 and dips to a minimum at 0.5 — with α,β<1, mass concentrates at the extremes rather than the center, the opposite of Beta(5,5)'s sharp single peak at 0.5 | assumes all Beta distributions with α=β must look the same bell shape regardless of whether the shared value is above or below 1 → `beta-distribution` |
| E3 | explain | derivation | 1.55 | Derive the mode formula (α−1)/(α+β−2) for α,β>1, by maximizing the log of the density's x-dependent part (ignore the normalizing constant B(α,β)). | let g(x)=(α−1)ln(x)+(β−1)ln(1−x); g′(x)=(α−1)/x−(β−1)/(1−x); setting g′(x)=0 and solving gives (α−1)(1−x)=(β−1)x, so x(α−1+β−1)=α−1, i.e. x=(α−1)/(α+β−2) *(required: the derivative set to zero and solved explicitly)* | quotes the mode formula without performing the derivative-based optimization → `beta-distribution` |
| E4 | explain | short-answer | 1.7 | Show directly from the density formula that Beta(1,1) reduces to Uniform(0,1), rather than just citing the fact. | plugging α=1, β=1 into f(x)=xᵅ⁻¹(1−x)ᵝ⁻¹/B(α,β) gives f(x)=x⁰(1−x)⁰/B(1,1)=1/B(1,1); since B(1,1)=1, f(x)=1 for all x∈[0,1] — a constant density on [0,1], which is exactly Uniform(0,1)'s density *(required: the substitution shown, including B(1,1)=1)* | asserts Beta(1,1)=Uniform(0,1) as a known special case without substituting into the density formula → `beta-distribution` |
| E5 | explain | short-answer | 1.85 | Explain intuitively why Beta(α,β) with both α<1 and β<1 concentrates mass near the endpoints 0 and 1 rather than the middle. | when α<1, the xᵅ⁻¹ factor blows up (→∞) as x→0⁺ rather than vanishing, pulling density mass toward 0; symmetrically, β<1 makes (1−x)ᵝ⁻¹ blow up as x→1⁻; both factors pulling toward their respective endpoints simultaneously produces the U-shape, the opposite of what happens when α,β>1 (where both factors vanish at the endpoints, pulling mass toward the middle) *(required: the blow-up-at-the-endpoint mechanism for both factors)* | — |
| T2 | transfer | short-answer | 2.1 | Two analysts start an A/B test with different prior beliefs about a button's click rate: Analyst 1 uses Beta(2,2) (a weak, symmetric prior around 0.5), Analyst 2 uses Beta(20,20) (a strong prior, also centered at 0.5 but much more concentrated). After the same click data comes in, whose posterior will move further from their prior, and why? | Analyst 1's posterior will move further — a weaker prior (smaller α,β, meaning fewer "pseudo-observations" it implicitly represents) is more easily overwhelmed by new data, while Analyst 2's strong, concentrated prior requires much more data to shift meaningfully, since it behaves like having already observed many pseudo-trials | assumes both posteriors move by the same amount since both priors are centered at the same value (0.5) → `beta-distribution` |
| T3 | transfer | short-answer | 2.15 | A factory engineer believes a machine's defect rate is "somewhere around 2%, definitely below 10%, rarely near 0%." Explain how choosing α and β for a Beta prior lets the engineer encode this belief, without doing any numerical fitting. | choosing α small relative to β (e.g. α around 2, β around 100) shifts the mean α/(α+β) down near 0.02 while keeping most mass away from exactly 0 — the *ratio* of α to β controls the center (per the mean formula) and their *combined size* controls how concentrated the belief is, giving the engineer two independent dials to match both the rough location and the confidence of the belief | tries to encode the belief using only a single parameter, missing that Beta needs both α and β to separately control location and spread → `beta-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.40…2.15 (2.55 logits).*

---

## Chi Square Distribution (`chi-square-distribution`)
*Prereq: Normal Distribution, Gamma Distribution · ancestors 20 · b₀ = 1.02*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | Define χ²ₖ in terms of standard normals, and state E[X], Var(X). | Z₁²+⋯+Zₖ² for iid Zᵢ~N(0,1); E[X]=k; Var(X)=2k | — |
| R2 | recall | mcq | 0.32 | If Z₁,…,Zₖ are iid N(0,1), then Z₁²+⋯+Zₖ² has distribution: | χ²ₖ | picks N(0,k), forgetting squaring destroys normality and negativity → `chi-square-distribution` |
| A1 | apply | numeric | 0.82 | X ~ χ²₁₀. Find E[X], Var(X). `[verified: 10, 20]` | E[X]=10; Var(X)=20 | — |
| A2 | apply | short-answer | 0.95 | X~χ²₈, Y~χ²₅ independent. What is X+Y's distribution, and why (one line, from the definition)? | χ²₁₃ — X+Y is the sum of 8+5=13 iid squared standard normals pooled from the two independent groups | adds the distributions' means but guesses a non-chi-square family for the sum → `chi-square-distribution` |
| E1 | explain | derivation | 1.52 | Prove E[χ²ₖ] = k directly from the definition, using E[Zᵢ²] = Var(Zᵢ) + E[Zᵢ]² for each standard normal. | E[Zᵢ²] = 1 + 0 = 1 (a callback to `variance`'s shortcut formula, run in reverse); linearity gives E[ΣZᵢ²] = k·1 = k *(required: the Var+mean² step, not just "each term contributes 1")* | — |
| E2 | explain | short-answer | 1.65 | Explain intuitively why χ²ₖ becomes more symmetric and Normal-looking as k grows, without invoking the formal CLT. | it's a sum of k iid terms, and sums of many iid terms tend to look increasingly Gaussian (the general intuition behind CLT, used informally here); concretely, the relative spread SD/mean = √(2k)/k = √(2/k) shrinks as k grows, so the distribution's *shape* relative to its own scale becomes tighter and more symmetric *(required: the relative-spread computation, not just an appeal to "CLT")* | — |
| T1 | transfer | short-answer | 2.02 | A quality engineer wants to test whether a machine's part-diameter variance exceeds a spec, and proposes comparing her variance *estimate* to a Normal distribution. Explain why Chi-Square is the right family instead — connect it to how a variance estimate is actually built. | a sample variance is built from **squared** deviations from a mean; since those (approximately) standardized deviations behave like standard normals, a sum of their squares is exactly what defines χ² — this is precisely the mechanism that will later justify `sample-variance`'s sampling distribution | reaches for Normal because "most sampling distributions are approximately Normal," missing that variance estimates are built from *squared* quantities, which is what chi-square is specifically for → `chi-square-distribution` |
| R3 | recall | mcq | −0.38 | The support of χ²ₖ is: | x ≥ 0 | picks all of ℝ, forgetting a sum of squares can never be negative → `chi-square-distribution` |
| R4 | recall | short-answer | −0.3 | Fill in the blank: Var(X) = ___ for χ²ₖ. | 2k | — |
| R5 | recall | short-answer | −0.22 | True or false: for k≥2, the mode of χ²ₖ is k−2. | true | — |
| R6 | recall | short-answer | −0.14 | Fill in the blank: χ²ₖ is a special case of Gamma(___, ___). | k/2, 1/2 (shape k/2, rate 1/2) | — |
| R7 | recall | mcq | −0.06 | χ²ₖ's entire distribution is determined by: | the single parameter k, the degrees of freedom | picks "k and a separate scale parameter" — there's no free scale parameter beyond k itself → `chi-square-distribution` |
| R8 | recall | short-answer | 0.02 | True or false: χ²ₖ is right-skewed, especially for small k. | true | — |
| R9 | recall | short-answer | 0.1 | Fill in the blank: as k→∞, the shape of χ²ₖ approaches ___. | approximately Normal | — |
| R10 | recall | mcq | 0.18 | χ²ₖ can never take which kind of value? | negative values | picks "values greater than k" — large values above k are perfectly possible, just less likely → `chi-square-distribution` |
| R11 | recall | short-answer | 0.26 | True or false: χ²₁ is exactly the distribution of Z² for a standard normal Z. | true | — |
| R12 | recall | mcq | 0.34 | The degrees of freedom k in χ²ₖ physically represents: | the number of independent squared standard normals being summed | picks "the number of parameters being estimated," a downstream statistical-inference usage, not the primitive definition → `chi-square-distribution` |
| R13 | recall | short-answer | 0.42 | Fill in the blank: for k≥2, the mode of χ²ₖ = ___. | k−2 | — |
| R14 | recall | short-answer | 0.5 | True or false: Var(χ²ₖ) grows linearly with k. | true — Var=2k | — |
| R15 | recall | mcq | 0.58 | The standard notation for chi-square with k degrees of freedom is: | χ²ₖ | picks χₖ² (squaring the subscript position rather than the symbol) — cosmetic but reveals confusion about what's being squared → `chi-square-distribution` |
| R16 | recall | short-answer | 0.66 | Define "degrees of freedom" as used in χ²ₖ, in one line. | the number of independent standard normal terms being squared and summed to build X | — |
| A3 | apply | numeric | 1.02 | X ~ χ²₁₅. Find E[X] and Var(X). `[verified: 15, 30]` | E[X]=15; Var(X)=2(15)=30 | computes Var(X)=15² instead of 2×15 → `chi-square-distribution` |
| A4 | apply | numeric | 1.1 | X ~ χ²₆. Find the mode. `[verified: 4]` | mode = 6−2 = 4 | reports the mode as equal to the mean, 6, not accounting for the right-skew → `chi-square-distribution` |
| A5 | apply | short-answer | 1.2 | X~χ²₄, Y~χ²₉ independent. What is X+Y's distribution? | χ²₁₃ — sum of 4+9=13 independent squared standard normals | adds the two distributions' means (4+9=13) but reports the wrong resulting family, e.g. Normal → `chi-square-distribution` |
| A6 | apply | numeric | 1.3 | Find the relative spread SD/mean = √(2k)/k for χ²₅₀. `[verified: 0.2]` | √100/50 = 10/50 = 0.2 | computes √(2×50)=10 correctly but then divides by 2k=100 instead of k=50, halving the answer → `chi-square-distribution` |
| E3 | explain | derivation | 1.62 | Derive the mode formula k−2 (for k≥2) using χ²ₖ = Gamma(k/2, 1/2) and the fact that Gamma(α,β)'s mode (for α≥1) is (α−1)/β. | substitute α=k/2, β=1/2 into (α−1)/β: (k/2−1)/(1/2) = 2(k/2−1) = k−2 *(required: the explicit substitution into the Gamma mode formula, reusing `gamma-distribution`'s E3 result rather than re-deriving from scratch)* | re-derives the mode by directly optimizing the χ² density from scratch instead of reusing the Gamma mode formula the prerequisite already established | 
| E4 | explain | short-answer | 1.75 | Using mean=k and mode=k−2 (for k≥2), explain what this gap tells you about the shape of χ²ₖ, and why the gap becomes *relatively* less important as k grows. | mode < mean is exactly the signature of right-skew — the bulk of typical outcomes sits a bit below the mean, while a long right tail pulls the average up; the *absolute* gap (2) stays fixed as k grows, but *relative* to the scale of the distribution (mean k, SD √(2k)), the gap shrinks, so the distribution looks increasingly symmetric for large k, matching `chi-square-distribution`'s own E2 result *(required: connects mode<mean to skew, and explains why the gap becomes relatively small)* | — |
| E5 | explain | short-answer | 1.9 | Chi-square is usually defined only for positive integer degrees of freedom k, even though its Gamma(k/2, 1/2) form technically makes sense for any real shape parameter. Explain why the *definitional* restriction to integers matters here. | the defining construction — a sum of k independent *squared standard normal* terms — only makes literal sense when k is a whole number of terms; a "half-integer" or irrational k has no such direct construction as a sum of squares, even though the Gamma formula would still compute a valid density *(required: distinguishes the combinatorial/counting definition from the more permissive Gamma formula)* | assumes the Gamma(k/2,1/2) formula being well-defined for any real k means chi-square itself is meaningfully defined for non-integer degrees of freedom too → `chi-square-distribution` |
| T2 | transfer | short-answer | 2.1 | A goodness-of-fit test compares observed category counts to expected counts using a statistic that sums squared standardized differences, (observed−expected)²/expected, across several categories. Explain, at a high level, why this statistic's distribution should be approximately χ²ₖ for some k, without doing the actual test. | each term is (approximately) a squared, standardized deviation — similar in spirit to a squared standard normal — and summing several such roughly-independent terms across categories produces exactly the "sum of squared approximately-standard-normal quantities" structure that defines χ²ₖ, with k tied to the number of independent categories being compared | assumes any sum of squared differences is automatically chi-square distributed, without the standardization (dividing by expected/variance) that makes each term approximately a squared standard normal → `chi-square-distribution` |
| T3 | transfer | short-answer | 2.15 | A lab reports a variance estimate built from summing 20 squared standardized residuals, and states the result "should be around 20, give or take about 6.3." Verify this claim using χ²₂₀'s mean and standard deviation. `[verified: mean=20, SD=√40≈6.32]` | E[χ²₂₀]=20 and SD=√(2×20)=√40≈6.32, matching the lab's stated figures exactly — the "give or take" language is a plain-English restatement of one standard deviation around the mean | computes the SD as 2×20=40 directly (confusing variance with standard deviation), reporting "give or take 40" as the check → `chi-square-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.38…2.15 (2.53 logits).*

---

## Student's t-Distribution (`t-distribution`)
*Prereq: Chi Square Distribution · ancestors 21 · b₀ = 1.05*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.05 | Define Tₖ in terms of a standard normal Z and an independent χ²ₖ variable V, and state what happens as k→∞. | Tₖ = Z/√(V/k); as k→∞, Tₖ → N(0,1) | — |
| R2 | recall | mcq | 0.35 | Compared to Normal(0,1), t with small degrees of freedom has: | heavier tails | picks "lighter tails," backwards → `t-distribution` |
| A1 | apply | short-answer | 0.85 | Without computing an exact value, explain why P(\|T₅\| > 2) is *larger* than P(\|Z\| > 2) for standard normal Z. | t has heavier tails than Normal at every fixed df, so it assigns strictly more probability to extreme values beyond any fixed threshold *(required)* | assumes t and Normal agree everywhere except very close to the tails' extreme ends → `t-distribution` |
| A2 | apply | numeric | 1.0 | Find Var(T₄) using Var(Tₖ)=k/(k−2). What happens to this formula as k→2⁺, and what does that mean? `[verified: 2; blows up]` | Var(T₄)=4/2=2 (already larger than Normal's variance of 1); as k→2⁺ the formula diverges, and for k≤2 the variance is infinite/undefined — an extreme version of "heavier tails" | assumes Var(Tₖ) is always close to 1 like the standard normal, missing that it can be undefined for small k → `t-distribution` |
| E1 | explain | short-answer | 1.55 | Show Var(Tₖ)=k/(k−2) is always > 1 for finite k>2, and explain why it approaches 1 as k→∞. | k/(k−2) > 1 whenever k>2 since k−2<k; as k→∞, V/k → 1 (an informal law-of-large-numbers intuition — the chi-square average concentrates near its mean of 1), so Tₖ ≈ Z/1 = Z, recovering variance 1 *(required: both halves)* | — |
| E2 | explain | short-answer | 1.7 | Explain the mechanism behind t's heavier tails, using Tₖ = Z/√(V/k) directly. | when V/k happens to be small by chance (the variance estimate underestimates the truth), the division inflates T, producing more extreme values than Z alone would generate; this self-correcting inflation *is* the heavy tail, and it's exactly what "pricing in" uncertain variance estimation looks like *(required: the division-by-a-noisy-quantity mechanism, not just "t has more spread")* | — |
| T1 | transfer | short-answer | 2.05 | Why must a small clinical trial (n=8) use the t-distribution for its confidence interval, while a huge trial (n=10,000) can essentially use Normal instead? Connect this to what "degrees of freedom" represents. | with small n, the sample standard deviation is itself a noisy, uncertain estimate of the true σ (few degrees of freedom), so t "prices in" that extra estimation uncertainty via heavier tails (E2's mechanism); with large n the sample SD is essentially exact (V/k→1 very precisely, per E1), so t converges to Normal and the distinction stops mattering in practice | treats "large sample" as making the *data* Normal, rather than making the *variance estimate* precise enough that t and Normal coincide → `t-distribution` |
| R3 | recall | mcq | −0.35 | The support of Tₖ is: | all of ℝ | picks x≥0, confusing t with a squared/chi-square-type quantity → `t-distribution` |
| R4 | recall | short-answer | −0.27 | Fill in the blank: E[Tₖ] = 0 for k > ___. | 1 | — |
| R5 | recall | short-answer | −0.19 | True or false: the t-distribution is symmetric about 0. | true | — |
| R6 | recall | short-answer | −0.11 | Fill in the blank: Var(Tₖ) = ___ for k > 2. | k/(k−2) | — |
| R7 | recall | mcq | −0.03 | As k→∞, Tₖ approaches: | N(0,1) | picks χ²∞ — an ill-defined and incorrect limit family → `t-distribution` |
| R8 | recall | short-answer | 0.05 | True or false: T₁ is also known as the (standard) Cauchy distribution. | true | — |
| R9 | recall | short-answer | 0.13 | Fill in the blank: Tₖ = Z/√(___), for standard normal Z and independent χ²ₖ variable V. | V/k | — |
| R10 | recall | mcq | 0.21 | For T₁ (Cauchy), which of these is undefined? | both the mean and the variance | picks "only the variance, since the mean is always 0 by symmetry" — symmetry doesn't guarantee the mean exists, and for Cauchy it genuinely doesn't → `t-distribution` |
| R11 | recall | short-answer | 0.29 | True or false: Tₖ has heavier tails than the standard normal for every finite k. | true | — |
| R12 | recall | mcq | 0.37 | The degrees of freedom k in Tₖ is inherited from: | the degrees of freedom of the χ²ₖ variable V in the denominator | picks "the sample size of Z alone" — Z is a single standard normal with no degrees-of-freedom parameter → `t-distribution` |
| R13 | recall | short-answer | 0.45 | Fill in the blank: Var(Tₖ) is undefined (infinite) for k ≤ ___. | 2 | — |
| R14 | recall | short-answer | 0.53 | True or false: as k increases, Tₖ's tails become lighter, approaching the standard normal's. | true | — |
| R15 | recall | mcq | 0.61 | In Tₖ = Z/√(V/k), the standard normal component is: | Z, in the numerator | picks V, the chi-square variable → `t-distribution` |
| R16 | recall | short-answer | 0.69 | In one line, why does the t-distribution arise in practice (rather than just using Normal directly)? | it accounts for the extra uncertainty from *estimating* the variance from a sample, rather than knowing the true σ | — |
| A3 | apply | numeric | 1.05 | Find Var(T₁₀). `[verified: 1.25]` | 10/(10−2) = 1.25 | computes 10/2=5, mishandling the subtraction → `t-distribution` |
| A4 | apply | short-answer | 1.15 | Rank T₃, T₃₀, and standard Normal from heaviest to lightest tails. | T₃ heaviest, then T₃₀, then Normal lightest — tail heaviness decreases monotonically as k increases, with Normal as the k→∞ limit | ranks T₃₀ as heavier than T₃, reversing the direction of the k-tail relationship → `t-distribution` |
| A5 | apply | numeric | 1.2 | Find Var(T₃). `[verified: 3]` | 3/(3−2) = 3/1 = 3 | computes 3/(3−2)² = 3, coincidentally right here but by squaring the denominator, an error that would fail on other k — flag as a method error even if this instance matches → `t-distribution` |
| A6 | apply | numeric | 1.25 | Find Var(T₂₀), and compare it to Var(Normal)=1. `[verified: ≈1.111]` | 20/18 ≈ 1.111 — already noticeably closer to 1 than Var(T₃)=3, illustrating convergence to Normal as k grows | assumes Var(Tₖ) equals exactly 1 for any k≥10 or so, rather than only approaching 1 in the limit → `t-distribution` |
| E3 | explain | derivation | 1.65 | Prove E[Tₖ]=0 for k>1, using symmetry of Tₖ's density about 0 (you may assume the density is an even function, f(−t)=f(t)). | for an even density, ∫t·f(t)dt over all of ℝ splits into two mirror-image regions where the integrand t·f(t) is odd (since t is odd and f is even, their product is odd), so the positive and negative contributions exactly cancel, giving E[Tₖ]=0 whenever the integral converges at all (which requires k>1) *(required: the odd-integrand cancellation argument, not just "symmetric implies mean 0")* | asserts symmetry alone guarantees a zero mean without addressing why k>1 is still needed for the integral to converge at all → `t-distribution` |
| E4 | explain | short-answer | 1.8 | Explain, mechanistically, why Var(Tₖ) is undefined for k≤2, connecting it to how heavy the tails already are at small k. | for small k, the denominator √(V/k) has a nontrivial chance of being very close to 0 (V itself can be small), which inflates T to extreme values often enough that E[T²] itself fails to converge to a finite number — the tails are so heavy that even the *variance's defining integral* diverges, not just some higher moment *(required: ties the undefined variance to the denominator's behavior, not just citing the formula k/(k−2) blowing up)* | treats "the formula k/(k−2) is undefined at k=2" as the *reason* variance doesn't exist, rather than as a symptom of the underlying tail-heaviness → `t-distribution` |
| E5 | explain | short-answer | 1.95 | The t-distribution was developed (by "Student," William Gosset) specifically for small-sample inference. Explain, in terms of what's being estimated, why small samples specifically create the need for t rather than Normal. | with a small sample, the sample standard deviation used to standardize a statistic is itself estimated with substantial noise; treating that noisy estimate as if it were the true σ (and using Normal critical values) understates how uncertain the resulting inference really is — t's heavier tails are the correction for that extra layer of estimation uncertainty, which becomes negligible once the sample is large *(required)* | — |
| T2 | transfer | short-answer | 2.15 | A researcher computes a 95% confidence interval from a sample of n=6 (5 degrees of freedom) but mistakenly uses the Normal critical value 1.96 instead of the correct t₅ critical value ≈2.571. Explain the practical consequence for the interval's actual coverage. | using 1.96 instead of ≈2.571 makes the interval too narrow, since it doesn't account for the extra tail weight needed to cover the true parameter 95% of the time with an *estimated* variance; the actual coverage of the resulting interval will be *less than* the claimed 95% — a systematic overconfidence | assumes using the smaller Normal critical value only shifts the interval's center, not its width, missing that the critical value directly scales the margin of error → `t-distribution` |
| T3 | transfer | short-answer | 2.2 | A data scientist with n=200,000 observations debates whether to use t or Normal critical values for a confidence interval, and a colleague says "it genuinely doesn't matter here." Justify the colleague's claim quantitatively, using what happens to Tₖ as k grows. | with k=199,999 degrees of freedom, Tₖ is for all practical purposes indistinguishable from N(0,1) — Var(Tₖ)=k/(k−2)≈1.00001, and the critical value t₀.₉₇₅,₁₉₉₉₉₉ is essentially 1.96 to many decimal places, so the choice between t and Normal changes the interval's width by a negligible amount at this sample size | insists t must always be used regardless of sample size "to be safe," without recognizing the practical convergence to Normal that makes the distinction immaterial here → `t-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.35…2.20 (2.55 logits). This whole section is deliberately built to motivate `one-sample-t-test` before that concept exists.*

---

## F-Distribution (`f-distribution`)
*Prereq: Chi Square Distribution · ancestors 21 · b₀ = 1.05*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.05 | Define F_{d₁,d₂} in terms of two independent chi-squares, and state its support. | F_{d₁,d₂} = (V₁/d₁)/(V₂/d₂) for independent V₁~χ²_d₁, V₂~χ²_d₂; support is (0,∞) only | — |
| R2 | recall | mcq | 0.35 | The square of a t-distributed variable with k df, T², has distribution: | F_{1,k} | picks χ²ₖ, missing that squaring a *ratio* (T is itself built from a ratio) doesn't collapse to a plain chi-square → `f-distribution` |
| A1 | apply | short-answer | 0.85 | Explain directly from the definition why F_{d₁,d₂} can never be negative. | both V₁ and V₂ are sums of squares, hence always ≥0; a ratio of two nonnegative quantities (each further divided by a positive constant) is always ≥0 *(required)* | — |
| A2 | apply | short-answer | 1.0 | If X ~ F_{5,20}, is 1/X also F-distributed? If so, with which parameters? | yes — 1/X ~ F_{20,5}; inverting the ratio swaps numerator and denominator, hence swaps the two degrees-of-freedom parameters directly | assumes 1/X has the same parameters as X → `f-distribution` |
| E1 | explain | short-answer | 1.55 | Without computing E[F] exactly, argue informally why E[F_{d₁,d₂}] should be *close to 1* when d₁ and d₂ are both large. | E[V₁/d₁]=E[V₂/d₂]=1 always; for large d₁,d₂ both ratios concentrate tightly around 1 (their relative spread √(2/d) shrinks, per `chi-square-distribution`'s E2), so their ratio concentrates near 1/1=1 *(required: the concentration argument, reusing the chi-square relative-spread fact)* | — |
| E2 | explain | short-answer | 1.7 | Why is F always positive and — unlike Normal or Chi-Square itself in the large-df limit — persistently *not* symmetric? | F is a ratio of two already-skewed, strictly positive quantities (chi-squares); ratios of skewed positive quantities tend to inherit and often amplify that skew, rather than symmetrizing the way a *sum* of many terms would *(required: contrasts ratio-of-skewed with sum-of-skewed, the mechanism, not just "it's skewed because it's F")* | — |
| T1 | transfer | derivation | 2.02 | *⚠ requires `t-distribution` in addition to `chi-square-distribution`.* Show that if T~tₖ, then T² ~ F_{1,k}. | Tₖ = Z/√(V/k), so T² = Z²/(V/k); Z² ~ χ²₁ (established in the Chi-Square section's own derivation of Z²~Gamma(½,½)=χ²₁); T² = (Z²/1)/(V/k), matching F_{1,k}'s definition exactly *(required: the full substitution chain)* | forgets Z² needs to be recognized as χ²₁ first, and tries to treat Z itself as chi-square → `chi-square-distribution` |
| T2 | transfer | short-answer | 2.02 | You want to compare the *variability* (not the means) of two manufacturing lines' output. Explain, briefly, why a **ratio** of variance estimates is a more natural comparison than a **difference**. | variances live on a squared, strictly-positive, multiplicative scale; a ratio is scale-free and naturally bounded below by 0, so "twice as variable" always reads as a ratio of 2 regardless of units — a difference is unit-dependent and treats a small perturbation and a genuine doubling inconsistently depending on the underlying scale *(required)* | — |
| R3 | recall | mcq | −0.35 | The support of F_{d₁,d₂} is: | (0, ∞) | picks [0,∞), missing that a ratio of two strictly positive chi-squares can never equal exactly 0 → `f-distribution` |
| R4 | recall | short-answer | −0.27 | Fill in the blank: E[F_{d₁,d₂}] = ___, for d₂>2. | d₂/(d₂−2) | — |
| R5 | recall | short-answer | −0.19 | True or false: the F-distribution is symmetric. | false — it's right-skewed, since it's a ratio of two positive, skewed quantities | — |
| R6 | recall | short-answer | −0.11 | Fill in the blank: F_{d₁,d₂} = (V₁/d₁)/___. | V₂/d₂ | — |
| R7 | recall | mcq | −0.03 | The two parameters of F_{d₁,d₂} are called: | numerator and denominator degrees of freedom | picks "shape and rate," borrowing Gamma's terminology incorrectly → `f-distribution` |
| R8 | recall | short-answer | 0.05 | True or false: E[F_{d₁,d₂}] depends only on d₂ (the denominator degrees of freedom), not on d₁. | true | — |
| R9 | recall | mcq | 0.13 | As d₂→∞ (d₁ fixed), F_{d₁,d₂} approaches the distribution of: | χ²_{d₁}/d₁ | picks a plain χ²_{d₁} (without dividing by d₁) → `f-distribution` |
| R10 | recall | short-answer | 0.21 | True or false: E[F_{d₁,d₂}] is undefined for d₂≤2. | true | — |
| R11 | recall | short-answer | 0.29 | True or false: the F-distribution is used to compare two variance estimates. | true | — |
| R12 | recall | mcq | 0.37 | If X ~ F_{d₁,d₂}, then 1/X is distributed as: | F_{d₂,d₁} | picks F_{d₁,d₂}, the same as X itself — inverting a ratio does swap the roles → `f-distribution` |
| R13 | recall | short-answer | 0.45 | Fill in the blank: if T ~ tₖ, then T² ~ ___. | F_{1,k} | — |
| R14 | recall | short-answer | 0.53 | True or false: F can take negative values. | false — it's a ratio of two non-negative quantities | — |
| R15 | recall | mcq | 0.61 | An F-test statistic is typically constructed as: | the ratio of two independent variance estimates | picks "the difference of two independent variance estimates" → `f-distribution` |
| R16 | recall | short-answer | 0.69 | In one line, what do the two degrees-of-freedom parameters d₁, d₂ represent? | the degrees of freedom of the two independent chi-square variables forming the numerator and denominator of the ratio | — |
| A3 | apply | numeric | 1.05 | Find E[F_{10,20}]. `[verified: ≈1.111]` | 20/(20−2) = 20/18 ≈ 1.111 | uses d₁ in the formula instead of d₂, computing 10/8=1.25 → `f-distribution` |
| A4 | apply | short-answer | 1.15 | Is F_{3,10} symmetric? Justify briefly. | no — F is a ratio of two positive, right-skewed chi-square-derived quantities, and such ratios are themselves right-skewed, not symmetric, regardless of the specific degrees of freedom | assumes some specific (d₁,d₂) combination could make F symmetric, without recognizing the ratio-of-skewed-positives structure always produces skew → `f-distribution` |
| A5 | apply | numeric | 1.2 | Find E[F_{4,8}]. `[verified: ≈1.333]` | 8/(8−2) = 8/6 ≈ 1.333 | computes 8/(8−4)=2, subtracting d₁ instead of the constant 2 → `f-distribution` |
| A6 | apply | numeric | 1.28 | Find E[F_{6,30}]. `[verified: ≈1.071]` | 30/(30−2) = 30/28 ≈ 1.071 | uses 6/(6−2)=1.5 by plugging the wrong (numerator) degrees of freedom into the mean formula → `f-distribution` |
| E3 | explain | derivation | 1.65 | Derive E[F_{d₁,d₂}] = d₂/(d₂−2), given the fact that for V₂~χ²_{d₂}, E[d₂/V₂] = d₂/(d₂−2) (a known reciprocal-chi-square moment), and that V₁, V₂ are independent. | F=(V₁/d₁)·(d₂/V₂); by independence, E[F]=E[V₁/d₁]·E[d₂/V₂] = 1 · d₂/(d₂−2) = d₂/(d₂−2), since E[V₁/d₁]=E[V₁]/d₁=d₁/d₁=1 *(required: the independence-factoring step and the E[V₁/d₁]=1 computation)* | assumes E[F]=E[V₁/d₁]/E[V₂/d₂] (dividing expectations directly) rather than correctly using E[d₂/V₂], a different quantity than d₂/E[V₂] → `f-distribution` |
| E4 | explain | short-answer | 1.8 | F_{d₁,d₂} and F_{d₂,d₁} are both "comparing the same two variance estimates," yet have different shapes. Explain why swapping which chi-square is on top changes the distribution's shape, using the inversion property from R12. | F_{d₂,d₁} is exactly the distribution of 1/X where X~F_{d₁,d₂} (R12); taking a reciprocal of a right-skewed positive quantity transforms its shape substantially (compressing large values, stretching small ones), so even though the same two underlying variance estimates are being compared, "which one is in the numerator" is a real modeling choice that changes the resulting distribution *(required: ties the shape change to the reciprocal transformation specifically)* | assumes F_{d₁,d₂} and F_{d₂,d₁} must be the same distribution since they involve "the same two variances" → `f-distribution` |
| E5 | explain | short-answer | 1.95 | Explain why the F-distribution arises naturally when testing whether two populations have equal variance, connecting back to how a single sample variance's sampling distribution is chi-square. | each sample variance estimate, once appropriately scaled, is (approximately) χ²-distributed (per `chi-square-distribution`'s own motivation from squared deviations); testing "are the two population variances equal" naturally becomes "is the ratio of these two independent χ²-based estimates close to 1," and a ratio of two independent chi-squares (each divided by its own degrees of freedom) is exactly the definition of F *(required)* | — |
| T3 | transfer | short-answer | 2.1 | Two factories' output weights are measured; Factory A's sample variance is computed from 11 items (10 df), Factory B's from 21 items (20 df). An engineer forms the ratio (Factory A's variance estimate)/(Factory B's variance estimate) and wants to know what distribution to compare it to, assuming the true variances are actually equal. Identify the distribution and its parameters. | F_{10,20} — the numerator's degrees of freedom (10) come from Factory A's sample, the denominator's (20) from Factory B's, matching the order the ratio was formed in | reports F_{20,10}, swapping which sample's degrees of freedom belongs in which slot, missing that the ratio's numerator/denominator order must match the F-distribution's own numerator/denominator convention → `f-distribution` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −0.35…2.10 (2.45 logits). T1 is filed here rather than dropped because the identity is too clean to lose; it is the one item in this whole bank that needs two sibling concepts unlocked at once rather than a strict ancestor chain.*

---

## Cluster 4 misconception index

| Tag | Blame |
|---|---|
| forgetting to standardize before a Normal lookup; percentile z-score direction reversed | `normal-distribution` |
| Uniform assumed max-variance or memoryless; density/CDF endpoint-shift slips | `uniform-distribution` |
| Exponential/Poisson conflated as the same distribution; time-unit rescaling forgotten | `exponential-distribution` |
| Gamma CDF attempted by hand instead of via the Poisson-count duality; shape/rate parameter mixups | `gamma-distribution` |
| Beta(1,1) mistaken for a point mass; mean/mode formula confusion | `beta-distribution` |
| chi-square sum-of-squares treated as Normal; Var(X)=2k formula slips | `chi-square-distribution` |
| t heavy tails direction reversed, or Var(Tₖ) assumed always finite | `t-distribution` |
| F ratio inversion or sign errors; mean formula's d₁/d₂ roles swapped | `f-distribution` |

**Cluster total: 240 items across 8 concepts (30 each).** All numeric claims verified by script,
including all newly added rows, the standard-normal quantile (bisection against an erf-based CDF),
and the Gamma/Poisson tail duality. The cluster is deliberately chained: Uniform's non-memorylessness
sets up Exponential's defining property; Exponential's sum sets up Gamma; Gamma's integer case sets up
Chi-Square via Z²~Gamma(½,½); Chi-Square sets up both t and F, which meet again in F's flagged T1.
Recall is now the dominant share (16 of 30 per concept) per the definitions-first expansion pass.
