# Machine Learning Cluster 4 — Generative/Discriminative & Classic Classifiers

Generative vs Discriminative Models, Naive Bayes, Linear Discriminant Analysis, K Nearest Neighbors,
Support Vector Machine, SVMs for Regression (6 concepts). Same format as
[Cluster 1](ml-01-foundations.md). This cluster is lighter on closed-form numeric items and heavier on
mechanism — no item here rests on an unverified arithmetic claim.

---

## Generative vs Discriminative Models (`generative-vs-discriminative-models`)
*Prereq: Classification vs Regression · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Distinguish generative from discriminative models. | generative: models the full joint P(X,Y) (often via P(X\|Y) and P(Y)); discriminative: models P(Y\|X) directly, focusing only on the decision boundary | — |
| R2 | recall | mcq | −0.45 | A key practical advantage of generative models is: | they can generate new synthetic data samples, since they model the full joint distribution | claims they're "always more accurate at classification" — not a general guarantee, and not the actual distinguishing superpower → `generative-vs-discriminative-models` |
| A1 | apply | short-answer | 0.1 | Is logistic regression generative or discriminative? Naive Bayes? | logistic regression: discriminative (models P(Y\|X) directly); naive Bayes: generative (models P(X\|Y) and P(Y), combines via Bayes' rule) | — |
| E1 | explain | short-answer | 0.8 | Why can a generative model always derive P(Y\|X) via Bayes' rule, while a discriminative model generally can't recover P(X) or generate new data? | having modeled the full joint P(X,Y), a generative model can compute *any* conditional or marginal from it, including P(Y\|X); a discriminative model only ever modeled P(Y\|X) and has no representation of P(X) at all to draw from *(required: the "has vs. lacks a representation of P(X)" argument)* | — |
| T1 | transfer | short-answer | 1.3 | Why does a modern generative image model need something like P(X) (the full distribution of images), while a discriminative image classifier only needs P(Y\|X)? | generating a *new*, realistic image requires sampling from a model of what images look like overall — P(X); a classifier only ever needs to map a *given* image to a label, never needing to represent the space of all possible images *(required: connects the classic distinction to why generative AI needs a fundamentally different objective than classification)* | — |

| R3 | recall | short-answer | −1.1 | What probability does a discriminative model learn directly? | P(Y\|X) — the conditional probability of the label given the features | — |
| R4 | recall | short-answer | −1.05 | What probability (or probabilities) does a generative model learn? | P(X\|Y) and P(Y) — the class-conditional feature distribution and the prior over classes, together forming the joint P(X,Y) | — |
| R5 | recall | mcq | −0.85 | Which formula lets a generative model recover P(Y\|X) from what it has learned? | Bayes' rule: P(Y\|X) ∝ P(X\|Y)P(Y) | claims it "cannot be recovered without additional data" — Bayes' rule converts exactly what a generative model already has → `generative-vs-discriminative-models` |
| R6 | recall | short-answer | −0.95 | Fill in the blank: a discriminative model directly models the ___ boundary, without modeling how the data itself was generated. | decision |
| R7 | recall | mcq | −0.75 | Which is an example of a discriminative model? | logistic regression | selects "naive Bayes" — naive Bayes models P(X\|Y) and P(Y), the generative route → `generative-vs-discriminative-models` |
| R8 | recall | short-answer | −0.8 | True or false: a discriminative model can be used to generate realistic new samples of X. | false — it never models P(X), so it has no representation of what typical X values look like | — |
| R9 | recall | mcq | −0.6 | Generative models are called "generative" because: | they model the full data distribution, which can be sampled from to produce new synthetic examples | claims it's "because they generate decision boundaries" — every classifier produces a decision boundary; that's not what distinguishes generative models → `generative-vs-discriminative-models` |
| R10 | recall | short-answer | −0.5 | Which type of model, generative or discriminative, typically needs fewer parameters to estimate the decision boundary directly, all else equal? | discriminative — it models only P(Y\|X), not the full joint distribution, which is often a simpler target | — |
| R11 | recall | mcq | −0.4 | An example of a generative model in the classifier catalogue is: | Linear Discriminant Analysis (LDA) | selects "logistic regression" — logistic regression models P(Y\|X) directly, making it discriminative → `generative-vs-discriminative-models` |
| R12 | recall | short-answer | −0.3 | What does it mean, informally, for a model to model "the joint distribution" P(X,Y)? | it captures how X and Y are generated together — both what typical feature values look like and what label typically accompanies them | — |
| R13 | recall | mcq | −0.2 | A discriminative model trained on data with class imbalance: | still directly estimates P(Y\|X) as observed in the training data, without separately modeling how X was generated | claims it "automatically corrects for class imbalance by modeling P(X)" — it has no model of P(X) to use for such a correction → `generative-vs-discriminative-models` |
| R14 | recall | short-answer | −0.1 | Fill in the blank: generative models model P(X,Y), while discriminative models model only ___. | P(Y\|X) |
| R15 | recall | mcq | −0.05 | Which statement correctly distinguishes the two model families? | generative models can, in principle, detect out-of-distribution inputs (unusual X) since they model P(X); discriminative models generally cannot | claims "neither family can detect unusual inputs" — a generative model's P(X) component gives it exactly this capability → `generative-vs-discriminative-models` |
| R16 | recall | short-answer | 0.0 | Is "generative vs. discriminative" a statement about model accuracy, or about what probability the model represents? | about what probability distribution the model represents — it is not, by itself, a claim about which family is more accurate | — |
| A2 | apply | short-answer | 0.15 | Is a Gaussian Mixture Model used for classification generative or discriminative? | generative — it models P(X\|Y) for each class as a Gaussian mixture component and combines with P(Y) via Bayes' rule | — |
| A3 | apply | short-answer | 0.2 | Is a support vector machine generative or discriminative? | discriminative — it directly finds a decision boundary (via the margin), with no model of P(X) or the joint distribution | — |
| A4 | apply | short-answer | 0.25 | A team wants a classifier that can also flag test inputs as "doesn't look like anything I was trained on." Should they lean generative or discriminative? | generative — only a model of P(X) (which generative models provide) can assess whether a new input looks atypical of the training distribution at all | — |
| A5 | apply | short-answer | 0.3 | A team has abundant labeled data and cares only about classification accuracy on inputs resembling the training distribution. Which family is often preferred, and why? | discriminative — since only the decision boundary is needed, directly modeling P(Y\|X) avoids the extra work (and potential model-misspecification risk) of estimating the full joint distribution | — |
| A6 | apply | short-answer | 0.35 | Random forests for classification: generative or discriminative? | discriminative — they directly partition feature space to predict Y, with no model of how X itself was generated | — |
| E2 | explain | short-answer | 0.85 | Why does a generative model typically need more data to fit well than a comparable discriminative model? | it must estimate the full class-conditional distribution P(X\|Y), which is a richer, higher-dimensional target than the decision boundary alone; more parameters generally require more data to estimate reliably *(required)* | — |
| E3 | explain | short-answer | 0.9 | Why might a discriminative model outperform a generative one when the generative model's distributional assumption (e.g. Gaussian) is wrong? | a discriminative model makes no commitment to how X is distributed — it only needs the decision boundary to be roughly right; a generative model's classification quality is directly limited by how well its assumed P(X\|Y) form matches reality, so a bad assumption directly corrupts the derived P(Y\|X) *(required)* | — |
| E4 | explain | short-answer | 0.95 | Why can a generative model be trained even without any labeled data for some components (e.g. using unlabeled data to help estimate P(X))? | since P(X) is part of what a generative model estimates, unlabeled examples (which reveal X but not Y) still contribute information toward that piece, unlike a purely discriminative model that has no use for X without a paired Y *(required)* | — |
| E5 | explain | short-answer | 1.0 | Why is "discriminative" the more direct approach when the only downstream goal is classification accuracy? | modeling the joint distribution solves a strictly harder problem than needed and introduces extra assumptions and estimation error that don't serve classification directly; directly optimizing for P(Y\|X) focuses effort exactly on what accuracy depends on *(required)* | — |
| T2 | transfer | short-answer | 1.4 | How does the generative/discriminative distinction reappear in the contrast between GANs (which learn to generate data) and standard classifiers used for the GAN's discriminator? | a GAN's generator is explicitly built to approximate P(X) so it can sample new realistic data — the generative task in its purest form — while the discriminator is an ordinary discriminative classifier distinguishing real from fake; the entire GAN architecture is built around pairing one model of each type *(required: names both roles explicitly)* | — |
| T3 | transfer | short-answer | 1.35 | Naive Bayes and logistic regression both ultimately produce a linear decision boundary under certain assumptions, yet one is generative and one discriminative. What does this reveal about the generative/discriminative distinction? | the distinction is about what the model represents and how it's trained (joint vs. conditional), not about the shape of the resulting decision boundary — two models from different families can converge to the same boundary form while differing fundamentally in what they estimate and how much data or assumptions that requires *(required: separates boundary shape from the modeling distinction)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.1…1.4.*

---

## Naive Bayes (`naive-bayes`)
*Prereq: Bayes' Rule, Generative vs Discriminative Models · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State naive Bayes' "naive" assumption. | features are conditionally independent given the class label | — |
| R2 | recall | mcq | 0.04 | Naive Bayes is called "naive" because: | it makes the often-unrealistic assumption that features are independent given the class | treats "naive" as a comment on the algorithm's sophistication rather than the specific independence assumption → `naive-bayes` |
| A1 | apply | short-answer | 0.54 | For a spam classifier, naive Bayes assumes "free" and "money" are independent given spam status. Why is this technically false, yet the classifier often still works reasonably well? | spam emails saying "free" plausibly also say "money" more often — genuinely correlated even within the spam class, violating the assumption; despite this, the classifier often ranks classes correctly in practice because the *relative ordering* of posterior probabilities can survive even when the assumption is technically wrong *(required: names both the violation and why it can still work)* | — |
| E1 | explain | short-answer | 1.24 | Why does the conditional independence assumption make naive Bayes' computation dramatically simpler? | estimating the full joint P(x₁,…,xₙ\|y) needs exponentially many parameters as features grow; assuming independence reduces this to estimating each P(xᵢ\|y) separately — linear in the number of features, directly sidestepping `curse-of-dimensionality`'s exponential blowup for joint distributions *(required: the exponential-vs-linear contrast, tied to curse of dimensionality)* | — |
| T1 | transfer | short-answer | 1.74 | Using Bayes' rule directly, explain how naive Bayes combines the simplified likelihood with the prior to get the posterior needed for classification. | P(y\|x₁,…,xₙ) ∝ P(y)·Π P(xᵢ\|y) — the prior P(y) times the naively-factored likelihood, an exact instance of `bayes-rule`'s formula with the likelihood term simplified *(required: the explicit Bayes'-rule framing, naming both pieces)* | — |

| R3 | recall | short-answer | −1.0 | What kind of classification problems is naive Bayes commonly applied to? | text classification tasks such as spam filtering, sentiment analysis, and document categorization, among other categorical/discrete-feature classification problems | — |
| R4 | recall | short-answer | −0.95 | What two quantities does naive Bayes need to estimate from training data? | the class priors P(Y) and the per-feature conditional likelihoods P(Xᵢ\|Y) for each feature | — |
| R5 | recall | mcq | −0.75 | Naive Bayes is a member of which model family, generative or discriminative? | generative — it models P(X\|Y) (via the factored likelihood) and P(Y), combining them via Bayes' rule | selects "discriminative" — naive Bayes models the class-conditional feature distribution, the defining generative move → `naive-bayes` |
| R6 | recall | short-answer | −0.85 | Fill in the blank: naive Bayes assumes features are conditionally ___ given the class label. | independent |
| R7 | recall | mcq | −0.65 | Gaussian naive Bayes assumes each feature, within a class, follows: | a Normal distribution | claims it assumes "a uniform distribution" — the "Gaussian" in the name specifically names the Normal assumption → `naive-bayes` |
| R8 | recall | short-answer | −0.7 | True or false: naive Bayes requires the features to actually be independent given the class for it to be usable at all. | false — it is routinely used even when the independence assumption is technically violated, and often still performs reasonably well | — |
| R9 | recall | mcq | −0.5 | The "multinomial" variant of naive Bayes is typically used for: | discrete count data, such as word counts in a document | claims it's used "specifically for continuous, real-valued features" — that describes Gaussian naive Bayes instead → `naive-bayes` |
| R10 | recall | short-answer | −0.4 | Without the conditional independence assumption, what would naive Bayes need to estimate instead of separate P(xᵢ|y) terms? | the full joint conditional distribution P(x₁,...,xₙ\|y), which requires exponentially many parameters as the number of features grows | — |
| R11 | recall | mcq | −0.3 | "Laplace smoothing" in naive Bayes exists to: | avoid zero probability estimates for feature values never seen with a given class in training data | claims it exists "to normalize the feature values to a common scale" — that describes feature scaling, an unrelated technique → `naive-bayes` |
| R12 | recall | short-answer | −0.2 | What happens to a naive Bayes prediction if one feature's likelihood P(xᵢ\|y)=0 exactly, with no smoothing? | the entire product for that class becomes zero, regardless of how strongly the other features support that class — one zero factor zeroes out the whole posterior | — |
| R13 | recall | mcq | −0.1 | Naive Bayes classifies a new example by: | choosing the class y that maximizes P(y)·Π P(xᵢ\|y) | claims it "chooses the class with the single highest individual P(xᵢ\|y) term" — the decision uses the full product across all features, not one term → `naive-bayes` |
| R14 | recall | short-answer | 0.0 | Fill in the blank: naive Bayes is called "naive" specifically because of its ___ assumption, not because the algorithm is unsophisticated. | (conditional) independence |
| R15 | recall | mcq | −0.05 | Naive Bayes tends to train very quickly compared to many other classifiers because: | estimating each P(xᵢ\|y) separately requires only simple counting or fitting one-dimensional distributions, with no iterative optimization needed | claims it's "because it ignores most of the training data" — it uses all training data, just processes it efficiently per-feature → `naive-bayes` |
| R16 | recall | short-answer | 0.05 | Does naive Bayes require the classes to be equally likely a priori? | no — the prior P(y) is estimated from the class frequencies in the training data and can be arbitrarily unequal | — |
| A2 | apply | numeric | 0.6 | P(spam)=0.4, P("free"\|spam)=0.7, P("money"\|spam)=0.6. Compute the unnormalized naive Bayes score P(spam)·P("free"\|spam)·P("money"\|spam). `[verified: 0.168]` | 0.4 × 0.7 × 0.6 = 0.168 | — |
| A3 | apply | numeric | 0.65 | P(not spam)=0.6, P("free"\|not spam)=0.2, P("money"\|not spam)=0.3. Using A2's spam score of 0.168, compute the normalized posterior P(spam\|"free","money"). `[verified: 0.824]` | not-spam score = 0.6×0.2×0.3 = 0.036; P(spam\|...) = 0.168/(0.168+0.036) ≈ 0.824 | — |
| A4 | apply | short-answer | 0.55 | A dataset has 50 binary features. Would you rather estimate a full joint P(X\|Y) or use naive Bayes' factored approximation, purely on data-efficiency grounds? | naive Bayes' factored approximation — the full joint over 50 binary features has up to 2⁵⁰ possible combinations to estimate probabilities for, while naive Bayes needs only 50 separate one-dimensional estimates per class | — |
| A5 | apply | short-answer | 0.5 | A word never appeared in any spam email in the training set. Without smoothing, what happens if that word appears in a new email being classified? | P("word"\|spam)=0 makes the entire spam-class product zero, so the email can never be classified as spam no matter what else it contains — smoothing is needed to avoid this | — |
| A6 | apply | short-answer | 0.45 | Is naive Bayes with Gaussian likelihoods appropriate for a feature that is a raw pixel intensity histogram bucket count? | not ideally — count data is better modeled with a multinomial (or Poisson) likelihood than a Gaussian, since counts are non-negative integers, not continuous real values | — |
| E2 | explain | short-answer | 1.1 | Why does naive Bayes often still rank classes correctly even when its independence assumption is technically violated? | classification only needs the *relative ordering* of the posterior probabilities across classes to be right, not their exact calibrated values; correlated features tend to bias all classes' scores in a similar direction, which can leave the ranking (and hence the decision) intact even though the absolute probabilities are off *(required)* | — |
| E3 | explain | short-answer | 1.15 | Why does naive Bayes remain usable even with a relatively small training set, unlike a model that must estimate the full joint distribution? | because it only needs to estimate one-dimensional (or low-dimensional) conditional distributions per feature per class, the amount of data needed per parameter is much smaller than what the exponentially-many joint-distribution parameters would require *(required)* | — |
| E4 | explain | short-answer | 1.2 | Why can naive Bayes still work reasonably well for text classification despite words obviously not being truly independent given the topic? | the model doesn't need perfect probability calibration, only enough signal to distinguish classes; the sheer number of weakly-informative word features, even individually imperfect, can combine into a strong enough aggregate signal for the correct class to usually win *(required)* | — |
| E5 | explain | short-answer | 1.05 | Why is Laplace smoothing described as adding "pseudo-counts" rather than as an ad hoc fix? | it can be derived as adding a small, principled Bayesian prior over the probability estimates themselves (a Beta or Dirichlet prior), which is a coherent way of expressing "assume every outcome is at least a little possible" rather than an arbitrary patch *(required)* | — |
| T2 | transfer | short-answer | 1.65 | How does naive Bayes' independence assumption parallel the assumption made when constructing a likelihood for i.i.d. samples in classical estimation theory? | both assume a factorization — i.i.d. sampling assumes each observation is independent of the others given the parameter, letting the joint likelihood factor into a product of per-observation terms; naive Bayes assumes each *feature* is independent given the class, letting the joint feature likelihood factor into a product of per-feature terms — the same "assume independence to make the product tractable" move, applied at a different level *(required: names the shared factorization-via-independence move)* | — |
| T3 | transfer | short-answer | 1.55 | Why might naive Bayes' probability outputs, despite often getting the classification right, be poorly calibrated as actual probabilities — and how does this connect to `probability-calibration`? | violating independence tends to make the model overconfident, because correlated features get "double-counted" as if they were independent pieces of evidence, pushing the posterior toward the extremes (near 0 or 1) more than warranted; `probability-calibration` techniques exist precisely to correct this kind of systematic miscalibration after the fact *(required: names the overconfidence mechanism and the calibration connection)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.0…1.74.*

---

## Linear Discriminant Analysis (`lda`)
*Prereq: Multivariate Normal, Generative vs Discriminative Models · ancestors 40 · b₀ = 1.36*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.36 | State LDA's generative assumption, and what makes its boundary linear. | each class's features are multivariate Normal; crucially, all classes *share the same covariance matrix*, differing only in their means — this shared covariance is exactly what makes the resulting decision boundary linear | — |
| R2 | recall | mcq | 0.66 | Dropping the shared-covariance assumption (each class gets its own covariance) gives: | Quadratic Discriminant Analysis (QDA), with a curved decision boundary | calls the result "still LDA" — a genuinely different method with a different boundary shape → `lda` |
| A1 | apply | short-answer | 1.16 | Why does sharing one covariance matrix but allowing different means produce a *linear* boundary between two classes? | the boundary is where the two classes' Gaussian densities are equal; with equal covariance, the quadratic terms in each Gaussian's exponent cancel exactly, leaving only a linear equation in x *(required: the cancellation argument specifically, not just "LDA is linear by definition")* | — |
| E1 | explain | short-answer | 1.86 | Why is LDA a generative model despite ultimately producing a classification boundary? | it models the full P(X\|Y) for each class (as a multivariate Normal) and combines with a prior P(Y) via Bayes' rule, the same generative structure as `naive-bayes`, rather than modeling P(Y\|X) directly *(required: the direct parallel to naive Bayes' generative structure)* | — |
| T1 | transfer | short-answer | 2.36 | Give a scenario where LDA's shared-covariance assumption is a poor fit, and explain why QDA might fit better despite needing more parameters. | two classes with genuinely different amounts of internal spread violate the shared-covariance assumption; QDA's per-class covariances reduce this bias, but estimating more covariance parameters per class increases variance, especially with limited data per class — a direct instance of the bias-variance tradeoff *(required: the explicit bias-variance framing of the QDA tradeoff)* | — |

| R3 | recall | short-answer | −0.3 | What distributional assumption does LDA make about each class's features? | each class's feature vector follows a multivariate Normal distribution | — |
| R4 | recall | short-answer | −0.25 | What is the key structural assumption that distinguishes LDA from QDA? | LDA assumes all classes share one common covariance matrix, differing only in their mean vectors; QDA lets each class have its own covariance matrix | — |
| R5 | recall | mcq | 0.0 | LDA is a member of which model family? | generative — it models each class's P(X\|Y) as Gaussian and combines with the prior P(Y) via Bayes' rule | selects "discriminative" — LDA explicitly models the class-conditional feature distribution, a generative move → `lda` |
| R6 | recall | short-answer | −0.15 | Fill in the blank: LDA's decision boundary between two classes is always ___ (a specific geometric shape), a direct consequence of the shared-covariance assumption. | linear (a hyperplane) |
| R7 | recall | mcq | 0.1 | QDA's decision boundary, in general, is: | quadratic (curved) — e.g. an ellipse, parabola, or hyperbola in two dimensions | claims QDA's boundary is "always linear, just like LDA's" — dropping the shared-covariance assumption is precisely what makes it curved → `lda` |
| R8 | recall | short-answer | 0.05 | True or false: LDA can be used for more than two classes at once. | true — LDA naturally extends to multi-class problems, computing a linear boundary between every pair implicitly through the shared model | — |
| R9 | recall | mcq | 0.2 | LDA needs to estimate, for K classes: | K mean vectors, but only one shared covariance matrix | claims it needs "K separate covariance matrices" — that describes QDA, not LDA's shared-covariance design → `lda` |
| R10 | recall | short-answer | 0.15 | What prerequisite concept does LDA's Gaussian class-conditional assumption directly draw on? | the multivariate Normal distribution | — |
| R11 | recall | mcq | 0.3 | Compared to QDA, LDA generally has: | fewer parameters to estimate, since it shares one covariance matrix across all classes | claims LDA has "more parameters than QDA" — sharing one covariance matrix instead of K separate ones is strictly fewer parameters → `lda` |
| R12 | recall | short-answer | 0.25 | What happens to LDA's shared covariance matrix estimate if it's computed by pooling within-class covariances? | it becomes a weighted average of each class's individual (co)variance around its own mean, combined across all classes | — |
| R13 | recall | mcq | 0.4 | LDA is expected to outperform QDA in which regime? | when training data per class is limited, since LDA's shared-covariance assumption reduces variance in the covariance estimate | claims LDA wins "whenever the true covariances genuinely differ across classes" — that scenario actually favors QDA, since LDA's assumption would then be wrong → `lda` |
| R14 | recall | short-answer | 0.35 | Fill in the blank: LDA assumes classes differ only in their ___, not in their spread or shape. | means (mean vectors) |
| R15 | recall | mcq | 0.45 | Which of the following would violate LDA's core assumption? | two classes with visibly different amounts of scatter/spread around their means | selects "two classes with different means" — different means are exactly what LDA expects and models; it's different covariances that break the assumption → `lda` |
| R16 | recall | short-answer | 0.5 | Is LDA's decision boundary shape (linear) a consequence of an optimization choice, or a direct mathematical consequence of its distributional assumptions? | a direct mathematical consequence — the linearity falls out of the algebra once you assume shared covariance and equate two Gaussian densities, it isn't chosen separately | — |
| A2 | apply | short-answer | 1.0 | Two classes have very different spreads in the data (one tightly clustered, one widely scattered). Would fitting LDA to this data violate the shared-covariance assumption? | yes — visibly different spreads across classes directly contradicts the assumption that all classes share one covariance matrix, and QDA (or another method) would likely be more appropriate | — |
| A3 | apply | short-answer | 1.05 | With only 20 training points per class and 15 features, would LDA or QDA more likely give a stable (well-estimated) covariance structure? | LDA — pooling all classes' data to estimate a single shared covariance matrix uses far more data per estimated parameter than QDA's per-class covariances would with so little data available | — |
| A4 | apply | short-answer | 0.95 | Given LDA's linear boundary, would it be well-suited to separating two classes that form concentric circles (one class inside a ring, one outside)? | no — no linear boundary can separate a circle-shaped class from a surrounding ring; this pattern needs a nonlinear boundary, which QDA or another nonlinear method could provide | — |
| A5 | apply | short-answer | 1.1 | A dataset's two classes have equal covariance matrices by design (a synthetic example). Would LDA's assumption hold exactly here? | yes — the shared-covariance assumption is met exactly by construction, so LDA's linear boundary is the theoretically correct (Bayes-optimal) one for this data | — |
| A6 | apply | short-answer | 1.15 | If you fit LDA to data where the true boundary is genuinely quadratic, is the resulting error more attributable to variance or to model-misspecification bias? | model-misspecification bias — no amount of additional data fixes a linear model's inability to represent a genuinely curved boundary; this is a form of high bias from an incorrect functional-form assumption | — |
| E2 | explain | short-answer | 1.5 | Why does LDA's linear boundary arise specifically from the shared-covariance assumption, algebraically? | the decision boundary is where the two classes' Gaussian log-densities are equal; each Gaussian's log-density has a quadratic term −½(x−μ)ᵀΣ⁻¹(x−μ); when Σ is shared across classes, the quadratic-in-x terms are identical for both classes and cancel exactly when set equal, leaving only terms linear in x *(required: the explicit cancellation mechanism)* | — |
| E3 | explain | short-answer | 1.55 | Why does LDA's pooling of covariance estimates across classes reduce estimation variance compared to QDA? | pooling combines data from every class into one covariance estimate, effectively using the full training set (rather than just one class's share) to estimate each entry of that matrix, which shrinks the sampling variability of the estimate compared to estimating each class's covariance from only its own, smaller subset of data *(required)* | — |
| E4 | explain | short-answer | 1.6 | Why is LDA sometimes preferred over logistic regression when classes are well-separated and genuinely close to Gaussian? | when LDA's generative assumptions actually hold, it uses more information (the full class-conditional distributions) than logistic regression's purely discriminative approach, which can yield a more efficient (lower-variance) estimate of the boundary — though logistic regression is more robust when the Gaussian assumption fails *(required: names the efficiency-when-assumptions-hold argument)* | — |
| E5 | explain | short-answer | 1.45 | Why does dropping to QDA only pay off with substantially more data per class than LDA needs? | QDA estimates a separate covariance matrix per class, multiplying the number of covariance parameters to estimate by the number of classes; each of those estimates needs enough data on its own to be reliable, so the "more flexible, assumption-free" gain comes at a real statistical cost that only large per-class samples can absorb *(required)* | — |
| T2 | transfer | short-answer | 2.1 | How does the LDA-vs-QDA choice mirror the general bias-variance tradeoff framework, connecting directly to `bias-variance-tradeoff`? | LDA's extra assumption (shared covariance) is a form of intentional bias — it constrains the model even when possibly wrong — in exchange for lower variance from needing far fewer parameters; QDA relaxes that constraint, lowering bias when the assumption was wrong but raising variance from more parameters to estimate — a direct instance of the general tradeoff applied to a specific modeling choice *(required: maps both LDA and QDA explicitly onto the bias/variance framework)* | — |
| T3 | transfer | short-answer | 2.2 | LDA can also be derived as a dimensionality-reduction technique (projecting onto the direction that best separates classes) rather than only as a classifier. How does this connect its generative classification role to the linear-algebra concept of finding an optimal projection direction? | both views solve the same underlying optimization — LDA's projection direction maximizes between-class separation relative to within-class scatter, an eigenvalue problem on a scatter-matrix ratio; the classification boundary derived from the Gaussian-density argument and the axis found by that eigenvalue problem coincide, so "LDA as classifier" and "LDA as optimal linear projection" are two readings of one linear-algebra structure *(required: names the shared eigenvalue-problem structure)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.3…2.36.*

---

## K Nearest Neighbors (`knn`)
*Prereq: Classification vs Regression, Curse of Dimensionality · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Describe the KNN prediction rule. | find the K closest training points to a new point, and take a majority vote (or average, for regression) among their labels | — |
| R2 | recall | mcq | −0.35 | KNN is called "lazy learning" because: | it does essentially no work at training time (just stores the data) and does all its computation at prediction time | claims it "requires no computation at prediction time" — the opposite; that's exactly when all the work happens → `knn` |
| A1 | apply | short-answer | 0.2 | As K increases from 1 toward N, does the decision boundary get smoother or more jagged? What does this imply for bias and variance? | smoother as K increases (at the extreme K=N, every point gets the same, majority-class prediction — very high bias, low variance); small K (e.g. K=1) gives a jagged, low-bias, high-variance fit sensitive to individual noisy points *(required: both extremes and their bias-variance character)* | — |
| E1 | explain | short-answer | 0.9 | Why does KNN suffer badly from the curse of dimensionality? | in high dimensions, per `curse-of-dimensionality`, all points start looking roughly equidistant, so "nearest" neighbors become increasingly uninformative as dimensionality grows — directly undermining KNN's core mechanism *(required: the direct callback, not a fresh restatement)* | — |
| T1 | transfer | short-answer | 1.4 | Why does KNN essentially require feature scaling, unlike decision trees? | without scaling, a feature measured in large units (income in dollars, tens of thousands) would completely dominate the distance calculation over a feature in small units (age in years, 0–100), even if age is actually more predictive — decision trees split on one feature at a time and are unaffected by scale, but KNN's distance metric mixes all features together *(required: the concrete large-vs-small-unit example, and the decision-tree contrast)* | — |

| R3 | recall | short-answer | −1.0 | What does the "K" in K-nearest-neighbors control? | how many of the closest training points are consulted (via vote or average) to make a prediction | — |
| R4 | recall | short-answer | −0.95 | What distance metric is most commonly used by default in KNN? | Euclidean distance | — |
| R5 | recall | mcq | −0.75 | For KNN regression (as opposed to classification), the prediction is: | the average of the K nearest neighbors' target values | claims it's "the majority vote of the K nearest neighbors' labels" — majority vote is for classification; regression uses averaging → `knn` |
| R6 | recall | short-answer | −0.85 | Fill in the blank: KNN stores the entire training set and defers all computation to ___ time. | prediction (query/test) |
| R7 | recall | mcq | −0.65 | K=1 nearest neighbor classification uses: | only the single closest training point's label as the prediction | claims it "averages over the 1% closest points" — K=1 means exactly one neighbor, not a percentage-based group → `knn` |
| R8 | recall | short-answer | −0.7 | True or false: KNN has an explicit training phase that fits parameters before predictions can be made. | false — it has no real "fitting" step beyond storing the data; all the work happens at prediction time | — |
| R9 | recall | mcq | −0.5 | An odd value of K is often preferred for binary classification specifically to: | avoid tie votes between the two classes | claims it's "to make the algorithm run faster" — parity has nothing to do with computational speed, only tie-breaking → `knn` |
| R10 | recall | short-answer | −0.4 | What is the computational cost characteristic that makes KNN prediction expensive on large datasets? | it must compute the distance from the query point to every stored training point (naively O(n) per query), which grows with dataset size | — |
| R11 | recall | mcq | −0.3 | KNN is best described as which kind of model? | non-parametric — it makes no fixed-form assumption about the decision boundary's shape and its effective complexity grows with the data | claims it's "parametric, since K is a fixed number chosen in advance" — K being fixed doesn't make the model parametric; the boundary itself has no fixed functional form → `knn` |
| R12 | recall | short-answer | −0.2 | What happens to the KNN decision boundary as K approaches N (the full training set size)? | it approaches a constant prediction everywhere — simply the majority class (or global average) across the entire training set | — |
| R13 | recall | mcq | −0.1 | Weighted KNN, where closer neighbors count more, differs from standard KNN by: | giving neighbors influence inversely proportional to their distance, rather than an equal vote each | claims it "ignores all but the single nearest neighbor" — weighted KNN still uses all K neighbors, just with unequal weighting → `knn` |
| R14 | recall | short-answer | 0.0 | Fill in the blank: KNN is called "___ learning" because virtually no computation happens until a prediction is actually requested. | lazy |
| R15 | recall | mcq | −0.05 | Which preprocessing step is typically essential before applying KNN, but unnecessary for decision trees? | feature scaling/normalization | claims "one-hot encoding categorical features" is essential for KNN but not decision trees — both families generally need categorical features encoded somehow; scaling is the step specific to distance-based methods → `knn` |
| R16 | recall | short-answer | 0.05 | Does increasing K always monotonically decrease variance? | broadly yes — averaging over more neighbors generally smooths the prediction and reduces sensitivity to any single noisy point, though very large K can eventually just return the global majority/average | — |
| A2 | apply | short-answer | 0.3 | A dataset has 100,000 points. Is KNN prediction fast or slow at test time, compared to a trained linear model's prediction? | slow, comparatively — KNN must compute distances to (a large fraction of) all 100,000 stored points for each new prediction, while a linear model's prediction is a single fast dot product regardless of training set size | — |
| A3 | apply | numeric | 0.35 | With K=5 and neighbor labels [1,1,0,1,0] (majority vote), what is the predicted class? `[verified: 1]` | three votes for class 1 versus two for class 0, so majority vote predicts class 1 | — |
| A4 | apply | short-answer | 0.4 | A feature "age" (0–100) and a feature "income" (0–200,000) are used unscaled in KNN. Which feature will dominate the distance calculation? | income — its much larger numeric range means differences in income values will overwhelm differences in age in any unscaled Euclidean distance computation | — |
| A5 | apply | short-answer | 0.45 | Comparing K=1 and K=25 on the same noisy dataset, which is more likely to misclassify due to a single mislabeled nearby training point? | K=1 — with only one neighbor consulted, a single mislabeled nearby point directly determines the (wrong) prediction; K=25 dilutes that one bad point's influence among 24 others | — |
| A6 | apply | short-answer | 0.5 | For KNN regression predicting house price, would K=1 or K=50 give a smoother predicted-price surface across nearby locations? | K=50 — averaging over many more neighbors produces a smoother, less jagged surface than the highly local, noisy K=1 prediction | — |
| E2 | explain | short-answer | 0.95 | Why does K=1 KNN have zero training error (on non-duplicate points), yet often generalize poorly? | with K=1, every training point is trivially its own nearest neighbor, so it "predicts itself" perfectly during training — but this fits noise as much as signal, giving a highly variable, overfit boundary that performs worse on genuinely new points *(required)* | — |
| E3 | explain | short-answer | 1.0 | Why does the "curse of dimensionality" undermine the core geometric intuition behind KNN specifically? | KNN relies on the idea that nearby points in feature space share similar labels; in high dimensions, per `curse-of-dimensionality`, the ratio between the nearest and farthest neighbor distances shrinks toward 1, so "nearest" stops being meaningfully different from "far" *(required: the direct callback, not a fresh restatement)* | — |
| E4 | explain | short-answer | 1.05 | Why does choosing K via cross-validation, rather than a fixed default, matter for KNN's performance? | the right K depends on the specific dataset's noise level and true decision boundary complexity — too small overfits, too large underfits — and this optimal balance point can't be known in advance, so it must be estimated empirically the same way any complexity hyperparameter would be *(required)* | — |
| E5 | explain | short-answer | 0.85 | Why is KNN's prediction time cost a genuine practical concern in production systems, unlike for a trained parametric model? | KNN defers essentially all computation to prediction time and its cost scales with the size of the stored training set, so as the deployed model accumulates more historical data, every single prediction gets slower — a parametric model's prediction cost stays fixed regardless of training set size once it's fitted *(required)* | — |
| T2 | transfer | short-answer | 1.45 | How does KNN's reliance on distance in feature space connect to the RBF kernel's use of distance, from the kernels cluster? | both directly use the raw distance ‖x−y‖ between points as their core mechanism — KNN uses it to select which points get a (binary) vote, while the RBF kernel uses it continuously to weight influence, decaying smoothly with distance rather than cutting off sharply at a fixed K; RBF can be seen as a "soft," continuously-weighted generalization of the same distance-based-similarity idea KNN uses discretely *(required: names the shared distance-based-similarity mechanism)* | — |
| T3 | transfer | short-answer | 1.35 | Why is KNN, despite being one of the simplest classifiers conceptually, often used as a strong baseline before trying more complex models? | it makes almost no assumptions about the shape of the decision boundary, so it can capture genuinely complex, nonlinear patterns without any model-design choices; if a more sophisticated, assumption-laden model can't beat KNN's accuracy, that's a signal the extra assumptions aren't paying off for this particular dataset *(required: names the assumption-free baseline role)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.0…1.45.*

---

## Support Vector Machine (`svm`)
*Prereq: Kernel, Classification vs Regression · ancestors 8 · b₀ = 0.60*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.4 | Describe SVM's core idea. | find the separating hyperplane that maximizes the margin — the distance to the nearest points of each class ("support vectors") — not just any separating boundary | — |
| R2 | recall | mcq | −0.1 | The support vectors are: | only the training points closest to the boundary, which alone determine where it's placed | claims "all the training points" are support vectors — most points, once the model is fit, are irrelevant to where the boundary sits → `svm` |
| A1 | apply | short-answer | 0.4 | If a training point far from the boundary (not a support vector) is removed and the SVM re-trained, does the boundary change? | no — non-support-vector points have no influence on the final boundary; only the support vectors matter *(required: the "no change" answer with the reasoning, not just a guess)* | — |
| E1 | explain | short-answer | 1.1 | Why does maximizing the margin, rather than finding any separating boundary, tend to generalize better to new data? | a boundary with a large buffer zone is less likely to misclassify a new test point that falls slightly off from where the training points sat, compared to a boundary that barely squeezes between the classes with no room to spare *(required)* | — |
| T1 | transfer | short-answer | 1.6 | Why does the kernel trick let SVM find nonlinear boundaries efficiently, without ever explicitly computing the (potentially infinite-dimensional) transformed features? | a kernel function computes the inner product *as if* the data had been mapped into a higher-dimensional space, without ever materializing that mapping — the SVM optimization only ever needs these inner products, never the transformed vectors themselves *(required: names that only inner products are needed, not the explicit mapping)* — exactly why `kernel` is SVM's prerequisite | — |

| R3 | recall | short-answer | −0.9 | What is the "margin" in SVM terminology? | the distance between the separating hyperplane and the closest training points of either class | — |
| R4 | recall | short-answer | −0.85 | What objective does the standard (hard-margin) SVM optimize? | maximize the margin, subject to all points being correctly classified and outside the margin | — |
| R5 | recall | mcq | −0.65 | The "soft margin" SVM was introduced specifically to handle: | data that is not perfectly linearly separable, by allowing some points to violate the margin at a controlled cost | claims it exists "to make the SVM run faster on large datasets" — the soft margin is about tolerating misclassification/violations, not speed → `svm` |
| R6 | recall | short-answer | −0.75 | Fill in the blank: the hyperparameter C in soft-margin SVM controls the tradeoff between maximizing the margin and ___. | minimizing margin violations (misclassifications) |
| R7 | recall | mcq | −0.55 | A large value of C in soft-margin SVM: | penalizes margin violations heavily, producing a narrower margin that fits the training data more closely | claims a large C "always produces a wider margin" — a large C penalizes violations more, which typically narrows the margin, the opposite → `svm` |
| R8 | recall | short-answer | −0.6 | True or false: SVM can only be used for binary classification, never regression. | false — SVR (SVMs for Regression) extends the same core idea to regression tasks | — |
| R9 | recall | mcq | −0.4 | SVM's decision boundary, without a kernel, is: | linear — a hyperplane in the original feature space | claims it's "always curved by default" — the base SVM is linear; curvature requires a nonlinear kernel → `svm` |
| R10 | recall | short-answer | −0.3 | What is the geometric relationship between the margin and the support vectors? | the support vectors lie exactly on the margin's boundary (or, in the soft-margin case, within or on the wrong side of it) — they are the points the margin's location is defined by | — |
| R11 | recall | mcq | −0.2 | Small C in soft-margin SVM: | tolerates more margin violations, favoring a wider margin over fitting every point correctly | claims small C "forces zero tolerance for misclassified points" — that describes large C (or the hard-margin limit), not small C → `svm` |
| R12 | recall | short-answer | −0.1 | What does it mean for SVM's optimization problem to be convex? | it has a single global optimum, with no risk of getting stuck at a worse local solution — unlike training a neural network | — |
| R13 | recall | mcq | 0.0 | The "hinge loss" used in SVM training penalizes: | points that are on the correct side but too close to the boundary (within the margin), and even more so, misclassified points | claims it "penalizes every point equally regardless of position" — hinge loss is zero for points comfortably beyond the margin and grows for those violating it → `svm` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: as C → ∞, soft-margin SVM approaches the ___-margin SVM, tolerating essentially no violations. | hard |
| R15 | recall | mcq | −0.05 | SVM is a member of which model family? | discriminative — it directly finds a decision boundary, with no model of P(X) or the joint distribution | selects "generative" — SVM never models the feature distribution, it only finds the separating boundary → `svm` |
| R16 | recall | short-answer | 0.1 | Does SVM's margin-maximizing objective use every training point equally in determining the final boundary? | no — only the support vectors influence the final boundary; other points (once training is complete) could be removed without changing it | — |
| A2 | apply | short-answer | 0.45 | A dataset has only 4 support vectors out of 10,000 training points. If 9,000 non-support-vector points are removed and the SVM re-trained, does the boundary change? | no — only the support vectors determine the boundary's position; removing any of the other 9,000 points has no effect on the result | — |
| A3 | apply | short-answer | 0.5 | With C set very small, does the resulting SVM boundary tend to be more sensitive or less sensitive to individual outlier points? | less sensitive — small C tolerates margin violations freely, so a single outlier is unlikely to drastically shift the boundary's placement | — |
| A4 | apply | short-answer | 0.55 | With C set very large, is the SVM more or less prone to overfitting on noisy data with some mislabeled points? | more prone — a very large C insists on classifying (nearly) every training point correctly, including noise and mislabeled points, which can distort the boundary to accommodate them | — |
| A5 | apply | short-answer | 0.35 | If the data is genuinely linearly separable with a wide gap between classes, does soft-margin SVM's C hyperparameter matter much? | not very much — with a wide, clean separation, essentially any reasonable C finds the same (or a very similar) max-margin boundary, since there's little pressure toward violations either way | — |
| A6 | apply | short-answer | 0.4 | Is hinge loss zero, positive, or negative for a point correctly classified with a comfortable margin beyond the boundary? | zero — hinge loss only activates a penalty for points inside the margin or on the wrong side; comfortably-correct points incur no loss at all | — |
| E2 | explain | short-answer | 1.15 | Why is the soft-margin formulation necessary for most real-world datasets, rather than the hard-margin version? | real data is rarely perfectly linearly separable — noise, overlapping classes, or mislabeled points make a hard, zero-violation boundary either impossible to find or wildly overfit to a few problematic points; the soft margin allows controlled tolerance for these unavoidable imperfections *(required)* | — |
| E3 | explain | short-answer | 1.2 | Why does a very large C risk overfitting, in bias-variance terms? | large C pushes the optimizer toward fitting every training point correctly, including noise-driven outliers, producing a boundary highly specific to this particular training sample — high sensitivity to the exact training data is precisely the signature of high variance *(required: the explicit variance framing)* | — |
| E4 | explain | short-answer | 1.25 | Why does SVM's convex optimization objective make it more reliable to train than, say, a neural network of comparable complexity? | convexity guarantees the optimizer reaches the same global optimum regardless of initialization or the specific optimization path taken, while a non-convex objective (as in most neural networks) can converge to different, potentially worse local optima depending on starting conditions *(required)* | — |
| E5 | explain | short-answer | 1.05 | Why does the hinge loss's "flat zero" region (for confidently-correct points) matter for which points end up as support vectors? | since points beyond the margin incur exactly zero loss and therefore contribute nothing to the gradient of the optimization objective, only points inside or violating the margin (where hinge loss is nonzero) can influence where the boundary settles — directly explaining why only those points become support vectors *(required)* | — |
| T2 | transfer | short-answer | 1.55 | How does SVM's margin-maximization principle connect to the bias-variance tradeoff, via the C hyperparameter? | C directly trades bias for variance: large C reduces bias (fits training data more closely, including its idiosyncrasies) but raises variance (boundary shifts more with different training samples); small C does the reverse — this is the same tradeoff `bias-variance-tradeoff` describes generally, here made explicit through one tunable knob *(required: explicit bias-variance framing of C)* | — |
| T3 | transfer | short-answer | 1.5 | Comparing hinge loss (SVM) to cross-entropy loss (logistic regression), both are used for binary classification. How are their penalty shapes fundamentally different? | hinge loss is exactly zero for any sufficiently-confident correct prediction — it stops caring once a point is safely beyond the margin — while cross-entropy loss keeps decreasing (never reaching exactly zero) as confidence increases toward certainty, always rewarding more confidence; this reflects SVM's geometric margin-focused objective versus cross-entropy's probabilistic, likelihood-based one *(required: names the flat-zero-vs-always-decreasing contrast)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.9…1.6.*

---

## SVMs for Regression (`svms-for-regression`)
*Prereq: Support Vector Machine · ancestors 9 · b₀ = 0.65*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.35 | Describe SVR's core idea. | fit a function so that as many points as possible fall within an ε-tolerance "tube" around it, ignoring errors smaller than ε; only points *outside* the tube incur a penalty and become support vectors | — |
| R2 | recall | mcq | −0.05 | The ε parameter in SVR controls: | how wide the no-penalty tube around the prediction is | confuses it with "the learning rate" — an unrelated optimization-algorithm parameter → `svms-for-regression` |
| A1 | apply | short-answer | 0.45 | With a very large ε (wide tube), do more or fewer points become support vectors? What does this imply about model flexibility? | fewer — more points fall comfortably inside the wide tube; this generally gives a simpler, less flexible fit, trading some accuracy on fine-grained variation for reduced sensitivity — a bias-variance tradeoff *(required: the direction and the bias-variance framing)* | — |
| E1 | explain | short-answer | 1.15 | What is the conceptual parallel between SVR's ε-tube and ordinary SVM's margin? | both define a buffer zone incurring no penalty; only points outside the zone (the "support vectors" in each case) actually influence or determine the final fitted function *(required: the direct margin/tube parallel)* | — |
| T1 | transfer | short-answer | 1.65 | Why might SVR be preferred over ordinary least-squares regression for data with occasional large outliers? | SVR's loss depends only on how far *outside* the ε-tube a point falls, giving it a degree of robustness to outliers; ordinary least-squares' squared-error loss penalizes large errors quadratically (per `loss-functions`), making it highly sensitive to even a single large outlier *(required: the explicit callback to squared error's quadratic penalty)* | — |

| R3 | recall | short-answer | −0.85 | What does the ε (epsilon) tube represent in SVR? | a band of tolerance around the fitted function within which prediction errors incur no penalty at all | — |
| R4 | recall | short-answer | −0.8 | Which points become support vectors in SVR? | only the points that fall outside the ε-tube — those inside are ignored, contributing nothing to the fit | — |
| R5 | recall | mcq | −0.6 | SVR generalizes ordinary linear regression by: | ignoring small errors (within ε) entirely, rather than penalizing every deviation from the fitted line | claims it "penalizes every deviation more heavily than OLS" — SVR is specifically more tolerant of small errors, not less → `svms-for-regression` |
| R6 | recall | short-answer | −0.7 | Fill in the blank: SVR uses an ___-insensitive loss function, which is zero within the tube and grows linearly outside it. | ε (epsilon) |
| R7 | recall | mcq | −0.5 | If ε is set to 0, SVR's tube: | shrinks to zero width, so essentially every nonzero-error point becomes a potential support vector | claims "SVR becomes undefined at ε=0" — ε=0 is a valid (if extreme) special case, just with the widest possible support vector set → `svms-for-regression` |
| R8 | recall | short-answer | −0.55 | True or false: SVR's core margin/hinge-style machinery is a direct extension of the classification SVM's margin idea to regression. | true — both center on a tolerance region with only "violating" points influencing the final result | — |
| R9 | recall | mcq | −0.4 | Like classification SVM, SVR can use: | kernels to fit a nonlinear function while still only needing dot-product-like computations | claims it "cannot use kernels since it's a regression, not classification, method" — SVR inherits the kernel trick from SVM exactly as classification SVM does → `svms-for-regression` |
| R10 | recall | short-answer | −0.3 | What geometric shape does the ε-tube form around a linear SVR fit in 2D? | two parallel lines, offset by ε above and below the central fitted line, forming a band | — |
| R11 | recall | mcq | −0.2 | SVR's C hyperparameter plays a role analogous to: | classification SVM's C — controlling the tradeoff between model complexity/flatness and tolerating errors beyond the tube | claims C in SVR "controls the width of the ε-tube directly" — the tube's width is controlled by ε, not C; C controls the penalty weight on violations → `svms-for-regression` |
| R12 | recall | short-answer | −0.1 | What does it mean for a fitted function to be "flat" in the SVR optimization objective (the thing being minimized alongside violation penalties)? | the function has small coefficients/low complexity — SVR explicitly favors simpler, smoother fits alongside satisfying the ε-tolerance | — |
| R13 | recall | mcq | 0.0 | SVR is best suited to: | continuous target prediction (regression tasks) | selects "binary classification tasks" — that is what ordinary SVM, not SVR, is designed for → `svms-for-regression` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: points strictly inside the ε-tube incur ___ penalty. | zero (no) |
| R15 | recall | mcq | −0.05 | Compared to ordinary least-squares regression, SVR's loss for points within the tolerance: | is exactly zero, rather than the (small but nonzero) squared penalty OLS would still apply | claims SVR's in-tolerance loss "is small but always nonzero, just like OLS" — the entire point of the ε-tube is that in-tolerance points get exactly zero loss → `svms-for-regression` |
| R16 | recall | short-answer | 0.1 | Does SVR's fitted function need to pass through every training point exactly, given a nonzero ε? | no — SVR explicitly does not need to pass exactly through points within ε of the fitted function, prioritizing a flat/simple fit over interpolating all data | — |
| A2 | apply | short-answer | 0.5 | With ε set to a very small value close to 0, does SVR's fit tend to behave more like ordinary least-squares regression or stay clearly different? | more like OLS — as ε→0, essentially every deviation from the fit incurs some penalty, closely resembling how OLS penalizes any nonzero residual | — |
| A3 | apply | short-answer | 0.55 | A dataset has 1,000 points, and after fitting SVR, only 40 are outside the ε-tube. How many points influence the final fitted function? | only those 40 — points inside the tube (the other 960) incur zero loss and have no effect on where the function is fit | — |
| A4 | apply | short-answer | 0.6 | Comparing SVR with a very wide ε to SVR with a very narrow ε on the same data, which is likely to have fewer support vectors? | the very wide ε — a wider tolerance band lets more points fall comfortably inside it, leaving fewer points as violators (support vectors) | — |
| A5 | apply | short-answer | 0.4 | If a training point lies exactly on the boundary of the ε-tube (zero violation, zero margin), is it typically treated as a support vector? | it's a borderline case — points exactly on the tube boundary are the threshold case between "inside" (no influence) and "outside" (influences the fit); implementations vary in the exact boundary handling, but points strictly outside always count | — |
| A6 | apply | short-answer | 0.45 | For a target variable measured with roughly ±0.5 units of sensor noise, is setting ε≈0.5 a reasonable heuristic starting point? | yes, reasonably — setting ε near the expected noise level lets SVR ignore variation that's likely just noise rather than genuine signal, avoiding penalizing the model for the sensor's inherent imprecision | — |
| E2 | explain | short-answer | 1.05 | Why does a wider ε-tube tend to produce a simpler, less flexible fitted function? | with more room for error tolerated freely, the optimizer has less pressure to bend the function to chase every data point's exact value, so it settles for a flatter/simpler function consistent with the (more forgiving) constraint *(required)* | — |
| E3 | explain | short-answer | 1.1 | Why might SVR be a poor choice when even small deviations from the target are meaningful (e.g. high-precision measurement prediction)? | SVR's ε-insensitive loss explicitly treats any error within ε as equally acceptable (zero loss), discarding exactly the fine-grained distinctions that matter in such a setting; a loss function penalizing every deviation (like squared error) would be more appropriate *(required)* | — |
| E4 | explain | short-answer | 1.15 | Why does SVR's use of kernels let it fit nonlinear regression functions using the same underlying machinery as linear SVR? | as in classification SVM, if the algorithm only ever uses dot products between data points, those dot products can be replaced by kernel evaluations, implicitly mapping into a higher-dimensional space where a "linear" fit in that space corresponds to a nonlinear fit in the original space *(required: the dot-product-only condition)* | — |
| E5 | explain | short-answer | 0.95 | Why does the flatness term in SVR's objective (alongside the ε-tolerance) matter, rather than optimizing tolerance alone? | without a preference for simpler/flatter functions, infinitely many functions could satisfy "stay within ε of every point," including wildly oscillating ones; the flatness term breaks the tie in favor of the simplest such function, analogous to a regularization term *(required)* | — |
| T2 | transfer | short-answer | 1.45 | How does SVR's ε-insensitive loss compare structurally to Huber loss from robust regression? | both are designed to reduce sensitivity to certain residuals — Huber loss transitions from quadratic (near zero) to linear (far from zero) to reduce outlier sensitivity, while SVR's loss is exactly zero near zero and linear beyond ε; both depart from pure squared-error loss specifically to avoid over-penalizing either small (SVR) or large (Huber) residuals in ways ordinary least squares would *(required: names both loss shapes and the shared motivation of departing from pure squared error)* | — |
| T3 | transfer | short-answer | 1.4 | Why does SVR's connection to `svm` mean that everything learned about the C hyperparameter and kernels there transfers almost unchanged to regression? | SVR is built on the identical optimization machinery as classification SVM — a convex objective trading margin/flatness against violation penalties, solved via the same dual formulation that only needs dot products (hence kernelizable); the target being continuous rather than categorical changes what "violation" means (outside the ε-tube vs. wrong side of the margin) but not the underlying mathematical structure, so C and kernel intuitions carry over directly *(required: names the shared underlying optimization structure)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.85…1.65.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| generative model advantage misattributed to accuracy rather than data generation | `generative-vs-discriminative-models` |
| generative/discriminative examples (naive Bayes vs. logistic regression, LDA) swapped | `generative-vs-discriminative-models` |
| discriminative models wrongly credited with recovering P(X) or generating data | `generative-vs-discriminative-models` |
| "naive" mistaken for a comment on sophistication rather than the independence assumption | `naive-bayes` |
| naive Bayes misclassified as discriminative | `naive-bayes` |
| Laplace smoothing confused with feature scaling | `naive-bayes` |
| Gaussian vs. multinomial naive Bayes likelihood forms confused | `naive-bayes` |
| LDA and QDA conflated | `lda` |
| LDA misclassified as discriminative | `lda` |
| LDA and QDA parameter counts (shared vs. per-class covariance) swapped | `lda` |
| KNN's lazy-learning timing (training vs. prediction cost) reversed | `knn` |
| KNN regression and classification aggregation rules (averaging vs. voting) swapped | `knn` |
| KNN's non-parametric nature confused with "parametric because K is fixed" | `knn` |
| all training points assumed to be support vectors | `svm` |
| soft-margin SVM's C direction (large C → narrower vs. wider margin) reversed | `svm` |
| hinge loss assumed to penalize every point equally regardless of position | `svm` |
| SVR's ε confused with an optimization learning rate | `svms-for-regression` |
| SVR's C and ε roles (violation penalty vs. tube width) swapped | `svms-for-regression` |
| SVR assumed unable to use the kernel trick | `svms-for-regression` |

**Cluster total: 180 items across 6 concepts (30 each).** No item in this cluster rests on an
unverified numeric claim — the content here is entirely mechanism and geometry, checked for correctness by construction
rather than by script.
