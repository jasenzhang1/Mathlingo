# Machine Learning Cluster 2 — Model Evaluation & Selection

Multiclass Classification, Confusion Matrices, ROC Curves, K-Fold Cross-Validation, Hyperparameters,
Sensitivity Analysis (6 concepts). Same format as [Cluster 1](ml-01-foundations.md).

`confusion-matrices`' cells map directly onto `type-i-ii-error` from the probability/statistics sweep
— false positive is a Type I error, false negative a Type II error — and this cluster's items make
that correspondence explicit rather than treating precision/recall as a fresh vocabulary.

---

## Multiclass Classification (`multiclass-classification`)
*Prereq: Classification vs Regression · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Define multiclass classification. | predicting one of more than two categories, as opposed to binary classification | — |
| R2 | recall | mcq | −0.45 | The "one-vs-rest" (OvR) strategy works by: | training a separate binary classifier per class (that class vs. everything else), then picking the most confident one | claims it "trains one combined classifier natively" — that's a native multiclass method, not OvR → `multiclass-classification` |
| A1 | apply | short-answer | 0.1 | Using OvR for 5-class digit classification, how many binary classifiers are trained? | 5, one per class | — |
| E1 | explain | short-answer | 0.8 | Distinguish one-vs-rest (OvR) from one-vs-one (OvO), including their classifier counts. | OvR trains K classifiers (each class vs. the rest); OvO trains K(K−1)/2 classifiers (every pair against each other) — OvO can be more expensive for many classes but each pairwise problem is often simpler/more balanced *(required: both counts and the tradeoff)* | — |
| T1 | transfer | short-answer | 1.3 | Why is softmax regression generally preferred over OvR/OvO wrappers for problems with very many classes (e.g. 1000-class image classification)? | softmax natively predicts one probability distribution over all classes simultaneously, avoiding the K or K(K−1)/2 separate classifiers OvR/OvO would require — the wrapper approaches scale poorly as the number of classes grows large *(required: the scaling comparison)* | — |
| R3 | recall | short-answer | −1.30 | State the general formula for the number of classifiers trained under one-vs-one (OvO), given K classes. | K(K−1)/2 | — |
| R4 | recall | mcq | −0.90 | Softmax regression, unlike OvR/OvO, produces predictions by: | computing one probability distribution over all K classes directly, from a single unified model | picks "training K independent binary classifiers and combining their outputs" — that describes OvR, not softmax's native approach → `multiclass-classification` |
| R5 | recall | short-answer | −1.20 | For OvR with K classes, how is the final predicted class chosen among the K binary classifiers' outputs? | the class whose binary classifier gives the highest confidence/score is selected | — |
| R6 | recall | mcq | −0.80 | One-vs-one (OvO) trains a classifier for: | every pair of classes | picks "every class against all the others combined" — that describes OvR, not OvO → `multiclass-classification` |
| R7 | recall | short-answer | −1.40 | Fill in the blank: one-vs-rest is also commonly called one-vs-___. | all | — |
| R8 | recall | mcq | −1.00 | Which multiclass strategy generally produces more balanced binary sub-problems, especially with many classes? | one-vs-one (OvO), since each sub-problem only involves two classes' worth of examples | picks "one-vs-rest (OvR), since it always uses the full dataset" — OvR's "rest" class can be far larger than the "one" class, creating imbalance → `multiclass-classification` |
| R9 | recall | short-answer | −0.60 | True or false: softmax regression is a native multiclass method, not a wrapper around binary classifiers. | true | — |
| R10 | recall | mcq | −1.35 | For 4-class classification, how many binary classifiers does OvR train? | 4 | picks "6" — that's OvO's count (4·3/2), not OvR's → `multiclass-classification` |
| R11 | recall | short-answer | −0.70 | State one practical downside of one-vs-one as the number of classes grows very large. | the number of classifiers grows quadratically (K(K−1)/2), which can become computationally expensive for many classes | — |
| R12 | recall | mcq | −1.10 | The softmax function converts a vector of raw scores into: | a valid probability distribution (nonnegative, summing to 1) over the classes | picks "a binary yes/no decision for each class independently" — that describes something closer to independent sigmoids per class, not softmax's coupled normalization → `multiclass-classification` |
| R13 | recall | short-answer | −0.50 | True or false: OvR and OvO can both be used with any binary classifier as the underlying building block. | true | — |
| R14 | recall | mcq | −1.45 | For 4-class classification, how many binary classifiers does OvO train? | 6 | picks "4" — that's OvR's count, not OvO's → `multiclass-classification` |
| R15 | recall | short-answer | −0.95 | Fill in the blank: OvR reduces a K-class problem to K separate ___ classification problems. | binary | — |
| R16 | recall | mcq | −0.65 | A key advantage of the OvR strategy is that it: | is computationally cheaper than OvO for a large number of classes, since it needs only K classifiers rather than K(K−1)/2 | picks "always achieves higher accuracy than OvO" — no general accuracy guarantee exists; the actual tradeoff is computational and about class balance → `multiclass-classification` |
| A2 | apply | numeric | −0.55 | For a 6-class classification problem, how many classifiers does OvO train? `[verified: 15]` | 6·5/2=15 | — |
| A3 | apply | numeric | −0.40 | For the same 6-class problem, how many classifiers does OvR train? `[verified: 6]` | 6 | — |
| A4 | apply | short-answer | −0.20 | A 100-class problem needs to be solved with limited compute budget. Which strategy, OvR or OvO, is more likely to be computationally feasible, and why? | OvR — it needs only 100 classifiers, versus OvO's 100·99/2=4,950, which likely exceeds the compute budget | — |
| A5 | apply | short-answer | 0.00 | A 3-class OvR setup gives class-A score 0.2, class-B score 0.7, class-C score 0.1 from the three binary classifiers. Which class is predicted? | class B, since it has the highest confidence score among the three | — |
| A6 | apply | numeric | 0.20 | For a 20-class problem, compute the ratio of OvO's classifier count to OvR's. `[verified: 9.5]` | OvO: 20·19/2=190; OvR: 20; ratio=190/20=9.5 | — |
| E2 | explain | short-answer | 0.20 | Explain why OvR can struggle with class imbalance even on an originally balanced K-class dataset. | for K classes with roughly equal-sized original groups, each OvR binary sub-problem pits one class (1/K of the data) against "the rest" (the other (K−1)/K of the data) — as K grows, this creates an increasingly imbalanced binary sub-problem even though the original multiclass dataset was balanced *(required: the fraction-based imbalance argument, showing it worsens as K grows)* | — |
| E3 | explain | short-answer | 0.35 | Explain why OvO's pairwise sub-problems tend to be easier for the underlying binary classifier than OvR's sub-problems. | each OvO sub-problem only needs to separate two specific classes from each other, often a more homogeneous and separable boundary, while each OvR sub-problem must separate one class from a heterogeneous mixture of all the other classes combined, which can require a more complex decision boundary to draw well *(required: the specific two-classes-versus-heterogeneous-mixture contrast)* | — |
| E4 | explain | short-answer | 0.50 | Explain why softmax regression, despite training only a single unified model, still effectively produces a decision boundary between every pair of classes. | softmax computes a score for every class from shared underlying parameters, and the predicted class is whichever has the highest score; the boundary between any two classes A and B is implicitly wherever their scores cross, which is a real, well-defined boundary even though no separate pairwise model was ever explicitly trained for that specific pair *(required: the implicit-pairwise-boundary-from-shared-parameters argument)* | — |
| E5 | explain | short-answer | 0.65 | Explain why OvR's binary classifiers' raw scores are not always directly comparable to each other, creating a subtlety in choosing "the most confident" prediction. | each of the K binary classifiers in OvR is trained completely independently, potentially on differently-scaled or differently-calibrated score outputs (depending on the base classifier and its specific one-vs-rest training data), so simply comparing raw scores across classifiers can be misleading unless those scores are calibrated onto a common, comparable scale first *(required: names the independent-training/uncalibrated-scores issue specifically)* | — |
| T2 | transfer | short-answer | 1.05 | Why might a practitioner choose OvO specifically when the underlying binary classifier's training time scales poorly with dataset size (e.g. quadratically), even though OvO requires training more classifiers overall than OvR? | if a base classifier's training cost scales badly with the number of examples, OvO's many small pairwise sub-problems (each using only the data from two classes) can actually be cheaper in total than OvR's few but much larger sub-problems (each using the full dataset), despite OvO needing more classifiers in count — the per-classifier cost, not just the classifier count, determines the actual total training time *(required: the per-classifier-cost-versus-classifier-count tradeoff argument)* | — |
| T3 | transfer | short-answer | 1.55 | A 1000-class image classifier trained with softmax achieves strong overall accuracy, but performs poorly distinguishing two visually similar classes buried within the 1000. Why might switching to a dedicated OvO classifier for just that specific pair (as a secondary refinement step) help, without abandoning softmax for the other 998 classes? | softmax's shared parameters must balance discriminating power across all 1000 classes simultaneously, which can leave subtle distinctions between any two visually similar classes under-emphasized relative to the many easier distinctions dominating the overall loss; a dedicated OvO classifier trained specifically and only on those two confusable classes can focus its entire capacity on that one hard boundary, without the diluting influence of the other 998 classes' gradients — a hybrid approach that uses softmax's efficiency broadly and OvO's focus narrowly, exactly where it's needed *(required: names the shared-capacity dilution in softmax and the focused-capacity advantage of a targeted OvO refinement)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.45…1.55.*

---

## Confusion Matrices (`confusion-matrices`)
*Prereq: Classification vs Regression · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Name the four cells of a binary confusion matrix. | true positive, false positive, true negative, false negative | — |
| R2 | recall | mcq | −0.45 | False positives and false negatives correspond exactly to which earlier concepts? | Type I error (false positive) and Type II error (false negative), from `type-i-ii-error` | treats precision/recall as an unrelated, fresh vocabulary rather than recognizing the direct correspondence → `confusion-matrices` |
| A1 | apply | numeric | 0.1 | TP=80, FP=20, TN=850, FN=50. Compute accuracy, precision, recall. `[verified: 0.93, 0.8, 0.615]` | accuracy=(80+850)/1000=0.93; precision=80/100=0.8; recall=80/130≈0.615 | — |
| E1 | explain | short-answer | 0.8 | Why can 95% accuracy still describe a genuinely useless classifier for a rare-event problem like fraud detection (1% true fraud)? | predicting "not fraud" for every case achieves 99% accuracy trivially while catching zero actual fraud (recall=0) — a devastating critique of accuracy alone as a metric, directly parallel to `power`'s point about low-power studies looking unremarkable despite missing real effects *(required: the trivial-classifier example with its recall)* | — |
| T1 | transfer | short-answer | 1.3 | Which metric — precision or recall — is more directly hurt by false positives, and which by false negatives? Why might a spam filter and a cancer screening test prioritize these differently? | precision (TP/(TP+FP)) is hurt by false positives; recall (TP/(TP+FN)) is hurt by false negatives; a spam filter often prioritizes precision (a legitimate email marked as spam is costly), while a cancer screen prioritizes recall (a missed cancer is far worse than a false alarm) — echoing `type-i-ii-error`'s smoke-detector/spam-filter contrast *(required: both directions and a concrete application pair)* | — |
| R3 | recall | short-answer | −0.85 | Give the formula for precision in terms of TP and FP. | precision = TP/(TP+FP) | — |
| R4 | recall | mcq | −0.45 | Give the formula for recall (sensitivity) in terms of TP and FN. | recall = TP/(TP+FN) | picks "TP/(TP+FP)" — that's precision's formula, not recall's → `confusion-matrices` |
| R5 | recall | short-answer | −0.75 | Define specificity (true negative rate) in terms of TN and FP. | specificity = TN/(TN+FP) | — |
| R6 | recall | mcq | −0.35 | The F1 score is defined as: | the harmonic mean of precision and recall | picks "the simple arithmetic mean of precision and recall" — F1 is specifically the harmonic mean → `confusion-matrices` |
| R7 | recall | short-answer | −0.95 | Give the formula for accuracy in terms of all four confusion-matrix cells. | accuracy = (TP+TN)/(TP+TN+FP+FN) | — |
| R8 | recall | mcq | −0.55 | For a K-class problem, a confusion matrix has dimensions: | K×K | picks "always 2×2, regardless of the number of classes" — only true for binary classification → `confusion-matrices` |
| R9 | recall | short-answer | −0.25 | True or false: precision and recall generally trade off against each other as the classification threshold is adjusted. | true | — |
| R10 | recall | mcq | −1.00 | The false positive rate is defined as: | FP/(FP+TN) | picks "FP/(FP+TP)" — that's a different quantity (1−precision), not the false positive rate → `confusion-matrices` |
| R11 | recall | short-answer | −0.65 | Fill in the blank: balanced accuracy averages ___ and specificity. | recall (sensitivity) | — |
| R12 | recall | mcq | −1.05 | In a multiclass confusion matrix, the diagonal cells represent: | correctly classified examples (predicted class matches actual class) | picks "always the majority class's predictions only" — the diagonal represents correct predictions across every class, not just the majority one → `confusion-matrices` |
| R13 | recall | short-answer | −0.40 | State the standard convention for which axis represents actual versus predicted labels in a confusion matrix. | conventionally, rows represent actual/true labels and columns represent predicted labels, though tools vary | — |
| R14 | recall | mcq | −0.80 | Macro-averaging a metric like precision across multiple classes means: | computing the metric separately per class, then taking the unweighted average across classes | picks "pooling all classes' TP/FP/etc. counts together before computing one overall metric" — that describes micro-averaging → `confusion-matrices` |
| R15 | recall | short-answer | −0.50 | True or false: F1 score, unlike accuracy, can't be inflated by a large number of easy true negatives on an imbalanced dataset. | true — F1 is built entirely from TP, FP, and FN, ignoring true negatives entirely | — |
| R16 | recall | mcq | −0.90 | Which single-number summary combines precision and recall, weighting them equally? | the F1 score | picks "accuracy" — accuracy doesn't combine precision and recall in the harmonic-mean sense at all → `confusion-matrices` |
| A2 | apply | numeric | 0.15 | TP=45, FP=15, TN=380, FN=60. Compute the F1 score. `[verified: 0.545]` | precision=45/60=0.75, recall=45/105≈0.4286, F1=2·(0.75·0.4286)/(0.75+0.4286)≈0.545 | — |
| A3 | apply | numeric | 0.25 | TP=45, FP=15, TN=380, FN=60. Compute specificity. `[verified: 0.962]` | TN/(TN+FP)=380/395≈0.962 | — |
| A4 | apply | numeric | 0.35 | TP=45, FP=15, TN=380, FN=60. Compute the false positive rate. `[verified: 0.038]` | FP/(FP+TN)=15/395≈0.038 | — |
| A5 | apply | short-answer | 0.45 | A 3-class confusion matrix's diagonal entries are 40, 35, 42, with actual per-class counts 50, 40, 50. Compute per-class recall. `[verified: 0.8, 0.875, 0.84]` | class 1: 40/50=0.8; class 2: 35/40=0.875; class 3: 42/50=0.84 | — |
| A6 | apply | numeric | 0.55 | Continuing A5, compute macro-averaged recall across the three classes. `[verified: 0.838]` | (0.8+0.875+0.84)/3≈0.838 | — |
| E2 | explain | short-answer | 0.70 | Explain why F1 is specifically the harmonic mean, not the arithmetic mean, of precision and recall. | the harmonic mean is far more sensitive to a low value in either precision or recall than the arithmetic mean is — if either is very low, the harmonic mean is pulled sharply downward, whereas an arithmetic mean could stay moderately high propped up by the other value; this makes F1 specifically penalize a lopsided classifier (great at one, terrible at the other) *(required: the sensitivity-to-the-lower-value argument, contrasting harmonic against arithmetic mean)* | — |
| E3 | explain | short-answer | 0.85 | Explain why macro-averaging and micro-averaging can give meaningfully different results on an imbalanced multiclass dataset. | macro-averaging treats every class equally regardless of size, so poor performance on a small rare class counts just as much as good performance on a large common class; micro-averaging pools all raw counts first, so it's effectively dominated by the largest classes — on an imbalanced dataset macro-average can look much worse than micro-average if the model specifically struggles on rare classes *(required: the class-size-weighting mechanism distinguishing the two schemes)* | — |
| E4 | explain | short-answer | 1.00 | Explain why specificity is often reported alongside recall for medical screening tests, rather than relying on recall or accuracy alone. | recall alone says how many actual positives are caught but says nothing about the false-positive burden on the many actual negatives screened; specificity directly measures how well the test avoids falsely flagging healthy people, which matters enormously at scale in a mostly-healthy screened population, since even a small false positive rate produces many unnecessary follow-ups *(required: names specifically the false-positive-burden-at-scale reasoning)* | — |
| E5 | explain | short-answer | 1.15 | Explain why an off-diagonal cell in a multiclass confusion matrix carries more diagnostic information than "the model was wrong here." | an off-diagonal cell identifies specifically *which* class the model confused with *which other* class, revealing systematic confusions (e.g. consistently mistaking one digit for a visually similar one) that a single aggregate accuracy number completely hides; examining the pattern of off-diagonal cells can point directly toward targeted fixes for the confused class pair, rather than a vague "the model needs improvement" *(required: names the which-class-confused-with-which-other-class diagnostic specificity)* | — |
| T2 | transfer | short-answer | 1.35 | Why might a company optimize for F2 score (weighting recall higher) instead of F1 for rare-disease screening, and what does this reveal about the F-beta family? | for rare-disease screening, missing an actual case (a false negative) is typically far more costly than a false alarm ruled out on follow-up, so recall should be weighted more heavily than precision; the F-beta family generalizes F1 by letting the practitioner choose how much more recall should matter relative to precision, reflecting the true asymmetric cost structure of the application rather than assuming they're always equally important *(required: names the asymmetric-cost-structure motivation and the F-beta generalization idea)* | — |
| T3 | transfer | short-answer | 1.70 | A hiring classifier has strong overall accuracy and F1, but subgroup-level confusion matrices reveal far more false negatives for one demographic subgroup. Why does this matter despite fine aggregate metrics, and what does it reveal about a single confusion matrix's limits? | an aggregate confusion matrix computed across the whole population is a population-weighted average that can mask systematically unequal error rates across subgroups, looking acceptable even when one subgroup is disproportionately harmed; a confusion matrix's diagnostic value depends entirely on the granularity it's computed at — a single overall matrix is necessary but not sufficient for auditing real-world fairness, and subgroup-level matrices are needed to detect this disparity *(required: names the aggregate-masks-subgroup-disparity mechanism and the granularity limitation)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.05…1.70.*

---

## ROC Curves (`roc-curves`)
*Prereq: Confusion Matrices · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Define an ROC curve. | true positive rate vs. false positive rate, plotted across every possible classification threshold | — |
| R2 | recall | mcq | −0.35 | The AUC for a random/useless classifier is: | 0.5 | picks 0.0, confusing "useless" with "always wrong" (which would actually be a *perfectly* informative, just inverted, classifier) → `roc-curves` |
| A1 | apply | short-answer | 0.2 | A classifier has AUC=0.95. Good or bad, relative to the random baseline? | very good — far above the 0.5 random baseline, indicating strong discrimination across thresholds | — |
| E1 | explain | short-answer | 0.9 | Why is evaluating across all thresholds, rather than one fixed cutoff, useful? | different applications want different thresholds (e.g. a conservative cutoff to reduce false positives); AUC summarizes performance regardless of which threshold is eventually chosen, rather than committing to one arbitrary default like 0.5 *(required)* | — |
| T1 | transfer | short-answer | 1.4 | Why can ROC/AUC be misleading on a heavily imbalanced dataset (99% negative, 1% positive)? | the false positive rate's denominator is the (huge) count of actual negatives, so even a substantial *absolute* number of false positives looks like a small rate — a precision-recall curve, whose denominators involve the rare positive class directly, is often preferred in such settings *(required: names the denominator-size mechanism, not just "ROC can be misleading")* | — |
| R3 | recall | short-answer | −0.70 | What quantity is plotted on the x-axis of an ROC curve? | false positive rate (FPR) | — |
| R4 | recall | mcq | −0.30 | What quantity is plotted on the y-axis of an ROC curve? | true positive rate (TPR, i.e. recall) | picks "precision" — that's the y-axis of a precision-recall curve, not ROC → `roc-curves` |
| R5 | recall | short-answer | −0.80 | What AUC value corresponds to a perfect classifier? | AUC = 1 | — |
| R6 | recall | mcq | −0.20 | Give the probabilistic interpretation of AUC. | the probability a randomly chosen positive example is ranked (scored) higher than a randomly chosen negative example | picks "the probability the classifier is correct on a randomly chosen example" — closer to accuracy's meaning, not AUC's ranking interpretation → `roc-curves` |
| R7 | recall | short-answer | −0.90 | What does the point (0,1) on an ROC curve represent? | perfect classification at that threshold — 0% false positive rate and 100% true positive rate | — |
| R8 | recall | mcq | −0.40 | An ROC curve is generated by: | plotting the (FPR, TPR) pair at every possible classification threshold | picks "plotting a single fixed (FPR, TPR) point, then connecting it to the origin" — the whole point is tracing every threshold's pair, not one point → `roc-curves` |
| R9 | recall | short-answer | −0.50 | True or false: a classifier's ROC curve staying entirely above the diagonal indicates it outperforms random guessing at every threshold. | true | — |
| R10 | recall | mcq | −0.85 | For multiclass problems, ROC curves are typically generalized using: | a one-vs-rest reduction, producing a separate ROC curve per class | picks "a single K-dimensional ROC surface that's rarely computed in practice" — one-vs-rest per-class curves are the standard practical approach → `roc-curves` |
| R11 | recall | short-answer | −0.15 | Fill in the blank: AUC stands for ___. | area under the (ROC) curve | — |
| R12 | recall | mcq | −0.75 | An AUC below 0.5 would indicate: | a classifier performing worse than random guessing | picks "an impossible value that can never occur in practice" — it's a valid, if unusual, outcome (e.g. a systematically inverted classifier) → `roc-curves` |
| R13 | recall | short-answer | −0.25 | State the valid range of possible AUC values. | between 0 and 1, inclusive | — |
| R14 | recall | mcq | −0.65 | A classifier's ROC curve that is a straight line from (0,0) to (1,1) has AUC: | exactly 0.5 | picks "exactly 1.0" — that describes a perfect classifier's curve, not the diagonal → `roc-curves` |
| R15 | recall | short-answer | −0.35 | True or false: ROC curves are constructed independently of any single fixed decision threshold. | true — that's the entire point of plotting across all thresholds | — |
| R16 | recall | mcq | −0.55 | Comparing two classifiers by AUC is most appropriate when: | you want an aggregate, threshold-independent measure of ranking quality | picks "you only care about performance at one specific, already-chosen threshold" — a direct metric like accuracy/F1 at that threshold is more relevant than aggregate AUC in that case → `roc-curves` |
| A2 | apply | short-answer | 0.25 | Classifier A has AUC=0.60, Classifier B has AUC=0.85. Which ranks positives above negatives better, in general? | Classifier B — its higher AUC means it more consistently ranks a random positive above a random negative | — |
| A3 | apply | short-answer | 0.35 | A classifier's ROC curve passes exactly through (0.1, 0.9). What are its FPR and TPR at that threshold? | FPR=0.1 (10%), TPR=0.9 (90%) | — |
| A4 | apply | numeric | 0.45 | A test set has 100 negatives and 50 positives. At a given threshold, 8 negatives are misclassified as positive. Compute the false positive rate. `[verified: 0.08]` | FPR=8/100=0.08 | — |
| A5 | apply | short-answer | 0.55 | Two ROC curves cross (A better at low FPR, B better at high FPR) yet have the same AUC. What does this suggest about relying on AUC alone? | AUC alone can hide meaningful differences in *where* along the curve each classifier performs well — for an application caring only about a low-FPR region, A could be the genuinely better practical choice despite an identical overall AUC | — |
| A6 | apply | short-answer | 0.65 | A perfect classifier and one that always predicts negative are both evaluated. What is each one's AUC? `[verified: perfect=1.0, always-negative is undefined/degenerate]` | the perfect classifier has AUC=1.0; the always-negative classifier produces only a single (FPR,TPR) point rather than a continuous curve, so its AUC in the usual sense is degenerate/undefined | — |
| E2 | explain | short-answer | 0.80 | Explain why "area under the curve" mathematically equals "probability a random positive outranks a random negative." | as the threshold sweeps from highest to lowest score, the ROC curve traces, at each threshold, the fraction of positives already captured (TPR) versus the fraction of negatives falsely captured (FPR); integrating this relationship is mathematically identical to computing, over all (positive, negative) pairs, the fraction where the positive's score exceeds the negative's — a known equivalence closely related to the Mann-Whitney U statistic *(required: connects the threshold-sweep integration to the pairwise-ranking count, even informally)* | — |
| E3 | explain | short-answer | 0.95 | Explain why an ROC curve is invariant to a monotonic transformation of scores (e.g. taking the log of every probability), while fixed-threshold accuracy is not. | the ROC curve depends only on the *ranking* scores induce among examples across every threshold, and a monotonic transformation preserves that ranking exactly, even though it changes the numeric values; accuracy at one fixed threshold depends on the actual scores relative to that specific cutoff, which a monotonic transformation can shift arbitrarily *(required: names the ranking-invariance property specifically, contrasted against threshold-dependence)* | — |
| E4 | explain | short-answer | 1.10 | Explain why "the ROC curve stays entirely above the diagonal" is a stronger statement than "AUC exceeds 0.5." | AUC exceeding 0.5 only guarantees beating random guessing *on average* across all thresholds — a curve could dip below the diagonal at some thresholds while still averaging above 0.5 overall, with higher area elsewhere compensating; staying entirely above the diagonal is the stronger, pointwise guarantee of beating random guessing at *every single threshold* *(required: distinguishes the average/aggregate guarantee from the pointwise-everywhere guarantee)* | — |
| E5 | explain | short-answer | 1.25 | Explain why two classifiers can have identical AUC yet make meaningfully different tradeoffs for a specific deployment. | AUC aggregates performance across the entire range of thresholds into one number, but a real deployment operates at only one (or a narrow range of) actual threshold; two curves can trade area evenly — one better at low-FPR thresholds, the other at high-FPR — averaging to the same AUC while being genuinely unequal at the specific operating point deployment would use, generalizing A5's crossing-curves example *(required: the aggregate-versus-single-operating-point argument, generalizing A5)* | — |
| T2 | transfer | short-answer | 1.45 | Why might a fraud-detection team focused on very-low-FPR performance find AUC a poor primary metric, despite it being a standard general-purpose choice? | AUC integrates equally over the entire FPR range from 0 to 1, including high-FPR regions irrelevant to a team that will only ever operate at a very low FPR in practice; two models could share identical AUC while differing substantially in the low-FPR region that actually matters, so partial AUC (restricted to the relevant range) or precision at a fixed low recall better reflects what the team cares about *(required: names the irrelevant-region-dilution problem and gestures at a better-targeted alternative)* | — |
| T3 | transfer | short-answer | 1.80 | Why does comparing AUC across two different datasets (e.g. last year's versus this year's) require caution, even holding the model fixed? | AUC, per its ranking interpretation, depends on the relative ease of separating positives from negatives in that specific dataset — a dataset with different inherent class separability (e.g. this year's fraud being subtler) produces a different AUC purely from that shift in difficulty, even if the model's genuine discriminative ability were unchanged, so an AUC drop conflates "the model got worse" with "the task got harder" *(required: distinguishes model-quality change from underlying task-difficulty change as competing explanations for an AUC shift)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.90…1.80.*

---

## K-Fold Cross-Validation (`k-fold-cross-validation`)
*Prereq: Training vs Validation vs Test Set, Overfitting and Underfitting · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | Describe k-fold cross-validation. | split into k equal folds; train on k−1, validate on the remaining fold, rotating which fold is held out; average the k validation scores | — |
| R2 | recall | mcq | 0.27 | The main advantage over a single train/validation split is: | every data point is used for validation exactly once, giving a more reliable estimate less dependent on one lucky/unlucky split | claims it "eliminates the need for a test set entirely" — cross-validation and a held-out test set serve different purposes → `k-fold-cross-validation` |
| A1 | apply | numeric | 0.77 | With 5-fold CV on 1000 examples, how many are in each validation fold, and how many for training per round? `[verified: 200, 800]` | 200 validation, 800 training per round | — |
| E1 | explain | short-answer | 1.47 | Why does averaging k validation scores give a more reliable estimate than a single split? | this is exactly `sample-mean`'s variance-reduction fact from the probability/statistics sweep, applied here: averaging several noisy estimates (each fold's score) reduces variance compared to relying on one *(required: the direct callback to averaging reducing variance, not just "more data is better")* | — |
| T1 | transfer | short-answer | 1.97 | Why is k-fold CV computationally more expensive than a single split, and when is that tradeoff worth it versus not? | it requires k separate full training runs instead of one; worth it for smaller datasets/cheaper models where the more reliable estimate matters and compute is affordable, but can become prohibitive for very large datasets or expensive models like large neural networks trained k times *(required: both sides of the tradeoff)* | — |
| R3 | recall | short-answer | −0.33 | Define leave-one-out cross-validation (LOOCV) in terms of k. | the special case where k equals n (the number of examples) — each fold leaves out exactly one example for validation | — |
| R4 | recall | mcq | 0.02 | Stratified k-fold cross-validation specifically ensures: | each fold preserves roughly the same class proportions as the full dataset | picks "each fold has exactly the same number of examples" — plain k-fold already balances fold sizes; stratification is specifically about class balance → `k-fold-cross-validation` |
| R5 | recall | short-answer | −0.23 | What are the two most common conventional choices for k in practice? | k=5 or k=10 | — |
| R6 | recall | mcq | 0.12 | The final reported cross-validation score is typically: | the mean of the k individual fold scores | picks "only the best-performing fold's score" — that would be a biased, overly optimistic selection rather than standard practice → `k-fold-cross-validation` |
| R7 | recall | short-answer | −0.43 | What additional summary statistic, beyond the mean, is commonly reported alongside the k fold scores? | the standard deviation (or variance) across the folds, as a measure of estimate stability | — |
| R8 | recall | mcq | 0.22 | Group k-fold cross-validation is specifically designed to prevent: | leakage from the same group (e.g. same patient) appearing in both a training fold and its corresponding validation fold | picks "leakage from the test set into cross-validation" — group k-fold is about grouped examples within the CV data itself → `k-fold-cross-validation` |
| R9 | recall | short-answer | −0.13 | True or false: preprocessing should be fit freshly inside each fold's training portion, not once on the whole dataset before CV begins. | true — fitting on the whole dataset first would leak fold-held-out information, echoing `data-leakage` | — |
| R10 | recall | mcq | −0.28 | For time-series data, standard random k-fold CV is generally: | inappropriate — a specialized time-series-respecting variant (e.g. rolling-origin CV) should be used instead | picks "perfectly fine, since cross-validation always accounts for data ordering automatically" — plain k-fold shuffles randomly, ignoring chronological order → `k-fold-cross-validation` |
| R11 | recall | short-answer | 0.32 | Define "nested cross-validation," briefly. | an outer CV loop for an unbiased performance estimate, wrapped around an inner CV loop used purely for hyperparameter tuning | — |
| R12 | recall | mcq | −0.18 | Repeated k-fold cross-validation (running the whole procedure multiple times with different splits) is done primarily to: | reduce the variance of the overall performance estimate by averaging over multiple partitions | picks "increase the total training set size used" — the available data doesn't grow; the point is a more stable estimate, not more data → `k-fold-cross-validation` |
| R13 | recall | short-answer | 0.37 | Fill in the blank: in k-fold CV, each example is used for validation exactly ___ time(s) across the whole procedure. | one (once) | — |
| R14 | recall | mcq | −0.08 | Increasing k (e.g. from 5 to 10) generally: | increases computational cost while typically reducing estimate bias, since each training fold uses a larger fraction of the data | picks "has no effect on computation, only on accuracy" — more folds directly means more separate training runs, a real cost → `k-fold-cross-validation` |
| R15 | recall | short-answer | 0.07 | True or false: LOOCV (k=n) is generally more computationally expensive than 5-fold or 10-fold CV. | true — it requires training n separate models, far more than 5 or 10 for any reasonably sized dataset | — |
| R16 | recall | mcq | −0.38 | The main purpose of nested cross-validation's outer loop, as distinct from the inner loop, is to: | provide an unbiased final performance estimate, uncontaminated by the inner loop's hyperparameter-tuning decisions | picks "tune the hyperparameters directly" — that's specifically the inner loop's job → `k-fold-cross-validation` |
| A2 | apply | numeric | 0.50 | 10-fold CV is run on 1,000 examples. How many examples are in each validation fold? `[verified: 100]` | 1000/10=100 | — |
| A3 | apply | numeric | 0.60 | Fold scores from a 5-fold run: 0.82, 0.79, 0.85, 0.81, 0.83. Compute the mean CV score. `[verified: 0.82]` | (0.82+0.79+0.85+0.81+0.83)/5=0.82 | — |
| A4 | apply | short-answer | 0.70 | A 50-example dataset uses LOOCV. How many separate models must be trained in total? `[verified: 50]` | 50, one per left-out example | — |
| A5 | apply | short-answer | 0.85 | A medical dataset has multiple scans per patient, and plain k-fold CV is used without grouping. What problem is likely, and what fix should be used? | the same patient's scans could split across a training fold and its validation fold, causing group leakage; group k-fold CV, keeping all of a patient's scans in the same fold, should be used instead | — |
| A6 | apply | short-answer | 0.95 | A team wants both to tune hyperparameters AND get an unbiased final estimate, using only cross-validation (no separate test set). What CV approach should they use, and why not plain k-fold alone? | nested cross-validation — using plain single-level k-fold for both tuning and reporting would let the hyperparameter choice implicitly overfit to the same folds used for the final report, echoing the leakage-through-decision-making mechanism from `data-leakage` | — |
| E2 | explain | short-answer | 1.20 | Explain why increasing k reduces bias but increases variance in the CV estimate, connecting to how much data each fold actually uses. | with larger k, each training fold uses a larger fraction of the data, closer to the full dataset, so the fold's model more closely resembles one trained on the complete dataset, reducing bias; but each validation set is smaller and the k training sets increasingly overlap, making the fold scores more correlated and the aggregate estimate's variance higher *(required: connects fold-size to bias and inter-fold overlap/correlation to variance, not just asserting the tradeoff)* | — |
| E3 | explain | short-answer | 1.35 | Explain why fitting preprocessing inside each fold gives a more honest estimate of a real deployment pipeline's variability, not just avoiding leakage. | in real deployment, preprocessing statistics will be fit fresh on whatever training data is available at the time; refitting inside each fold simulates exactly that, capturing how much the preprocessing itself varies across different training subsets — a genuine source of variability a single upfront preprocessing step would hide entirely *(required: names the captured-preprocessing-variability point, beyond simply "it avoids leakage")* | — |
| E4 | explain | short-answer | 1.50 | Explain why stratified k-fold matters more as class imbalance grows more severe, using an extreme unstratified example. | with severe imbalance (e.g. 1% positive), an unstratified random fold could, by chance, end up with very few or zero positive examples in its validation portion, making that fold's recall essentially meaningless; stratification guarantees every fold retains a representative minority-class share, increasingly necessary — not just nice-to-have — as the minority share shrinks *(required: the concrete zero-or-near-zero-minority-examples-in-a-fold extreme case)* | — |
| E5 | explain | short-answer | 1.65 | Explain why the standard deviation across fold scores is informative beyond the mean, using two models with the same mean but different spreads. | two models could share a mean CV score of 0.80, but one has fold scores tightly clustered (0.79, 0.80, 0.81) while the other varies widely (0.65, 0.80, 0.95) — the first is consistent and predictable across data subsets, while the second is highly sensitive to which examples land in each split, a meaningfully riskier property the mean alone completely hides *(required: the concrete same-mean-different-spread contrast and naming the consistency-versus-sensitivity distinction)* | — |
| T2 | transfer | short-answer | 1.75 | Why might a practitioner with a very large dataset skip k-fold CV for a single train/val/test split, despite CV's greater rigor? | with enough data, even a single validation split already gives a stable, low-variance estimate on its own, so CV's main advantage — reducing variance by using data more efficiently — has diminishing returns; meanwhile training k separate models instead of one can become computationally prohibitive at that scale, making a single split genuinely favorable despite CV's theoretical rigor *(required: names both the diminishing-statistical-benefit-at-scale point and the real computational-cost tradeoff)* | — |
| T3 | transfer | short-answer | 2.10 | Why does a large gap between a model's LOOCV score and its 5-fold CV score potentially signal something about the dataset, rather than just "CV noise"? | LOOCV's training folds each differ from the full dataset by a single example, making its fold models nearly identical to one trained on the whole dataset, while 5-fold's training folds each omit a full 20%, training meaningfully different models; a large systematic gap suggests sensitivity to a moderate amount of missing training data that a single held-out example never reveals — potentially a dataset too small relative to the model's genuine data requirements, or an unstable model *(required: names the near-identical-LOOCV-models-versus-meaningfully-different-5-fold-models mechanism as the source of the gap)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.43…2.10.*

---

## Hyperparameters (`hyperparameters`)
*Prereq: K-Fold Cross-Validation · ancestors 19 · b₀ = 1.00*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.0 | Define a hyperparameter, with an example. | a setting chosen before training, controlling the learning process or model complexity — not learned directly from data via the optimization process; e.g. learning rate, number of trees, k in KNN | — |
| R2 | recall | mcq | 0.3 | Hyperparameters are typically tuned using: | the validation set (via cross-validation), not the training set | claims they're tuned "the same way model parameters are learned," directly on the training set → `hyperparameters` |
| A1 | apply | short-answer | 0.8 | Is the learning rate a parameter or a hyperparameter? Is the learned weight vector? | learning rate: hyperparameter (set before training); weight vector: parameter (learned from data during training) | — |
| E1 | explain | short-answer | 1.5 | Why can't hyperparameters simply be learned via gradient descent on the training loss, the way parameters are? | letting training loss choose hyperparameters like model complexity would always favor more complexity (lower training loss achievable that way), leading straight to overfitting; hyperparameter tuning needs the separate validation signal precisely because training loss alone is a biased guide for these choices *(required: the "training loss always prefers more complexity" mechanism)* | — |
| T1 | transfer | short-answer | 2.0 | Why does grid search become infeasible as the number of hyperparameters grows, and what's typically used instead? | trying every combination across several hyperparameters blows up exponentially in the number of hyperparameters — the same exponential mechanism as `curse-of-dimensionality`; random search or Bayesian optimization are preferred for tuning many hyperparameters at once, since they don't require exhaustively covering the grid *(required: the explicit exponential-blowup connection)* | — |
| R3 | recall | short-answer | −0.30 | List three example hyperparameters found in common ML algorithms. | e.g. learning rate, regularization strength (λ), number of trees, k (KNN), max depth, batch size, number of layers | — |
| R4 | recall | mcq | 0.05 | Grid search tunes hyperparameters by: | exhaustively trying every combination from a predefined discrete set of values for each hyperparameter | picks "randomly sampling combinations from a distribution" — that's random search, not grid search → `hyperparameters` |
| R5 | recall | short-answer | −0.20 | Define random search, as a hyperparameter tuning method. | randomly sampling hyperparameter combinations from specified distributions/ranges, rather than exhaustively trying every combination | — |
| R6 | recall | mcq | 0.15 | Bayesian optimization for hyperparameter tuning works by: | building a probabilistic model of how hyperparameters affect performance, then using it to choose promising combinations to try next | picks "trying every combination in a fixed grid, just in a different order" — that's still exhaustive grid search → `hyperparameters` |
| R7 | recall | short-answer | −0.40 | What is the term for the range of possible values considered for each hyperparameter during tuning? | the search space | — |
| R8 | recall | mcq | 0.25 | True or false: a model's default hyperparameter values are guaranteed to be optimal for any new dataset. | false — defaults are reasonable general-purpose starting points, not guaranteed optimal for a specific dataset | picks "true — defaults are mathematically derived to be universally optimal" → `hyperparameters` |
| R9 | recall | short-answer | −0.10 | State one hyperparameter commonly searched over on a log scale rather than a linear one, and why. | learning rate — its effect often spans several orders of magnitude, so a log scale samples that range more evenly | — |
| R10 | recall | mcq | −0.45 | "Early stopping patience" (epochs to wait without improvement before stopping) is: | a hyperparameter — set before training, controlling the training process, not learned from data | picks "a parameter, since it's related to the loss during training" — it's set by the practitioner beforehand, not learned via the optimization process → `hyperparameters` |
| R11 | recall | short-answer | 0.35 | True or false: different algorithms can have vastly different numbers of tunable hyperparameters. | true — e.g. plain linear regression has very few, while a deep neural network can have dozens | — |
| R12 | recall | mcq | −0.15 | Excessive hyperparameter tuning against a single fixed validation set can itself lead to: | overfitting to that validation set, giving an overly optimistic performance estimate | picks "no risk at all, since hyperparameters aren't learned via gradient descent" — the method of setting them doesn't exempt tuning from overfitting to whatever data guides the choice → `hyperparameters` |
| R13 | recall | short-answer | 0.45 | Fill in the blank: choices about a model's learning process made before training begins are collectively called ___. | hyperparameters | — |
| R14 | recall | mcq | −0.35 | In KNN, the parameter k (number of neighbors) is: | a hyperparameter — chosen before training/prediction, not learned via optimization | picks "a parameter, since it's a specific number used by the algorithm" — being a specific number doesn't make it learned; it's set by the practitioner → `hyperparameters` |
| R15 | recall | short-answer | 0.10 | True or false: regularization strength (λ) is typically treated as a hyperparameter rather than a model parameter. | true | — |
| R16 | recall | mcq | −0.25 | Which of grid search, random search, or Bayesian optimization is the most exhaustive at the highest cost for a fine-grained grid? | grid search | picks "random search, since it covers more of the space per unit compute" — grid search with a fine grid is the most exhaustive and most expensive of the standard options → `hyperparameters` |
| A2 | apply | numeric | 0.60 | A grid search tunes 2 hyperparameters, each with 4 candidate values. How many combinations are tried? `[verified: 16]` | 4×4=16 | — |
| A3 | apply | numeric | 0.70 | A grid search tunes 3 hyperparameters, each with 5 candidate values. How many combinations are tried? `[verified: 125]` | 5³=125 | — |
| A4 | apply | short-answer | 0.80 | A neural network has 6 tunable hyperparameters, each with 10 candidate values. Why is exhaustive grid search likely impractical? | it would require 10⁶=1,000,000 combinations, each a full training run — computationally infeasible for most practical budgets | — |
| A5 | apply | short-answer | 0.90 | A practitioner tunes learning rate over 0.0001 to 0.1. Linear or logarithmic search, and why? | logarithmically — the range spans three orders of magnitude, and a linear search would waste most sampled points at the large end, barely exploring the small end where meaningful differences often occur | — |
| A6 | apply | short-answer | 1.05 | A team tunes hyperparameters using 5-fold CV, then reports the best fold-averaged CV score as their final expected real-world performance. What's the risk? | the reported score was itself used to select the hyperparameters, so it's optimistically biased — a genuinely separate held-out test set, untouched by tuning, should provide the final reported estimate | — |
| E2 | explain | short-answer | 1.25 | Explain why grid search's cost grows exponentially with the number of hyperparameters, using the combinatorics directly. | if each of h hyperparameters has v candidate values, the total combinations to try is v^h — each additional hyperparameter multiplies the total by another factor of v, so cost grows exponentially in h even though it grows only polynomially in v *(required: the explicit v^h formula and naming which variable the exponential growth is in)* | — |
| E3 | explain | short-answer | 1.40 | Explain why random search can sometimes outperform grid search at the same compute budget, particularly when only a few hyperparameters actually matter. | grid search spreads its budget evenly across combinations, including many wasted ones that vary an unimportant hyperparameter while barely varying an important one; random search samples each hyperparameter's value independently every trial, so across the same number of trials it explores far more distinct values of each individual hyperparameter — when only a few genuinely matter, this gives more effective coverage of the dimensions that actually influence performance *(required: the distinct-values-per-dimension argument, specifically for the case where few hyperparameters matter)* | — |
| E4 | explain | short-answer | 1.55 | Explain why Bayesian optimization can find good hyperparameters with fewer trials than random or grid search, at the cost of more overhead per trial. | it builds and updates a probabilistic model of how hyperparameters map to performance after every trial, using that model to intelligently choose the next combination (balancing exploring uncertain regions against exploiting known-good ones) rather than sampling blindly or gridding exhaustively; this informed choice typically needs fewer total runs, at the cost of the extra computation to fit and query the model between trials *(required: names the model-guided-next-choice mechanism and the fewer-trials-more-overhead-per-trial tradeoff)* | — |
| E5 | explain | short-answer | 1.70 | Explain why tuning against a single fixed validation set risks selecting hyperparameters "lucky" for that split rather than genuinely good. | a single validation split is itself a noisy sample of true performance, and different hyperparameter combinations can, by chance, align well or poorly with the specific examples that split happened to include; without averaging over multiple splits, as cross-validation does, tuning can be misled by this noise into selecting hyperparameters that overfit to that one split's quirks rather than ones that genuinely generalize best *(required: names the single-split-is-a-noisy-sample argument, distinguishing it from genuine hyperparameter quality)* | — |
| T2 | transfer | short-answer | 1.80 | Why might a company training very expensive large models prefer Bayesian optimization even more strongly than a smaller-scale project would? | the fewer-total-trials advantage of Bayesian optimization (E4) becomes proportionally far more valuable as each trial's cost grows — saving even a handful of runs on an extremely expensive model represents enormous savings, easily outweighing the modest per-trial overhead, whereas for cheap fast-training models that overhead might not be worth it relative to just brute-forcing more trials with random search *(required: connects the per-trial-cost scale specifically to when Bayesian optimization's tradeoff becomes worthwhile)* | — |
| T3 | transfer | short-answer | 2.15 | Why does "hyperparameter tuning" blur into "architecture search" for deep learning, and why might this distinction matter less in practice than it seems? | strictly, the number of layers, widths, and connectivity fit the hyperparameter definition, yet they're discussed separately as "architecture" because they qualitatively reshape what the model even is, not just how it learns; in practice, neural architecture search (NAS) treats these choices with the same optimization techniques (random search, Bayesian optimization) used for ordinary hyperparameters, since from the search algorithm's perspective they're just another dimension of a search space — the conceptual distinction matters for how humans reason about the choices, not for how the search itself is conducted *(required: names both why the terms are conventionally separated and why the underlying search methodology treats them identically)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.45…2.15.*

---

## Sensitivity Analysis (`sensitivity-analysis`)
*Prereq: Hyperparameters · ancestors 20 · b₀ = 1.02*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | Define sensitivity analysis. | systematically varying a model's inputs or hyperparameters to see how much the output/performance changes — assessing robustness and which factors matter most | — |
| R2 | recall | mcq | 0.32 | A model whose predictions change wildly with tiny hyperparameter changes is: | sensitive/unstable with respect to that hyperparameter | calls this "well-regularized" — the opposite is true | — |
| A1 | apply | short-answer | 0.82 | Accuracy is 85% at learning rate 0.01, 84% at 0.011, but only 40% at 0.015. What does this suggest, and what caution follows? | a genuinely sensitive/unstable region beyond 0.011, possibly reflecting the onset of divergence; practically, avoid settling on a value too close to this instability boundary even if it currently tests well, since small real-world variations (a different seed, slightly different data) could push performance into the unstable region *(required: the caution about proximity to instability, not just "it's sensitive here")* | — |
| E1 | explain | short-answer | 1.52 | How does sensitivity analysis differ conceptually from ordinary hyperparameter tuning? | tuning searches for the single best validation score; sensitivity analysis instead asks how much performance changes as hyperparameters vary *near* a chosen setting, providing information about the robustness of that choice rather than which choice scores highest *(required: the different question each one asks)* | — |
| T1 | transfer | short-answer | 2.02 | Why should a safety-critical deployment (medical diagnosis, autonomous driving) care about sensitivity analysis even after finding hyperparameters with the best validation score? | a model perched at the edge of an unstable region (as in A1) might behave unpredictably once real-world data drifts even slightly from the validation distribution — the best validation score alone says nothing about how close to a cliff that setting sits *(required: connects to A1's instability-edge risk specifically)* | — |
| R3 | recall | short-answer | −0.28 | Define "one-at-a-time" (OAT) sensitivity analysis. | varying a single input/hyperparameter while holding all others fixed, to observe its individual effect on the output | — |
| R4 | recall | mcq | 0.07 | Global sensitivity analysis, as opposed to local, specifically captures: | interactions between multiple inputs varying simultaneously, not just each one's isolated individual effect | picks "the same information as local sensitivity analysis, just computed faster" — global methods specifically add interaction effects that one-at-a-time local methods cannot capture → `sensitivity-analysis` |
| R5 | recall | short-answer | −0.18 | What visualization tool is commonly used to rank inputs by how much their variation affects the output? | a tornado diagram (bars sorted by sensitivity magnitude) | — |
| R6 | recall | mcq | 0.17 | Sensitivity analysis can be applied to: | both hyperparameters and input features/data, not just hyperparameters alone | picks "only hyperparameters, never the input data itself" — sensitivity analysis is a general technique also used to study how perturbing input features affects predictions → `sensitivity-analysis` |
| R7 | recall | short-answer | −0.38 | True or false: sensitivity analysis originated specifically for ML and has no broader engineering usage. | false — it has a long history in engineering, economics, and other quantitative fields predating its ML applications | — |
| R8 | recall | mcq | 0.27 | A model whose predictions barely change under small input perturbations is described as: | robust (or stable) to that input's variation | picks "sensitive" — the opposite; barely changing means low sensitivity, i.e. robustness → `sensitivity-analysis` |
| R9 | recall | short-answer | −0.08 | State one practical output metric commonly tracked during a sensitivity analysis sweep. | model performance (accuracy or loss), or alternatively the model's raw prediction value | — |
| R10 | recall | mcq | −0.43 | Compared to an ablation study (removing a component entirely), sensitivity analysis typically involves: | continuously varying a setting's value, rather than a binary present/absent removal | picks "the exact same procedure, just under a different name" — ablation studies component removal, a related but different technique → `sensitivity-analysis` |
| R11 | recall | short-answer | 0.37 | Fill in the blank: sensitivity analysis performed by perturbing input features is closely related to studying a model's ___ to adversarial or noisy inputs. | robustness | — |
| R12 | recall | mcq | −0.13 | A "local" sensitivity analysis specifically examines behavior: | near one particular chosen setting/point, rather than across the whole plausible range | picks "across the entire global range of every input simultaneously" — that describes global sensitivity analysis, the opposite scope → `sensitivity-analysis` |
| R13 | recall | short-answer | 0.47 | True or false: sensitivity analysis can help identify which of several hyperparameters is most important to tune carefully. | true — hyperparameters the output is highly sensitive to deserve more careful tuning than ones the output barely responds to | — |
| R14 | recall | mcq | −0.23 | One-at-a-time sensitivity analysis has a specific limitation: it: | cannot detect interaction effects between two or more inputs varying together | picks "always requires far more compute than global sensitivity analysis" — OAT is typically cheaper, not more expensive → `sensitivity-analysis` |
| R15 | recall | short-answer | 0.02 | True or false: sensitivity analysis is inherently tied to one specific ML algorithm and can't apply broadly across model types. | false — it's a general technique applicable to essentially any model or system with tunable inputs and a measurable output | — |
| R16 | recall | mcq | 0.57 | In a tornado diagram, the input near the top with the widest bar represents: | the input the output is most sensitive to | picks "the input that was tested first, in the order it was analyzed" — tornado diagrams are sorted by sensitivity magnitude, not testing order → `sensitivity-analysis` |
| A2 | apply | short-answer | 0.65 | Accuracy is 85% at batch size 32, 84% at 64, 83% at 128. Is performance highly sensitive to batch size here? | relatively low sensitivity — accuracy changes only modestly (85% to 83%) across a fourfold range of the hyperparameter | — |
| A3 | apply | short-answer | 0.75 | The same model's accuracy is 85% at learning rate 0.01, 60% at 0.02, 20% at 0.05. Which hyperparameter is the model more sensitive to, batch size or learning rate? | learning rate — a small change produces a much larger swing in accuracy than the same relative change in batch size | — |
| A4 | apply | short-answer | 0.85 | OAT analysis on hyperparameters A and B shows each individually has a mild effect, yet varying both together produces a large unexpected drop. What does this reveal, and what analysis would catch it? | an interaction effect between A and B that OAT, by design, cannot detect; a global sensitivity analysis, varying multiple inputs simultaneously, would be needed | — |
| A5 | apply | short-answer | 1.00 | A tornado diagram shows learning rate with a much wider bar than any other hyperparameter. What practical conclusion should a team draw? | learning rate deserves the most careful/precise tuning effort, since performance is most sensitive to it relative to the other hyperparameters shown | — |
| A6 | apply | short-answer | 1.15 | A sensitivity analysis perturbs input features slightly with small random noise and finds predictions barely change. What property does this demonstrate? | robustness to small input perturbations/noise | — |
| E2 | explain | short-answer | 1.30 | Explain why OAT's inability to detect interaction effects can lead a practitioner to miss a genuinely important joint hyperparameter setting. | OAT holds all but one hyperparameter fixed at a baseline while sweeping the other, exploring only "cross-shaped" slices through the full space, one dimension at a time; a setting where two hyperparameters must move together away from that baseline to reveal a strong joint effect lies off every one of those slices and is systematically invisible to OAT, however finely each sweep is resolved *(required: the cross-shaped-slices-through-the-space argument, explaining specifically why joint effects are invisible)* | — |
| E3 | explain | short-answer | 1.45 | Explain why a model being "sensitive" to a hyperparameter isn't automatically bad, giving a case where high sensitivity is actually desirable. | high sensitivity just means the output responds strongly to that input's variation; for a hyperparameter that genuinely controls something important (e.g. regularization strength trading off underfitting against overfitting), strong sensitivity is exactly what you'd expect, confirming it's doing meaningful work — a hyperparameter expected to matter showing near-zero sensitivity might itself be the surprising, worth-investigating result *(required: gives a concrete case where sensitivity is expected/desirable, not framing all sensitivity as bad)* | — |
| E4 | explain | short-answer | 1.60 | Explain why sensitivity analysis on input features and on hyperparameters, despite the same perturb-and-measure methodology, answer fundamentally different practical questions. | perturbing hyperparameters and observing performance changes answers "how carefully must I tune this setting," a question about the *model development process*; perturbing input features and observing prediction changes answers "how much does the model's output depend on this information, and how robust is it to noise or adversarial changes," a question about the *deployed model's real-world behavior* — the same mechanical technique answers two genuinely different kinds of question *(required: explicitly separates the development-process question from the deployed-model-robustness question)* | — |
| E5 | explain | short-answer | 1.75 | Explain why a local sensitivity analysis at one baseline setting might not generalize to a very different baseline setting. | a local sensitivity analysis measures response to small perturbations specifically around the one baseline point it was computed at; if the underlying input-output relationship is nonlinear, as ML performance often is, the sensitivity — the local slope of that relationship — can genuinely differ at a different point in the range, so "this hyperparameter barely matters" near one baseline might not hold near a very different baseline value *(required: the nonlinearity-means-locally-varying-slope argument)* | — |
| T2 | transfer | short-answer | 1.90 | Why might financial regulators specifically require a documented sensitivity analysis for a risk model, beyond just good backtested performance? | overall backtested performance can look strong while masking that the model's output swings wildly for small, plausible changes in a handful of inputs — regulators specifically care whether risk assessments are stable under the kind of input variation that could genuinely occur, which aggregate backtested performance doesn't directly demonstrate; a documented sensitivity analysis provides exactly this evidence of stability, or reveals its absence, in a way aggregate accuracy cannot *(required: names the aggregate-performance-can-mask-instability distinction and why regulators specifically need the stability evidence)* | — |
| T3 | transfer | short-answer | 2.30 | Why does sensitivity analysis become dramatically more expensive and require different techniques for a deep model with millions of parameters, compared to one with a handful of interpretable hyperparameters? | with a handful of hyperparameters, sweeping each one, or even a modest grid, is tractable; a deep model's millions of weights make a naive per-weight sweep utterly infeasible, so practitioners instead rely on aggregate techniques — computing gradients of the output with respect to inputs or parameter groups all at once, since a gradient is itself a first-order local sensitivity measure, or examining sensitivity at a higher level like layers or attention heads rather than individual weights — trading fine-grained per-input sweeping for a gradient-based, structure-aware approach *(required: names both the infeasibility-at-scale problem and the gradient-based/structural resolution actually used)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.43…2.30.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| OvR and native multiclass methods conflated | `multiclass-classification` |
| OvR and OvO classifier counts swapped | `multiclass-classification` |
| confusion-matrix cells treated as unrelated to Type I/II error | `confusion-matrices` |
| precision and recall formulas swapped | `confusion-matrices` |
| macro- and micro-averaging conflated | `confusion-matrices` |
| ROC baseline (0.5) confused with the "always wrong" extreme (0.0) | `roc-curves` |
| ROC axes (TPR/FPR) confused with precision-recall's axes | `roc-curves` |
| AUC's ranking interpretation confused with plain accuracy | `roc-curves` |
| CV believed to replace the need for a held-out test set | `k-fold-cross-validation` |
| LOOCV and standard k-fold classifier counts swapped | `k-fold-cross-validation` |
| time-series CV assumed safe to shuffle like ordinary k-fold | `k-fold-cross-validation` |
| hyperparameters tuned directly against training loss | `hyperparameters` |
| grid search assumed to scale linearly in hyperparameter count | `hyperparameters` |
| tunable settings like k in KNN or early-stopping patience mistaken for learned parameters | `hyperparameters` |
| sensitivity analysis conflated with ordinary tuning | `sensitivity-analysis` |
| one-at-a-time sensitivity analysis assumed to capture interaction effects | `sensitivity-analysis` |
| local and global sensitivity analysis scopes swapped | `sensitivity-analysis` |

**Cluster total: 180 items across 6 concepts (30 each).** All numeric claims verified.
