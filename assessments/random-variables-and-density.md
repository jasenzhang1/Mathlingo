# Cluster 2 — Random Variables & Density Machinery

Random Variables → Variance (7 concepts). Same format as [Cluster 1](foundations-of-probability.md).

**A structural note worth stating up front**, because it shaped every item below: `random-variables`
and `discrete-vs-continuous-random-variables` sit *above* PMF, PDF, and CDF in the graph, not below
them. An item filed under those two concepts cannot lean on density or mass-function formalism to
make its point — that machinery hasn't been unlocked yet. Several early drafts of these items reached
for "the density is..." as a shortcut and had to be rewritten to argue from countable additivity and
monotonicity alone instead. The rewritten versions are better items, not just compliant ones: they
show the property follows from the axioms, rather than from a formula the learner has half-memorized.

---

## Random Variables (`random-variables`)
*Prereq: Probability Function · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | mcq | −0.7 | A random variable is best described as: | a function from the sample space Ω to the real numbers | picks "a variable that changes randomly over time" — treating X as an intrinsically random quantity rather than a deterministic function of the outcome ω → `random-variables` |
| R2 | recall | short-answer | −0.5 | Why must X satisfy a measurability condition — that {ω: X(ω)≤x} is an event for every x — rather than being an arbitrary function Ω→ℝ? | so that P(X≤x) is well-defined; if the preimage weren't in the σ-algebra, we couldn't assign it a probability at all *(required)* | — |
| A1 | apply | short-answer | 0.1 | Ω={HH,HT,TH,TT} (fair coin twice), X = number of heads. Give the range of X and P(X=x) for each x. `[verified]` | X∈{0,1,2}; P(0)=1/4, P(1)=1/2, P(2)=1/4 | lists 4 outcomes as 4 values of X, not noticing HT and TH collapse to the same X-value → `random-variables` |
| A2 | apply | short-answer | 0.25 | Two dice are rolled. X = sum, Y = max. Give the range of each — note they differ even though both are defined on the same Ω. | X∈{2,…,12}; Y∈{1,…,6} | — |
| E1 | explain | derivation | 0.8 | Show that events like {a<X≤b} can be built from events of the form {X≤c}. | {a<X≤b} = {X≤b} ∖ {X≤a} *(required)* — this is the fact that later makes the CDF alone sufficient to answer every question about X | — |
| E2 | explain | short-answer | 0.95 | Explain the distinction between the random variable X (a function) and a value x it might take, and why P(X=x) only parses once you keep the two separate. | X is the pre-experiment function; x is a fixed real number; P(X=x) is shorthand for P({ω : X(ω)=x}) *(required)* | writes "P(X)" as if X itself were a probability → `random-variables` |
| T1 | transfer | short-answer | 1.3 | A forecaster says "30% chance of rain tomorrow, and if it rains, some amount will fall." Model this with a single random variable Y = rainfall amount (0 if no rain). Explain why Y needs both a discrete "spike" of probability at 0 *and* a continuous spread over positive values — you don't need to compute anything, just describe the shape. | identifies Y as neither purely discrete nor purely continuous: P(Y=0) > 0 is a genuine point mass, while the rainfall-given-rain part spreads continuously over (0,∞) *(required)* — this is the seed of the discrete/continuous distinction the next concept makes precise | assumes every random variable must be one or the other → `random-variables` |
| R3 | recall | mcq | −1.55 | The notation X(ω) refers to: | the value the random variable X assigns to the specific outcome ω | picks "the probability of outcome ω" — confuses the random variable's *value* with a probability → `random-variables` |
| R4 | recall | short-answer | −1.45 | Define the "range" (or support) of a random variable X. | the set of all values X(ω) can actually take as ω ranges over Ω | — |
| R5 | recall | short-answer | −1.35 | True or false: two different random variables can be defined on the same sample space Ω. | true — e.g. X=sum and Y=max of two dice rolls, both on the same Ω | — |
| R6 | recall | short-answer | −1.25 | Define the indicator random variable 1_A for an event A. | 1_A(ω) = 1 if ω∈A, and 0 otherwise | — |
| R7 | recall | mcq | −1.15 | For X: Ω→ℝ to be a valid random variable, it must satisfy: | {ω: X(ω)≤x} is an event (in the σ-algebra) for every real x | picks "X must take only finitely many values" → `random-variables` |
| R8 | recall | short-answer | −1.05 | Fill in the blank: P(X=x) is shorthand for P({ω : ___}). | X(ω) = x | — |
| R9 | recall | short-answer | −0.95 | True or false: a random variable is not itself random — the randomness comes from which outcome ω occurs, while X is a fixed, deterministic function. | true | — |
| R10 | recall | mcq | −0.85 | Which of these is NOT a valid example of "a random variable as a function of ω"? | "the coin itself" | picks "the number of heads in 3 flips" as invalid, when it's a perfectly good random variable → `random-variables` |
| R11 | recall | short-answer | −0.75 | For X = sum of two fair dice, what is the range of X? | {2, 3, 4, …, 12} | — |
| R12 | recall | short-answer | −0.65 | True or false: two random variables defined on the same Ω can have different ranges. | true — X=sum∈{2,…,12} and Y=max∈{1,…,6} on the same two-dice Ω | — |
| R13 | recall | mcq | −0.55 | The indicator random variable 1_A takes values in: | {0, 1} | picks [0,1], confusing an indicator with a probability → `random-variables` |
| R14 | recall | short-answer | −0.45 | Fill in the blank: formally, a random variable is a function X: Ω → ___. | ℝ (the real numbers) | — |
| R15 | recall | mcq | −0.35 | Which best completes: "A random variable assigns..."? | ...a real number to every outcome in the sample space | picks "...a probability to every outcome" — that's P, not X → `random-variables` |
| R16 | recall | short-answer | −0.25 | True or false: the sample space Ω itself must already be a subset of ℝ before a random variable can be defined on it. | false — Ω can be anything (coin sequences, card hands, …); X is what maps it into ℝ | — |
| A3 | apply | short-answer | 0.15 | Three fair coins are flipped. X = number of heads. Give the range of X and P(X=x) for each x. `[verified: P(0)=1/8,P(1)=3/8,P(2)=3/8,P(3)=1/8]` | X∈{0,1,2,3}; P(0)=1/8, P(1)=3/8, P(2)=3/8, P(3)=1/8 | lists 8 equally likely outcomes but forgets several map to the same X-value, treating each outcome as its own X-value → `random-variables` |
| A4 | apply | short-answer | 0.3 | A fair die is rolled. Y = 1{roll is 6} (an indicator). Give the range of Y and P(Y=y) for each y. `[verified: P(0)=5/6, P(1)=1/6]` | Y∈{0,1}; P(Y=1)=1/6, P(Y=0)=5/6 | treats Y as ranging over {1,...,6} like the underlying die roll, not recognizing it's an indicator → `random-variables` |
| A5 | apply | short-answer | 0.4 | Two fair dice are rolled. Z = \|difference of the two rolls\|. Give the range of Z. | Z∈{0,1,2,3,4,5} | includes negative differences, forgetting the absolute value collapses the range → `random-variables` |
| A6 | apply | short-answer | 0.5 | A website visit either results in a purchase or not. Define a random variable that captures this, name its range, and say what kind of random variable it is. | e.g. X = 1{visit results in purchase}, an indicator random variable with range {0,1} | describes the scenario in words without ever defining X as a function with a stated range → `random-variables` |
| E3 | explain | derivation | 0.95 | Extend E1's argument: show that {X<x} (strict inequality) is also expressible from events of the form {X≤c}. | {X<x} = ⋃ₙ {X≤x−1/n} (a countable union over n=1,2,3,…), each of which is an event by the measurability condition, and a σ-algebra is closed under countable unions, so {X<x} is an event too *(required: the countable-union construction, not just asserting closure)* | asserts {X<x} is "obviously" an event without constructing it from the measurability condition | 
| E4 | explain | short-answer | 1.05 | Using the two-dice example (X=sum, Y=max), explain why a single random experiment can support many different random variables simultaneously. | a random variable is just *one particular summary function* of the outcome; nothing stops defining several different summaries (sum, max, min, difference, …) of the same underlying ω, each producing its own distribution *(required)* | — |
| E5 | explain | short-answer | 1.15 | For a *finite* Ω, the measurability condition ({X≤x} is an event) is automatically satisfied for any function X. Explain why the condition still matters as a general definition, even though it's "free" here. | for finite (or countable) Ω the power set is usually the whole σ-algebra, so every subset is measurable and the condition imposes no real restriction; but the definition has to work uniformly for *continuous* Ω too, where not every subset is measurable, so stating the condition in general keeps one definition valid everywhere *(required)* | — |
| T2 | transfer | short-answer | 1.4 | An online ad platform wants to model "did the user click the ad" as a random variable, to later compute click-through statistics. Define this random variable precisely (as a function of ω), name its type, and give its range. | X(ω) = 1 if user ω clicked, 0 otherwise — an indicator random variable, range {0,1}; ω itself ranges over "which user/session occurred," and X summarizes just the one bit of that outcome relevant to click-through analysis | describes "click-through rate" as if it were the random variable itself, rather than defining a per-user 0/1 random variable first → `random-variables` |
| T3 | transfer | short-answer | 1.45 | A single customer transaction ω determines both W = the dollar amount spent and V = the number of items purchased. Explain why W and V are both legitimate random variables on the same Ω, and why they need not have related ranges. | both are just different real-valued summaries of the same underlying outcome ω, exactly like X=sum and Y=max on two dice; W's range might be a continuum of dollar amounts while V's range is a small set of counts — nothing requires the two ranges to resemble each other *(required)* | assumes W and V must be "the same random variable" since they come from the same transaction → `random-variables` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.55…1.45 (3.0 logits).*

---

## Discrete vs Continuous Random Variables (`discrete-vs-continuous-random-variables`)
*Prereq: Random Variables · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | mcq | −0.6 | A continuous random variable is one whose range is uncountable and for which: | P(X=x) = 0 for every single value x | picks "takes non-integer values" — conflating *continuous* with *not an integer* (a variable taking values in {0.5, 1.5, 2.5, …} is still discrete) → `discrete-vs-continuous-random-variables` |
| R2 | recall | short-answer | −0.45 | Give an example of a random variable that is neither purely discrete nor purely continuous. | e.g. insurance claim size: P(claim = 0) > 0 (no claim filed), with a continuous spread over claim > 0 — the T1 example from the previous concept, now named | — |
| A1 | apply | short-answer | 0.2 | Classify each as discrete or continuous: (a) number of defective items in a batch of 100 (b) time until a server crashes (c) number of typos on a page (d) a room's temperature measured with infinite precision. | (a) discrete (b) continuous (c) discrete (d) continuous | classifies (b) as discrete because "time" is often measured in discrete units (seconds) in practice → `discrete-vs-continuous-random-variables` |
| A2 | apply | short-answer | 0.35 | X takes values in the countably *infinite* set {1, 2, 3, …}. Is X discrete? Why doesn't "discrete" mean "finite"? | yes, still discrete — discrete means *countable* (finite or countably infinite), not finite *(required)* | — |
| E1 | explain | short-answer | 0.9 | Without invoking a density function, explain why it doesn't make sense for a *continuous* random variable to assign positive probability to every individual value — use X = the exact height of a randomly chosen adult (infinite precision) as the example. | there are uncountably many possible exact values; if each carried probability p>0, even countably many of them would already force total probability above 1, contradicting P(Ω)=1 *(required: an argument from countable additivity, not an appeal to "there are too many values")* | — |
| E2 | explain | short-answer | 1.05 | Why can a discrete RV's full distribution be written as a table (value, probability), while this is impossible for a continuous one? | countably many point masses can be summed via countable additivity to exhaust probability 1; uncountably many points cannot be enumerated or summed this way — probability has to be assigned to *intervals* instead *(required)* | — |
| T1 | transfer | short-answer | 1.4 | A stock's daily return has a CDF that jumps by exactly 0.05 at return = −10% (a "circuit breaker" halts trading and clusters losses at exactly that value) and is otherwise smooth everywhere else. Is the underlying random variable discrete, continuous, or neither? | neither — mixed, same structure as the insurance-claim and rainfall examples: one genuine point mass (the circuit-breaker floor) plus a continuous spread elsewhere | forces a binary discrete-or-continuous answer → `discrete-vs-continuous-random-variables` |
| R3 | recall | mcq | −1.5 | A discrete random variable has a range that is: | countable (finite or countably infinite) | picks "always finite" → `discrete-vs-continuous-random-variables` |
| R4 | recall | short-answer | −1.4 | Define "countable" as used in "discrete means countable range." | finite, or countably infinite (able to be put in one-to-one correspondence with the positive integers) | — |
| R5 | recall | short-answer | −1.3 | True or false: a (purely) continuous random variable can have P(X=x) > 0 for some value x. | false — every single point has probability exactly 0 | — |
| R6 | recall | short-answer | −1.2 | Fill in the blank: for a continuous random variable X, P(X=x) = ___ for every value x. | 0 | — |
| R7 | recall | mcq | −1.1 | Which is an example of a discrete random variable? | the number of emails received in a day | picks "the exact weight of a package" — that's continuous → `discrete-vs-continuous-random-variables` |
| R8 | recall | mcq | −1.0 | Which is an example of a continuous random variable? | the exact weight of a package, measured with infinite precision | picks "the number of packages shipped" — that's discrete → `discrete-vs-continuous-random-variables` |
| R9 | recall | short-answer | −0.9 | True or false: "mixed" random variables — neither purely discrete nor purely continuous — exist. | true | — |
| R10 | recall | short-answer | −0.8 | What makes a random variable "mixed"? | it has at least one point with positive probability mass (a discrete-like spike) *and* a continuous spread of probability elsewhere | — |
| R11 | recall | mcq | −0.7 | A random variable taking only integer values is: | always discrete — integers are countable | picks "sometimes continuous, depending on the range" → `discrete-vs-continuous-random-variables` |
| R12 | recall | short-answer | −0.6 | True or false: a random variable with an uncountable range must be continuous (assuming no point masses). | true | — |
| R13 | recall | short-answer | −0.5 | Give an example of a discrete random variable with an infinite (but still countable) range. | e.g. the number of coin flips until the first head, which can in principle be any positive integer | — |
| R14 | recall | short-answer | −0.4 | Fill in the blank: for a continuous random variable, individual point probabilities are ___, so probability must instead be assigned to ___. | zero; intervals | — |
| R15 | recall | mcq | −0.3 | What actually distinguishes discrete from continuous random variables? | the countability of the range, not whether the values happen to be integers | picks "whether the values are whole numbers" → `discrete-vs-continuous-random-variables` |
| R16 | recall | short-answer | −0.2 | True or false: a random variable taking values in a finite set is always discrete. | true — finite is a special case of countable | — |
| A3 | apply | short-answer | 0.25 | Classify each: (a) number of goals in a soccer match (b) daily rainfall amount (c) number of atoms in a lab sample (d) exact human reaction time in seconds. | (a) discrete (b) continuous (c) discrete, even though astronomically large — still countable (d) continuous | classifies (c) as continuous because the count is enormous, conflating "very large" with "uncountable" → `discrete-vs-continuous-random-variables` |
| A4 | apply | short-answer | 0.35 | A random variable X takes values in {0, 0.5, 1, 1.5, 2, …} (multiples of 0.5). Is X discrete or continuous? | discrete — the set is countably infinite, even though the values aren't integers | classifies X as continuous because the values include non-integers like 0.5 → `discrete-vs-continuous-random-variables` |
| A5 | apply | short-answer | 0.45 | Give your own example (not rainfall or insurance) of a mixed random variable, and identify its point mass. | e.g. the payout of a lottery ticket: P(payout=0) > 0 (most tickets lose, a genuine point mass) with a continuous spread over positive payout amounts for the rare winning tickets | describes a scenario that is actually purely discrete (e.g. "number of prizes won") and mislabels it mixed → `discrete-vs-continuous-random-variables` |
| A6 | apply | short-answer | 0.55 | Is "the number of typos in a randomly selected published book" discrete or continuous? Justify briefly. | discrete — it's a count, taking values in {0,1,2,…}, a countable set, even though the upper limit isn't fixed in advance | argues it's continuous because a book "could in principle have any number" of typos, conflating "unbounded" with "uncountable" → `discrete-vs-continuous-random-variables` |
| E3 | explain | short-answer | 0.95 | Explain precisely why "countable" (not "finite") is the correct technical criterion for "discrete," using an example where the distinction matters. | a variable like "number of trials until first success" ranges over the infinite but still countable set {1,2,3,…}; if "discrete" meant only "finite," this variable would wrongly be classified as neither discrete nor continuous, when it's a completely standard discrete random variable *(required: the concrete example)* | — |
| E4 | explain | short-answer | 1.1 | Without naming either tool specifically, explain why classifying a random variable as discrete or continuous is a necessary *first step* before you can describe its distribution numerically. | discrete and continuous random variables need fundamentally different machinery to describe how probability is distributed over their values — a discrete variable can be described by listing probabilities at each point, while a continuous one needs a device describing probability density over intervals, since no single point carries positive probability *(required — without naming PMF/PDF explicitly, since they aren't unlocked yet)* | — |
| E5 | explain | short-answer | 1.2 | Explain why "the range consists only of integers" is neither necessary nor sufficient for a random variable to be discrete. | not necessary: R14's multiples-of-0.5 example is discrete without being integer-valued; not sufficient in the naive sense either — a variable could in principle be defined to take *every real number* value that happens to round to an integer in some reporting convention, while the true underlying variable is continuous *(required: both directions addressed)* | argues only one direction (e.g. "integers aren't required" but not the converse) → `discrete-vs-continuous-random-variables` |
| T2 | transfer | short-answer | 1.45 | A file's size in bytes is, strictly, a discrete random variable (an integer count of bytes). In practice, statisticians often model large file sizes with a continuous distribution (e.g. log-normal) anyway. Explain why this approximation is reasonable despite the strict discreteness. | when the countable range is extremely fine-grained relative to the scale of interest (millions of possible byte counts), a continuous approximation captures the shape of the distribution well and is far more tractable, even though no individual byte-count technically has probability given by a density — this is a modeling choice, not a claim that the variable is "actually" continuous | insists the continuous model must be technically wrong and therefore never appropriate, missing the practical-approximation argument → `discrete-vs-continuous-random-variables` |
| T3 | transfer | short-answer | 1.5 | A retail store tracks both N = the number of customers in a day (discrete) and S = the total amount spent that day (continuous, assuming no minimum purchase increment). Explain why modeling S as continuous is reasonable even though S is built from N discrete transactions, each with a discrete dollar-and-cents amount. | dollars-and-cents amounts are technically discrete (multiples of $0.01), but summed across many transactions with fine granularity relative to the scale of typical daily totals, a continuous approximation captures the aggregate distribution's shape well — exactly the same practical-approximation logic as T2, now applied to a sum rather than a single large count | treats S as automatically continuous "because it's money," without connecting it to the same fine-granularity argument used for N vs S → `discrete-vs-continuous-random-variables` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.50…1.50 (3.0 logits).*

---

## Cumulative Distribution Function (`cdf`)
*Prereq: Random Variables · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Define the CDF of a random variable X. | F(x) = P(X ≤ x) for every real x | defines it with strict inequality P(X<x) → `cdf` |
| R2 | recall | multi-select | −0.45 | Which must every CDF satisfy? | non-decreasing; right-continuous; F(x)→0 as x→−∞; F(x)→1 as x→+∞ | "must be continuous everywhere" picked as required — true only when X is purely continuous, not in general → `cdf` |
| A1 | apply | short-answer | 0.2 | X takes value 0 with probability 0.3 and value 1 with probability 0.7. Describe F(x) for all x. | F(x)=0 for x<0; F(x)=0.3 for 0≤x<1; F(x)=1 for x≥1 — a step function with jumps at the values X can take | draws F as continuous, smoothing over the jumps → `cdf` |
| A2 | apply | numeric | 0.35 | Using the F from A1, find P(X≤0.5), P(X<1), and P(X=1). `[verified]` | F(0.5)=0.3; P(X<1)=F(1⁻)=0.3; P(X=1)=F(1)−F(1⁻)=1−0.3=0.7 | computes P(X<1) as F(1)=1 instead of the left-limit → `cdf` |
| E1 | explain | derivation | 0.9 | Prove F is non-decreasing, using only monotonicity of probability. | for a<b, {X≤a}⊆{X≤b}, so F(a)=P(X≤a)≤P(X≤b)=F(b) by monotonicity *(required)* — monotonicity itself traces back to `probability-function` | — |
| E2 | explain | short-answer | 1.05 | Explain why P(X=x) = F(x) − F(x⁻) (the size of the jump at x), from the definition of F alone. | {X≤x} = {X<x} ⊔ {X=x}; taking the limit of F as the left endpoint approaches x from below gives F(x⁻)=P(X<x), so the gap is exactly P(X=x) *(required)* | — |
| T1 | transfer | short-answer | 1.4 | *(Companion to the mixed-distribution transfer item above.)* A grading curve's CDF is smooth everywhere except a single jump of size 0.02 exactly at the passing score, because ties at that score are rounded up. What does the jump tell you, on its own, without any other information about the curve? | a jump of size 0.02 at a point means exactly that much probability mass sits *at that single value* — P(score = passing) = 0.02, regardless of what the rest of the distribution looks like | reads the jump as information about the *slope* of F near that point rather than a point mass at it → `cdf` |
| R3 | recall | mcq | −1.5 | Which formula defines the CDF F(x)? | F(x) = P(X ≤ x) | picks F(x) = P(X = x), confusing the CDF with the point-mass function → `cdf` |
| R4 | recall | short-answer | −1.4 | Fill in the blank: F is non-decreasing means x₁ < x₂ implies F(x₁) ___ F(x₂). | ≤ | — |
| R5 | recall | short-answer | −1.3 | True or false: F(x) → 1 as x → +∞. | true | — |
| R6 | recall | short-answer | −1.2 | True or false: F(x) → 0 as x → −∞. | true | — |
| R7 | recall | mcq | −1.1 | "Right-continuity" of F means: | the limit of F(x) as x approaches a point from the right equals F at that point exactly | picks "F is continuous everywhere," which is only true for purely continuous X → `cdf` |
| R8 | recall | short-answer | −1.0 | Define F(x⁻), the left-hand limit of F at x. | the limit of F(t) as t approaches x from below, i.e. P(X < x) | — |
| R9 | recall | short-answer | −0.9 | Fill in the blank: P(a < X ≤ b) = ___. | F(b) − F(a) | — |
| R10 | recall | mcq | −0.8 | For a purely continuous random variable, the CDF is: | continuous everywhere (no jumps) | picks "always a step function" — that describes the discrete case → `cdf` |
| R11 | recall | short-answer | −0.7 | What does a jump in F at a point x indicate? | that x carries positive probability mass, P(X=x) > 0 | — |
| R12 | recall | short-answer | −0.6 | True or false: F can decrease over some interval. | false — F is non-decreasing everywhere, by definition | — |
| R13 | recall | mcq | −0.5 | The values F(x) can take are always bounded within: | [0, 1] | picks (−∞, ∞) → `cdf` |
| R14 | recall | short-answer | −0.4 | Define the median of X in terms of F. | the smallest value m such that F(m) ≥ 0.5 | — |
| R15 | recall | short-answer | −0.3 | True or false: F fully determines the distribution of X — nothing about X's probabilities is left unspecified once F is known. | true | — |
| R16 | recall | short-answer | −0.2 | Fill in the blank: P(X > x) = 1 − ___. | F(x) | — |
| A3 | apply | short-answer | 0.25 | A fair six-sided die is rolled; X = the result. Describe F(x) for all x. | F(x)=0 for x<1; jumps by 1/6 at each of x=1,…,6, reaching F(x)=k/6 for k≤x<k+1; F(x)=1 for x≥6 — a 6-step staircase | draws F as a smooth increasing curve instead of a step function → `cdf` |
| A4 | apply | numeric | 0.35 | Using the die's F from A3, find P(X≤4), P(X<4), and P(X=4). `[verified: 2/3, 1/2, 1/6]` | F(4)=4/6=2/3; P(X<4)=F(4⁻)=3/6=1/2; P(X=4)=F(4)−F(4⁻)=4/6−3/6=1/6 | computes P(X<4) as F(4)=2/3 instead of the left-limit F(4⁻) → `cdf` |
| A5 | apply | short-answer | 0.45 | X takes values 10, 20, 30, 40 with probabilities 0.1, 0.2, 0.3, 0.4. Find the median using F. `[verified: median = 30]` | F(10)=0.1, F(20)=0.3, F(30)=0.6, F(40)=1.0; the smallest value with F ≥ 0.5 is 30 | picks 20 since F(20)=0.3 is "closest to but under 0.5," misreading the ≥0.5 rule → `cdf` |
| A6 | apply | numeric | 0.55 | Using the same X as A5, find the 25th percentile. `[verified: 20]` | smallest x with F(x) ≥ 0.25: F(10)=0.1<0.25, F(20)=0.3≥0.25, so the 25th percentile is 20 | picks 10 by comparing F(10)=0.1 to 0.25 and rounding up loosely, without checking F(20) is the first value that actually clears the threshold → `cdf` |
| E3 | explain | derivation | 0.95 | Prove P(a<X≤b) = F(b) − F(a) using additivity of probability. | write {X≤b} = {X≤a} ⊔ {a<X≤b} (a disjoint union, since a<b); additivity gives F(b) = F(a) + P(a<X≤b); rearranging gives the result *(required, the disjoint decomposition shown explicitly)* | asserts the formula by analogy to subtraction without the disjoint-union argument → `cdf` |
| E4 | explain | short-answer | 1.05 | Percentiles are defined via F even when F has jumps (as in A5/A6). Explain why the "smallest x with F(x)≥p" definition still gives a sensible, well-defined answer in that case. | because F is non-decreasing and right-continuous, the set of x satisfying F(x)≥p is always a closed-below interval (or ray), so it has a well-defined smallest element regardless of whether F jumps there or passes through smoothly *(required)* | — |
| E5 | explain | short-answer | 1.15 | Explain why F is defined using ≤ (making it right-continuous) rather than < (which would make it left-continuous), tracing the choice back to the definition of the CDF itself. | F(x)=P(X≤x) includes the point x itself in the event, and as x decreases toward a value x₀ from above, {X≤x} shrinks down to exactly {X≤x₀} — matching F(x₀) in the limit, i.e. right-continuity; had the definition used < instead, the analogous left-continuity would hold instead, so the convention is a direct consequence of which inequality is chosen *(required)* | — |
| T2 | transfer | short-answer | 1.45 | A household-income CDF shows F($40,000) = 0.35 and F($60,000) = 0.70. What fraction of households earn between $40,000 and $60,000? What is the smallest income at or above the 70th percentile? | P($40k<income≤$60k) = 0.70−0.35 = 0.35, i.e. 35% of households; the 70th percentile is $60,000, since F reaches exactly 0.70 there | subtracts the two F-values to get a percentile figure rather than a between-two-values probability, confusing the two computations → `cdf` |
| T3 | transfer | short-answer | 1.5 | A standardized test's score CDF shows F(650) = 0.82. A student asks "does that mean 82% of test-takers scored exactly 650?" Correct the misunderstanding using the definition of F. | no — F(650)=0.82 means 82% of test-takers scored *at or below* 650, not exactly 650; the probability of scoring exactly 650 is instead the jump size F(650)−F(650⁻), which is typically much smaller (and zero if scores are treated as continuous) | agrees that F(650)=0.82 describes the probability of scoring exactly 650 → `cdf` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.50…1.50 (3.0 logits).*

---

## Probability Mass Function (`pmf`)
*Prereq: Discrete vs Continuous RVs, CDF · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define the PMF of a discrete X, and state its two defining conditions. | p(x) = P(X=x); p(x) ≥ 0 for all x; Σₓ p(x) = 1 | — |
| R2 | recall | mcq | −0.3 | Which of these could *not* be a valid PMF on {1,2,3}? | p(1)=0.5, p(2)=0.6, p(3)=−0.1 (negative probability *and* doesn't sum to 1) | picks p(1)=1, p(2)=0, p(3)=0, thinking a PMF can't put all its mass on one point → `pmf` |
| A1 | apply | numeric | 0.34 | p(1)=c, p(2)=2c, p(3)=3c is a valid PMF. Find c and P(X≥2). `[verified: c=1/6, P=5/6]` | c=1/6; P(X≥2)=5/6 | normalizes over the wrong set, e.g. divides by 3 (the number of outcomes) instead of solving Σp(x)=1 → `pmf` |
| A2 | apply | short-answer | 0.5 | F has jumps of size 0.2, 0.3, 0.5 at x=1,2,3 and is flat elsewhere. Recover the PMF. | p(1)=0.2, p(2)=0.3, p(3)=0.5 — jump size *is* the PMF value, from `cdf`'s E2 | reads off the *cumulative* values (0.2, 0.5, 1.0) instead of the jump sizes → `cdf` |
| E1 | explain | derivation | 1.04 | Prove Σₓ p(x) = 1 from countable additivity, using the fact that {X=x} for distinct x partitions Ω. | the events {X=x} are pairwise disjoint and their union is Ω (every outcome gives *some* value); countable additivity then gives Σ P(X=x) = P(Ω) = 1 *(required — the partition property must be stated, not assumed)* | — |
| E2 | explain | short-answer | 1.2 | Why is the PMF the wrong tool for a continuous random variable? | p(x) = P(X=x) = 0 for every x when X is continuous, so the "PMF" would be identically zero and carry no information *(required — ties back to the previous cluster's E1)* | — |
| T1 | transfer | short-answer | 1.54 | Two different random experiments: (1) roll a fair six-sided die and record 1 if the result is prime (2,3,5), 0 otherwise; (2) flip a coin that lands heads with probability exactly 1/2. Verify both produce the *same* PMF, and explain what that tells you about what a PMF does and doesn't capture. | primes on a die: P(1)=3/6=1/2=P(0), matching the coin exactly `[verified]`. Required: a PMF describes only the induced distribution of values, not the underlying sample space or experiment — two unrelated experiments can be "the same random variable" in every distributional sense | treats the two experiments as necessarily different random variables because the sample spaces differ → `pmf` |
| R3 | recall | mcq | −1.5 | The non-negativity condition on a PMF requires: | p(x) ≥ 0 for every x | picks "p(x) > 0 for every x," which would forbid any x from having zero probability at all → `pmf` |
| R4 | recall | short-answer | −1.4 | Fill in the blank: Σₓ p(x) = ___, summed over the support of X. | 1 | — |
| R5 | recall | short-answer | −1.3 | True or false: a PMF value p(x) can be negative for some x. | false | — |
| R6 | recall | short-answer | −1.2 | Define the "support" of a PMF. | the set of values x for which p(x) > 0 | — |
| R7 | recall | mcq | −1.1 | The relationship between the PMF and the CDF is: | p(x) = F(x) − F(x⁻), the jump size at x | picks p(x) = F(x), conflating cumulative and point values → `pmf` |
| R8 | recall | short-answer | −1.0 | True or false: the PMF only applies to discrete random variables. | true | — |
| R9 | recall | short-answer | −0.9 | Fill in the blank: p(x) = P(X = ___). | x | — |
| R10 | recall | mcq | −0.8 | Together with non-negativity, which condition makes p a valid PMF? | the values sum to 1 over the support | picks "the support must be finite" — countably infinite supports are fine too → `pmf` |
| R11 | recall | short-answer | −0.7 | True or false: p(x) can exceed 1 for some x. | false — unlike a density, a PMF value is itself a probability, so it's capped at 1 | — |
| R12 | recall | mcq | −0.6 | The PMF of a degenerate random variable that always equals the constant c is: | p(c) = 1, and p(x) = 0 for all other x | picks a PMF spread evenly over several values, missing that "always equals c" means all mass sits at one point → `pmf` |
| R13 | recall | short-answer | −0.5 | Define a "degenerate" (point-mass) distribution. | a distribution where all the probability mass sits on a single value, i.e. P(X=c)=1 for some constant c | — |
| R14 | recall | short-answer | −0.4 | True or false: a valid PMF must assign p(x)=0 for every x outside some finite or countable support. | true — by definition, only countably many values can carry positive probability | — |
| R15 | recall | mcq | −0.3 | A PMF can be specified: | either as a table of (value, probability) pairs or as a formula p(x) | picks "only as a table," missing that formulas like p(x)=(1/2)ˣ are equally valid PMFs | 
| R16 | recall | short-answer | −0.2 | Fill in the blank: the PMF is also called the "probability ___ function." | mass | — |
| A3 | apply | numeric | 0.44 | p(1)=0.2, p(2)=0.5, p(3)=0.3. Verify this is a valid PMF, then find P(X≥2). `[verified: valid; P=0.8]` | sums to 1 and all values ≥0, so valid; P(X≥2)=p(2)+p(3)=0.5+0.3=0.8 | includes p(1) in the "X≥2" sum by mistake, reporting 1.0 → `pmf` |
| A4 | apply | numeric | 0.55 | p(0)=k, p(1)=2k, p(2)=3k, p(3)=4k is a valid PMF. Find k and P(X≤2). `[verified: k=0.1, P=0.6]` | 10k=1 so k=0.1; P(X≤2)=k+2k+3k=6k=0.6 | solves 4k=1 (using only the largest term) instead of summing all four terms → `pmf` |
| A5 | apply | short-answer | 0.6 | F has jumps of size 0.15, 0.35, 0.5 at x=−1, 0, 2 and is flat elsewhere. Recover the PMF. | p(−1)=0.15, p(0)=0.35, p(2)=0.5 | reads off cumulative F-values at those points instead of the jump sizes → `cdf` |
| A6 | apply | numeric | 0.65 | Using the PMF from A5, find P(−1 < X < 2). `[verified: 0.35]` | only x=0 lies strictly between −1 and 2 in the support, so P(−1<X<2) = p(0) = 0.35 | includes x=−1 in the sum despite the strict inequality, reporting 0.5 → `pmf` |
| E3 | explain | derivation | 1.1 | Show that for a discrete X, Σₓ∈(a,b] p(x) = F(b) − F(a), connecting the PMF sum directly to the CDF difference. | F(b)−F(a) = P(a<X≤b) (from `cdf`'s addition-rule fact) = P(X∈(a,b]) = Σ over the support values x in (a,b] of p(x), since {X=x} for those x partition the event {X∈(a,b]} *(required: the partition step)* | asserts the equality without connecting it back through the partition of the interval into individual point-mass events → `pmf` |
| E4 | explain | short-answer | 1.25 | Explain why the PMF gives actual probabilities directly, with no further computation needed, unlike a tool you'll meet for continuous variables that requires integration. | p(x) is *already* P(X=x) by definition — reading a table entry or evaluating the formula gives the exact probability of that outcome, with nothing else to compute; this directness is special to the discrete case *(required — framed as a property of PMF, without needing to describe the continuous tool in detail)* | — |
| E5 | explain | short-answer | 1.35 | Trace back to the axioms: why must the support of a discrete random variable's PMF be countable? | the events {X=x} for distinct x in the support are pairwise disjoint; countable additivity only guarantees ΣP(X=x)=1 sums correctly for a *countable* collection of such events — an uncountable collection of positive terms cannot sum to a finite value at all, so the axioms themselves force the support to be countable *(required, ties to countable additivity specifically)* | — |
| T2 | transfer | short-answer | 1.6 | Design a fair carnival game: a player pays $1, and wins $0, $2, or $5 with probabilities 0.6, 0.3, 0.1 respectively. Verify this is a valid PMF for the winnings, then state (without computing an expectation) whether this PMF alone tells you if the game is profitable for the house. | valid PMF: 0.6+0.3+0.1=1, all nonnegative; the PMF alone does *not* answer profitability — that requires combining it with the $1 cost via an expected-value calculation, a tool this concept doesn't provide yet | claims the game is unfair to the house just from seeing three payout probabilities, without any expectation calculation → `pmf` |
| T3 | transfer | short-answer | 1.65 | A call center's PMF for calls received in an hour is p(0)=0.05, p(1)=0.15, p(2)=0.30, p(3)=0.30, p(4)=0.20 (calls beyond 4 are negligible here). Find P(more than 2 calls). `[verified: 0.5]` | P(X>2) = p(3)+p(4) = 0.30+0.20 = 0.5 | includes p(2) in "more than 2," reporting 0.8 → `pmf` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.50…1.65 (3.15 logits).*

---

## Probability Density Function (`pdf`)
*Prereq: Discrete vs Continuous RVs, CDF · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define the PDF f of a continuous X, and its two defining conditions. | f = F′ where F is differentiable; f(x) ≥ 0; ∫₋∞^∞ f(x)dx = 1 | — |
| R2 | recall | mcq | −0.3 | Which is true of f(x) for a continuous random variable? | f(x) can exceed 1 | picks "f(x) = P(X=x)" — the single most common density misreading → `pdf` |
| A1 | apply | numeric | 0.34 | f(x) = c for 0 ≤ x ≤ 4, and 0 elsewhere. Find c and P(1≤X≤3). `[verified: c=0.25, P=0.5]` | c = 0.25; P = 0.25×2 = 0.5 | integrates over the whole real line as if the support were unbounded, rather than restricting to [0,4] → `pdf` |
| A2 | apply | numeric | 0.5 | f(x) = 2x for 0≤x≤1, 0 elsewhere. Verify it's a valid density, then find P(X>0.5). `[verified: valid; P=0.75]` | ∫₀¹ 2x dx = 1, valid; P(X>0.5) = ∫₀.₅¹ 2x dx = 0.75 | computes P(X>0.5) as 1 − 0.5 = 0.5, treating the density as if it were uniform → `pdf` |
| E1 | explain | short-answer | 1.04 | Explain concretely why f(x) can exceed 1, using f(x)=5 on [0, 0.2] as an example. | f is a *density* — probability per unit length — not a probability; a narrow support can carry a tall density and still integrate to 1 (5 × 0.2 = 1) *(required: the concrete check that it does integrate to 1)* | — |
| E2 | explain | derivation | 1.2 | Show P(a≤X≤b) = F(b) − F(a) = ∫ₐᵇ f(x)dx, connecting the two definitions via the fundamental theorem of calculus. | F(b)−F(a) by definition of CDF; equals ∫ₐᵇ f by FTC since f=F′ *(required, both directions stated)* | — |
| T1 | transfer | short-answer | 1.54 | A dart lands at a random distance X (normalized to [0,1]) from the center of a board, uniformly over the *board's area* (not uniformly in distance!). Before computing anything: argue geometrically why the density of X should *increase* with x. Then verify f(x)=2x is consistent with that argument. | a thin ring at radius x has area proportional to its circumference, 2πx; a uniform-over-area dart is proportionally more likely to land in a ring farther out, so the density in x must grow linearly — matching f(x)=2x after normalizing *(required: the area argument comes first, not just confirming the formula integrates to 1)* | assumes uniform-over-area means uniform-in-distance (constant density) → `pdf` |
| R3 | recall | mcq | −1.5 | The normalization condition on a PDF requires: | ∫₋∞^∞ f(x) dx = 1 | picks "f(x) sums to 1 over its support," confusing the discrete PMF condition with the continuous one → `pdf` |
| R4 | recall | short-answer | −1.4 | Fill in the blank: a valid PDF must satisfy f(x) ≥ ___ for all x. | 0 | — |
| R5 | recall | short-answer | −1.3 | True or false: f(x) = P(X=x) for a continuous random variable. | false — f(x) is a density, not a probability; P(X=x)=0 for continuous X regardless of f(x) | — |
| R6 | recall | short-answer | −1.2 | In one line, what does f(x) represent? | probability *per unit length* near x — a density, not a probability itself | — |
| R7 | recall | mcq | −1.1 | The relationship between the PDF f and the CDF F is: | f = F′ (f is the derivative of F, where F is differentiable) | picks f = F, treating density and cumulative probability as the same object → `pdf` |
| R8 | recall | short-answer | −1.0 | True or false: f(x) can exceed 1 at some x. | true — a narrow enough support can carry a tall density and still integrate to 1 | — |
| R9 | recall | short-answer | −0.9 | Fill in the blank: P(a≤X≤b) = ∫ₐᵇ ___ dx. | f(x) | — |
| R10 | recall | mcq | −0.8 | Can f(x) ever be negative? | no — non-negativity is required, just like the PMF | picks "yes, as long as the total integral is still 1" → `pdf` |
| R11 | recall | short-answer | −0.7 | True or false: the PDF applies only to continuous random variables. | true | — |
| R12 | recall | mcq | −0.6 | The total area under a valid PDF's curve is: | 1 | picks "the maximum value of X" → `pdf` |
| R13 | recall | short-answer | −0.5 | Fill in the blank: outside the support of X, f(x) = ___. | 0 | — |
| R14 | recall | short-answer | −0.4 | True or false: for continuous X, P(X=a) = ∫ₐᵃ f(x)dx = 0 for any specific value a. | true — an integral over a single point (zero width) is always 0 | — |
| R15 | recall | mcq | −0.3 | A density that is very tall over a very narrow interval can still be valid because: | density measures probability *per unit length*, not probability itself, so a narrow tall spike can still integrate to 1 | picks "because probabilities are allowed to exceed 1 in special cases" → `pdf` |
| R16 | recall | short-answer | −0.2 | Define the "support" of a PDF. | the set of x-values where f(x) > 0 | — |
| A3 | apply | numeric | 0.44 | f(x) = 3x² for 0≤x≤1, 0 elsewhere. Verify it's a valid density, then find P(X<0.5). `[verified: valid; P=0.125]` | ∫₀¹3x²dx=1, valid; P(X<0.5)=∫₀^0.5 3x²dx = 0.5³ = 0.125 | computes P(X<0.5) as 0.5 by treating the density as if it were uniform → `pdf` |
| A4 | apply | numeric | 0.55 | f(x) = c(2−x) for 0≤x≤2, 0 elsewhere. Find c and P(X>1). `[verified: c=0.5, P=0.25]` | ∫₀²c(2−x)dx = c·2 = 1, so c=0.5; P(X>1)=∫₁²0.5(2−x)dx=0.25 | integrates c(2−x) over [0,2] incorrectly as if it were constant, getting c=1 by dividing by the interval width alone → `pdf` |
| A5 | apply | short-answer | 0.6 | Is f(x) = x for 0≤x≤1, 0 elsewhere, a valid PDF? Check by direct computation. | no — ∫₀¹x dx = 0.5 ≠ 1, so it fails normalization even though f(x)≥0 everywhere | checks only non-negativity and declares it valid without integrating to check normalization → `pdf` |
| A6 | apply | numeric | 0.65 | X is uniform on [3, 10]. Find the constant value of f(x) on this interval, and confirm it does not exceed 1. `[verified: f=1/7≈0.143]` | f(x) = 1/(10−3) = 1/7 ≈ 0.143 on [3,10], 0 elsewhere; this is well under 1, unlike a density on a narrow interval | computes f(x) = 1/10 or 1/3 by misreading which quantity the interval width is | 
| E3 | explain | derivation | 1.1 | Using f = F′, give the differential interpretation: explain what f(x)dx approximates for a small dx, and derive it from the definition of the derivative. | F′(x) = lim_{dx→0} [F(x+dx)−F(x)]/dx = lim [P(x<X≤x+dx)]/dx, so for small dx, f(x)dx ≈ P(x<X≤x+dx) — the density times a small width approximates the probability of landing in that thin sliver *(required: the limit-definition derivation, not just stating the approximation)* | states f(x)dx≈P(X∈[x,x+dx]) as a definition without deriving it from F′'s limit | 
| E4 | explain | short-answer | 1.25 | Explain why changing f's value at a single isolated point never changes any probability computed from it. | any probability computed from f is an integral, ∫ₐᵇf(x)dx, and integrals are insensitive to the value of the integrand at individual points (a set of measure zero) — so redefining f(x₀) for one x₀ leaves every P(a≤X≤b) exactly unchanged *(required)* | — |
| E5 | explain | short-answer | 1.35 | Densities are often defined piecewise (different formulas on different intervals). Explain why the CDF F can still be continuous even when f has jump discontinuities at the pieces' boundaries. | continuity of F only requires F itself (an integral of f) to have no jumps, which holds as long as f is bounded and integrable — F's *value* changes smoothly because it accumulates area continuously, even though f's *shape* (its slope, via F′) can change abruptly at a boundary *(required)* | assumes a jump in f necessarily forces a jump in F, conflating a discontinuity in the density with a discontinuity in the cumulative function → `pdf` |
| T2 | transfer | short-answer | 1.6 | A task's completion time X (in hours, on [0,2]) has density f(x) = x/2, reflecting that completions become more likely as the deadline approaches. Verify this is valid, then find P(the task takes more than 1.5 hours). `[verified: valid; P=0.4375]` | ∫₀²(x/2)dx = [x²/4]₀² = 1, valid; P(X>1.5) = ∫₁.₅²(x/2)dx = [x²/4]₁.₅² = 1 − 0.5625 = 0.4375 | integrates from 0 to 1.5 instead of 1.5 to 2, reporting the complement 0.5625 → `pdf` |
| T3 | transfer | short-answer | 1.65 | A triangular density peaks sharply at x=5 and tapers to 0 at x=3 and x=7. Explain why "the value of X is most likely to be exactly 5" is a subtly wrong way to describe this, even though f(5) is the largest density value. | for a continuous X, P(X=5) is still exactly 0, just like every other single point — "most likely" in the continuous setting has to be understood as "the density is highest near 5," meaning outcomes *near* 5 are more probable than outcomes near the tails, not that the single value 5 itself has any positive probability *(required)* | describes x=5 as literally "the most probable outcome" in the same sense a discrete mode would be, without the density-vs-probability distinction → `pdf` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.50…1.65 (3.15 logits).*

---

## Expectation (`expectation`)
*Prereq: PMF, PDF · ancestors 9 · b₀ = 0.65*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.35 | State the definition of E[X] for discrete and for continuous X. | Σₓ x·p(x) discrete; ∫x·f(x)dx continuous | — |
| R2 | recall | mcq | −0.2 | Which is FALSE in general? (a) E[X] is always a value X can actually take (b) E[aX+b] = aE[X]+b (c) E is linear. | (a) is the false one | picks (a) as *true* — expects the mean of a fair die (3.5) to be a possible roll → `expectation` |
| A1 | apply | numeric | 0.45 | X has PMF p(1)=0.5, p(2)=0.3, p(3)=0.2. Find E[X]. `[verified: 1.7]` | 1(0.5)+2(0.3)+3(0.2) = 1.7 | — |
| A2 | apply | numeric | 0.6 | X has density f(x)=2x on [0,1] (from the dart example). Find E[X]. `[verified: 2/3]` | ∫₀¹ x·2x dx = ∫2x² = 2/3 ≈ 0.667 | applies the discrete formula (sums instead of integrates) → `expectation` |
| E1 | explain | derivation | 1.15 | Prove linearity: E[aX+b] = aE[X] + b, from the discrete definition. | Σ(ax+b)p(x) = aΣx p(x) + bΣp(x) = aE[X] + b, using Σp(x)=1 *(required — the full algebraic chain, not just the statement)* | — |
| E2 | explain | short-answer | 1.3 | Explain why E[X] is a "probability-weighted average" and, using the die-roll intuition from R2, why it need not equal any outcome X can produce. | a weighted average pulled toward more-likely values need not land on any single achievable value, the same way a center of mass need not sit on the object itself *(required)* | — |
| T1 | transfer | short-answer | 1.65 | An insurance policy pays $0 with probability 0.9, and (if a claim occurs) a random amount uniform on [$1000, $5000] with probability 0.1. Find the fair premium — the expected payout — by splitting into the two cases and combining. `[verified: $300]` | E[payout] = 0.9(0) + 0.1·E[Uniform(1000,5000)] = 0.1(3000) = $300 — a first, informal encounter with conditioning on cases before `law-of-total-expectation` names it | computes E[Uniform(1000,5000)] correctly but forgets to weight by the 0.1 probability of a claim occurring at all → `expectation` |
| R3 | recall | mcq | −1.3 | E[c] for a constant c (a "random variable" that never varies) equals: | c | picks 0, assuming expectation of a non-random quantity is undefined or zero → `expectation` |
| R4 | recall | short-answer | −1.2 | Fill in the blank: E[X+Y] = ___, regardless of whether X and Y are independent. | E[X] + E[Y] | — |
| R5 | recall | short-answer | −1.1 | True or false: linearity of expectation, E[X+Y]=E[X]+E[Y], requires X and Y to be independent. | false — it holds unconditionally, independent or not | — |
| R6 | recall | short-answer | −1.0 | State the formula for E[g(X)] for discrete X (the "law of the unconscious statistician"). | Σₓ g(x)·p(x) | — |
| R7 | recall | mcq | −0.9 | E[1_A], the expectation of the indicator of event A, equals: | P(A) | picks 1, thinking the expectation of an indicator is always its maximum value → `expectation` |
| R8 | recall | short-answer | −0.8 | True or false: E[X] is also called the "mean" or "first moment" of X. | true | — |
| R9 | recall | short-answer | −0.7 | Fill in the blank: E[aX] = ___. | a·E[X] | — |
| R10 | recall | mcq | −0.6 | Which best describes E[X]? | a probability-weighted average of the values X can take | picks "the value X takes most often (the mode)" → `expectation` |
| R11 | recall | short-answer | −0.5 | True or false: E[X] can be interpreted as the "center of mass" of the distribution's probability. | true | — |
| R12 | recall | mcq | −0.4 | The discrete expectation formula is: | Σₓ x·p(x) | picks Σₓ p(x), which just sums to 1 and discards the values entirely → `expectation` |
| R13 | recall | short-answer | −0.3 | Fill in the blank: the continuous expectation formula is ∫x·___dx. | f(x) | — |
| R14 | recall | short-answer | −0.2 | True or false: E[g(X)] generally equals g(E[X]) for a nonlinear g. | false — this only holds when g is linear (or affine); in general E[g(X)] ≠ g(E[X]) | — |
| R15 | recall | mcq | −0.1 | E[X²] is called the: | second moment of X | picks "the variance of X" — variance is E[X²]−(E[X])², not E[X²] alone → `expectation` |
| R16 | recall | short-answer | 0.0 | True or false: expectation is a single number summarizing a distribution's central tendency. | true | — |
| A3 | apply | numeric | 0.5 | X has PMF p(0)=0.4, p(1)=0.35, p(2)=0.25. Find E[X]. `[verified: 0.85]` | 0(0.4)+1(0.35)+2(0.25) = 0.85 | omits the x=0 term's contribution check and adds only the nonzero-value terms without weighting, e.g. reports (0.35+0.25)/2 → `expectation` |
| A4 | apply | numeric | 0.55 | Using the PMF from A3, find E[X²] via LOTUS. `[verified: 1.35]` | 0²(0.4)+1²(0.35)+2²(0.25) = 0+0.35+1.0 = 1.35 | computes (E[X])² = 0.85² = 0.7225 instead of E[X²] directly → `expectation` |
| A5 | apply | numeric | 0.62 | E[X] = 2. Using linearity, find E[4X−3]. `[verified: 5]` | 4(2)−3 = 5 | applies linearity to the multiplicative part only, forgetting the constant shift, reporting 8 → `expectation` |
| A6 | apply | short-answer | 0.68 | It rains on 30% of days (modeled as independent days). Let Y = 1{it rains today}. Find E[Y], and explain what the number represents in plain terms. | E[Y] = P(rain) = 0.3; the expectation of an indicator is just the probability of the event it indicates — here, "0.3" means 30% of days are rainy on average, not that 0.3 of a day is rainy | treats E[Y]=0.3 as meaning "0.3 inches of rain typically falls," confusing the indicator's expectation with a physical quantity → `expectation` |
| E3 | explain | derivation | 1.2 | Prove E[X+Y] = E[X] + E[Y] for two discrete random variables on the same space, *without* assuming independence. | E[X+Y] = Σ_{x,y}(x+y)p(x,y) = Σx·Σ_y p(x,y) + Σy·Σ_x p(x,y) = Σx·p_X(x) + Σy·p_Y(y) = E[X]+E[Y], where the joint pmf sums to the marginals regardless of any dependence between X and Y *(required: the joint-to-marginal summation step, showing independence was never used)* | assumes the proof requires p(x,y)=p_X(x)p_Y(y) (independence) and gets stuck without it → `expectation` |
| E4 | explain | short-answer | 1.3 | Give a concrete example showing E[g(X)] ≠ g(E[X]) for a nonlinear g, and explain in one line why linearity fails here. | e.g. X is −1 or 1 with probability 0.5 each: E[X]=0, so g(E[X])=0²=0, but E[X²]=1²(0.5)+1²(0.5)=1 ≠ 0; squaring is nonlinear, so it doesn't commute with the averaging operation the way multiplication by a constant does *(required: a worked numeric counterexample)* | — |
| E5 | explain | short-answer | 1.4 | Explain, without computing an actual example, why E[X] can fail to exist for some distributions. | E[X] is defined by a sum or integral, and that sum/integral can *diverge* (fail to converge to a finite value) for distributions with sufficiently heavy tails — when that happens, "the average" is simply undefined, not just hard to compute *(required)* | assumes every random variable must have a well-defined finite expectation → `expectation` |
| T2 | transfer | short-answer | 1.55 | Without ever writing down the joint distribution of 5 dice, use linearity of expectation to find E[sum of 5 dice rolls]. `[verified: 17.5]` | each die has E[Xᵢ]=3.5; by linearity, E[ΣXᵢ]=ΣE[Xᵢ]=5(3.5)=17.5 — no joint distribution or independence assumption needed at all | insists on computing the joint distribution of all 5 dice first before linearity can be applied → `expectation` |
| T3 | transfer | short-answer | 1.6 | A vendor's daily profit is π(X) = 5X − 20 dollars, where X is the (random) number of units sold. If E[X] = 12, find E[π(X)] using linearity, and explain why this shortcut works here but wouldn't if profit depended on X² (e.g. quadratic costs). | E[π(X)] = 5(12) − 20 = 40, valid because π is *affine* (linear plus a constant) in X, so E[π(X)]=π(E[X]) exactly by linearity; if profit involved X² instead, E[π(X)] ≠ π(E[X]) in general, per E4's counterexample — the shortcut is special to affine functions | applies E[π(X)]=π(E[X]) as if it were a general rule, without noting it only works because π is affine → `expectation` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.30…1.65 (2.95 logits).*

---

## Variance (`variance`)
*Prereq: Expectation · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | Give two equivalent formulas for Var(X). | E[(X−E[X])²] and E[X²] − (E[X])² | — |
| R2 | recall | mcq | −0.15 | Which is FALSE? (a) Var(X) ≥ 0 always (b) Var(aX+b) = a²Var(X) (c) Var(aX+b) = a·Var(X) + b. | (c) is false | picks (c) as true — treats variance as linear the way expectation is, forgetting shifts vanish and scales square → `variance` |
| A1 | apply | numeric | 0.5 | Using the PMF from Expectation's A1 (E[X]=1.7), find Var(X). `[verified: 0.61]` | E[X²] = 1(0.5)+4(0.3)+9(0.2) = 3.5; Var = 3.5 − 1.7² = 0.61 | forgets to square E[X] before subtracting → `variance` |
| A2 | apply | numeric | 0.65 | Using the density f(x)=2x on [0,1] (E[X]=2/3), find Var(X). `[verified: 1/18 ≈ 0.0556]` | E[X²] = ∫₀¹ x²·2x dx = 2/4 = 0.5; Var = 0.5 − (2/3)² = 1/18 | — |
| E1 | explain | derivation | 1.2 | Derive the shortcut formula Var(X) = E[X²] − (E[X])² starting from E[(X−E[X])²], using linearity of expectation. | expand (X−μ)² = X² − 2μX + μ²; apply linearity term by term; μ=E[X] is a constant so E[μ²]=μ² and E[2μX]=2μ² *(required, full expansion)* — arguably the single most-used algebra trick in the subject | expands the square but drops a term, or forgets μ is a constant under the expectation → `expectation` |
| E2 | explain | short-answer | 1.35 | Explain why Var(aX+b) = a²Var(X): why does the shift *b* vanish entirely, while the scale *a* enters *squared*? | shifting every outcome by b shifts the mean by b too, so deviations from the mean are unchanged — spread is shift-invariant; scaling by a scales every deviation by a, and variance is an *average squared* deviation, so the factor becomes a² *(required, both halves)* | — |
| T1 | transfer | short-answer | 1.7 | Variance is reported in squared units (e.g. dollars²), which is why practitioners usually quote the standard deviation instead. If a temperature's variance in Celsius is V, what is its variance in Fahrenheit (F = 1.8C + 32), and by what factor did it change? `[verified: 1.8² V = 3.24V]` | Var(F) = 1.8² · Var(C) = 3.24V — a real unit-conversion consequence of E2's scaling rule | assumes temperature variance is unchanged by a unit conversion, since "temperature is temperature" → `variance` |
| R3 | recall | mcq | −1.25 | Var(c) for a constant c equals: | 0 — a non-random quantity has no spread | picks c², confusing the variance of a constant with the constant squared → `variance` |
| R4 | recall | short-answer | −1.15 | Fill in the blank: Var(X) ≥ ___ for any X. | 0 | — |
| R5 | recall | short-answer | −1.05 | True or false: standard deviation is defined as √Var(X). | true | — |
| R6 | recall | short-answer | −0.95 | Define standard deviation in one line. | the square root of the variance; a measure of spread in the same units as X | — |
| R7 | recall | mcq | −0.85 | Which expression *defines* Var(X)? | E[(X−E[X])²] | picks E[X]−E[X²], which isn't even nonnegative in general → `variance` |
| R8 | recall | short-answer | −0.75 | True or false: Var(X)=0 implies X is (almost surely) a constant. | true | — |
| R9 | recall | short-answer | −0.65 | Fill in the blank: Var(aX+b) = ___. | a²Var(X) | — |
| R10 | recall | mcq | −0.55 | If X is measured in dollars, Var(X) is measured in: | dollars squared | picks "dollars," the same units as X — that's the standard deviation's units, not variance's → `variance` |
| R11 | recall | short-answer | −0.45 | True or false: standard deviation has the same units as X. | true | — |
| R12 | recall | mcq | −0.35 | Var(X+c) for a constant c equals: | Var(X) — shifting by a constant doesn't change spread | picks Var(X)+c → `variance` |
| R13 | recall | short-answer | −0.25 | True or false: Var(X) can be negative. | false — it's an average of squared (hence non-negative) deviations | — |
| R14 | recall | short-answer | −0.15 | Fill in the blank: the shortcut formula is Var(X) = E[X²] − ___. | (E[X])² | — |
| R15 | recall | mcq | −0.05 | Rearranging the shortcut formula, E[X²] equals: | Var(X) + (E[X])² | picks Var(X) − (E[X])², getting the sign backwards → `variance` |
| R16 | recall | short-answer | 0.05 | True or false: Var(X+Y) = Var(X)+Var(Y) holds for *any* X, Y, regardless of dependence. | false — it requires X, Y independent (or more precisely, zero covariance); dependent variables pick up an extra covariance term | — |
| A3 | apply | numeric | 0.55 | X has PMF p(0)=0.4, p(1)=0.35, p(2)=0.25 (E[X]=0.85, E[X²]=1.35 from the previous concept). Find Var(X). `[verified: 0.6275]` | Var = E[X²]−(E[X])² = 1.35 − 0.85² = 1.35−0.7225 = 0.6275 | uses E[X]=0.85 directly instead of squaring it, computing 1.35−0.85=0.5 → `variance` |
| A4 | apply | numeric | 0.6 | Var(X) = 4. Find Var(3X−2). `[verified: 36]` | 3²·4 = 36 | scales by 3 instead of 3², reporting 12, and/or subtracts something for the −2 → `variance` |
| A5 | apply | numeric | 0.65 | Var(X) = 16. Find the standard deviation of X. `[verified: 4]` | √16 = 4 | reports 16 itself as the standard deviation, not taking the square root → `variance` |
| A6 | apply | numeric | 0.7 | An indicator Y = 1{event with probability 0.3} takes value 1 w.p. 0.3, 0 w.p. 0.7. Find Var(Y). `[verified: 0.21]` | E[Y]=0.3, E[Y²]=0.3 (since Y²=Y for a 0/1 variable); Var(Y)=0.3−0.3²=0.3−0.09=0.21 — this is exactly the p(1−p) formula that will reappear for Bernoulli | forgets that Y²=Y for an indicator and instead computes E[Y²] as if Y ranged continuously, getting the wrong second moment → `variance` |
| E3 | explain | derivation | 1.25 | Prove both directions: if X is a constant c (almost surely), then Var(X)=0; and if Var(X)=0, then X must be a constant (almost surely). | (⟹) Var(c)=E[(c−c)²]=E[0]=0. (⟸) Var(X)=0 means E[(X−μ)²]=0; since (X−μ)² ≥ 0 always, an average of a non-negative quantity can only be 0 if the quantity itself is 0 almost everywhere, forcing X=μ almost surely *(required: both directions, with the second needing the "non-negative average is 0 only if the terms are 0" argument)* | proves only the easy direction (constant ⟹ Var=0) and treats that as establishing the biconditional → `variance` |
| E4 | explain | short-answer | 1.4 | Explain why practitioners usually report standard deviation rather than variance when communicating results. | standard deviation is in the *same units* as X (e.g. dollars, not dollars²), making it directly interpretable alongside the mean — "the average error is $50 ± $5" is meaningful in a way "± 25 dollars²" is not *(required)* | — |
| E5 | explain | short-answer | 1.5 | Prove directly (not by citing E2's result) that Var(X+c) = Var(X) for any constant c, starting from the definition E[(X−E[X])²]. | E[X+c] = E[X]+c; Var(X+c) = E[((X+c)−(E[X]+c))²] = E[(X−E[X])²] = Var(X) — the c's cancel inside the square *(required: the explicit cancellation step)* | asserts the shift-invariance without showing the c's cancel algebraically → `variance` |
| T2 | transfer | short-answer | 1.65 | Two independent investments have Var(X)=$10,000² and Var(Y)=$6,000². Find Var(X+Y), the variance of the combined portfolio's return, and explain why independence is essential to this calculation. `[verified: $16,000²]` | independence lets the cross term vanish, so Var(X+Y)=Var(X)+Var(Y)=10,000²+6,000²... wait — combine correctly: 100,000,000+36,000,000=136,000,000=$11,661.90²-ish; report as Var(X+Y)=136,000,000 dollars² | adds the standard deviations directly (10,000+6,000=16,000) instead of adding the variances and then taking a square root → `variance` |
| T3 | transfer | short-answer | 1.7 | A casino doubles every payout in a game (new payout = 2×old payout). Explain, without recomputing from scratch, what happens to the payout's variance and standard deviation, and why a player might misjudge the change in "riskiness." | variance scales by 2²=4 (quadruples), while standard deviation scales by only 2 (doubles) — a player who thinks in terms of variance might overestimate how much riskier the game became, while standard deviation (matching the payout's own units) gives the more intuitive "twice as spread out" reading | assumes both variance and standard deviation double along with the payout, missing the squaring in the variance scale factor → `variance` |

*Coverage: 16 recall, 6 apply, 5 explain, 3 transfer — 30 items, spread −1.25…1.7 (2.95 logits).*

---

## Cluster 2 misconception index

| Tag | Blame |
|---|---|
| RV-as-intrinsically-random / P(X) written as if X were a probability | `random-variables` |
| continuous ⟺ non-integer conflation; "very large but countable" mistaken for uncountable | `discrete-vs-continuous-random-variables` |
| CDF jump/limit errors (P(X<x) vs P(X≤x)); percentile/interval-probability confusion | `cdf` |
| PMF/CDF confusion (reading cumulative values as point masses); support/normalization slips | `cdf` or `pmf` depending on direction |
| density read as a probability, or as bounded by 1; jump-in-density assumed to force jump in CDF | `pdf` |
| mean must be an attainable value; E[g(X)] assumed to equal g(E[X]); linearity assumed to require independence | `expectation` |
| variance treated as linear (shift not absorbed, scale not squared); Var(X+Y)=Var(X)+Var(Y) assumed unconditionally | `variance` |

**Cluster total: 210 items across 7 concepts (30 each).** All numeric claims verified by script,
including all newly added rows; all concepts clear `auditCoverage`'s three bars with recall now the
dominant share (16 of 30 per concept) per the definitions-first expansion pass. The structural note at
the top of this file — no density/CDF machinery inside `random-variables` or
`discrete-vs-continuous-random-variables` — is the main thing worth re-checking if these items are
ever edited independently of each other.
