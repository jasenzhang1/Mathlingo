# Linear Algebra Cluster 4 — The Four Fundamental Subspaces

Four Fundamental Subspaces, Column Space, Null Space, Row Space, Left Null Space, Matmul on the Four
Fundamental Subspaces, Disjointness of the Four Fundamental Subspaces (7 concepts). Same format as
[Cluster 1](la-01-vectors-and-operations.md).

This whole cluster is one unified picture, deliberately reused across every item: A=[[1,1],[2,2]] has
row space span{(1,1)} and null space span{(1,−1)}, which turn out to be orthogonal — the concrete
instance every later item in the cluster verifies or extends.

---

## Four Fundamental Subspaces (`four-fundamental-subspaces`)
*Prereq: Linear Transformations, Vector Spaces · ancestors 6 · b₀ = 0.47*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.53 | Name the four fundamental subspaces of an m×n matrix A. | column space C(A); null space N(A); row space C(Aᵀ); left null space N(Aᵀ) | — |
| R2 | recall | mcq | −0.28 | Which pair lives in Rⁿ (the "input" space)? | row space and null space | picks column space and left null space, the *output*-side pair → `four-fundamental-subspaces` |
| A1 | apply | short-answer | 0.27 | For a 3×2 matrix A, which spaces live in R³ and which in R²? | R³: column space, left null space; R²: row space, null space | — |
| E1 | explain | short-answer | 0.97 | Why do these four subspaces come in two natural pairs? | A maps Rⁿ→Rᵐ, so the "domain-side" subspaces (row space, null space) naturally live in Rⁿ, and the "codomain-side" subspaces (column space, left null space) live in Rᵐ *(required)* | — |
| T1 | transfer | short-answer | 1.47 | Why is understanding all four subspaces, not just the column space, necessary to fully understand what Ax=b can and cannot do? | column space determines which b are reachable at all; null space determines whether a reachable b has a *unique* solution or infinitely many — together they answer both "can I solve it" and "how many ways" *(required: both questions named)* | — |
| R3 | recall | mcq | −0.5 | For an m×n matrix A, which two subspaces live in Rᵐ (the "output" space)? | column space and left null space | picks "row space and column space," mixing an input-side and an output-side subspace → `four-fundamental-subspaces` |
| R4 | recall | short-answer | −0.45 | Fill in the blank: the row space of A is another name for ___. | C(Aᵀ) — the column space of the transpose | — |
| R5 | recall | mcq | −0.4 | True or false: for a square invertible matrix, the null space and left null space are both {0}. | True — a full-rank square matrix has trivial null space and, since row rank=column rank=n, trivial left null space too | answers "False," assuming a square matrix must have some nonzero null space by default → `four-fundamental-subspaces` |
| R6 | recall | short-answer | −0.35 | For an m×n matrix, which dimension (m or n) does the null space's ambient space match? | n — the null space consists of input vectors x∈Rⁿ | — |
| R7 | recall | mcq | −0.3 | The left null space of A is defined as the null space of: | Aᵀ | picks "A itself," confusing it with the ordinary null space → `four-fundamental-subspaces` |
| R8 | recall | short-answer | −0.25 | State which of the four subspaces is exactly {x : Ax=0}. | the (ordinary) null space, N(A) | — |
| R9 | recall | mcq | −0.2 | Which pair of subspaces both live in Rⁿ (the input space)? | row space and null space | picks "column space and null space," pairing an output-side and input-side subspace → `four-fundamental-subspaces` |
| R10 | recall | short-answer | −0.15 | Fill in the blank: the four fundamental subspaces come from two ambient spaces — Rⁿ and ___. | Rᵐ | — |
| R11 | recall | mcq | −0.1 | For a square n×n matrix, all four fundamental subspaces live in: | Rⁿ — since m=n, both ambient spaces coincide | picks "different spaces, as always," forgetting that a square matrix collapses the m vs n distinction → `four-fundamental-subspaces` |
| R12 | recall | short-answer | −0.05 | Which two of the four subspaces would you consult to find all solutions of Ax=b for a given b? | column space (to check b is reachable at all) and null space (to find the family of solutions once one is known) | — |
| R13 | recall | mcq | 0.0 | The column space of a 5×3 matrix A is a subspace of: | R⁵ | picks "R³," confusing it with the row space's ambient space → `four-fundamental-subspaces` |
| R14 | recall | short-answer | −0.55 | Fill in the blank: A is thought of as a linear map from ___ to ___. | Rⁿ; Rᵐ | — |
| R15 | recall | mcq | 0.05 | Which subspace answers "what inputs get mapped to output zero"? | the null space | picks "the left null space," which instead answers a question about outputs orthogonal to every column → `four-fundamental-subspaces` |
| R16 | recall | short-answer | 0.1 | For a 4×6 matrix A, state the ambient space (R⁴ or R⁶) of each of the four subspaces. | R⁶: row space, null space; R⁴: column space, left null space | — |
| A2 | apply | short-answer | 0.42 | For a 2×5 matrix A, which spaces live in R² and which in R⁵? | R²: column space, left null space; R⁵: row space, null space | — |
| A3 | apply | short-answer | 0.47 | For the square matrix A=[[1,1],[2,2]], both ambient spaces are the same (R²). Which subspace pairing therefore becomes possible that isn't for a rectangular matrix? | row space and column space can be directly compared as subsets of the same space R² — here row space is span{(1,1)} and column space is also span{(1,1)}, so they coincide in this example | — |
| A4 | apply | short-answer | 0.52 | For a 6×2 matrix with rank 2 (full column rank), what can you say about its null space? | N(A)={0} — full column rank means the columns are independent, so no nontrivial combination gives 0 | — |
| A5 | apply | short-answer | 0.57 | A 3×4 matrix A has rank 3 (full row rank). What is its left null space? | {0} — full row rank means the rows are independent, which corresponds to a trivial left null space (rank=m) | — |
| A6 | apply | short-answer | 0.62 | Which two subspaces would you check to determine whether Ax=b has a solution, and separately, whether that solution is unique? | column space (existence: is b∈C(A)?) and null space (uniqueness: is N(A)={0}?) | — |
| E2 | explain | short-answer | 1.02 | Why does a square matrix collapse the usual "input space vs. output space" distinction between the four subspaces? | when m=n, Rⁿ and Rᵐ are literally the same space, so there is no longer a geometric distinction between where the "input-side" pair (row space, null space) and "output-side" pair (column space, left null space) live — they're all subsets of the same Rⁿ, even though they remain conceptually distinct roles *(required)* | — |
| E3 | explain | short-answer | 1.07 | Why does the null space answer a question about A's *inputs*, while the column space answers a question about A's *outputs*? | the null space is defined by which inputs x produce output zero (a statement about the domain), while the column space is defined by which outputs are achievable at all (a statement about the range) — the two subspaces describe fundamentally different sides of the map A *(required)* | — |
| E4 | explain | short-answer | 1.12 | Why is it that knowing all four subspaces' dimensions for an m×n matrix requires only knowing rank(A), not four independent numbers? | Rank-Nullity ties null space dimension to n−rank(A) and left-null-space dimension to m−rank(A), while row space and column space dimensions both equal rank(A) itself — every one of the four dimensions is a function of the single number rank(A) *(required: names rank as the single generating quantity)* | — |
| E5 | explain | short-answer | 0.92 | Why must the row space and column space of the same matrix A have the same dimension, even though they can live in entirely different spaces? | this is the row-rank-equals-column-rank theorem (`row-space`'s E1) — a nontrivial fact about elimination that holds regardless of the shape of A, ensuring rank(A) is a single well-defined number rather than two potentially different ones *(required: names the row-rank=column-rank fact)* | — |
| T2 | transfer | short-answer | 1.57 | A recommendation engine represents users and items as vectors related by a ratings matrix A. Why does knowing all four fundamental subspaces of A tell you more about the system than just knowing its rank? | rank alone gives the *size* of each subspace, but the subspaces themselves identify *which* specific user-preference or item-feature directions are reachable, redundant, or orthogonal to everything — the practical question of which recommendations are achievable, or which user profiles are indistinguishable from each other, needs the subspaces themselves, not just their dimension *(required)* | — |
| T3 | transfer | short-answer | 1.67 | An electrical circuit's behavior is modeled by Ax=b, where x are unknown currents and b are known voltage sources. Explain, using the four fundamental subspaces, what it would mean physically for A's left null space to be nontrivial. | a nontrivial left null space means there exist output combinations (voltage patterns) orthogonal to every column of A — physically, certain combinations of the voltage sources can never be produced by any combination of currents through the circuit's components, revealing a structural constraint on achievable voltage configurations *(required: connects left null space to unreachable output combinations)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.67.*

---

## Column Space (`column-space`)
*Prereq: Four Fundamental Subspaces · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define C(A). | the span of A's columns — equivalently, the set of all possible outputs Ax as x ranges over every input | — |
| R2 | recall | mcq | −0.21 | C(A) equals: | the set of all possible outputs Ax | claims C(A) is "always all of Rᵐ" — only true when A's columns span the full output space, not in general → `column-space` |
| A1 | apply | short-answer | 0.34 | A=[[1,0],[0,1],[1,1]] (3×2). Describe C(A). | a 2D plane in R³, spanned by the two (independent) columns | — |
| E1 | explain | short-answer | 1.04 | Why does Ax=b have a solution if and only if b∈C(A)? | this is essentially the definition of C(A) restated: b is reachable as some Ax exactly when b is a linear combination of A's columns, which is exactly what "b∈C(A)" means *(required)* | — |
| T1 | transfer | short-answer | 1.54 | In y=Xβ+ε, fitted values Xβ̂ must lie in C(X). Why can residuals (y−Xβ̂) never be zero unless y itself already lies in C(X)? | Xβ̂ is confined to C(X) no matter what β̂ is chosen; if y is outside C(X), no choice of β̂ can make Xβ̂ equal y exactly, so some nonzero residual is unavoidable — connecting column space directly to when a perfect regression fit is even possible *(required)* | — |
| R3 | recall | mcq | −0.43 | The column space of A is another name for: | the range (or image) of A, viewed as a linear map | picks "the domain of A," confusing range and domain → `column-space` |
| R4 | recall | short-answer | −0.38 | Fill in the blank: dim(C(A)) is, by definition, equal to ___. | rank(A) | — |
| R5 | recall | mcq | −0.33 | True or false: C(A) always contains the zero vector. | True — Ax=0 for x=0, so 0∈C(A) always, since C(A) is a subspace | answers "False," missing that any subspace, including C(A), must contain 0 → `column-space` |
| R6 | recall | short-answer | −0.28 | For an m×n matrix A, C(A) is a subspace of which space? | Rᵐ | — |
| R7 | recall | mcq | −0.23 | C(A) equals all of Rᵐ exactly when: | A has rank m (full row rank, in the sense of achieving the maximum possible column-space dimension) | picks "A is square," which is neither necessary nor sufficient for C(A)=Rᵐ → `column-space` |
| R8 | recall | short-answer | −0.18 | If A has a column of all zeros, what can you say about how that column affects C(A)? | nothing — a zero column contributes only the zero vector to any combination, so it never expands C(A) beyond what the other columns already reach | — |
| R9 | recall | mcq | −0.13 | If two columns of A are identical, C(A): | is exactly the same as if the duplicate column were removed | picks "is twice as large," misunderstanding that a duplicate column adds no new span → `column-space` |
| R10 | recall | short-answer | −0.08 | Fill in the blank: b∈C(A) if and only if the equation Ax=___ has at least one solution. | b | — |
| R11 | recall | mcq | −0.03 | For A with more columns than rows (n>m), C(A) can be at most: | all of Rᵐ (bounded by the ambient space, regardless of how many columns there are) | picks "an n-dimensional space," incorrectly letting the column count exceed the ambient dimension → `column-space` |
| R12 | recall | short-answer | 0.02 | What does it mean, in terms of C(A), for a linear system Ax=b to be "inconsistent" (no solution)? | b does not lie in C(A) | — |
| R13 | recall | mcq | 0.07 | Which best describes C(A) for A=[[1,2],[2,4]]? | a line through the origin, since the two columns are parallel | picks "a plane," missing that the columns are dependent → `column-space` |
| R14 | recall | short-answer | −0.53 | Fill in the blank: C(A) is spanned by the ___ of A. | columns | — |
| R15 | recall | mcq | 0.12 | The dimension of C(A) can never exceed: | min(m,n) | picks "n only" or "m only" individually, missing the joint bound → `column-space` |
| R16 | recall | short-answer | 0.17 | True or false, with reasoning: "if A is square and invertible, C(A)=Rⁿ." | True — an invertible square matrix has full rank n, so its column space (dimension n, a subspace of Rⁿ) must be all of Rⁿ | — |
| A2 | apply | short-answer | 0.39 | A=[[1,0,1],[0,1,1],[0,0,0]] (3×3). Describe C(A). `[verified: third row all-zero, so outputs always have 0 third coordinate]` | a plane in R³, the xy-plane {(x,y,0)} — since the last row is entirely zero, every Ax has third coordinate 0 | — |
| A3 | apply | short-answer | 0.44 | Is b=(1,2,0) in the column space of A from A2? `[verified: (1,2,0) has 0 third coord]` | yes — b lies in the xy-plane {(x,y,0)}, which is exactly C(A) | — |
| A4 | apply | short-answer | 0.49 | Is b=(1,2,3) in the column space of A from A2? | no — its third coordinate is 3≠0, and every vector in C(A) has third coordinate 0 | — |
| A5 | apply | numeric | 0.54 | A=[[1,2],[3,6]] (2×2, columns parallel). What is dim(C(A))? `[verified: second column = 2×first]` | 1 — the columns (1,3) and (2,6)=2(1,3) are dependent, so they span only a line | — |
| A6 | apply | short-answer | 0.59 | A is 4×2 with independent columns. What is dim(C(A)), and is C(A) all of R⁴? | dim(C(A))=2 (rank equals the number of independent columns); no — a 2-dimensional subspace of R⁴ is only a small slice of the full 4-dimensional space | — |
| E2 | explain | short-answer | 0.99 | Why does adding a column to A that is already in the span of A's other columns leave C(A) completely unchanged? | the new column contributes no combination that wasn't already reachable using the existing columns, since it is itself expressible as one of those combinations — exactly `span`'s "already-in-the-span" fact, applied to a matrix's own columns *(required: the direct callback to span)* | — |
| E3 | explain | short-answer | 1.09 | Why does C(A) shrink (in dimension) when two of A's columns become linearly dependent, even though the number of columns hasn't changed? | C(A) is the span of the columns, and dependence among the columns means fewer independent "directions" are actually contributing — the span's dimension tracks the number of independent directions, not the raw count of vectors, so dependence directly reduces it *(required)* | — |
| E4 | explain | short-answer | 1.14 | Why is checking "is b∈C(A)?" equivalent to asking "does Ax=b have a solution?" rather than these being two separate facts to remember? | they are literally the same statement phrased two ways: C(A) is defined as the set of all Ax outputs, so "b is one of those outputs" and "some x makes Ax=b" say exactly the same thing *(required)* | — |
| E5 | explain | short-answer | 0.89 | Why can a matrix with many columns still have a very low-dimensional column space? | dim(C(A)) depends on the number of *independent* columns, not the total number of columns — if most columns are combinations of a few others, the span collapses to just those few independent directions regardless of how many total columns exist *(required)* | — |
| T2 | transfer | short-answer | 1.64 | In collaborative filtering, C(A) for a user-feature matrix A represents every rating pattern the model can currently explain. Why would a business team care about a new item whose true rating pattern falls outside C(A)? | the model, no matter how it tunes its parameters, can only ever predict rating patterns inside C(A) — an item genuinely outside that span is structurally unpredictable by the current model, a limitation that no amount of retraining on the same feature set can fix without adding new features (new columns) *(required: names that retraining alone can't fix it)* | — |
| T3 | transfer | short-answer | 1.74 | A control system's actuators can only produce forces lying in C(A) for some matrix A determined by the actuator geometry. Why might redesigning the actuators (changing A's columns) be the only way to reach a desired force outside the current C(A), rather than just commanding harder? | commanding "harder" only scales existing achievable combinations, which stays within the same span; reaching a force *direction* outside C(A) requires genuinely new columns (new actuator directions), since no linear combination of the existing columns can ever produce output outside their span *(required)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.74.*

---

## Null Space (`null-space`)
*Prereq: Four Fundamental Subspaces · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define N(A). | all x such that Ax=0 | — |
| R2 | recall | mcq | −0.21 | N(A) always contains: | at least the zero vector, possibly more | claims N(A) can be "empty sometimes" — impossible, since A·0=0 always → `null-space` |
| A1 | apply | short-answer | 0.34 | A=[[1,1],[2,2]]. Find N(A). `[verified: (1,-1) works]` | rows are dependent, giving one equation x₁+x₂=0; N(A)=span{(1,−1)} | — |
| E1 | explain | short-answer | 1.04 | Why is N(A)={0} exactly when A's columns are linearly independent? | Ax=0 means some combination of A's columns (weighted by x's entries) sums to zero; if the columns are independent, the *only* combination summing to zero is the trivial one (x=0); if dependent, a nontrivial combination exists, giving a nonzero x in N(A) *(required: connects to `linear-dependence` directly)* | — |
| T1 | transfer | short-answer | 1.54 | If Ax=b has a solution xₚ, why must it have either exactly one solution or infinitely many — never exactly two or three? | the full solution set is xₚ+N(A); N(A) is a subspace, so it's either just {0} (giving the one solution xₚ) or an infinite set (since any nonzero subspace is infinite, containing every scalar multiple of its elements) — there's no subspace with exactly 2 or 3 elements *(required)* | — |
| R3 | recall | mcq | −0.43 | N(A) is a subspace of: | Rⁿ — the domain of A, for an m×n matrix | picks "Rᵐ," confusing it with the ambient space of the column space → `null-space` |
| R4 | recall | short-answer | −0.38 | Fill in the blank: N(A) is often also called the ___ of A. | kernel | — |
| R5 | recall | mcq | −0.33 | True or false: if A is the zero matrix, N(A) is all of Rⁿ. | True — every x satisfies A·x=0 when A itself is the zero matrix | answers "False," assuming N(A) must always be a proper subspace → `null-space` |
| R6 | recall | short-answer | −0.28 | What is dim(N(A)) called? | the nullity of A | — |
| R7 | recall | mcq | −0.23 | If A is n×n and invertible, N(A) is: | {0} only | picks "all of Rⁿ," backwards from the invertible case → `null-space` |
| R8 | recall | short-answer | −0.18 | Fill in the blank: N(A)={0} exactly when A's columns are linearly ___. | independent | — |
| R9 | recall | mcq | −0.13 | The solution set of the homogeneous system Ax=0 is exactly: | N(A) | picks "C(A)," confusing the null space with the column space → `null-space` |
| R10 | recall | short-answer | −0.08 | If x₁ and x₂ both solve Ax=0, is x₁+x₂ also guaranteed to solve Ax=0? Why? | yes — A(x₁+x₂)=Ax₁+Ax₂=0+0=0, by linearity; this is exactly N(A)'s closure under addition | — |
| R11 | recall | mcq | −0.03 | Which of these vectors is guaranteed to be in every matrix's null space? | the zero vector | picks "the all-ones vector," which is only in N(A) for specific matrices, not universally → `null-space` |
| R12 | recall | short-answer | 0.02 | If A has more columns than rows (n>m), what can you conclude about N(A)? | N(A) must be nontrivial (contains more than just 0) — there are more unknowns than independent equations, guaranteeing at least one free variable | — |
| R13 | recall | mcq | 0.07 | For a matrix A with a repeated column, N(A): | is nontrivial — the repeated column gives a nonzero vector whose entries cancel it out | picks "is always {0}, since repetition doesn't matter for the null space," missing the direct dependence it creates → `null-space` |
| R14 | recall | short-answer | −0.53 | Fill in the blank: nullity(A) + rank(A) = ___. | n, the number of columns (Rank-Nullity, previewed here) | — |
| R15 | recall | mcq | 0.12 | Geometrically, N(A) for A=[[1,1],[1,1]] (both rows identical) is: | a line — the set of vectors whose coordinates sum to zero along the repeated direction | picks "a single point (just the origin)," missing that A has rank 1, not full rank → `null-space` |
| R16 | recall | short-answer | 0.17 | Why is N(A) always a subspace, and not just some arbitrary set of vectors? | it's closed under addition and scalar multiplication (both preserve Ax=0 by linearity of A) and contains 0, satisfying all three subspace checks | — |
| A2 | apply | numeric | 0.39 | A=[[1,2],[2,4]]. Find N(A) explicitly. `[verified: x1+2x2=0, e.g. (-2,1)]` | rows give the single equation x₁+2x₂=0; N(A)=span{(−2,1)} | — |
| A3 | apply | short-answer | 0.44 | A=[[1,0],[0,1]] (identity). Find N(A). | {0} only — the identity matrix has trivial null space, since Ax=x=0 forces x=0 | — |
| A4 | apply | short-answer | 0.49 | A=[[0,0],[0,0]] (zero matrix). Find N(A). | all of R² — every x satisfies Ax=0 | — |
| A5 | apply | numeric | 0.54 | A=[[1,1,1]] (1×3, a single row). Find N(A). `[verified: plane x+y+z=0]` | N(A)={(x,y,z) : x+y+z=0}, a 2-dimensional plane through the origin in R³ | — |
| A6 | apply | short-answer | 0.59 | A=[[2,0],[0,3]] (diagonal, nonzero entries). Find N(A). | {0} — both equations 2x=0 and 3y=0 force x=y=0 independently | — |
| E2 | explain | short-answer | 0.99 | Why does N(A) always contain 0, no matter what A is? | A·0=0 for any matrix A, by the basic properties of matrix multiplication (each entry of the product is a sum of zero terms) — this holds universally, regardless of A's other entries *(required)* | — |
| E3 | explain | short-answer | 1.09 | Why does a matrix with more columns than rows (n>m) always have a nontrivial null space? | a linear system with more unknowns than independent equations always has at least one free variable — that freedom translates directly into a nonzero vector satisfying Ax=0, since the system is underdetermined *(required: names the free-variable/underdetermined argument)* | — |
| E4 | explain | short-answer | 1.14 | Why does knowing one particular solution xₚ to Ax=b, together with N(A), give you *every* solution to Ax=b? | if x is any other solution, A(x−xₚ)=Ax−Axₚ=b−b=0, so x−xₚ∈N(A), meaning x=xₚ+(something in N(A)) — every solution has this exact form, and conversely every such vector is a solution *(required: the subtraction argument in both directions)* | — |
| E5 | explain | short-answer | 0.89 | Why is "the null space measures redundancy among the columns" a fair informal description? | a nonzero vector in N(A) is exactly a nontrivial combination of the columns that cancels to zero — precisely the statement that the columns are linearly dependent, i.e. redundant *(required: ties nontrivial N(A) directly to column dependence)* | — |
| T2 | transfer | short-answer | 1.64 | In a linear classifier w·x=0 (the decision boundary), why is the set of feature vectors x for which the model outputs exactly zero score literally the null space of the weight vector w (viewed as a 1×n matrix)? | the decision boundary is defined as {x : w·x=0}, which is exactly N(w) by the null space's definition — a geometric object learners often picture as "the boundary" is, algebraically, nothing more than a null space *(required)* | — |
| T3 | transfer | short-answer | 1.74 | In an overparameterized regression (more parameters than data points), why does a nontrivial null space of the design matrix mean multiple parameter settings fit the training data identically? | if x₀ solves the normal equations, then x₀+n for any n∈N(X) also solves them identically (E4's argument), since the fitted values Xβ are unaffected by adding a null-space vector to β — a nontrivial N(X) is the direct algebraic source of this non-uniqueness *(required: connects nontrivial null space to identical fitted values)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.74.*

---

## Row Space (`row-space`)
*Prereq: Four Fundamental Subspaces · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define the row space of A. | the span of A's rows, equivalently C(Aᵀ) | — |
| R2 | recall | mcq | −0.21 | Row space and column space of the same A generally: | have the *same dimension* (rank), even though they may live in different spaces | assumes their dimensions are unrelated in general, missing the deep row-rank=column-rank fact → `row-space` |
| A1 | apply | short-answer | 0.34 | A=[[1,2],[2,4],[3,6]] (3×2). Describe the row space. | span{(1,2)} — every row is a multiple of (1,2) | — |
| E1 | explain | short-answer | 1.04 | Why is it at all surprising that row rank equals column rank, given that rows and columns of a non-square matrix live in *completely different* spaces (Rⁿ vs Rᵐ)? | there's no a priori reason two quantities computed in unrelated ambient spaces should coincide numerically — the fact that they always do is one of the deepest, most useful results in elementary linear algebra, not an obvious bookkeeping coincidence *(required: names the surprise explicitly, not just restates the fact)* | — |
| T1 | transfer | short-answer | 1.54 | Row operations (Gaussian elimination) preserve the row space exactly, but generally change the column space. Why does this make row-reduction valid for finding a row-space basis but not a column-space basis directly? | each row operation replaces a row with a linear combination of rows, keeping the span of the rows unchanged; but replacing rows can create entirely different linear combinations among the columns, so the columns of a reduced matrix generally span a *different* space than A's original columns did *(required: the mechanism, not just "it works differently")* | — |
| R3 | recall | mcq | −0.43 | The row space of A is a subspace of: | Rⁿ, for an m×n matrix | picks "Rᵐ," confusing it with the column space's ambient space → `row-space` |
| R4 | recall | short-answer | −0.38 | Fill in the blank: the row space of A equals the column space of ___. | Aᵀ | — |
| R5 | recall | mcq | −0.33 | True or false: the row space of A and the row space of a row-reduced version of A are identical. | True — elementary row operations only take linear combinations of existing rows, which doesn't change their span | answers "False," thinking row reduction changes the row space → `row-space` |
| R6 | recall | short-answer | −0.28 | What is dim(row space of A) called? | the (row) rank of A, equal to rank(A) overall | — |
| R7 | recall | mcq | −0.23 | For A=[[1,0],[0,1],[1,1]] (3×2), the row space is a subspace of: | R² — the rows are 2-component vectors | picks "R³," confusing the number of rows with the dimension of each row vector → `row-space` |
| R8 | recall | short-answer | −0.18 | Why does the row space of an m×n matrix live in Rⁿ rather than Rᵐ? | each row of A has n entries, so each row is a vector in Rⁿ, and the row space (their span) inherits that ambient dimension | — |
| R9 | recall | mcq | −0.13 | If A has a row of all zeros, that row: | contributes nothing to the row space — it's already the zero vector, which every span contains | picks "doubles the row space's dimension," misunderstanding that a zero row adds no new direction → `row-space` |
| R10 | recall | short-answer | −0.08 | Fill in the blank: row-reducing a matrix to echelon form is a standard way to find a ___ for the row space. | basis | — |
| R11 | recall | mcq | −0.03 | The nonzero rows of a matrix in row-echelon form: | form a basis for the original matrix's row space | picks "form a basis for the original matrix's column space," mixing up which subspace elimination directly reveals a basis for → `row-space` |
| R12 | recall | short-answer | 0.02 | If two rows of A are identical, what happens to the row space's dimension compared to a version of A with the duplicate row removed? | nothing changes — the row space's dimension (and the space itself) is identical either way, since a duplicate row adds no new direction to the span | — |
| R13 | recall | mcq | 0.07 | For A=[[1,2],[2,4],[3,6]] (3×2, all rows parallel), the row space is: | a line through the origin, span{(1,2)} | picks "a plane," missing that all three rows are scalar multiples of each other → `row-space` |
| R14 | recall | short-answer | −0.53 | Fill in the blank: row rank equals column rank — this shared number is simply called ___. | rank(A) | — |
| R15 | recall | mcq | 0.12 | Which operation changes the row space of a matrix? | replacing a row with something that is NOT a linear combination of the existing rows | picks "swapping two rows," which leaves the row space (as a set) completely unchanged, only reordering the rows → `row-space` |
| R16 | recall | short-answer | 0.17 | Is the row space of Aᵀ the same as the column space of A? | yes — by definition, row space of A = C(Aᵀ), so equivalently, row space of Aᵀ = C((Aᵀ)ᵀ) = C(A) | — |
| A2 | apply | numeric | 0.39 | A=[[1,0],[0,1],[1,1]] (3×2). Describe the row space. `[verified: rows (1,0),(0,1) already independent, span R²]` | all of R² — the rows (1,0) and (0,1) alone already form a basis, and (1,1) adds nothing new | — |
| A3 | apply | short-answer | 0.44 | A=[[0,0],[0,0]] (zero matrix). Describe the row space. | {0} — every row is the zero vector, so the span is trivial | — |
| A4 | apply | short-answer | 0.49 | A=[[1,2,3],[2,4,6]] (2×3). Describe the row space. `[verified: row 2 = 2×row 1]` | a line in R³, span{(1,2,3)} — the second row is twice the first, contributing no new direction | — |
| A5 | apply | numeric | 0.54 | A=[[1,0,0],[0,1,0]] (2×3). Describe the row space, and give one vector that is NOT in it. `[verified: (0,0,1) has nonzero 3rd coord]` | the row space is the xy-plane {(x,y,0)} in R³; (0,0,1) is not in it, since every combination of the two rows has third coordinate 0 | — |
| A6 | apply | short-answer | 0.59 | A is 5×3 with rank 3 (full column rank in the sense of using all 3 dimensions). What is the row space? | all of R³ — a row space of dimension 3 inside the 3-dimensional ambient space must be the whole space | — |
| E2 | explain | short-answer | 0.99 | Why does swapping two rows of a matrix leave its row space unchanged, even though it changes the matrix itself? | the row space is defined as the *span* of the rows — a set operation that doesn't care about the order in which the spanning vectors are listed, so reordering rows produces the exact same set of reachable combinations *(required)* | — |
| E3 | explain | short-answer | 1.09 | Why does replacing a row with (that row) + c·(another row) leave the row space unchanged, but replacing a row with an entirely unrelated vector would not? | the new row is still a linear combination of the original rows, so it's already in the original row space's span — anything reachable using the new row was already reachable using the old rows, and vice versa; an unrelated vector, by contrast, may introduce a genuinely new direction *(required: distinguishes "combination of existing rows" from "arbitrary new vector")* | — |
| E4 | explain | short-answer | 1.14 | Why does the row space of A generally differ from the row space of a matrix obtained by deleting one of A's columns? | deleting a column removes an entry from every row vector, changing each row's ambient dimension and its actual components — the resulting rows are literally different vectors, so their span is generally an entirely different subspace, not merely a smaller version of the same one *(required)* | — |
| E5 | explain | short-answer | 0.89 | Why can you find a basis for the row space just by reading off the nonzero rows after Gaussian elimination, with no further work? | elimination preserves the row space exactly (R5), and the nonzero rows of an echelon-form matrix are automatically linearly independent (each has a pivot in a column position no later row has) — independent vectors that still span the (preserved) row space are, by definition, a basis for it *(required: both preservation and the pivot-independence argument)* | — |
| T2 | transfer | short-answer | 1.64 | A text-analysis system represents documents as rows of a term-frequency matrix. Why does the row space of that matrix represent "every combination of documents' term patterns that's achievable," and why would two documents with proportional term-frequency rows contribute nothing new to it? | the row space is exactly the span of the document rows, so it captures every reachable combination of their term patterns by definition; two proportional rows point in the same direction, so the second contributes no direction the first didn't already provide — exactly `span`'s "already in the span" fact, applied to text data *(required)* | — |
| T3 | transfer | short-answer | 1.74 | A survey has more respondents (rows) than questions (columns). Why does this alone guarantee the row space cannot exceed the dimension given by the number of *questions*, no matter how many respondents are surveyed? | the row space lives in Rⁿ, where n is the number of questions (columns) — its dimension is capped by n regardless of how many rows (respondents) exist, since the ambient space itself only has n dimensions to offer *(required: names the ambient-space cap, not just "more rows than columns")* | — |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.74.*

---

## Left Null Space (`left-null-space`)
*Prereq: Four Fundamental Subspaces · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Define the left null space of A. | all y such that yᵀA=0, equivalently N(Aᵀ) | — |
| R2 | recall | mcq | −0.21 | The left null space consists of vectors y such that: | y is orthogonal to every column of A | picks "y is a multiple of a column of A" — unrelated to the actual defining condition → `left-null-space` |
| A1 | apply | short-answer | 0.34 | A=[[1,2],[2,4]] (columns dependent). Find the left null space. `[verified: (-2,1) works]` | y₁+2y₂=0 and 2y₁+4y₂=0 are the same equation; left null space = span{(−2,1)} | — |
| E1 | explain | short-answer | 1.04 | How does the left null space of A relate to the ordinary null space of Aᵀ? | they're the *same object*, viewed two ways — "left null space of A" and "null space of Aᵀ" are two names for one subspace, since yᵀA=0 is exactly Aᵀy=0 transposed *(required: the notational unification stated explicitly)* | — |
| T1 | transfer | short-answer | 1.54 | Why does understanding what's orthogonal to every column of a design matrix X matter for understanding regression residuals? | the left null space of X characterizes directions no linear combination of X's columns can reach; regression residuals are exactly the part of y that lands outside C(X), so their structure is tied to this orthogonal complement *(required: connects the left null space to the residual space conceptually)* | — |
| R3 | recall | mcq | −0.43 | The left null space of A is a subspace of: | Rᵐ, for an m×n matrix | picks "Rⁿ," confusing it with the (ordinary) null space's ambient space → `left-null-space` |
| R4 | recall | short-answer | −0.38 | Fill in the blank: the left null space of A equals the null space of ___. | Aᵀ | — |
| R5 | recall | mcq | −0.33 | True or false: yᵀA=0 and Aᵀy=0 describe the same condition on y. | True — yᵀA=0 transposed is Aᵀy=0, the same equation written two ways | answers "False," treating yᵀA=0 and Aᵀy=0 as unrelated conditions → `left-null-space` |
| R6 | recall | short-answer | −0.28 | What is dim(left null space of A) equal to, in terms of m and rank(A)? | m − rank(A) | — |
| R7 | recall | mcq | −0.23 | If A is m×n with full row rank (rank(A)=m), the left null space is: | {0} only | picks "all of Rᵐ," backwards from the full-row-rank case → `left-null-space` |
| R8 | recall | short-answer | −0.18 | Why is it called the "left" null space, as opposed to the ordinary "null space"? | the defining equation yᵀA=0 has y multiplying A from the left (as a row vector), unlike the ordinary null space's Ax=0, where x multiplies A from the right | — |
| R9 | recall | mcq | −0.13 | The left null space consists of vectors orthogonal to every: | column of A | picks "row of A," confusing it with the condition defining the ordinary null space (orthogonal to every row) → `left-null-space` |
| R10 | recall | short-answer | −0.08 | Fill in the blank: for a square invertible matrix A, the left null space is ___. | {0} — trivial, since full rank leaves no room for a nontrivial left null space | — |
| R11 | recall | mcq | −0.03 | For an m×n matrix with m>n (more rows than columns), the left null space is generally: | nontrivial — there are more equations (rows/columns of Aᵀ) than the rank can cover, leaving room for a nonzero left null space | picks "always trivial," missing that more rows than columns tends to leave the left null space nontrivial → `left-null-space` |
| R12 | recall | short-answer | 0.02 | If y is in the left null space of A, what is yᵀA equal to? | the zero row vector, 0 | — |
| R13 | recall | mcq | 0.07 | For A=[[1,2],[2,4]] (rank 1), the left null space has dimension: | 1 — nullity of Aᵀ equals m−rank(A)=2−1=1 | picks "0," forgetting that a rank-deficient matrix has a nontrivial left null space → `left-null-space` |
| R14 | recall | short-answer | −0.53 | Fill in the blank: y is in the left null space of A exactly when y is orthogonal to every ___ of A. | column | — |
| R15 | recall | mcq | 0.12 | Which is an equivalent way to state "y is in the left null space of A"? | Aᵀy=0 | picks "Ay=0," which is the ordinary null-space condition on y, requiring y∈Rⁿ rather than Rᵐ → `left-null-space` |
| R16 | recall | short-answer | 0.17 | Why does computing the left null space of A amount to computing the ordinary null space of a different, related matrix? | N(Aᵀ) is exactly the ordinary null space, just applied to Aᵀ instead of A — so every technique for finding N(A) (row reduction, etc.) applies directly to Aᵀ to find A's left null space | — |
| A2 | apply | numeric | 0.39 | A=[[1,0],[0,1],[1,1]] (3×2). Find the left null space. `[verified: y1+y3=0, y2+y3=0 → (-1,-1,1) works]` | Aᵀy=0 means y₁+y₃=0 and y₂+y₃=0; solving gives left null space = span{(−1,−1,1)} | — |
| A3 | apply | short-answer | 0.44 | A=[[1,0],[0,1]] (identity, 2×2). Find the left null space. | {0} — the identity is full rank, so both null space and left null space are trivial | — |
| A4 | apply | short-answer | 0.49 | A=[[1,1,1],[2,2,2]] (2×3, second row = 2× first). Find the left null space. `[verified: 1*(1,1,1)+(-0.5)*(2,2,2)=0]` | span{(2,−1)} — since row 2 is twice row 1, y=(2,−1) gives yᵀA = 2(1,1,1)−1(2,2,2)=(0,0,0) | — |
| A5 | apply | numeric | 0.54 | A=[[1],[2],[3]] (3×1, a single column). Find the left null space. `[verified: dimension 3-1=2]` | a 2-dimensional subspace of R³ — all y with y₁+2y₂+3y₃=0, since rank(A)=1 and nullity=m−rank=3−1=2 | — |
| A6 | apply | short-answer | 0.59 | A is 3×3 with rank 3 (full rank). What is its left null space? | {0} — full rank (rank=m=3) leaves nullity(Aᵀ)=m−rank=0, so only the zero vector | — |
| E2 | explain | short-answer | 0.99 | Why does the left null space grow (in dimension) as A's rank drops, for a fixed number of rows m? | dim(left null space)=m−rank(A); as rank(A) decreases (more dependent rows), the gap between m and rank(A) widens, directly increasing the left null space's dimension *(required)* | — |
| E3 | explain | short-answer | 1.09 | Why is it natural that the left null space and the ordinary null space are computed by the "same" method, just applied to A versus Aᵀ? | both are defined by the same equation form, "(matrix)(vector)=0" — the left null space is just N(Aᵀ) rather than N(A), so any null-space-finding procedure (elimination, etc.) transfers directly by first transposing the matrix *(required)* | — |
| E4 | explain | short-answer | 1.14 | Why does a matrix with more rows than columns (m>n) tend to have a nontrivial left null space? | rank(A) is capped at n (since there are only n columns to be independent among), so dim(left null space)=m−rank(A) ≥ m−n > 0 whenever m>n — the "extra" rows beyond n cannot all be independent, guaranteeing some nontrivial y with yᵀA=0 *(required: the m−n>0 argument)* | — |
| E5 | explain | short-answer | 0.89 | Why does the left null space being trivial ({0}) tell you something specific about A's rows? | left null space = {0} means rank(A)=m (full row rank) — every row is genuinely independent of the others, with no nontrivial combination of rows summing to zero *(required)* | — |
| T2 | transfer | short-answer | 1.64 | In a chemical reaction network, A's columns represent reactions and its rows represent chemical species; a vector y in the left null space corresponds to a conserved quantity across all reactions. Why does yᵀA=0 capture "this combination of species amounts never changes, no matter which reaction proceeds"? | yᵀA=0 means y is orthogonal to every column (every reaction's effect vector) — so for any reaction, the weighted combination yᵀ(effect) is zero, meaning that weighted combination of species amounts is unchanged by that reaction, and therefore by any combination of reactions *(required)* | — |
| T3 | transfer | short-answer | 1.74 | A sensor network has more sensors (rows) than the true number of underlying physical signals (columns) it's built to detect. Why does a nontrivial left null space here reveal a redundancy check the network can perform "for free"? | a nontrivial left null space means some combination of the sensor readings is always orthogonal to every true signal direction, i.e. some fixed linear combination of the raw sensor outputs should always equal zero if the sensors are working correctly — any deviation from zero in that combination signals a sensor fault, with no need for extra hardware *(required: names the free consistency-check use)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.53…1.74.*

---

## Matmul on the Four Fundamental Subspaces (`matmul-four-fundamental-subspaces`)
*Prereq: Column Space, Null Space, Row Space, Left Null Space · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | For x in the null space, what is Ax? For x in the row space, what is Ax? | Ax=0 for x∈N(A); Ax is generally nonzero and lands in C(A) for x in the row space | — |
| R2 | recall | mcq | 0.04 | Every x∈Rⁿ decomposes uniquely as x=x_row+x_null. Then Ax equals: | A(x_row) — the null-space part contributes nothing | claims Ax is "always just 0," overgeneralizing the null-space behavior to the whole decomposition → `matmul-four-fundamental-subspaces` |
| A1 | apply | short-answer | 0.54 | Explain, without fully computing it, why A maps the row space *invertibly* onto the column space, even when A itself (on all of Rⁿ) is not invertible. | restricted to the row space, A has no nonzero vectors sent to 0 (those live in the null space, which the row space doesn't overlap except at 0), so distinct row-space vectors map to distinct column-space vectors — an invertible correspondence between the two, even though A is singular overall *(required: the no-overlap-except-zero argument)* | — |
| E1 | explain | short-answer | 1.24 | In what precise sense does this reveal an "invertible core" hiding inside a non-invertible matrix? | A's full failure of invertibility comes entirely from the null space collapsing those directions to 0; on the complementary row-space directions, A behaves like a genuine, invertible correspondence onto C(A) — a preview of exactly what SVD makes fully explicit *(required: names the null space as the sole source of non-invertibility)* | — |
| T1 | transfer | short-answer | 1.74 | Why does choosing a basis for Rⁿ built from a row-space basis plus a null-space basis give an especially clean picture of what A does to every vector? | in such a basis, A's action splits cleanly: the null-space basis vectors are annihilated entirely, and the row-space basis vectors map invertibly onto a basis of C(A) — every vector's image is determined by discarding its null-space component and invertibly transforming the rest *(required: describes both halves of the split)* | — |
| R3 | recall | mcq | −0.21 | Every x∈Rⁿ decomposes uniquely as x_row+x_null because: | row space and null space are orthogonal complements, together spanning all of Rⁿ | picks "because A is invertible," which isn't required for this decomposition to exist → `matmul-four-fundamental-subspaces` |
| R4 | recall | short-answer | −0.16 | Fill in the blank: restricted to the row space, A acts ___ onto the column space. | invertibly (bijectively) | — |
| R5 | recall | mcq | −0.11 | True or false: A(x_row) can itself be the zero vector for some nonzero x_row. | False — a nonzero row-space vector always maps to a nonzero output, since row space and null space share only the zero vector | answers "True," missing that the row space contains no nonzero vector that A sends to 0 → `matmul-four-fundamental-subspaces` |
| R6 | recall | short-answer | −0.06 | Why does x_null contribute nothing to Ax? | x_null is, by definition, in N(A), so Ax_null=0 | — |
| R7 | recall | mcq | −0.01 | The dimension of the row space equals the dimension of: | the column space (both equal rank(A)) | picks "the null space," conflating two different subspace dimensions → `matmul-four-fundamental-subspaces` |
| R8 | recall | short-answer | 0.09 | Fill in the blank: A maps the row space onto C(A) in a way that is ___-to-one. | one — a bijection, not many-to-one | — |
| R9 | recall | mcq | 0.14 | Given x=x_row+x_null, Ax equals: | Ax_row | picks "Ax_row+Ax_null" as if both terms mattered, forgetting Ax_null=0 makes it collapse to just Ax_row → `matmul-four-fundamental-subspaces` |
| R10 | recall | short-answer | 0.19 | Why must the row space and null space intersect only at the zero vector for this decomposition argument to work? | if a nonzero vector were in both, it would have to be simultaneously "invertibly mapped" (row space) and "annihilated" (null space) by A, a contradiction — the decomposition's cleanliness relies on there being no overlap | — |
| R11 | recall | mcq | 0.24 | The invertible correspondence between row space and column space means their dimensions must be: | equal | picks "row space's dimension must be larger," misunderstanding what an invertible correspondence between two spaces implies about their sizes → `matmul-four-fundamental-subspaces` |
| R12 | recall | short-answer | 0.29 | Fill in the blank: A's "invertible core" lives entirely on the ___ subspace. | row space | — |
| R13 | recall | mcq | 0.34 | For a square invertible matrix A, N(A)={0}, so the row-space/null-space decomposition reduces to: | just the row space, i.e. all of Rⁿ — since there's no null space to speak of, A is invertible everywhere | picks "just the null space," backwards from the invertible case → `matmul-four-fundamental-subspaces` |
| R14 | recall | short-answer | −0.31 | Fill in the blank: for x∈N(A), Ax=___; for x in the row space (x≠0), Ax is generally ___. | 0; nonzero | — |
| R15 | recall | mcq | 0.39 | Which best summarizes what "A restricted to the row space" means? | treating A as a function whose domain is limited to just the row-space vectors | picks "treating A as acting only on the null space," backwards from the intended restriction → `matmul-four-fundamental-subspaces` |
| R16 | recall | short-answer | 0.44 | Why is the map "A restricted to the row space" specifically onto C(A), not just into it? | every vector in C(A) is, by definition, Ax for some x; decomposing that x as x_row+x_null and using Ax_null=0 shows the same output is achieved by x_row alone — so every element of C(A) is hit by some row-space vector | — |
| A2 | apply | short-answer | 0.64 | For A=[[1,1],[2,2]] (row space span{(1,1)}, null space span{(1,−1)}), verify that A(1,1) is nonzero and lies in C(A). `[verified: A(1,1)=(2,4)]` | A(1,1)=(1·1+1·1, 2·1+2·1)=(2,4), which is nonzero and a multiple of A's column (1,2), i.e. lies in C(A)=span{(1,2)} | — |
| A3 | apply | short-answer | 0.69 | For the same A, verify A(1,−1)=0 directly. `[verified]` | A(1,−1)=(1·1+1·(−1), 2·1+2·(−1))=(0,0) ✓ — confirming (1,−1)∈N(A) | — |
| A4 | apply | short-answer | 0.74 | Decompose x=(3,1) into its row-space and null-space components for A=[[1,1],[2,2]]. `[verified: x=2(1,1)+(-1)(1,-1)/... ]` | write x=a(1,1)+b(1,−1): a+b=3, a−b=1 → a=2, b=1; so x_row=2(1,1)=(2,2), x_null=1(1,−1)=(1,−1), and indeed (2,2)+(1,−1)=(3,1) | — |
| A5 | apply | short-answer | 0.79 | Using A4's decomposition, compute Ax two ways: directly, and via Ax_row. `[verified: both give (4,8)]` | directly: A(3,1)=(3+1,6+2)=(4,8); via x_row: A(2,2)=(2+2,4+4)=(4,8) — matches, confirming x_null=(1,−1) contributed nothing | — |
| A6 | apply | short-answer | 0.84 | For a rank-1 2×2 matrix like A=[[1,1],[2,2]], how many independent "directions" does the row-space restriction actually need to handle? | just 1 — since rank(A)=1, the row space is only 1-dimensional (a line), so the "invertible core" is a map between two 1-dimensional spaces (a line to a line) | — |
| E2 | explain | short-answer | 1.04 | Why does describing A's "invertible core" require the row space specifically, rather than any arbitrary complement to the null space? | any complement to the null space would technically also give a one-to-one correspondence with C(A), but the row space is the *canonical* choice because it's orthogonal to the null space — giving a decomposition that's not just unique but also geometrically meaningful (via orthogonality) *(required: names orthogonality as what makes the row space the natural choice)* | — |
| E3 | explain | short-answer | 1.09 | Why does this row-space/null-space picture make it obvious that rank(A) equals dim(C(A))? | the row space maps bijectively onto C(A), and a bijection preserves dimension — so dim(row space)=dim(C(A)); since dim(row space)=rank(A) by definition, this directly gives dim(C(A))=rank(A) *(required: the bijection-preserves-dimension step)* | — |
| E4 | explain | short-answer | 1.14 | Why does this picture explain why rank(A)+nullity(A)=n without invoking a separate counting argument? | Rⁿ splits into row space (dimension rank(A)) and null space (dimension nullity(A)), which together span all of Rⁿ (dimension n) with no overlap — dimensions of a direct-sum decomposition simply add, giving rank(A)+nullity(A)=n directly from the geometry *(required: the direct-sum dimension-adding argument)* | — |
| E5 | explain | short-answer | 0.94 | Why is it not quite accurate to say "A is invertible on the row space" without any qualification about which space the output lives in? | A maps the row space (a subspace of Rⁿ) bijectively onto the column space (a subspace of Rᵐ, possibly different from Rⁿ) — it's invertible as a map *between these two specific subspaces*, not as a self-map of any single space, so the codomain must be stated explicitly *(required)* | — |
| T2 | transfer | short-answer | 1.54 | An audio compression algorithm discards the null-space component of a signal vector before transmission, keeping only its row-space component. Why does the receiver still recover the exact same transformed output A(signal) after this discarding? | Ax=Ax_row (since Ax_null=0 always), so throwing away the null-space part before applying A produces the identical result as applying A to the full original vector — the null-space component was informationally irrelevant to the output all along *(required)* | — |
| T3 | transfer | short-answer | 1.79 | A control system needs to invert a sensor equation Ax=b to recover the state x that produced measurement b. Why can the system only ever recover x_row exactly, with x_null remaining fundamentally unknowable from b alone? | A maps the row space bijectively onto C(A), so b=Ax_row uniquely determines x_row via that bijection's inverse; but any x_null could be added to the true x without changing b at all (since Ax_null=0), making x_null completely invisible to any measurement of the form Ax *(required: the invisibility of x_null argument)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.26…1.79.*

---

## Disjointness of the Four Fundamental Subspaces (`disjointness-four-fundamental-subspaces`)
*Prereq: Matmul on the Four Fundamental Subspaces · ancestors 12 · b₀ = 0.78*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.22 | State the two orthogonality relationships among the four subspaces. | row space ⊥ null space (in Rⁿ); column space ⊥ left null space (in Rᵐ) | — |
| R2 | recall | mcq | 0.08 | Row space and null space together span: | all of Rⁿ | says "it depends on the matrix" — missing that they are full orthogonal *complements*, which always together span the whole space → `disjointness-four-fundamental-subspaces` |
| A1 | apply | numeric | 0.58 | For A=[[1,1],[2,2]] (row space span{(1,1)}, null space span{(1,−1)}), verify orthogonality directly. `[verified: dot=0]` | (1,1)·(1,−1)=1−1=0 ✓ | — |
| E1 | explain | derivation | 1.28 | Prove row space ⊥ null space directly from the definitions. | for any x∈N(A), Ax=0 means *every row* of A dotted with x gives 0 — so x is orthogonal to every row, hence orthogonal to their entire span (the row space) *(required: the "orthogonal to every row, hence the span" step)* | — |
| T1 | transfer | short-answer | 1.78 | Connect back to `subspace-operations`'s T1: why does "orthogonal complement" here mean more than "these subspaces only share the zero vector"? | many unrelated subspace pairs intersect only at zero without being orthogonal complements at all; orthogonal complements additionally require every vector in one to be perpendicular to every vector in the other, *and* together span the whole ambient space — row space and null space satisfy both, not merely a trivial intersection *(required: both extra conditions named)* | treats "trivial intersection" as sufficient to conclude orthogonal complement status → `disjointness-four-fundamental-subspaces` |
| R3 | recall | mcq | −0.18 | Row space and null space are orthogonal complements in: | Rⁿ | picks "Rᵐ," mixing up which ambient space this particular pair lives in → `disjointness-four-fundamental-subspaces` |
| R4 | recall | short-answer | −0.13 | Fill in the blank: column space and left null space together span all of ___. | Rᵐ | — |
| R5 | recall | mcq | −0.08 | True or false: row space and null space share exactly one common vector. | True — only the zero vector, which every subspace contains | answers "False, they share none at all," forgetting that every subspace pair shares at least 0 → `disjointness-four-fundamental-subspaces` |
| R6 | recall | short-answer | −0.03 | What does it mean for two subspaces to be "orthogonal complements," stated as two separate conditions? | (1) every vector in one is orthogonal to every vector in the other, and (2) together they span the entire ambient space | — |
| R7 | recall | mcq | 0.02 | Column space and left null space together span: | all of Rᵐ | picks "all of Rⁿ," using the wrong ambient space for this output-side pair → `disjointness-four-fundamental-subspaces` |
| R8 | recall | short-answer | 0.07 | Fill in the blank: dim(row space) + dim(null space) = ___. | n (the ambient dimension they jointly fill) | — |
| R9 | recall | mcq | 0.12 | Which fact is a *consequence* of row space ⊥ null space, rather than a separate assumption? | Rank-Nullity (rank(A)+nullity(A)=n) | picks "Rank-Nullity is assumed first, and orthogonality follows from it," reversing the logical dependency this cluster establishes → `disjointness-four-fundamental-subspaces` |
| R10 | recall | short-answer | 0.17 | Why must dim(column space) + dim(left null space) equal m? | column space and left null space are orthogonal complements in Rᵐ, and orthogonal-complement dimensions always add to the ambient dimension | — |
| R11 | recall | mcq | 0.22 | For x∈N(A), what is Ax·y for any y? (Trick: think about what x⊥row space gives you, not this.) Actually: for x∈N(A), x is orthogonal to: | every row of A (equivalently, the whole row space) | picks "every column of A," confusing which subspace x is orthogonal to → `disjointness-four-fundamental-subspaces` |
| R12 | recall | short-answer | 0.27 | Fill in the blank: for y in the left null space, y is orthogonal to every ___ of A. | column | — |
| R13 | recall | mcq | 0.32 | The two orthogonality relationships in this cluster mirror each other by applying the same fact to: | A and Aᵀ | picks "A and A⁻¹," which isn't the relevant pairing (A need not even be invertible) → `disjointness-four-fundamental-subspaces` |
| R14 | recall | short-answer | −0.28 | Fill in the blank: N(A) is the orthogonal complement of the ___ space, inside Rⁿ. | row | — |
| R15 | recall | mcq | 0.37 | If x is simultaneously in the row space AND orthogonal to the entire row space, then x must be: | the zero vector — the only vector orthogonal to itself is 0 | picks "any vector in the null space," conflating "orthogonal to the row space" (null space) with "in the row space" — the overlap of the two is only {0} → `disjointness-four-fundamental-subspaces` |
| R16 | recall | short-answer | 0.42 | Why does knowing row space ⊥ null space immediately tell you column space ⊥ left null space too, without a separate proof? | apply the exact same orthogonality fact to Aᵀ instead of A — Aᵀ's "row space" is A's column space, and Aᵀ's "null space" is A's left null space, so the identical theorem transfers directly | — |
| A2 | apply | short-answer | 0.63 | For A=[[1,1],[2,2]], verify that column space span{(1,2)} and left null space span{(−2,1)} are orthogonal. `[verified: dot=-2+2=0]` | (1,2)·(−2,1)=−2+2=0 ✓ | — |
| A3 | apply | numeric | 0.68 | For the same A, verify dim(row space)+dim(null space)=n=2. `[verified: 1+1=2]` | dim(row space)=1 (span{(1,1)}), dim(null space)=1 (span{(1,−1)}); 1+1=2=n ✓ | — |
| A4 | apply | numeric | 0.73 | For the same A, verify dim(column space)+dim(left null space)=m=2. `[verified: 1+1=2]` | dim(column space)=1 (span{(1,2)}), dim(left null space)=1 (span{(−2,1)}); 1+1=2=m ✓ | — |
| A5 | apply | short-answer | 0.78 | For A=[[1,0],[0,1],[1,1]] (3×2, rank 2), what is the null space, and why does that make the orthogonality relationship trivial here? | N(A)={0}, since rank(A)=2=n means the columns are independent; with the null space trivial, "row space ⊥ null space" holds vacuously (nothing to check beyond the zero vector), and the row space alone fills all of R² | — |
| A6 | apply | short-answer | 0.83 | For the same A (3×2, rank 2), what is the left null space, and verify it's orthogonal to C(A). `[verified: left null space is 1D since m-rank=3-2=1]` | left null space has dimension 3−2=1; any vector in it is, by construction, orthogonal to every column of A, hence to all of C(A) — the defining property, automatically satisfied | — |
| E2 | explain | short-answer | 1.03 | Why does proving column space ⊥ left null space require essentially no new work once row space ⊥ null space is established? | the left null space is defined as N(Aᵀ) and the column space as the row space of Aᵀ — applying the already-proven row-space/null-space orthogonality theorem to the matrix Aᵀ instead of A gives exactly this second relationship for free *(required: names the Aᵀ substitution explicitly)* | — |
| E3 | explain | short-answer | 1.08 | Why does "orthogonal complement," not just "orthogonal," matter for the dimension-counting facts like rank+nullity=n? | mere orthogonality between two subspaces doesn't guarantee they span the whole space — dimension-counting like rank(A)+nullity(A)=n relies specifically on the *complement* half (spanning everything together), not just the perpendicularity half *(required: separates the two conditions and ties dimension-counting to the spanning half)* | — |
| E4 | explain | short-answer | 1.13 | Why can't row space and column space themselves be orthogonal complements of each other (for a rectangular matrix), the way row space and null space are? | row space and column space generally live in *different* ambient spaces (Rⁿ vs Rᵐ) when A is rectangular, so "orthogonal complement of each other" doesn't even make sense as a question — orthogonal complements are always defined as a pair within one shared ambient space *(required: names the different-ambient-space obstruction)* | — |
| E5 | explain | short-answer | 0.98 | Why does the same underlying theorem (row space ⊥ null space) need to be proven only once, even though it produces two distinct orthogonality facts for the whole cluster? | the "second" fact (column space ⊥ left null space) is not actually a different theorem — it's the identical statement applied to Aᵀ, so proving the general claim "row space of any matrix M is orthogonal to N(M)" for arbitrary M covers both cases as two instantiations (M=A and M=Aᵀ) *(required)* | — |
| T2 | transfer | short-answer | 1.58 | A GPS receiver's measurement model Ax=b has a nontrivial left null space. Why does this mean there is a fixed combination of the raw measurements that should always sum to (approximately) zero, regardless of the true position x — and why is checking that combination a useful sanity check on the hardware? | a nontrivial left null space means some y satisfies yᵀA=0, so yᵀb=yᵀAx=0 for the true (noiseless) b, no matter what x is — that fixed combination of measurements is structurally forced to be zero, so consistently observing it far from zero signals a sensor or model error rather than a change in position *(required: connects yᵀA=0 to a position-independent check)* | — |
| T3 | transfer | short-answer | 1.83 | An engineer claims two subspaces U and W of R⁵ with dim(U)=3, dim(W)=2, and U∩W={0} must automatically be orthogonal complements. Using this cluster's distinction, explain why they're wrong. | U and W satisfy the dimension-sum condition (3+2=5) and the trivial-intersection condition, but nothing here guarantees every vector in U is actually *perpendicular* to every vector in W — orthogonal complement requires that geometric perpendicularity condition in addition to the dimension/intersection bookkeeping, and two subspaces can satisfy the latter while sitting at an oblique (non-perpendicular) angle to each other *(required: explicitly separates the dimension/intersection facts from the perpendicularity requirement)* | conflates "dimensions add up and intersection is trivial" with genuine orthogonality → `disjointness-four-fundamental-subspaces` |

*Coverage: 16/6/5/3 — 30 items, −0.28…1.83.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| input/output-space pairing of the four subspaces reversed | `four-fundamental-subspaces` |
| square-matrix collapse of input/output spaces misunderstood | `four-fundamental-subspaces` |
| C(A) assumed to always equal the full output space | `column-space` |
| duplicate/zero columns assumed to change C(A)'s size | `column-space` |
| N(A) assumed possibly empty | `null-space` |
| more-columns-than-rows not recognized as forcing a nontrivial null space | `null-space` |
| row rank / column rank assumed independent | `row-space` |
| row operations assumed to change the row space | `row-space` |
| left null space's defining condition confused with column membership | `left-null-space` |
| left null space confused with the ordinary null space's ambient space | `left-null-space` |
| A's action on x=x_row+x_null over-generalized to "always 0" | `matmul-four-fundamental-subspaces` |
| row-space/null-space bijection assumed to allow a nonzero vector mapping to 0 | `matmul-four-fundamental-subspaces` |
| trivial intersection conflated with full orthogonal complement | `disjointness-four-fundamental-subspaces` |
| dimension-sum and trivial-intersection conditions conflated with genuine orthogonality | `disjointness-four-fundamental-subspaces` |

**Cluster total: 210 items across 7 concepts (30 each).** All numeric claims verified against the
single running example A=[[1,1],[2,2]], reused from `null-space` through
`disjointness-four-fundamental-subspaces` so the same concrete matrix accumulates meaning across the
whole cluster.
