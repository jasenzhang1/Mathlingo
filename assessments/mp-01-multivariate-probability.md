# Multivariate Probability & Asymptotics — Complete Cluster

Central Limit Theorem, Change of Variables (Jacobian), Covariance Matrix, Bivariate Normal,
Multivariate Normal, Pearson Correlation, Kullback-Leibler Divergence (7 concepts — the entire
`multivariate-probability` domain in one file). Same table format as the earlier clusters (e.g.
[foundations-of-probability.md](foundations-of-probability.md)).

This domain sits at the seam between the probability/statistics sweep and the linear-algebra sweep,
and every item here is written to cash in that position: `change-of-variables-jacobian` generalizes
`distribution-transformations` using `determinant`'s geometric meaning; `covariance-matrix` and
`multivariate-normal` are built directly on `positive-definite-matrices` and the Spectral Theorem;
`kl-divergence`'s nonnegativity proof reuses `jensen-inequality` outright.

---

## Central Limit Theorem (`central-limit-theorem`)
*Prereq: MGF, Law of Large Numbers · ancestors 16 · b₀ = 0.92*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.08 | State the Central Limit Theorem precisely. | for iid X₁,…,Xₙ with mean μ, variance σ², √n(X̄−μ)/σ converges in distribution to N(0,1) as n→∞ | — |
| R2 | recall | mcq | 0.22 | CLT applies to: | any population with finite variance, regardless of shape | claims it applies "only to normally-distributed populations" — missing CLT's entire point, per `sampling-distribution`'s earlier treatment | — |
| A1 | apply | numeric | 0.72 | Population mean 50, variance 100, n=64. State X̄'s approximate distribution per CLT. `[verified]` | approximately N(50, 100/64=1.5625) | — |
| E1 | explain | derivation | 1.42 | Sketch an MGF-based proof of CLT. `[verified: [1+t²/(2n)]^n → e^{t²/2} numerically, matching e^0.5 to 4 decimals at n=1000]` | standardize Yᵢ=(Xᵢ−μ)/σ (mean 0, var 1); its MGF near 0 is M_Y(t)=1+t²/2+o(t²); the MGF of √n(X̄−μ)/σ is [M_Y(t/√n)]ⁿ = [1+t²/(2n)+o(1/n)]ⁿ → e^{t²/2}, the standard normal's MGF; convergence of MGFs implies convergence in distribution, per `mgf`'s uniqueness property *(required: the Taylor expansion and the MGF-convergence-implies-distributional-convergence step)* | — |
| T1 | transfer | short-answer | 1.92 | How does CLT relate to `law-of-large-numbers` — what does CLT add? | LLN says X̄→μ, a single point; CLT describes the *fluctuations around* that point at the √n scale, revealing their shape (asymptotically Normal) — CLT can be seen as "zooming in" on LLN's convergence to see the randomness LLN itself doesn't describe *(required: the "zooming in" framing, not just restating both facts separately)* | — |
| R3 | recall | short-answer | −0.4 | Fill in the blank: for the CLT to apply, X₁,…,Xₙ must be ___ and have finite ___. | iid (independent and identically distributed); variance | — |
| R4 | recall | mcq | −0.35 | Which condition is *not* required by the CLT? | that the population itself be normally distributed | picks a genuine requirement (finite variance, or independence) as if it were the missing one, rather than recognizing normality of the population is never required → `central-limit-theorem` |
| R5 | recall | mcq | −0.3 | True or false: the CLT describes convergence in distribution, not convergence in probability. | true | answers "false," conflating the two distinct modes of convergence → `central-limit-theorem` |
| R6 | recall | short-answer | −0.25 | What does σ represent in the CLT's statement √n(X̄−μ)/σ → N(0,1)? | the population standard deviation | — |
| R7 | recall | mcq | −0.2 | As n→∞, the distribution of √n(X̄−μ)/σ converges to: | N(0,1), the standard normal | picks N(μ, σ²), the population's own distribution, instead of the standardized limit → `central-limit-theorem` |
| R8 | recall | short-answer | −0.15 | What is the commonly cited rule-of-thumb minimum sample size for the CLT's normal approximation to be considered adequate for most populations? | n ≥ 30 (a rough heuristic, not a guarantee) | — |
| R9 | recall | mcq | −0.1 | As n→∞, the standardized quantity √n(X̄−μ)/σ has: | mean 0 and variance 1, matching the standard normal it converges to | claims its mean and variance are still μ and σ², not recognizing that standardizing removes them by construction → `central-limit-theorem` |
| R10 | recall | short-answer | −0.05 | Fill in the blank: the CLT is why the sampling distribution of X̄ is approximately ___ for large n, regardless of the population's shape. | Normal (Gaussian) | — |
| R11 | recall | mcq | 0.0 | True or false: the CLT requires the population to have a finite fourth moment. | false — only a finite variance (second moment) is required | claims a fourth moment is required, confusing the CLT's minimal hypothesis with stronger conditions needed for rates of convergence (e.g. Berry-Esseen) → `central-limit-theorem` |
| R12 | recall | mcq | 0.05 | Which of these is a direct consequence of the CLT for a large-n sample mean? | X̄ is approximately N(μ, σ²/n) | claims X̄ is approximately N(μ, σ²), forgetting the sample mean's variance shrinks by a factor of n → `central-limit-theorem` |
| R13 | recall | short-answer | 0.1 | State the two population parameters that, together with n, determine X̄'s approximate distribution under the CLT. | the population mean μ and variance σ² | — |
| R14 | recall | mcq | 0.15 | The CLT is a statement about which mode of convergence? | convergence in distribution | picks "almost sure convergence," the strong mode used by the Strong Law of Large Numbers, not the CLT → `central-limit-theorem` |
| R15 | recall | short-answer | 0.18 | As n grows, what happens to Var(X̄) under the CLT's setup, and why does the √n rescaling compensate for it? | Var(X̄) = σ²/n shrinks toward 0; multiplying the deviation (X̄−μ) by √n exactly offsets that shrinkage, leaving a rescaled quantity with a nondegenerate limiting variance | — |
| R16 | recall | mcq | 0.25 | Which of the following situations does the CLT most directly justify? | using a normal approximation for the sampling distribution of a large-sample mean, from any population with finite variance | claims it justifies treating individual observations Xᵢ (not their average) as normal → `central-limit-theorem` |
| A2 | apply | numeric | 0.62 | Population mean 20, variance 25, n=100. State X̄'s approximate distribution per CLT. `[verified]` | approximately N(20, 25/100 = 0.25) | — |
| A3 | apply | numeric | 0.66 | Population variance 64, n=16. Give the approximate standard error of X̄. `[verified: 2.0]` | √(64/16) = 2 | — |
| A4 | apply | numeric | 0.7 | n=400 draws from Bernoulli(p=0.3). Give the approximate variance of X̄ per CLT. `[verified: 0.000525]` | p(1−p)/n = 0.21/400 = 0.000525 | — |
| A5 | apply | numeric | 0.75 | n=50, σ=10. Give the approximate standard error of X̄. `[verified: 1.4142]` | σ/√n = 10/√50 ≈ 1.4142 | — |
| A6 | apply | short-answer | 0.95 | A population is heavily right-skewed with finite variance σ²=9. For n=4 versus n=400, in which case is the CLT's normal approximation for X̄ more accurate, and why? | n=400 — larger n improves the normal approximation regardless of the population's skew, since the CLT's convergence is asymptotic in n *(required: names n=400 with the asymptotic reasoning)* | — |
| E2 | explain | short-answer | 1.3 | Why does averaging reduce variance by a factor of n, and how does that interact with the CLT's √n rescaling? | Var(X̄)=σ²/n scales down by n as n grows; the CLT rescales deviations by √n precisely to offset that shrinkage, so the limiting distribution is neither degenerate (collapsing to a point) nor exploding *(required: the explicit factor-of-n and √n-offset connection)* | — |
| E3 | explain | short-answer | 1.35 | Explain why the CLT does not claim the individual Xᵢ become normal, only X̄ does. | each Xᵢ keeps the original population's shape forever, however non-normal; the CLT is a statement about the sampling distribution of a specific *statistic* (the standardized average), not about the population itself *(required: the statistic-vs-population distinction)* | — |
| E4 | explain | short-answer | 1.5 | Without invoking the MGF proof, explain why the CLT is described as "universal" — independent of the underlying population's shape. | only two population features — the mean and variance — enter the limiting formula; any distribution with those two moments finite converges to the same N(0,1) limit after standardizing, regardless of skewness, kurtosis, or any other higher-moment feature of the population *(required: names that only the first two moments matter)* | — |
| E5 | explain | short-answer | 1.6 | Why is the CLT's convergence described as being "in distribution" rather than pointwise convergence of density functions? | for discrete or mixed populations X̄ may have no density at all at finite n; convergence in distribution — of CDFs, at every continuity point — is the precise statement that remains well-defined in those cases too, unlike a density-convergence claim *(required: names why a density-based statement would fail to cover discrete/mixed cases)* | — |
| T2 | transfer | short-answer | 2.05 | A quality-control engineer averages 25 independent defect-count measurements from a heavily skewed process. Is she justified in treating the average as approximately normal, and what should she check first? | only if the variance is finite and n=25 is "large enough" for the degree of skew present; for very heavy-tailed populations the normal approximation can still be poor at n=25, so she should confirm finite variance and consider whether a larger n or a resampling method (e.g. bootstrap) is warranted *(required: names finite variance and skew-dependent adequacy of n=25, not just "yes, CLT applies")* | — |
| T3 | transfer | short-answer | 2.15 | The CLT's rescaling factor is √n, not n. Why does that exact power matter for confidence-interval widths, and what would go wrong with a different rescaling power? | only the √n rescaling produces a nondegenerate (finite, nonzero-variance) limiting distribution — a larger power would send the rescaled statistic to 0 and a smaller power would send it to infinity; the familiar confidence-interval width ∝ 1/√n is a direct consequence of this exact scaling choice *(required: connects the scaling power to why 1/√n specifically governs interval width)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.4…2.15.*

---

## Change of Variables (Jacobian) (`change-of-variables-jacobian`)
*Prereq: PDF, Determinant · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | State the multivariate change-of-variables formula. | for Y=g(X) with g invertible and differentiable, f_Y(y) = f_X(g⁻¹(y))·\|det(J)\|, J the Jacobian matrix of g⁻¹ | — |
| R2 | recall | mcq | 0.32 | \|det(J)\| appears in the formula because it: | measures the local volume-scaling factor of the transformation | dismisses it as "an arbitrary convention" — missing the direct geometric meaning from `determinant` | — |
| A1 | apply | short-answer | 0.82 | Show that the 1-D special case of this formula reduces exactly to `distribution-transformations`'s earlier formula. | in 1D, the Jacobian matrix is the single number g⁻¹′(y), and \|det(J)\|=\|g⁻¹′(y)\| — exactly `distribution-transformations`'s f_Y(y)=f_X(g⁻¹(y))·\|g⁻¹′(y)\|, recovered as n=1 *(required: the explicit reduction, not just "they look similar")* | — |
| E1 | explain | short-answer | 1.52 | Why is the \|det(J)\| factor intuitively necessary? | a transformation that locally expands volume (\|det(J)\|>1) spreads the same total probability over a larger region, decreasing density there; a contraction (\|det(J)\|<1) concentrates probability, increasing density — density scaling is exactly the inverse of `determinant`'s volume-scaling interpretation *(required: both directions of the scaling argument)* | — |
| T1 | transfer | short-answer | 2.02 | Why does converting a 2D density from Cartesian (x,y) to polar (r,θ) require multiplying by r? `[verified: det of the polar Jacobian is exactly r]` | the Jacobian of (x,y)=(r cosθ, r sinθ) has determinant exactly r; this is precisely why polar integration always includes "r dr dθ" rather than "dr dθ" — a calculus fact many students memorize mechanically, now grounded in its density-transformation meaning *(required: the explicit determinant computation, not just citing the memorized rule)* | — |
| R3 | recall | short-answer | −0.28 | Fill in the blank: the change-of-variables formula requires g to be ___ and ___ on its domain. | invertible (bijective); differentiable | — |
| R4 | recall | mcq | −0.23 | The Jacobian matrix J in the formula is built from partial derivatives of: | g⁻¹, the inverse transformation, with respect to y | picks partial derivatives of g itself with respect to x, the wrong direction → `change-of-variables-jacobian` |
| R5 | recall | mcq | −0.18 | True or false: the formula uses det(J) directly, without an absolute value. | false — it uses \|det(J)\| | claims no absolute value is needed, which would let the formula output a negative "density" whenever the transformation reverses orientation → `change-of-variables-jacobian` |
| R6 | recall | short-answer | −0.13 | What does it mean for J to be "evaluated at y" rather than at x? | J is the matrix of partial derivatives of g⁻¹, evaluated at the point y in the transformed space, since f_Y is being expressed as a function of y | — |
| R7 | recall | numeric | −0.08 | If Y=AX for an invertible constant matrix A, what is the Jacobian determinant of the inverse map X=A⁻¹Y? `[verified: det(A⁻¹) = 1/det(A)]` | det(A⁻¹) = 1/det(A) | — |
| R8 | recall | short-answer | −0.03 | Fill in the blank: f_Y(y) = f_X(g⁻¹(y)) · ___. | \|det(J)\| | — |
| R9 | recall | mcq | 0.04 | Why must g be invertible for the formula to apply as stated? | so g⁻¹(y) is a single well-defined point, giving one unambiguous density value | claims invertibility is "just a convenience for notation," missing that a non-invertible g makes g⁻¹(y) multi-valued and the formula ill-defined → `change-of-variables-jacobian` |
| R10 | recall | short-answer | 0.09 | In the 1-D special case, what does the Jacobian matrix reduce to? | the single scalar g⁻¹′(y), the ordinary derivative of the inverse function | — |
| R11 | recall | mcq | 0.14 | The absolute value in \|det(J)\| exists because: | density must stay nonnegative, and a raw determinant can be negative when the map reverses orientation | claims it's "just a rounding convention," missing the nonnegativity requirement on densities → `change-of-variables-jacobian` |
| R12 | recall | mcq | 0.19 | True or false: the formula, as stated, applies unmodified when g is only piecewise invertible. | false — it needs to be summed over branches of the inverse | claims no modification is needed for a piecewise-invertible g → `change-of-variables-jacobian` |
| R13 | recall | short-answer | 0.24 | What object does "J" denote in this formula? | the Jacobian matrix of partial derivatives (of g⁻¹) | — |
| R14 | recall | mcq | 0.29 | The multivariate change-of-variables formula directly generalizes which earlier univariate result? | `distribution-transformations`'s f_Y(y) = f_X(g⁻¹(y))·\|g⁻¹′(y)\| | names `law-of-large-numbers`, an unrelated concept, instead of the univariate transformation formula it actually generalizes → `change-of-variables-jacobian` |
| R15 | recall | short-answer | 0.34 | Why is \|det(J)\| interpreted as a "volume-scaling factor"? | because the determinant of a linear (or locally linearized) map measures exactly how much it scales n-dimensional volume, per `determinant`'s geometric meaning | — |
| R16 | recall | numeric | 0.39 | For g the identity map (Y=X), what is \|det(J)\|? `[verified: 1]` | 1 — the identity map distorts no volume | — |
| A2 | apply | numeric | 0.87 | Y=2X (1-D). Compute the Jacobian factor \|g⁻¹′(y)\| for the inverse map. `[verified: 0.5]` | g⁻¹(y)=y/2, so g⁻¹′(y)=1/2 | — |
| A3 | apply | numeric | 0.92 | Y=AX with A=[[2,0],[0,3]]. Compute \|det(J)\| for the inverse transform. `[verified: 1/6]` | det(A)=6, so det(A⁻¹)=1/6 | — |
| A4 | apply | short-answer | 0.97 | Y=X+c (a pure shift, c constant). Find J for the inverse map and explain why every translation gives \|det(J)\|=1. | the inverse map is X=Y−c, whose Jacobian is the identity matrix, so \|det(J)\|=1 always — a translation slides every point the same amount without stretching or compressing any region *(required: identifying the identity Jacobian, not just asserting the answer)* | — |
| A5 | apply | short-answer | 1.02 | Y=3X−5 (1-D affine), X standard normal. Use the change-of-variables formula to write f_Y(y). `[verified: matches the known N(−5,9) density up to the 1/3 scaling]` | g⁻¹(y)=(y+5)/3, so \|g⁻¹′(y)\|=1/3, giving f_Y(y) = φ((y+5)/3)/3, where φ is the standard normal density *(required: the explicit substitution and the 1/3 factor)* | — |
| A6 | apply | numeric | 1.07 | Y=AX with A=[[1,1],[0,1]] (a shear). Compute \|det(J)\| for the inverse transform. `[verified: 1]` | det(A)=1, so det(A⁻¹)=1 — a shear preserves area | — |
| E2 | explain | short-answer | 1.57 | Why does a linear transformation Y=AX have a *constant* Jacobian determinant, unlike a general nonlinear g? | a linear map's derivative is the matrix A itself everywhere, so the local volume-scaling factor doesn't vary from point to point, unlike a nonlinear map whose Jacobian generally depends on where it's evaluated *(required: names the constant-derivative property of linear maps)* | — |
| E3 | explain | short-answer | 1.62 | In terms of local linearization, why must a nonlinear transformation's Jacobian be evaluated pointwise rather than computed once globally? | near any point a differentiable map is well-approximated by its (locally varying) derivative; because that local linear approximation changes from point to point, the volume-scaling factor it captures must be recomputed at each point rather than assumed constant *(required: the local-linearization argument)* | — |
| E4 | explain | derivation | 1.67 | For Y=g(X)=X³ on X>0 (so g is invertible there), derive f_Y(y) in terms of f_X. `[verified: g⁻¹′(y)=(1/3)y^{-2/3}]` | g⁻¹(y)=y^{1/3}, so g⁻¹′(y) = (1/3)y^{−2/3}; substituting gives f_Y(y) = f_X(y^{1/3})·(1/3)y^{−2/3} for y>0 *(required: the explicit derivative of the inverse and the substitution)* | — |
| E5 | explain | short-answer | 1.72 | Why does the formula, as stated, fail for a transformation g that isn't injective, e.g. Y=X² over all real X? | g⁻¹(y) is not single-valued — both x=+√y and x=−√y map to the same y — so the formula must be modified to sum the contributions of every branch of the inverse; using a single-valued g⁻¹(y) would silently drop half the probability mass *(required: identifies the two-branch structure, not just "it's not invertible")* | — |
| T2 | transfer | short-answer | 2.07 | The MVN's standardizing transform is Z=Σ^(−1/2)(X−μ). For the inverse map X=Σ^(1/2)Z+μ, what is \|det(J)\|, and how does that explain the 1/√\|Σ\| factor in the MVN density? | \|det(J)\| = \|det(Σ^(1/2))\| = √(det Σ); inverting the roles, f_X(x) = f_Z(z)·\|det(Σ^(−1/2))\| = f_Z(z)/√(det Σ) — exactly the 1/√\|Σ\| normalizing factor that appears in `multivariate-normal`'s density, now explained as the volume-scaling cost of correlating a standard normal vector *(required: connects √(det Σ) to the MVN's own normalizing constant)* | — |
| T3 | transfer | short-answer | 2.12 | For a Cartesian-to-spherical change of variables, explain qualitatively why the volume-scaling factor must depend on the radius r but not on the angular coordinates, for a rotationally symmetric transformation. | moving outward in r sweeps out increasingly large spherical shells, so the local volume-scaling factor grows with r; rotating around a fixed r sweeps out an equal infinitesimal volume regardless of angular position by symmetry, so the angles don't enter the scaling factor — the 3-D generalization of T1's 2-D r factor *(required: the shell-growing-with-r argument and the rotational-symmetry argument)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.28…2.12.*

---

## Covariance Matrix (`covariance-matrix`)
*Prereq: Covariance, Positive Definite Matrices · ancestors 27 · b₀ = 1.17*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.17 | Define the covariance matrix Σ for a random vector X. | Σᵢⱼ = Cov(Xᵢ,Xⱼ) — symmetric, variances on the diagonal, covariances off-diagonal | — |
| R2 | recall | mcq | 0.47 | Σ is guaranteed to be: | positive semi-definite always, though not necessarily strictly positive definite | claims Σ is "positive definite always" — false whenever an exact linear relationship exists among components, per `positive-definite-matrices`' earlier finding | — |
| A1 | apply | numeric | 0.97 | Var(X₁)=4, Var(X₂)=9, Cov(X₁,X₂)=3. Write Σ. | [[4,3],[3,9]] | — |
| E1 | explain | short-answer | 1.67 | What does a singular Σ (positive semi-definite but not strictly definite) mean about X's components? | reuses `positive-definite-matrices`' T1 directly: aᵀΣa=0 for some nonzero a means Var(aᵀX)=0, i.e. that linear combination is almost surely constant — an exact linear relationship among the components *(required: the direct reuse, not a fresh derivation)* | — |
| T1 | transfer | short-answer | 2.17 | Why is a sample covariance matrix always singular when there are more features than samples (p>n)? What breaks as a result? | the underlying data matrix has rank ≤ min(n,p)=n<p per `rank`'s bound, forcing the sample covariance (built from that data) to be singular too; methods requiring Σ⁻¹ (LDA, Mahalanobis distance) break down or require regularization to proceed *(required: the explicit rank-bound connection)* | — |
| R3 | recall | short-answer | −0.13 | Fill in the blank: the covariance matrix Σ is always ___ and ___. | symmetric; positive semi-definite | — |
| R4 | recall | mcq | −0.08 | The diagonal entries of Σ are: | the variances of each component | picks "the standard deviations of each component," off by a square root → `covariance-matrix` |
| R5 | recall | mcq | −0.03 | True or false: Σ is symmetric because Cov(Xᵢ,Xⱼ)=Cov(Xⱼ,Xᵢ). | true | answers "false," missing that covariance is symmetric by definition → `covariance-matrix` |
| R6 | recall | short-answer | 0.02 | What is Σᵢᵢ, the i-th diagonal entry, equal to? | Var(Xᵢ) | — |
| R7 | recall | numeric | 0.07 | If Xᵢ and Xⱼ are uncorrelated, what is Σᵢⱼ? `[verified: 0]` | 0 | — |
| R8 | recall | short-answer | 0.12 | Fill in the blank: trace(Σ) equals the sum of the ___. | variances — Σᵢ Var(Xᵢ) | — |
| R9 | recall | mcq | 0.16 | The sample covariance matrix of n observations of a p-dimensional vector is a: | p×p matrix | claims it's an n×n matrix, confusing the number of observations with the number of features → `covariance-matrix` |
| R10 | recall | short-answer | 0.24 | State Σᵢⱼ in terms of expectations. | Σᵢⱼ = E[(Xᵢ−μᵢ)(Xⱼ−μⱼ)] | — |
| R11 | recall | mcq | 0.29 | Which property does Σ NOT necessarily have? | strict positive definiteness — only positive *semi*-definiteness is guaranteed | claims symmetry is the property Σ might lack, when Σ is symmetric by construction, always → `covariance-matrix` |
| R12 | recall | mcq | 0.34 | True or false: every eigenvalue of a covariance matrix is ≥ 0. | true — a direct consequence of positive semi-definiteness | answers "false," missing that PSD forces every eigenvalue to be nonnegative → `covariance-matrix` |
| R13 | recall | short-answer | 0.37 | What does it mean, structurally, for Σ to be diagonal? | every pair of components is pairwise uncorrelated — all off-diagonal covariances are zero | — |
| R14 | recall | mcq | 0.4 | For a random vector with mutually independent components, Σ is: | diagonal | claims Σ must be the identity matrix, conflating "diagonal" with "identity" — independence fixes the off-diagonal entries to zero but says nothing about the diagonal variances → `covariance-matrix` |
| R15 | recall | short-answer | 0.43 | Describe Σᵢⱼ in words for i=j versus i≠j. | i=j: the variance of component i; i≠j: the covariance between components i and j | — |
| R16 | recall | mcq | 0.44 | Which best describes what Σ encodes, all at once? | the full second-moment (variance/covariance) structure of the random vector | describes Σ as encoding only pairwise linear correlations, missing that it also carries each component's own variance on the diagonal → `covariance-matrix` |
| A2 | apply | numeric | 1.02 | Var(X₁)=2, Var(X₂)=5, Var(X₃)=3, all pairwise covariances 0. Write Σ. `[verified]` | diag(2,5,3) | — |
| A3 | apply | numeric | 1.07 | Var(X₁)=1, Var(X₂)=4, Cov(X₁,X₂)=−1. Write Σ and check it is positive definite. `[verified: det=4−1=3>0]` | Σ=[[1,−1],[−1,4]]; det=3>0 and trace=5>0, so it is positive definite | — |
| A4 | apply | numeric | 1.12 | Using A1's Σ=[[4,3],[3,9]], compute trace(Σ) and state what it equals in words. `[verified: 13]` | trace(Σ)=4+9=13 = Var(X₁)+Var(X₂), the sum of the individual variances | — |
| A5 | apply | numeric | 1.17 | σ₁=3, σ₂=5, ρ=0.4. Write Σ. `[verified: Cov=0.4×3×5=6]` | Cov=ρσ₁σ₂=6; Σ=[[9,6],[6,25]] | — |
| A6 | apply | short-answer | 1.22 | Is Σ=[[5,7],[7,5]] a valid covariance matrix? Justify with a computation. `[verified: det=25−49=−24]` | no — det(Σ)=25−49=−24<0, so Σ is not positive semi-definite (a 2×2 PSD matrix needs a nonnegative determinant), so it cannot be a genuine covariance matrix *(required: the explicit determinant check)* | — |
| E2 | explain | short-answer | 1.72 | Why is Σ's symmetry a direct consequence of the definition of covariance, not an extra assumption? | Cov(Xᵢ,Xⱼ)=E[(Xᵢ−μᵢ)(Xⱼ−μⱼ)]=E[(Xⱼ−μⱼ)(Xᵢ−μᵢ)]=Cov(Xⱼ,Xᵢ) by commutativity of multiplication inside the expectation, so Σᵢⱼ=Σⱼᵢ automatically *(required: the commutativity step)* | — |
| E3 | explain | short-answer | 1.77 | Why must Σ be positive semi-definite, rather than merely symmetric with a nonnegative diagonal? | for any vector a, aᵀΣa = Var(aᵀX) ≥ 0, since variance can never be negative, and this holds for every a simultaneously — exactly the definition of PSD; a symmetric matrix with nonnegative diagonal but a negative eigenvalue would imply some linear combination of the components has negative variance, which is impossible *(required: the aᵀΣa = Var(aᵀX) identity)* | — |
| E4 | explain | derivation | 1.82 | Derive Σᵢⱼ = E[XᵢXⱼ] − μᵢμⱼ from Σᵢⱼ = E[(Xᵢ−μᵢ)(Xⱼ−μⱼ)]. `[verified symbolically]` | expanding the product: E[XᵢXⱼ − Xᵢμⱼ − μᵢXⱼ + μᵢμⱼ] = E[XᵢXⱼ] − μⱼE[Xᵢ] − μᵢE[Xⱼ] + μᵢμⱼ = E[XᵢXⱼ] − μᵢμⱼ − μᵢμⱼ + μᵢμⱼ = E[XᵢXⱼ] − μᵢμⱼ *(required: the full term-by-term expansion)* | — |
| E5 | explain | short-answer | 1.87 | How does Σ relate to the correlation matrix R? | Σ = D R D, where D=diag(σ₁,…,σₚ) is the diagonal matrix of standard deviations and R has 1's on the diagonal — the matrix analogue of rearranging ρ=Cov(X,Y)/(σ_Xσ_Y) into Cov(X,Y)=ρσ_Xσ_Y *(required: the explicit D R D form)* | — |
| T2 | transfer | short-answer | 2.22 | Why does PCA operate on the eigendecomposition of Σ (or its sample estimate) rather than on the raw data matrix directly? | Σ's eigenvectors give the directions of maximal variance and its eigenvalues give the variance captured along each, per the Spectral Theorem — PCA is literally finding this eigenbasis, so working from Σ goes straight to the object whose structure PCA is describing, rather than rediscovering it some other way *(required: names the eigenvector-as-max-variance-direction connection)* | — |
| T3 | transfer | short-answer | 2.27 | A portfolio's variance is wᵀΣw for weights w. Why does Σ's off-diagonal structure, not just each asset's own variance, matter for that computation? | the cross terms 2wᵢwⱼΣᵢⱼ contribute directly to portfolio variance; if assets are negatively correlated those terms are negative and can push portfolio variance *below* the weighted average of the individual variances — the entire diversification argument in finance rests on these off-diagonal covariance entries, not on the diagonal alone *(required: names the cross-term contribution and its sign)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.13…2.27.*

---

## Bivariate Normal (`bivariate-normal`)
*Prereq: Normal Distribution, Covariance, Change of Variables (Jacobian) · ancestors 20 · b₀ = 1.02*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | Name the 5 parameters that fully characterize a bivariate normal. | two means, two variances, one correlation ρ | — |
| R2 | recall | mcq | 0.32 | For a bivariate normal specifically, ρ=0: | does imply independence — a special property of the (multivariate) Normal family | claims "correlation zero never implies independence, even for Normals" — false for jointly Normal variables specifically | — |
| A1 | apply | short-answer | 0.82 | Why doesn't `covariance`'s Cov(X,X²)=0 counterexample contradict R2? | X and X² are not *jointly* Gaussian (bivariate normal) even though X alone is Normal — R2's guarantee requires *joint* normality, which that counterexample never had *(required: names the missing joint-normality condition specifically)* | applies R2's guarantee to any pair involving a Normal variable, regardless of their joint distribution → `bivariate-normal` |
| E1 | explain | derivation | 1.52 | Show directly, from the bivariate normal density formula, that ρ=0 makes it factor into the product of the two marginals. `[verified: f(1,2)=fx(1)*fy(2) exactly]` | at ρ=0, √(1−ρ²)=1 and the cross term −2ρ(x−μx)(y−μy)/(σxσy) vanishes from the exponent, leaving exp(−½[(x−μx)²/σx²+(y−μy)²/σy²]) — which splits exactly into the product of the two univariate Normal densities *(required: the explicit exponent simplification, not just citing the result)* | — |
| T1 | transfer | short-answer | 2.02 | What's the practical implication of R2 — when is checking correlation alone a valid test for independence, and when does that shortcut fail? | it's valid exactly when the variables are (at least approximately) jointly Normal; the shortcut fails the moment joint normality is violated, which is exactly why ICA (from the ML sweep) needed the *stronger* independence criterion for genuinely non-Gaussian sources *(required: the explicit connection back to ICA's motivation)* | — |
| R3 | recall | short-answer | −0.2 | Fill in the blank: for jointly normal X and Y, the marginal distribution of X is always ___. | a univariate Normal | — |
| R4 | recall | mcq | −0.15 | The parameter ρ in the bivariate normal density must lie in: | [−1, 1] | claims ρ can be any real number, missing the bounded-correlation constraint → `bivariate-normal` |
| R5 | recall | mcq | −0.1 | True or false: the conditional distribution of Y given X=x is itself normal. | true | answers "false," missing that joint normality forces every conditional to be normal too → `bivariate-normal` |
| R6 | recall | short-answer | −0.05 | Name the 5 parameters of the bivariate normal, and identify which one captures the dependence between X and Y. | μₓ, μᵧ, σₓ, σᵧ, ρ; ρ captures the dependence | — |
| R7 | recall | mcq | 0.0 | If ρ=1 exactly, X and Y are related by: | an exact positive linear (deterministic) relationship | claims ρ=1 still allows genuine randomness in the relationship, missing that ρ=±1 forces an exact linear dependence → `bivariate-normal` |
| R8 | recall | short-answer | 0.06 | Fill in the blank: at ρ=0, the bivariate normal density factors into the product of the two ___. | marginal (univariate normal) densities | — |
| R9 | recall | mcq | 0.1 | Which of these is NOT one of the bivariate normal's 5 parameters? | the joint median | picks σᵧ, one of the genuine five parameters, as if it were the extraneous one → `bivariate-normal` |
| R10 | recall | short-answer | 0.14 | The *standardized* bivariate normal (μ=0, σ=1 for both variables) is parameterized by which single quantity? | ρ alone | — |
| R11 | recall | mcq | 0.18 | True or false: every marginal and conditional distribution derived from a bivariate normal is itself normal. | true | answers "false," missing the closure property that makes the bivariate normal so tractable → `bivariate-normal` |
| R12 | recall | mcq | 0.22 | For ρ=−1, X and Y are related by: | an exact negative linear (deterministic) relationship | claims ρ=−1 means X and Y are independent, confusing the two opposite extremes → `bivariate-normal` |
| R13 | recall | short-answer | 0.26 | What does "jointly normal" assert, beyond "X is normal and Y is normal separately"? | it asserts the *full joint distribution* has the bivariate normal form — a stronger condition than each marginal merely being normal on its own | — |
| R14 | recall | mcq | 0.29 | The exponent of the bivariate normal density contains a cross term involving: | ρ(x−μₓ)(y−μᵧ)/(σₓσᵧ) | omits ρ from the cross term entirely, describing the exponent as if X and Y always entered additively → `bivariate-normal` |
| R15 | recall | short-answer | 0.33 | What happens to the bivariate normal's elliptical contours as ρ→±1? | they become increasingly elongated, collapsing toward a straight line | — |
| R16 | recall | mcq | 0.36 | Which statement about ρ=0 for a bivariate normal is correct? | it implies X and Y are independent — a special property of the (jointly) Normal family | claims ρ=0 never implies independence, over-generalizing the general-distribution caveat to the jointly-normal case where it doesn't apply → `bivariate-normal` |
| A2 | apply | short-answer | 0.87 | μₓ=0, μᵧ=0, σₓ=1, σᵧ=1, ρ=0.5. Are X and Y independent? | no — ρ≠0, and for a bivariate normal ρ=0 is required (and sufficient) for independence | — |
| A3 | apply | short-answer | 0.92 | Bivariate normal with ρ=1, μₓ=1, μᵧ=5, σₓ=2, σᵧ=3. Write Y as an exact linear function of X. `[verified: slope=σᵧ/σₓ=1.5]` | Y = 5 + 1.5(X−1) | — |
| A4 | apply | short-answer | 0.97 | Bivariate normal with ρ=0.6. Is Cov(X,Y) positive or negative? | positive — Cov(X,Y)=ρσₓσᵧ has the same sign as ρ, given σₓ, σᵧ>0 | — |
| A5 | apply | numeric | 1.02 | μₓ=2, μᵧ=−3, σₓ=1, σᵧ=4, ρ=−0.2. Compute Cov(X,Y). `[verified: −0.8]` | Cov(X,Y) = ρσₓσᵧ = −0.2×1×4 = −0.8 | — |
| A6 | apply | short-answer | 1.07 | X, Y bivariate normal and Y\|X=x ~ N(3+2x, 4). What does the "4" tell you about how the conditional variance behaves as x changes? | the conditional variance is constant at 4 regardless of x — a distinguishing feature of joint normality (homoskedastic conditional variance) | — |
| E2 | explain | short-answer | 1.57 | Why is "ρ captures all the dependence" a stronger statement for a bivariate normal than for a general joint distribution? | for jointly normal variables the entire dependence structure is linear and is fully captured by ρ together with the marginal σ's; for a general distribution there can be nonlinear dependence that ρ=0 completely misses, as `covariance`'s Cov(X, X²)=0 counterexample shows *(required: the linear-vs-nonlinear-dependence contrast)* | — |
| E3 | explain | short-answer | 1.62 | Why does the bivariate normal's conditional mean E[Y\|X=x] come out linear in x? | completing the square in the joint exponent, conditioning on X=x leaves a quadratic in y whose vertex — the conditional mean — is an affine function of x, a structural consequence of the joint exponent being quadratic in both variables together *(required: the completing-the-square framing)* | — |
| E4 | explain | derivation | 1.67 | Show that Var(Y\|X=x) for a bivariate normal does not depend on x. | completing the square in y from the joint exponent isolates a term (y − [μᵧ+ρ(σᵧ/σₓ)(x−μₓ)])²/(2σᵧ²(1−ρ²)); the coefficient of that squared term — and hence the conditional variance σᵧ²(1−ρ²) — contains no x, so it is identical for every value of x *(required: identifying that x drops out of the coefficient, not just the mean term)* | — |
| E5 | explain | short-answer | 1.72 | Why does ρ² (rather than ρ) appear in the conditional variance formula Var(Y\|X)=σᵧ²(1−ρ²)? | the conditional variance measures a *reduction* in uncertainty relative to σᵧ², and that reduction is symmetric in sign — knowing X reduces uncertainty about Y equally whether the correlation is positive or negative — so only the squared magnitude of ρ can enter *(required: the sign-symmetry argument)* | — |
| T2 | transfer | short-answer | 2.07 | Simple linear regression assumes a linear conditional mean and constant conditional variance. How does the bivariate normal justify both simultaneously? | under joint normality E[Y\|X=x] is exactly linear in x (E3) and Var(Y\|X=x) is exactly constant in x (E4) — both classical regression assumptions become theorems of joint normality rather than separate assumptions, whenever the data genuinely are bivariate normal *(required: names both E3 and E4 as the source of the two assumptions)* | — |
| T3 | transfer | short-answer | 2.12 | Two variables have ρ=0.9 but their scatterplot shows a clear U-shaped pattern. What does this rule out? | it rules out the variables being jointly (bivariate) normal — genuine bivariate normal data always shows an elliptical, not U-shaped, scatter regardless of ρ; a strong nonlinear pattern coexisting with a high linear ρ signals a non-normal joint distribution, the same caution T1 raises about correlation as an independence test *(required: names joint normality, not marginal normality, as what's ruled out)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.2…2.12.*

---

## Multivariate Normal (`multivariate-normal`)
*Prereq: Bivariate Normal, Covariance Matrix, Eigendecomposition · ancestors 34 · b₀ = 1.28*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.28 | State the general k-dimensional multivariate normal density. | f(x) = (2π)^(−k/2)\|Σ\|^(−1/2) exp(−½(x−μ)ᵀΣ⁻¹(x−μ)) | — |
| R2 | recall | mcq | 0.58 | The multivariate Normal requires Σ to be: | positive definite (strictly), so Σ⁻¹ and \|Σ\|>0 are well-defined | picks "diagonal" — Σ can have any positive-definite structure, not just a diagonal one → `multivariate-normal` |
| A1 | apply | short-answer | 1.08 | Using Σ=QΛQᵀ (the Spectral Theorem), explain why the MVN's level sets are ellipsoids aligned with Σ's eigenvectors. | writing the quadratic form (x−μ)ᵀΣ⁻¹(x−μ) in the eigenbasis of Σ decouples it into a sum of independent squared terms scaled by 1/λᵢ; equal-density contours are therefore ellipsoids whose axes point along Σ's eigenvectors, with axis lengths proportional to √λᵢ (the standard deviations along those principal directions) *(required: the eigenbasis decoupling, not just "it's an ellipse")* | — |
| E1 | explain | derivation | 1.78 | Show that Z=Σ^(−1/2)(X−μ), for X~MVN(μ,Σ), has a standard MVN distribution, and connect this to univariate standardization. | Σ^(−1/2) is defined via eigendecomposition (QΛ^(−1/2)Qᵀ); this linear transformation gives Cov(Z)=Σ^(−1/2)ΣΣ^(−1/2)=I and E[Z]=0 — the exact multivariate generalization of Z=(X−μ)/σ from `normal-distribution`, with the matrix square root replacing the scalar one *(required: the explicit Cov(Z)=I computation and the univariate parallel)* | — |
| T1 | transfer | short-answer | 2.28 | Why does the multivariate normal underlie so many classical multivariate methods (LDA, MANOVA, Gaussian graphical models)? | once multivariate normality is assumed, every conditional and marginal distribution derived from it is *also* Normal — a remarkably "closed" family under exactly the operations (conditioning, marginalizing) that statistical methods need, making downstream calculations tractable in ways a generic joint distribution would not allow *(required: the closure-under-conditioning-and-marginalizing property specifically)* | — |
| R3 | recall | short-answer | −0.05 | Fill in the blank: the k-dimensional MVN density requires Σ to be ___, so that Σ⁻¹ exists. | strictly positive definite (invertible) | — |
| R4 | recall | mcq | 0.0 | The mean vector μ of an MVN has dimension: | k, matching the dimension of X | claims μ is always a scalar regardless of k, confusing the mean vector with a single summary number → `multivariate-normal` |
| R5 | recall | mcq | 0.05 | True or false: a linear transformation of a multivariate normal vector is itself multivariate normal. | true | answers "false," missing the MVN's closure under linear (affine) transformations → `multivariate-normal` |
| R6 | recall | short-answer | 0.1 | What is the "standard" multivariate normal, analogous to N(0,1) in one dimension? | N(0, I) — the zero mean vector with the identity covariance matrix | — |
| R7 | recall | mcq | 0.15 | The quantity (x−μ)ᵀΣ⁻¹(x−μ) appearing in the MVN density is known as: | the squared Mahalanobis distance | calls it "the squared Euclidean distance," ignoring that Σ⁻¹ rescales by the covariance structure rather than treating every direction equally → `multivariate-normal` |
| R8 | recall | short-answer | 0.2 | Fill in the blank: the level sets (equal-density contours) of the MVN density are ___. | ellipsoids | — |
| R9 | recall | mcq | 0.24 | If Σ is singular (not strictly positive definite), the MVN density as written: | does not exist — the distribution is degenerate, concentrated on a lower-dimensional subspace | claims the density "still exists, just with a larger normalizing constant," missing that Σ⁻¹ and \|Σ\|^(−1/2) are undefined for singular Σ → `multivariate-normal` |
| R10 | recall | short-answer | 0.33 | What does \|Σ\| contribute to in the MVN density formula? | it appears in the normalizing constant (2π)^(−k/2)\|Σ\|^(−1/2), which ensures the density integrates to 1 | — |
| R11 | recall | mcq | 0.38 | Every marginal distribution of a subset of an MVN's components is: | itself (multivariate or univariate) normal | claims marginals of an MVN can fail to be normal, missing the closure property that makes marginalizing an MVN especially convenient → `multivariate-normal` |
| R12 | recall | mcq | 0.42 | True or false: every conditional distribution derived from an MVN is also normal. | true | answers "false," contradicting the closure-under-conditioning property T1 relies on → `multivariate-normal` |
| R13 | recall | short-answer | 0.46 | How many free parameters does a k-dimensional MVN have (mean plus distinct covariance entries)? | k (mean) + k(k+1)/2 (distinct entries of the symmetric Σ) | — |
| R14 | recall | mcq | 0.5 | The MVN density's normalizing constant (2π)^(−k/2)\|Σ\|^(−1/2) multiplies: | exp(−½(x−μ)ᵀΣ⁻¹(x−μ)) | pairs the constant with exp(−(x−μ)ᵀΣ⁻¹(x−μ)), dropping the ½ inherited from the univariate exponent → `multivariate-normal` |
| R15 | recall | short-answer | 0.54 | For k=1, what does the general MVN density formula reduce to? | the familiar univariate normal density | — |
| R16 | recall | mcq | 0.63 | Which is guaranteed about the eigenvectors of Σ for an MVN? | they give the principal axes of the ellipsoidal level sets | claims the eigenvectors are guaranteed to align with the coordinate axes, which is true only when Σ is diagonal → `multivariate-normal` |
| A2 | apply | numeric | 1.13 | X ~ N₃(μ, Σ). What are the dimensions of Σ? `[verified: 3×3]` | Σ is 3×3 | — |
| A3 | apply | short-answer | 1.18 | Σ=diag(4,9) for X=(X₁,X₂). Does this Σ imply X₁ and X₂ are independent? Justify. | yes — for a jointly normal vector, zero off-diagonal covariance implies independence, unlike the general case where zero covariance alone is not enough | — |
| A4 | apply | numeric | 1.23 | X ~ N₂(0, I₂). Compute the Mahalanobis distance of x=(3,4) from the mean. `[verified: 3²+4²=25, distance=5]` | (x−μ)ᵀΣ⁻¹(x−μ) = 9+16 = 25, so the Mahalanobis distance is √25 = 5 | — |
| A5 | apply | numeric | 1.28 | X ~ N₂(0, Σ) with Σ=[[2,0],[0,8]]. Compute the squared Mahalanobis distance of x=(2,4). `[verified: 4]` | 2²/2 + 4²/8 = 2 + 2 = 4 | — |
| A6 | apply | short-answer | 1.33 | For X ~ N_k(μ, Σ) and a fixed matrix A with vector b, state the distribution of Y=AX+b. | Y ~ N(Aμ+b, AΣAᵀ) — the MVN is closed under affine transformations | — |
| E2 | explain | short-answer | 1.83 | Why does the MVN density require Σ to be strictly positive definite, rather than merely positive semi-definite? | Σ⁻¹ must exist and \|Σ\| must be strictly positive for the normalizing constant and the exponent to be well defined; a singular Σ means the distribution is concentrated on a lower-dimensional subspace and has no density with respect to Lebesgue measure on ℝᵏ *(required: names both the Σ⁻¹ and \|Σ\|>0 requirements)* | — |
| E3 | explain | short-answer | 1.88 | Why is the k=1 case of the MVN density exactly the familiar univariate normal formula? | with k=1, Σ reduces to the scalar variance σ², so Σ⁻¹=1/σ², \|Σ\|^(−1/2)=1/σ, and the quadratic form (x−μ)ᵀΣ⁻¹(x−μ) becomes (x−μ)²/σ²; substituting these collapses the general formula to (2πσ²)^(−1/2)exp(−(x−μ)²/(2σ²)) *(required: the explicit scalar substitutions)* | — |
| E4 | explain | derivation | 1.93 | Show that AΣAᵀ is the covariance of Y=AX+b when X~N(μ,Σ). | Cov(Y)=Cov(AX+b)=Cov(AX)=E[(AX−Aμ)(AX−Aμ)ᵀ]=E[A(X−μ)(X−μ)ᵀAᵀ]=A E[(X−μ)(X−μ)ᵀ]Aᵀ=AΣAᵀ, using linearity of expectation to pull A and Aᵀ outside *(required: the explicit E[·] pull-through)* | — |
| E5 | explain | short-answer | 1.98 | Why does the MVN's closure under marginalization make it especially convenient for statistical modeling? | dropping components of X leaves the remaining components jointly normal with the corresponding sub-block of μ and Σ, with no extra integration required — a property a general joint distribution does not enjoy, where computing a marginal generally requires actually integrating the other variables out *(required: contrasts against the general-distribution case)* | — |
| T2 | transfer | short-answer | 2.33 | How does the MVN's affine-closure property explain why linear regression's predictions and residuals are jointly normal under Gaussian errors? | fitted values ŷ=Hy and residuals e=(I−H)y are both linear (affine) transformations of y; if y is multivariate normal, affine-closure (A6/E4) guarantees ŷ and e are themselves (jointly) multivariate normal, which is what licenses the exact t and F distributions used in regression inference *(required: names ŷ and e as affine images of y)* | — |
| T3 | transfer | short-answer | 2.38 | Why does the Mahalanobis distance, not ordinary Euclidean distance, correctly measure "how many standard deviations away" a point is from μ for a correlated MVN? | Euclidean distance treats every direction as equally spread out, but a correlated MVN's ellipsoidal contours mean some directions carry much more spread than others; the Mahalanobis distance rescales by Σ⁻¹, effectively converting to the standardized coordinate system where distance genuinely reflects probability density rather than raw coordinate difference *(required: names the direction-dependent spread that Euclidean distance ignores)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.05…2.38.*

---

## Pearson Correlation (`pearson-correlation`)
*Prereq: Correlation, Sample Variance · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | State the sample Pearson correlation formula. | r = Σ(xᵢ−x̄)(yᵢ−ȳ) / √(Σ(xᵢ−x̄)²Σ(yᵢ−ȳ)²) | — |
| R2 | recall | mcq | 0.27 | Like its population counterpart, sample Pearson correlation measures only: | linear association | claims it measures "any kind of association" — the same limitation `correlation`'s A2 established for ρ carries over exactly to r → `pearson-correlation` |
| A1 | apply | numeric | 0.77 | Data (1,2),(2,4),(3,5),(4,8). Compute r. `[verified: 0.9812]` | r ≈ 0.981 | — |
| E1 | explain | short-answer | 1.47 | Show term-by-term that r is exactly the sample analog of ρ=Cov(X,Y)/(σ_Xσ_Y). | the numerator Σ(xᵢ−x̄)(yᵢ−ȳ) is a sample-covariance-like sum, and the denominator terms √(Σ(xᵢ−x̄)²) are sample-SD-like quantities — r substitutes sample estimates for each population quantity in ρ's formula directly, term by term *(required: the explicit term-by-term correspondence)* | — |
| T1 | transfer | short-answer | 1.97 | Why can reporting r without a confidence interval or a test of H₀:ρ=0 be misleading, especially for small samples? | r is a *statistic* estimating the parameter ρ, per `parameter-vs-statistic`, and carries genuine sampling variability — with a small sample, r can look "impressively large" purely by chance even when the true ρ is zero, so an unqualified r overstates the evidence for a real relationship *(required: the explicit statistic-vs-parameter framing)* | — |
| R3 | recall | short-answer | −0.28 | Fill in the blank: r always lies in the interval ___. | [−1, 1] | — |
| R4 | recall | mcq | −0.23 | r is invariant under which kind of transformation of X or Y? | a positive linear (affine) rescaling — e.g. shifting and scaling by a positive constant | claims r changes under any rescaling, including a positive linear one, missing r's scale invariance → `pearson-correlation` |
| R5 | recall | mcq | −0.18 | True or false: r=0 always implies X and Y are independent. | false | answers "true," repeating for the sample statistic the same over-generalization `covariance`'s counterexample warns against for the population case → `pearson-correlation` |
| R6 | recall | short-answer | −0.13 | What population quantity is r a sample estimate of? | the population Pearson correlation ρ | — |
| R7 | recall | mcq | −0.08 | r is especially sensitive to: | outliers | claims r is "robust to outliers," the opposite of its actual behavior → `pearson-correlation` |
| R8 | recall | short-answer | −0.02 | Fill in the blank: in simple linear regression, r² is called the ___. | coefficient of determination | — |
| R9 | recall | mcq | 0.05 | Multiplying every X value by a positive constant changes r by: | nothing — r is unchanged | claims r scales by the same constant, treating r like a covariance rather than a normalized quantity → `pearson-correlation` |
| R10 | recall | short-answer | 0.1 | What sign does r take when larger X values tend to pair with larger Y values? | positive | — |
| R11 | recall | mcq | 0.14 | If X and Y have a perfect negative linear relationship, r equals: | −1 | picks 0, confusing "no linear relationship" with "perfect negative relationship" → `pearson-correlation` |
| R12 | recall | mcq | 0.18 | True or false: r is symmetric, i.e. r(X,Y)=r(Y,X). | true | answers "false," missing that the formula is symmetric in x and y → `pearson-correlation` |
| R13 | recall | short-answer | 0.21 | Name the rank-based correlation coefficient that captures monotonic, not just linear, relationships. | Spearman's rank correlation | — |
| R14 | recall | mcq | 0.24 | Multiplying every X value by a *negative* constant: | flips the sign of r | claims it leaves r unchanged, missing that a negative rescaling reverses the direction of association → `pearson-correlation` |
| R15 | recall | short-answer | 0.3 | What does an r near 0 tell you, and what does it *not* tell you, about X and Y? | it indicates little or no *linear* association; it does not indicate there is no association at all — a strong nonlinear relationship can still coexist with r≈0 | — |
| R16 | recall | mcq | 0.34 | The numerator of the sample r formula, Σ(xᵢ−x̄)(yᵢ−ȳ), is proportional to: | the sample covariance between X and Y | claims it's proportional to the sample variance of X alone, dropping Y from the numerator entirely → `pearson-correlation` |
| A2 | apply | numeric | 0.82 | Data (1,1),(2,2),(3,3),(4,4). Compute r. `[verified: 1]` | r = 1 — a perfect positive linear relationship | — |
| A3 | apply | numeric | 0.87 | Data (1,4),(2,3),(3,2),(4,1). Compute r. `[verified: −1]` | r = −1 — a perfect negative linear relationship | — |
| A4 | apply | short-answer | 0.92 | A dataset has r=0.85 between height in cm and weight in kg. If height is remeasured in inches, what happens to r? | r is unchanged at 0.85 — r is invariant under a positive linear rescaling of either variable | — |
| A5 | apply | short-answer | 0.97 | Data (1,2),(2,2),(3,2),(4,2) — Y is constant. What happens to r, and why is that the right answer rather than 0? | r is undefined, not 0 — Y has zero sample variance, so the denominator √(Σ(xᵢ−x̄)²Σ(yᵢ−ȳ)²) contains a factor of zero, making the ratio 0/0 rather than a well-defined correlation *(required: names the 0/0 structure, not just "undefined")* | — |
| A6 | apply | numeric | 1.02 | Data (2,10),(4,8),(6,12),(8,6). Compute r. `[verified: −0.4]` | r ≈ −0.4 | — |
| E2 | explain | short-answer | 1.52 | Why is r invariant under a positive linear (affine) rescaling of X or Y, working directly from the formula? | scaling X by a>0 and shifting by b scales every deviation (xᵢ−x̄) by a (the shift cancels inside the deviation), which scales the numerator by a and the √(Σ(xᵢ−x̄)²) term in the denominator by a as well — the a's cancel exactly, leaving r unchanged *(required: the explicit cancellation, not just citing invariance)* | — |
| E3 | explain | short-answer | 1.57 | Why does r become undefined, rather than equal to 0, when one variable has zero sample variance? | the denominator √(Σ(xᵢ−x̄)²Σ(yᵢ−ȳ)²) contains a factor of zero when one variable is constant, making the whole ratio 0/0 — an undefined quantity, not a well-defined correlation of exactly zero *(required: identifies the 0/0 form)* | — |
| E4 | explain | derivation | 1.62 | Express r in terms of standardized (z-scored) values zₓᵢ=(xᵢ−x̄)/sₓ and z_yᵢ=(yᵢ−ȳ)/s_y, and show it reduces to the usual formula. | Σzₓᵢz_yᵢ = Σ[(xᵢ−x̄)(yᵢ−ȳ)]/(sₓs_y); dividing by (n−1) and recognizing sₓs_y(n−1)=√(Σ(xᵢ−x̄)²Σ(yᵢ−ȳ)²) recovers r exactly, showing r is (up to n−1) the average product of standardized values *(required: the explicit substitution back to the original ratio form)* | — |
| E5 | explain | short-answer | 1.67 | Why is r bounded in [−1,1], using the same Cauchy-Schwarz argument `cauchy-schwarz`'s T1 used for the population ρ? | treating the deviation vectors (xᵢ−x̄) and (yᵢ−ȳ) as vectors in ℝⁿ, r is exactly the cosine of the angle between them — numerator is their dot product, denominator is the product of their norms; Cauchy-Schwarz guarantees any such cosine lies in [−1,1] *(required: the explicit vectors-and-cosine identification)* | — |
| T2 | transfer | short-answer | 2.02 | A dataset shows r=0.02 between X and Y, but the scatterplot reveals a clear symmetric parabola centered at x̄. Reconcile "r near zero" with this pattern. | r detects only linear association; a perfectly deterministic but symmetric nonlinear relationship like a centered parabola can produce essentially zero linear correlation, because the positive and negative deviations of Y cancel in the numerator sum, even though X fully determines Y — the same point R15/T1 make, now illustrated concretely *(required: the deviation-cancellation mechanism, not just "r only measures linear")* | — |
| T3 | transfer | short-answer | 2.07 | Why does reporting only r, without a scatterplot, risk missing Anscombe's-quartet-style situations where wildly different data patterns share the same r? | r condenses an entire bivariate relationship into a single number capturing only its linear component and reacting to outliers in a particular way; datasets that are linear, curved, or dominated by one outlier can all produce nearly identical r values, so the summary statistic alone cannot distinguish genuinely different relationships — visualizing the data is necessary to catch this *(required: names that r alone cannot distinguish these cases)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.28…2.07.*

---

## Kullback-Leibler Divergence (`kl-divergence`)
*Prereq: Expectation · ancestors 10 · b₀ = 0.70*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.3 | Define KL divergence. | D_KL(P‖Q) = E_P[log(P(X)/Q(X))] — measures how different Q is from reference P | — |
| R2 | recall | mcq | 0.0 | D_KL(P‖Q) = D_KL(Q‖P): | is generally false — KL divergence is asymmetric | claims KL "is always symmetric," treating it as a true distance/metric → `kl-divergence` |
| A1 | apply | short-answer | 0.5 | What is always true about D_KL(P‖Q), regardless of P and Q? | it is always ≥0, with equality if and only if P=Q (almost everywhere) — Gibbs' inequality | — |
| E1 | explain | derivation | 1.2 | Prove D_KL(P‖Q)≥0 using Jensen's inequality. `[verified numerically: KL(0.5,0.5 vs 0.9,0.1)=0.511>0]` | D_KL(P‖Q) = E_P[−log(Q(X)/P(X))]; since −log is convex, Jensen gives E_P[−log(Q(X)/P(X))] ≥ −log(E_P[Q(X)/P(X)]); E_P[Q(X)/P(X)] = Σₓ P(x)·Q(x)/P(x) = Σₓ Q(x) = 1 over P's support, so the bound is −log(1)=0 *(required: the full Jensen application with −log's convexity, and the sum-to-1 step)* — a direct reuse of `jensen-inequality` | — |
| T1 | transfer | short-answer | 1.7 | Why is KL divergence so widely used in statistics and ML despite not being a true distance? | it's always nonnegative with a well-defined minimum of exactly 0 at P=Q (E1's Gibbs' inequality), which is precisely what makes "minimize KL divergence" a sensible optimization objective — this same nonnegativity property is what `t-sne` and `variational-inference-elbo` both lean on when using it as a loss *(required: names the nonnegative-minimum property as what licenses using it as an optimization target)* | — |
| R3 | recall | short-answer | −0.55 | Fill in the blank: D_KL(P‖Q) = E_P[log(___)]. | P(X)/Q(X) | — |
| R4 | recall | mcq | −0.5 | KL divergence requires which relationship between the supports of P and Q? | Q(x)>0 wherever P(x)>0 (absolute continuity of P with respect to Q) | claims P and Q must have identical supports, a stronger requirement than what the formula actually needs → `kl-divergence` |
| R5 | recall | mcq | −0.45 | True or false: KL divergence satisfies the triangle inequality. | false | answers "true," missing a second reason (beyond asymmetry) that KL is not a true metric → `kl-divergence` |
| R6 | recall | short-answer | −0.4 | What does D_KL(P‖Q)=0 mean? | P and Q are equal, almost everywhere | — |
| R7 | recall | mcq | −0.35 | KL divergence is best described as a measure of: | how one distribution diverges from a reference distribution, not a true distance metric | describes it as "the shortest path between two distributions," importing a metric-space intuition that doesn't apply → `kl-divergence` |
| R8 | recall | short-answer | −0.25 | Fill in the blank: H(P,Q) = H(P) + ___. | D_KL(P‖Q) | — |
| R9 | recall | mcq | −0.2 | Which property does D_KL lack that a true metric requires? | symmetry (D_KL(P‖Q)≠D_KL(Q‖P) in general) | claims D_KL lacks non-negativity, contradicting Gibbs' inequality established in A1 → `kl-divergence` |
| R10 | recall | short-answer | −0.15 | In D_KL(P‖Q), which distribution is the expectation taken over? | P, the reference (often "true" or data) distribution | — |
| R11 | recall | mcq | −0.1 | The two directions D_KL(P‖Q) and D_KL(Q‖P) are respectively called: | forward KL and reverse KL | swaps the names, calling D_KL(P‖Q) "reverse KL" → `kl-divergence` |
| R12 | recall | mcq | −0.05 | True or false: D_KL(P‖Q) can be negative. | false — it is always ≥0 | answers "true," contradicting Gibbs' inequality (A1) → `kl-divergence` |
| R13 | recall | short-answer | 0.05 | What inequality underlies the proof that D_KL≥0? | Jensen's inequality, applied to the convex function −log — this is Gibbs' inequality | — |
| R14 | recall | mcq | 0.1 | For discrete distributions, D_KL(P‖Q) is computed as a: | sum, Σₓ P(x) log(P(x)/Q(x)) | describes it as a maximum over x of the log-ratio, rather than a probability-weighted sum → `kl-divergence` |
| R15 | recall | short-answer | 0.15 | For continuous distributions, how does the KL divergence formula change from the discrete case? | the sum becomes an integral: ∫ p(x) log(p(x)/q(x)) dx | — |
| R16 | recall | mcq | 0.2 | Which ML use case relies directly on minimizing a KL divergence? | the ELBO objective in variational inference (e.g. VAEs) | names "gradient descent step size selection," an unrelated hyperparameter choice, instead of a genuine KL-minimization objective → `kl-divergence` |
| A2 | apply | numeric | 0.55 | P=(0.5,0.5), Q=(0.5,0.5). Compute D_KL(P‖Q). `[verified: 0]` | 0 — P and Q are identical | — |
| A3 | apply | numeric | 0.6 | P=(0.8,0.2), Q=(0.5,0.5). Compute D_KL(P‖Q). `[verified: 0.1927]` | D_KL(P‖Q) = 0.8·log(0.8/0.5) + 0.2·log(0.2/0.5) ≈ 0.193 | — |
| A4 | apply | numeric | 0.65 | Using A3's P and Q, compute D_KL(Q‖P) and compare it to D_KL(P‖Q). `[verified: 0.2231 vs 0.1927]` | D_KL(Q‖P) = 0.5·log(0.5/0.8) + 0.5·log(0.5/0.2) ≈ 0.223, different from A3's 0.193, confirming the asymmetry | reports the two directions as necessarily equal, expecting D_KL(Q‖P)=D_KL(P‖Q) → `kl-divergence` |
| A5 | apply | numeric | 0.7 | P=(0.6,0.4), Q=(0.4,0.6). Compute D_KL(P‖Q). `[verified: 0.0811]` | D_KL(P‖Q) = 0.6·log(0.6/0.4) + 0.4·log(0.4/0.6) ≈ 0.081 | — |
| A6 | apply | numeric | 0.75 | P=(0.9,0.1), Q=(0.5,0.5). Compute D_KL(P‖Q). `[verified: 0.3681]` | D_KL(P‖Q) = 0.9·log(0.9/0.5) + 0.1·log(0.1/0.5) ≈ 0.368 | — |
| E2 | explain | short-answer | 1.25 | Why does D_KL(P‖Q) require Q(x)>0 wherever P(x)>0, and what happens to the formula if this fails? | the ratio P(x)/Q(x) would involve division by zero wherever P assigns positive probability but Q assigns none, sending log(P(x)/Q(x)) to +∞; the divergence is then taken to be +∞ in that case, reflecting that Q cannot represent an event P says can happen at all *(required: the division-by-zero mechanism, not just "it's undefined")* | — |
| E3 | explain | short-answer | 1.3 | Why does D_KL(P‖Q)≠D_KL(Q‖P) in general — what breaks the symmetry, mechanically? | D_KL(P‖Q)'s expectation is taken over P, weighting the log-ratio by P's probabilities, while D_KL(Q‖P) weights the same log-ratio (sign-flipped, since log(Q/P)=−log(P/Q)) by Q's probabilities instead — averaging the same quantity under two different distributions generally gives two different numbers *(required: names the different weighting distribution as the mechanism)* | — |
| E4 | explain | derivation | 1.35 | Derive H(P,Q)=H(P)+D_KL(P‖Q) from the definitions of cross-entropy and KL divergence. `[verified symbolically]` | H(P,Q)=E_P[−log Q(X)]; D_KL(P‖Q)=E_P[log P(X)−log Q(X)]=E_P[−log Q(X)]−E_P[−log P(X)]=H(P,Q)−H(P); rearranging gives H(P,Q)=H(P)+D_KL(P‖Q) *(required: the explicit E_P[·] manipulation, not just quoting the identity)* | — |
| E5 | explain | short-answer | 1.4 | Why does minimizing cross-entropy H(P,Q) over Q, with the data distribution P fixed, amount to minimizing D_KL(P‖Q)? | from E4's decomposition H(P,Q)=H(P)+D_KL(P‖Q), and H(P) does not depend on Q at all once P is fixed — so the two objectives differ only by a Q-independent constant, and minimizing one over Q minimizes the other *(required: names H(P) as the Q-independent constant)* | — |
| T2 | transfer | short-answer | 1.75 | Why is "reverse KL" D_KL(Q‖P) typically minimized in variational inference, rather than forward KL D_KL(P‖Q)? | reverse KL only requires taking expectations under Q, the tractable distribution being optimized, whereas forward KL would require expectations under the often-intractable true posterior P; reverse KL is also "mode-seeking" — it concentrates Q's mass on a single high-probability region of P rather than spreading it to cover every mode — a property variational inference exploits *(required: names both the tractability reason and the mode-seeking behavior)* | — |
| T3 | transfer | short-answer | 1.8 | Symmetrized alternatives like Jensen-Shannon divergence are sometimes preferred to KL. What property are they fixing, and at what cost? | JS divergence symmetrizes (and bounds) KL, fixing the asymmetry that makes D_KL(P‖Q) and D_KL(Q‖P) answer different questions and complicates direct comparisons; the cost is that JS loses KL's direct connection to maximum-likelihood estimation and the cross-entropy decomposition (E4) that make ordinary KL so natural as a training objective *(required: names both the fix and the specific cost)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.55…1.8.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| CLT restricted to already-Normal populations | `central-limit-theorem` |
| Jacobian determinant treated as an arbitrary convention rather than a volume-scaling factor | `change-of-variables-jacobian` |
| covariance matrices assumed always strictly positive definite | `covariance-matrix` |
| ρ=0⟹independence over-generalized beyond the jointly-Normal case | `bivariate-normal` |
| MVN's Σ requirement relaxed to "diagonal" | `multivariate-normal` |
| sample r treated as a linear-association-free general dependence measure | `pearson-correlation` |
| KL divergence assumed symmetric | `kl-divergence` |

**Cluster total: 35 items across 7 concepts — the entire multivariate-probability domain.** All
numeric and derivation claims verified, including the exact bivariate-normal factorization at ρ=0 and
the MGF Taylor-expansion convergence to e^0.5 at n=1000.
