# Machine Learning Cluster 6 — Trees & Ensembles

Decision Tree, Splitting Criteria, Pruning Trees, Ensemble Methods, Bagging, Random Forests, AdaBoost,
Gradient Boosting, XGBoost (9 concepts). Same format as [Cluster 1](ml-01-foundations.md).

`splitting-criteria`'s E1 closes the loop all the way back to the very first pilot file: Gini impurity
for a binary split is *exactly* 2p(1−p), twice the Bernoulli variance from `bernoulli-binomial`, not
merely an analogous formula.

---

## Decision Tree (`decision-tree`)
*Prereq: Classification vs Regression · ancestors 4 · b₀ = 0.30*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.7 | Describe a decision tree's prediction mechanism. | a sequence of if-then splits on feature values, recursively partitioning the data, ending in leaf nodes that give a prediction (majority class or average value) | — |
| R2 | recall | mcq | −0.45 | Unlike KNN, decision trees are: | invariant to feature scale, since splits compare relative order within one feature at a time | claims they're "sensitive to feature scale" — directly backwards from KNN's own weakness → `decision-tree` |
| A1 | apply | short-answer | 0.1 | A tree splits on "income > $50,000." Does rescaling income to thousands ("income > 50") change predictions? | no — the threshold rescales correspondingly, but the actual partition of the data (who's above/below) is unchanged, since the split only compares relative ordering *(required)* | — |
| E1 | explain | short-answer | 0.8 | Why are decision trees called "greedy" in how they're built? | at each step, the tree picks the single best split available right now by some criterion, without considering whether a locally-worse split might lead to a better tree several levels down — local, not global, optimality *(required)* | — |
| T1 | transfer | short-answer | 1.3 | Why does a single tree grown to full depth almost always overfit severely, and what two different fixes does this motivate? | an unconstrained tree splits until every leaf is pure, becoming extremely flexible and essentially memorizing the training data — a textbook high-variance, low-bias failure mode per `bias-variance-tradeoff`; this motivates both pruning (simplify one tree) and ensemble methods (combine many trees) as two different fixes for the same underlying problem *(required: names both fixes and that they address the same root cause)* | — |

| R3 | recall | short-answer | −1.1 | What is a "leaf node" in a decision tree? | a terminal node with no further splits, holding the tree's final prediction for any example that reaches it | — |
| R4 | recall | short-answer | −1.05 | What is a "root node"? | the topmost node of the tree, containing the entire dataset before any splits are made | — |
| R5 | recall | mcq | −0.85 | For a classification tree, a leaf's prediction is typically: | the majority class among the training examples that ended up in that leaf | claims it's "the average of all training examples' feature values in that leaf" — that describes a regression-tree leaf value, not a classification prediction → `decision-tree` |
| R6 | recall | short-answer | −0.95 | Fill in the blank: a decision tree recursively partitions the feature space using a sequence of ___-based rules. | threshold (if-then / axis-aligned split) |
| R7 | recall | mcq | −0.65 | For a regression tree, a leaf's prediction is typically: | the average target value among the training examples in that leaf | claims it's "the majority class of examples in that leaf" — majority-vote is for classification, not for predicting a continuous target → `decision-tree` |
| R8 | recall | short-answer | −0.75 | True or false: a decision tree's splits are always axis-aligned (comparing one feature to a threshold), not diagonal combinations of features. | true — standard decision trees split on one feature at a time against a threshold | — |
| R9 | recall | mcq | −0.5 | The depth of a decision tree refers to: | the number of splits along the longest path from root to a leaf | claims it's "the total number of leaf nodes" — depth measures the longest root-to-leaf path length, a different quantity from leaf count → `decision-tree` |
| R10 | recall | short-answer | −0.4 | What are the two broad task types a decision tree can be built for? | classification and regression | — |
| R11 | recall | mcq | −0.3 | Decision trees are considered highly interpretable mainly because: | each prediction can be traced as a short, human-readable sequence of if-then rules | claims it's "because their predictions are always more accurate than other models" — interpretability is about tracing the reasoning, not about accuracy → `decision-tree` |
| R12 | recall | short-answer | −0.2 | What determines how many training examples end up in a given leaf? | which sequence of split conditions (from root to that leaf) every example happens to satisfy | — |
| R13 | recall | mcq | −0.1 | A decision tree with only one split (depth 1) is sometimes called: | a decision stump | claims it's called "a forest" — a forest refers to a collection of many trees (as in random forests), not a single shallow tree → `decision-tree` |
| R14 | recall | short-answer | 0.0 | Fill in the blank: building a decision tree by repeatedly choosing the locally best split is an example of a ___ algorithm. | greedy |
| R15 | recall | mcq | −0.05 | Which of these is NOT typically a hyperparameter of a decision tree? | the learning rate | selects "maximum depth" as not a hyperparameter — but max depth *is* a standard, commonly-tuned decision-tree hyperparameter, unlike learning rate which belongs to gradient-based methods → `decision-tree` |
| R16 | recall | short-answer | 0.1 | Does a decision tree require the target variable to be numeric, categorical, or can it handle either? | either — the same recursive splitting structure supports classification (categorical target) and regression (numeric target) trees | — |
| A2 | apply | short-answer | 0.4 | A tree splits on "age > 30" at the root, then on "income > 50000" within the "age > 30" branch. Which examples reach the leaf satisfying both conditions? | only examples with age > 30 AND income > 50000 — reaching a leaf requires satisfying every split condition on the path from the root | — |
| A3 | apply | short-answer | 0.45 | Given a regression tree leaf containing target values [10, 20, 30], what would that leaf predict for any new example routed there? | 20 — the average of the leaf's training target values | — |
| A4 | apply | short-answer | 0.5 | A categorical feature "color" with values {red, blue, green} is used in a tree. Can the tree meaningfully use a single threshold split like "color > red"? | not directly — categorical features without a natural order need a different split mechanism (e.g. one-vs-rest grouping of categories) rather than a numeric-style threshold comparison | — |
| A5 | apply | short-answer | 0.35 | If a tree's max depth hyperparameter is set to 1, how many total splits can the tree make? | exactly 1 — depth 1 means only the root node's single split is allowed, producing two leaves | — |
| A6 | apply | short-answer | 0.55 | Two nearly-identical examples differ only in one feature crossing a split threshold by a tiny margin. Could they end up predicted very differently? | yes — since predictions are entirely determined by which side of hard thresholds an example falls on, an infinitesimally small change that crosses a threshold can route the example to a completely different leaf with a very different prediction | — |
| E2 | explain | short-answer | 0.85 | Why can a decision tree, in principle, fit the training data perfectly if grown without any depth limit? | with no stopping criterion, the tree can keep splitting until every leaf contains examples of a single class (or a single target value), effectively memorizing the training set example by example *(required)* | — |
| E3 | explain | short-answer | 0.9 | Why does a decision tree's interpretability come specifically from its "if-then" structure rather than from any property of its predictions? | interpretability is about being able to explain *why* a specific prediction was made — following the exact chain of threshold comparisons that led an example to its leaf gives a short, human-readable justification, regardless of how accurate or complex the overall model's behavior is *(required)* | — |
| E4 | explain | short-answer | 0.95 | Why does a decision tree naturally handle interactions between features (e.g. "the effect of feature A depends on feature B") without any explicit feature-engineering step? | each split can occur along a different feature within different branches, so the sequence of conditions an example must satisfy to reach a given leaf can encode arbitrarily complex feature interactions purely through the tree's branching structure *(required)* | — |
| E5 | explain | short-answer | 0.8 | Why is a decision tree's axis-aligned splitting a limitation for data whose true decision boundary is diagonal? | since each split can only compare one feature to a threshold, approximating a genuinely diagonal boundary requires many small "staircase" steps rather than one clean split, needing more splits (and potentially more overfitting risk) than the true boundary's complexity would suggest *(required)* | — |
| T2 | transfer | short-answer | 1.45 | How does a decision tree's greedy, locally-optimal splitting strategy parallel greedy algorithms studied elsewhere in computer science (e.g. greedy graph algorithms)? | both make the best-looking choice available at each step without reconsidering it later or exploring alternate paths, trading the guarantee of a globally optimal solution for tractability — a decision tree's split choice at each node is exactly this kind of local, irrevocable commitment, the same tradeoff any greedy algorithm accepts *(required: names the local-vs-global optimality tradeoff explicitly)* | — |
| T3 | transfer | short-answer | 1.4 | Why does a single decision tree's tendency to overfit make it primarily useful today as a building block for ensembles rather than a standalone model, foreshadowing `ensemble-methods`? | a single unconstrained tree's high variance (per this concept's T1) makes it individually unreliable, but that very instability — different trees fit to different data subsets disagreeing in their specific errors — is exactly the kind of diversity that averaging across many trees can exploit to cancel out noise, motivating the entire tree-ensemble family covered next in this cluster *(required: connects instability to why it's useful raw material for ensembling)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.1…1.45.*

---

## Splitting Criteria (`splitting-criteria`)
*Prereq: Decision Tree · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Name two common classification splitting criteria. | Gini impurity and entropy (information gain) — both measure how "mixed" the classes are within a node; splits are chosen to maximize the reduction | — |
| R2 | recall | mcq | −0.35 | A perfectly pure node (only one class present) has: | zero impurity | claims it has "maximum impurity" — backwards → `splitting-criteria` |
| A1 | apply | numeric | 0.2 | Compute Gini = 1−Σpᵢ² for a binary node with proportions (0.5, 0.5), then for (0.9, 0.1). `[verified: 0.5, 0.18]` | Gini(0.5,0.5)=1−0.5=0.5 (maximum); Gini(0.9,0.1)=1−0.82=0.18 (much lower, more pure) | — |
| E1 | explain | short-answer | 0.9 | Why is Gini impurity maximized at p=0.5 for a binary split — and what earlier concept does this connect to exactly? | for a binary split, Gini = 1−(p²+(1−p)²) = 2p(1−p) `[verified algebraically]` — *exactly* twice the Bernoulli variance p(1−p) from `bernoulli-binomial`, not a mere resemblance; both are maximized at p=0.5 for the same reason: maximal uncertainty about a single binary outcome *(required: the explicit 2p(1−p) derivation and the Bernoulli-variance identification)* | — |
| T1 | transfer | short-answer | 1.4 | Why is a split producing two perfectly pure children always considered "good" by impurity reduction, regardless of how unbalanced the resulting sizes are (e.g. 990 vs 10)? | the impurity criterion only measures how mixed each child's classes are, not how many points land in each child — a 990/10 split into two pure groups reduces impurity to zero just as effectively as an even split would, which can make a greedy tree choose splits that look "unhelpful" by human intuition yet are mathematically optimal by this criterion *(required: names that size imbalance is simply outside what the criterion measures)* | — |

| R3 | recall | short-answer | −1.0 | What does "impurity" measure at a tree node? | how mixed the classes are among the examples at that node — low impurity means the node is mostly one class, high impurity means classes are evenly mixed | — |
| R4 | recall | short-answer | −0.95 | What is "information gain"? | the reduction in impurity (e.g. entropy) achieved by a split, compared to the parent node's impurity | — |
| R5 | recall | mcq | −0.75 | Splitting criteria are used to: | decide which feature and threshold produce the best split at each node during tree construction | claims they're used "to decide when to stop growing the tree entirely" — that's the role of a stopping criterion, a different decision from which split to make → `splitting-criteria` |
| R6 | recall | short-answer | −0.85 | Fill in the blank: entropy for a binary node with class probability p is H = −p log₂(p) − ___. | (1−p) log₂(1−p) |
| R7 | recall | mcq | −0.55 | Variance reduction, used as a splitting criterion for regression trees, plays the analogous role to: | Gini impurity or entropy for classification trees | claims it's "identical to accuracy, just renamed" — variance reduction is a distinct impurity-style measure for continuous targets, not simply another name for classification accuracy → `splitting-criteria` |
| R8 | recall | short-answer | −0.65 | True or false: entropy and Gini impurity, though computed differently, both reach their maximum at p=0.5 for a binary split. | true — both measures peak at maximal class mixing, p=0.5 | — |
| R9 | recall | mcq | −0.45 | A split is chosen at each step by: | picking the feature/threshold combination that maximizes information gain (or equivalently minimizes weighted child impurity) among the candidates considered | claims it's chosen "at random among all valid split candidates" — splitting criteria specifically exist to make this choice non-random, based on impurity reduction → `splitting-criteria` |
| R10 | recall | short-answer | −0.35 | What information does a splitting criterion need about the *children* nodes, not just the parent, to compute a split's information gain? | each child's impurity, weighted by the fraction of examples that would go to that child | — |
| R11 | recall | mcq | −0.25 | The entropy formula uses which base for its logarithm most conventionally? | base 2 (giving units of "bits") | claims it's "always base 10" — entropy in information theory and decision-tree splitting is conventionally computed in base 2 (bits) → `splitting-criteria` |
| R12 | recall | short-answer | −0.15 | What is the Gini impurity formula for a node with class proportions p₁, ..., pₖ? | Gini = 1 − Σ pᵢ² | — |
| R13 | recall | mcq | −0.05 | Compared to entropy, Gini impurity is generally: | computationally cheaper (no logarithm needed) while producing broadly similar splits in practice | claims Gini is "fundamentally incapable of being used for multi-class problems" — Gini generalizes to any number of classes via 1 − Σpᵢ² just as entropy does → `splitting-criteria` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: information gain = parent impurity − ___ impurity. | weighted average child |
| R15 | recall | mcq | 0.0 | For a regression tree, variance reduction as a splitting criterion measures: | how much the weighted average variance of the target within child nodes decreases compared to the parent | claims it measures "how balanced the two child nodes' sizes are" — variance reduction is about the target value's spread within nodes, not about node size balance → `splitting-criteria` |
| R16 | recall | short-answer | 0.1 | Does a splitting criterion look ahead multiple levels of the tree when choosing the current split? | no — it evaluates the immediate (one-step) impurity reduction only, which is exactly why tree-building is described as greedy | — |
| A2 | apply | numeric | 0.55 | Compute Gini for a binary node with proportions (0.3, 0.7). `[verified: 0.42]` | Gini = 1 − (0.3² + 0.7²) = 1 − (0.09+0.49) = 1 − 0.58 = 0.42 | — |
| A3 | apply | numeric | 0.6 | Compute entropy (base 2) for a binary node with proportions (0.5, 0.5). `[verified: 1.0]` | H = −0.5 log₂(0.5) − 0.5 log₂(0.5) = 0.5 + 0.5 = 1.0 bit | — |
| A4 | apply | numeric | 0.65 | Compute entropy (base 2) for a binary node with proportions (0.9, 0.1). `[verified: 0.469]` | H = −0.9 log₂(0.9) − 0.1 log₂(0.1) ≈ 0.1368 + 0.3322 ≈ 0.469 | — |
| A5 | apply | short-answer | 0.5 | A candidate split produces two children with Gini 0.1 and 0.15, versus another candidate producing children with Gini 0.4 and 0.35 (same weighting). Which split would a Gini-based criterion prefer? | the first candidate — lower weighted-average child impurity (0.1, 0.15) means a larger reduction from the parent's impurity, so it's the preferred split | — |
| A6 | apply | short-answer | 0.45 | A regression tree candidate split reduces weighted child variance from a parent variance of 10 down to a weighted average of 3. Is this considered a good split by the variance-reduction criterion? | yes — a large reduction (10 → 3) means the split substantially improves how tightly clustered the target values are within each child, which is exactly what variance reduction rewards | — |
| E2 | explain | short-answer | 1.0 | Why do entropy and Gini impurity, despite using different formulas, tend to produce similar splitting decisions in practice? | both are concave functions of the class proportions that are 0 at purity (p=0 or 1) and maximal at p=0.5, so they respond to changes in node composition in a qualitatively similar way — the specific numeric values differ but the ranking of candidate splits by "how much they reduce mixing" is usually close *(required)* | — |
| E3 | explain | short-answer | 1.05 | Why does information gain use a *weighted* average of child impurities, rather than a simple unweighted average? | a split sending most examples to a pure child and few examples to an impure one should be scored differently from an even split with the same two impurity values — weighting by child size ensures the criterion reflects how much of the actual dataset benefits from the improved purity *(required)* | — |
| E4 | explain | short-answer | 0.95 | Why can a greedy, one-step-ahead splitting criterion sometimes miss a globally better tree structure? | a split that looks locally unhelpful (low immediate information gain) might set up a much more informative subsequent split two levels down, but since the criterion only evaluates the immediate impurity reduction, it can never "see" that downstream payoff and will pass over that split in favor of a locally-better but globally-worse one *(required)* | — |
| E5 | explain | short-answer | 1.1 | Why is variance reduction the natural regression-tree analogue of Gini/entropy for classification? | Gini and entropy both measure "spread" or "mixedness" of a categorical outcome within a node; variance measures the analogous spread of a continuous outcome, so choosing splits that reduce variance parallels choosing splits that reduce categorical impurity — both pick the split that makes each resulting group as internally homogeneous as possible *(required)* | — |
| T2 | transfer | short-answer | 1.7 | How does entropy's role as a splitting criterion connect to Shannon entropy's role in information theory more broadly? | Shannon entropy measures the expected number of bits needed to encode outcomes from a distribution; using it as a splitting criterion is a direct application — a "pure" node (low entropy) needs almost no bits to describe which class an example belongs to, so choosing splits that minimize post-split entropy is equivalent to choosing splits that make the class label maximally predictable (compressible) given the split *(required: names the bits/predictability connection explicitly)* | — |
| T3 | transfer | short-answer | 1.6 | Why does the greedy, myopic nature of splitting criteria (discussed in E4) motivate ensemble methods like random forests, which inject randomness into split selection? | since greedy splitting can get "locked into" a locally-optimal but globally-suboptimal tree structure, randomizing which features are even considered at each split (as random forests do) lets different trees explore different locally-greedy paths through the same data — averaging over many such greedy-but-different trees compensates for any single tree's myopic mistakes, foreshadowing this cluster's ensemble concepts *(required: connects greediness's limitation to the ensembling motivation)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.0…1.7.*

---

## Pruning Trees (`pruning-trees`)
*Prereq: Decision Tree, Overfitting and Underfitting · ancestors 19 · b₀ = 1.00*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.0 | Describe pruning. | after growing a tree to full (or near-full) depth, remove branches that don't improve validation performance, to combat overfitting | — |
| R2 | recall | mcq | 0.3 | Pruning is best understood as: | a regularization technique trading some training accuracy for better generalization | describes it as "a way to make training faster" — a possible side effect, not the actual purpose → `pruning-trees` |
| A1 | apply | short-answer | 0.8 | Cost-complexity pruning penalizes tree size with α·\|T\|. What happens to tree size as α increases from 0 toward a large value? | at α=0, no penalty, so the full unpruned tree is "optimal"; as α grows, the penalty for extra leaves increases, favoring smaller trees; as α→∞, the tree shrinks toward a single leaf *(required: all three regimes)* | — |
| E1 | explain | short-answer | 1.5 | Why is the optimal α chosen using validation performance, not training performance? | training loss always prefers less pruning (more leaves lower training error monotonically); this is exactly `hyperparameters`' earlier point that training loss is a biased guide for complexity-controlling choices, requiring a separate validation signal *(required: the direct callback to that hyperparameters argument)* | — |
| T1 | transfer | short-answer | 2.0 | Why do pruning and ensembling (bagging, random forests) represent philosophically different fixes for an overfitting tree? | pruning makes one tree simpler, trading flexibility for reduced variance directly; ensembling instead combines many complex, unpruned trees and reduces variance by averaging out their individual noise, without simplifying any single tree at all *(required: names both mechanisms and that they don't overlap)* | — |

| R3 | recall | short-answer | −0.55 | What is "cost-complexity pruning" (also called weakest-link pruning)? | a pruning method that minimizes training error plus a penalty α·\|T\| on the number of leaves \|T\|, trading fit for tree size | — |
| R4 | recall | short-answer | −0.5 | Why is pruning applied after growing a full tree, rather than by simply stopping growth early? | growing fully first and then pruning back lets the algorithm see the full tree structure before deciding what to remove, avoiding the risk of stopping too early and missing a split that would have enabled a valuable later split | — |
| R5 | recall | mcq | −0.35 | "Pre-pruning" (early stopping during growth) differs from "post-pruning" in that: | pre-pruning halts splitting during construction based on a stopping rule, while post-pruning grows a full tree first and removes branches afterward | claims pre-pruning and post-pruning "always produce identical final trees" — they follow different procedures and can produce different results, especially when a locally-unhelpful split would have enabled a valuable one later → `pruning-trees` |
| R6 | recall | short-answer | −0.4 | Fill in the blank: cost-complexity pruning minimizes training error + α·___. | \|T\| (the number of leaves) |
| R7 | recall | mcq | −0.2 | At α=0 (no complexity penalty), cost-complexity pruning selects: | the full, unpruned tree | claims α=0 "always produces the smallest possible tree (a single leaf)" — a single leaf minimizes complexity but with no penalty at all, there's no incentive to shrink from the full, best-training-fit tree → `pruning-trees` |
| R8 | recall | short-answer | −0.3 | True or false: pruning can only remove entire subtrees at once, never a single individual split in isolation. | true — pruning collapses a subtree back into its parent node, removing every split below that point together, not one split independently | — |
| R9 | recall | mcq | −0.1 | The pruning hyperparameter α is typically chosen by: | evaluating performance on a validation set across a range of α values | claims it's chosen "by picking the value that gives zero training error" — that criterion would always select α=0 (no pruning), defeating pruning's purpose → `pruning-trees` |
| R10 | recall | short-answer | 0.0 | What structural effect does pruning have on the tree's overall depth and number of leaves? | it reduces both — removed subtrees mean fewer leaves and typically a shallower overall structure | — |
| R11 | recall | mcq | 0.1 | Pruning is a technique used with: | a single decision tree, to simplify it directly | claims it's "primarily used to combine multiple trees into an ensemble" — pruning operates on one tree's own structure, unlike ensembling which combines separate trees → `pruning-trees` |
| R12 | recall | short-answer | 0.2 | What is the tradeoff pruning navigates, expressed in one sentence? | fitting the training data closely (many leaves, low training error) versus keeping the tree simple enough to generalize well (fewer leaves, controlled complexity) | — |
| R13 | recall | mcq | 0.3 | If α is set extremely large, cost-complexity pruning will: | shrink the tree toward a single leaf (essentially no splits at all) | claims extreme α "has no effect on the tree, since it only affects training speed" — an extremely large complexity penalty dominates the objective and collapses the tree toward minimal size → `pruning-trees` |
| R14 | recall | short-answer | 0.4 | Fill in the blank: choosing the pruning parameter α via validation performance is a direct instance of controlling model ___. | complexity |
| R15 | recall | mcq | 0.15 | Pruning most directly targets which failure mode? | overfitting, from an overly complex, unconstrained tree | claims it targets "underfitting, from a tree that's too shallow" — pruning only ever removes branches, shrinking a tree, which addresses overfitting, not underfitting → `pruning-trees` |
| R16 | recall | short-answer | 0.25 | Does pruning change which splits were originally chosen during tree growth, or only whether those splits are kept? | only whether they're kept — pruning selects a subset of the already-grown tree's structure to retain, it doesn't re-choose or re-optimize any split | — |
| A2 | apply | short-answer | 0.85 | Two candidate pruned trees have training errors 0.05 and 0.08, with leaf counts 40 and 15 respectively. At α=0.001, which has the lower cost-complexity objective (training error + α·\|T\|)? Compute both. `[verified: 0.09 vs 0.095]` | tree A: 0.05 + 0.001(40) = 0.05+0.04 = 0.09; tree B: 0.08 + 0.001(15) = 0.08+0.015 = 0.095; tree A has the lower objective at this α despite more leaves | — |
| A3 | apply | short-answer | 0.75 | A grown tree has one subtree that, if removed, barely changes validation error at all. Should it be pruned? | yes — since removing it costs almost nothing in validation performance but reduces complexity (and overfitting risk), the cost-complexity tradeoff favors pruning it away | — |
| A4 | apply | short-answer | 0.7 | Comparing α=0.0001 (small) and α=1.0 (large) on the same grown tree, which produces the smaller final pruned tree? | α=1.0 — a larger penalty per leaf more aggressively favors removing leaves, shrinking the tree further | — |
| A5 | apply | short-answer | 0.9 | A tree with 50 leaves has training error 0.02, but validation error 0.30. What does pruning aim to fix here? | the large train/validation gap signals overfitting; pruning back some of those 50 leaves should raise training error somewhat while lowering validation error, closing the gap toward a model that generalizes better | — |
| A6 | apply | short-answer | 0.8 | If cross-validation shows validation error is nearly flat across a wide range of α values, what does this suggest about the tree's original overfitting severity? | it suggests the fully-grown tree wasn't overfitting all that severely to begin with — if pruning barely changes held-out performance across a wide α range, the unpruned tree's extra complexity wasn't costing much generalization performance | — |
| E2 | explain | short-answer | 1.3 | Why does cost-complexity pruning's α·\|T\| penalty need to be calibrated against validation, not training, error to be meaningful at all? | training error can only stay the same or worsen as leaves are removed (a bigger tree can never fit training data worse than a smaller one derived from it), so choosing α by training performance alone would always favor α=0 (no pruning) — validation error is the only signal that can reveal when additional leaves stop helping generalization *(required)* | — |
| E3 | explain | short-answer | 1.35 | Why is pruning back from a fully-grown tree generally preferred over stopping growth early at a fixed depth? | a split that looks locally unhelpful during growth (low immediate information gain) can still enable a genuinely valuable split one level further down; stopping early would never discover that split at all, while growing fully and pruning afterward at least has the option to keep it if it turns out to matter *(required)* | — |
| E4 | explain | short-answer | 1.4 | Why does pruning's effect on bias and variance mirror the general bias-variance tradeoff, and in which direction does it move each? | pruning removes leaves that were fitting fine-grained (often noisy) patterns, which reduces variance since the simpler tree is less sensitive to the specific training sample; it can raise bias somewhat, since some genuine signal captured by those removed leaves is lost — the same tradeoff `bias-variance-tradeoff` describes, applied concretely via α *(required: names both directions explicitly)* | — |
| E5 | explain | short-answer | 1.25 | Why is pruning generally computationally cheap compared to re-growing an alternative smaller tree from scratch? | pruning operates on an already-built tree structure, evaluating which subtrees to collapse using precomputed statistics from the original growth phase, rather than repeating the expensive greedy search over all possible split candidates that growing a tree from scratch requires *(required)* | — |
| T2 | transfer | short-answer | 1.85 | How does cost-complexity pruning's α·\|T\| penalty structurally parallel AIC/BIC's complexity penalty in regression model selection? | both add an explicit term proportional to model complexity (number of leaves here, number of parameters there) to a goodness-of-fit measure, deliberately making a more complex model "pay a price" it must overcome with genuinely better fit to be preferred — the exact same complexity-penalized-fit philosophy, applied to two structurally different model families *(required: names the shared penalized-fit structure)* | — |
| T3 | transfer | short-answer | 1.95 | Selecting α via a validation curve produces a graph much like the classic bias-variance U-curve against model complexity. How does that curve's two failure regions map onto pruning specifically? | at α near 0 (large, unpruned tree), validation error is high from overfitting (high variance); at very large α (tree collapsed toward one leaf), validation error is high from underfitting (high bias); the minimum in between is the pruning "sweet spot" — the identical shape `overfitting-underfitting`'s T3 describes for complexity curves generally, here traced out explicitly by sweeping α *(required: both regions correctly mapped to α's extremes)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.55…1.95.*

---

## Ensemble Methods (`ensemble-methods`)
*Prereq: Decision Tree · ancestors 5 · b₀ = 0.40*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.6 | Define ensemble methods. | combining predictions from multiple models to get a better overall prediction than any single model alone | — |
| R2 | recall | mcq | −0.35 | The statistical principle behind why ensembling reduces variance is: | averaging multiple noisy estimates reduces overall variance, per `sample-mean`'s 1/√n scaling | claims "more models always means more overfitting" — the opposite of ensembling's actual variance-reduction effect → `ensemble-methods` |
| A1 | apply | numeric | 0.2 | Averaging 10 independent models each with prediction variance σ², what's the averaged prediction's variance? `[verified: σ²/10]` | σ²/10 — a direct instance of Var(X̄)=σ²/n from `sample-mean` | — |
| E1 | explain | short-answer | 0.9 | Why does ensembling work best when individual models are as independent/diverse as possible? | if all models make the exact same errors (perfectly correlated), averaging does nothing to reduce variance — exactly as averaging perfectly correlated random variables gives no variance reduction, only independent (or weakly correlated) ones benefit *(required: the correlated-variables parallel)* | — |
| T1 | transfer | short-answer | 1.4 | Contrast bagging and boosting's philosophies for achieving ensemble diversity. | bagging trains many models on randomized resamples of the same data, hoping randomness alone produces diversity; boosting trains models sequentially, each deliberately targeting the previous model's specific mistakes — diversity through design, not chance *(required: both mechanisms named, foreshadowing `bagging` and `adaboost`)* | — |

| R3 | recall | short-answer | −1.0 | Name two families of ensemble methods distinguished by how they train their component models. | parallel/independent ensembling (e.g. bagging) and sequential ensembling (e.g. boosting) | — |
| R4 | recall | short-answer | −0.95 | What is the simplest possible ensemble combination rule for classification? | majority voting across the individual models' predictions | — |
| R5 | recall | mcq | −0.75 | For regression, the standard way to combine ensemble members' predictions is: | averaging their individual numeric predictions | claims it's "taking the single member with the lowest training error" — ensembling combines predictions from all members, it doesn't just select the historically-best one → `ensemble-methods` |
| R6 | recall | short-answer | −0.85 | Fill in the blank: ensembling reduces prediction variance primarily by ___ over multiple models' predictions. | averaging |
| R7 | recall | mcq | −0.55 | A "weak learner" in ensemble terminology refers to: | a model that performs only slightly better than random guessing on its own | claims it means "a model trained on too little data" — weak learner describes limited *predictive power by design* (e.g. a decision stump), not a data-quantity issue → `ensemble-methods` |
| R8 | recall | short-answer | −0.65 | True or false: ensembling always improves performance over the single best individual model in the ensemble. | false — ensembling helps most when members are diverse and roughly comparable in quality; it can underperform a single excellent model, especially if members are highly correlated or of poor quality | — |
| R9 | recall | mcq | −0.45 | The primary statistical justification for why averaging reduces variance traces back to: | `sample-mean`'s Var(X̄) = σ²/n scaling for averages of (ideally independent) random variables | claims it traces back to "the central limit theorem's shape-of-the-distribution result" — the variance-reduction fact used here is the σ²/n scaling, a distinct (though related) result from the CLT's convergence-to-Normal claim → `ensemble-methods` |
| R10 | recall | short-answer | −0.35 | What two named ensemble strategies does this cluster cover in depth, corresponding to the "parallel" and "sequential" families? | bagging (parallel) and boosting, specifically AdaBoost and gradient boosting (sequential) | — |
| R11 | recall | mcq | −0.25 | Ensembling is often described as trading off: | some interpretability (harder to trace one clean explanation) for improved predictive performance | claims it trades off "training data quantity for prediction speed" — ensembling doesn't reduce the data needed nor is speed the primary tradeoff being described → `ensemble-methods` |
| R12 | recall | short-answer | −0.15 | What must be true of ensemble members' errors, ideally, for averaging to meaningfully reduce variance? | the errors should be as uncorrelated (independent) as possible across members — perfectly correlated errors don't cancel out under averaging | — |
| R13 | recall | mcq | −0.05 | "Stacking" as an ensembling approach differs from simple majority voting/averaging by: | training a second-level model to learn how to best combine the base models' predictions | claims stacking "always simply averages base model outputs, same as bagging" — stacking's distinguishing feature is a learned combination step, not a fixed averaging rule → `ensemble-methods` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: an ensemble of models that all make the exact same errors provides ___ variance-reduction benefit from averaging. | no (zero) |
| R15 | recall | mcq | 0.0 | Ensembles are typically built from: | multiple instances of the same base model type (e.g. many trees) or a mix of different model types | claims they can "only ever combine copies of the exact same trained model" — a single trained model copied identically would make identical predictions, providing no ensembling benefit at all; ensembles need some form of diversity → `ensemble-methods` |
| R16 | recall | short-answer | 0.1 | Does ensembling always require training multiple entirely separate models from scratch? | typically yes for the classic methods here (bagging, boosting), though variations exist (e.g. training one model and combining checkpoints); the classic mechanism is genuinely separate base models | — |
| A2 | apply | numeric | 0.3 | Averaging 25 independent models each with prediction variance σ²=10, what's the averaged prediction's variance? `[verified: 0.4]` | σ²/n = 10/25 = 0.4 | — |
| A3 | apply | short-answer | 0.35 | Three weak classifiers each get 55% accuracy individually, and their errors are largely uncorrelated. Would majority voting across all three likely beat any one of them alone? | yes, plausibly — with largely independent errors slightly better than chance, majority voting tends to boost accuracy above any single member, since the cases where all three agree-and-are-wrong become rarer than any single member being wrong | — |
| A4 | apply | short-answer | 0.4 | Two models are trained on the exact same data with the exact same algorithm and hyperparameters (fully deterministic). Would ensembling these two provide any benefit? | no — with identical training producing identical models, their predictions are perfectly correlated (identical), so averaging them gives exactly the same prediction as either alone, with no variance reduction | — |
| A5 | apply | short-answer | 0.25 | A weather forecaster combines predictions from five different meteorological models via a learned weighting scheme. Is this stacking, bagging, or boosting? | stacking — a second-level combination (the learned weighting) is used to blend the base models' outputs, rather than simple resampling (bagging) or sequential error-correction (boosting) | — |
| A6 | apply | short-answer | 0.45 | If one ensemble member is dramatically worse than the rest (near-random accuracy), does simple unweighted averaging risk hurting overall performance? | yes — unweighted averaging gives the poor member equal say, which can drag down the aggregate prediction compared to excluding it or down-weighting it (motivating weighted-combination approaches like boosting or stacking) | — |
| E2 | explain | short-answer | 0.9 | Why is ensembling especially effective for high-variance, low-bias base models (e.g. deep decision trees)? | ensembling's core mechanism (averaging) specifically targets variance reduction, so it can only meaningfully help where variance is the dominant source of error; a base model already suffering mainly from high bias gets little benefit since averaging doesn't correct a systematic error shared across all members *(required)* | — |
| E3 | explain | short-answer | 0.95 | Why does correlated error among ensemble members undermine the variance-reduction argument from `sample-mean`? | Var(X̄) = σ²/n specifically assumes independence between the averaged quantities; with correlation ρ, the actual variance is closer to σ²(1/n + (1−1/n)ρ), which stays well above σ²/n as ρ grows — highly correlated members' errors simply don't cancel the way independent ones do *(required: names the correlation term inflating variance)* | — |
| E4 | explain | short-answer | 1.0 | Why does an ensemble of "weak learners" (individually barely better than random) still let boosting achieve strong aggregate performance? | boosting sequentially and deliberately targets each predecessor's specific mistakes, so even members individually only slightly better than chance can, when combined and weighted appropriately, collectively cover the full pattern space the way no single weak member could alone *(required)* | — |
| E5 | explain | short-answer | 0.85 | Why does ensembling typically cost more at prediction time than using a single model? | every ensemble member must be run on the new input and their outputs combined, so prediction time scales roughly with the number of members, unlike a single model whose prediction cost is fixed regardless of any ensembling decision *(required)* | — |
| T2 | transfer | short-answer | 1.45 | How does the "wisdom of crowds" phenomenon in social science parallel ensembling's variance-reduction argument? | both rely on aggregating many individually-imperfect, largely independent estimates to cancel out idiosyncratic errors while preserving shared signal — a crowd's average guess of a jar's bean count works for the same statistical reason ensembling works: independent errors partially cancel under averaging, while any systematic bias shared by everyone (or every model) does not *(required: names the shared independent-error-cancellation mechanism)* | — |
| T3 | transfer | short-answer | 1.35 | Why does this concept's contrast between bagging and boosting (T1) predict that bagging parallelizes trivially across machines while boosting does not? | bagging trains each member entirely independently on its own bootstrap resample, so nothing about training member 5 depends on member 3's result — perfectly parallelizable; boosting trains sequentially, with each new member's target explicitly depending on the current ensemble's errors so far, creating a hard dependency chain that blocks naive parallelization across members *(required: ties the parallel/sequential distinction to a concrete engineering consequence)* | — |

*Coverage: 16/6/5/3 — 30 items, −1.0…1.45.*

---

## Bagging (`bagging`)
*Prereq: Ensemble Methods · ancestors 6 · b₀ = 0.47*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.53 | Describe bagging. | train many models, each on a different bootstrap resample of the training data (sampling with replacement), then average or majority-vote their predictions | — |
| R2 | recall | mcq | −0.28 | Bagging primarily reduces: | variance | claims it primarily reduces "bias" — bagging's averaging mechanism targets variance, per `ensemble-methods`' R2 → `bagging` |
| A1 | apply | short-answer | 0.27 | How does bagging apply `bootstrapping`'s exact resampling procedure to create each model's training set? | each model's training set is one bootstrap resample — drawn with replacement, the same size n as the original — of the full training data, exactly as `bootstrapping` defined it *(required: the direct reuse, not a fresh description)* | — |
| E1 | explain | short-answer | 0.97 | Why is bagging especially effective for high-variance, low-bias models (deep trees) but of little benefit for high-bias, low-variance models (linear regression)? | bagging's entire mechanism is variance reduction (R2); it can only help where variance is the dominant problem — a model already suffering mainly from bias gets no benefit from averaging away noise it doesn't have much of *(required)* | — |
| T1 | transfer | short-answer | 1.47 | Explain out-of-bag (OOB) error estimation, and why it's a free alternative to a separate validation set. `[verified: ~37% of points excluded from a given bootstrap sample]` | each bootstrap resample leaves out roughly 1/3 of the original data on average (per (1−1/n)ⁿ→e⁻¹); each point can therefore validate the models that didn't include it in training, giving a built-in performance estimate with no separate held-out set needed *(required: the ~1/3 figure and its source)* | — |

| R3 | recall | short-answer | −0.95 | What does "bootstrap" mean in the context of bagging's training-set construction? | sampling with replacement from the original training set, to the same size n, so some points may appear multiple times and others not at all in a given resample | — |
| R4 | recall | short-answer | −0.9 | What does the "agg" in "bagging" stand for? | aggregating (bootstrap AGGregating) — combining the predictions of the many bootstrap-trained models | — |
| R5 | recall | mcq | −0.7 | The individual base models used in bagging are typically: | high-variance models, most commonly deep, largely unpruned decision trees | claims they're "typically high-bias, low-variance models like linear regression" — bagging's variance-reduction mechanism is most useful for models that already have high variance to reduce → `bagging` |
| R6 | recall | short-answer | −0.8 | Fill in the blank: each bagging model is trained on an independently drawn ___ of the training data. | bootstrap resample |
| R7 | recall | mcq | −0.5 | For bagged regression trees, predictions are combined by: | averaging the individual trees' predictions | claims they're combined "by majority vote" — majority vote applies to bagged classification trees; regression predictions are averaged → `bagging` |
| R8 | recall | short-answer | −0.6 | True or false: bagging's bootstrap resamples are drawn with replacement, so a single resample can be the same size as the original dataset. | true — sampling with replacement to size n is the standard bootstrap procedure | — |
| R9 | recall | mcq | −0.4 | The number of bagged models (trees) is a hyperparameter that, when increased, typically: | improves or maintains performance, with diminishing returns, rather than causing overfitting on its own | claims that "more bagged trees directly cause more overfitting, just like a deeper single tree" — bagging's averaging mechanism means more trees generally don't add overfitting risk the way more depth in a single tree does → `bagging` |
| R10 | recall | short-answer | −0.3 | What fraction of the original data is expected to be excluded from any single bootstrap resample, as n grows large? | approximately 1/e ≈ 36.8% | — |
| R11 | recall | mcq | −0.2 | "Out-of-bag" (OOB) points, for a given bagged model, refer to: | the roughly 1/3 of training points that weren't included in that model's particular bootstrap resample | claims OOB points are "the points the model got wrong during training" — OOB is defined by whether a point was sampled into the resample at all, not by training accuracy → `bagging` |
| R12 | recall | short-answer | −0.1 | What can each OOB point be used for, that a validation set would otherwise be needed for? | evaluating the performance of the models that didn't include it in their training, giving a built-in estimate of generalization error at no extra data cost | — |
| R13 | recall | mcq | 0.0 | Bagging reduces overfitting risk primarily because it: | averages over many high-variance models fit to different resamples, reducing the sensitivity of the aggregate prediction to any one training sample's noise | claims it works "because each bootstrap resample contains less data than the original, regularizing each tree individually" — resamples are the same size n as the original; bagging's benefit comes from averaging, not from training on less data → `bagging` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: bagging primarily addresses the ___ component of a model's error, not the bias component. | variance |
| R15 | recall | mcq | −0.05 | Bagging's bootstrap resampling introduces randomness by: | randomly sampling training examples (with replacement) for each model | claims it "randomly samples which features to consider at each split" — that describes random forests' extra ingredient, not bagging's own resampling mechanism → `bagging` |
| R16 | recall | short-answer | 0.1 | Is bagging applicable to base models other than decision trees? | yes — bagging's bootstrap-and-average procedure is a general technique applicable to any base learner, though it's most commonly paired with high-variance models like deep trees where it has the most to offer | — |
| A2 | apply | numeric | 0.4 | For n=100, compute the expected fraction of points excluded from a single bootstrap resample, using (1−1/n)ⁿ. `[verified: 0.366]` | (1−1/100)¹⁰⁰ ≈ 0.366, so about 36.6% of points are excluded on average | — |
| A3 | apply | short-answer | 0.35 | If a bagged ensemble has 500 trees, roughly what fraction of trees, on average, exclude a given training point from their bootstrap sample? | roughly 1/e ≈ 36.8% of the 500 trees — regardless of ensemble size, each individual tree's resample excludes about that fraction of points on average | — |
| A4 | apply | short-answer | 0.45 | A dataset has heavy class imbalance (95% negative, 5% positive). Does bootstrap resampling change this imbalance ratio within a given resample, on average? | not substantially — since resampling is uniform with replacement, the expected class proportions in a bootstrap resample match the original dataset's proportions, though individual resamples will show some random variation around that expectation | — |
| A5 | apply | short-answer | 0.3 | If 20% of trees in a bagged ensemble happen to exclude a specific training point x from their resample, can OOB error be computed for x using those trees? | yes — the models that never saw x during training (the ~20% that excluded it here) can validly predict on x, giving an honest out-of-sample check for that point using only those models | — |
| A6 | apply | short-answer | 0.5 | Two bagged ensembles use the same 500 trees but Ensemble A averages predictions while Ensemble B takes the single tree with lowest OOB error. Which better reflects bagging's core mechanism? | Ensemble A — averaging across all members is bagging's defining aggregation step; picking a single "best" tree abandons the variance-reduction benefit that comes specifically from combining many members | — |
| E2 | explain | short-answer | 1.05 | Why does bagging's OOB error estimate serve as a genuinely unbiased proxy for test-set performance, without needing a separate held-out set? | each OOB prediction for a point is made only by models that never saw that point during training, exactly matching the condition a true held-out test set is meant to create — the "held-out-ness" comes for free from the randomness already built into the bootstrap procedure *(required)* | — |
| E3 | explain | short-answer | 1.1 | Why doesn't bagging typically overfit as more trees are added to the ensemble, unlike growing a single tree deeper? | each additional bagged tree is trained on its own independent bootstrap resample and simply averaged in; adding more independent samples to an average can only reduce variance further (per σ²/n scaling), it doesn't add new capacity to memorize any particular training point the way growing one tree deeper does *(required)* | — |
| E4 | explain | short-answer | 1.15 | Why is bagging particularly well-suited to deep, unpruned decision trees specifically, more than to a model like linear regression? | deep trees are exactly the high-variance, low-bias regime where averaging has the most to offer (per E1's argument); linear regression already has low variance on its own, so there's comparatively little variance for bagging's averaging mechanism to remove *(required)* | — |
| E5 | explain | short-answer | 0.95 | Why does the ~1/e out-of-bag exclusion fraction not depend much on the size of the dataset n, once n is reasonably large? | the exclusion probability (1−1/n)ⁿ converges to e⁻¹ ≈ 0.368 as n grows, because it's the classic limit definition of e; this limit is reached quite quickly, so datasets of even moderate size already see this fraction stabilize near 36.8% *(required)* | — |
| T2 | transfer | short-answer | 1.55 | How does bagging's OOB estimate parallel k-fold cross-validation's role in estimating generalization error, and how do they differ? | both estimate held-out performance by ensuring each prediction comes only from models that didn't train on that specific point; but k-fold CV requires deliberately partitioning and retraining k separate times specifically for that purpose, while OOB estimation gets the same guarantee "for free" as a side effect of bagging's already-random resampling — no extra retraining required *(required: names both the shared held-out logic and the free-vs-deliberate distinction)* | — |
| T3 | transfer | short-answer | 1.5 | How does bagging's reliance on bootstrap resampling connect this concept to `bootstrapping`'s broader statistical role in estimating a sampling distribution? | `bootstrapping` uses resampling to approximate the sampling distribution of a statistic (e.g. a confidence interval for a mean) without further distributional assumptions; bagging reuses the exact same resampling mechanism, but instead of estimating a statistic's *uncertainty*, it uses the resamples to train an *ensemble of models* whose averaged predictions have lower variance — one resampling idea, applied to two different downstream goals *(required: distinguishes the shared mechanism from the different goals)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.95…1.55.*

---

## Random Forests (`random-forests`)
*Prereq: Bagging, Splitting Criteria · ancestors 8 · b₀ = 0.60*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.4 | Describe random forests' extra ingredient beyond bagging. | at each split, only a random subset of features is considered, not all of them — an additional source of randomness that further decorrelates the trees | — |
| R2 | recall | mcq | −0.1 | The feature-subsampling step exists specifically to: | further reduce correlation between the trees | claims it's "mainly to speed up training" — a side effect, not the statistical motivation → `random-forests` |
| A1 | apply | short-answer | 0.4 | Without feature subsampling, why might many bagged trees choose the same dominant feature at the top, even on different bootstrap samples? | the strongest feature typically produces the best split by the impurity criterion regardless of which bootstrap sample is used, so plain bagging can still leave trees more correlated than desired — exactly the correlation problem `ensemble-methods`' E1 warned reduces variance-reduction benefit *(required: the direct callback)* | — |
| E1 | explain | short-answer | 1.1 | Connect feature subsampling directly to `ensemble-methods`' diversity argument. | forcing different random feature subsets at each split makes the resulting trees less correlated with each other than plain bagging alone would produce, giving more effective variance reduction when averaged — a direct application of "diversity maximizes averaging benefit" *(required)* | — |
| T1 | transfer | short-answer | 1.6 | Why are random forests often called a "nearly free lunch," needing little hyperparameter tuning? | averaging many diverse, low-bias trees is inherently robust to exact choices like the number of trees or features sampled per split — more trees essentially never hurts performance, only costs more compute, unlike hyperparameters in methods where a wrong setting can actively harm results *(required: names the "more never hurts" robustness specifically)* | — |

| R3 | recall | short-answer | −0.8 | What two sources of randomness does a random forest combine, beyond a single decision tree? | bootstrap resampling of the training data (as in bagging) plus random feature subsampling at each split | — |
| R4 | recall | short-answer | −0.75 | What common rule of thumb is used to choose how many features to consider at each split for classification? | approximately √p, where p is the total number of features | — |
| R5 | recall | mcq | −0.55 | For random-forest regression, a common rule of thumb for the number of features considered per split is: | approximately p/3 | claims it's "always exactly the same √p rule used for classification" — regression commonly uses a different default (p/3) than classification's √p → `random-forests` |
| R6 | recall | short-answer | −0.65 | Fill in the blank: random forests apply feature subsampling at every ___, not once per tree. | split (node) |
| R7 | recall | mcq | −0.45 | If the number of features considered per split equals the total number of features p, a random forest reduces to: | plain bagging | claims it "reduces to a single unbagged decision tree" — with bootstrap resampling still active, it remains bagging, just without the extra feature-subsampling ingredient → `random-forests` |
| R8 | recall | short-answer | −0.5 | True or false: a random forest's feature subsample is chosen freshly and independently at every single split, not fixed once per tree. | true — the random subset of candidate features is redrawn at each split point throughout the tree | — |
| R9 | recall | mcq | −0.3 | Random forests, compared to a single bagged deep tree, typically achieve: | lower overall ensemble variance, due to less-correlated trees | claims they achieve "lower bias only, with no change to variance" — the entire motivation for feature subsampling is decorrelating trees to further reduce variance, not bias → `random-forests` |
| R10 | recall | short-answer | −0.2 | What hyperparameter, along with the number of trees, is most distinctively associated with random forests (beyond what plain bagging has)? | the number of features sampled per split (often called `max_features` or `mtry`) | — |
| R11 | recall | mcq | −0.1 | A random forest's feature-importance scores are typically derived from: | how much each feature's splits reduce impurity (or increase OOB error when permuted), aggregated across all trees | claims they're derived "by counting how many times a human manually flagged that feature as important" — feature importance is computed automatically from the trained ensemble's structure, no manual flagging involved → `random-forests` |
| R12 | recall | short-answer | 0.0 | Does a random forest inherit bagging's out-of-bag (OOB) error estimation capability? | yes — since random forests still use bootstrap resampling for each tree, the same OOB mechanism applies unchanged | — |
| R13 | recall | mcq | −0.05 | Setting the per-split feature subsample size very small (e.g. 1 feature out of 100) tends to: | increase decorrelation between trees further, but can weaken each individual tree | claims it "has no effect on tree correlation, only on training speed" — the size of the feature subsample directly controls how correlated trees end up being, which is the entire mechanism this hyperparameter exists to tune → `random-forests` |
| R14 | recall | short-answer | 0.1 | Fill in the blank: random forests are bagging plus random ___ subsampling. | feature |
| R15 | recall | mcq | 0.05 | Random forests are generally considered to require less hyperparameter tuning than gradient boosting mainly because: | adding more trees rarely hurts performance and default feature-subsampling rules work reasonably well out of the box | claims it's "because random forests have no tunable hyperparameters at all" — they do have tunable hyperparameters (number of trees, features per split, tree depth); they're just comparatively forgiving of imperfect tuning → `random-forests` |
| R16 | recall | short-answer | 0.15 | Is feature subsampling applied at training time, prediction time, or both? | training time only — each split during tree construction considers a random feature subset; prediction simply follows the already-built tree's fixed splits | — |
| A2 | apply | short-answer | 0.5 | With p=25 features, using the √p rule, roughly how many features would be considered at each split? `[verified: 5]` | √25 = 5 features per split | — |
| A3 | apply | short-answer | 0.45 | A dataset has one extremely dominant feature and 19 weak ones. Would plain bagging or a random forest be more likely to produce highly correlated trees? | plain bagging — since every tree considers all 20 features at every split, the dominant feature is very likely to be chosen at the top of nearly every tree regardless of the bootstrap sample, correlating the trees; random forest's feature subsampling sometimes excludes the dominant feature from consideration, forcing more variety | — |
| A4 | apply | short-answer | 0.55 | If max_features is set to exactly 1 (only one randomly chosen feature considered per split), what tradeoff does this push toward? | maximal decorrelation between trees, but each individual tree becomes weaker (since it can't necessarily use the genuinely best available feature at each split) — an extreme point on the decorrelation-vs-individual-strength tradeoff | — |
| A5 | apply | short-answer | 0.4 | A random forest with 500 trees achieves 91% OOB accuracy. Does computing this require a separate validation set? | no — OOB accuracy is computed from each tree's held-out (not-in-its-bootstrap-sample) points, inherited from the underlying bagging mechanism, with no separate validation split needed | — |
| A6 | apply | short-answer | 0.6 | Comparing a random forest's feature-importance ranking to a single decision tree's, which is typically considered more reliable, and why? | the random forest's — averaging importance scores over many decorrelated trees, each considering different feature subsets, produces a more stable ranking than relying on the splits chosen by one single, potentially idiosyncratic tree | — |
| E2 | explain | short-answer | 1.15 | Why can restricting the features considered at each split actually improve the ensemble's aggregate performance, even though each individual tree is weakened? | the ensemble's final performance depends on the *combination* of many decorrelated, individually-slightly-weaker trees more than on any single tree's individual strength — per the variance-reduction argument, lower correlation between members can outweigh the modest cost to each member's individual quality *(required)* | — |
| E3 | explain | short-answer | 1.2 | Why does feature subsampling specifically target the "identical dominant feature" correlation problem, more directly than bootstrap resampling alone does? | bootstrap resampling only randomizes which rows a tree sees, not which features are available at each split, so if one feature is genuinely the best predictor, nearly every bootstrap sample will still let the tree choose it at the top; feature subsampling directly removes that feature from consideration at some splits, forcing genuinely different structural choices *(required)* | — |
| E4 | explain | short-answer | 1.25 | Why does the choice of how many features to sample per split (max_features) represent its own bias-variance tradeoff within random forests? | a very small subsample size decorrelates trees strongly (lower ensemble variance) but weakens each individual tree's fit (higher individual bias); a large subsample size (approaching p) keeps trees individually strong but more correlated, closer to plain bagging — the same underlying tradeoff, now expressed through this one hyperparameter *(required: explicit bias-variance framing of the hyperparameter)* | — |
| E5 | explain | short-answer | 1.05 | Why is random forests' OOB error estimate not affected by adding feature subsampling on top of bagging? | OOB status is determined entirely by whether a data point was included in a given tree's bootstrap resample, which is unaffected by feature subsampling (a separate, orthogonal source of randomness applied at split time, not at resampling time) *(required)* | — |
| T2 | transfer | short-answer | 1.65 | How does random forests' deliberate injection of extra randomness (feature subsampling) parallel dropout in neural network training, from a later cluster? | both techniques deliberately introduce randomness during training specifically to prevent components (trees, or neurons) from relying too heavily on the same dominant signal, forcing a more distributed, decorrelated representation; dropout randomly disables neurons per training pass the way feature subsampling randomly disables features per split — both trade a bit of individual-component strength for a more robust aggregate *(required: names the shared decorrelation-via-randomness mechanism)* | — |
| T3 | transfer | short-answer | 1.7 | Why does random forests' robustness to hyperparameter choices (T1) make it a common first model to try on a new tabular dataset, before more carefully-tuned methods like XGBoost? | since more trees essentially never hurts and default feature-subsampling rules work reasonably well without careful tuning, a random forest can produce a strong, reliable baseline with minimal setup effort — establishing a performance floor other, more sensitive methods (which need careful hyperparameter search to reach their potential) must be compared against *(required: names the low-effort-baseline role explicitly)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.8…1.7.*

---

## AdaBoost (`adaboost`)
*Prereq: Ensemble Methods · ancestors 6 · b₀ = 0.47*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.53 | Describe AdaBoost's core mechanism. | train models sequentially; after each, increase the weight of examples the previous model got wrong; combine all models via a weighted vote, with better-performing models getting more say | — |
| R2 | recall | mcq | −0.28 | AdaBoost's approach to diversity differs from bagging's because it: | sequentially and deliberately targets the previous models' mistakes, rather than relying on randomness | claims it "uses random resampling, just like bagging" — missing the entire sequential-targeting mechanism → `adaboost` |
| A1 | apply | short-answer | 0.27 | If an example is correctly classified by the current ensemble, does its weight increase or decrease for the next round? | decrease (relatively) — AdaBoost upweights the *missed* examples specifically, so correctly-classified ones receive relatively less emphasis going forward | — |
| E1 | explain | short-answer | 0.97 | Why does AdaBoost reduce bias, unlike bagging's variance-focused mechanism? | each successive model is explicitly forced to focus on and fix the ensemble's current mistakes, progressively covering more of the true pattern earlier (weaker) models missed — a fundamentally different, sequential-correction mechanism rather than parallel averaging *(required)* | — |
| T1 | transfer | short-answer | 1.47 | Why is AdaBoost more sensitive to noisy/mislabeled data than bagging or random forests? | a genuinely mislabeled point gets "wrong" repeatedly, causing its weight to grow larger and larger across rounds, which can drive later models to overfit specifically to that single bad point in an attempt to satisfy its now-huge weight *(required: the runaway-weight mechanism)* | — |

| R3 | recall | short-answer | −0.95 | What is the standard base learner (weak classifier) used in classic AdaBoost? | a decision stump — a decision tree of depth 1 (a single split) | — |
| R4 | recall | short-answer | −0.9 | What does each model's "weight" (or "say") in the final weighted vote depend on? | how accurate that individual model was on its (weighted) training round — more accurate models get a larger say in the final combined vote | — |
| R5 | recall | mcq | −0.7 | The formula for a model's voting weight α in AdaBoost typically involves: | the model's weighted training error rate ε, via α = ½ ln((1−ε)/ε) | claims it "is always set equal for every model in the ensemble" — AdaBoost specifically computes a different, error-dependent weight for each model, that's the entire point of weighting the vote → `adaboost` |
| R6 | recall | short-answer | −0.8 | Fill in the blank: AdaBoost's per-example weights are updated after each round so that ___ examples receive more emphasis next round. | misclassified |
| R7 | recall | mcq | −0.5 | AdaBoost's overall prediction for a new example is: | a weighted vote among all trained models, weighted by each model's own accuracy-based weight α | claims it's "a simple unweighted majority vote across all models" — that ignores each model's individually computed α weight, a core part of AdaBoost's combination rule → `adaboost` |
| R8 | recall | short-answer | −0.6 | True or false: a model with weighted training error of exactly 0.5 (no better than chance) receives an α weight of 0 in the classic AdaBoost formula. | true — α = ½ ln((1−0.5)/0.5) = ½ ln(1) = 0, giving that model no say at all in the final vote | — |
| R9 | recall | mcq | −0.4 | If a base model's weighted error exceeds 0.5 (worse than random guessing), the AdaBoost weight formula gives it: | a negative α, effectively inverting its vote | claims it "is simply clipped to zero and the model is discarded silently" — the standard formula produces a negative α mathematically rather than discarding the model outright → `adaboost` |
| R10 | recall | short-answer | −0.3 | What happens to a correctly-classified example's relative weight after a round, compared to before? | it decreases relative to the misclassified examples' weight, since only the misclassified ones get boosted upward | — |
| R11 | recall | mcq | −0.2 | AdaBoost stands for: | Adaptive Boosting | claims it stands for "Averaged Decision Boosting" — the actual name reflects that the algorithm adaptively reweights examples each round, not an averaging operation → `adaboost` |
| R12 | recall | short-answer | −0.1 | Is the number of rounds (models trained) in AdaBoost a hyperparameter that must be chosen? | yes — too few rounds may underfit, too many can eventually overfit (especially to noisy data), so it's typically tuned via validation performance | — |
| R13 | recall | mcq | 0.0 | AdaBoost is best classified as which kind of ensembling strategy? | sequential (boosting) | claims it's "parallel (bagging-style)" — AdaBoost's entire defining mechanism is training models one after another, each depending on the previous round's errors → `adaboost` |
| R14 | recall | short-answer | 0.05 | Fill in the blank: each new AdaBoost model is trained to focus on the examples the current ensemble finds ___. | hardest (most often misclassified) |
| R15 | recall | mcq | −0.05 | AdaBoost's example-reweighting scheme means later base models in the sequence: | are trained on data that's been reweighted to emphasize the previous models' mistakes | claims later models "train on exactly the same weighting as the very first model" — the entire adaptive-weighting mechanism updates weights round by round, so later rounds see different weightings → `adaboost` |
| R16 | recall | short-answer | 0.1 | Does AdaBoost require its base learners to individually be strong (highly accurate) classifiers? | no — the classic formulation works even with "weak" learners only slightly better than random chance, as long as each is consistently a bit better than 50% weighted accuracy | — |
| A2 | apply | numeric | 0.4 | A weak model has weighted training error ε=0.3. Compute its voting weight α = ½ ln((1−ε)/ε). `[verified: 0.424]` | α = ½ ln(0.7/0.3) = ½ ln(2.333) ≈ ½(0.847) ≈ 0.424 | — |
| A3 | apply | numeric | 0.45 | A weak model has weighted training error ε=0.1. Compute its voting weight α. `[verified: 1.099]` | α = ½ ln(0.9/0.1) = ½ ln(9) ≈ ½(2.197) ≈ 1.099 | — |
| A4 | apply | short-answer | 0.35 | Comparing the models from A2 (ε=0.3) and A3 (ε=0.1), which gets more say in the final weighted vote? | the model from A3 (ε=0.1, α≈1.099) — a lower error rate produces a larger α, giving that model more weight in the combined prediction | — |
| A5 | apply | short-answer | 0.5 | A single training example is misclassified by 8 out of the first 10 rounds' models. What would you expect to have happened to its weight over those rounds? | it would have grown substantially — being repeatedly misclassified means it gets upweighted at nearly every round, compounding into a much larger weight than examples the ensemble handles correctly | — |
| A6 | apply | short-answer | 0.3 | If a base model achieves exactly 50% weighted accuracy (ε=0.5) at some round, should training continue adding more such models? | not usefully — since α=0 for ε=0.5, that particular model contributes nothing to the final vote regardless of how many more rounds like it are added | — |
| E2 | explain | short-answer | 1.05 | Why does AdaBoost's α formula assign zero weight to a model with 50% weighted accuracy? | a model no better than random guessing provides no genuine information about the true labels beyond chance, so giving it any nonzero say in the vote would only add noise; the formula's ln((1−ε)/ε) term is exactly 0 at ε=0.5, encoding "no information, no vote" *(required)* | — |
| E3 | explain | short-answer | 1.1 | Why can AdaBoost's training error on the training set often reach zero even while using only weak (barely-better-than-random) individual learners? | each round targets the ensemble's current residual mistakes specifically, and repeated rounds of this targeted correction can progressively cover every training example's correct classification, even though no single round's model alone captures the whole pattern *(required)* | — |
| E4 | explain | short-answer | 1.15 | Why does the sequential dependency in AdaBoost's training (each round depends on the previous round's weights) make it harder to parallelize than bagging? | each round's reweighting scheme is computed directly from the errors of the model just trained in the previous round, so round k+1 cannot begin until round k's model and its errors are fully known — a hard sequential dependency chain that bagging's independent resamples don't have *(required)* | — |
| E5 | explain | short-answer | 0.9 | Why is a decision stump (depth-1 tree) a natural choice of weak learner for classic AdaBoost? | a single split is about as simple (weak) a classifier as possible while still being better than random guessing on most problems, matching the algorithm's design assumption of combining many individually-weak learners rather than a few individually-strong ones *(required)* | — |
| T2 | transfer | short-answer | 1.55 | How does AdaBoost's exponential reweighting of misclassified examples connect to the exponential loss function it can be shown to minimize? | AdaBoost can be derived as forward stagewise additive modeling that greedily minimizes exponential loss L=exp(−y·F(x)) at each round; the per-round example-weight update turns out to be exactly proportional to this exponential loss's derivative, so the "increase weight of mistakes" rule isn't an ad hoc heuristic — it's the natural consequence of greedily minimizing a specific, named loss function round by round *(required: names the exponential-loss derivation explicitly)* | — |
| T3 | transfer | short-answer | 1.6 | Why does understanding AdaBoost as minimizing a specific loss function (T2) make it a direct conceptual predecessor to `gradient-boosting`, which minimizes an arbitrary differentiable loss? | once AdaBoost is understood as forward stagewise fitting against exponential loss specifically, the natural generalization is asking what happens for *other* loss functions — gradient boosting answers this by fitting each new model to the negative gradient of any differentiable loss, making AdaBoost a special case of a more general framework rather than an unrelated algorithm *(required: names AdaBoost as the exponential-loss special case of the general framework)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.95…1.6.*

---

## Gradient Boosting (`gradient-boosting`)
*Prereq: Ensemble Methods, Gradient Descent · ancestors 16 · b₀ = 0.92*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.08 | Describe gradient boosting's core mechanism. | train models sequentially, each fit to the negative gradient of the loss (for squared error, this is just the residuals) of the current ensemble — gradient descent performed in "function space" | — |
| R2 | recall | mcq | 0.22 | Gradient boosting generalizes AdaBoost by: | allowing any differentiable loss function, by fitting each new model to that loss's gradient | claims it "removes the sequential structure entirely" — gradient boosting is still fully sequential → `gradient-boosting` |
| A1 | apply | short-answer | 0.72 | For squared-error loss, show that fitting to the negative gradient is (up to a constant) the same as fitting to the residuals. `[verified: dL/dF=-2(y-F), negative gradient = 2(y-F), proportional to residual]` | L=(y−F(x))², dL/dF=−2(y−F(x)); the negative gradient is 2(y−F(x)), proportional to the residual (y−F(x)) *(required: the explicit derivative)* | — |
| E1 | explain | short-answer | 1.42 | Why is gradient boosting described as "gradient descent in function space"? | ordinary gradient descent updates a fixed vector of parameters; here, each step *adds an entirely new function* (a tree) to the ensemble, and the "step direction" is a function (the negative gradient evaluated at each training point) rather than a parameter-update vector *(required: the parameter-vector vs. function contrast)* | — |
| T1 | transfer | short-answer | 1.92 | Why is gradient boosting, like AdaBoost but unlike bagging, prone to overfitting with too many rounds, and what's the standard remedy? | each additional round keeps fitting more closely to the current ensemble's residual errors, eventually starting to fit noise rather than signal; practitioners monitor validation performance and use early stopping — halting once it stops improving — as a critical hyperparameter for boosting methods specifically *(required: names early stopping as the specific remedy)* | — |

| R3 | recall | short-answer | −0.6 | What quantity does each new tree in gradient boosting predict, for squared-error loss? | the current residuals — the differences between the true target values and the current ensemble's predictions | — |
| R4 | recall | short-answer | −0.55 | What role does the "learning rate" (shrinkage) hyperparameter play in gradient boosting? | it scales down each new tree's contribution before adding it to the ensemble, controlling how large a step is taken at each round | — |
| R5 | recall | mcq | −0.35 | A smaller learning rate in gradient boosting typically requires: | more boosting rounds (trees) to reach comparable training performance, but often generalizes better | claims it "requires fewer rounds to converge, since each step is more precise" — a smaller step size generally means slower progress per round, needing *more* rounds, not fewer → `gradient-boosting` |
| R6 | recall | short-answer | −0.45 | Fill in the blank: gradient boosting fits each new model to the ___ of the loss function, evaluated at the current ensemble's predictions. | negative gradient |
| R7 | recall | mcq | −0.15 | Unlike AdaBoost's per-example weight reweighting, gradient boosting's mechanism for sequential correction is: | fitting new models directly to the loss gradient (residual-like values), rather than reweighting examples | claims gradient boosting "also reweights examples exactly the way AdaBoost does" — gradient boosting's correction mechanism is fitting to gradients, a generalization that doesn't require the same explicit example-reweighting scheme → `gradient-boosting` |
| R8 | recall | short-answer | −0.25 | True or false: gradient boosting can, in principle, be used with any differentiable loss function, not just squared error. | true — that generality (fitting to whatever loss's gradient is specified) is exactly what distinguishes it from AdaBoost's fixed exponential-loss framing | — |
| R9 | recall | mcq | −0.05 | The base learners in gradient boosting are most commonly: | small decision trees (often shallow, e.g. depth 3-8) | claims they're "always single-feature linear regressions" — gradient boosting is most commonly implemented with shallow decision trees as its base learner → `gradient-boosting` |
| R10 | recall | short-answer | 0.05 | What does it mean for gradient boosting's trees to be added "additively" to the ensemble? | the final prediction is the sum of the initial prediction plus every subsequent tree's (shrunk) contribution, rather than a vote or an average | — |
| R11 | recall | mcq | 0.15 | The initial prediction F₀ that gradient boosting starts from is typically: | a simple constant, such as the mean of the target (for regression) or the log-odds of the base rate (for classification) | claims it's "always zero for every problem" — the standard initialization is a simple constant model, e.g. the target mean, not always literally zero → `gradient-boosting` |
| R12 | recall | short-answer | 0.25 | Fill in the blank: gradient boosting's number of rounds (trees) and learning rate are jointly tuned, since they both affect how much ___ each additional round contributes. | progress (fit/shrinkage) |
| R13 | recall | mcq | 0.1 | For binary classification via gradient boosting with log-loss, the gradient being fit at each round is proportional to: | the difference between the true label and the current predicted probability | claims it's "proportional to the raw feature values" — the gradient of log-loss with respect to the model's output depends on the label and predicted probability, not directly on the raw input features → `gradient-boosting` |
| R14 | recall | short-answer | 0.2 | Does gradient boosting train its trees in parallel or sequentially? | sequentially — each new tree's target (the gradient) depends on the current ensemble's predictions, which include every previous tree | — |
| R15 | recall | mcq | 0.0 | Early stopping in gradient boosting means: | halting the addition of new trees once validation performance stops improving | claims it means "reducing the learning rate to zero partway through training" — early stopping halts the *number of rounds*, it doesn't manipulate the learning rate mid-training → `gradient-boosting` |
| R16 | recall | short-answer | 0.3 | Is gradient boosting's use of the term "gradient" referring to a gradient with respect to model parameters, or with respect to the model's predicted values? | with respect to the model's predicted values (function space) at each training point, not with respect to a fixed parameter vector | — |
| A2 | apply | numeric | 0.55 | Current ensemble prediction F₀=5 for a point with true target y=8. Learning rate=0.1. If a new tree predicts the full residual, what's the updated prediction F₁ = F₀ + learning_rate × residual? `[verified: 5.3]` | residual = 8−5 = 3; F₁ = 5 + 0.1(3) = 5.3 | — |
| A3 | apply | short-answer | 0.6 | Comparing learning rate 0.01 (many rounds needed) versus 0.3 (few rounds needed) on the same problem, which is generally safer against overfitting, all else equal? | learning rate 0.01 — smaller steps combined with many rounds tend to generalize better than large steps that fit the training data aggressively in few rounds, a common empirical finding for boosting | — |
| A4 | apply | short-answer | 0.5 | A gradient boosting model's validation error decreases for 200 rounds, then starts rising through round 500. What would early stopping do here? | halt training around where validation error stopped decreasing (near round 200), discarding the later rounds that were fitting noise rather than genuine signal | — |
| A5 | apply | short-answer | 0.65 | For binary classification with log-loss, if the current predicted probability is 0.3 and the true label is 1, is the gradient direction pushing the next tree's fit up or down? | up — the gradient is proportional to (y − p̂) = (1 − 0.3) = 0.7, a positive value, so the next tree is fit to push predictions toward the correct (higher) direction | — |
| A6 | apply | short-answer | 0.45 | If F₀ (the initial constant prediction) is set poorly (far from any reasonable target values), does gradient boosting eventually recover with enough rounds? | yes, generally — since each subsequent tree corrects the current residual/gradient, a poor starting point mainly costs some extra rounds to correct, rather than permanently limiting the final fit | — |
| E2 | explain | short-answer | 1.3 | Why does gradient boosting's "gradient descent in function space" framing (E1) explain why it generalizes AdaBoost rather than merely resembling it? | AdaBoost can itself be derived as greedily minimizing exponential loss via forward stagewise fitting; recognizing gradient boosting as performing this exact same greedy stagewise minimization but for an arbitrary differentiable loss shows AdaBoost is the special case where that loss happens to be exponential — not a separate algorithm gradient boosting merely resembles *(required)* | — |
| E3 | explain | short-answer | 1.35 | Why does a smaller learning rate combined with more rounds tend to produce a smoother approximation to the optimal function than a large learning rate with few rounds? | each round adds a discrete "step" toward reducing the loss; smaller steps taken more often can navigate the loss surface more carefully, correcting course incrementally, while a few large steps risk overshooting and locking in overly aggressive corrections based on any single round's residuals *(required)* | — |
| E4 | explain | short-answer | 1.4 | Why is gradient boosting particularly sensitive to outliers when using squared-error loss, and how might switching to a different loss function help? | squared-error loss's gradient (the residual) grows linearly with the size of the error, so a single large outlier produces an outsized residual that subsequent trees are pushed hard to fit; switching to a loss like Huber loss or absolute error, whose gradients don't grow as sharply for large residuals, reduces this outsized influence *(required: names the linear-in-residual sensitivity and a concrete remedy)* | — |
| E5 | explain | short-answer | 1.2 | Why does gradient boosting's sequential structure make it prone to overfitting in a way that bagging's parallel structure specifically avoids? | every additional round explicitly targets whatever the current ensemble still gets wrong, including any noise-driven residual left over; there's no natural point where adding more trees stops helping the way averaging more independent bagged trees does — each new tree keeps chasing signal until it starts chasing noise instead *(required)* | — |
| T2 | transfer | short-answer | 1.75 | How does gradient boosting's core insight — replacing a fixed loss (AdaBoost's exponential loss) with an arbitrary differentiable one — parallel the generalization from linear regression's squared-error loss to a broader family of generalized linear models with different link functions? | both moves take an algorithm originally tied to one specific loss/error structure and re-derive it around the *general mechanism* (gradient-based fitting, or a linear predictor plus a link function) so that the choice of loss/link becomes a free parameter rather than a fixed assumption — generalizing a specific successful method into a flexible framework by identifying and abstracting its core mechanism *(required: names the shared "generalize by abstracting the core mechanism" move)* | — |
| T3 | transfer | short-answer | 1.8 | Why does gradient boosting's function-space gradient descent connect back to `gradient-descent` from the bias-variance-and-optimization cluster, despite operating on trees rather than continuous parameters? | both are literally instances of moving iteratively in the direction that most reduces a loss function — ordinary gradient descent updates a parameter vector θ by −η∇L(θ), while gradient boosting updates a *function* F by adding a new function approximating −∇L(F) evaluated at the training points; the update rule's logic (move opposite the gradient, scaled by a step size) is identical, only the object being updated differs *(required: names the shared "move opposite the gradient" update rule across both objects)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.6…1.92.*

---

## XGBoost (`xgboost`)
*Prereq: Gradient Boosting · ancestors 17 · b₀ = 0.95*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.05 | Describe XGBoost in one sentence. | a highly optimized, regularized implementation of gradient boosting, adding explicit complexity penalties to the objective plus significant engineering optimizations for speed and scale | — |
| R2 | recall | mcq | 0.25 | XGBoost's explicit regularization term primarily combats: | the overfitting risk identified in `gradient-boosting`'s T1 (too many/too complex trees) | picks "the curse of dimensionality specifically" — an unrelated concern from regularization's actual purpose here → `xgboost` |
| A1 | apply | short-answer | 0.75 | Why does XGBoost's leaf-count and leaf-value penalty serve a similar purpose to `pruning-trees`' α·\|T\| penalty? | both add an explicit complexity penalty directly into the optimization objective, rather than relying solely on early stopping to control complexity — the same principle applied at a different stage of tree-building *(required: the direct parallel to cost-complexity pruning)* | — |
| E1 | explain | short-answer | 1.45 | Distinguish XGBoost's engineering optimizations from its statistical improvements. | engineering (sparse-data handling, parallelized construction, cache-aware computation) makes the algorithm run faster and at larger scale; the regularization term is a statistical improvement that makes the resulting model generalize better for a given amount of training — two different, complementary kinds of improvement over plain gradient boosting *(required: both categories named and distinguished)* | — |
| T1 | transfer | short-answer | 1.95 | Why have gradient-boosted trees historically dominated tabular-data competitions, while neural networks dominate unstructured data? | decision trees and their ensembles naturally handle mixed, nonlinear relationships among a moderate number of meaningfully-named tabular features without extensive feature engineering; neural networks instead excel at learning hierarchical feature representations from raw, high-dimensional, unstructured signal where hand-crafted features are hard to construct at all *(required: both halves of the contrast)* | — |

| R3 | recall | short-answer | −0.55 | What does the "X" in XGBoost stand for, informally? | "Extreme" — Extreme Gradient Boosting | — |
| R4 | recall | short-answer | −0.5 | Name one engineering optimization XGBoost is known for, beyond its statistical regularization. | parallelized (multi-threaded) tree construction, cache-aware computation, or efficient handling of sparse/missing data (any one) | — |
| R5 | recall | mcq | −0.3 | XGBoost's regularization term is typically applied to: | both the number of leaves and the magnitude of each leaf's predicted value | claims it's "applied only to the learning rate hyperparameter" — the regularization term penalizes tree structure/leaf values directly in the objective, it isn't the same thing as the learning rate → `xgboost` |
| R6 | recall | short-answer | −0.4 | Fill in the blank: XGBoost's objective function combines a loss term with an explicit ___ term. | regularization (complexity penalty) |
| R7 | recall | mcq | −0.2 | Compared to plain gradient boosting, XGBoost handles missing feature values by: | learning a default direction to send missing values at each split, from the training data itself | claims it "always simply discards any example with a missing value before training" — XGBoost specifically learns how to route missing values rather than dropping examples → `xgboost` |
| R8 | recall | short-answer | −0.25 | True or false: XGBoost was specifically designed to also work efficiently on datasets too large to fit fully in memory. | true — its engineering design explicitly targets scalability to large datasets, including out-of-core computation support | — |
| R9 | recall | mcq | −0.05 | XGBoost's use of second-order (Hessian) information in its objective, beyond gradient boosting's first-order gradient, allows: | a more precise per-step optimization of the objective at each round, akin to a Newton's-method-style update | claims it "removes the need for a learning rate entirely" — using second-order information changes how precisely each step is computed, but a learning rate / shrinkage hyperparameter is still used → `xgboost` |
| R10 | recall | short-answer | 0.05 | What kind of trees does XGBoost build as its base learners, matching gradient boosting generally? | shallow decision trees (regression trees), fit sequentially like standard gradient boosting | — |
| R11 | recall | mcq | 0.15 | XGBoost's column (feature) subsampling option is conceptually borrowed from: | random forests' feature-subsampling idea | claims it's "a technique unique to XGBoost with no precedent in earlier methods" — column subsampling in boosting is a direct borrow of random forests' decorrelation idea, adapted to a sequential setting → `xgboost` |
| R12 | recall | short-answer | 0.1 | Does XGBoost require the loss function to be squared error specifically? | no — like general gradient boosting, XGBoost supports any differentiable (and, since it also uses second-order information, twice-differentiable) loss function | — |
| R13 | recall | mcq | 0.0 | A key reason XGBoost became dominant in tabular-data machine-learning competitions is: | its combination of strong regularization (reducing overfitting) and highly optimized, fast training on large datasets | claims it's "because it eliminates the need for any hyperparameter tuning" — XGBoost still has many tunable hyperparameters; its edge comes from the regularization/speed combination, not tuning-free operation → `xgboost` |
| R14 | recall | short-answer | 0.2 | Fill in the blank: XGBoost's leaf-value penalty is analogous to the ___ penalty used in ridge regression. | L2 (squared-magnitude) |
| R15 | recall | mcq | 0.1 | XGBoost's own tunable regularization hyperparameters (e.g. λ, γ) let a practitioner: | directly control how strongly the objective penalizes tree complexity, beyond what early stopping alone provides | claims they "have no effect once early stopping is used, making them redundant" — the explicit regularization terms and early stopping are separate, complementary complexity controls, not redundant with each other → `xgboost` |
| R16 | recall | short-answer | 0.15 | Is XGBoost, in terms of its core statistical framework, a fundamentally different algorithm from gradient boosting, or an engineered/regularized variant of it? | an engineered and regularized variant — it builds on the same additive, gradient-fitting framework, adding an explicit complexity penalty and substantial computational optimizations rather than replacing the underlying method | — |
| A2 | apply | numeric | 0.65 | XGBoost's leaf penalty for a tree is γ·T + ½λ·Σw², where T=5 leaves, γ=0.1, λ=1, and Σw²=3 (sum of squared leaf values). Compute the penalty. `[verified: 2.0]` | 0.1(5) + 0.5(1)(3) = 0.5 + 1.5 = 2.0 | — |
| A3 | apply | short-answer | 0.7 | Two candidate trees fit the training residuals equally well, but Tree A has 20 leaves and Tree B has 6 leaves. Which would XGBoost's regularized objective favor, all else equal? | Tree B — with equal fit quality, the regularization term penalizes the larger number of leaves in Tree A more, so the smaller tree achieves a lower overall (loss + penalty) objective | — |
| A4 | apply | short-answer | 0.6 | A dataset has 15% of one feature's values missing. Would XGBoost need that feature's missing entries imputed before training? | not necessarily — XGBoost can learn a default split direction for missing values directly from the training data, avoiding the need for a separate imputation step for that feature | — |
| A5 | apply | short-answer | 0.55 | Comparing training XGBoost on a single machine's single thread versus its default multi-threaded mode, would you expect a meaningful difference in wall-clock training time on a large dataset? | yes — XGBoost's engineering optimizations specifically include parallelized tree construction, so multi-threading typically produces substantially faster training on large datasets compared to a single-threaded run | — |
| A6 | apply | short-answer | 0.75 | If λ (the leaf-value regularization strength) is increased substantially, what tends to happen to the magnitude of individual leaf predictions? | they tend to shrink toward zero — larger λ more heavily penalizes large leaf values in the objective, pushing the optimizer toward smaller, more conservative leaf predictions | — |
| E2 | explain | short-answer | 1.1 | Why does adding an explicit regularization term to the objective (rather than relying only on early stopping) give XGBoost finer control over overfitting? | early stopping is a blunt, global instrument — it only controls *how many* rounds run; an explicit per-tree complexity penalty shapes *every* tree's structure and leaf values throughout training, allowing the model to prefer simpler trees at every single round rather than only cutting off training globally once problems appear *(required)* | — |
| E3 | explain | short-answer | 1.2 | Why does using second-order (Hessian) information let XGBoost take a more targeted optimization step than a first-order-only method at each round? | the gradient alone says which direction reduces loss, but the Hessian (curvature) says how far that direction can be trusted before the linear approximation breaks down — combining both, as in a Newton-style update, lets each round's tree be fit to a more accurate local approximation of the true loss reduction than the gradient alone would give *(required)* | — |
| E4 | explain | short-answer | 1.15 | Why does XGBoost's engineering-level scalability matter as much as its statistical improvements for its practical dominance? | a statistically superior model that takes prohibitively long to train or can't fit in available memory is unusable in practice; XGBoost's cache-aware computation, parallelization, and sparsity-handling let its statistical advantages (regularization, second-order fitting) actually be realized at the scale of real competition and production datasets *(required)* | — |
| E5 | explain | short-answer | 1.05 | Why does XGBoost's learned handling of missing values (rather than requiring pre-imputation) avoid introducing bias that manual imputation might? | manual imputation (e.g. filling with the mean) invents a specific value that may not reflect why the value is missing, potentially distorting splits; letting the algorithm learn the best default direction for missing values directly from how they correlate with the target during training avoids injecting an arbitrary, possibly misleading assumption *(required)* | — |
| T2 | transfer | short-answer | 1.7 | How does XGBoost's explicit leaf-value L2 penalty structurally parallel ridge regression's coefficient-shrinkage penalty? | both add a term proportional to the sum of squared "weights" (leaf values here, regression coefficients there) to the objective being minimized, shrinking those values toward zero and trading a little training fit for reduced sensitivity to noise — the identical L2-regularization idea, applied to two different kinds of model parameters *(required: names the shared L2-shrinkage mechanism)* | — |
| T3 | transfer | short-answer | 1.6 | Why does XGBoost's success illustrate a broader pattern in applied machine learning — that engineering optimization and statistical improvement often need to advance together for a method to become dominant in practice? | a purely statistical improvement (regularized gradient boosting) without the engineering work to make it fast and scalable would have remained a research curiosity rather than a competition-winning, industry-standard tool; conversely, engineering speed alone without the regularization would just train an overfit model faster — XGBoost's actual impact came from both advancing together, a pattern that recurs across many widely-adopted ML tools *(required: names both dimensions and that neither alone would suffice)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.55…1.95.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| decision trees assumed sensitive to feature scale | `decision-tree` |
| classification vs. regression leaf-prediction rules swapped | `decision-tree` |
| tree depth conflated with leaf count | `decision-tree` |
| decision stump confused with a forest/ensemble | `decision-tree` |
| pure-node impurity direction reversed | `splitting-criteria` |
| role of splitting criteria confused with a stopping/pruning rule | `splitting-criteria` |
| entropy's logarithm base assumed to be base 10 | `splitting-criteria` |
| Gini impurity assumed inapplicable to multi-class problems | `splitting-criteria` |
| pruning's α tuned against training rather than validation performance | `pruning-trees` |
| pre-pruning and post-pruning assumed always equivalent | `pruning-trees` |
| pruning's α=0 case confused with maximal pruning | `pruning-trees` |
| pruning conflated with an ensembling technique | `pruning-trees` |
| ensembling assumed to worsen overfitting | `ensemble-methods` |
| ensembling's variance-reduction basis attributed to the wrong statistical result | `ensemble-methods` |
| stacking conflated with simple averaging/voting | `ensemble-methods` |
| bagging's variance-vs-bias target confused | `bagging` |
| out-of-bag points confused with points the model misclassified | `bagging` |
| bagging's variance reduction misattributed to smaller per-model training sets | `bagging` |
| random forest's feature subsampling reduced to a speed optimization | `random-forests` |
| random forest and bagging's regression/classification default subsample-size rules swapped | `random-forests` |
| feature-importance scores assumed to be manually assigned rather than computed | `random-forests` |
| AdaBoost's sequential targeting conflated with bagging's randomness | `adaboost` |
| AdaBoost's per-model voting weight assumed uniform across models | `adaboost` |
| AdaBoost's α=0.5-error case mishandled (clipped rather than negative) | `adaboost` |
| gradient boosting's sequential nature assumed removed by generalizing AdaBoost | `gradient-boosting` |
| gradient boosting's example-correction mechanism confused with AdaBoost's reweighting | `gradient-boosting` |
| smaller learning rate wrongly assumed to need fewer rounds | `gradient-boosting` |
| XGBoost's regularization and engineering improvements conflated | `xgboost` |
| XGBoost's second-order information assumed to eliminate the learning rate | `xgboost` |
| XGBoost's missing-value handling assumed to require discarding examples | `xgboost` |

**Cluster total: 270 items across 9 concepts (30 each).** All numeric claims verified, including the
exact Gini=2p(1−p) identity (matching Bernoulli variance precisely, not approximately), the ~37%
out-of-bag exclusion rate confirmed at n=100, the AdaBoost voting weights α≈0.424 (ε=0.3) and α≈1.099
(ε=0.1), and the XGBoost leaf-penalty worked example (0.1·5 + 0.5·1·3 = 2.0).
