# Cluster 11 — Named Tests & Resampling

One/Two-Sample Z/t-tests, Chi-Square Tests, Fisher's Exact, Wilcoxon, Bootstrapping (11 concepts).
Same format as [Cluster 1](foundations-of-probability.md). This is the last cluster of the
probability/statistics sweep — everything here is an *application* of machinery built in Clusters
1–10, so items lean on cross-references rather than re-deriving shared logic, and run one level
lighter (6 items each) since the underlying test-construction logic repeats across the family.

---

## One Sample Z-Test (`one-sample-z-test`)
*Prereq: Hypothesis Test, Standard Error, Normal Distribution · ancestors 27 · b₀ = 1.17*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.17 | State the one-sample Z test statistic and its one defining, often-unrealistic assumption. | Z=(X̄−μ₀)/(σ/√n); requires σ **known** | — |
| R2 | recall | mcq | 0.47 | If σ is actually unknown but this test is used anyway (plugging in s), what's the consequence, especially for small n? | the test's true error rate departs from the nominal α — one-sample-t-test exists specifically to handle this case correctly | assumes plugging in s changes nothing about the test's validity → `one-sample-z-test` |
| A1 | apply | numeric | 0.97 | n=36, σ=12 (known), μ₀=50, x̄=57. Test H₀:μ=50 at α=0.05. `[verified: Z=3.5]` | Z=(57−50)/(12/6)=3.5; \|3.5\|>1.96, **reject** | — |
| E1 | explain | short-answer | 1.67 | Is this test's Type I error rate exactly α, or only approximately? Under what condition does each hold? | exactly α if the underlying data is truly Normal; only approximately α (via CLT, from `sampling-distribution`) if the data isn't Normal but n is large enough for X̄ to be approximately Normal regardless *(required: both cases distinguished)* | — |
| T1 | transfer | short-answer | 2.17 | Why is "σ known" almost never realistic in practice? What test replaces this one as a result? | σ is a population parameter, and if it's unknown (the near-universal case — even the calibration data used to "know" σ is itself a finite sample), the correct move is to *estimate* it with s and account for that extra uncertainty via a wider-tailed distribution — exactly what `one-sample-t-test` does next *(required: names the estimation-uncertainty gap, not just "σ is unknown")* | — |
| R3 | recall | short-answer | −1.6 | State the full null and alternative hypotheses tested by a two-tailed one-sample Z-test. | H₀: μ=μ₀; H₁: μ≠μ₀ | — |
| R4 | recall | mcq | −1.5 | The one-sample Z-test statistic is: | Z = (X̄ − μ₀) / (σ/√n) | writes it as Z=(X̄−μ₀)/σ, omitting the division by √n entirely → `one-sample-z-test` |
| R5 | recall | short-answer | −1.4 | True or false: the one-sample Z-test is a direct application of the general test-statistic and rejection-region machinery from Cluster 10. | true — it's exactly that machinery, applied with the specific assumption that σ is known | — |
| R6 | recall | short-answer | −1.3 | Fill in the blank: the one-sample Z-test compares a sample mean against a hypothesized ____ value. | population mean (μ₀) | — |
| R7 | recall | mcq | −1.2 | This test's sampling distribution result relies most directly on which earlier concept? | Sampling Distribution (CLT) — or exact normality if the population itself is Normal | relies on "the Law of Total Variance," an unrelated concept that plays no role in deriving this test's reference distribution → `one-sample-z-test` |
| R8 | recall | short-answer | −1.1 | Define what "μ₀" represents in this test's hypotheses. | the specific, hypothesized value of the population mean being tested against, stated in H₀ | — |
| R9 | recall | mcq | −1.0 | A one-tailed version of this test (H₁: μ>μ₀) would use which critical value at α=0.05? | 1.645 | uses 1.96, the two-tailed critical value, for a one-tailed test → `one-sample-z-test` |
| R10 | recall | short-answer | −0.9 | True or false: this test can be used validly even for a moderately non-Normal population, provided n is large. | true — CLT lets X̄ be approximately Normal for large n even from a non-Normal population, echoing `sampling-distribution`'s content | — |
| R11 | recall | short-answer | −0.8 | Fill in the blank: this test requires knowing σ, the population ____. | standard deviation | — |
| R12 | recall | mcq | −0.7 | Which scenario most plausibly has a genuinely known σ? | a well-calibrated manufacturing process with decades of historical variance data | picks "a brand-new startup's first customer survey," a scenario with no plausible source of a truly known σ → `one-sample-z-test` |
| R13 | recall | short-answer | −0.6 | State this test's decision rule in terms of the test statistic and rejection region. | reject H₀ if the computed Z falls in the rejection region determined by α and the tail direction of H₁ | — |
| R14 | recall | mcq | −0.5 | This test's assumption "σ known" is most often only approximately satisfied by: | plugging in a very large, separately-collected historical sample's s as if it were the true σ | claims the assumption is "never even approximately satisfiable," overstating the case — in some settings a well-estimated historical σ is a reasonable stand-in → `one-sample-z-test` |
| R15 | recall | short-answer | −0.4 | True or false: if σ is unknown and s is used in its place in the Z formula without adjustment, the resulting test's true error rate can depart from the nominal α, especially at small n. | true — this is exactly R2's point, and the reason `one-sample-t-test` exists | — |
| R16 | recall | short-answer | −0.3 | Fill in the blank: this test's exact validity (Type I rate exactly α) requires the underlying population data to be ____. | (exactly) Normal | — |
| A3 | apply | numeric | 1.0 | n=25, σ=10 (known), μ₀=100, x̄=104. Test H₀:μ=100 at α=0.05 (two-tailed). `[verified: Z=2]` | Z=(104−100)/(10/5)=2; \|2\|<1.96, **fail to reject** | — |
| A4 | apply | numeric | 1.1 | n=49, σ=14 (known), μ₀=30, x̄=34, one-tailed H₁:μ>30, α=0.05. `[verified: Z=2]` | Z=(34−30)/(14/7)=2; 2>1.645, **reject** | — |
| A5 | apply | short-answer | 1.2 | A calibration lab claims decades of historical data give a trustworthy σ=3 for a measurement instrument. A new batch of n=40 gives x̄=52 against μ₀=50. Should this test or the t-test be used, and why? | the Z-test — since σ is treated as known from extensive historical calibration data rather than estimated from this specific small sample, the defining assumption of the Z-test (not the t-test) is satisfied | — |
| A6 | apply | numeric | 1.3 | n=100, σ=5 (known), μ₀=20, x̄=21.2. Compute Z and state the two-tailed decision at α=0.01 (critical value ≈2.576). `[verified: Z=2.4, fail to reject]` | Z=(21.2−20)/(5/10)=2.4; \|2.4\|<2.576, **fail to reject** at this stricter α, though it would have been rejected at α=0.05 | — |
| A7 | apply | short-answer | 1.35 | State, without computing, whether a two-tailed test at α=0.10 is more or less likely to reject H₀ than the same data tested at α=0.01. | more likely — a larger α produces a smaller-magnitude critical value, making rejection easier for the same observed Z, mirroring `rejection-region`'s critical-value-shrinks-with-larger-α fact | — |
| E3 | explain | short-answer | 1.9 | Explain why this test, despite being "simple," is still worth studying on its own rather than jumping straight to the t-test. | it isolates the core test-statistic/rejection-region logic in its cleanest form, with one fewer moving part (no need to separately estimate σ and account for that extra uncertainty) — understanding this test thoroughly makes the *contrast* with the t-test (R2, and `one-sample-t-test`'s whole content) meaningful rather than an isolated new formula to memorize *(required)* | — |
| E4 | explain | short-answer | 2.0 | Explain why using the wrong assumption (σ known when it's actually estimated) tends to make the resulting test's actual Type I error rate *higher* than the nominal α, not lower. | treating s as if it were the exactly-known σ ignores the extra sampling variability in s itself, making the computed Z systematically overstate how far X̄ really is (in truly-known-precision terms) from μ₀; this makes rejection happen more often than the nominal α promises, especially at small n where s is a noisier estimate of σ *(required: the "understates true uncertainty, so over-rejects" direction specifically)* | — |
| E5 | explain | short-answer | 2.05 | Explain why the exact Type I rate of this test is guaranteed only under Normal population data, connecting to `sampling-distribution`'s CLT content. | the exact Normal reference distribution for Z assumes X̄ itself is exactly Normal, which holds precisely only when the underlying population is Normal; for non-Normal populations, X̄ is only *approximately* Normal (via CLT), so the test's actual Type I rate is only approximately α, with the approximation improving as n grows *(required: explicitly ties to CLT's approximate-vs-exact distinction)* | — |
| E6 | explain | short-answer | 2.1 | Explain why this test's rejection region, hypothesis structure, and decision rule are all *identical in form* to the general `hypothesis-test`/`rejection-region` machinery, with only the specific test statistic formula being new here. | nothing about the five-step procedure (state hypotheses, choose α, compute statistic, determine region, decide) changes between the abstract Cluster 10 treatment and this concrete instance — the only new content is the specific formula Z=(X̄−μ₀)/(σ/√n) and its assumption that σ is known; everything else is inherited machinery, which is exactly why this concept's own item count stays comparatively light *(required)* | — |
| T2 | transfer | short-answer | 2.25 | A researcher insists on using this Z-test for a small clinical sample (n=12) because "σ is basically 15, we've seen it before in similar studies." Critique the reliability of this justification compared to a scenario with true industrial calibration data. | "we've seen it before in similar studies" is a much weaker basis for treating σ as known than a long, well-controlled calibration history — similar studies could differ in ways that shift the true variance, and borrowing an approximate σ from loosely related prior work reintroduces exactly the kind of unaccounted uncertainty this test's core assumption is supposed to rule out; the more defensible choice here is the t-test, which doesn't require this leap of faith *(required: explicitly contrasts the weak "similar studies" justification against genuine calibration-grade knowledge of σ)* | accepts "we've seen a similar value before" as an adequate basis for treating σ as known, on the same footing as a rigorous calibration history → `one-sample-z-test` |
| T3 | transfer | numeric | 2.35 | A researcher wrongly uses this Z-test (assuming σ=8 is "known" from a rough guess) on n=10 data with x̄=54, μ₀=50, getting Z=1.58 (fail to reject at α=0.05, since 1.58<1.96). A more careful colleague instead properly treats σ as unknown, uses s=8 in a t-test with df=9 (critical t≈2.262), and gets the same T=1.58. Explain why both tests happen to give the same "fail to reject" conclusion here despite the flawed assumption, and why this doesn't excuse the original mistake. `[verified: Z=T=1.58 numerically, both below their respective (different) critical values]` | both computed statistics use the identical formula value (1.58) here since s was numerically equal to the guessed σ; but the *critical values differ* (1.96 for Z vs. 2.262 for t at df=9) — the t-test's wider critical value reflects the extra uncertainty from estimating σ, and in general (with a different sample) the two tests can disagree; getting the same conclusion this one time is a coincidence of this specific dataset, not evidence the Z-test's flawed assumption was harmless in general *(required: distinguishes the coincidental numerical agreement from a general validity claim)* | argues the Z-test's assumption "must have been fine" because it happened to agree with the t-test's conclusion on this one dataset → `one-sample-z-test` |

*Coverage: 16/6/5/3 — 30 items, −1.6…2.35.*

---

## One Sample T-Test (`one-sample-t-test`)
*Prereq: One Sample Z-Test, Student's t-Distribution · ancestors 38 · b₀ = 1.33*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.33 | State the one-sample T statistic, its distribution under H₀, and the key difference from the Z-test. | T=(X̄−μ₀)/(s/√n) ~ t_{n−1} under H₀; the difference from Z is that σ is **unknown**, estimated by s | — |
| R2 | recall | mcq | 0.63 | For small n, the t-test's critical value is ___ the z-test's at the same α: | always larger | assumes t and z critical values are always equal, missing that t has heavier tails (`t-distribution`, Cluster 4) → `one-sample-t-test` |
| A1 | apply | numeric | 1.13 | n=16, x̄=52, s=8, μ₀=48, α=0.05 two-tailed, t₁₅,₀.₀₂₅≈2.131. `[verified: T=2]` | T=(52−48)/(8/4)=2; \|2\|<2.131, **fail to reject** — but a naive z-test using 1.96 would have wrongly *rejected* (2>1.96), illustrating exactly why the wider t critical value matters *(required: notes the would-be-wrong z-based decision)* | uses z's 1.96 critical value here, concluding "reject" → `one-sample-t-test` |
| E1 | explain | short-answer | 1.83 | Why do t_{n−1} critical values converge to z critical values as n grows? | matches `t-distribution`'s E1 exactly: as degrees of freedom grow, V/k concentrates near 1 (an informal LLN argument), so Tₖ≈Z/1=Z, and the extra uncertainty from estimating σ becomes negligible *(required: the direct callback)* | — |
| T1 | transfer | short-answer | 2.33 | A small clinical trial (n=16) must use t, not z, because σ is never truly known. What's the practical consequence of incorrectly using z instead, as in A1? | using z understates the genuine uncertainty from estimating σ with a small sample, producing more false rejections (Type I errors) than the nominal α promises — precisely what A1 demonstrated numerically *(required: connects back to the specific A1 result)* | — |
| R3 | recall | short-answer | −1.4 | State the general formula for the one-sample T statistic. | T = (X̄ − μ₀) / (s/√n) | — |
| R4 | recall | mcq | −1.3 | The t-distribution used has how many degrees of freedom for a one-sample test with n observations? | n − 1 | uses n degrees of freedom, missing the same n−1 adjustment established for `sample-variance` → `one-sample-t-test` |
| R5 | recall | short-answer | −1.2 | True or false: the t-distribution has heavier tails than the standard Normal distribution. | true — this is exactly why its critical values are larger in magnitude than the corresponding Z critical values, especially at small df | — |
| R6 | recall | short-answer | −1.1 | Fill in the blank: as degrees of freedom → ∞, the t-distribution approaches the ____ distribution. | standard Normal | — |
| R7 | recall | mcq | −1.0 | Which is the key difference between the one-sample Z-test and T-test formulas? | the T-test uses s (the sample SD) in place of σ | claims the difference is "the T-test uses n−1 observations while the Z-test uses all n," confusing the degrees-of-freedom adjustment inside the *distribution* with a change to how many data points are used → `one-sample-t-test` |
| R8 | recall | short-answer | −0.9 | Define, in one sentence, why this test's critical values shrink toward the Z-test's as n grows. | as n grows, s becomes a more precise estimate of σ, so the extra uncertainty the t-distribution's heavier tails account for shrinks, and t_{n−1} converges to the standard Normal | — |
| R9 | recall | mcq | −0.8 | For very small n (e.g. n=5), the t-critical value at α=0.05 two-tailed is: | noticeably larger than 1.96 | claims it's "smaller than 1.96, since small samples need less evidence to reject," backwards from the actual heavier-tail behavior at low df → `one-sample-t-test` |
| R10 | recall | short-answer | −0.7 | True or false: this test requires the population to be exactly Normal for its Type I rate to be exactly α, just like the Z-test. | true — the exact t_{n−1} reference distribution result requires exactly Normal data, though it's commonly used approximately for mildly non-Normal data at moderate-to-large n | — |
| R11 | recall | short-answer | −0.6 | Fill in the blank: this test is used instead of the Z-test whenever σ is ____. | unknown | — |
| R12 | recall | mcq | −0.5 | The one-sample t-test's null hypothesis is stated as: | H₀: μ = μ₀ | states H₀ in terms of s or the sample statistic directly (e.g. "H₀: X̄=μ₀"), rather than correctly as a claim about the unknown population parameter μ → `one-sample-t-test` |
| R13 | recall | short-answer | −0.4 | State what happens to the t-distribution's shape as degrees of freedom increase from very small (e.g. 2) to large (e.g. 100). | its tails become progressively thinner, and its overall shape converges toward the standard Normal bell curve | — |
| R14 | recall | mcq | −0.3 | Which quantity, computed from the sample, appears in the T formula but not the Z formula? | s, the sample standard deviation | claims "n appears in T but not Z," which is false — n (via √n) appears in both formulas' denominators identically → `one-sample-t-test` |
| R15 | recall | short-answer | −0.2 | True or false: t-distribution critical values are tabulated separately for each degrees-of-freedom value, unlike the single fixed Z table. | true — since the t-distribution's shape itself depends on df, a full table (or df-specific computation) is needed, unlike the one fixed standard Normal table for Z | — |
| R16 | recall | short-answer | −0.1 | Fill in the blank: the one-sample t-test statistic's distribution under H₀ is t with ____ degrees of freedom. | n − 1 | — |
| A3 | apply | numeric | 1.05 | n=25, x̄=48, s=10, μ₀=45, α=0.05 two-tailed, t₂₄,₀.₀₂₅≈2.064. `[verified: T=1.5]` | T=(48−45)/(10/5)=1.5; \|1.5\|<2.064, **fail to reject** | — |
| A4 | apply | numeric | 1.15 | n=9, x̄=105, s=12, μ₀=100, α=0.05 two-tailed, t₈,₀.₀₂₅≈2.306. `[verified: T=1.25]` | T=(105−100)/(12/3)=1.25; \|1.25\|<2.306, **fail to reject** | — |
| A5 | apply | short-answer | 1.25 | A study has n=8, giving df=7 and a two-tailed critical t of about 2.365. Would this critical value be larger or smaller if n were instead 40? Why? | smaller — with df=39, the t-distribution is much closer to standard Normal, so its two-tailed critical value at α=0.05 would be close to 1.96, noticeably smaller than 2.365 | — |
| A6 | apply | short-answer | 1.35 | A colleague computes a t-statistic correctly as T=2.5 for n=10 (df=9), but looks up the critical value using df=10 instead of df=9 by mistake. Would this error tend to make the test too conservative or too liberal? | slightly too conservative in this direction (using df=10's critical value, which is marginally smaller than df=9's, since more df means thinner tails and smaller critical values) — meaning the mistaken critical value very slightly overstates how easy it is to reject, though the effect is small since 9 and 10 df are close | — |
| A7 | apply | numeric | 1.45 | n=16, x̄=61, s=8, μ₀=55, α=0.01 two-tailed, t₁₅,₀.₀₀₅≈2.947. `[verified: T=3]` | T=(61−55)/(8/4)=3; \|3\|>2.947, **reject** — narrowly clears even this strict threshold | — |
| E3 | explain | short-answer | 2.0 | Explain why the t-distribution has heavier tails than the Normal for small df, in terms of what's being estimated. | with small n, s is itself a noisy, unreliable estimate of σ — sometimes it happens to come out too small, which would make T artificially large purely from that estimation noise, not a genuine departure from H₀; the t-distribution's heavier tails build in extra allowance for this added source of variability, which shrinks as n (and hence the reliability of s) grows *(required: the explicit "s itself is noisy" mechanism)* | — |
| E4 | explain | short-answer | 2.05 | Explain why it would be a mistake to always use the t-distribution's critical values, "just to be safe," even in the rare case where σ truly is known. | when σ genuinely is known, using the Z-test's exact reference distribution is *correct* and more powerful — the t-distribution's extra tail-width exists specifically to compensate for the *additional* uncertainty of estimating σ, and applying that compensation when it isn't needed makes the test unnecessarily conservative, reducing power to detect real effects for no statistical reason *(required: the explicit power cost of over-correcting)* | — |
| E5 | explain | short-answer | 2.1 | Explain the relationship between this test's degrees of freedom (n−1) and `sample-variance`'s earlier degrees-of-freedom fact, and why the same number appears in both places. | s² in the T formula is computed with the same (n−1)-divisor sample variance from `sample-variance`, whose degrees of freedom reflect that only n−1 of the n deviations from X̄ are free to vary once X̄ itself is fixed; that same n−1 count then determines which t-distribution correctly describes T's behavior, since T's derivation depends on s² following a chi-square distribution with exactly that many degrees of freedom *(required: explicitly connects the shared n−1 back to the "last deviation is forced" argument)* | — |
| E6 | explain | short-answer | 2.15 | Explain why a two-tailed t-test's critical value table has one row per degrees-of-freedom value, while a Z-table needs only one fixed set of critical values regardless of n. | the t-distribution's exact shape (tail heaviness) depends on df, so a different df genuinely implies a different reference distribution and hence different critical values; the standard Normal used for Z has no such parameter at all — it's a single, fixed distribution regardless of n, which is precisely why n enters the Z-test only through SE, never through a change in which reference distribution is used *(required: explicitly contrasts "df changes the reference distribution" with "n never changes which distribution Z uses")* | — |
| T2 | transfer | short-answer | 2.4 | A meta-analysis pools results from many small clinical trials (n between 8 and 20 each), all of which correctly used the t-test. A researcher wants to combine their p-values by treating all the t-statistics "as if" they were Z-statistics for simplicity. Explain what goes wrong with this simplification, using this concept's core distinction. | each t-statistic's reference distribution depends on its own trial's specific degrees of freedom (n−1), which differ across the pooled trials; treating them all as Z-statistics discards the heavier-tailed correction each trial actually needed, systematically understating how surprising each result really was under its own H₀ — especially for the smallest trials, where the t/Z gap is largest (per A5) *(required: notes that the t/Z gap varies by trial size, so the error isn't uniform across the pooled studies)* | assumes treating t-statistics as Z-statistics is a harmless simplification since "they're both standardized," without accounting for the varying, sample-size-dependent gap between the two → `one-sample-t-test` |
| T3 | transfer | numeric | 2.5 | A quality engineer tests n=5 parts (df=4, two-tailed critical t≈2.776) and separately tests n=50 parts (df=49, two-tailed critical t≈2.01) from the same process, both producing T=2.2. Compare the two decisions and explain why identical T values lead to different conclusions. `[verified: n=5 fails to reject (2.2<2.776); n=50 rejects (2.2>2.01)]` | at n=5, T=2.2 falls short of the much larger critical value (2.776) required at such low df, so the test **fails to reject**; at n=50, the same T=2.2 comfortably exceeds the much smaller, near-Normal critical value (2.01), so the test **rejects** — identical evidence (T=2.2) is judged completely differently because the reference distribution itself is narrower and more decisive at higher df, directly illustrating R9's "small n needs bigger t-critical" fact with a concrete side-by-side numeric contrast *(required: computes and states both decisions explicitly, and explains the mechanism, not just "the critical values differ")* | assumes the same T value must lead to the same reject/fail-to-reject decision regardless of degrees of freedom → `one-sample-t-test` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.5.*

---

## One Sample Proportions Z-Test (`one-sample-proportions-z-test`)
*Prereq: One Sample Z-Test, Bernoulli and Binomial Distributions · ancestors 33 · b₀ = 1.26*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.26 | State the test statistic for H₀:p=p₀, noting exactly which value goes inside the SE. | Z=(p̂−p₀)/√(p₀(1−p₀)/n) — uses **p₀**, the hypothesized value, inside the SE, not the observed p̂ | — |
| R2 | recall | mcq | 0.56 | Why use p₀ rather than p̂ in the SE formula? | under H₀ we *assume* p=p₀ is true, so the correct SE to use is the one implied by that assumption | claims "it doesn't matter which you use" — a common shortcut that quietly breaks the test's logic → `one-sample-proportions-z-test` |
| A1 | apply | numeric | 1.06 | n=200, 130 successes (p̂=0.65), H₀:p=0.6, α=0.05. `[verified: SE≈0.0346, Z≈1.443]` | SE=√(0.6·0.4/200)≈0.0346; Z=(0.65−0.6)/0.0346≈1.44; \|1.44\|<1.96, **fail to reject** | plugs p̂=0.65 into the SE formula instead of p₀=0.6, per R2's error → `one-sample-proportions-z-test` |
| E1 | explain | short-answer | 1.76 | Why does this test require np₀≥10 and n(1−p₀)≥10 to be valid? | this is the Normal-approximation-to-Binomial condition, echoing Cluster 3's Poisson-limit intuition in reverse — here we need the Binomial count to look *Normal*, not Poisson, which requires n large enough relative to how extreme p₀ is *(required: connects to the same "n large, event not too rare" logic underlying both limits)* | — |
| T1 | transfer | short-answer | 2.26 | Why is it conceptually wrong (not just a stylistic difference) to substitute p̂ for p₀ in the SE — even though it seems like "using the best available estimate"? | the whole logic of a hypothesis test is to ask what the data would look like *if H₀ were true*; substituting p̂ abandons that assumption mid-calculation, mixing "what we assumed" with "what we observed" in a way that invalidates the test's error-rate guarantees *(required: the logical-inconsistency argument, not just "it's the convention")* | — |
| R3 | recall | short-answer | −1.4 | State the formula for p̂ (the sample proportion) given x successes in n trials. | p̂ = x/n | — |
| R4 | recall | mcq | −1.3 | This test's underlying distributional assumption relies most directly on which earlier concept? | Bernoulli and Binomial Distributions, via a Normal approximation to the Binomial | relies on "the exact hypergeometric distribution," confusing this test's Normal-approximation approach with the exact-probability approach used later by `fischers-exact-test` → `one-sample-proportions-z-test` |
| R5 | recall | short-answer | −1.2 | True or false: this test can, in principle, be run with any sample size, with no minimum requirement. | false — it requires np₀≥10 and n(1−p₀)≥10 for the Normal approximation to be trustworthy | — |
| R6 | recall | short-answer | −1.1 | Fill in the blank: p̂ is a statistic; p₀ is a ____. | (hypothesized) parameter value | — |
| R7 | recall | mcq | −1.0 | The formula for the SE used in this test's denominator is: | √(p₀(1−p₀)/n) | writes √(p̂(1−p̂)/n), substituting p̂ for p₀ exactly as R2/T1 warn against → `one-sample-proportions-z-test` |
| R8 | recall | short-answer | −0.9 | Define H₀ and H₁ for a two-tailed test of whether a coin's true probability of heads differs from 0.5. | H₀: p=0.5; H₁: p≠0.5 | — |
| R9 | recall | mcq | −0.8 | For a claimed p₀=0.5 and n=40, the check np₀≥10 gives: | 20 ≥ 10, satisfied | computes n(1−p₀) using p̂ instead of p₀, illustrating the same substitution error this concept's misconception targets → `one-sample-proportions-z-test` |
| R10 | recall | short-answer | −0.7 | True or false: this test's Z statistic can be negative. | true — whenever p̂ < p₀, the numerator p̂−p₀ is negative | — |
| R11 | recall | short-answer | −0.6 | Fill in the blank: this test is the categorical-data analogue of the one-sample ____-test for a continuous mean. | Z (one-sample Z-test) | — |
| R12 | recall | mcq | −0.5 | Which of these is an appropriate H₀ for testing whether a website's true conversion rate is 5%? | H₀: p = 0.05 | states H₀ in terms of the observed conversion count directly, rather than the hypothesized population proportion parameter → `one-sample-proportions-z-test` |
| R13 | recall | short-answer | −0.4 | State what "n(1−p₀)≥10" checks, in words. | that the expected number of "failures" (non-successes) under H₀ is large enough for the Normal approximation to the Binomial to be reliable | — |
| R14 | recall | mcq | −0.3 | If p₀=0.02 (a rare-event hypothesis), a much larger n is typically needed for this test's validity because: | np₀≥10 requires n≥500 when p₀ is this small, a far larger n than for p₀ near 0.5 | claims "smaller p₀ values always make the test more accurate regardless of n," ignoring the np₀≥10 validity condition entirely → `one-sample-proportions-z-test` |
| R15 | recall | short-answer | −0.2 | True or false: p̂ is used anywhere at all in the correctly-computed Z formula for this test. | true — p̂ appears in the *numerator* (p̂−p₀); it's only the *SE* (denominator) that must use p₀, not p̂ | — |
| R16 | recall | short-answer | −0.1 | Fill in the blank: this test's Z statistic is Z = (p̂ − p₀) / ____. | √(p₀(1−p₀)/n) | — |
| A3 | apply | numeric | 1.0 | n=500, 275 successes (p̂=0.55), H₀:p=0.5, α=0.05. `[verified: SE≈0.0224, Z≈2.236]` | SE=√(0.5·0.5/500)≈0.0224; Z=(0.55−0.5)/0.0224≈2.24; \|2.24\|>1.96, **reject** | — |
| A4 | apply | numeric | 1.1 | n=100, 8 successes (p̂=0.08), H₀:p=0.05, α=0.05. `[verified: SE≈0.0218, Z≈1.376]` | SE=√(0.05·0.95/100)≈0.0218; Z=(0.08−0.05)/0.0218≈1.38; \|1.38\|<1.96, **fail to reject** | — |
| A5 | apply | short-answer | 1.2 | For a test of H₀:p=0.5 with n=30, check whether the np₀≥10 and n(1−p₀)≥10 conditions are satisfied. | np₀=30(0.5)=15≥10 ✓; n(1−p₀)=30(0.5)=15≥10 ✓ — both satisfied, so the Normal approximation should be reasonably trustworthy here | — |
| A6 | apply | short-answer | 1.3 | For a test of H₀:p=0.03 with n=200, check whether np₀≥10 is satisfied, and comment on the validity of using this test here. | np₀=200(0.03)=6, which is <10 — the validity condition is **not** satisfied, so the Normal approximation may be unreliable here; a much larger n (at least ~334) or an exact method would be more appropriate | uses this test anyway despite np₀<10, without flagging the validity concern → `one-sample-proportions-z-test` |
| A7 | apply | numeric | 1.4 | n=1000, 560 successes (p̂=0.56), H₀:p=0.5, α=0.01 (critical ≈2.576). `[verified: SE≈0.0158, Z≈3.795]` | SE=√(0.5·0.5/1000)≈0.0158; Z=(0.56−0.5)/0.0158≈3.80; \|3.80\|>2.576, **reject**, even at this strict α | — |
| E3 | explain | short-answer | 1.9 | Explain, in terms of the Binomial-to-Normal approximation, why this test breaks down for extreme p₀ values (very close to 0 or 1) at moderate n. | the Binomial distribution becomes highly skewed as p₀ approaches 0 or 1, and the Normal approximation is poorest exactly when the distribution is skewed rather than roughly symmetric; the np₀≥10 and n(1−p₀)≥10 checks exist specifically to guard against using a symmetric Normal curve to approximate a badly asymmetric Binomial shape *(required: the explicit skewness mechanism)* | — |
| E4 | explain | short-answer | 1.95 | Explain why this test's structure (a Z formula, a known reference distribution, a rejection region) is fundamentally the same template as the one-sample Z-test for means, despite testing a different kind of parameter. | both tests instantiate the identical general pattern: compute (statistic − hypothesized value)/SE-under-H₀, and compare against a standard Normal reference distribution; only the specific statistic (p̂ vs. X̄) and its SE formula differ — the underlying test-statistic/rejection-region logic from Cluster 10 is unchanged *(required: explicitly names the shared template)* | — |
| E5 | explain | short-answer | 2.0 | Explain why n needed to satisfy the validity condition depends heavily on how close p₀ is to 0.5 versus how close it is to an extreme value. | np₀≥10 and n(1−p₀)≥10 are both easiest to satisfy simultaneously when p₀ is near 0.5 (both terms comparably sized); as p₀ moves toward an extreme, one of the two conditions becomes the binding constraint and requires an increasingly large n to satisfy, since the smaller of p₀ and (1−p₀) shrinks *(required: names p₀ near 0.5 as the "easiest" case and explains why extremes need larger n)* | — |
| E6 | explain | short-answer | 2.05 | Explain why the "use p₀ in the SE" rule specifically prevents a subtle inconsistency that "use p̂ instead" would introduce into the test's logic. | the entire test asks "how surprising is this data, assuming H₀ (p=p₀) is true?" — every piece of the calculation, including how much variability to *expect*, should be computed under that same assumption; using p̂ in the SE would mean computing "how surprising is the data under H₀" while simultaneously measuring variability as if the *observed* value were the truth, an internally inconsistent mixture of assumed and observed information *(required: the explicit internal-consistency framing)* | — |
| T2 | transfer | short-answer | 2.3 | A social media platform tests whether its true "like rate" differs from an industry-benchmark 4%, using n=150 posts. Before running the test, evaluate whether the sample size is adequate, and explain the consequence if it isn't. | np₀=150(0.04)=6 <10 — the validity condition fails, meaning the Normal approximation used by this test may not be trustworthy at this n; running the test anyway risks a Z statistic (and resulting p-value) that doesn't actually reflect the claimed α, since the assumed Normal reference distribution may poorly approximate the true (still quite skewed) sampling behavior of p̂ at this n and p₀ *(required: computes np₀ explicitly and states the specific consequence of proceeding anyway)* | — |
| T3 | transfer | numeric | 2.4 | A quality control team believes a machine's defect rate is 1% (p₀=0.01). To validly apply this test (np₀≥10), what is the minimum n required? Given that minimum n, is n(1−p₀)≥10 automatically satisfied too? `[verified: n≥1000; n(1−p₀)=990≥10, yes]` | np₀≥10 ⟹ n≥10/0.01=1000; at n=1000, n(1−p₀)=1000(0.99)=990, which is ≫10, so yes, that condition is automatically satisfied — for small p₀, np₀≥10 is always the *binding* (harder to satisfy) constraint, exactly as E5 argued *(required: computes both the minimum n and confirms the second condition, connecting back to E5's asymmetry argument)* | assumes both validity conditions require separately solving for a minimum n, missing that for small p₀ only one of the two conditions is ever the binding constraint → `one-sample-proportions-z-test` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.4.*

---

## Two Sample Z-Test (`two-sample-z-test`)
*Prereq: One Sample Z-Test · ancestors 28 · b₀ = 1.18*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.18 | State the two-sample Z statistic, and explain why the combined SE is √(σ₁²/n₁+σ₂²/n₂) rather than a sum or average of the two individual SEs. | Z=((X̄₁−X̄₂)−Δ₀)/√(σ₁²/n₁+σ₂²/n₂); the *variances* add for independent samples (Var(X̄₁−X̄₂)=Var(X̄₁)+Var(X̄₂)), not the standard deviations — the SE is the square root of that sum *(required: names variances, not SDs, as what adds)* | — |
| R2 | recall | mcq | 0.48 | This test assumes the two samples are: | independent | assumes it works for paired/matched data too, missing the setup `paired-t-test` exists to handle instead → `two-sample-z-test` |
| A1 | apply | numeric | 0.98 | n₁=50,x̄₁=100,σ₁=10 (known); n₂=40,x̄₂=95,σ₂=12 (known). Test H₀:μ₁=μ₂ at α=0.05. `[verified: SE≈2.366, Z≈2.113]` | SE=√(100/50+144/40)≈2.366; Z=(100−95)/2.366≈2.11; \|2.11\|>1.96, **reject** | — |
| E1 | explain | derivation | 1.68 | Derive Var(X̄₁−X̄₂)=Var(X̄₁)+Var(X̄₂) for independent samples, from Var(X−Y)=Var(X)+Var(Y)−2Cov(X,Y). | for independent X̄₁,X̄₂, Cov(X̄₁,X̄₂)=0 (an ancestor fact from `covariance`), leaving Var(X̄₁−X̄₂)=Var(X̄₁)+Var(X̄₂) directly *(required: the Cov=0 step named explicitly)* | — |
| T1 | transfer | short-answer | 2.18 | Two independent clinical sites measure the same drug's effect. Why is independence essential here, and what would go wrong if some patients were accidentally counted in both samples? | double-counted patients would introduce a nonzero Cov(X̄₁,X̄₂) term that E1's formula doesn't account for, invalidating the SE and hence the test's error-rate guarantees *(required: names the unaccounted Cov term specifically)* | assumes double-counting only affects sample size, not the validity of the independence-based SE formula → `two-sample-z-test` |
| R3 | recall | short-answer | −1.4 | State the null and alternative hypotheses this test typically addresses. | H₀: μ₁=μ₂ (or μ₁−μ₂=Δ₀); H₁: μ₁≠μ₂ (or, one-tailed, μ₁>μ₂ or μ₁<μ₂) | — |
| R4 | recall | mcq | −1.3 | This test requires which of the following about σ₁ and σ₂? | both must be known | requires "σ₁=σ₂, and their common value is unknown," conflating this test's known-σ requirement with the pooled-variance assumption that actually belongs to `two-sample-t-test` → `two-sample-z-test` |
| R5 | recall | short-answer | −1.2 | True or false: the numerator of this test's Z statistic is (X̄₁ − X̄₂), possibly minus a hypothesized difference Δ₀. | true | — |
| R6 | recall | short-answer | −1.1 | Fill in the blank: the two-sample Z-test compares two population ____, using two independent samples. | means | — |
| R7 | recall | mcq | −1.0 | Which scenario is a valid application of this test? | comparing average scores of two entirely separate groups of students at two different schools | comparing before-and-after scores for the *same* students, which violates the independence assumption and calls for `paired-t-test` instead → `two-sample-z-test` |
| R8 | recall | short-answer | −0.9 | Define Δ₀ as used in a generalized H₀: μ₁−μ₂=Δ₀. | the hypothesized value of the true difference between the two population means (commonly 0, for "no difference") | — |
| R9 | recall | mcq | −0.8 | If both σ₁ and σ₂ are in fact unknown, which test should be used instead of this one? | the two-sample t-test | claims "this test can simply be run anyway, plugging in s₁ and s₂ for σ₁ and σ₂ with no other change," which is precisely the same unadjusted-substitution error `one-sample-z-test`'s R2 warns against → `two-sample-z-test` |
| R10 | recall | short-answer | −0.7 | True or false: this test's combined SE formula √(σ₁²/n₁+σ₂²/n₂) requires n₁ and n₂ to be equal. | false — n₁ and n₂ can differ; the formula works for any positive sample sizes in each group | — |
| R11 | recall | short-answer | −0.6 | Fill in the blank: for independent samples, Var(X̄₁−X̄₂) = Var(X̄₁) + ____. | Var(X̄₂) | — |
| R12 | recall | mcq | −0.5 | Which of these correctly states this test's full Z formula? | Z = ((X̄₁−X̄₂)−Δ₀) / √(σ₁²/n₁+σ₂²/n₂) | writes the denominator as σ₁/√n₁+σ₂/√n₂ (summing the two SEs directly rather than combining variances under a square root) → `two-sample-z-test` |
| R13 | recall | short-answer | −0.4 | State, in words, why the combined variance formula sums σ₁²/n₁ and σ₂²/n₂ rather than averaging them. | Var(X̄₁−X̄₂) is a sum of two independent variances (Var(X̄₁)+Var(X̄₂)), following directly from Var(X−Y)=Var(X)+Var(Y) under independence — there's no reason for this sum to be an average of the two component variances | — |
| R14 | recall | mcq | −0.3 | This test's Type I error rate is exactly α when: | both underlying populations are exactly Normal | is exactly α "whenever n₁+n₂≥30," misapplying a rough CLT rule of thumb to a claim about *exact* (not approximate) validity → `two-sample-z-test` |
| R15 | recall | short-answer | −0.2 | True or false: this test can be one-tailed or two-tailed, just like the one-sample Z-test. | true — the same one-/two-tailed rejection-region logic from Cluster 10 applies here unchanged | — |
| R16 | recall | short-answer | −0.1 | Fill in the blank: this test's prerequisite includes the one-sample Z-test because it reuses its ____ machinery, generalized to two groups. | test-statistic/rejection-region (Z-test) | — |
| A3 | apply | numeric | 1.05 | n₁=25,x̄₁=60,σ₁=8 (known); n₂=25,x̄₂=55,σ₂=6 (known). Test H₀:μ₁=μ₂ at α=0.05. `[verified: SE=2, Z=2.5]` | SE=√(64/25+36/25)=√4=2; Z=(60−55)/2=2.5; \|2.5\|>1.96, **reject** | — |
| A4 | apply | numeric | 1.15 | n₁=100,x̄₁=50,σ₁=10 (known); n₂=64,x̄₂=48,σ₂=8 (known). Test H₀:μ₁=μ₂ at α=0.05. `[verified: SE≈1.414, Z≈1.414]` | σ₁²/n₁=100/100=1; σ₂²/n₂=64/64=1; SE=√(1+1)=√2≈1.414; Z=(50−48)/1.414≈1.41; \|1.41\|<1.96, **fail to reject** | — |
| A5 | apply | short-answer | 1.25 | For a test H₀: μ₁−μ₂=5 (a specific hypothesized gap, not 0), with X̄₁−X̄₂=8.4 and SE=2.1, compute Z. `[verified: Z≈1.619]` | Z=(8.4−5)/2.1≈1.62 — the same formula, just with Δ₀=5 substituted instead of the more common Δ₀=0 | — |
| A6 | apply | short-answer | 1.35 | Two groups have SE contributions σ₁²/n₁=9 and σ₂²/n₂=16. Compute the combined SE. `[verified: 5]` | SE=√(9+16)=√25=5 | — |
| A7 | apply | numeric | 1.45 | n₁=50,x̄₁=200,σ₁=15 (known); n₂=50,x̄₂=207,σ₂=15 (known). Test H₀:μ₁=μ₂ at α=0.01 (critical≈2.576). `[verified: SE≈3, Z≈2.333]` | SE=√(225/50+225/50)=√9=3; Z=(200−207)/3≈−2.33; \|−2.33\|<2.576, **fail to reject** at this strict α | — |
| E3 | explain | short-answer | 1.85 | Explain why this test's combined SE formula does *not* simplify to a single "pooled σ" the way `two-sample-t-test`'s pooled variant does. | this test allows σ₁≠σ₂ from the start — it never assumes a single common variance, so there's no pooling step; each population's own variance contributes its own term (σᵢ²/nᵢ) to the sum, unlike the pooled t-test which specifically assumes σ₁=σ₂ and blends s₁² and s₂² into one shared estimate *(required: explicitly contrasts with the pooled-variance approach)* | — |
| E4 | explain | short-answer | 1.9 | Explain why testing H₀:μ₁=μ₂ is mathematically identical to testing H₀: (μ₁−μ₂)=0, and why this framing generalizes naturally to testing any specific hypothesized gap Δ₀. | μ₁=μ₂ can be rewritten as μ₁−μ₂=0 by simple algebra, so the "difference equals a hypothesized constant" framing (with Δ₀=0 as the default) is just the general form of the "equal means" test; this framing costs nothing extra and immediately supports testing whether the gap equals any specific value, not just 0 *(required: the explicit rewriting, and the generalization point)* | — |
| E5 | explain | short-answer | 1.95 | Explain why this test is described as "requiring independence" rather than "requiring identical distributions" for the two samples. | the derivation only relies on Cov(X̄₁,X̄₂)=0 (E1's derivation step) — nothing in that derivation requires the two populations to share the same distribution, mean, or variance; two samples from genuinely different populations, with different means and variances, are perfectly valid here as long as they were drawn independently of each other *(required: explicitly distinguishes "independent" from "identically distributed")* | — |
| E6 | explain | short-answer | 2.0 | Explain, in terms of the Var(X−Y) formula, exactly which term becomes 0 under independence, and why that term would otherwise complicate this test's SE. | the general formula Var(X̄₁−X̄₂)=Var(X̄₁)+Var(X̄₂)−2Cov(X̄₁,X̄₂) has a covariance term that vanishes only under independence; without independence, that term could be positive or negative, either shrinking or inflating the true variance of the difference relative to what the simple sum-of-variances formula assumes, invalidating the SE used in the Z formula *(required: names the −2Cov term specifically and both possible directions of its effect)* | — |
| T2 | transfer | short-answer | 2.25 | Two A/B test variants are shown to overlapping (not disjoint) sets of users, with some users seeing both variants across sessions. Explain why this violates this test's core requirement, and what statistical consequence follows. | overlapping users across the two "independent" samples introduces a nonzero covariance between X̄₁ and X̄₂ (since the same user's behavior partly determines both group's averages), exactly the E1/E6 violation — the resulting SE formula (which assumes Cov=0) would be wrong, making the computed Z statistic and any resulting p-value or decision unreliable, in an unpredictable direction depending on the sign of the true covariance *(required: explicitly names the introduced covariance and that its effect on the SE is not simply "conservative" or "anti-conservative" in a fixed direction without more information)* | — |
| T3 | transfer | numeric | 2.35 | Two independent survey waves give: n₁=200,x̄₁=72,σ₁=18 (known); n₂=150,x̄₂=69,σ₂=15 (known). Test H₀:μ₁=μ₂ at α=0.05, and separately compute what the (incorrect) SE would be if a researcher mistakenly used σ₁ alone (ignoring σ₂ and n₂ entirely). `[verified: correct SE≈1.766, Z≈1.698, fail to reject; incorrect SE≈1.273, Z≈2.357]` | correct SE=√(324/200+225/150)=√(1.62+1.5)=√3.12≈1.766; Z=(72−69)/1.766≈1.70; \|1.70\|<1.96, **fail to reject**; the incorrect SE using σ₁/√n₁ alone ≈18/√200≈1.273 is smaller than the correct combined SE, which would have produced an inflated, incorrect Z≈2.36 and a wrongly rejected H₀ *(required: computes both the correct and incorrect SE, and notes the incorrect one understates the true uncertainty)* | computes the SE using only one group's variance, ignoring the second sample's contribution entirely → `two-sample-z-test` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.35.*

---

## Two Sample T-Test (`two-sample-t-test`)
*Prereq: One Sample T-Test, Two Sample Z-Test · ancestors 40 · b₀ = 1.36*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.36 | Name the two main variants of the two-sample t-test and the assumption distinguishing them. | pooled (assumes σ₁=σ₂, combines s₁²,s₂² into one estimate, df=n₁+n₂−2) vs Welch's (does not assume equal variances, uses an approximate df formula) | — |
| R2 | recall | mcq | 0.66 | Welch's t-test is generally the safer *default* choice because: | it doesn't require assuming equal variances, often unrealistic in practice | claims Welch's "always gives a smaller p-value" — not a general fact, and not the actual reason it's recommended → `two-sample-t-test` |
| A1 | apply | numeric | 1.16 | Two samples, n₁=n₂=20, s₁²=s₂²=25, x̄₁=80, x̄₂=75. Compute the pooled T statistic. `[verified: pooled var=25, SE≈1.581, T≈3.162]` | pooled variance = (19·25+19·25)/38 = 25 (equal here, since the sample variances happen to match); SE=√(25/20+25/20)≈1.581; T=(80−75)/1.581≈3.16 | — |
| E1 | explain | short-answer | 1.86 | Why is pooling invalid — or at least misleading — when the two true variances genuinely differ? | pooling implicitly assumes a single common σ²; if σ₁≠σ₂ in truth, the pooled estimate is a biased blend that can distort the test's actual error rate, sometimes substantially *(required)* | — |
| T1 | transfer | short-answer | 2.36 | A new manufacturing process (likely to have *different variability*, not just a different mean, than the old one) is compared to the old one. Why is assuming equal variances risky here, and which test variant is safer? | a new process plausibly has a different σ as well as a different μ, making the pooled test's equal-variance assumption risky by default; Welch's test, which doesn't require that assumption, is the safer choice *(required: names the plausible-variance-difference reasoning specifically, not just "Welch's is generally safer")* | defaults to the pooled test out of habit without considering whether equal variances is plausible here → `two-sample-t-test` |
| R3 | recall | short-answer | −1.4 | State the degrees of freedom for the pooled two-sample t-test. | df = n₁ + n₂ − 2 | — |
| R4 | recall | mcq | −1.3 | Welch's t-test's degrees of freedom are: | given by an approximate formula (the Welch–Satterthwaite equation), and generally not a whole number | given as n₁+n₂−2, the pooled test's formula, mistakenly applied to Welch's variant too → `two-sample-t-test` |
| R5 | recall | short-answer | −1.2 | True or false: both the pooled and Welch's two-sample t-tests require the two populations' *means* to be tested via H₀:μ₁=μ₂ (or a hypothesized gap). | true — both variants share the same hypotheses; they differ only in how the SE/df is computed | — |
| R6 | recall | short-answer | −1.1 | Fill in the blank: the pooled t-test's shared variance estimate is called the ____ variance. | pooled | — |
| R7 | recall | mcq | −1.0 | The pooled variance formula combines s₁² and s₂² by: | a weighted average, weighted by each group's degrees of freedom (nᵢ−1) | combines them by "a simple unweighted average of s₁² and s₂², regardless of sample size," missing that the pooling formula weights by each group's own degrees of freedom → `two-sample-t-test` |
| R8 | recall | short-answer | −0.9 | Define, in one sentence, when the pooled two-sample t-test is theoretically the more powerful choice over Welch's. | when σ₁ and σ₂ truly are equal, pooling both samples' information into one shared variance estimate is more efficient (higher power) than estimating the two variances separately, as Welch's test does | — |
| R9 | recall | mcq | −0.8 | A common informal check before choosing pooled vs. Welch's is: | comparing the two sample variances (e.g. via a rule of thumb like "ratio under 2") for rough similarity | comparing the two sample *means* for similarity, which is irrelevant to the equal-variance decision and in fact is the very question the test is trying to answer → `two-sample-t-test` |
| R10 | recall | short-answer | −0.7 | True or false: modern statistical software often defaults to Welch's test rather than the pooled test. | true — because Welch's doesn't require the potentially-unrealistic equal-variance assumption, many tools (e.g. R's default `t.test`) use it as the default | — |
| R11 | recall | short-answer | −0.6 | Fill in the blank: the pooled t-test's SE formula is √(s²ₚ(1/n₁ + 1/n₂)), where s²ₚ is the ____ variance. | pooled | — |
| R12 | recall | mcq | −0.5 | Which of these correctly states the pooled variance formula? | s²ₚ = [(n₁−1)s₁² + (n₂−1)s₂²] / (n₁+n₂−2) | states s²ₚ = (s₁²+s₂²)/2, an unweighted average that ignores each group's sample size entirely → `two-sample-t-test` |
| R13 | recall | short-answer | −0.4 | State the practical tradeoff between choosing pooled vs. Welch's when you're genuinely unsure whether variances are equal. | pooled offers slightly more power *if* variances truly are equal, but risks a distorted error rate if they're not; Welch's is robust to unequal variances at a small power cost when they are equal — making Welch's the generally safer default under uncertainty | — |
| R14 | recall | mcq | −0.3 | If s₁²=s₂² exactly (identical sample variances), the pooled and Welch's test statistics will: | be numerically identical (though their degrees of freedom formulas may still differ slightly) | be "always numerically different regardless of the sample variances," which is false when s₁²=s₂² exactly, as in `two-sample-z-test`'s A1-style example → `two-sample-t-test` |
| R15 | recall | short-answer | −0.2 | True or false: this test requires knowing σ₁ and σ₂, like the two-sample Z-test does. | false — this test exists specifically because σ₁ and σ₂ are *unknown*, estimated via s₁ and s₂ | — |
| R16 | recall | short-answer | −0.1 | Fill in the blank: the two-sample t-test is the two-sample analogue of the one-sample ____-test. | t (one-sample t-test) | — |
| A3 | apply | numeric | 1.05 | Two samples, n₁=n₂=15, s₁²=36, s₂²=64, x̄₁=90, x̄₂=82. Compute the pooled variance. `[verified: 50]` | s²ₚ=[(14)(36)+(14)(64)]/28=(504+896)/28=1400/28=50 | — |
| A4 | apply | numeric | 1.15 | Using A3's pooled variance (50) with n₁=n₂=15, compute the pooled T statistic. `[verified: SE≈2.582, T≈3.098]` | SE=√(50/15+50/15)=√(6.667)≈2.582; T=(90−82)/2.582≈3.10 | — |
| A5 | apply | short-answer | 1.25 | Two samples have s₁²=100 and s₂²=4 (a 25:1 ratio). Would the common "ratio under 2" rule of thumb favor pooled or Welch's here? | Welch's — a variance ratio this extreme (25:1) badly violates the rough equal-variance rule of thumb, so pooling would be inappropriate | — |
| A6 | apply | numeric | 1.35 | Two samples, n₁=10, n₂=20, s₁²=s₂²=9. Compute the pooled variance, and note why it equals the common value even though sample sizes differ. `[verified: 9]` | s²ₚ=[(9)(9)+(19)(9)]/28=(81+171)/28=252/28=9 — since s₁²=s₂²=9 exactly, any weighted average of the two still equals 9, regardless of the weights | — |
| A7 | apply | short-answer | 1.45 | A researcher has n₁=8, n₂=9, and finds s₁²=15, s₂²=60 (a 4:1 ratio). Which test is the safer default choice, and why? | Welch's — a 4:1 variance ratio is well beyond a "roughly equal" range, especially with such small, unequal sample sizes, making the pooled test's equal-variance assumption risky here | — |
| E3 | explain | short-answer | 1.9 | Explain why the pooled test's degrees of freedom (n₁+n₂−2) is larger than either individual sample's own (nᵢ−1), and why this matters for the resulting critical value. | pooling combines information from both samples into a single variance estimate, and the combined degrees of freedom reflect the total number of independent deviations available across both groups (n₁−1 from group 1, n₂−1 from group 2); a larger df means a t-distribution closer to standard Normal, with a smaller (less conservative) critical value than either individual sample's own t-distribution would give *(required: the explicit "more combined data, closer-to-Normal, smaller critical value" chain)* | — |
| E4 | explain | short-answer | 1.95 | Explain why Welch's test's degrees-of-freedom formula is not a simple whole number like n₁+n₂−2, and what that reflects about the underlying uncertainty. | Welch's approach doesn't assume a single shared variance, so it can't cleanly combine the two samples' "degrees of information" the way pooling does; the Welch–Satterthwaite approximation instead computes an *effective* degrees of freedom that interpolates between the two samples' own dfs, weighted by how much each contributes to the total variance estimate — the resulting fractional value reflects a genuinely more complex uncertainty structure than the pooled case *(required: explains why the formula can't simplify to a whole number)* | — |
| E5 | explain | short-answer | 2.0 | Explain why choosing the pooled test when variances are genuinely unequal can distort the test's actual Type I error rate, using the SE mechanism. | the pooled SE blends s₁² and s₂² into one number as if a single common variance existed; if the true variances differ substantially, this blended estimate can be systematically too small or too large for the *actual* combined variability, especially when paired with unequal sample sizes — a distorted SE feeds directly into a distorted (no longer exactly α) Type I error rate *(required: connects the blended, potentially-wrong SE to the actual error-rate distortion)* | — |
| E6 | explain | short-answer | 2.05 | Explain why the choice between pooled and Welch's is sometimes framed as "why take a risk you don't need to take" in favor of Welch's, even when the true variances might actually be equal. | when variances truly are equal, Welch's test loses only a small amount of power compared to pooled; but when variances are actually unequal and pooled is used anyway, the error-rate distortion (E5) can be substantial — the asymmetry of the downside (small power loss vs. potentially serious validity problems) is exactly why many practitioners and software defaults favor Welch's unless equal variances are specifically justified *(required: the explicit asymmetric-risk argument, not just "Welch's is generally safer")* | — |
| T2 | transfer | short-answer | 2.4 | A researcher runs both a pooled and a Welch's t-test on the same dataset, gets p=0.03 from pooled and p=0.07 from Welch's, and reports only the pooled result because it's "significant." Critique this practice. | this is a form of test-selection p-hacking — choosing *after seeing both results* which test's conclusion is more favorable, rather than deciding in advance (based on the actual equal-variance evidence) which test is appropriate; it mirrors `rejection-region`'s post-hoc-threshold-picking problem and `hypothesis-test`'s test-switching problem, now applied to choosing between two valid-looking test variants *(required: explicitly frames this as a post-hoc, results-driven choice, connecting to the earlier timing principle)* | — |
| T3 | transfer | numeric | 2.5 | Two lab groups report n₁=12, s₁²=25, x̄₁=110; n₂=12, s₂²=100, x̄₂=100 (a 4:1 variance ratio). Using the pooled formula anyway, compute s²ₚ and the pooled T, and separately note the specific reason this pooled result should be treated with caution. `[verified: s²ₚ=62.5, SE≈3.227, T≈3.099]` | s²ₚ=[(11)(25)+(11)(100)]/22=(275+1100)/22=1375/22=62.5; SE=√(62.5/12+62.5/12)=√10.417≈3.227; T=(110−100)/3.227≈3.10 — but with a 4:1 variance ratio, this pooled estimate blends a much noisier group 2 with a much tighter group 1 into one number that doesn't accurately represent either, so the resulting T (and any p-value from it) should be treated with real suspicion; Welch's test would be the more defensible choice here *(required: computes the pooled result correctly, but explicitly flags the equal-variance violation as a reason for caution)* | reports the pooled T-statistic as straightforwardly trustworthy without flagging the variance-ratio violation → `two-sample-t-test` |

*Coverage: 16/6/5/3 — 30 items, −1.4…2.5.*

---

## Paired T-Test (`paired-t-test`)
*Prereq: Two Sample T-Test · ancestors 41 · b₀ = 1.37*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.37 | Describe the paired t-test's key trick, in terms of what test it reduces to. | compute differences dᵢ=Xᵢ−Yᵢ for each matched pair, then run a **one-sample** t-test on the dᵢ against H₀: mean difference = 0 | — |
| R2 | recall | mcq | 0.67 | Pairing is most beneficial (vs. an unpaired two-sample test) when: | the pairs are highly correlated/similar to each other within a pair | assumes pairing helps regardless of how related the pairs are, missing the actual mechanism → `paired-t-test` |
| A1 | apply | numeric | 1.17 | 8 subjects' before/after differences: 3,5,2,4,6,1,3,4. Test H₀: mean difference=0 at α=0.05 (df=7, critical t≈2.365). `[verified: mean=3.5, SE≈0.567, T≈6.17]` | mean=3.5, s≈1.604, SE≈0.567, T≈6.17; \|6.17\|≫2.365, **reject** — a strong, unambiguous treatment effect | — |
| E1 | explain | short-answer | 1.87 | Why does pairing increase power compared to an unpaired test on the same data? | taking differences cancels out each subject's own baseline level (some people are just naturally healthier/taller/etc.), removing *between-subject* variability from the comparison and leaving only the within-subject effect plus noise — directly the within/between decomposition from `law-of-total-variance`, with the between-term deliberately eliminated *(required: the explicit connection to that decomposition)* | — |
| T1 | transfer | short-answer | 2.37 | Why would it be a mistake to analyze before/after data on the *same* subjects with an unpaired two-sample t-test instead? | before/after measurements on the same people are inherently correlated, violating the unpaired test's independence assumption; using the wrong test typically makes it too *conservative* (understates significance), since it ignores the correlation pairing exploits *(required: names independence as the violated assumption, and the direction of the resulting error)* | uses the unpaired test anyway, treating it as merely "less efficient" rather than assumption-violating → `paired-t-test` |

*Coverage: 4 items, 0.37…2.37.*

---

## Chi Square Test of Independence (`chi-square-test-of-independence`)
*Prereq: Chi Square Distribution, Hypothesis Test · ancestors 35 · b₀ = 1.29*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.29 | State the test statistic and its degrees of freedom for an r×c contingency table. | Σ(O−E)²/E ~ χ²_{(r−1)(c−1)} under H₀ | — |
| R2 | recall | mcq | 0.59 | The "expected count" for a cell under H₀ (independence) is: | (row total × column total) / grand total | picks "the average of all cells," missing the marginal-product structure entirely → `chi-square-test-of-independence` |
| A1 | apply | numeric | 1.09 | 2×2 table [[30,10],[20,40]] (rows: exposed/not; columns: disease/no disease). Compute the chi-square statistic. `[verified: 16.667]` | row totals 40,60; col totals 50,50; expected cells 20,20,30,30; χ²=(30−20)²/20+(10−20)²/20+(20−30)²/30+(40−30)²/30 ≈16.67 | — |
| E1 | explain | short-answer | 1.79 | Connect this test's "expected under independence" computation directly to `joint-distribution`'s E1. | this test IS `joint-distribution`'s earlier row/column-sum independence check — comparing observed cell counts against the product of the marginals — now formalized with a known null distribution (χ²) attached, turning an ad hoc comparison into a proper hypothesis test *(required: names that earlier item explicitly)* | — |
| T1 | transfer | short-answer | 2.29 | Testing whether smoking status and disease occurrence are associated: why does a *large* chi-square statistic count as evidence against independence? | large (O−E)² terms mean observed counts deviate substantially from what independence would predict in every cell; since independence would make the expected and observed counts close on average, a large accumulated deviation is evidence the independence assumption is false *(required)* | — |

*Coverage: 4 items, 0.29…2.29.*

---

## Chi Square Goodness of Fit Test (`chi-square-goodness-of-fit-test`)
*Prereq: Chi Square Test of Independence · ancestors 36 · b₀ = 1.31*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.31 | State the test statistic (same form as the independence test) and its degrees of freedom, contrasting with that test's df. | Σ(O−E)²/E ~ χ²_{k−1} for k categories — df is k−1, *not* (r−1)(c−1) as in the independence test | — |
| R2 | recall | mcq | 0.61 | Goodness-of-fit tests whether: | a single variable's distribution matches a specified/hypothesized distribution | describes it as testing whether two variables are independent — confusing it with `chi-square-test-of-independence` → `chi-square-goodness-of-fit-test` |
| A1 | apply | numeric | 1.11 | A die is rolled 60 times: observed counts 8,12,7,15,9,9. Test H₀: fair die (expected 10 each) at α=0.05 (df=5, critical≈11.07). `[verified: χ²=4.4]` | χ²=Σ(O−10)²/10 = 4.4; 4.4 < 11.07, **fail to reject** — no evidence of an unfair die | — |
| E1 | explain | short-answer | 1.81 | Why does this test use df=k−1 while the independence test uses (r−1)(c−1)? | here the hypothesized proportions are *fully specified* in advance (e.g. 1/6 each), using up only one constraint (total count fixed); the independence test instead estimates *two* sets of marginal totals from the data itself, costing more degrees of freedom *(required: the "fully specified vs. estimated from data" contrast)* | — |
| T1 | transfer | short-answer | 2.31 | To test whether continuous measurements (e.g. inter-arrival times) follow a specific named distribution (e.g. Exponential), you must first bin the data into categories. Why can the choice of bins affect the conclusion? | different binning choices redistribute how deviations from the hypothesized distribution get aggregated into (O−E)² terms — a coarse binning can average away a real local misfit, while a very fine binning can create small-expected-count cells that make the χ² approximation unreliable, so the result is not fully bin-choice-independent *(required: names a concrete mechanism, not just "binning is subjective")* | — |

*Coverage: 4 items, 0.31…2.31.*

---

## Fisher's Exact Test (`fischers-exact-test`)
*Prereq: Chi Square Test of Independence, Hypergeometric Distribution · ancestors 37 · b₀ = 1.32*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.32 | What makes this test "exact," and when is it preferred over chi-square? | no large-sample/Normal approximation is used at all; preferred for small samples, especially when expected counts fall below chi-square's validity threshold of 5 | — |
| R2 | recall | mcq | 0.62 | Fisher's Exact Test computes exact p-values using which distribution, conditioning on the observed marginal totals? | Hypergeometric | picks Chi-square, missing that this test's entire point is to avoid the chi-square approximation → `fischers-exact-test` |
| A1 | apply | derivation | 1.12 | For the small 2×2 table [[3,1],[1,4]] (row totals 4,5; column totals 4,5; N=9), set up the exact probability of observing top-left=3 given the fixed marginals. `[verified: 20/126≈0.159]` | P(top-left=3) = C(4,3)·C(5,1)/C(9,4) = 4·5/126 ≈ 0.159 — a direct hypergeometric pmf evaluation | uses the chi-square approximation instead, despite the table being far too small for it to be trustworthy → `chi-square-test-of-independence` |
| E1 | explain | short-answer | 1.82 | Explain why conditioning on the fixed marginal totals makes this exactly the `hypergeometric-distribution` setup from Cluster 3. | fixing both row and column totals is equivalent to imagining all N units shuffled together, with a predetermined number belonging to each row-category, then "dealt" into the column-categories according to the fixed column totals — exactly drawing without replacement from a population with two types *(required: the explicit shuffling/dealing analogy)* | — |
| T1 | transfer | short-answer | 2.32 | A rare-disease study with only 9 total patients wants to test whether a treatment is associated with recovery. Why is Fisher's Exact Test the appropriate choice here rather than chi-square? | with only 9 patients, expected cell counts are almost certainly below chi-square's validity threshold of 5, making its Normal-based approximation unreliable; Fisher's computes an exact probability with no such approximation, remaining valid at any sample size *(required: the explicit small-expected-count justification)* | — |

*Coverage: 4 items, 0.32…2.32.*

---

## Wilcoxon Rank Sum Test (`wilcoxon-rank-sum-test`)
*Prereq: Order Statistics, Hypothesis Test · ancestors 26 · b₀ = 1.15*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.15 | Describe the basic idea of the Wilcoxon rank-sum test. | rank *all* observations from both groups combined, then compare the sum of ranks between the two groups | — |
| R2 | recall | mcq | 0.45 | The main advantage of Wilcoxon over the two-sample t-test is: | it doesn't require assuming the data follows a Normal (or any specific) distribution | claims it's "always more powerful" — an honest tradeoff is being missed; it typically sacrifices some power on genuinely Normal data in exchange for validity elsewhere → `wilcoxon-rank-sum-test` |
| A1 | apply | short-answer | 0.95 | Group A: 5,7,9; Group B: 2,4,6. Rank all 6 combined, and find each group's rank sum. `[verified: A=14, B=7]` | combined sorted: 2,4,5,6,7,9 → ranks 1–6; Group A gets ranks 3,5,6 (sum 14); Group B gets ranks 1,2,4 (sum 7) | — |
| E1 | explain | short-answer | 1.65 | Why does being "non-parametric" specifically help with skewed data, like the income/reaction-time examples from `normal-distribution`'s earlier T1? | ranks are invariant to any monotonic transformation of the data (e.g. taking a log doesn't change the ranks at all), so the test's conclusion doesn't depend on getting a distributional assumption right — unlike the t-test, which directly assumes approximate Normality of the sampling distribution of the mean *(required: the monotonic-invariance mechanism, not just "it avoids assuming Normality")* | — |
| T1 | transfer | short-answer | 2.15 | Reaction-time data is right-skewed with occasional extreme outliers. Why might a t-test on mean reaction time be misleadingly influenced by outliers, while Wilcoxon is more robust? | an extreme outlier (say 10 seconds against a typical 0.3) massively inflates a mean and its variance-based test statistic; that same outlier, however extreme, can only ever occupy the single largest rank — ranks encode order, not magnitude, so one outlier can't distort the rank-sum any more than a "somewhat large" value would *(required: the "ranks encode order, not magnitude" mechanism)* | — |

*Coverage: 4 items, 0.15…2.15.*

---

## Bootstrapping (`bootstrapping`)
*Prereq: Sampling Distribution · ancestors 22 · b₀ = 1.07*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.07 | Describe the basic bootstrap procedure in one sentence. | resample *with replacement* from the observed data (same size n as the original), repeat many times, computing the statistic of interest each time, to approximate its sampling distribution | — |
| R2 | recall | mcq | 0.37 | The bootstrap's "plug-in principle" treats: | the observed sample as a stand-in for the true population | treats "the true population as known exactly" — missing that the whole method exists because the population is *not* known → `bootstrapping` |
| A1 | apply | short-answer | 0.87 | Original sample: {2,4,4,7,9}. Which are valid bootstrap resamples (same size, with replacement)? (a) {4,4,7,7,9} (b) {2,4,7,9} (c) {4,4,4,4,4} | (a) and (c) are both valid — any combination of the original values, repeats allowed, of size 5; (b) is invalid, having only 4 values | rejects (c) as "too extreme to be valid," not recognizing that any resample of the correct size, however repetitive, is legitimate → `bootstrapping` |
| E1 | explain | short-answer | 1.57 | Why is bootstrapping especially valuable for a statistic like the median, which has no simple closed-form SE formula? | there is no formula analogous to σ/√n for the median's standard error; the bootstrap sidesteps this entirely by simulating the sampling process directly, and the same approach generalizes to *any* statistic computable on a resampled dataset, however complicated *(required: the "no formula needed" payoff stated explicitly)* | — |
| T1 | transfer | short-answer | 2.07 | Why can't you bootstrap by resampling *without* replacement (same size n from a sample of size n)? | resampling without replacement of the same size n would just reproduce a permutation of the exact same n values every time — no new information about sampling variability at all; *with* replacement is essential because it allows genuinely different resamples (some values repeated, others omitted), which is what lets the bootstrap distribution capture any variability in the first place *(required: the "just a permutation, no new information" argument)* | — |

*Coverage: 4 items, 0.07…2.07.*

---

## Cluster 11 misconception index

| Tag | Blame |
|---|---|
| σ-unknown case handled as if σ were known | `one-sample-z-test` |
| t and z critical values assumed identical regardless of n | `one-sample-t-test` |
| p̂ substituted for p₀ inside a proportions-test SE | `one-sample-proportions-z-test` |
| independence assumption of the two-sample test overlooked for paired data | `two-sample-z-test`, `paired-t-test` |
| pooled variance test used despite plausibly unequal variances | `two-sample-t-test` |
| independence-test and goodness-of-fit df formulas swapped | `chi-square-goodness-of-fit-test` |
| chi-square approximation used at sample sizes too small for its validity | `fischers-exact-test` |
| Wilcoxon assumed strictly more powerful than the t-test in general | `wilcoxon-rank-sum-test` |
| bootstrap resampling done without replacement | `bootstrapping` |

**Cluster total: 44 items across 11 concepts.** All numeric claims verified by script, including the
pooled two-sample t statistic, the exact hypergeometric probability underlying Fisher's test, and the
Wilcoxon rank sums by direct computation.

---

# Probability & Statistics: complete

**81 / 81 concepts done.** See [README.md](README.md) for the full cluster index. Total item count
across all 12 files (11 clusters + the `bernoulli-binomial` pilot): **~618 items.**
