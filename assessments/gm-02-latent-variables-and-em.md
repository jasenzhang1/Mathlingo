# Graphical Models Cluster 2 — Latent Variables & EM

Mixture Models and Latent Variables, EM Algorithm, Gaussian Mixture Models, Laplace Approximation (4
concepts). Same format as [foundations-of-probability.md](foundations-of-probability.md).

`em-algorithm`'s A1 draws a direct structural parallel to `k-means-clustering` from the ML sweep (EM
*is* soft K-means, not merely analogous to it), and `laplace-approximation`'s R2 reuses
`fisher-information`'s "sharper peak = more information = lower variance" intuition as an exact identity
rather than a resemblance.

---

## Mixture Models and Latent Variables (`mixture-models-and-latent-variables`)
*Prereq: Marginal Distribution · ancestors 7 · b₀ = 0.54*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.46 | Describe a mixture model. | observed data comes from one of several component distributions, with a latent variable Z indicating which component generated each observation; the observed distribution is the marginal of the joint (Z,X), summed/integrated over Z | — |
| R2 | recall | mcq | −0.16 | The latent variable Z is: | never observed — it must be inferred | claims Z is "directly observed in the data" — contradicting the entire meaning of "latent" → `mixture-models-and-latent-variables` |
| A1 | apply | short-answer | 0.34 | Using `marginal-distribution`'s formula p_X(x)=Σ_z p(x,z), derive the mixture density p(x)=Σₖ πₖfₖ(x). | this is literally `marginal-distribution`'s summing-out-the-other-variable operation applied to Z: p(x)=Σ_z p(x,z)=Σₖ P(Z=k)·p(x\|Z=k)=Σₖ πₖfₖ(x) *(required: the explicit derivation from that earlier formula, not a fresh statement)* | — |
| E1 | explain | short-answer | 1.04 | Why can a mixture of simple unimodal components represent complex, multi-modal distributions? | even though each component is unimodal, their weighted combination can produce arbitrarily complex shapes with multiple "bumps" — similar in spirit to how a Fourier series builds complex functions from simple sine waves *(required: the Fourier-series-style analogy or an equivalent argument)* | — |
| T1 | transfer | short-answer | 1.54 | In customer segmentation, why is the "type" (budget-conscious vs. luxury) latent, and why is inferring it still useful? | a company typically has no direct record of which underlying type each customer belongs to — hence latent; the model's inferred segment membership, though a statistical inference rather than an observed fact, can still guide targeted marketing effectively *(required: names both why Z is latent here and why the inference remains useful)* | — |
| R3 | recall | mcq | −0.41 | In a mixture model with K components, the mixing weights πₖ must satisfy: | πₖ≥0 for all k and Σπₖ=1 | allows the weights to sum to any positive number, not requiring them to sum exactly to 1 → `mixture-models-and-latent-variables` |
| R4 | recall | short-answer | −0.36 | Fill in the blank: the generative process for a mixture model first samples ___, then samples ___. | Z (the component) from the mixing weights π; then X from that component's distribution | — |
| R5 | recall | short-answer | −0.31 | True or false: a mixture model assumes each observation is generated by exactly one component. | True — even though inference gives a soft posterior over components, the generative story assigns each point to one component | — |
| R6 | recall | mcq | −0.26 | The number of components K in a mixture model is: | typically a model choice specified in advance or selected via a criterion like BIC, not learned as a continuous parameter | claims K is estimated the same way π and the component parameters are, via ordinary maximum likelihood alone → `mixture-models-and-latent-variables` |
| R7 | recall | short-answer | −0.21 | Define "label switching" as a mixture-model identifiability issue. | since components are exchangeable, permuting the component labels and their associated parameters leaves the likelihood unchanged, so which numerical label corresponds to which component is not identifiable from the data alone | — |
| R8 | recall | short-answer | −0.11 | Fill in the blank: the mixture density is p(x) = Σₖ ___ · fₖ(x). | πₖ | — |
| R9 | recall | short-answer | −0.06 | True or false: in a mixture model, all K components must be from the same parametric family (e.g. all Gaussian). | False — components can in principle come from different families, though in practice they are usually the same family | — |
| R10 | recall | mcq | −0.01 | The "posterior responsibility" γ_k(x) refers to: | the posterior probability that observation x was generated by component k, given the observed x | claims γ_k(x) is the prior mixing weight πₖ itself, missing that it's a Bayesian update conditioned on x → `mixture-models-and-latent-variables` |
| R11 | recall | short-answer | 0.04 | Write the formula for the posterior responsibility γ_k(x) using Bayes' rule. | γ_k(x) = πₖfₖ(x) / Σⱼ πⱼfⱼ(x) | — |
| R12 | recall | short-answer | 0.09 | Fill in the blank: the stated prerequisite concept for Mixture Models and Latent Variables is ___. | Marginal Distribution | — |
| R13 | recall | short-answer | 0.14 | True or false: a mixture model with K=1 component reduces to a single (non-mixture) distribution. | True | — |
| R14 | recall | mcq | 0.19 | Which of the following is NOT a latent variable in a finite mixture model? | the observed data point x itself | claims the observed data point x is a latent variable, confusing it with the genuinely latent component indicator Z → `mixture-models-and-latent-variables` |
| R15 | recall | short-answer | 0.24 | Give one real-world example, other than customer segmentation, where a latent variable is natural. | e.g. disease subtype in a patient population when only symptoms are observed, or topic membership of a document when only words are observed | — |
| R16 | recall | short-answer | 0.29 | True or false: the observed marginal distribution p(x) in a mixture model can be multimodal even if every component fₖ is unimodal. | True | — |
| A2 | apply | numeric | 0.39 | π=(0.3,0.7), f₁(x₀)=0.4, f₂(x₀)=0.1. Marginal density p(x₀)? `[verified: 0.19]` | 0.3·0.4+0.7·0.1=0.12+0.07=0.19 | — |
| A3 | apply | numeric | 0.44 | Using the same numbers, the posterior responsibility γ₁(x₀)? `[verified: 0.632]` | 0.12/0.19≈0.632 | — |
| A4 | apply | numeric | 0.49 | K=4 equal weights (πₖ=0.25 each). f₁(x₀)=2, f₂(x₀)=1, f₃(x₀)=1, f₄(x₀)=0. Marginal density p(x₀)? `[verified: 1]` | 0.25·(2+1+1+0)=1 | — |
| A5 | apply | numeric | 0.54 | Using the same scenario, the posterior responsibility γ₁(x₀)? `[verified: 0.5]` | (0.25·2)/1=0.5 | — |
| A6 | apply | numeric | 0.59 | Unnormalized component weights (2,3,5). Normalized mixing weight π₃? `[verified: 0.5]` | total=10; π₃=5/10=0.5 | — |
| E2 | explain | short-answer | 1.09 | Why does the posterior responsibility γ_k(x) generally differ from the prior mixing weight πₖ? | πₖ is the marginal (prior) probability of belonging to component k before seeing x; γ_k(x) updates this via Bayes' rule using how well component k's density explains the specific observed x, so an observation strongly favoring one component's shape pulls its responsibility well above or below that component's baseline weight *(required: names the prior-to-posterior Bayesian update)* | — |
| E3 | explain | short-answer | 1.19 | Why is label switching a genuine identifiability problem rather than merely a labeling inconvenience? | the likelihood is exactly invariant under any permutation of component labels, so no amount of data can ever determine which numerical label corresponds to which real-world subpopulation — this is a fundamental non-identifiability of the model, not a fixable estimation error *(required: names the likelihood-invariance-under-permutation argument)* | — |
| E4 | explain | short-answer | 1.29 | Why can a mixture model with too many components K overfit, even though it's not adding predictors in the usual regression sense? | extra components give the model additional flexible density mass it can place to closely track sample-specific noise, such as a component collapsing onto a single outlying point, increasing the fitted training likelihood without genuinely describing new data equally well *(required: connects extra components to increased flexibility that can chase noise)* | — |
| E5 | explain | short-answer | 1.39 | Why does treating a mixture model's Z as observed data, rather than latent, change what estimation problem you're solving? | if Z were observed, estimating each component's parameters would reduce to K separate, ordinary parameter-estimation problems, each using only the data assigned to it; because Z is latent, the assignment itself must be inferred simultaneously with the parameters — exactly the harder joint problem `em-algorithm` is built to solve *(required: contrasts the trivial-if-observed case with the genuinely coupled latent case)* | — |
| T2 | transfer | short-answer | 1.59 | Why is choosing K by simply maximizing the log-likelihood on training data a flawed model-selection strategy? | the training likelihood strictly increases, or stays the same, as K grows, since more components only add flexibility, exactly paralleling why training RSS cannot be used to select polynomial degree — a penalized criterion (AIC/BIC) or cross-validation is needed to balance fit against complexity *(required: draws the explicit parallel to the same monotonic-fit problem in model selection generally)* | — |
| T3 | transfer | short-answer | 1.64 | How does the mixture model's generative story ("first pick a component, then generate from it") generalize to a hierarchical model with more than one layer of latent structure? | nothing in the mixture model's logic requires stopping at one latent layer — the same "pick an upper-level cause, then generate conditionally on it" structure can be nested, e.g. Z₁ picks a group, Z₂ picks a subgroup within it, then X is generated — exactly the generative logic hierarchical Bayesian models formalize *(required: names the recursive/nested generalization of the single-latent-layer idea)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.46…1.64.*

---

## EM Algorithm (`em-algorithm`)
*Prereq: MLE, Mixture Models and Latent Variables · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | Describe the EM algorithm's two steps. | E-step: given current parameters, compute the expected value (posterior distribution) of the latent variables given the data; M-step: given those inferred latent values, find parameters maximizing the likelihood as if the latent values were known | — |
| R2 | recall | mcq | 0.15 | EM is needed specifically because: | the likelihood with latent variables marginalized out is generally too complex to maximize directly — the sum/integral over Z inside a log doesn't decompose nicely | claims EM "guarantees finding the global maximum" — false; like K-means, it can get stuck in local optima → `em-algorithm` |
| A1 | apply | short-answer | 0.65 | Why is EM described as alternating between a "soft" K-means assignment step and a K-means update step? | the E-step probabilistically assigns each point to *every* component (soft assignment) rather than K-means' hard, single-component assignment; the M-step then recomputes parameters given those soft assignments, exactly paralleling K-means' centroid update *(required: the explicit soft-vs-hard assignment contrast with `k-means-clustering`)* | — |
| E1 | explain | short-answer | 1.35 | Why does EM guarantee the marginal likelihood increases monotonically each iteration, without directly maximizing it in one step? | the E-step and M-step together construct and maximize a lower bound on the true log-likelihood at each iteration — a preview of the ELBO ("Evidence Lower BOund") formalized in `variational-inference-elbo` later in this domain *(required: names the lower-bound mechanism, not just "EM always improves")* | — |
| T1 | transfer | short-answer | 1.85 | Why do practitioners use the same mitigation (multiple random restarts) for both EM and K-means' local-optimum issue? | given EM's direct structural parallel to K-means established in A1, it inherits the identical local-optimum vulnerability, so the identical fix (multiple restarts, keeping the best result by objective value) applies for the same underlying reason *(required: connects the shared fix to the shared structural cause from A1)* | — |
| R3 | recall | mcq | −0.1 | The E-step computes: | the expected complete-data log-likelihood under the current posterior over latent variables (equivalently, the responsibilities) | claims the E-step maximizes the parameters directly, which is actually the M-step's job → `em-algorithm` |
| R4 | recall | short-answer | −0.05 | Fill in the blank: the M-step ___ the expected complete-data log-likelihood (the Q function) with respect to the model parameters. | maximizes | — |
| R5 | recall | short-answer | 0.0 | True or false: EM guarantees the observed-data log-likelihood never decreases from one iteration to the next. | True | — |
| R6 | recall | mcq | 0.05 | The "Q function" Q(θ,θ^old) in EM is defined as: | the expected complete-data log-likelihood, with the expectation taken over the latent variables' posterior given the current estimate θ^old | claims Q is simply the observed-data log-likelihood itself, missing that it's an expectation over the latent posterior → `em-algorithm` |
| R7 | recall | short-answer | 0.1 | Why is EM sensitive to initialization? | it can converge to different local optima of the likelihood depending on the starting parameter values, since it is a local, not global, optimization procedure | — |
| R8 | recall | short-answer | 0.2 | Fill in the blank: a common practical fix for EM's sensitivity to initialization is ___. | running EM multiple times from different random starts and keeping the best result by final likelihood | — |
| R9 | recall | short-answer | 0.25 | True or false: EM requires the complete-data likelihood, if Z were observed, to be easy to maximize. | True — this tractability is exactly what EM exploits | — |
| R10 | recall | mcq | 0.3 | "Complete data" in the EM context refers to: | the observed data X together with the unobserved latent variables Z | claims "complete data" means only the latent variables Z, omitting the observed data X from the definition → `em-algorithm` |
| R11 | recall | short-answer | 0.35 | Contrast the "observed-data log-likelihood" with the "complete-data log-likelihood." | the observed-data log-likelihood is log p(X\|θ), the actual quantity to maximize but often intractable directly; the complete-data log-likelihood is log p(X,Z\|θ), tractable if Z were known, and EM iteratively maximizes its expectation instead | — |
| R12 | recall | short-answer | 0.4 | Fill in the blank: the stated prerequisites for EM Algorithm are ___ and ___. | MLE and Mixture Models and Latent Variables | — |
| R13 | recall | short-answer | 0.45 | True or false: "Generalized EM" (GEM) requires the M-step to find the exact maximizer of Q at every iteration. | False — GEM only requires the M-step to increase Q, not necessarily maximize it exactly | — |
| R14 | recall | mcq | 0.5 | EM is applicable beyond mixture models, for example to: | missing-data problems in general, wherever some variables are unobserved and their absence makes direct maximum likelihood intractable | claims EM is only ever applicable to Gaussian mixture models specifically, missing its much broader missing-data applicability → `em-algorithm` |
| R15 | recall | short-answer | 0.55 | What two steps repeat until convergence in the EM algorithm? | the E-step (compute the expected complete-data log-likelihood given current parameters) and the M-step (maximize that expectation to get new parameters) | — |
| R16 | recall | short-answer | 0.6 | State one commonly used convergence criterion for stopping EM iterations. | the change in log-likelihood, or in the parameters, between iterations falls below a small threshold | — |
| A2 | apply | short-answer | 0.7 | Log-likelihood values across EM iterations: −200, −180, −175. Is this sequence consistent with EM's monotonicity guarantee? `[verified: yes]` | yes — each value is greater than or equal to the previous, matching the guarantee that observed-data log-likelihood never decreases | — |
| A3 | apply | numeric | 0.75 | Responsibilities γ₁=0.7, γ₂=0.3 for one point; log f₁=−2, log f₂=−5. That point's contribution to the expected complete-data log-likelihood? `[verified: -2.9]` | 0.7·(−2)+0.3·(−5)=−1.4−1.5=−2.9 | — |
| A4 | apply | numeric | 0.8 | A second point has γ₁=0.4, γ₂=0.6; log f₁=−3, log f₂=−1. Its contribution? `[verified: -1.8]` | 0.4·(−3)+0.6·(−1)=−1.2−0.6=−1.8 | — |
| A5 | apply | numeric | 0.85 | Summing the two points' contributions from A3 and A4, the total Q value? `[verified: -4.7]` | −2.9+(−1.8)=−4.7 | — |
| A6 | apply | numeric | 0.9 | EM is run 5 times from random starts, giving final log-likelihoods −102, −98, −105, −98, −110. Which value is selected as the result, and what is it? `[verified: -98]` | the run with the highest (least negative) log-likelihood is kept: −98 | — |
| E2 | explain | short-answer | 1.4 | Why does maximizing the Q function at each M-step guarantee the actual observed-data log-likelihood does not decrease? | Q is constructed as a lower bound on the observed-data log-likelihood that touches it exactly at the current estimate θ^old; increasing this lower bound (the M-step) therefore brings the actual log-likelihood at the new θ to at least as high as it was at θ^old — a specific instance of the general ELBO-maximization argument formalized later in `variational-inference-elbo` *(required: names the touching-lower-bound mechanism)* | — |
| E3 | explain | short-answer | 1.5 | Why is the E-step's expectation taken with respect to the posterior p(Z\|X,θ^old), rather than the prior p(Z\|θ^old)? | using the posterior incorporates what the observed data X actually reveals about the likely values of Z given the current parameters, rather than ignoring the data and using only the marginal component probabilities — this posterior weighting is exactly what makes Q a tight lower bound at θ^old *(required: names that the posterior, not the prior, is what makes Q tight)* | — |
| E4 | explain | short-answer | 1.6 | Why can running EM with too few components look like it "converged nicely" while still being the wrong model? | EM's convergence guarantee is only about the likelihood not decreasing within a fixed model structure; it says nothing about whether that structure (e.g. the chosen K) is adequate, so a too-small K can converge cleanly to a stable but poorly-fitting local optimum *(required: separates convergence-within-a-model from model-adequacy)* | — |
| E5 | explain | short-answer | 1.7 | Why does "Generalized EM," which only requires the M-step to increase (not maximize) Q, retain the same monotonic-improvement guarantee as ordinary EM? | the monotonic-improvement proof only relies on Q not decreasing at each M-step, since Q's tangency to the true log-likelihood at θ^old is what licenses the argument — exact maximization is a stronger, sufficient but not necessary, way to guarantee that increase *(required: names that the core argument only needs Q not to decrease, not to be maximized exactly)* | — |
| T2 | transfer | short-answer | 1.9 | Why might a practitioner run K-means first to initialize a GMM's EM, rather than starting from random parameter values? | K-means is fast, and its hard cluster assignments give EM a reasonable starting point close to a good local optimum, reducing both the iterations needed and the risk of converging to a poor local optimum from an arbitrary random start *(required: connects K-means initialization to specifically addressing EM's local-optimum risk)* | — |
| T3 | transfer | short-answer | 1.95 | How does the EM framework apply to genuinely missing (not model-latent) data, such as a survey with some unanswered questions? | the same alternating logic applies directly: the E-step imputes the expected values (or a full distribution) for the missing responses given the current parameter estimates and observed responses, and the M-step re-estimates the parameters as if that imputed data were complete — missing-data imputation is EM applied with Z playing the role of the unanswered questions *(required: names the direct Z-as-missing-data reinterpretation)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.15…1.95.*

---

## Gaussian Mixture Models (`gaussian-mixture-models`)
*Prereq: EM Algorithm, Multivariate Normal · ancestors 40 · b₀ = 1.36*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.36 | Describe a Gaussian Mixture Model. | a mixture model where each component is a multivariate Normal, fit via EM, alternating between soft cluster assignments and re-estimating each component's mean, covariance, and mixing weight | — |
| R2 | recall | mcq | 0.66 | Compared to K-means, GMMs offer: | soft cluster assignments and the ability to model elliptical (not just spherical) shapes, via each component's own covariance | claims GMMs use "hard, all-or-nothing assignments, exactly like K-means" — missing the entire probabilistic-assignment advantage → `gaussian-mixture-models` |
| A1 | apply | short-answer | 1.16 | Why is K-means a special, limiting case of GMM? | as component covariances shrink toward a shared, spherical form (σ²I for small σ²), soft E-step assignments become increasingly "hard" (nearly all probability mass to the single nearest component), recovering K-means' hard-assignment behavior exactly in that limit *(required: the explicit shrinking-covariance limit argument)* | — |
| E1 | explain | short-answer | 1.86 | Why can GMMs capture cluster shapes K-means cannot? | using `multivariate-normal`'s eigendecomposition-based visualization (Σ=QΛQᵀ), each GMM component's covariance can have arbitrary eigenvectors/eigenvalues, allowing ellipsoidal clusters oriented and shaped in any direction — unlike K-means' implicit assumption of spherical clusters *(required: the explicit eigendecomposition connection, not just "GMMs are more flexible")* | — |
| T1 | transfer | short-answer | 2.36 | GMMs face the same challenge as K-means in choosing K, but have one genuine advantage there — what is it? | GMMs provide an explicit likelihood, so AIC/BIC (from the regression domain) can principledly compare different values of K — something ordinary K-means, having no explicit likelihood, cannot do as directly *(required: names the explicit-likelihood-enables-AIC/BIC advantage specifically)* | — |
| R3 | recall | mcq | 0.41 | Each component of a GMM is parameterized by: | a mean vector μₖ, a covariance matrix Σₖ, and a mixing weight πₖ | omits the mixing weight πₖ, describing each component only by its mean and covariance → `gaussian-mixture-models` |
| R4 | recall | short-answer | 0.46 | Fill in the blank: the multivariate Gaussian component density is fₖ(x) = N(x; ___, ___). | μₖ, Σₖ | — |
| R5 | recall | short-answer | 0.51 | True or false: a "spherical" covariance constraint in a GMM restricts each component's covariance to be a scalar multiple of the identity matrix. | True | — |
| R6 | recall | mcq | 0.56 | Compared to a "full" covariance GMM, a "diagonal" covariance GMM: | assumes no correlation between features within a component (off-diagonal entries are 0), reducing the number of parameters | claims a diagonal-covariance GMM has more free parameters than a full-covariance GMM, backwards from the actual parameter reduction → `gaussian-mixture-models` |
| R7 | recall | short-answer | 0.61 | How does the number of free parameters in a full-covariance GMM's covariance matrix, per component, relate to the dimension d? | d(d+1)/2, since a symmetric d×d matrix has that many independent entries | — |
| R8 | recall | short-answer | 0.71 | Fill in the blank: GMM parameters are typically estimated using the ___ algorithm. | EM | — |
| R9 | recall | short-answer | 0.76 | True or false: K-means can be derived as a limiting case of GMM as all component covariances shrink to a shared, infinitesimally small spherical form. | True | — |
| R10 | recall | mcq | 0.81 | Choosing K in a GMM via BIC is possible because: | a GMM provides an explicit likelihood, unlike K-means, which lacks one | claims BIC is used for GMM because GMM has fewer parameters than K-means, rather than because it has an explicit likelihood → `gaussian-mixture-models` |
| R11 | recall | short-answer | 0.86 | State the GMM's responsibility formula γ_k(x) in terms of πₖ and the Gaussian density N. | γ_k(x) = πₖN(x;μₖ,Σₖ) / Σⱼ πⱼN(x;μⱼ,Σⱼ) | — |
| R12 | recall | short-answer | 0.91 | Fill in the blank: the prerequisite concepts for Gaussian Mixture Models are ___ and ___. | EM Algorithm and Multivariate Normal | — |
| R13 | recall | short-answer | 0.96 | True or false: GMM cluster assignments are "hard" — each point belongs fully to one cluster. | False — GMM gives soft, probabilistic assignments via the responsibilities | — |
| R14 | recall | mcq | 1.01 | The covariance matrix Σₖ of a GMM component determines: | the shape, size, and orientation of that component's elliptical cluster | claims Σₖ only determines the cluster's size, missing that it also controls shape and orientation → `gaussian-mixture-models` |
| R15 | recall | short-answer | 1.06 | Name one practical application domain for GMMs besides general clustering. | e.g. speaker/voice identification, background subtraction in video, or anomaly detection via density estimation | — |
| R16 | recall | short-answer | 1.11 | True or false: a GMM's log-likelihood is guaranteed to increase monotonically across EM iterations, exactly as for a general mixture model. | True | — |
| A2 | apply | numeric | 1.21 | d=3-dimensional data, full covariance. Free parameters in one component's covariance matrix? `[verified: 6]` | d(d+1)/2=3·4/2=6 | — |
| A3 | apply | numeric | 1.26 | K=2 components, d=3, full covariance. Total free parameters (means + covariances + weights)? `[verified: 19]` | means: 2·3=6; covariances: 2·6=12; weights: 2−1=1; total=6+12+1=19 | — |
| A4 | apply | numeric | 1.31 | Same setup (K=2, d=3), but diagonal covariance instead of full. Total free parameters? `[verified: 13]` | means: 6; covariances: 2·3=6; weights: 1; total=13 | — |
| A5 | apply | numeric | 1.36 | Same setup (K=2, d=3), but spherical covariance. Total free parameters? `[verified: 9]` | means: 6; covariances: 2·1=2; weights: 1; total=9 | — |
| A6 | apply | short-answer | 1.41 | A 2D GMM component has covariance Σ=[[4,0],[0,1]]. Is this spherical, diagonal, or full (non-diagonal)? `[verified: diagonal, not spherical]` | it's diagonal (off-diagonal entries are 0) but not spherical, since the diagonal entries 4 and 1 are unequal | — |
| E2 | explain | short-answer | 1.91 | Why does restricting GMM components to spherical covariance reduce it toward something closer to K-means, even before taking any limit? | spherical covariance forces every component's cluster shape to be perfectly circular in every direction, eliminating GMM's main shape-modeling advantage over K-means well before the full covariance-shrinking limit that recovers K-means exactly, since the elliptical-shape flexibility from unequal or off-diagonal covariance entries is already gone *(required: distinguishes the shape restriction itself from the full limiting argument)* | — |
| E3 | explain | short-answer | 2.01 | Why does a full-covariance GMM's parameter count grow much faster with dimension d than a diagonal-covariance GMM's? | a full covariance matrix has d(d+1)/2 free parameters, growing quadratically with d, while a diagonal covariance has only d free parameters, growing linearly — in high dimensions this quadratic growth can make a full-covariance GMM badly overfit or numerically unstable with limited data *(required: names the quadratic-vs-linear growth distinction)* | — |
| E4 | explain | short-answer | 2.11 | Why is choosing K via BIC in a GMM not simply "pick the K with the highest likelihood," even though GMM has an explicit likelihood? | exactly as with mixture models generally, the raw training likelihood increases with K regardless of the true structure; BIC instead penalizes the number of parameters, which grows with both K and covariance type, trading off fit against complexity rather than rewarding fit alone *(required: connects back to the training-likelihood-monotonicity issue, not treating BIC as arbitrary)* | — |
| E5 | explain | short-answer | 2.21 | Why can a GMM component's covariance sometimes collapse toward a singular matrix during EM fitting, and why is this a problem? | if very few points end up with high responsibility for a component, that component's covariance estimate is based on too little effective data and can shrink toward a degenerate, singular matrix, at which point the Gaussian density, which requires Σ⁻¹, becomes undefined or numerically unstable *(required: names the low-effective-sample-size mechanism behind covariance collapse)* | — |
| T2 | transfer | short-answer | 2.41 | Contrast a GMM used for clustering with a GMM used purely for density estimation, with no interest in cluster labels at all. | the underlying model and EM fitting procedure are identical either way; the difference is purely in how the output is used — as a clustering tool, the responsibilities or a hard argmax assignment are read off per point, while as a density estimator only the overall fitted p(x)=Σπₖfₖ(x) matters, and individual component identities may be irrelevant or non-identifiable due to label switching *(required: names that the model/fitting is identical, only the downstream use differs)* | — |
| T3 | transfer | short-answer | 2.46 | Why does the eigendecomposition Σ=QΛQᵀ let you describe what a GMM component "looks like" before ever plotting the data? | the eigendecomposition of a component's covariance matrix directly gives the ellipse's or ellipsoid's axis directions (eigenvectors) and lengths (√eigenvalues), so purely from the fitted Σₖ one can describe the cluster's orientation and spread analytically, the same eigendecomposition argument E1 already used *(required: connects the eigendecomposition to an analytic, pre-visualization description)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.36…2.46.*

---

## Laplace Approximation (`laplace-approximation`)
*Prereq: MLE, Multivariate Normal · ancestors 37 · b₀ = 1.32*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.32 | Describe the Laplace approximation. | approximate a complicated distribution with a Normal centered at its mode, with covariance given by the inverse of the negative Hessian of the log-density at that mode | — |
| R2 | recall | mcq | 0.62 | The Laplace approximation's covariance matrix connects directly to: | Fisher information — the negative Hessian of the log-likelihood at the MLE is (asymptotically) the observed Fisher information | treats the covariance formula as an unrelated technical device rather than recognizing it as Fisher information → `laplace-approximation` |
| A1 | apply | short-answer | 1.12 | Why does a sharply-peaked mode produce a narrow Normal approximation, and a flatter mode a wider one? | a sharp peak means large negative second derivative — large Fisher information, per `fisher-information`'s "sharper peak = more information = lower variance" intuition — directly reused, not a fresh geometric argument *(required: the explicit callback to that earlier intuition)* | — |
| E1 | explain | derivation | 1.82 | Sketch the derivation of the Laplace approximation via Taylor expansion. | Taylor-expand the log of the target density around its mode to second order; the first-order term vanishes since the gradient is zero at a mode; the resulting quadratic approximation to the log-density is exactly the log of a (possibly unnormalized) Normal density, which is why the approximation is Normal *(required: names the vanishing first-order term specifically)* | — |
| T1 | transfer | short-answer | 2.32 | What's a genuine limitation of the Laplace approximation, and what does it motivate? | it performs poorly for highly skewed or multi-modal distributions, per `mixture-models-and-latent-variables`'s earlier discussion — a single Normal centered at one mode can't capture multiple separate "bumps" or severe asymmetry, motivating more flexible methods like variational inference, covered next in this domain *(required: the explicit multi-modal-distribution failure case, connecting back to that earlier concept)* | — |
| R3 | recall | mcq | 0.37 | The Laplace approximation approximates a target distribution with a: | multivariate Normal distribution centered at the mode | claims it approximates the target with a uniform distribution over a region near the mode, rather than a Normal shaped by local curvature → `laplace-approximation` |
| R4 | recall | short-answer | 0.42 | Fill in the blank: the mean of the Laplace-approximated Normal is the ___ of the target distribution. | mode (e.g. the MAP estimate) | — |
| R5 | recall | short-answer | 0.47 | True or false: the Laplace approximation's covariance matrix is the inverse of the negative Hessian of the log-density at the mode. | True | — |
| R6 | recall | mcq | 0.52 | The Laplace approximation is derived via a ___ expansion of the log-density around its mode. | second-order Taylor | claims it is derived via a first-order Taylor expansion, which would not produce a quadratic (and hence Normal-shaped) approximation → `laplace-approximation` |
| R7 | recall | short-answer | 0.57 | Why does the first-order term vanish in the Taylor expansion used to derive the Laplace approximation? | the expansion is taken around the mode, where the gradient of the log-density is zero by definition of a stationary point | — |
| R8 | recall | short-answer | 0.67 | Fill in the blank: the Laplace approximation is most accurate when the true posterior is close to ___ in shape. | Gaussian (unimodal and roughly symmetric) | — |
| R9 | recall | short-answer | 0.72 | True or false: the Laplace approximation tends to become more accurate as sample size grows, by the Bernstein-von Mises theorem. | True | — |
| R10 | recall | mcq | 0.77 | A key limitation of the Laplace approximation is that it: | performs poorly on multi-modal or highly skewed distributions, since it fits only a single Gaussian at one mode | claims its key limitation is being too computationally expensive, rather than its shape-fitting limitation → `laplace-approximation` |
| R11 | recall | short-answer | 0.82 | State one common application of the Laplace approximation in Bayesian statistics. | approximating an intractable posterior — e.g. in Bayesian logistic regression or Gaussian process classification — with a Gaussian for tractable downstream computation | — |
| R12 | recall | short-answer | 0.87 | Fill in the blank: the prerequisite concepts for Laplace Approximation are ___ and ___. | MLE and Multivariate Normal | — |
| R13 | recall | short-answer | 0.92 | True or false: the Laplace approximation can also be used to approximate an intractable normalizing constant (marginal likelihood/evidence). | True | — |
| R14 | recall | mcq | 0.97 | Compared to variational inference, the Laplace approximation is: | generally simpler to compute, just requiring the mode and Hessian, but typically less flexible for non-Gaussian-shaped posteriors | claims the Laplace approximation is always more accurate than variational inference regardless of the posterior's shape → `laplace-approximation` |
| R15 | recall | short-answer | 1.02 | What matrix must be inverted to obtain the Laplace approximation's covariance, and what property must it have? | the negative Hessian of the log-density at the mode; it must be positive definite, confirming the point is truly a maximum | — |
| R16 | recall | short-answer | 1.07 | True or false: computing the Hessian at the mode can become computationally expensive in high dimensions. | True — it requires O(d²) storage and computing all second partial derivatives | — |
| A2 | apply | numeric | 1.17 | A 1-D log-density has second derivative −4 at its mode. Laplace-approximated variance? `[verified: 0.25]` | variance = 1/(−(−4)) = 1/4 = 0.25 | — |
| A3 | apply | numeric | 1.22 | A 1-D log-density has second derivative −0.5 at its mode. Laplace-approximated variance? `[verified: 2]` | variance = 1/0.5 = 2 | — |
| A4 | apply | numeric | 1.27 | A 2-D log-density has a diagonal Hessian [−2, −8] at its mode. Laplace-approximated variances along each axis? `[verified: 0.5, 0.125]` | 1/2=0.5; 1/8=0.125 | — |
| A5 | apply | short-answer | 1.32 | Log-posterior second derivatives at the mode: Problem A is −100, Problem B is −4. Which posterior's Laplace approximation is narrower? `[verified: A]` | larger curvature magnitude (100 vs 4) means smaller variance, so Problem A's approximation is narrower | — |
| A6 | apply | numeric | 1.37 | Fisher information per observation is 2; n=50 iid observations. Approximate expected Hessian magnitude n·I? `[verified: 100]` | 50·2=100 | — |
| E2 | explain | short-answer | 1.87 | Why does the Laplace approximation's accuracy generally improve as sample size n grows? | as n grows, the log-posterior, dominated by the log-likelihood for a fixed prior, becomes increasingly peaked and increasingly well-approximated by its second-order Taylor expansion around the mode, by essentially the same argument behind the Bernstein-von Mises theorem — the posterior becomes asymptotically Normal *(required: connects growing n to the increasing accuracy of the quadratic/Taylor approximation)* | — |
| E3 | explain | short-answer | 1.97 | Why is a positive-definite negative Hessian a necessary check, not just a technical formality, when computing the Laplace approximation? | if the negative Hessian is not positive definite at the point being expanded around, that point is not actually a local maximum — it could be a saddle point — so the resulting "variance" from inverting it could be negative or nonsensical, meaning the quadratic approximation is built around the wrong kind of point *(required: names that non-positive-definiteness signals the point is not a true mode)* | — |
| E4 | explain | short-answer | 2.07 | Why does the Laplace approximation typically underestimate posterior uncertainty when the true posterior is skewed rather than symmetric? | a Normal approximation is inherently symmetric around its mean, so when the true distribution has a longer tail on one side, the Laplace approximation, matched only to the local curvature at the mode, cannot represent that asymmetric spread, and the light-tailed side of the fitted Normal underrepresents the true uncertainty there *(required: connects the Normal's symmetry to the specific underestimation direction)* | — |
| E5 | explain | short-answer | 2.17 | Why can the Laplace approximation be used to approximate a marginal likelihood (evidence) integral, not just a posterior shape? | the same second-order Taylor expansion that produces the approximating Gaussian also gives a closed-form expression for the integral of the unnormalized posterior, since integrating a Gaussian density's unnormalized form has a known closed form involving the determinant of the Hessian — exactly what's needed to approximate the intractable normalizing constant *(required: names that integrating the fitted Gaussian's closed form is what approximates the evidence)* | — |
| T2 | transfer | short-answer | 2.37 | Why might a practitioner choose the Laplace approximation over full MCMC sampling for a large-scale Bayesian logistic regression, and what's the risk? | the Laplace approximation is far cheaper computationally — one optimization to find the mode plus one Hessian, versus many expensive sampling iterations — but risks understating uncertainty or missing multimodality if the true posterior deviates substantially from a single Gaussian bump, a risk that shrinks as sample size grows per E2's Bernstein-von Mises argument *(required: names the speed-vs-risk trade-off and connects the risk's shrinking to sample size)* | — |
| T3 | transfer | short-answer | 2.42 | How does the Laplace approximation relate to variational inference as two different strategies for the same underlying problem? | both approximate an intractable posterior with a tractable distribution, but use different criteria: the Laplace approximation matches local curvature at a single point (the mode), while variational inference explicitly minimizes a divergence (e.g. KL divergence) between the approximating family and the true posterior over its whole support — variational inference is generally more flexible, at higher optimization cost *(required: names the mode-matching vs divergence-minimization distinction)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.32…2.42.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| latent variable Z assumed observed | `mixture-models-and-latent-variables` |
| EM assumed to guarantee the global optimum | `em-algorithm` |
| GMM's soft assignment advantage over K-means missed | `gaussian-mixture-models` |
| Laplace approximation's covariance treated as an arbitrary formula rather than Fisher information | `laplace-approximation` |
| mixing weights not required to sum to 1 | `mixture-models-and-latent-variables` |
| label switching treated as a fixable labeling inconvenience rather than genuine non-identifiability | `mixture-models-and-latent-variables` |
| posterior responsibility confused with the prior mixing weight | `mixture-models-and-latent-variables` |
| E-step and M-step roles swapped (maximization assigned to E-step) | `em-algorithm` |
| Q function confused with the observed-data log-likelihood itself | `em-algorithm` |
| complete data defined as only the latent variables, omitting the observed data | `em-algorithm` |
| Generalized EM assumed to require exact maximization of Q | `em-algorithm` |
| GMM component parameterization missing the mixing weight | `gaussian-mixture-models` |
| diagonal vs full covariance parameter counts reversed | `gaussian-mixture-models` |
| BIC's role in GMM model selection attributed to the wrong mechanism | `gaussian-mixture-models` |
| Laplace approximation's target distribution confused with a uniform distribution | `laplace-approximation` |
| Laplace approximation assumed derived from a first-order Taylor expansion | `laplace-approximation` |
| Laplace approximation's key limitation misattributed to computational cost rather than shape-fitting | `laplace-approximation` |

**Cluster total: 120 items across 4 concepts (30 each), expanded from the original 20-item design-doc
skeleton.** This cluster is conceptual/derivational rather than numerically heavy, though the added
apply items verify small arithmetic (mixture-density and responsibility calculations, GMM parameter
counts by covariance type, Laplace-approximation variances from Hessian curvature) by hand — its core
claims still rest on exact identities (EM as soft K-means, Laplace's covariance as observed Fisher
information) rather than heavier computation.
