# Linear Algebra Cluster 7 — Spectral Theory & Special Matrices

Spectral Theorem, Orthogonal Matrices, Positive Definite Matrices, Cholesky Decomposition, Schur
Complement, Rayleigh Quotient, Matrix Stability (7 concepts). Same format as
[Cluster 1](la-01-vectors-and-operations.md).

`spectral-theorem`'s T1 resolves `eigendecomposition`'s rotation-matrix cliffhanger from the previous
cluster in full; `positive-definite-matrices` and `schur-complement` both reach directly back into the
probability/statistics sweep (covariance matrices, conditional variance).

---

## Spectral Theorem (`spectral-theorem`)
*Prereq: Symmetric Matrices, Eigendecomposition, Orthonormal Basis · ancestors 22 · b₀ = 1.07*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.07 | State the Spectral Theorem. | every symmetric A can be written A=QΛQᵀ, where Q is orthogonal (orthonormal eigenvectors as columns) and Λ is diagonal with real eigenvalues | — |
| R2 | recall | mcq | 0.37 | The Spectral Theorem strengthens ordinary diagonalization (A=PDP⁻¹) by guaranteeing: | P can always be chosen orthogonal, so P⁻¹=Pᵀ | claims "D always has positive entries" — eigenvalues of a symmetric matrix can certainly be negative → `spectral-theorem` |
| A1 | apply | short-answer | 0.87 | A symmetric A has orthonormal eigenvectors q₁,q₂ with eigenvalues 3,5. Write A=QΛQᵀ explicitly. | Q=[q₁ q₂], Λ=diag(3,5); equivalently A=3q₁q₁ᵀ+5q₂q₂ᵀ, a sum of rank-1 pieces | — |
| E1 | explain | derivation | 1.57 | Prove symmetric matrices have orthogonal eigenvectors for distinct eigenvalues. | if Av₁=λ₁v₁, Av₂=λ₂v₂ with λ₁≠λ₂: λ₁(v₁·v₂)=(Av₁)·v₂=v₁·(Av₂)=λ₂(v₁·v₂) using A=Aᵀ; so (λ₁−λ₂)(v₁·v₂)=0, and since λ₁≠λ₂, v₁·v₂=0 *(required: the full symmetric-substitution chain)* | — |
| T1 | transfer | short-answer | 2.07 | Why does PCA rely fundamentally on the Spectral Theorem applied to a covariance matrix? | a covariance matrix's eigenvectors are *guaranteed* real and orthogonal (not merely generically likely to be), which is exactly what's needed for the principal components to form a valid orthonormal coordinate system — this resolves `eigendecomposition`'s rotation-matrix cliffhanger: symmetry is precisely the property that rules out the rotational behavior that broke real eigendecomposition there *(required: the explicit resolution of that earlier example)* | — |
| R3 | recall | mcq | −0.10 | Which property must a matrix have for the Spectral Theorem to apply? | must be symmetric (A=Aᵀ) | picks "invertible" — invertibility is neither necessary (a singular symmetric matrix still diagonalizes this way) nor sufficient → `spectral-theorem` |
| R4 | recall | short-answer | 0.02 | What does the Λ in A=QΛQᵀ contain? | a diagonal matrix holding A's (real) eigenvalues | — |
| R5 | recall | short-answer | 0.10 | What do the columns of Q consist of in the Spectral Theorem decomposition? | the orthonormal eigenvectors of A | — |
| R6 | recall | mcq | 0.20 | For an n×n symmetric matrix, how many real eigenvalues does the Spectral Theorem guarantee, counted with multiplicity? | exactly n | picks "at most n, since some could be complex" — the theorem guarantees all n are real → `spectral-theorem` |
| R7 | recall | short-answer | −0.20 | True or false, with justification: the Spectral Theorem guarantees Q is invertible. | true — Q is orthogonal, and every orthogonal matrix is invertible (Q⁻¹=Qᵀ) | — |
| R8 | recall | short-answer | 0.30 | Fill in the blank: in the Spectral Theorem, A = QΛ ___. | Qᵀ (equivalently Q⁻¹, since Q is orthogonal) | — |
| R9 | recall | short-answer | −0.05 | Give another name for the collection of columns of Q in A=QΛQᵀ. | an orthonormal eigenbasis of A | — |
| R10 | recall | mcq | 0.15 | According to the Spectral Theorem, the eigenvalues of a real symmetric matrix are always: | real numbers | picks "complex in general" — the theorem's entire content is that they are guaranteed real → `spectral-theorem` |
| R11 | recall | short-answer | 0.25 | State how det(A) relates to the entries of Λ for a symmetric A=QΛQᵀ. | det(A) equals the product of Λ's diagonal entries (A's eigenvalues), since det(Q)det(Qᵀ)=1 | — |
| R12 | recall | short-answer | −0.15 | State how trace(A) relates to the entries of Λ. | trace(A) equals the sum of Λ's diagonal entries (A's eigenvalues), since trace is invariant under the similarity transform | — |
| R13 | recall | mcq | 0.05 | Which of the following is guaranteed to be diagonalizable by an *orthogonal* matrix? | any real symmetric matrix | picks "any square matrix with real entries" — a real rotation matrix is a counterexample with no real eigenvectors at all → `spectral-theorem` |
| R14 | recall | short-answer | 0.35 | Give a common alternative name for the Spectral Theorem as applied to symmetric matrices. | the symmetric eigenvalue decomposition (also called the principal axis theorem) | — |
| R15 | recall | mcq | −0.25 | True or false: a symmetric matrix can have a repeated eigenvalue. | true — repeated eigenvalues are allowed, with an orthogonal eigenspace of matching dimension | picks "false, all eigenvalues of a symmetric matrix must be distinct" → `spectral-theorem` |
| R16 | recall | short-answer | 0.40 | Given A=QΛQᵀ with A invertible, write A⁻¹ directly in terms of Q and Λ. | A⁻¹ = QΛ⁻¹Qᵀ | — |
| A2 | apply | numeric | 0.60 | A symmetric 2×2 matrix has eigenvalues 4 and −2 with orthonormal eigenvectors q₁,q₂. Write Λ. `[verified: diag(4,-2)]` | Λ=diag(4,−2) | — |
| A3 | apply | numeric | 0.70 | A symmetric A=QΛQᵀ has Λ=diag(9,4). Compute det(A). `[verified: 36]` | det(A)=9·4=36, using R11 | — |
| A4 | apply | numeric | 0.80 | A symmetric matrix has Λ=diag(3,7). Compute trace(A). `[verified: 10]` | 3+7=10, using R12 | — |
| A5 | apply | short-answer | 0.95 | A symmetric A has orthonormal eigenvectors q₁,q₂,q₃ with eigenvalues 1,0,−1. Write A as a sum of rank-1 outer products. | A = 1·q₁q₁ᵀ + 0·q₂q₂ᵀ + (−1)·q₃q₃ᵀ = q₁q₁ᵀ − q₃q₃ᵀ | — |
| A6 | apply | numeric | 1.05 | Symmetric A has eigenvalues 2, 2, 5. Compute the eigenvalues of A⁻¹. `[verified: 0.5, 0.5, 0.2]` | 1/2, 1/2, 1/5, using R16 | — |
| E2 | explain | short-answer | 1.35 | Explain why Q being orthogonal (not merely invertible) is essential for A=QΛQᵀ to automatically be symmetric. | Aᵀ=(QΛQᵀ)ᵀ=QΛᵀQᵀ=QΛQᵀ=A, since Λ is diagonal hence symmetric — this substitution only closes because the same Q appears transposed on the outside; a merely invertible P in A=PΛP⁻¹ would not generally satisfy (PΛP⁻¹)ᵀ=PΛP⁻¹ *(required: the explicit (QΛQᵀ)ᵀ=A computation)* | — |
| E3 | explain | short-answer | 1.45 | Explain why the Spectral Theorem implies a symmetric matrix's eigenvectors span the *entire* space, not just form an orthogonal set. | Q is a full n×n orthogonal matrix, so its n columns form a complete orthonormal basis of ℝⁿ; combined with E1's orthogonality result and R6's guarantee of exactly n real eigenvalues counted with multiplicity, the eigenvectors are guaranteed to span all of ℝⁿ, not merely be pairwise orthogonal *(required: connects orthogonality plus full count to spanning)* | — |
| E4 | explain | short-answer | 1.65 | Explain why A=QΛQᵀ makes it easy to compute any power of A (e.g. A³) just by raising Λ's entries to that power. | A²=(QΛQᵀ)(QΛQᵀ)=QΛ(QᵀQ)ΛQᵀ=QΛ²Qᵀ, since QᵀQ=I collapses the middle term — this repeats for any power, so Aᵏ=QΛᵏQᵀ, and Λᵏ just raises each diagonal eigenvalue to the k-th power *(required: the explicit QᵀQ=I collapse)* | — |
| E5 | explain | short-answer | 1.75 | Explain why a repeated eigenvalue's eigenvectors are not unique, yet the Spectral Theorem's guarantee still holds exactly. | any orthonormal basis of the eigenspace belonging to the repeated eigenvalue serves equally well as those columns of Q, since Λ only records the eigenvalue and its multiplicity, not a particular choice of basis — A=QΛQᵀ holds exactly for any valid such choice *(required: names the eigenspace/basis-choice argument)* | — |
| T2 | transfer | short-answer | 1.95 | Why does the Spectral Theorem guarantee that Cholesky decomposition succeeds on any positive definite matrix, connecting to `cholesky-decomposition`? | positive definiteness means every entry of Λ is strictly positive (per `positive-definite-matrices`), so each has a real positive square root; combined with A=QΛQᵀ this lets a real L be built with A=LLᵀ — the Spectral Theorem's guarantee of *real* eigenvalues is exactly what rules out ever needing a complex square root along the way *(required: the real-positive-square-root argument)* | — |
| T3 | transfer | short-answer | 2.15 | Why does the Spectral Theorem make the Rayleigh quotient's maximum provably *attainable*, foreshadowing `rayleigh-quotient`, rather than merely an upper bound? | because Q's columns form a genuine orthonormal basis, any vector x can be re-expressed exactly in that eigenbasis with real coordinates, turning the Rayleigh quotient into a literal weighted average of the real eigenvalues — this is what makes the maximum an equality achieved at an eigenvector, not just a bound that might never be reached *(required: the exact re-expression in the eigenbasis)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.25…2.15.*

---

## Orthogonal Matrices (`orthogonal-matrices`)
*Prereq: Orthonormal Basis · ancestors 9 · b₀ = 0.65*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.35 | Define an orthogonal matrix Q. | QᵀQ=I, equivalently Q⁻¹=Qᵀ, and Q's columns form an orthonormal basis | — |
| R2 | recall | mcq | −0.05 | Orthogonal matrices, as transformations, always: | preserve lengths and angles (rotations/reflections only) | claims they "stretch some vectors and shrink others" — that's exactly what orthogonal transformations *don't* do → `orthogonal-matrices` |
| A1 | apply | numeric | 0.45 | Q=[[0,−1],[1,0]] (90° rotation). Verify QᵀQ=I. `[verified: identity]` | QᵀQ = [[1,0],[0,1]] ✓ | — |
| E1 | explain | derivation | 1.15 | Prove QᵀQ=I implies ‖Qx‖=‖x‖ for every x. | ‖Qx‖²=(Qx)ᵀ(Qx)=xᵀQᵀQx=xᵀx=‖x‖² *(required: the full substitution)* | — |
| T1 | transfer | short-answer | 1.65 | Why does numerical software strongly prefer orthogonal matrices for rotations, reflections, and basis changes? | Q⁻¹=Qᵀ makes inversion free (no matrix inversion algorithm needed), and E1 guarantees lengths and angles are preserved exactly — both a computational and a geometric advantage, and exactly why `eigendecomposition`'s rotation example was orthogonal to begin with despite having no real eigenvectors *(required: both the computational and geometric points)* | — |
| R3 | recall | mcq | −0.30 | For an orthogonal matrix Q, which identity always holds? | QᵀQ=QQᵀ=I | picks "Q²=I" — false in general (e.g. a 90° rotation squared is a 180° rotation, not the identity) → `orthogonal-matrices` |
| R4 | recall | short-answer | −0.20 | What is det(Q) for any orthogonal matrix Q? | ±1, since det(Q)²=det(QᵀQ)=det(I)=1 | — |
| R5 | recall | mcq | −0.10 | The set of orthogonal matrices with det=+1 (pure rotations, no reflections) is called: | the special orthogonal group, SO(n) | picks "the general linear group GL(n)" — that's the set of *all* invertible matrices, a much larger set → `orthogonal-matrices` |
| R6 | recall | short-answer | 0.0 | Define "orthonormal" as applied to a matrix's columns. | each column has unit length, and every pair of distinct columns has zero dot product | — |
| R7 | recall | mcq | −0.15 | True or false: the identity matrix I is orthogonal. | true — IᵀI=I | picks "false, only rotation matrices with a nonzero angle qualify" → `orthogonal-matrices` |
| R8 | recall | short-answer | 0.10 | True or false: a reflection matrix such as [[1,0],[0,−1]] is orthogonal. | true — its columns are still orthonormal, even though det=−1 | — |
| R9 | recall | mcq | −0.25 | The product Q₁Q₂ of two orthogonal matrices is: | also orthogonal | picks "not necessarily orthogonal" → `orthogonal-matrices` |
| R10 | recall | short-answer | 0.15 | What magnitude do the (possibly complex) eigenvalues of an orthogonal matrix always have? | magnitude exactly 1 | — |
| R11 | recall | mcq | −0.05 | For a square orthogonal matrix, do its *rows* also form an orthonormal set? | yes — Qᵀ is also orthogonal, and Qᵀ's columns are Q's rows | picks "no, only the columns are guaranteed" → `orthogonal-matrices` |
| R12 | recall | short-answer | 0.20 | Is a 1×1 orthogonal matrix restricted to particular values? | yes — it must be [1] or [−1], since q²=1 | — |
| R13 | recall | mcq | 0.05 | Which of the following is NOT guaranteed for an orthogonal matrix Q? | all entries of Q are nonnegative (false in general) | picks "Q⁻¹=Qᵀ" as the false one — that identity always holds → `orthogonal-matrices` |
| R14 | recall | short-answer | −0.40 | State the two equivalent conditions defining an orthogonal matrix Q, in terms of Qᵀ. | QᵀQ=I, equivalently Q⁻¹=Qᵀ | — |
| R15 | recall | mcq | 0.25 | A 90° rotation matrix Q, applied four times (Q⁴), returns to: | the identity matrix I | picks "a different rotation" — false, since 4×90°=360° | `orthogonal-matrices` |
| R16 | recall | short-answer | −0.35 | True or false: every orthogonal matrix (in the QᵀQ=I sense) is square. | true — this notion of orthogonality is only defined for square matrices | — |
| A2 | apply | numeric | 0.20 | Q=[[cos60°,−sin60°],[sin60°,cos60°]]. Compute det(Q). `[verified: 1]` | cos²60°+sin²60°=1, so det(Q)=1 | — |
| A3 | apply | numeric | 0.30 | Q=[[0,1],[1,0]]. Verify QᵀQ=I. `[verified: identity]` | QᵀQ=[[1,0],[0,1]] ✓ | — |
| A4 | apply | short-answer | 0.50 | Q=[[1,0],[0,−1]]. What is det(Q), and is Q a rotation or a reflection? | det(Q)=−1, so Q is a reflection, not in SO(2) | — |
| A5 | apply | numeric | 0.60 | For orthogonal Q with Qx=(3,4), find ‖x‖. `[verified: 5]` | ‖x‖=‖Qx‖=√(3²+4²)=5, by length preservation | — |
| A6 | apply | short-answer | 0.65 | Q₁ and Q₂ are both orthogonal 3×3 matrices. Is Q₁Q₂ orthogonal? Justify using R9's fact directly. | yes — (Q₁Q₂)ᵀ(Q₁Q₂)=Q₂ᵀQ₁ᵀQ₁Q₂=Q₂ᵀIQ₂=Q₂ᵀQ₂=I | — |
| E2 | explain | short-answer | 0.95 | Explain why det(Q)=±1 for any orthogonal Q, using det(QᵀQ)=det(I). | det(QᵀQ)=det(Qᵀ)det(Q)=det(Q)²; since QᵀQ=I, det(Q)²=det(I)=1, so det(Q)=±1 *(required: the det(Q)² derivation)* | — |
| E3 | explain | short-answer | 1.05 | Explain why every eigenvalue of an orthogonal matrix must have magnitude exactly 1. | if Qv=λv with v≠0, then ‖Qv‖=‖v‖ (length preservation) gives \|λ\|‖v‖=‖v‖, so \|λ\|=1 *(required: uses length preservation explicitly)* | — |
| E4 | explain | short-answer | 1.25 | Explain why columns that are merely orthogonal (not unit length) would break Q⁻¹=Qᵀ. | QᵀQ would then be diagonal but with entries equal to the squared column norms, not 1's — QᵀQ=I requires those norms to be exactly 1, so unit length is essential, not just perpendicularity *(required: the QᵀQ=diag(norms²) argument)* | — |
| E5 | explain | short-answer | 1.35 | Explain why Q⁻¹=Qᵀ makes orthogonal transformations especially cheap to undo in numerical software. | a general matrix inverse costs O(n³) (Gaussian elimination) and can be numerically unstable near singularity; for orthogonal Q the inverse is simply the transpose, an O(n²) operation, and Q is never close to singular since all its singular values equal 1 *(required: the cost-comparison argument)* | — |
| T2 | transfer | short-answer | 1.45 | Why does QR decomposition rely on Q being orthogonal rather than merely invertible? | Q⁻¹=Qᵀ makes solving Qy=b instantaneous (y=Qᵀb, no inversion needed), and length preservation means Q never worsens the numerical conditioning of the solve — an arbitrary invertible factor would offer neither property *(required: names both the free-inverse and conditioning benefits)* | — |
| T3 | transfer | short-answer | 1.85 | Why does computer graphics actively re-orthogonalize rotation matrices after repeated floating-point multiplication? | repeated floating-point products slowly drift away from exact orthogonality due to rounding error, and a drifted matrix stops preserving lengths and angles, introducing visible shearing/scaling artifacts; re-orthogonalization (e.g. via QR or Gram-Schmidt) restores QᵀQ=I so the transform stays a pure rotation *(required: names the drift mechanism and why re-orthogonalization fixes it)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.40…1.85.*

---

## Positive Definite Matrices (`positive-definite-matrices`)
*Prereq: Symmetric Matrices, Eigenvalues and Eigenvectors · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | Define positive definite. | symmetric A such that xᵀAx>0 for every nonzero x | — |
| R2 | recall | mcq | 0.15 | A symmetric matrix is positive definite if and only if: | all its eigenvalues are positive | claims "all its entries are positive" — false in general, and refuted directly by A1 → `positive-definite-matrices` |
| A1 | apply | numeric | 0.65 | Is A=[[2,−1],[−1,2]] positive definite? `[verified: eigenvalues 3,1, both positive]` | eigenvalues 3 and 1, both positive — yes, positive definite, *despite* having a negative off-diagonal entry, directly refuting R2's false alternative | rejects A as positive definite because of the negative entry → `positive-definite-matrices` |
| E1 | explain | short-answer | 1.35 | Why is every covariance matrix automatically positive *semi*-definite? | for any linear combination aᵀX of a random vector X, Var(aᵀX)=aᵀΣa, and variance can never be negative — so aᵀΣa≥0 for every a, which is exactly positive semi-definiteness *(required: the direct Var(aᵀX)=aᵀΣa identity)* — a genuine link to `covariance` in the probability sweep | — |
| T1 | transfer | short-answer | 1.85 | Why does strict positive *definiteness* (not just semi-) of a covariance matrix correspond to no exact linear relationship existing among the variables? | aᵀΣa=0 for some nonzero a means Var(aᵀX)=0, i.e. the linear combination aᵀX is (almost surely) a constant — an exact linear redundancy among the components; strict positive definiteness rules this out entirely, requiring every nontrivial linear combination to have genuine variance *(required: the Var=0 ⟹ constant argument)* | — |
| R3 | recall | short-answer | −0.30 | Define positive semi-definite. | symmetric A such that xᵀAx≥0 for every x (equality is allowed) | — |
| R4 | recall | mcq | −0.05 | A positive definite matrix's diagonal entries must be: | strictly positive (take x=eᵢ, giving xᵀAx=Aᵢᵢ>0) | picks "equal to each other" — no such requirement exists → `positive-definite-matrices` |
| R5 | recall | short-answer | 0.05 | State the relation between negative definite and positive definite matrices. | A is negative definite iff −A is positive definite | — |
| R6 | recall | mcq | −0.10 | An indefinite symmetric matrix is one whose eigenvalues are: | a mix of positive and negative values | picks "all exactly zero" — that describes the zero matrix, not a generic indefinite one → `positive-definite-matrices` |
| R7 | recall | short-answer | 0.20 | True or false: every positive definite matrix is invertible. | true — all eigenvalues are strictly positive, so none is zero, so det(A)≠0 | — |
| R8 | recall | mcq | −0.20 | A positive definite matrix's determinant is always: | strictly positive (the product of positive eigenvalues) | picks "could be zero" — a zero determinant would mean a zero eigenvalue, ruling out positive definiteness → `positive-definite-matrices` |
| R9 | recall | short-answer | 0.30 | Can a positive semi-definite matrix be singular? | yes — a zero eigenvalue is allowed for PSD, just not for strict PD | — |
| R10 | recall | mcq | 0.0 | Sylvester's criterion tests positive definiteness by checking that: | all leading principal minors are positive | picks "the trace is positive" — a positive trace is necessary but far from sufficient → `positive-definite-matrices` |
| R11 | recall | short-answer | −0.35 | True or false: the sum of two positive definite matrices is positive definite. | true — xᵀ(A+B)x=xᵀAx+xᵀBx, a sum of two strictly positive quantities | — |
| R12 | recall | mcq | 0.10 | A diagonal matrix with all positive diagonal entries is: | positive definite (xᵀAx=Σaᵢxᵢ² is a sum of positive terms) | picks "only positive semi-definite" — it's strictly definite since every term is strictly positive → `positive-definite-matrices` |
| R13 | recall | short-answer | −0.05 | State the symmetry requirement built into the definition of positive definite. | A must be symmetric (A=Aᵀ), not merely satisfy xᵀAx>0 | — |
| R14 | recall | mcq | 0.35 | A positive definite matrix's Cholesky factorization A=LLᵀ is unique once L's diagonal entries are required to be: | strictly positive | picks "either sign, since L is never unique" — the positive-diagonal convention is exactly what pins down uniqueness → `positive-definite-matrices` |
| R15 | recall | short-answer | −0.40 | Fill in the blank: a symmetric matrix A is positive definite if xᵀAx ___ 0 for every nonzero x. | > (strictly greater than) | — |
| R16 | recall | mcq | 0.45 | Which condition, on a candidate matrix's eigenvalues, certifies it is NOT positive definite? | any eigenvalue that is zero or negative | picks "a negative determinant only" — two negative eigenvalues give a *positive* determinant while the matrix is still not PD, so determinant sign alone is insufficient → `positive-definite-matrices` |
| A2 | apply | numeric | 0.40 | Is A=[[1,2],[2,1]] positive definite? `[verified: eigenvalues 3,-1]` | eigenvalues 3 and −1; since −1≤0, A is NOT positive definite | — |
| A3 | apply | numeric | 0.50 | Is A=[[5,0],[0,3]] positive definite? `[verified: eigenvalues 5,3]` | eigenvalues 5 and 3, both positive — yes, positive definite | — |
| A4 | apply | short-answer | 0.60 | Use Sylvester's criterion to check whether A=[[4,1],[1,3]] is positive definite. `[verified: leading minors 4, 11]` | leading minors are 4>0 and det=4·3−1·1=11>0 — both positive, so A is positive definite | — |
| A5 | apply | numeric | 0.75 | A=[[2,0],[0,−1]]. Is A positive semi-definite? `[verified: eigenvalues 2,-1]` | eigenvalues 2 and −1; since −1<0, A is not even PSD — it's indefinite | — |
| A6 | apply | numeric | 0.85 | A=[[0,0],[0,3]]. Positive definite or merely positive semi-definite? `[verified: eigenvalues 0,3]` | eigenvalues 0 and 3; the zero eigenvalue means A is only positive semi-definite, not positive definite | — |
| E2 | explain | short-answer | 1.15 | Explain why Sylvester's criterion (all leading principal minors positive) is equivalent to all eigenvalues being positive. | the minors test checks that the pivots produced by Gaussian elimination on a symmetric matrix (without row swaps) are all positive; those pivots and the leading submatrices' eigenvalue products track each other exactly, so a failure of either test shows up as a non-positive minor or a non-positive eigenvalue together *(required: names the minors-as-elimination-pivots connection)* | — |
| E3 | explain | short-answer | 1.25 | Explain why positive semi-definite but not positive definite corresponds to A having at least one zero eigenvalue. | writing xᵀAx in the eigenbasis expresses it as a weighted sum of squared coordinates, weighted by the (nonnegative, for PSD) eigenvalues; this sum can equal exactly 0 for some nonzero x only if x's component along every strictly-positive-eigenvalue direction vanishes, leaving only a zero-eigenvalue direction to carry x *(required: the weighted-sum-of-squares argument)* | — |
| E4 | explain | short-answer | 1.40 | Explain why a positive definite matrix cannot have a zero row or column. | if row/column i were entirely zero, then Aeᵢ=0, so xᵀAx=0 at x=eᵢ≠0, contradicting the strict positivity required for every nonzero x *(required: the x=eᵢ construction)* | — |
| E5 | explain | short-answer | 1.55 | Explain why positive diagonal entries alone are NOT sufficient to conclude a symmetric matrix is positive definite. | [[1,3],[3,1]] has all-positive diagonal entries yet its eigenvalues are 4 and −2 — not positive definite — showing R4's necessary condition is not sufficient; Sylvester's criterion or an eigenvalue check is needed *(required: a concrete counterexample with positive diagonal but non-PD)* | — |
| T2 | transfer | short-answer | 1.70 | Why must a valid kernel matrix in machine learning be positive semi-definite, and what breaks if it isn't? | a kernel matrix represents inner products in some feature space, xᵀAx=‖φ(x)‖²≥0 by construction; if it weren't PSD, algorithms like SVM or Gaussian processes that rely on this structure to guarantee a convex optimization problem or a valid covariance would break, potentially yielding negative "variances" or a non-convex objective *(required: the ‖φ(x)‖² inner-product argument and a concrete algorithmic consequence)* | — |
| T3 | transfer | short-answer | 2.05 | Why does Newton's method require the Hessian to be positive definite at a candidate point to certify a local minimum rather than a saddle point? | positive definiteness of the Hessian means xᵀHx>0 in every direction — the function curves upward along every direction from that point; a saddle point has an indefinite Hessian (per R6, a mix of positive and negative curvature directions), so Newton's method could otherwise step toward a saddle or maximum instead of a genuine minimum *(required: the every-direction-curves-upward argument tied to indefiniteness)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.40…2.05.*

---

## Cholesky Decomposition (`cholesky-decomposition`)
*Prereq: Positive Definite Matrices · ancestors 15 · b₀ = 0.89*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.11 | State the Cholesky decomposition and its requirement. | A=LLᵀ, L lower triangular; requires A symmetric and positive definite | — |
| R2 | recall | mcq | 0.19 | Cholesky decomposition requires: | a symmetric and positive definite matrix | claims "any square matrix" works — general LU decomposition, not Cholesky, applies there → `cholesky-decomposition` |
| A1 | apply | short-answer | 0.69 | Why is Cholesky roughly twice as fast as general LU decomposition on the same matrix? | symmetry means only about half the matrix's information is independent (the upper triangle mirrors the lower) — Cholesky exploits this redundancy directly, unlike general LU which treats every entry independently *(required)* | — |
| E1 | explain | derivation | 1.39 | Explain how Cholesky is used to generate correlated samples with target covariance Σ, and verify the covariance claim. | draw independent standard normal z, and transform as Lz where Σ=LLᵀ; Cov(Lz)=L·Cov(z)·Lᵀ=L·I·Lᵀ=LLᵀ=Σ exactly *(required: the explicit Cov(Lz) computation)* | — |
| T1 | transfer | short-answer | 1.89 | Why does Cholesky decomposition fail (attempting a square root of a negative number) if mistakenly applied to a matrix that isn't actually positive definite — and how is that failure itself useful? | a non-positive-definite matrix has at least one non-positive eigenvalue, which surfaces as an attempted square root of a negative quantity partway through the algorithm; this failure is a genuinely useful diagnostic that an estimated covariance matrix has a numerical error or isn't valid *(required: the diagnostic-value framing, not just "it errors out")* | — |
| R3 | recall | short-answer | −0.25 | What shape does L have in A=LLᵀ? | lower triangular | — |
| R4 | recall | mcq | 0.05 | The convention that makes the Cholesky factor L unique requires its diagonal entries to be: | strictly positive | picks "equal to 1" — that's the LDLᵀ convention, not Cholesky's → `cholesky-decomposition` |
| R5 | recall | short-answer | −0.15 | State the two-step procedure for solving Ax=b once A=LLᵀ is known. | first solve Ly=b by forward substitution, then solve Lᵀx=y by back substitution | — |
| R6 | recall | mcq | 0.15 | Cholesky decomposition is named after: | André-Louis Cholesky | picks "Carl Friedrich Gauss" — Gauss is associated with elimination and LU, not this decomposition → `cholesky-decomposition` |
| R7 | recall | short-answer | −0.30 | True or false: every symmetric positive definite matrix has a Cholesky decomposition. | true, and it is unique under the positive-diagonal convention | — |
| R8 | recall | mcq | 0.25 | Once L is known, forward and back substitution each cost: | O(n²) operations | picks "O(n³), the same as computing L itself" — triangular solves are much cheaper than the factorization → `cholesky-decomposition` |
| R9 | recall | short-answer | 0.0 | What goes wrong, mechanically, if Cholesky is attempted on a matrix that is not positive definite? | at some step the algorithm needs the square root of a negative number — a diagonal entry computed as Aᵢᵢ minus a sum turns out negative | — |
| R10 | recall | mcq | −0.05 | Compared to computing the full inverse A⁻¹, using Cholesky factors to solve Ax=b for one b is: | cheaper — it avoids ever forming the full inverse, needing only two triangular solves | picks "always slower, since two solves are needed instead of one" → `cholesky-decomposition` |
| R11 | recall | short-answer | 0.30 | Give the formula for L₁₁, the first diagonal entry of A's Cholesky factor, in terms of A₁₁. | L₁₁=√(A₁₁) | — |
| R12 | recall | mcq | −0.20 | For a positive definite A, is Cholesky guaranteed to succeed without ever hitting a negative number under a square root? | yes — positive definiteness is exactly the condition guaranteeing this | picks "no, it can still fail unpredictably" → `cholesky-decomposition` |
| R13 | recall | short-answer | 0.35 | State the upper-triangular variant A=UᵀU and its relation to A=LLᵀ. | U=Lᵀ — the same decomposition, written with the triangular factor on the opposite side | — |
| R14 | recall | mcq | 0.10 | Cholesky is a standard tool for sampling from a multivariate normal because: | it provides L with Σ=LLᵀ, used to transform independent standard normal draws into correlated ones | picks "it computes the mean vector directly" — Cholesky has nothing to do with the mean → `cholesky-decomposition` |
| R15 | recall | short-answer | −0.35 | True or false: the Cholesky factor L is unique for a given positive definite A, once the positive-diagonal convention is fixed. | true | — |
| R16 | recall | mcq | 0.40 | Relative to computing the full eigendecomposition of A, computing the Cholesky factorization is typically: | significantly cheaper, since it never needs to find eigenvalues or eigenvectors | picks "the same cost, since both require O(n³) work with identical constants" → `cholesky-decomposition` |
| A2 | apply | numeric | 0.40 | A=[[4,0],[0,9]]. Find its Cholesky factor L. `[verified: diag(2,3)]` | L=diag(2,3), since L₁₁=√4=2 and L₂₂=√9=3 | — |
| A3 | apply | short-answer | 0.50 | A=[[9,3],[3,5]]. Compute the first column of L: L₁₁ and L₂₁. `[verified: L11=3, L21=1]` | L₁₁=√9=3; L₂₁=A₂₁/L₁₁=3/3=1 | — |
| A4 | apply | numeric | 0.60 | Continuing A3 (A₂₂=5), compute L₂₂ using L₂₂=√(A₂₂−L₂₁²). `[verified: 2]` | √(5−1²)=√4=2 | — |
| A5 | apply | numeric | 0.75 | Given L=[[2,0],[1,3]], reconstruct A=LLᵀ. `[verified: [[4,2],[2,10]]]` | A=[[4,2],[2,10]] | — |
| A6 | apply | short-answer | 0.85 | You must solve Ax=b for 500 different b's, same A each time. Why compute the Cholesky factorization once up front rather than solving from scratch each time? | computing L costs O(n³) once, but each subsequent solve via two triangular substitutions costs only O(n²) — amortizing the O(n³) cost over 500 solves beats paying that cost 500 times | — |
| E2 | explain | short-answer | 1.15 | Explain, mechanically, why Cholesky's ~O(n³/3) cost is roughly half of general LU's ~O(2n³/3). | general LU treats the upper and lower triangles as independent information; Cholesky exploits A's symmetry, computing only L's lower triangle and deriving everything else from A=LLᵀ, so roughly half the arithmetic is redundant and skipped *(required: names symmetry as the source of the skipped work)* | — |
| E3 | explain | short-answer | 1.25 | Explain why the recursive formula Lᵢᵢ=√(Aᵢᵢ−Σⱼ<ᵢLᵢⱼ²) can only be carried out safely when A is positive definite. | the quantity under each square root is exactly the Schur-complement-reduced diagonal entry of the remaining submatrix, and positive definiteness guarantees every leading submatrix of A is itself positive definite, hence has a positive reduced diagonal — essentially Sylvester's criterion applied one step at a time *(required: connects to Sylvester's criterion / leading submatrices)* | — |
| E4 | explain | short-answer | 1.40 | Explain why Cholesky-based solving is numerically more stable than first computing A⁻¹ explicitly and multiplying by b. | forming A⁻¹ explicitly and multiplying introduces extra rounding error that can be amplified when A is close to singular; solving via two triangular substitutions directly, without ever forming an inverse, avoids that extra error source and is the standard numerical-linear-algebra recommendation *(required: names avoiding the explicit inverse as the source of the improved stability)* | — |
| E5 | explain | short-answer | 1.55 | Explain why attempting Cholesky is sometimes used deliberately as a computational positive-definiteness test, rather than computing all the eigenvalues. | per R9, Cholesky fails precisely (and only) when positive definiteness is violated at some step, and computing L costs O(n³/3) versus a full eigendecomposition's higher constant-factor cost — attempting Cholesky and checking for failure is a cheaper yes/no test than extracting the whole spectrum *(required: the cost-comparison plus "fails iff not PD" argument)* | — |
| T2 | transfer | short-answer | 1.70 | Why does Gaussian process regression rely on repeated Cholesky factorizations of the kernel matrix, and why does this become the computational bottleneck for large datasets? | GP regression solves linear systems involving the n×n kernel matrix for both the posterior mean and the marginal likelihood used to fit hyperparameters, and Cholesky is the standard stable way to do this; but computing L costs O(n³), so as n grows this single factorization dominates runtime, which is why GPs are traditionally infeasible beyond a few thousand points *(required: names both why Cholesky is used and why its O(n³) cost becomes the bottleneck)* | — |
| T3 | transfer | short-answer | 2.10 | An estimated covariance matrix sometimes fails Cholesky due to tiny negative eigenvalues from rounding error, even though it should be PSD in theory. Why is adding "jitter" (A+εI) a principled fix rather than a hack? | adding εI shifts every eigenvalue of A up by exactly ε, since (A+εI)v=Av+εv=(λ+ε)v for every eigenvector v — any eigenvalue that was zero or slightly negative due to rounding becomes strictly positive for small enough ε>0, restoring positive definiteness with a provably bounded, controllable perturbation rather than an arbitrary correction *(required: the eigenvalue-shift derivation, not just "it works in practice")* | — |

*Coverage: 16/6/5/3 — 30 items, −0.35…2.10.*

---

## Schur Complement (`schur-complement`)
*Prereq: Invertible Matrices · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | Define the Schur complement of D in the block matrix [[A,B],[C,D]]. | A − BD⁻¹C, assuming D is invertible | — |
| R2 | recall | mcq | 0.04 | The Schur complement is primarily useful for: | computing determinants and inverses of block matrices efficiently, by reducing to smaller sub-blocks | picks "finding eigenvalues directly" — an unrelated use → `schur-complement` |
| A1 | apply | short-answer | 0.54 | State the block-determinant identity using the Schur complement. | det([[A,B],[C,D]]) = det(D)·det(A−BD⁻¹C), for invertible D | — |
| E1 | explain | short-answer | 1.24 | Connect the Schur complement to conditional distributions of a jointly Gaussian vector. | for a jointly Gaussian vector split into two blocks, the conditional covariance of one block given the other is *exactly* the Schur complement of the other block's covariance submatrix — a purely algebraic block-matrix tool that turns out to compute `conditional-distribution`'s conditional covariance directly *(required: names this as an exact identity, not an analogy)* | — |
| T1 | transfer | short-answer | 1.74 | Why does "conditioning reduces uncertainty" (a natural intuition from `conditional-distribution`) show up in the Schur complement formula as subtracting a nonnegative term? | BD⁻¹C represents how much the other blocks predict about A; subtracting it from A can only shrink (never grow) A's effective spread, so the conditional covariance is never larger than the unconditional one — the algebra directly encodes the intuition *(required: the "subtracting a nonnegative term can only shrink" argument)* | — |
| R3 | recall | short-answer | −0.36 | Define the Schur complement of A (rather than D) in [[A,B],[C,D]]. | D − CA⁻¹B, assuming A is invertible | — |
| R4 | recall | mcq | −0.06 | The block-matrix determinant identity, using the Schur complement of A, is: | det([[A,B],[C,D]]) = det(A)·det(D−CA⁻¹B) | picks "det(A)+det(D−CA⁻¹B)" — determinants of block-triangular forms multiply, they don't add → `schur-complement` |
| R5 | recall | short-answer | 0.14 | What condition on D is required for the Schur complement A−BD⁻¹C to be defined? | D must be invertible | — |
| R6 | recall | mcq | −0.16 | The Schur complement is named after: | Issai Schur | picks "Carl Friedrich Gauss" — Gauss is associated with elimination, not this specific construction → `schur-complement` |
| R7 | recall | short-answer | 0.24 | If D=I, what does the Schur complement of D, A−BD⁻¹C, simplify to? | A − BC | — |
| R8 | recall | mcq | −0.26 | For a symmetric block matrix [[A,B],[Bᵀ,D]], the Schur complement A−BD⁻¹Bᵀ is: | also symmetric | picks "generally not symmetric, even though the block matrix is" → `schur-complement` |
| R9 | recall | short-answer | 0.34 | True or false: the Schur complement is what block-wise Gaussian elimination produces when eliminating one block using another. | true | — |
| R10 | recall | mcq | 0.04 | The Schur complement plays a central role in which identity for cheaply updating (A+UCV)⁻¹? | the Woodbury matrix identity | picks "the Spectral Theorem" — unrelated to low-rank inverse updates → `schur-complement` |
| R11 | recall | short-answer | −0.46 | State, in words, the connection between the block-matrix inverse formula and the Schur complement. | the inverse of a block matrix can be written entirely in terms of the original blocks and the Schur complement of one of the diagonal blocks | — |
| R12 | recall | mcq | 0.19 | A block matrix [[A,B],[Bᵀ,D]] is positive definite if and only if: | D (say) is positive definite AND its Schur complement A−BD⁻¹Bᵀ is positive definite | picks "A and D are each positive definite, with no condition on B" — the cross-term B genuinely matters → `schur-complement` |
| R13 | recall | short-answer | −0.11 | Are the Schur complement of D and the Schur complement of A, in [[A,B],[C,D]], the same quantity? | no — they are generally different (A−BD⁻¹C versus D−CA⁻¹B), each inverting a different block, though linked by the same determinant identity | — |
| R14 | recall | mcq | 0.29 | Schur complements arise in recursive/online least squares because they let you: | update a solution incrementally as new data blocks arrive, without re-solving the whole system from scratch | picks "eliminate the need for matrix inversion altogether" — an inverse of some block is still required → `schur-complement` |
| R15 | recall | short-answer | −0.31 | Fill in the blank: the Schur complement of D in [[A,B],[C,D]] is A − B ___ C. | D⁻¹ | — |
| R16 | recall | mcq | 0.39 | Computing the Schur complement of D requires inverting: | only the D block, potentially much smaller than the full block matrix | picks "the entire block matrix first" — defeating the purpose of the shortcut → `schur-complement` |
| A2 | apply | numeric | 0.30 | A=2 (1×1), B=[1,0], C=Bᵀ, D=I₂. Compute the Schur complement of D, A−BD⁻¹C. `[verified: 1]` | BD⁻¹C=B·I·Bᵀ=1²+0²=1; Schur complement = 2−1=1 | — |
| A3 | apply | numeric | 0.40 | A=4, D=3, B=C=2 (all 1×1). Compute the Schur complement of D. `[verified: 8/3]` | 4 − (2·(1/3)·2) = 4 − 4/3 = 8/3 | — |
| A4 | apply | numeric | 0.55 | Using A3's numbers, compute det([[4,2],[2,3]]) via det(D)·(Schur complement of D). `[verified: 8]` | 3 · (8/3) = 8; matches the direct computation 4·3−2·2=8 | — |
| A5 | apply | short-answer | 0.65 | A jointly Gaussian (X,Y) has Σ_XX=4, Σ_XY=Σ_YX=2, Σ_YY=3. Using the Schur complement, state Var(X\|Y). `[verified: 8/3]` | Var(X\|Y)=Σ_XX−Σ_XYΣ_YY⁻¹Σ_YX=4−2·(1/3)·2=8/3, the same Schur complement computed in A3 | — |
| A6 | apply | short-answer | 0.78 | For [[A,B],[C,D]] with D 100×100 and A 2×2, which Schur complement is cheaper to compute, and why? | the Schur complement of A (D−CA⁻¹B), since it only requires inverting the small 2×2 block A, unlike the Schur complement of D, which would require inverting the large 100×100 block | — |
| E2 | explain | short-answer | 1.05 | Explain why A−BD⁻¹C appears naturally when you eliminate the C,D block via block Gaussian elimination. | a block row operation that subtracts BD⁻¹ times the bottom block-row from the top block-row zeros out B's counterpart there, replacing A with A−BD⁻¹C in the top-left — exactly standard elimination, carried out with matrix blocks instead of scalar entries *(required: the block-row-elimination mechanism)* | — |
| E3 | explain | short-answer | 1.15 | Explain why det([[A,B],[C,D]])=det(D)·det(A−BD⁻¹C) follows from block elimination rather than being a separate fact to memorize. | block elimination (E2) transforms the matrix into block-upper-triangular form with diagonal blocks D and A−BD⁻¹C, via determinant-preserving row operations; a block-triangular matrix's determinant is the product of its diagonal blocks' determinants, giving the identity directly *(required: connects to E2's elimination and the block-triangular determinant fact)* | — |
| E4 | explain | short-answer | 1.30 | Explain why positive definiteness of [[A,B],[Bᵀ,D]] requires the Schur complement A−BD⁻¹Bᵀ to be positive definite, not just A and D individually. | block elimination expresses the full quadratic form as a sum involving A−BD⁻¹Bᵀ acting on a transformed variable plus a term in D alone; a non-positive direction in the Schur complement could make the full quadratic form non-positive along a corresponding combined direction, even with A and D each individually positive definite *(required: the transformed-quadratic-form argument, not just citing the fact)* | — |
| E5 | explain | short-answer | 1.45 | Explain why A−BD⁻¹C can be viewed as "A, with the part explainable by D removed." | BD⁻¹C represents how much of A's structure is predictable from, or redundant with, D through the cross terms B and C; subtracting it leaves the part of A carrying genuinely new information beyond D — the same intuition that makes it equal the conditional covariance in the Gaussian case (E1) *(required: the "predictable-from-D" framing, connecting to E1)* | — |
| T2 | transfer | short-answer | 1.55 | Why does the Schur complement give a computational shortcut for updating a regression's coefficients when a new predictor is added, rather than refitting from scratch? | adding one new column to X extends XᵀX by one row/column; the new coefficient and the update to the existing ones can be expressed via the Schur complement of the small new block against the already-computed (XᵀX)⁻¹ for the old predictors, reusing the old inverse instead of recomputing a fresh one from scratch *(required: names reusing the old inverse via the Schur complement)* | — |
| T3 | transfer | short-answer | 1.90 | Why is the Schur complement central to the Woodbury matrix identity, which cheaply updates a matrix inverse after a low-rank change? | the Woodbury identity (A+UCV)⁻¹=A⁻¹−A⁻¹U(C⁻¹+VA⁻¹U)⁻¹VA⁻¹ is derived by writing the low-rank-updated system as a block matrix and applying block elimination; the small matrix C⁻¹+VA⁻¹U being inverted in the middle *is* a Schur complement, so the whole identity is block elimination in disguise, letting the update be computed by inverting only a small matrix instead of the full updated A+UCV *(required: identifies the middle term as a Schur complement and the block-elimination derivation)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.46…1.90.*

---

## Rayleigh Quotient (`rayleigh-quotient`)
*Prereq: Symmetric Matrices, Eigenvalues and Eigenvectors · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | Define the Rayleigh quotient. | R(x) = (xᵀAx)/(xᵀx), for symmetric A and nonzero x | — |
| R2 | recall | mcq | 0.15 | The maximum of R(x) over all nonzero x equals: | the largest eigenvalue of A | picks "the trace of A" — trace is the *sum* of eigenvalues, not the max → `rayleigh-quotient` |
| A1 | apply | numeric | 0.65 | A=diag(2,5). Compute R(x) for x=(1,0), (0,1), (1,1); which is largest? `[verified: 2, 5, 3.5]` | R(1,0)=2, R(0,1)=5, R(1,1)=3.5 — the maximum among these matches the largest eigenvalue, 5, achieved at its eigenvector | — |
| E1 | explain | short-answer | 1.35 | Why is the maximum of R(x) achieved exactly at the largest eigenvalue's eigenvector? | writing x in the eigenbasis, xᵀAx becomes a weighted sum of eigenvalues (weighted by squared coordinates), making R(x) a weighted average of the eigenvalues; this average is maximized by putting all the weight on the single largest eigenvalue's direction *(required: the weighted-average argument)* | — |
| T1 | transfer | short-answer | 1.85 | Why is PCA's first principal component found by maximizing the Rayleigh quotient of the data's covariance matrix? | the direction of maximum variance is exactly the direction maximizing xᵀΣx/xᵀx — this is literally the Rayleigh quotient maximization from E1, applied to Σ, not merely an analogous problem *(required: states this as the identical mechanism, not a parallel one)* | — |
| R3 | recall | short-answer | −0.31 | State why R(x) is scale-invariant: R(cx)=R(x) for any nonzero scalar c. | numerator and denominator both scale by c², which cancels | — |
| R4 | recall | mcq | −0.06 | The minimum of R(x) over all nonzero x equals: | the smallest eigenvalue of A | picks "the negative of the largest eigenvalue" — unrelated to the actual minimum → `rayleigh-quotient` |
| R5 | recall | short-answer | 0.05 | If x=qᵢ, an eigenvector of A with eigenvalue λᵢ, what does R(qᵢ) equal exactly? | R(qᵢ)=λᵢ exactly | — |
| R6 | recall | mcq | −0.21 | For every nonzero x, R(x) always lies: | between the smallest and largest eigenvalues of A, inclusive | picks "strictly between the two extremes, never equal to either" — false, since R equals λ_min or λ_max exactly at the corresponding eigenvector → `rayleigh-quotient` |
| R7 | recall | short-answer | 0.20 | What requirement on A guarantees R(x) is real for every real x AND that its extremes equal actual eigenvalues? | A must be symmetric, guaranteeing real eigenvalues via the Spectral Theorem | — |
| R8 | recall | mcq | −0.11 | The Rayleigh quotient underlies which classical iterative algorithm for estimating an eigenvalue/eigenvector pair? | Rayleigh quotient iteration (related to power iteration) | picks "Gaussian elimination" — unrelated to eigenvalue estimation → `rayleigh-quotient` |
| R9 | recall | short-answer | 0.30 | True or false: maximizing R(x) is typically restated as maximizing xᵀAx subject to ‖x‖=1. | true — this removes the scale-invariance redundancy while keeping the same extremal values | — |
| R10 | recall | mcq | −0.36 | For a non-symmetric real A, what specifically breaks down about the Rayleigh quotient's usual guarantees? | its extreme values are no longer guaranteed to equal actual eigenvalues of A | picks "R(x) is no longer guaranteed to be a real number" — R(x)=xᵀAx/xᵀx is still real for any real x and real A regardless of symmetry → `rayleigh-quotient` |
| R11 | recall | short-answer | 0.35 | Fill in the blank: the Rayleigh quotient is defined as R(x) = ___ / ___. | xᵀAx / xᵀx | — |
| R12 | recall | mcq | −0.16 | As x ranges over all nonzero vectors, the set of achievable R(x) values forms: | the closed interval [λ_min, λ_max] | picks "a discrete set of exactly n values, one per eigenvalue" — R is continuous and fills the whole interval, not just the eigenvalues → `rayleigh-quotient` |
| R13 | recall | short-answer | 0.10 | What is R(x) called when x is restricted to a unit vector? | still the Rayleigh quotient, now simply xᵀAx directly, since the denominator is 1 | — |
| R14 | recall | mcq | −0.26 | The Rayleigh quotient of the identity matrix I, R(x)=xᵀIx/xᵀx, equals for every nonzero x: | exactly 1 | picks "varies with x, since I still has directions" — it is constant at 1 regardless of x → `rayleigh-quotient` |
| R15 | recall | short-answer | 0.40 | True or false: R(x) for symmetric A is a continuous function of x away from x=0. | true — it's a ratio of polynomials in x's entries, continuous wherever xᵀx≠0 | — |
| R16 | recall | mcq | −0.01 | Restricting to vectors orthogonal to the top eigenvector q₁, the maximum of R(x) over that restricted set equals: | the second-largest eigenvalue, λ₂ | picks "still the largest eigenvalue, λ₁" — excluding q₁'s direction is exactly what rules that value out → `rayleigh-quotient` |
| A2 | apply | numeric | 0.40 | A=diag(1,6). Compute R(x) for x=(1,1). `[verified: 3.5]` | R=(1·1²+6·1²)/(1²+1²)=7/2=3.5 | — |
| A3 | apply | numeric | 0.50 | A=diag(2,8). Compute R(x) for x=(2,0) and x=(0,3). `[verified: 2, 8]` | R(2,0)=2, R(0,3)=8 — both match an eigenvalue exactly, per R5 | — |
| A4 | apply | numeric | 0.60 | A=diag(3,9). Verify R(1,1)=R(2,2). `[verified: 6, 6]` | R(1,1)=(3+9)/2=6; R(2,2)=(3·4+9·4)/8=48/8=6 — confirms scale-invariance | — |
| A5 | apply | short-answer | 0.75 | A=diag(1,4,9). What is the minimum possible value of R(x), and at which x is it achieved? | minimum is 1 (the smallest eigenvalue), achieved at x=(1,0,0) or any scalar multiple | — |
| A6 | apply | numeric | 0.85 | A=diag(5,5,1). Compute R(x) for x=(1,1,0). `[verified: 5]` | R=(5+5+0)/2=5 — matches the repeated top eigenvalue, achieved throughout that 2D eigenspace | — |
| E2 | explain | short-answer | 1.10 | Explain why R(x) is scale-invariant, using the eigenbasis expansion from E1. | writing x=Σcᵢqᵢ, R(x)=Σλᵢcᵢ²/Σcᵢ² depends only on the *ratios* of the cᵢ², which are unchanged when x scales by c — every cᵢ scales by c, and c² cancels top and bottom *(required: the ratio-of-cᵢ² argument, building on E1's weighted-average form)* | — |
| E3 | explain | short-answer | 1.20 | Explain why restricting the maximization to ‖x‖=1 loses no information compared to maximizing over all nonzero x. | since R is scale-invariant (R3/E2), every nonzero x shares its R-value with its normalization x/‖x‖, which does have unit norm — the set of achievable R-values is identical either way, only the parameterization changes *(required: connects explicitly to scale-invariance)* | — |
| E4 | explain | short-answer | 1.35 | Explain why the minimum of R(x) equals the smallest eigenvalue, adapting E1's weighted-average argument to a minimum. | R(x) is always a weighted average of A's eigenvalues, weighted by squared eigenbasis coordinates (E1); a weighted average can be no smaller than the minimum of the values being averaged, achieved exactly when all the weight sits on the minimizing eigenvalue's coordinate *(required: explicitly adapts the weighted-average logic to the minimum case)* | — |
| E5 | explain | short-answer | 1.50 | Explain why maximizing R(x) subject to x⊥q₁ yields the second-largest eigenvalue λ₂, not λ₁ again. | forcing x⊥q₁ zeroes out q₁'s coordinate in the eigenbasis expansion (c₁=0), so R(x) becomes a weighted average using only λ₂,...,λₙ — reapplying the weighted-average logic (E1) then puts the maximum at λ₂, the largest of what remains *(required: the c₁=0 mechanism plus reapplying the weighted-average argument)* | — |
| T2 | transfer | short-answer | 1.70 | Why does power iteration for the top eigenvector implicitly rely on the Rayleigh quotient's maximization property? | power iteration repeatedly applies A and renormalizes; the iterate drifts toward the top eigenvector because that direction grows fastest under repeated multiplication (`matrix-stability`'s dominant-eigenvalue argument) — evaluating the Rayleigh quotient of the current iterate gives an increasingly accurate estimate of λ_max exactly because the iterate is converging toward the maximizer characterized in E1/T1 *(required: connects power iteration's convergence to the Rayleigh-quotient-maximizer characterization)* | — |
| T3 | transfer | short-answer | 2.05 | Why is PCA's variance maximization equivalent to finding a *stationary point* of the Rayleigh quotient, and why does that matter for finding every principal component, not only the first? | setting the gradient of R(x)=xᵀΣx/xᵀx to zero yields exactly the eigenvector equation Σx=λx, so every eigenvector of Σ — not only the top one — is a stationary point of R; the top eigenvector is R's global maximum (the first PC), while the remaining eigenvectors are saddle points corresponding to the second, third, … PCs, which is why all principal components can be characterized via the same Rayleigh quotient *(required: the gradient-equals-eigenvector-equation derivation and the saddle-point extension to later PCs)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.36…2.05.*

---

## Matrix Stability (`matrix-stability`)
*Prereq: Eigenvalues and Eigenvectors, Matrix Norms · ancestors 16 · b₀ = 0.92*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.08 | Describe what "stability" means for the iteration x_{k+1}=Ax_k. | whether repeated application of A drives x_k to zero, keeps it bounded, or blows up, as k→∞ — governed by A's eigenvalues | — |
| R2 | recall | mcq | 0.22 | This iteration converges to zero for *any* starting x₀ if and only if: | all eigenvalues of A have magnitude less than 1 (spectral radius <1) | picks "det(A)<1" — the determinant is a product of eigenvalues, and can be small even with one large eigenvalue present → `matrix-stability` |
| A1 | apply | short-answer | 0.72 | A=diag(0.5, 1.5). Does the iteration converge to zero for a generic starting vector? | no — the component along the eigenvalue-1.5 direction grows without bound, even though the eigenvalue-0.5 component shrinks to zero; the largest eigenvalue magnitude (the spectral radius) governs long-run behavior | assumes convergence because "most" eigenvalues (or the average) are below 1 → `matrix-stability` |
| E1 | explain | derivation | 1.42 | Using x_k=A^kx₀=PD^kP⁻¹x₀, explain why the largest eigenvalue magnitude eventually dominates. | D^k raises each diagonal entry to the k-th power; whichever eigenvalue has the largest magnitude grows or shrinks slower relative to the others and eventually dominates every other term as k grows, regardless of the initial mixture (unless that direction's component in x₀ happens to be exactly zero) *(required: the term-domination argument via D^k)* | — |
| T1 | transfer | short-answer | 1.92 | Why do engineers check the spectral radius when analyzing whether a control system or iterative algorithm stays stable over many iterations? Connect to `matrix-norms`' operator norm. | the spectral radius is exactly what governs long-run growth or decay under repeated application, per E1; the operator norm is always an upper bound on the spectral radius (though not always equal for non-symmetric matrices), so a small operator norm gives a safe, if sometimes loose, stability guarantee when the exact spectral radius is hard to compute *(required: the operator-norm-as-upper-bound relationship)* | — |
| R3 | recall | short-answer | −0.23 | Define the spectral radius ρ(A). | the largest magnitude among A's (possibly complex) eigenvalues, ρ(A)=max\|λᵢ\| | — |
| R4 | recall | mcq | 0.02 | If ρ(A)>1, the iteration x_{k+1}=Ax_k, for a generic starting x₀, will: | diverge (‖x_k‖→∞) | picks "converge to zero" — the opposite of what ρ(A)>1 implies → `matrix-stability` |
| R5 | recall | short-answer | −0.13 | What is the fixed point of x_{k+1}=Ax_k, for any matrix A? | the zero vector — A·0=0 always | — |
| R6 | recall | mcq | 0.12 | The eigenvalue with the largest magnitude is called the: | dominant eigenvalue | picks "principal eigenvalue's inverse" — not a real term → `matrix-stability` |
| R7 | recall | short-answer | −0.03 | True or false: eigenvalues used in stability analysis can be complex numbers. | true — magnitude is then the complex modulus \|a+bi\|=√(a²+b²) | — |
| R8 | recall | mcq | 0.27 | If ρ(A)=1 exactly (A diagonalizable), the iteration x_k=A^kx₀ typically: | stays bounded, neither growing nor decaying in the dominant direction | picks "always converges to zero eventually" — ρ(A)=1 does not force decay → `matrix-stability` |
| R9 | recall | short-answer | −0.18 | Fill in the blank: x_{k+1}=Ax_k converges to zero for every starting x₀ iff ρ(A) ___ 1. | < (strictly less than) | — |
| R10 | recall | mcq | 0.17 | For a continuous-time system ẋ=Ax, stability is instead governed by: | whether all eigenvalues of A have negative real part | picks "the same magnitude-less-than-1 condition as the discrete case" — the continuous-time criterion is on the real part, not magnitude → `matrix-stability` |
| R11 | recall | short-answer | 0.32 | What does "marginally stable" mean for this iteration? | bounded but not decaying to zero — typically occurring when ρ(A)=1 | — |
| R12 | recall | mcq | −0.28 | The operator norm ‖A‖ relates to the spectral radius ρ(A) by: | ρ(A) ≤ ‖A‖ always | picks "ρ(A)=‖A‖ always, for every matrix" — equality is only guaranteed for special cases like symmetric matrices → `matrix-stability` |
| R13 | recall | short-answer | 0.07 | State x_k=A^kx₀ in terms of the eigendecomposition A=PDP⁻¹. | x_k = PD^kP⁻¹x₀ | — |
| R14 | recall | mcq | −0.08 | A diagonal matrix A=diag(0.9, 1.1) has spectral radius: | 1.1, the largest magnitude entry | picks "1.0, the average of the two" — spectral radius is a max, not an average → `matrix-stability` |
| R15 | recall | short-answer | 0.37 | True or false: if A itself is the zero matrix, x_{k+1}=Ax_k reaches the zero vector after just one step, for any x₀. | true — Ax₀=0 immediately for every x₀ | — |
| R16 | recall | mcq | −0.33 | For a real matrix with a complex eigenvalue pair a±bi, the relevant magnitude for stability is: | √(a²+b²), the modulus of the complex number | picks "just \|a\|, ignoring the imaginary part" — ignores the oscillatory bi component entirely → `matrix-stability` |
| A2 | apply | numeric | 0.50 | A=diag(0.8, 0.3). What is ρ(A)? `[verified: 0.8]` | 0.8, the larger-magnitude entry | — |
| A3 | apply | short-answer | 0.60 | A=diag(0.8,0.3). Does x_{k+1}=Ax_k converge to zero for any starting x₀? | yes — ρ(A)=0.8<1, so per R9 it converges to zero regardless of x₀ | — |
| A4 | apply | numeric | 0.68 | A=diag(1,0.5). What is ρ(A), and does the iteration converge to zero? `[verified: rho=1, no convergence to zero]` | ρ(A)=1; since ρ(A) is not strictly less than 1, the iteration does NOT converge to zero — the component along eigenvalue 1 stays constant forever | — |
| A5 | apply | numeric | 0.85 | A has eigenvalues 2+i and 2−i. Compute ρ(A). `[verified: √5≈2.236]` | ρ(A)=\|2+i\|=√(4+1)=√5≈2.236 | — |
| A6 | apply | short-answer | 0.95 | A continuous-time system ẋ=Ax has eigenvalues −1 and 3. Is it stable? What about the discrete iteration with the same A? | continuous-time is unstable (eigenvalue 3 has positive real part, per R10); the discrete iteration is also unstable since ρ(A)=3>1 — both diverge here, though the criteria differ in general | — |
| E2 | explain | short-answer | 1.22 | Explain, using x_k=PD^kP⁻¹x₀ (R13), exactly why the dominant eigenvalue eventually dominates every other term. | D^k has entries λᵢᵏ; factoring out the dominant λ_max leaves λ_maxᵏ·(1 plus terms shrinking like (λᵢ/λ_max)ᵏ for every other eigenvalue, each ratio having magnitude strictly less than 1) — every subordinate term vanishes relative to the dominant one as k→∞ *(required: the explicit factoring-out and ratio-shrinking argument)* | — |
| E3 | explain | short-answer | 1.35 | Explain why ρ(A)≤‖A‖ always holds, using the definition of the operator norm. | the operator norm satisfies ‖Ax‖≤‖A‖‖x‖ for every x; applying this to an eigenvector v with Av=λv gives ‖λv‖=\|λ\|‖v‖≤‖A‖‖v‖, so \|λ\|≤‖A‖ for every eigenvalue, hence ρ(A)≤‖A‖ *(required: the explicit eigenvector substitution into the operator-norm inequality)* | — |
| E4 | explain | short-answer | 1.50 | Explain why ρ(A)=1 is a genuinely ambiguous boundary case, rather than simply "stable." | at ρ(A)=1 the dominant term in x_k=PD^kP⁻¹x₀ neither grows nor decays (\|λ_max\|ᵏ=1ᵏ=1 for all k), so long-run behavior depends entirely on finer structure (e.g. whether A is diagonalizable at that eigenvalue) — the magnitude-based criterion that cleanly separates convergence from divergence elsewhere gives no clean answer exactly at the boundary *(required: names why the criterion becomes uninformative exactly at ρ=1)* | — |
| E5 | explain | short-answer | 1.65 | Explain why complex eigenvalues of a real matrix A still produce genuinely real trajectories x_k. | complex eigenvalues of a real matrix always occur in conjugate pairs a±bi with correspondingly conjugate eigenvectors; for real x₀ the contributions from the conjugate pair combine via conjugate coefficients to cancel their imaginary parts exactly, leaving a real (often oscillatory, spiraling) trajectory *(required: the conjugate-pair-cancellation argument)* | — |
| T2 | transfer | short-answer | 1.80 | Why do control engineers design feedback systems with spectral radius strictly less than 1, with margin, rather than aiming for exactly ρ(A)=1? | per E4, ρ(A)=1 is a knife-edge case where tiny model errors, numerical rounding, or real-world disturbances could push the true system into convergence or divergence unpredictably; a safety margin below 1 guarantees robust convergence even under modeling uncertainty, echoing `matrix-norms`' operator-norm safety-margin argument from T1 *(required: connects the knife-edge risk from E4 to the practical need for a margin)* | — |
| T3 | transfer | short-answer | 2.15 | Why does training a deep neural network with many stacked layers risk "exploding" or "vanishing" activations/gradients, connecting to the spectral radius of each layer's weight matrix? | passing a signal through many layers is analogous to repeatedly applying each layer's weight matrix, similar to x_k=A^kx₀; weight matrices with spectral radii consistently above 1 grow the signal (or backpropagated gradient) exponentially with depth (exploding), while radii consistently below 1 shrink it toward zero (vanishing) — this is exactly why weight-initialization schemes try to keep each layer's effective spectral radius near 1 *(required: the explicit repeated-multiplication analogy to x_k=A^kx₀ and naming both directions)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.33…2.15.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| Spectral Theorem assumed to force positive eigenvalues | `spectral-theorem` |
| Spectral Theorem assumed to apply to any square matrix | `spectral-theorem` |
| repeated eigenvalues assumed impossible for symmetric matrices | `spectral-theorem` |
| orthogonal matrices assumed to stretch/shrink vectors | `orthogonal-matrices` |
| product of orthogonal matrices assumed not necessarily orthogonal | `orthogonal-matrices` |
| only columns (not also rows) of an orthogonal matrix assumed orthonormal | `orthogonal-matrices` |
| positive definiteness judged from entry signs rather than eigenvalues | `positive-definite-matrices` |
| determinant sign alone assumed to certify positive definiteness | `positive-definite-matrices` |
| Cholesky assumed valid for any square matrix | `cholesky-decomposition` |
| Cholesky's LDLᵀ-style diagonal convention confused with Cholesky's own | `cholesky-decomposition` |
| Schur complement mistaken for an eigenvalue tool | `schur-complement` |
| Schur complement of A and of D treated as interchangeable | `schur-complement` |
| Rayleigh quotient max confused with trace | `rayleigh-quotient` |
| Rayleigh quotient's real-valuedness assumed to require symmetry | `rayleigh-quotient` |
| stability judged from determinant or average eigenvalue rather than spectral radius | `matrix-stability` |
| operator norm assumed always equal to (rather than an upper bound on) spectral radius | `matrix-stability` |

**Cluster total: 210 items across 7 concepts (30 each).** All numeric claims verified. This cluster
completes the resolution of `eigendecomposition`'s rotation cliffhanger (`spectral-theorem`'s T1) and
plants two direct callbacks into the probability/statistics sweep (`positive-definite-matrices`' E1 on
covariance, `schur-complement`'s E1 on conditional covariance).
