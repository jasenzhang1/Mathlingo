# Graphical Models Cluster 3 — Variational Inference & Kernels

Variational Inference: ELBO, Variational Inference: VAEs, Gaussian Process, Reproducing Kernel
Hilbert Space, Wasserstein Distance (5 concepts). Same format as
[foundations-of-probability.md](foundations-of-probability.md). This is the final cluster of the
graphical-models domain and of the entire expanded sweep.

`variational-inference-elbo`'s A1 reuses `kl-divergence`'s Gibbs'-inequality proof directly to explain
the ELBO's name, and its T1 resolves `em-algorithm`'s lower-bound cliffhanger from the previous
cluster by showing EM is a special case of variational inference, not merely similar to it.

---

## Variational Inference: ELBO (`variational-inference-elbo`)
*Prereq: Mixture Models and Latent Variables, KL Divergence · ancestors 14 · b₀ = 0.85*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.15 | State variational inference's goal. | approximate an intractable posterior p(Z\|X) with a simpler, tractable q(Z), by minimizing D_KL(q(Z)‖p(Z\|X)) | — |
| R2 | recall | mcq | 0.15 | The ELBO is used instead of directly minimizing that KL divergence because: | the true posterior (and its KL divergence) involves the intractable evidence p(X); maximizing the ELBO is mathematically equivalent to minimizing that KL divergence without needing p(X) directly | claims "KL divergence is easier to compute directly" — the opposite is true; it's the intractability of p(X) that forces the ELBO detour → `variational-inference-elbo` |
| A1 | apply | short-answer | 0.65 | State the ELBO decomposition log p(X) = ELBO + D_KL(q‖p(Z\|X)), and explain why this makes ELBO a lower bound. | since D_KL≥0 always (Gibbs' inequality, proven via Jensen's inequality in `kl-divergence`'s E1), rearranging gives ELBO = log p(X) − D_KL(q‖p(Z\|X)) ≤ log p(X) — a direct reuse of that earlier nonnegativity proof, not a fresh argument *(required: the explicit reuse of `kl-divergence`'s nonnegativity)* | — |
| E1 | explain | short-answer | 1.35 | Why is maximizing ELBO over q equivalent to minimizing D_KL(q‖p(Z\|X))? | log p(X) is a constant with respect to q; maximizing ELBO = log p(X) − D_KL(q‖p(Z\|X)) over q is therefore identical to minimizing D_KL(q‖p(Z\|X)) over q — a direct algebraic consequence of the decomposition in A1 *(required: the explicit "constant w.r.t. q" argument)* | — |
| T1 | transfer | short-answer | 1.85 | How is the EM algorithm a special case of variational inference, resolving `em-algorithm`'s earlier lower-bound cliffhanger? | the E-step exactly maximizes the ELBO over q by setting q(Z) to the true posterior when tractable, and the M-step maximizes the ELBO over the model parameters — unifying EM and variational inference under one framework rather than treating EM's "lower bound" as a separate, unrelated fact *(required: names both steps' role in the unified framework)* | — |
| R3 | recall | mcq | −0.1 | The ELBO, expressed as an expectation, is: | E_q[log p(X,Z)] − E_q[log q(Z)] | writes it as E_q[log p(X,Z)] + E_q[log q(Z)], with the sign on the entropy term flipped → `variational-inference-elbo` |
| R4 | recall | short-answer | −0.05 | Fill in the blank: the "mean-field" assumption in variational inference factorizes q(Z) as ___. | a product of independent factors, one per latent variable or block of variables | — |
| R5 | recall | short-answer | 0.0 | True or false: the ELBO is always less than or equal to the true log evidence log p(X). | True | — |
| R6 | recall | mcq | 0.05 | "Coordinate ascent variational inference" (CAVI) optimizes the ELBO by: | iteratively updating each factor of a mean-field q, holding the others fixed, cycling until convergence | claims CAVI updates all factors of q simultaneously in one joint step, rather than one factor at a time → `variational-inference-elbo` |
| R7 | recall | short-answer | 0.1 | How can the ELBO be derived directly from Jensen's inequality applied to log p(X)? | log p(X) = log E_q[p(X,Z)/q(Z)] ≥ E_q[log(p(X,Z)/q(Z))] by Jensen's inequality, since log is concave, and the right-hand side is exactly the ELBO | — |
| R8 | recall | short-answer | 0.2 | Fill in the blank: the ELBO decomposes as a ___ term minus a ___ term (its "reconstruction minus regularization" form). | expected log-likelihood (reconstruction); KL divergence between q(Z) and the prior p(Z) (regularization) | — |
| R9 | recall | short-answer | 0.25 | True or false: the ELBO is tight — equals log p(X) exactly — when q(Z) equals the true posterior p(Z\|X). | True | — |
| R10 | recall | mcq | 0.3 | The mean-field assumption's main cost is that it: | cannot capture posterior correlations between the latent variables it factorizes across | claims the mean-field assumption's main cost is computational speed, when its actual cost is the loss of captured correlations → `variational-inference-elbo` |
| R11 | recall | short-answer | 0.35 | Define "variational parameters." | the parameters governing the shape of the approximating distribution q (e.g. the mean and variance of a Gaussian q), optimized to make q as close as possible to the true posterior | — |
| R12 | recall | short-answer | 0.4 | Fill in the blank: the prerequisite concepts for Variational Inference: ELBO are ___ and ___. | Mixture Models and Latent Variables and KL Divergence | — |
| R13 | recall | short-answer | 0.45 | True or false: variational inference converts a Bayesian inference problem into an optimization problem. | True — a defining feature, contrasted with sampling-based approaches like MCMC | — |
| R14 | recall | mcq | 0.5 | ELBO stands for: | Evidence Lower BOund | expands it as "Expected Log-likelihood Bound," an incorrect expansion of the acronym → `variational-inference-elbo` |
| R15 | recall | short-answer | 0.55 | Why is CAVI only guaranteed to find a local, not global, optimum of the ELBO? | the ELBO is generally non-convex in the variational parameters, especially with a factorized q, so coordinate ascent can converge to different local optima depending on initialization | — |
| R16 | recall | short-answer | 0.6 | True or false: unlike MCMC, variational inference does not asymptotically converge to the exact posterior as computation increases, since it is limited by the chosen family for q. | True | — |
| A2 | apply | numeric | 0.7 | For one sampled Z, log p(X,Z)=−5 and log q(Z)=−2. That sample's contribution to log p(X,Z)−log q(Z)? `[verified: -3]` | −5−(−2)=−3 | — |
| A3 | apply | numeric | 0.75 | True log evidence log p(X)=−10; computed ELBO=−12. Implied D_KL(q‖p(Z\|X))? `[verified: 2]` | log p(X)=ELBO+KL → KL=−10−(−12)=2 | — |
| A4 | apply | numeric | 0.8 | ELBO=−8 exactly equals the true evidence log p(X)=−8. Implied D_KL(q‖p(Z\|X))? `[verified: 0]` | KL=log p(X)−ELBO=−8−(−8)=0 | — |
| A5 | apply | numeric | 0.85 | Reconstruction term = −3, KL-to-prior term = 1.5. ELBO in the reconstruction-minus-regularization form? `[verified: -4.5]` | −3−1.5=−4.5 | — |
| A6 | apply | numeric | 0.9 | A mean-field q over 2 independent latent dimensions, each a Gaussian(μ,σ²). Total variational parameters? `[verified: 4]` | 2 parameters (μ,σ²) per dimension × 2 dimensions = 4 | — |
| E2 | explain | short-answer | 1.4 | Why does mean-field variational inference systematically underestimate posterior variance on correlated latent variables? | forcing q to factorize independently across variables that are truly correlated under the posterior forces the approximation to ignore that correlation, and the ELBO's KL direction tends to concentrate q around a single mode rather than spreading to cover the posterior's full joint spread *(required: names the factorization-forces-independence mechanism and connects it to underestimated variance)* | — |
| E3 | explain | short-answer | 1.5 | Why does maximizing the ELBO not minimize the "reverse" direction of KL divergence that would instead be mode-covering? | D_KL(q‖p), used by the ELBO, penalizes q for placing mass where p has none, favoring q that hugs a single high-density region of p ("mode-seeking"); the reverse D_KL(p‖q) instead penalizes q for missing mass where p has some, favoring a broader, mode-covering q — the ELBO commits to the former because only that direction gives a tractable, computable objective *(required: distinguishes the two KL directions and why the tractable one is used)* | — |
| E4 | explain | short-answer | 1.6 | Why is coordinate ascent (CAVI) a natural fitting algorithm specifically for a mean-field q, but not for an unconstrained q? | CAVI's update for each factor has a convenient closed form only because the mean-field factorization lets each factor's optimal update be derived in isolation, holding the others fixed; without that factorization there is no similarly clean per-factor closed form to cycle through *(required: connects the closed-form per-factor update specifically to the mean-field structure)* | — |
| E5 | explain | short-answer | 1.7 | Why does the ELBO's "reconstruction minus regularization" decomposition read almost identically to a VAE's loss function? | a VAE trains its encoder/decoder by directly maximizing this same ELBO, so its loss — reconstruction error plus a KL-regularization term pulling q(Z\|X) toward the prior — is literally the negative ELBO under neural-network-parameterized q and p, not merely an analogous design *(required: names that the VAE loss is the negative ELBO exactly, not just similar in spirit)* | — |
| T2 | transfer | short-answer | 1.9 | Why might a practitioner choose variational inference over MCMC for a large dataset with a complex model, despite VI's approximation bias? | VI turns inference into an optimization problem that can use stochastic gradient methods scaling to large datasets, and typically converges much faster than MCMC's many sequential sampling steps; the trade-off is that VI's answer is only as good as the chosen q family allows, whereas MCMC is asymptotically exact given enough samples and time *(required: names the scalability advantage and the approximation-bias cost explicitly)* | — |
| T3 | transfer | short-answer | 1.95 | How does the choice of q's family relate to the bias-variance trade-off framing used elsewhere in this curriculum (e.g. `regularization`)? | a more flexible, expressive q family can get closer to the true posterior, reducing the approximation's bias, but requires more variational parameters to fit, increasing the risk of poor optimization on limited data — the same expressiveness-versus-fitting-difficulty trade-off appears again, cast in terms of an approximating family rather than a predictive model *(required: connects q-family flexibility explicitly to a bias/fitting-difficulty trade-off)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.15…1.95.*

---

## Variational Inference: VAEs (`variational-inference-vaes`)
*Prereq: Variational Inference: ELBO, Neural Networks, Backpropagation · ancestors 32 · b₀ = 1.25*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.25 | Describe a VAE's two networks. | an "encoder" network parameterizing the approximate posterior q(Z\|X); a "decoder" network modeling p(X\|Z); trained by maximizing the ELBO via backpropagation | — |
| R2 | recall | mcq | 0.55 | VAEs generalize classical variational inference by: | using neural networks (flexible function approximators) for the approximate posterior and likelihood, instead of simple hand-chosen parametric families | claims VAEs are "identical to classical VI with no changes" — missing the entire neural-network generalization → `variational-inference-vaes` |
| A1 | apply | short-answer | 1.05 | Why does training a VAE require the reparameterization trick? | backpropagation requires differentiable operations throughout, but sampling directly from a distribution isn't differentiable with respect to its parameters (μ,σ); the trick samples Z=μ+σε for ε~N(0,1), moving randomness into a parameter-free noise variable so the remaining computation is differentiable and backprop-compatible *(required: names the non-differentiability of direct sampling as the specific problem)* | — |
| E1 | explain | short-answer | 1.75 | Why are the encoder and decoder trained jointly rather than separately? | the ELBO decomposes into a reconstruction term (how well the decoder recovers X from a sampled Z) plus a KL-regularization term (how close q(Z\|X) stays to a simple prior p(Z)); both networks' parameters appear in both terms, requiring joint optimization of the shared objective *(required: names both ELBO terms and that both networks appear in both)* | — |
| T1 | transfer | short-answer | 2.25 | Why are VAEs considered generative models, per `generative-vs-discriminative-models`'s ML-sweep distinction? | once trained, new data can be generated by sampling Z from the simple prior p(Z) and passing it through the decoder — the same "generate new samples" capability that concept identified as generative models' distinctive advantage over discriminative ones *(required: the explicit callback to that earlier concept's distinguishing capability)* | — |
| R3 | recall | mcq | 0.3 | The VAE's encoder network outputs: | the parameters (e.g. mean and variance) of the approximate posterior q(Z\|X), not Z itself directly | claims the encoder outputs Z directly as a deterministic value, missing that it outputs distribution parameters, which Z is then sampled from → `variational-inference-vaes` |
| R4 | recall | short-answer | 0.35 | Fill in the blank: the reparameterization trick samples Z as Z = ___, where ε~N(0,1). | μ+σε | — |
| R5 | recall | short-answer | 0.4 | True or false: in a VAE, the prior p(Z) is typically chosen to be a simple, fixed distribution such as a standard Normal N(0,I). | True | — |
| R6 | recall | mcq | 0.45 | Compared to a standard (non-variational) autoencoder, a VAE's key addition is: | a KL-divergence regularization term pulling the encoder's output distribution toward a prior, giving the latent space a well-defined probabilistic structure | claims the VAE's key addition is simply a larger latent dimension, rather than the KL-regularization term that gives the latent space its probabilistic structure → `variational-inference-vaes` |
| R7 | recall | short-answer | 0.5 | What does "amortized inference" mean in the VAE context? | instead of solving a separate optimization for each data point's approximate posterior, a single encoder network learns to map any input X directly to its approximate posterior's parameters, sharing the cost of inference across all data points | — |
| R8 | recall | short-answer | 0.6 | Fill in the blank: to generate new samples from a trained VAE, one samples Z from the ___ and passes it through the ___. | prior p(Z); decoder network | — |
| R9 | recall | short-answer | 0.65 | True or false: the decoder network in a VAE outputs the parameters of p(X\|Z). | True | — |
| R10 | recall | mcq | 0.7 | For binary pixel data (e.g. black/white images), the decoder's output distribution is typically modeled as: | Bernoulli, per pixel | uses a Gaussian output distribution for strictly binary pixel data, mismatching the decoder's likelihood to the actual data type → `variational-inference-vaes` |
| R11 | recall | short-answer | 0.75 | Why is "amortized inference" more efficient than optimizing q for each data point separately, once the encoder is trained? | a single forward pass through the encoder computes an approximate posterior for a new data point, avoiding a fresh optimization from scratch for each point, at the cost of the encoder's approximation quality being shared across all points rather than individually tuned | — |
| R12 | recall | short-answer | 0.8 | Fill in the blank: the prerequisite concepts for Variational Inference: VAEs are ___, ___, and ___. | Variational Inference: ELBO, Neural Networks, and Backpropagation | — |
| R13 | recall | short-answer | 0.85 | True or false: the reparameterization trick is necessary because backpropagation cannot differentiate through a direct sampling operation. | True | — |
| R14 | recall | mcq | 0.9 | The random noise variable ε in the reparameterization trick is: | sampled from a fixed, parameter-free distribution, such as standard Normal, so gradients don't need to flow through it | claims ε is itself a learned parameter optimized during training, rather than a fixed, parameter-free noise source → `variational-inference-vaes` |
| R15 | recall | short-answer | 0.95 | What loss function does a VAE typically minimize during training? | the negative ELBO, consisting of a reconstruction loss term and a KL-divergence regularization term | — |
| R16 | recall | short-answer | 1.0 | True or false: a "beta-VAE" modifies the standard VAE loss by weighting the KL term with a factor β, often to encourage more disentangled latent representations. | True | — |
| A2 | apply | numeric | 1.1 | Reparameterization: μ=2, σ=0.5, ε=1.5. Sampled Z? `[verified: 2.75]` | Z=μ+σε=2+0.5·1.5=2.75 | — |
| A3 | apply | numeric | 1.15 | Reparameterization: μ=−1, σ=2, ε=−0.3. Sampled Z? `[verified: -1.6]` | Z=−1+2·(−0.3)=−1.6 | — |
| A4 | apply | numeric | 1.2 | q=N(1,4), prior N(0,1). D_KL(q‖prior) via D_KL=0.5(σ²+μ²−1−ln σ²)? `[verified: 1.307]` | 0.5·(4+1−1−ln4)=0.5·(4−1.386)=1.307 | — |
| A5 | apply | numeric | 1.25 | q=N(0,1), prior N(0,1). D_KL(q‖prior)? `[verified: 0]` | 0.5·(1+0−1−ln1)=0.5·(0−0)=0 | — |
| A6 | apply | numeric | 1.3 | q=N(2,0.25), prior N(0,1). D_KL(q‖prior)? `[verified: 2.318]` | 0.5·(0.25+4−1−ln0.25)=0.5·(3.25+1.386)=2.318 | — |
| E2 | explain | short-answer | 1.8 | Why does the reparameterization trick move the randomness into ε rather than eliminating it entirely? | the model must still be genuinely stochastic — Z must actually be a sample from q(Z\|X), not a deterministic function of X alone — so the randomness can't be removed, only relocated to a parameter-free noise source ε so that the remaining computation Z=μ+σε is a differentiable, deterministic function of the encoder's outputs and ε *(required: names that stochasticity is preserved, only its location changes)* | — |
| E3 | explain | short-answer | 1.9 | Why does a plain, non-variational autoencoder's latent space typically fail to support meaningful sampling of new data, even though it reconstructs training data well? | without a KL term pulling the encoding toward a known prior, a plain autoencoder's latent space can have arbitrary, disconnected, or unbounded structure shaped only by minimizing reconstruction error — there's no guarantee that a randomly sampled latent point lands anywhere the decoder has learned to produce sensible output *(required: connects the missing KL regularization to the absence of a well-structured, samplable latent space)* | — |
| E4 | explain | short-answer | 2.0 | Why does increasing β in a β-VAE's loss (β·D_KL + reconstruction) tend to produce more disentangled but blurrier reconstructions? | a larger β more strongly forces q(Z\|X) toward the simple, factorized prior p(Z)=N(0,I), pressuring the model to use each latent dimension for a genuinely independent factor of variation, but this also more aggressively limits the information the latent code can carry about X, degrading reconstruction fidelity *(required: connects the weight increase to both effects, not just one)* | — |
| E5 | explain | short-answer | 2.1 | Why is the choice between a Bernoulli and a Gaussian decoder output distribution not just a technical detail, but a modeling assumption with real consequences? | the choice determines what the reconstruction-loss term in the ELBO actually is — binary cross-entropy for Bernoulli, squared error for Gaussian — and what values the decoder can meaningfully produce; a Gaussian decoder for strictly 0/1 pixel data implicitly, and incorrectly, treats pixel intensities as continuous measurements with additive Gaussian noise *(required: names the concrete consequence of a mismatched decoder-distribution choice)* | — |
| T2 | transfer | short-answer | 2.3 | Why might a VAE's generated samples look blurrier than a GAN's, even when both are trained on the same image dataset? | the VAE's reconstruction loss, e.g. pixel-wise squared error under a Gaussian decoder, is minimized, on average, by predicting something close to the mean of plausible reconstructions when uncertainty remains, which blurs fine detail, whereas a GAN's adversarial loss directly rewards producing sharp, realistic-looking samples *(required: connects the specific pixel-wise-averaging mechanism to the blurriness, not just "VAEs are blurrier")* | — |
| T3 | transfer | short-answer | 2.35 | How does the VAE's encoder-decoder structure relate to the "first pick a latent cause, then generate conditionally on it" logic from `mixture-models-and-latent-variables`? | the VAE is the same generative logic, made continuous and neural: instead of a discrete component indicator Z choosing among a handful of mixture components, a continuous latent vector Z, given a simple prior, is passed through a flexible neural decoder to generate X — the same two-stage generative story, with discrete/parametric machinery replaced by continuous latent variables and a neural network *(required: names the discrete-to-continuous, parametric-to-neural generalization explicitly)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.25…2.35.*

---

## Gaussian Process (`gaussian-process`)
*Prereq: Multivariate Normal, Kernel · ancestors 37 · b₀ = 1.32*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.32 | Describe a GP as a Bayesian nonparametric prior over functions. | "nonparametric" means effective model complexity grows with data, unlike a fixed-size parametric model; a GP is nonetheless fully specified by a mean function and a covariance/kernel function | — |
| R2 | recall | mcq | 0.62 | Which is *not* true of a GP being "nonparametric"? | that it has no parameters whatsoever | the kernel itself typically carries hyperparameters (e.g. bandwidth σ); "nonparametric" describes growing effective complexity, not zero parameters → `gaussian-process` |
| A1 | apply | short-answer | 1.12 | Contrast a parametric model (fixed β regardless of data amount) with a GP's growing effective complexity. | a linear regression's parameter count stays fixed no matter how much data arrives; a GP's effective flexibility grows as more data reveals more structure, since it's a distribution over entire functions rather than a fixed finite parameter vector — the same framing `gp-regression` used in the ML sweep *(required: the explicit fixed-vs-growing contrast)* | — |
| E1 | explain | short-answer | 1.82 | Why does a GP fit into the "graphical models" domain despite not looking like a traditional node/edge graph? | a GP is an infinite-dimensional generalization of a multivariate Normal graphical model, where every possible input point is implicitly a "node," and the kernel function determines the continuum of pairwise dependencies ("edges") between them *(required: the explicit infinite-dimensional-MVN framing)* | — |
| T1 | transfer | short-answer | 2.32 | Why is a GP considered a Bayesian approach specifically? | the GP itself serves as a prior over functions, and observing data updates it into a posterior (also a GP, via the conditioning argument from `gp-regression`) — this prior-to-posterior updating via Bayes' rule is the defining characteristic of a Bayesian method, here applied to an infinite-dimensional function rather than a finite parameter vector *(required: names the prior-to-posterior Bayes'-rule update explicitly)* | — |
| R3 | recall | mcq | 0.37 | A Gaussian Process is fully specified by: | a mean function m(x) and a covariance (kernel) function k(x,x') | claims a GP is fully specified by a mean function and a fixed variance scalar, omitting the kernel function that governs correlations between points → `gaussian-process` |
| R4 | recall | short-answer | 0.42 | Fill in the blank: for any finite set of input points, a GP states that the corresponding function values are jointly ___. | (multivariate) Gaussian/Normal distributed | — |
| R5 | recall | short-answer | 0.47 | True or false: the RBF (squared exponential) kernel's length-scale hyperparameter controls how quickly correlation between points decays with distance. | True | — |
| R6 | recall | mcq | 0.52 | A larger length-scale in the RBF kernel produces a GP prior over functions that are: | smoother, varying more slowly | claims a larger length-scale produces rougher, more rapidly varying functions, the opposite of its actual effect → `gaussian-process` |
| R7 | recall | short-answer | 0.57 | What does the "signal variance" hyperparameter in a kernel control? | the overall vertical scale, or amplitude, of the function's variation — how far the function typically deviates from its mean | — |
| R8 | recall | short-answer | 0.67 | Fill in the blank: GP regression's exact computational cost for n training points scales as ___. | O(n³), due to inverting an n×n covariance matrix | — |
| R9 | recall | short-answer | 0.72 | True or false: the GP posterior, after conditioning on observed data, is generally no longer a Gaussian Process. | False — it remains a GP | — |
| R10 | recall | mcq | 0.77 | The "noise variance" hyperparameter in GP regression accounts for: | observation noise in the training targets, separate from the underlying function's own variability | conflates the noise variance with the signal variance, treating them as the same hyperparameter → `gaussian-process` |
| R11 | recall | short-answer | 0.82 | How are a GP's hyperparameters typically chosen? | by maximizing the marginal likelihood (evidence) of the observed data, which naturally trades off fit against model complexity | — |
| R12 | recall | short-answer | 0.87 | Fill in the blank: the prerequisite concepts for Gaussian Process are ___ and ___. | Multivariate Normal and Kernel | — |
| R13 | recall | short-answer | 0.92 | True or false: sparse GP approximations exist specifically to address the O(n³) computational cost for large datasets. | True | — |
| R14 | recall | mcq | 0.97 | The RBF kernel k(x,x')=σ²exp(−‖x−x'‖²/(2ℓ²)) implies: | points close together in input space are highly correlated, and correlation decays smoothly with distance | claims the RBF kernel implies correlation is constant regardless of distance between points → `gaussian-process` |
| R15 | recall | short-answer | 1.02 | Distinguish the GP "prior" from the GP "posterior." | the prior is the GP before observing any data, defined purely by the mean and kernel functions; the posterior is the GP after conditioning on observed training points, narrower near observed data and reverting toward the prior far from it | — |
| R16 | recall | short-answer | 1.07 | True or false: a GP with a linear kernel is, in its prior over functions, equivalent to Bayesian linear regression. | True | — |
| A2 | apply | numeric | 1.17 | RBF kernel, σ²=1, ℓ=2, ‖x−x'‖=2. k(x,x')? `[verified: 0.607]` | exp(−4/(2·4))=exp(−0.5)≈0.6065 | — |
| A3 | apply | numeric | 1.22 | Same RBF kernel (σ²=1, ℓ=2), but x=x' so ‖x−x'‖=0. k(x,x')? `[verified: 1]` | exp(0)=1 | — |
| A4 | apply | numeric | 1.27 | RBF kernel, σ²=4, ℓ=1, ‖x−x'‖=1. k(x,x')? `[verified: 2.426]` | 4·exp(−1/2)=4·0.6065≈2.426 | — |
| A5 | apply | numeric | 1.32 | Exact GP regression scales as O(n³). Going from n=100 to n=200 training points, how many times more expensive is the computation (ignoring constants)? `[verified: 8]` | (200/100)³=2³=8 | — |
| A6 | apply | numeric | 1.37 | Going from n=10 to n=100 training points, how many times more expensive is exact GP regression (ignoring constants)? `[verified: 1000]` | (100/10)³=10³=1000 | — |
| E2 | explain | short-answer | 1.87 | Why does a GP's predictive variance shrink near observed training points but grow away from them? | conditioning on a training point directly constrains the function values correlated with it, per the kernel, collapsing uncertainty near that point, while points far away — with low kernel correlation to any observed point — remain governed mostly by the prior's own, comparatively larger, variance *(required: connects the kernel's correlation-decay-with-distance to the variance-shrinkage pattern)* | — |
| E3 | explain | short-answer | 1.97 | Why does maximizing the marginal likelihood, rather than minimizing training error, guard against overfitting when choosing GP hyperparameters? | the marginal likelihood integrates out the function values entirely rather than fitting them directly, automatically penalizing complexity, such as an overly short length-scale that fits training noise, through an Occam's-razor-like term in its closed form, unlike training error, which would keep improving as the fit gets more flexible *(required: names the automatic complexity penalty built into the marginal likelihood)* | — |
| E4 | explain | short-answer | 2.07 | Why does the O(n³) cost of exact GP regression become the primary obstacle to using GPs on large datasets, more so than the number of hyperparameters? | the cubic cost comes from inverting the full n×n kernel matrix at every hyperparameter evaluation, which grows explosively with the number of training points n, while the number of hyperparameters stays small and roughly constant regardless of n *(required: distinguishes the data-size-driven cost from a parameter-count-driven cost)* | — |
| E5 | explain | short-answer | 2.17 | Why does a GP with a linear kernel reduce to Bayesian linear regression? | a linear kernel k(x,x')=xᵀx' corresponds exactly to a Bayesian linear model with a Gaussian prior on the weights; both describe the same joint Gaussian distribution over function values at any finite set of points, since the kernel is precisely the covariance function induced by that linear basis and weight prior *(required: names the kernel-induces-the-same-covariance argument, not just "they're similar")* | — |
| T2 | transfer | short-answer | 2.37 | Why might a practitioner choose a periodic kernel over an RBF kernel for modeling atmospheric CO2 concentration data? | CO2 data exhibits a genuine seasonal cycle superimposed on a long-term trend; an RBF kernel alone has no way to encode this repeating structure, treating distant points as simply less correlated, while a periodic kernel component directly encodes that points one period apart are strongly correlated regardless of absolute distance *(required: names the specific structural mismatch RBF has for periodic data)* | — |
| T3 | transfer | short-answer | 2.42 | How does the choice of kernel in a GP relate to the choice of basis functions in classical, finite-dimensional regression? | a kernel implicitly defines an infinite, or very high, dimensional feature space via Mercer's theorem, of which classical basis-function regression is a special, finite-dimensional case — choosing a kernel is choosing an entire family of basis functions at once, with the RKHS machinery from `rkhs` making this correspondence precise *(required: connects kernel choice to implicit basis-function choice via Mercer's theorem/RKHS)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.32…2.42.*

---

## Reproducing Kernel Hilbert Space (`rkhs`)
*Prereq: Mercer's Theorem · ancestors 18 · b₀ = 0.97*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.03 | Describe an RKHS informally, and its reproducing property. | the (possibly infinite-dimensional) feature space implicitly defined by a valid kernel; a Hilbert space where ⟨f, K(x,·)⟩ = f(x) for any f in the space | — |
| R2 | recall | mcq | 0.27 | RKHS theory provides the rigorous foundation for: | why the kernel trick is valid — why replacing dot products with kernel evaluations corresponds to a genuine inner product in some well-defined space | picks "computing p-values" — entirely unrelated to what RKHS theory establishes → `rkhs` |
| A1 | apply | short-answer | 0.77 | Why is the reproducing property ⟨f,K(x,·)⟩=f(x) useful? | it lets evaluating f at a point x be done via an inner product (with the "kernel slice" K(x,·)) instead of computing f(x) directly — connecting pointwise evaluation to the geometric inner-product structure of the space *(required: the explicit "evaluation via inner product" framing)* | — |
| E1 | explain | short-answer | 1.47 | How does RKHS theory relate directly to `mercers-theorem`'s validity condition? | Mercer's theorem guarantees a positive-semi-definite kernel corresponds to *some* genuine inner-product space; RKHS theory then provides the precise, rigorous construction of that exact space, making mathematically precise what "the feature space implied by a kernel" actually is *(required: distinguishes Mercer's existence guarantee from RKHS's explicit construction)* | — |
| T1 | transfer | short-answer | 1.97 | How does RKHS theory justify kernel ridge regression's complexity control? | the RKHS norm of a function serves as a natural complexity penalty (smoother/simpler functions have smaller RKHS norm), giving a rigorous foundation for why penalizing a function's RKHS norm controls model complexity and prevents overfitting — connecting directly back to `regularization`'s general principle from the regression domain *(required: names the RKHS-norm-as-complexity-penalty mechanism, with the explicit regularization callback)* | — |
| R3 | recall | mcq | 0.02 | The "feature map" φ(x) associated with a kernel k maps x into: | the (possibly infinite-dimensional) RKHS, such that k(x,x')=⟨φ(x),φ(x')⟩ | claims φ(x) maps x back into the original input space, rather than into the (possibly higher- or infinite-dimensional) feature space → `rkhs` |
| R4 | recall | short-answer | 0.07 | Fill in the blank: the "kernel trick" allows computing inner products in feature space ___. | without ever explicitly computing the possibly infinite-dimensional feature vectors φ(x) themselves | — |
| R5 | recall | short-answer | 0.12 | True or false: a valid kernel function must be positive semi-definite. | True | — |
| R6 | recall | mcq | 0.17 | The RBF kernel's associated feature space is: | infinite-dimensional | claims the RBF kernel's feature space is exactly 2-dimensional, confusing it with a low-degree polynomial kernel's finite feature space → `rkhs` |
| R7 | recall | short-answer | 0.22 | What does a small RKHS norm ‖f‖ typically indicate about a function f? | that f is smooth or simple (low complexity), since the norm serves as a complexity/roughness penalty | — |
| R8 | recall | short-answer | 0.32 | Fill in the blank: the "representer theorem" states that the minimizer of a regularized empirical risk over an RKHS can always be written as a ___ of kernel evaluations at the training points. | finite linear combination | — |
| R9 | recall | short-answer | 0.37 | True or false: every symmetric function k(x,x') is automatically a valid kernel. | False — it must also be positive semi-definite | — |
| R10 | recall | mcq | 0.42 | Mercer's theorem guarantees that a valid, positive semi-definite kernel corresponds to: | an inner product in some, possibly infinite-dimensional, feature space | claims Mercer's theorem guarantees the kernel corresponds to a finite-dimensional feature space in every case, which is false for kernels like RBF → `rkhs` |
| R11 | recall | short-answer | 0.47 | Why is the representer theorem practically important for kernel methods like kernel ridge regression or SVMs? | it guarantees the optimal solution, even in a possibly infinite-dimensional RKHS, can be represented using only a finite number of kernel evaluations, one per training point, making the optimization problem finite-dimensional and tractable | — |
| R12 | recall | short-answer | 0.52 | Fill in the blank: the stated prerequisite concept for Reproducing Kernel Hilbert Space is ___. | Mercer's Theorem | — |
| R13 | recall | short-answer | 0.57 | True or false: a polynomial kernel of degree d corresponds to a finite-dimensional feature space. | True — unlike the RBF kernel's infinite-dimensional feature space | — |
| R14 | recall | mcq | 0.62 | The RKHS norm penalty in kernel ridge regression plays the same role as: | the L2 penalty on coefficients in ordinary ridge regression, generalized to function space | claims the RKHS norm penalty plays the role of an L1 penalty specifically, rather than the L2-generalization it actually is → `rkhs` |
| R15 | recall | short-answer | 0.67 | Give the reproducing property's formula relating a function f and the kernel slice K(x,·). | ⟨f, K(x,·)⟩ = f(x) | — |
| R16 | recall | short-answer | 0.72 | True or false: the RKHS associated with a given kernel is unique. | True — the Moore-Aronszajn theorem guarantees a unique RKHS for each valid kernel | — |
| A2 | apply | numeric | 0.82 | Feature map φ(x)=(x,x²). Kernel k(x,x')=φ(x)·φ(x') for x=2, x'=3? `[verified: 42]` | 2·3+2²·3²=6+36=42 | — |
| A3 | apply | numeric | 0.87 | Linear kernel k(x,x')=xᵀx' for x=(1,2), x'=(3,4)? `[verified: 11]` | 1·3+2·4=3+8=11 | — |
| A4 | apply | numeric | 0.92 | Polynomial kernel k(x,x')=(xᵀx'+1)² for x=(1,2), x'=(3,4)? `[verified: 144]` | xᵀx'=11; (11+1)²=144 | — |
| A5 | apply | numeric | 0.97 | Gram matrix K=[[1,0.5],[0.5,1]]. Its eigenvalues? Is this a valid kernel matrix? `[verified: 1.5, 0.5, valid (PSD)]` | eigenvalues of [[1,0.5],[0.5,1]] are 1±0.5=1.5 and 0.5, both ≥0, so K is PSD | — |
| A6 | apply | numeric | 1.02 | Gram matrix K=[[1,2],[2,1]]. Its eigenvalues? Is this a valid kernel matrix? `[verified: 3, -1, not valid (not PSD)]` | eigenvalues of [[1,2],[2,1]] are 1±2=3 and −1; the negative eigenvalue means K is not PSD, so this cannot be a valid kernel's Gram matrix | — |
| E2 | explain | short-answer | 1.52 | Why does the kernel trick provide a genuine computational advantage rather than just a notational convenience? | for many useful kernels, such as RBF, the associated feature space is infinite-dimensional, so explicitly forming φ(x) is not merely inefficient but literally impossible to store; the kernel trick lets algorithms that only need inner products compute k(x,x') directly and finitely, sidestepping the impossible feature-vector computation entirely *(required: names that the infinite-dimensionality makes explicit computation impossible, not just slow)* | — |
| E3 | explain | short-answer | 1.62 | Why does the representer theorem's guarantee not contradict the RKHS potentially being infinite-dimensional? | the theorem is about the specific minimizer of a particular regularized objective over training data, not about the whole space; even though the ambient RKHS has infinitely many directions available, the optimization problem, structured around n training points, only ever needs to move in the n directions those points' kernel evaluations span *(required: distinguishes the whole infinite-dimensional space from the finite-dimensional subspace the specific problem needs)* | — |
| E4 | explain | short-answer | 1.72 | Why must a kernel be positive semi-definite for the Gram matrix it produces on any dataset to be a valid covariance-like structure? | positive semi-definiteness of the kernel is exactly the condition guaranteeing every possible Gram matrix built from it is itself positive semi-definite, which is required for it to correspond to a genuine inner-product or covariance structure — a non-PSD kernel could produce "negative variance" contradictions when used this way *(required: connects PSD-of-the-kernel to PSD-of-every-resulting-Gram-matrix)* | — |
| E5 | explain | short-answer | 1.82 | Why does penalizing the RKHS norm of f, rather than just minimizing empirical loss, prevent a kernel method from overfitting by interpolating every training point exactly? | the RKHS norm grows with a function's roughness needed to hit every training point exactly, especially with noisy data; penalizing it trades off perfect training fit against a smoother, lower-norm function — the same bias-variance trade-off `regularization` formalizes, applied here in function space rather than coefficient space *(required: connects the norm penalty to the same bias-variance trade-off logic, in function space)* | — |
| T2 | transfer | short-answer | 2.02 | How does the choice between a linear kernel and an RBF kernel in an SVM relate to the bias-variance trade-off? | a linear kernel restricts the decision boundary to the relatively low-complexity, finite-dimensional space of linear functions, giving higher bias but lower variance, while an RBF kernel's infinite-dimensional feature space allows arbitrarily flexible, highly non-linear boundaries, giving lower bias but higher variance risk *(required: connects the specific kernel choice to the bias-variance language explicitly)* | — |
| T3 | transfer | short-answer | 2.07 | Why does understanding RKHS theory help explain why "kernelizing" an algorithm, like PCA or ridge regression, is possible at all? | any algorithm expressible using only inner products between data points, rather than raw feature vectors, can have those inner products replaced by a kernel evaluation, implicitly operating in the kernel's RKHS without ever forming its feature vectors explicitly — RKHS theory guarantees this substitution is mathematically valid, connecting back to `mercers-theorem`'s existence guarantee *(required: names the inner-product-only requirement as what makes kernelization possible)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.03…2.07.*

---

## Wasserstein Distance (`wasserstein-distance`)
*Prereq: KL Divergence · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | Describe the Wasserstein ("earth mover's") distance, and how it differs from KL divergence structurally. | the minimum cost of transforming one distribution into another, cost being mass moved times distance moved; unlike KL divergence, it *is* a true metric — symmetric and satisfying the triangle inequality | — |
| R2 | recall | mcq | 0.04 | A key advantage of Wasserstein distance over KL divergence is: | it stays well-defined and meaningful even when two distributions have non-overlapping support, where KL divergence becomes infinite or undefined | claims it's "identical to KL divergence mathematically" — missing the entire point of using a different measure → `wasserstein-distance` |
| A1 | apply | short-answer | 0.54 | Why is D_KL(P‖Q) infinite when P puts positive probability where Q has zero probability, and how does Wasserstein avoid this? | D_KL(P‖Q)=E_P[log(P(X)/Q(X))]; if Q(x)=0 where P(x)>0, the log term blows up to +∞ at that point; Wasserstein distance instead stays finite, reflecting the physical distance the mass would need to travel, regardless of overlap *(required: the explicit log-blowup mechanism from `kl-divergence`'s own definition)* | — |
| E1 | explain | short-answer | 1.24 | State the "earth mover's" intuition precisely. | imagining P as a pile of dirt shaped according to its density and Q as a target shape, Wasserstein distance is the minimum total (mass × distance) work needed to reshape the P-pile into the Q-shape *(required: the explicit mass-times-distance "work" framing)* | — |
| T1 | transfer | short-answer | 1.74 | Why did Wasserstein distance become important for training GANs (WGANs) more stably? | Wasserstein distance stays meaningful when a generative model's early, poorly-trained output distribution shares almost no support with the true data distribution — exactly R2's advantage — while KL-divergence-like objectives can vanish or explode in that non-overlapping regime, a genuine source of earlier GANs' training instability *(required: connects the non-overlapping-support scenario explicitly to training instability)* | — |
| R3 | recall | mcq | −0.21 | A "coupling" (or transport plan) γ(x,y) between distributions P and Q is: | a joint distribution with marginals P and Q, describing how much mass is moved from each x to each y | claims a coupling is simply the product distribution P(x)Q(y), missing that it must be a joint distribution with the two specified marginals, not necessarily their product → `wasserstein-distance` |
| R4 | recall | short-answer | −0.16 | Fill in the blank: the Wasserstein distance is defined as the ___ over all valid couplings of the expected transport cost. | infimum (minimum) | — |
| R5 | recall | short-answer | −0.11 | True or false: the Wasserstein distance satisfies the triangle inequality, unlike KL divergence. | True | — |
| R6 | recall | mcq | −0.06 | "Optimal transport" refers to: | the mathematical problem of finding the coupling that minimizes total transport cost, which the Wasserstein distance solves | describes optimal transport as a special case of gradient descent, rather than as the underlying minimization problem the Wasserstein distance itself is defined by → `wasserstein-distance` |
| R7 | recall | short-answer | −0.01 | What does the "Kantorovich-Rubinstein duality" express the Wasserstein-1 distance as? | a supremum over 1-Lipschitz functions f of the difference in their expectations under P and Q: sup_{‖f‖_L≤1}(E_P[f]−E_Q[f]) | — |
| R8 | recall | short-answer | 0.09 | Fill in the blank: computing the exact Wasserstein distance for large-scale problems is often too expensive, so a common approximation is ___. | the Sinkhorn algorithm (entropy-regularized optimal transport) | — |
| R9 | recall | short-answer | 0.14 | True or false: the Wasserstein distance, unlike KL divergence, is symmetric: W(P,Q)=W(Q,P). | True | — |
| R10 | recall | mcq | 0.19 | Compared to KL divergence, the Wasserstein distance is generally: | more computationally expensive to compute exactly | claims the Wasserstein distance is always cheaper to compute than KL divergence, the opposite of its actual computational cost trade-off → `wasserstein-distance` |
| R11 | recall | short-answer | 0.24 | State the prerequisite concept for Wasserstein Distance. | KL Divergence | — |
| R12 | recall | short-answer | 0.29 | Fill in the blank: in WGAN, the discriminator, called a "critic," is trained to approximate the ___ used in the Kantorovich-Rubinstein dual formulation. | 1-Lipschitz function f | — |
| R13 | recall | short-answer | 0.34 | True or false: the Wasserstein distance is also known as the "earth mover's distance." | True | — |
| R14 | recall | mcq | 0.39 | The "cost function" in an optimal transport problem typically represents: | the cost of moving one unit of mass from location x to location y, e.g. the distance ‖x−y‖ | claims the cost function represents the probability of moving mass between locations, confusing a transport cost with a transport probability → `wasserstein-distance` |
| R15 | recall | short-answer | 0.44 | Name one application domain for Wasserstein distance besides GAN training. | e.g. domain adaptation, generative model evaluation, or image retrieval/registration | — |
| R16 | recall | short-answer | 0.49 | True or false: Wasserstein-1 and Wasserstein-2 distance use the same underlying cost, raised to different powers (order 1 vs order 2 of the underlying distance). | True | — |
| A2 | apply | numeric | 0.59 | P puts all mass at x=0, Q puts all mass at y=5, cost=\|x−y\|. Wasserstein-1 distance? `[verified: 5]` | all the mass must move from 0 to 5: cost=5 | — |
| A3 | apply | numeric | 0.64 | P: 0.5 mass at x=0, 0.5 mass at x=10; Q: all mass at y=5. Minimum transport cost? `[verified: 5]` | 0.5·5+0.5·5=5 | — |
| A4 | apply | numeric | 0.69 | P: mass 1 at x=2; Q: mass 1 at x=2 (identical). Wasserstein-1 distance? `[verified: 0]` | no mass needs to move: cost=0 | — |
| A5 | apply | numeric | 0.74 | P: 0.3 at x=0, 0.7 at x=1; Q: 0.7 at x=0, 0.3 at x=1. Minimum transport cost to turn P into Q? `[verified: 0.4]` | 0.4 units of mass must move the distance 1 to shift the imbalance: 0.4·1=0.4 | — |
| A6 | apply | numeric | 0.79 | P: uniform on {0,1,2}, each mass 1/3; Q: mass 1 at x=1. Minimum transport cost? `[verified: 0.667]` | 1/3 moves from 0 to 1 (cost 1/3) and 1/3 moves from 2 to 1 (cost 1/3); the mass already at 1 stays: total=2/3≈0.667 | — |
| E2 | explain | short-answer | 1.29 | Why is the Wasserstein distance well-defined even when two distributions have completely disjoint support, unlike KL divergence? | it is fundamentally about the cost of physically moving probability mass from one distribution's support to the other's, a finite, meaningful quantity regardless of whether the supports overlap; KL divergence instead compares density ratios pointwise, which blows up to infinity wherever the reference distribution assigns zero density to a point the other assigns positive density *(required: contrasts the transport-based definition to the density-ratio definition and why only the former stays finite)* | — |
| E3 | explain | short-answer | 1.39 | Why does the Kantorovich-Rubinstein duality make the Wasserstein-1 distance tractable to estimate with a neural network, as in WGAN? | instead of solving the potentially very high-dimensional optimal-transport coupling problem directly, the dual formulation reduces the computation to finding a single 1-Lipschitz function maximizing a simple expectation difference — a much more familiar optimization problem a neural network, constrained to be approximately 1-Lipschitz, can be trained to approximate *(required: names that the dual reformulation is what makes neural-network estimation practical)* | — |
| E4 | explain | short-answer | 1.49 | Why is the Lipschitz constraint on the critic function in Kantorovich-Rubinstein duality not an arbitrary technical restriction? | the supremum in the dual formulation only equals the true Wasserstein-1 distance when restricted to exactly the 1-Lipschitz functions; without that constraint the supremum over all functions would be unbounded, since scaling any suitable f up without limit could always increase E_P[f]−E_Q[f], so the constraint is precisely what keeps the dual formulation equal to a finite, meaningful distance *(required: names that removing the constraint makes the supremum unbounded, not just "less accurate")* | — |
| E5 | explain | short-answer | 1.59 | Why does exact optimal transport computation become intractable for large datasets, motivating approximations like the Sinkhorn algorithm? | exact optimal transport is a linear program whose complexity grows at least cubically with the number of points being matched, quickly becoming infeasible for large datasets; the Sinkhorn algorithm adds an entropy regularization term that makes the problem strictly convex and solvable via a much faster iterative matrix-scaling procedure, at the cost of only approximating the true, unregularized, Wasserstein distance *(required: names the cubic-or-worse exact cost and the entropy-regularization trade-off)* | — |
| T2 | transfer | short-answer | 1.79 | Why might a practitioner use the Wasserstein distance rather than KL divergence to evaluate how similar a generative model's output distribution is to real data? | generative models, especially early in training, often produce a distribution with little or no support overlap with the real data, exactly the regime where KL divergence becomes infinite or uninformative, while the Wasserstein distance remains finite and meaningfully graded, providing a genuinely useful evaluation signal throughout training, not just once the distributions start to overlap *(required: connects the non-overlapping-support advantage specifically to the evaluation use case)* | — |
| T3 | transfer | short-answer | 1.84 | Could a "Wasserstein autoencoder" replace a VAE's KL term with a Wasserstein term, and what would change? | yes — a Wasserstein autoencoder (WAE) replaces the VAE's per-example KL-to-prior regularization with a Wasserstein-distance penalty between the aggregated posterior over the whole dataset and the prior, avoiding KL's aggressive per-example matching in favor of matching the overall population of encodings to the prior in aggregate, often producing sharper reconstructions *(required: names the aggregated-posterior-vs-per-example distinction between the two regularization approaches)* | — |

*Coverage: 16/6/5/3 — 30 items, −0.26…1.84. This is the final concept of the entire graphical-models
domain and of the full expanded sweep.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| ELBO's necessity attributed to KL divergence being hard to compute directly | `variational-inference-elbo` |
| VAEs treated as identical to classical VI | `variational-inference-vaes` |
| GP's "nonparametric" label misread as "zero parameters" | `gaussian-process` |
| RKHS theory conflated with an unrelated statistical tool | `rkhs` |
| Wasserstein distance treated as mathematically identical to KL divergence | `wasserstein-distance` |
| ELBO sign/formula written with the entropy term's sign flipped | `variational-inference-elbo` |
| CAVI assumed to update all mean-field factors jointly rather than one at a time | `variational-inference-elbo` |
| mean-field's cost misattributed to speed rather than lost posterior correlations | `variational-inference-elbo` |
| ELBO acronym expanded incorrectly | `variational-inference-elbo` |
| VAE encoder assumed to output Z directly rather than q's distribution parameters | `variational-inference-vaes` |
| VAE decoder likelihood mismatched to the data type (e.g. Gaussian for binary pixels) | `variational-inference-vaes` |
| reparameterization noise ε treated as a learned parameter | `variational-inference-vaes` |
| GP's kernel/covariance function omitted from what specifies a GP | `gaussian-process` |
| RBF length-scale's smoothing effect reversed | `gaussian-process` |
| GP noise variance and signal variance conflated | `gaussian-process` |
| RBF kernel treated as implying constant correlation regardless of distance | `gaussian-process` |
| RKHS feature map assumed to map back into the original input space | `rkhs` |
| RBF kernel's infinite-dimensional feature space confused with a finite one | `rkhs` |
| Mercer's theorem assumed to guarantee only finite-dimensional feature spaces | `rkhs` |
| RKHS norm penalty confused with an L1 rather than L2-generalizing penalty | `rkhs` |
| optimal transport/coupling confused with the product distribution P(x)Q(y) | `wasserstein-distance` |
| Wasserstein distance assumed cheaper to compute exactly than KL divergence | `wasserstein-distance` |
| transport cost function confused with a transport probability | `wasserstein-distance` |

**Cluster total: 150 items across 5 concepts (30 each), expanded from the original 25-item design-doc
skeleton.** Every new numeric claim was verified independently, including the RBF kernel evaluations
(0.607, 1, 2.426), the Gram-matrix eigenvalue checks (1.5/0.5 valid, 3/−1 invalid), the KL-to-prior
divergences for the VAE (1.307, 0, 2.318), and the optimal-transport costs for the Wasserstein examples
(5, 5, 0, 0.4, 0.667).

---

# Graphical Models: complete

**15 / 15 concepts done.** Total items across all 3 clusters: **450** (30 per concept).
