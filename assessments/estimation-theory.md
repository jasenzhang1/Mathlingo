# Cluster 8 — Estimation Theory

Sufficient Statistic → Cramér–Rao Lower Bound (5 concepts). Same format as
[Cluster 1](foundations-of-probability.md).

This cluster resolves two cliffhangers deliberately left open earlier in the bank:
`law-of-total-variance` finally lets the marketing-campaign risk question from Cluster 5's
`law-of-total-expectation` be answered with actual numbers, and `sufficient-statistic`'s E2 cashes in
the T(x) term `exponential-family` flagged as a preview two clusters ago.

---

## Sufficient Statistic (`sufficient-statistic`)
*Prereq: MLE · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | Define a sufficient statistic T(X) for θ. | the conditional distribution of the data given T(X) does *not* depend on θ | — |
| R2 | recall | mcq | 0.04 | The Factorization theorem: f(x;θ) factors as g(T(x),θ)h(x) if and only if: | T(x) is sufficient for θ | picks "T(x) is unbiased for θ" — confuses two unrelated properties of a statistic → `sufficient-statistic` |
| A1 | apply | derivation | 0.54 | For iid Bernoulli(θ), use factorization to show T(X)=ΣXᵢ is sufficient. | f(x;θ) = Πθˣⁱ(1−θ)¹⁻ˣⁱ = θ^{Σxᵢ}(1−θ)^{n−Σxᵢ} = g(Σxᵢ, θ)·h(x) with h(x)=1 — factors cleanly through Σxᵢ alone *(required: the explicit factorization, matching `exponential-family`'s earlier Bernoulli derivation)* | — |
| A2 | apply | derivation | 0.65 | For iid Normal(μ, σ²) with σ² *known*, show T(X)=Σxᵢ is sufficient for μ via factorization. | the joint density's μ-dependent part is exp(μΣxᵢ/σ² − nμ²/(2σ²)), depending on the data only through Σxᵢ; the rest (exp(−Σxᵢ²/(2σ²)) and normalizing constants) doesn't involve μ, giving the required g(T,θ)h(x) split | includes x's individual values in the g(T,θ) factor rather than isolating them into h(x) → `sufficient-statistic` |
| E1 | explain | short-answer | 1.24 | Explain intuitively why sufficiency means T(X) captures *all* the information in the data relevant to θ. | if the rest of the data is conditionally independent of θ given T (the defining property), then once T is known, examining the rest of the data cannot teach you anything more about θ — there is nothing left for it to reveal *(required)* | — |
| E2 | explain | short-answer | 1.44 | Connect this directly to `exponential-family`'s T(x) term: explain why the T(x) in any exponential family's canonical form f(x;θ)=h(x)exp(η(θ)T(x)−A(θ)) is *automatically* sufficient, by the Factorization theorem. | the density is already exactly in the required g(T(x),θ)·h(x) form, with g(T,θ)=exp(η(θ)T−A(θ)) — sufficiency is immediate, with no extra work, purely from the shape of the canonical form *(required: makes the connection to that earlier concept explicit, not just re-deriving sufficiency from scratch)* | — |
| T1 | transfer | short-answer | 1.74 | A hospital tracking 10,000 patients' Bernoulli(θ) recovery outcomes could safely delete every record except the total recovery count T=ΣXᵢ and n, losing nothing needed to estimate θ. Why might the hospital still want to keep the full data anyway? | sufficiency guarantees nothing is lost *for estimating this specific θ under this specific model* — but the full data may be needed for other purposes never contemplated by that model: checking model assumptions, studying interactions with other variables, auditing individual cases, or estimating a different parameter entirely later *(required: names sufficiency as relative to a specific parameter/model, not an absolute statement)* | treats sufficiency as proof the rest of the data is worthless in general → `sufficient-statistic` |
| R3 | recall | short-answer | −0.22 | Fill in the blank: in the Factorization theorem, f(x;θ)=g(T(x),θ)h(x), the factor h(x) must NOT depend on ___. | θ | — |
| R4 | recall | mcq | −0.18 | A "minimal" sufficient statistic is one that: | is a function of every other sufficient statistic for the same model — it achieves the maximal possible data reduction | claims minimal sufficient means "the smallest possible number of data points used," rather than the smallest possible *summary* of any size | — |
| R5 | recall | mcq | −0.14 | True or false: the entire raw dataset X itself is always a (trivial) sufficient statistic for θ. | True — the conditional distribution of "the rest of the data" given all of X is degenerate (nothing left to condition on), trivially not depending on θ | claims the raw dataset is never itself sufficient, since "sufficiency is supposed to mean data reduction," missing that reduction isn't part of the definition, only a nice-to-have | — |
| R6 | recall | short-answer | −0.1 | Restate R1's definition of sufficiency in your own words, emphasizing what "does not depend on θ" means practically. | once T(X) is known, no matter what the true θ is, the remaining details of the data are distributed the same way — so observing the rest of the data (beyond T) can never help distinguish one candidate θ from another | — |
| R7 | recall | mcq | −0.06 | For iid Poisson(θ) observations, which statistic is sufficient for θ? | T(X) = Σxᵢ | picks T(X) = X̄ᵢ rather than the sum, missing that while X̄ is *also* sufficient (a one-to-one rescaling of Σxᵢ), the standard factorization directly produces Σxᵢ as the natural statistic → `sufficient-statistic` |
| R8 | recall | mcq | −0.02 | True or false: a sufficient statistic must always be a single number (one-dimensional). | False — for iid Normal(μ,σ²) with both parameters unknown, the minimal sufficient statistic is a *pair* (e.g. Σxᵢ and Σxᵢ²) | claims sufficiency always collapses to a single scalar summary regardless of how many parameters are unknown → `sufficient-statistic` |
| R9 | recall | short-answer | 0.02 | Fill in the blank: for iid Normal(μ,σ²) with both parameters unknown, a (minimal) sufficient statistic is the pair (___, ___). | Σxᵢ, Σxᵢ² (equivalently, the pair X̄ and the sum of squared deviations) | — |
| R10 | recall | mcq | 0.06 | Sufficiency is a property defined relative to: | a specific parametric model — the distribution family and the parameter θ being estimated | claims sufficiency is an absolute property of a dataset, holding regardless of what model or parameter is under consideration, exactly the error T1 targets → `sufficient-statistic` |
| R11 | recall | mcq | 0.1 | True or false: all sufficient statistics for the same model carry exactly the same information about θ, even if some achieve more data reduction than others. | True — by definition, each one already captures everything the data has to say about θ; a non-minimal sufficient statistic just carries some redundant extra baggage alongside that same complete information | claims a "less compressed" sufficient statistic must be carrying *more* information about θ than a minimal one, when both actually carry the same information — one just has more redundancy → `sufficient-statistic` |
| R12 | recall | short-answer | 0.14 | Fill in the blank: the identity statistic T(X)=X (the entire raw dataset) is called the ___ sufficient statistic. | trivial | — |
| R13 | recall | short-answer | 0.18 | In an exponential family's canonical form f(x;θ)=h(x)exp(η(θ)T(x)−A(θ)), which term is guaranteed to be a (minimal, in the regular case) sufficient statistic, and why? | T(x) — because the density is already in exactly the g(T(x),θ)·h(x) form the Factorization theorem requires, with g(T,θ)=exp(η(θ)T−A(θ)) | — |
| R14 | recall | mcq | 0.22 | Which of these is NOT part of checking whether T(X) is sufficient via the Factorization theorem? | computing the estimator's bias | includes "verifying g depends on the data only through T(x)" as not part of the check, when that's actually central to applying the theorem → `sufficient-statistic` |
| R15 | recall | mcq | 0.26 | True or false: sufficiency, by itself, says nothing about whether an estimator built from T(X) is unbiased or efficient. | True — sufficiency and unbiasedness/efficiency (R2's distractor) are genuinely separate properties, and one doesn't imply the other | claims a sufficient statistic automatically yields an unbiased or efficient estimator, conflating sufficiency with these unrelated estimator properties → `sufficient-statistic` |
| R16 | recall | short-answer | 0.3 | Fill in the blank: sufficiency requires that, once T(X) is known, the remaining details of the data are ___ about θ. | uninformative (conditionally independent of θ) | — |
| A3 | apply | derivation | 0.46 | For iid Exponential(θ), use factorization to show T(X)=Σxᵢ is sufficient for θ. | f(x;θ) = Πθe^{−θxᵢ} = θⁿe^{−θΣxᵢ} = g(Σxᵢ,θ)·h(x), with g(T,θ)=θⁿe^{−θT} and h(x)=1 — a clean factorization through Σxᵢ alone | leaves an individual xᵢ term inside g(T,θ) instead of collapsing the full product into a function of Σxᵢ alone → `sufficient-statistic` |
| A4 | apply | derivation | 0.58 | For iid Uniform(0,θ), use factorization to show T(X)=max(xᵢ) is sufficient for θ. | f(x;θ) = (1/θⁿ)·1{all xᵢ≤θ}·1{all xᵢ≥0} = (1/θⁿ)·1{max(xᵢ)≤θ}·1{all xᵢ≥0}; setting g(T,θ)=(1/θⁿ)1{T≤θ} and h(x)=1{all xᵢ≥0} gives the required factorization through max(xᵢ) alone | writes the indicator as depending on each individual xᵢ≤θ separately, rather than collapsing it to the single condition max(xᵢ)≤θ → `sufficient-statistic` |
| A5 | apply | short-answer | 0.5 | For iid Normal(μ known, σ² unknown), use factorization to show T(X)=Σ(xᵢ−μ)² is sufficient for σ². | f(x;σ²) = Π (1/√(2πσ²)) exp(−(xᵢ−μ)²/(2σ²)) = (2πσ²)^{−n/2} exp(−Σ(xᵢ−μ)²/(2σ²)); this depends on the data only through Σ(xᵢ−μ)², giving g(T,σ²)=(2πσ²)^{−n/2}exp(−T/(2σ²)) and h(x)=1 | uses Σxᵢ² instead of Σ(xᵢ−μ)², forgetting that μ is known and already centered out of the sufficient statistic here → `sufficient-statistic` |
| A6 | apply | numeric | 0.42 | For iid Bernoulli(θ) with n=10 and T=Σxᵢ=6, find the MLE θ̂ using only T and n (illustrating sufficiency's data reduction in action). `[verified: 0.6]` | θ̂ = T/n = 6/10 = 0.6 — no need for the individual 10 recovery/failure labels, only their sum, exactly as sufficiency guarantees | insists the individual sequence of 10 outcomes (not just their sum) must be inspected to compute θ̂, missing that A1 already established Σxᵢ alone is sufficient → `sufficient-statistic` |
| E3 | explain | derivation | 1.32 | Prove that the trivial statistic T(X)=X (the identity, R12) is always sufficient, directly from the definition in R1. | the conditional distribution of "the rest of the data" given T(X)=X is the distribution of the data given *itself* — there is nothing left unspecified once T=X is known, so this conditional distribution is degenerate (a point mass on the observed data) for every θ; a degenerate distribution that doesn't change with θ trivially satisfies "does not depend on θ" *(required: the degenerate-conditional-distribution argument, not just "it's trivially true")* | — |
| E4 | explain | short-answer | 1.5 | Explain, informally, why a *low-dimensional* sufficient statistic (rather than the trivial T(X)=X) is what makes sufficiency practically valuable, beyond the pure existence of the factorization. | any dataset trivially admits T(X)=X as sufficient (E3), so sufficiency alone guarantees nothing useful by itself; the real payoff comes when a *low-dimensional* T(X) — like a single sum or a pair of sums — turns out to satisfy the same defining property, meaning a dataset of any size can be losslessly compressed down to that handful of numbers for the purposes of estimating θ, which is the storage, computation, and communication saving E2/`exponential-family` build on | treats any sufficient statistic as automatically valuable, without distinguishing the trivial T(X)=X case from a genuinely compressive one | — |
| E5 | explain | short-answer | 1.6 | For iid Uniform(0,θ), explain why T(X)=(X₁, max(x₁,...,xₙ)) is technically still sufficient (it contains max, which is sufficient by A4) but is NOT *minimal*, unlike max(xᵢ) alone. | any statistic that contains a sufficient statistic as part of it is itself still sufficient, since nothing informative is lost — so (X₁, max) is sufficient simply because max is already inside it; but X₁ carries no additional information about θ beyond what max already provides (per A4's factorization, which used max alone), so keeping X₁ around is pure redundancy — R4's minimality requires being a function of every other sufficient statistic, and max(xᵢ) alone is such a function of (X₁, max) but not vice versa, so (X₁, max) fails minimality *(required: explains both why it's sufficient and specifically why it's not minimal)* | — |
| T2 | transfer | short-answer | 1.85 | A genomics lab, under an assumed iid Poisson(θ) model for read counts at a gene, plans to store only Σxᵢ per sample instead of every individual read count, citing sufficiency (R7). A collaborator objects that future analyses might need to test whether the Poisson model itself is even correct. Evaluate the objection using this concept's scope. | the collaborator is right to be cautious, echoing T1: sufficiency guarantees Σxᵢ loses nothing *for estimating θ under the assumed Poisson model* — but checking whether the data really *is* Poisson-distributed (rather than, say, over-dispersed) requires examining the full distribution of individual counts, which Σxᵢ alone cannot supply; sufficiency is a statement conditional on trusting the model, not a license to discard the ability to test that trust *(required: names that model-checking is a distinct purpose sufficiency doesn't cover)* | treats the collaborator's concern as already answered by the Factorization theorem, since Σxᵢ is provably sufficient for θ → `sufficient-statistic` |
| T3 | transfer | short-answer | 1.95 | A data engineering team decides to log only daily aggregate conversion counts (sufficient for a Bernoulli(θ) daily-rate model) instead of every individual user's timestamped conversion event, to save storage. Months later, an analyst suspects the true conversion rate actually drifts *within* each day (e.g. higher in the evening) rather than staying constant. Discuss what the aggregation decision cost the team. | if the true process has a time-varying rate within each day, the constant-θ Bernoulli model the aggregation was sufficient *for* was already the wrong model — the daily counts remain sufficient for estimating a single daily average rate, but they cannot recover any information about intra-day variation, because that information was never part of what the assumed model (and hence sufficiency) was defined relative to; the team traded away exactly the individual timestamps that would let them test and model the drift they now suspect *(required: ties the loss specifically to sufficiency being relative to the (wrong) constant-rate model, echoing R10/T1)* | assumes the daily aggregate counts must still contain everything needed to detect intra-day drift, since they were "sufficient," missing that sufficiency was only ever defined relative to the constant-rate model → `sufficient-statistic` |

*Coverage: 16/6/5/3 — 30 items, −0.26…1.95.*

---

## Correlation (`correlation`)
*Prereq: Covariance, Variance · ancestors 13 · b₀ = 0.82*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.18 | Define ρ(X,Y) in terms of covariance and standard deviations. | ρ(X,Y) = Cov(X,Y)/(σ_X σ_Y) | — |
| R2 | recall | mcq | 0.12 | ρ(X,Y) always lies in: | [−1, 1] | picks [0,1], treating correlation like a probability that can't be negative → `correlation` |
| A1 | apply | numeric | 0.62 | Cov(X,Y)=2, Var(X)=4, Var(Y)=9. Find ρ(X,Y). `[verified: 1/3]` | ρ = 2/(2·3) = 1/3 ≈ 0.333 | forgets to take square roots of the variances before dividing → `correlation` |
| A2 | apply | short-answer | 0.75 | Reuse `covariance`'s example: X~Uniform(−1,1), Y=X², where Cov(X,Y)=0. What is ρ(X,Y) here, and what does it confirm? | ρ=0 (the numerator is 0); confirms correlation is exactly zero despite X, Y being maximally (nonlinearly) dependent — correlation captures *linear* association only, missing this perfect quadratic relationship entirely | concludes X, Y are independent because ρ=0 → `covariance` (the same error `covariance`'s R2/E2 already targeted, now recurring under a new name) |
| E1 | explain | derivation | 1.32 | Prove −1 ≤ ρ(X,Y) ≤ 1, using Var(aX−Y) ≥ 0 for *every* real a — without invoking Cauchy-Schwarz by name. | Var(aX−Y) = a²Var(X) − 2a·Cov(X,Y) + Var(Y) ≥ 0 for all a; viewed as a quadratic in a that is never negative, its discriminant must satisfy 4Cov(X,Y)² − 4Var(X)Var(Y) ≤ 0, giving Cov(X,Y)² ≤ Var(X)Var(Y), i.e. \|ρ\| ≤ 1 after dividing by σ_Xσ_Y *(required: the full discriminant argument, self-contained from Var≥0 alone)* | — |
| E2 | explain | short-answer | 1.5 | What does ρ=±1 mean about the relationship between X and Y, and how does it fall out of E1's proof? | equality in E1's discriminant condition means there *exists* an a where Var(aX−Y)=0 exactly — i.e. aX−Y is (almost surely) constant, i.e. Y is an **exact linear function of X** *(required: connects the equality case explicitly to E1's derivation, not stated as a separate fact)* | — |
| T1 | transfer | short-answer | 1.82 | A study reports ρ(screen time, happiness) = −0.3. A headline claims "screen time makes teens 30% less happy." Name at least two distinct problems with this interpretation. | (1) correlation ≠ causation — reverse causation (unhappy teens use more screens) or a confounder (e.g. underlying mental health) could equally explain it; (2) −0.3 is a *correlation coefficient*, not a percentage effect size, and "30% less happy" badly mangles what the number means; bonus: ρ only captures *linear* association (per A2), so it could understate a real but nonlinear effect *(required: at least two distinct, correctly-articulated problems)* | names only the causation issue, missing that the magnitude itself is being misreported → `correlation` |
| R3 | recall | short-answer | −0.14 | Fill in the blank: correlation is a ___ measure of linear association — unlike covariance, it carries no units. | dimensionless (unitless) | — |
| R4 | recall | mcq | −0.1 | ρ(X,Y)=1 means: | Y is a perfect increasing linear function of X (Y=aX+b for some a>0) | claims ρ=1 means X and Y are identical, rather than related by any positive linear (affine) function | — |
| R5 | recall | mcq | −0.06 | True or false: ρ(X,X)=1 always (for any random variable X with positive variance). | True — a variable is trivially a perfect increasing linear function of itself | claims ρ(X,X) can be anything depending on X's distribution, missing that it's always exactly 1 by the defining formula | — |
| R6 | recall | short-answer | −0.02 | State what ρ(X,Y)=−1 means about the relationship between X and Y. | Y is a perfect *decreasing* linear function of X (Y=aX+b for some a<0) | — |
| R7 | recall | mcq | 0.02 | Unlike covariance, correlation ρ(X,Y) is invariant under: | positive linear (affine) rescalings of X and/or Y | claims correlation is invariant under *any* transformation of X or Y, not just linear ones — exactly the boundary case T3 probes → `correlation` |
| R8 | recall | mcq | 0.06 | True or false: correlation is symmetric, ρ(X,Y)=ρ(Y,X). | True — both Cov(X,Y) and the product σ_Xσ_Y are symmetric in X, Y | claims correlation is asymmetric, perhaps confusing it with a regression coefficient (which does depend on which variable is the predictor) | — |
| R9 | recall | short-answer | 0.1 | Fill in the blank: Pearson's sample correlation r is the sample-based estimate of the population quantity ___. | ρ, the population correlation coefficient | — |
| R10 | recall | mcq | 0.14 | Which is the correct formula for the sample correlation r from paired data (xᵢ,yᵢ)? | r = Σ(xᵢ−x̄)(yᵢ−ȳ) / √(Σ(xᵢ−x̄)²·Σ(yᵢ−ȳ)²) | omits the square root in the denominator, dividing by Σ(xᵢ−x̄)²·Σ(yᵢ−ȳ)² directly → `correlation` |
| R11 | recall | mcq | 0.18 | True or false: ρ(X,Y)=0 is necessary but not sufficient for X, Y to be independent. | True — independence always implies ρ=0, but A2's example shows ρ=0 does not imply independence | claims ρ=0 is both necessary and sufficient for independence, repeating `covariance`'s central misconception in correlation's own vocabulary → `correlation` |
| R12 | recall | short-answer | 0.22 | Fill in the blank: correlation's numerator is ___, and its denominator is ___. | Cov(X,Y); σ_X σ_Y | — |
| R13 | recall | short-answer | 0.26 | State what happens to ρ(X,Y) if X is transformed to aX+b, for a>0 versus a<0. | for a>0, ρ is unchanged; for a<0, ρ flips sign (its magnitude stays the same) | — |
| R14 | recall | mcq | 0.3 | Which of these values could NOT be a valid correlation coefficient? | 1.5 | picks 0.9 or −0.9 as impossible values, not recognizing they're well within the valid [−1,1] range → `correlation` |
| R15 | recall | mcq | 0.34 | True or false: a correlation near 0 always means there is no relationship of any kind between X and Y. | False — A2's Cov(X,X²)=0 example shows a correlation of exactly 0 despite a perfect (nonlinear) relationship | claims ρ≈0 rules out any meaningful relationship, missing that correlation only detects *linear* association → `correlation` |
| R16 | recall | short-answer | 0.38 | Fill in the blank: a correlation magnitude close to ±1 indicates a strong ___ relationship between X and Y. | linear | — |
| A3 | apply | numeric | 0.5 | Cov(X,Y)=−6, Var(X)=9, Var(Y)=16. Find ρ(X,Y). `[verified: −0.5]` | ρ = −6/(√9·√16) = −6/(3·4) = −0.5 | forgets the square roots and divides by 9·16 directly, getting a much smaller magnitude | — |
| A4 | apply | short-answer | 0.58 | Y=3X+5, a perfect positive linear function of X. Show ρ(X,Y)=1 using Cov and Var directly (not just citing R4). `[verified: 1]` | Cov(X,Y)=Cov(X,3X+5)=3Var(X) (bilinearity, R5/R10 of `covariance`); Var(Y)=9Var(X); ρ = 3Var(X) / (√Var(X)·√(9Var(X))) = 3Var(X)/(3Var(X)) = 1 | computes Var(Y) as 3Var(X) instead of 9Var(X), forgetting that scaling a variable by a squares its variance | — |
| A5 | apply | numeric | 0.68 | Paired data (1,2), (2,4), (3,5), (4,4), (5,5). Compute the sample correlation r. `[verified: ≈0.7746]` | x̄=3, ȳ=4; Σ(xᵢ−x̄)(yᵢ−ȳ)=6; √(Σ(xᵢ−x̄)²)=√10≈3.162; √(Σ(yᵢ−ȳ)²)=√6≈2.449; r = 6/(3.162×2.449) ≈ 0.7746 | computes the numerator and denominator sums correctly but forgets to take the square roots in the denominator, dividing by 10×6=60 instead | — |
| A6 | apply | short-answer | 0.62 | Y=−2X+1, a perfect negative linear function of X. Show ρ(X,Y)=−1. `[verified: −1]` | Cov(X,Y)=Cov(X,−2X+1)=−2Var(X); Var(Y)=4Var(X); ρ = −2Var(X)/(√Var(X)·√(4Var(X))) = −2Var(X)/(2Var(X)) = −1 | computes ρ as +1, forgetting that the negative coefficient −2 should carry through into the sign of Cov(X,Y) and hence of ρ | — |
| E3 | explain | derivation | 1.4 | Derive that ρ(aX+b, cY+d) = sign(ac)·ρ(X,Y), for a,c≠0. | Cov(aX+b,cY+d) = ac·Cov(X,Y) (bilinearity, dropping the additive constants); σ_{aX+b}=\|a\|σ_X and σ_{cY+d}=\|c\|σ_Y; so ρ(aX+b,cY+d) = ac·Cov(X,Y) / (\|a\|σ_X·\|c\|σ_Y) = [ac/\|ac\|]·[Cov(X,Y)/(σ_Xσ_Y)] = sign(ac)·ρ(X,Y) *(required: the bilinearity step, the \|a\|,\|c\| in the denominator, and the sign(ac) extraction)* | — |
| E4 | explain | short-answer | 1.55 | Explain why correlation, unlike covariance, allows a meaningful comparison of "how strong is the linear association" across variable pairs measured in completely different units (e.g. height-weight in cm/kg versus income-education in dollars/years). | covariance's units are the product of its two variables' units, so a covariance of 500 for (income, years of education) and a covariance of 2 for (height, weight) are not comparable numbers at all — they live in different unit systems; correlation divides out both standard deviations, leaving a pure, unitless number in [−1,1] that measures *only* the strength of linear association, regardless of what scale either original variable was measured on, which is exactly what makes ρ=0.6 for one pair directly comparable in strength to ρ=0.6 for a completely different pair *(required: the units-cancel-out mechanism, not just "correlation is easier to interpret")* | — |
| E5 | explain | derivation | 1.65 | Prove that ρ(X,Y) = E[Z_X Z_Y], where Z_X=(X−μ_X)/σ_X and Z_Y=(Y−μ_Y)/σ_Y are the standardized versions of X and Y. | E[Z_X Z_Y] = E[(X−μ_X)(Y−μ_Y)]/(σ_Xσ_Y) = Cov(X,Y)/(σ_Xσ_Y) = ρ(X,Y), using the definition of covariance directly in the numerator *(required: the substitution of the standardized variables' product-expectation into the covariance definition)* — so correlation is literally "the covariance of the standardized variables," a fact that makes E3's invariance to linear rescaling immediate, since standardizing already removes scale and location | — |
| T2 | transfer | short-answer | 1.9 | A college admissions officer finds ρ(SAT score, first-year GPA) = 0.4 and proposes weighting SAT scores at "40% of the admissions decision" because of this number. Explain what's wrong with treating a correlation coefficient as a direct percentage weight. | ρ=0.4 measures the *strength of linear association* between SAT and GPA on a −1-to-1 scale — it says nothing about how a decision-maker should weight SAT relative to other factors in a composite score; conflating a correlation coefficient with a percentage allocation is a category error much like T1's screen-time headline, and moreover explains only a fraction (ρ²=0.16, or 16%) of the *variance* in GPA even taken at face value, not 40% of anything in decision-weight terms *(required: identifies the category error and, ideally, that even the "variance explained" reading gives 16%, not 40%)* | accepts "40% weight" as a reasonable operational translation of ρ=0.4, repeating T1's magnitude-misreading error in a new domain → `correlation` |
| T3 | transfer | short-answer | 2.0 | Two labs measure ρ(gene expression, drug response): Lab A uses raw expression counts and gets ρ=0.55; Lab B log-transforms the same underlying counts before computing correlation and gets ρ=0.71. A researcher assumes this discrepancy must be a data error, citing "correlation is invariant to rescaling." Using E3, evaluate this assumption. | the researcher has over-applied E3's invariance result — it holds specifically for *linear* (affine) rescalings (aX+b), not for a nonlinear transformation like log; a log transform is not of the form aX+b, so it is entirely expected, not an error, that correlation computed on raw counts can genuinely differ from correlation computed on log-transformed counts — the two labs are, in a real sense, measuring the strength of two *different* relationships (linear-in-raw-counts vs. linear-in-log-counts) *(required: correctly identifies log as nonlinear and outside E3's invariance guarantee, so the discrepancy is expected rather than a data error)* | assumes the discrepancy must indicate a computational mistake, since "correlation is invariant to rescaling" is taken to apply to any transformation, not just linear ones → `correlation` |

*Coverage: 16/6/5/3 — 30 items, −0.18…2.0.*

---

## Law of Total Variance (`law-of-total-variance`)
*Prereq: Variance, Law of Total Expectation · ancestors 15 · b₀ = 0.89*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.11 | State the law of total variance. | Var(X) = E[Var(X\|Y)] + Var(E[X\|Y]) | — |
| R2 | recall | mcq | 0.19 | The two terms represent: | within-group variance and between-group variance | treats the two terms as "two unrelated quantities that happen to sum to Var(X)," missing the within/between interpretation → `law-of-total-variance` |
| A1 | apply | numeric | 0.69 | Reuse `law-of-total-expectation`'s exam example: Method A → mean 75, variance 100; Method B → mean 85, variance 64; chosen 50/50. Find Var(overall score). `[verified: 107]` | E[Var\|method] = 0.5(100)+0.5(64) = 82; Var(E[·\|method]) — a 2-point variable (75 or 85, each w.p. 0.5) with mean 80 — has variance 0.5(75−80)²+0.5(85−80)² = 25; total = 82+25 = 107 | computes only one of the two terms and reports that as the total → `law-of-total-variance` |
| A2 | apply | numeric | 0.8 | Revisit `law-of-total-expectation`'s marketing cliffhanger: 3 equally-likely campaigns with expected revenues $2M, $3M, $7M, each with within-campaign variance 1 ($M²). Find Var(overall revenue). `[verified: 5.667]` | E[Var\|campaign]=1; Var(E[·\|campaign]) uses mean 4, second moment (4+9+49)/3≈20.67, giving between-variance ≈4.67; total ≈ 1+4.67 = 5.667 | — |
| E1 | explain | derivation | 1.39 | Derive the law of total variance from Var(X)=E[X²]−(E[X])², applying the law of total expectation twice. | Var(X) = E[E[X²\|Y]] − (E[E[X\|Y]])²; substitute E[X²\|Y] = Var(X\|Y) + (E[X\|Y])² (the shortcut variance formula applied conditionally); this gives E[Var(X\|Y)] + E[(E[X\|Y])²] − (E[E[X\|Y]])², and the last two terms are exactly Var(E[X\|Y]) by definition *(required: the full multi-step substitution, including recognizing the last two terms as a variance)* | — |
| E2 | explain | short-answer | 1.6 | Why are *both* terms needed to fully understand a grouped random quantity? Contrast a case with large between-group variance and small within-group variance against the reverse. | knowing only the overall mean (as `law-of-total-expectation` alone provides) hides whether the risk comes from genuine group-to-group differences (large between-term) or from noise within each group (large within-term) — these have very different practical implications even at the same total variance *(required: the explicit contrast between the two regimes)* — this decomposition is exactly what `law-of-total-expectation`'s T1 was missing | — |
| T1 | transfer | short-answer | 1.89 | Resolve the marketing cliffhanger definitively: suppose the $7M campaign *alone* has variance 50, while A2's mixed strategy across all three campaigns has total variance only 5.667. Using both the mean and the variance, argue why a risk-averse company might prefer the mixed strategy or the $3M campaign over committing fully to the $7M one. `[verified: 5.667 vs 50]` | expected value alone (favoring $7M) ignores that committing fully to it inherits its own large variance (50) directly, while the mixed strategy's total variance (5.667) is far smaller — a genuine risk/return tradeoff that `law-of-total-expectation` alone could never surface, and exactly why this companion tool exists *(required: the explicit numeric contrast, 50 vs 5.667, driving the argument)* | repeats the "always pick highest expected value" reasoning from the original cliffhanger without incorporating the new variance information → `law-of-total-variance` |

*Coverage: 2/2/2/1 — 7 items, −0.11…1.89. This is the one concept in the whole bank explicitly built to answer a question posed two clusters earlier.*

---

## Fisher Information (`fisher-information`)
*Prereq: MLE · ancestors 11 · b₀ = 0.74*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.26 | State the two equivalent definitions of Fisher information I(θ). | I(θ) = E[(∂/∂θ log f(X;θ))²] = −E[∂²/∂θ² log f(X;θ)] | — |
| R2 | recall | mcq | 0.04 | Higher Fisher information corresponds to: | a sharper, more peaked likelihood — a more precisely estimable θ | picks "a flatter, more spread-out likelihood," backwards → `fisher-information` |
| A1 | apply | derivation | 0.54 | For X~Bernoulli(θ), compute I(θ) using the curvature form. `[verified: 1/(θ(1−θ))]` | log f = x ln θ + (1−x)ln(1−θ); first derivative x/θ − (1−x)/(1−θ); second derivative −x/θ² − (1−x)/(1−θ)²; −E[·] = θ/θ² + (1−θ)/(1−θ)² = 1/θ + 1/(1−θ) = 1/(θ(1−θ)) *(required: the full differentiate-twice-then-expect chain)* | — |
| A2 | apply | numeric | 0.65 | Using A1, compare I(0.5) and I(0.1). Which θ carries *more* information per observation? `[verified: 4 vs 11.11]` | I(0.5)=1/(0.25)=4; I(0.1)=1/(0.09)≈11.11 — **more** information near the extremes than at 0.5, which can feel counterintuitive but reflects that the MLE's variance 1/(nI(θ)) is *smaller* there | assumes information should be highest at θ=0.5, by analogy with variance being maximized there for Bernoulli → `fisher-information` |
| E1 | explain | short-answer | 1.24 | Explain, geometrically, why sharper likelihood curvature means θ is more precisely estimable. | a sharply peaked likelihood drops off quickly for θ away from θ̂, meaning the data strongly rules out nearby alternative values; a flat peak leaves many nearby θ values almost equally consistent with the data, so θ is poorly pinned down *(required: the "how quickly nearby θ are ruled out" framing)* | — |
| E2 | explain | short-answer | 1.4 | Why is Fisher information additive across independent observations — total information for n iid observations is n·I(θ)? | the log-likelihood of n iid observations is a *sum* of individual log-likelihoods; derivatives (and their squares/expectations, under independence) of a sum decompose into a sum of the individual terms' contributions *(required)* | — |
| T1 | transfer | short-answer | 1.74 | A pollster with a fixed sample size wants to estimate the true support proportion θ. Using A2's finding that I(θ) is smallest at θ=0.5, explain what this implies about polling a close (50/50) race versus a landslide (90/10) race with the *same* sample size. | a close race (θ near 0.5) is the *hardest* to poll precisely — lowest Fisher information means the MLE's variance 1/(nI(θ)) is largest there — while a landslide is easier to pin down with the same n; close elections genuinely need larger samples to hit the same margin of error *(required: the explicit link from A2's I(θ) shape to sample-size requirements)* | assumes all races are equally hard to poll to a given precision regardless of θ → `fisher-information` |

*Coverage: 2/2/2/1 — 7 items, −0.26…1.74.*

---

## Cramér–Rao Lower Bound (`cramer-rao-lower-bound`)
*Prereq: Fisher Information, Unbiased Estimator · ancestors 15 · b₀ = 0.89*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | −0.11 | State the Cramér–Rao Lower Bound for an unbiased estimator θ̂ from n iid observations. | Var(θ̂) ≥ 1/(nI(θ)) | — |
| R2 | recall | mcq | 0.19 | An estimator achieving the CRLB with equality is called: | efficient | picks "sufficient" — a different, related-sounding term from `sufficient-statistic` → `sufficient-statistic` |
| A1 | apply | numeric | 0.69 | For iid Bernoulli(θ) with I(θ)=1/(θ(1−θ)) and n=100, find the CRLB for Var(θ̂) at θ=0.5. `[verified: 0.0025]` | CRLB = 1/(nI(θ)) = 1/(100·4) = 0.0025 | — |
| A2 | apply | short-answer | 0.8 | The sample mean has Var(X̄)=θ(1−θ)/n exactly for Bernoulli data. Compare this to A1's bound and state what it proves. `[verified: exactly equal, 0.0025]` | θ(1−θ)/n = 0.5·0.5/100 = 0.0025, matching the CRLB *exactly* — the sample mean **achieves** the bound, so it is an *efficient* estimator for Bernoulli's θ *(required: notes the exact equality, not just "it's close")* | notes the two numbers match but doesn't draw the "therefore efficient" conclusion → `cramer-rao-lower-bound` |
| E1 | explain | short-answer | 1.39 | Without a full Cauchy-Schwarz-based proof, explain intuitively why the bound has the specific form 1/(nI(θ)). | more observations (larger n) should make estimation easier, giving a *smaller* lower bound — n in the denominator matches; more information per observation (larger I(θ)) should also make estimation easier, giving a smaller bound — I(θ) also belongs in the denominator *(required: both directions of the intuition, matched to the formula's structure)* | — |
| E2 | explain | short-answer | 1.55 | Why is the CRLB called a "universal speed limit," rather than just a bound on one specific estimator? | it applies to *every possible* unbiased estimator simultaneously — not one you happened to construct — so no cleverly designed unbiased estimator, however sophisticated, can ever beat it *(required: the "applies to every possible estimator" universality, not just "it's a tight bound")* | — |
| T1 | transfer | short-answer | 1.89 | Two competing unbiased estimators of a drug's efficacy θ exist. A company wants to know if some third, cleverer unbiased method could ever beat both. How does the CRLB answer this *without* testing every conceivable estimator? | compute the CRLB (1/(nI(θ))) once, from the model and sample size alone; if either existing estimator's variance already *equals* this bound, it is already optimally efficient among all unbiased estimators, and no unbiased method — however clever — can ever do better *(required: names that hitting the bound settles the question definitively, with no exhaustive search needed)* | proposes testing more candidate estimators empirically rather than checking against the bound directly → `cramer-rao-lower-bound` |

*Coverage: 2/2/2/1 — 7 items, −0.11…1.89. A1/A2 verify a genuinely striking fact: the ordinary sample mean is exactly efficient for Bernoulli, achieving the universal bound with no slack at all.*

---

## Cluster 8 misconception index

| Tag | Blame |
|---|---|
| sufficiency mistaken for unbiasedness or for a claim about all future uses of the data | `sufficient-statistic` |
| correlation assumed to range over [0,1] | `correlation` |
| ρ=0 mistaken for independence (recurrence of `covariance`'s central misconception) | `covariance` |
| correlation/causation and magnitude misreadings conflated | `correlation` |
| law-of-total-variance's two terms treated as unrelated | `law-of-total-variance` |
| Fisher information assumed maximized at θ=0.5 by analogy with variance | `fisher-information` |
| "efficient" and "sufficient" terminology confused | `sufficient-statistic` |
| CRLB match noted but its "no estimator can do better" conclusion missed | `cramer-rao-lower-bound` |

**Cluster total: 35 items across 5 concepts.** All numeric claims verified, including the exact
equality between the Bernoulli CRLB and the sample mean's true variance — not an approximation, a
genuine algebraic identity confirmed to machine precision. Two concepts in this cluster exist
specifically to complete arcs opened earlier: `law-of-total-variance` answers `law-of-total-expectation`'s
marketing question, and `sufficient-statistic` cashes in `exponential-family`'s T(x) preview.
