# Machine Learning Cluster 9 — Gaussian Processes

GP Regression, GP Classification (2 concepts). Same format as [Cluster 1](ml-01-foundations.md). This
is the final cluster of the machine-learning domain.

---

## GP Regression (`gp-regression`)
*Prereq: Multivariate Normal, Kernel · ancestors 37 · b₀ = 1.32*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.32 | Describe a Gaussian Process. | a distribution over functions such that any finite set of function values follows a multivariate Normal distribution, governed by a mean function and a covariance (kernel) function | — |
| R2 | recall | mcq | 0.62 | The kernel function in a GP determines: | how correlated the function's values are at nearby vs. far-apart input points — a smoother kernel implies a smoother expected function | picks "the random seed used" — unrelated to what the kernel actually encodes → `gp-regression` |
| A1 | apply | short-answer | 1.12 | Why do GP predictions come with a built-in measure of uncertainty, rather than just a point estimate? | since a GP defines a full multivariate Normal over function values, predicting at a new point is simply *conditioning* that Gaussian on observed points — the multivariate Normal's own conditional-distribution formulas (closely related to the Schur complement from the linear-algebra sweep) naturally yield both a predicted mean and a predicted variance *(required: the conditioning argument, with the Schur complement connection)* | — |
| E1 | explain | short-answer | 1.82 | Why is GP predictive uncertainty typically small near training points and larger far from them? | a decaying kernel (e.g. RBF-style) means points far from any observed data are only weakly correlated with it, leaving the conditional Gaussian's variance large there; near observed points, strong correlation pins the prediction down tightly *(required: the kernel-decay-to-correlation-to-variance chain)* | — |
| T1 | transfer | short-answer | 2.32 | Why are GPs especially valued in Bayesian optimization (e.g. tuning expensive-to-evaluate hyperparameters)? | the built-in uncertainty estimates (E1) let an optimization algorithm balance exploitation (trying points near where the GP predicts good outcomes) against exploration (trying points where the GP is highly uncertain and might reveal a surprising improvement) — directly useful for the expensive hyperparameter tuning `hyperparameters` described *(required: names both exploitation and exploration explicitly)* | — |
| R3 | recall | short-answer | 0.37 | What two functions fully specify a Gaussian Process? | a mean function m(x) and a covariance (kernel) function k(x, x') | — |
| R4 | recall | mcq | 0.42 | A GP is often described as a distribution over: | functions — an infinite-dimensional object, any finite slice of which is multivariate Normal | picks "a distribution over a single scalar parameter" — a GP is a distribution over entire functions, not one number → `gp-regression` |
| R5 | recall | short-answer | 0.47 | Fill in the blank: the "length-scale" hyperparameter of a kernel like the RBF controls how quickly correlation ___ as two input points move apart. | decays (drops off) | — |
| R6 | recall | mcq | 0.52 | Name a commonly used kernel function for Gaussian Processes. | the RBF (squared exponential) kernel | picks "the sigmoid activation function" — that is a neural-network activation, not a GP kernel → `gp-regression` |
| R7 | recall | short-answer | 0.57 | What role does the "signal variance" hyperparameter play in a GP's kernel? | it scales the overall amplitude of the function's variation — how far function values typically range above and below the mean | — |
| R8 | recall | mcq | 0.62 | True or false: a GP's computational cost for exact inference typically scales cubically, O(n³), with the number of training points n. | True | picks "False" — exact GP inference requires inverting an n×n covariance matrix, which costs O(n³), a well-known scalability bottleneck → `gp-regression` |
| R9 | recall | short-answer | 0.67 | What is meant by calling a GP a "non-parametric" model? | its effective complexity grows with the amount of data (via the size of the covariance matrix over observed points), rather than being fixed by a small, predetermined set of parameters | — |
| R10 | recall | mcq | 0.72 | Sparse GP approximations exist primarily to address: | the O(n³) computational cost of exact GP inference on large datasets | picks "the need to model discrete rather than continuous outputs" — that's a different problem (addressed by GP classification), not what sparse approximations target → `gp-regression` |
| R11 | recall | short-answer | 0.77 | What does the "noise variance" hyperparameter in a GP regression model represent? | the assumed variance of the observation noise added to the true function values, σ² in y = f(x) + ε | — |
| R12 | recall | short-answer | 0.82 | Fill in the blank: hyperparameters of a GP's kernel are commonly tuned by maximizing the ___. | marginal likelihood (evidence) of the observed data | — |
| R13 | recall | mcq | 0.87 | A GP prior with a very short length-scale kernel will produce sample functions that are: | wiggly, varying rapidly over short input distances | picks "extremely smooth, nearly constant" — a short length-scale means correlation decays quickly, producing rapidly-varying, not smooth, functions → `gp-regression` |
| R14 | recall | short-answer | 0.92 | What does the Matérn kernel family let a practitioner control that the RBF kernel does not directly expose? | the smoothness (differentiability) of the resulting sample functions, via an explicit smoothness parameter | — |
| R15 | recall | mcq | 0.97 | True or false: choosing a GP's kernel function is equivalent to choosing a prior belief about the function's smoothness and structure. | True | picks "False" — the kernel directly determines the covariance structure that shapes which functions the GP prior favors, which is exactly a prior belief about smoothness/structure → `gp-regression` |
| R16 | recall | mcq | 1.02 | The GP's predictive distribution at a new input point x* is: | a Gaussian with a mean and variance computed by conditioning the joint Gaussian on the observed data | picks "a point estimate with no associated uncertainty" — the entire appeal of GPs is that they yield a full predictive distribution, not just a point estimate → `gp-regression` |
| A2 | apply | short-answer | 1.07 | A dataset has 50,000 training points. Why is fitting an exact GP directly likely to be impractical? | exact GP inference requires inverting an n×n kernel matrix, costing O(n³) time and O(n²) memory; at n=50,000 this is computationally prohibitive without a sparse or approximate method | — |
| A3 | apply | short-answer | 1.17 | Two GPs are fit to the same data with RBF kernels, one with a short length-scale and one with a long length-scale. Which will interpolate more smoothly between training points? | the long length-scale GP — it assumes correlation persists over larger distances, producing a smoother interpolation, while the short length-scale GP will track local wiggles more closely | — |
| A4 | apply | mcq | 1.22 | A practitioner selects kernel hyperparameters by maximizing the marginal likelihood rather than by cross-validation. What is a reasonable justification? | the marginal likelihood already integrates over the uncertainty in the function values, giving a principled Bayesian criterion without needing a separate held-out validation set | picks "cross-validation is never applicable to GPs" — cross-validation can be used with GPs too; marginal-likelihood tuning is simply an alternative, not the only option → `gp-regression` |
| A5 | apply | short-answer | 1.27 | Why would a practitioner choose a periodic kernel instead of an RBF kernel to model monthly temperature data? | a periodic kernel directly encodes the belief that the function repeats with a fixed period, matching the seasonal cycle in temperature, which a plain RBF kernel has no mechanism to represent | — |
| A6 | apply | short-answer | 1.3 | A GP is fit with the noise variance hyperparameter set far too low. What symptom would you expect in its predictions at training points? | it would essentially interpolate the training data exactly (near-zero uncertainty at observed points), likely overfitting to noise in the observations rather than smoothing over it | — |
| E2 | explain | short-answer | 1.87 | Why does the choice of kernel function amount to encoding a prior belief about the space of plausible functions, before seeing any data? | the kernel determines the covariance structure of the GP prior — how correlated function values are at nearby versus distant points — which directly shapes which functions are likely to be drawn from the prior (smooth vs. wiggly, periodic vs. not) *(required: connects the covariance structure directly to what functions the prior favors)* | — |
| E3 | explain | short-answer | 1.92 | Explain why maximizing the marginal likelihood to choose hyperparameters naturally balances fit against complexity, without an explicit regularization term. | the marginal likelihood integrates over all possible function values consistent with the prior and data, and an overly flexible kernel (very short length-scale) spreads its prior probability mass over vastly more functions, diluting the likelihood assigned to any one — this automatically penalizes excessive flexibility the way an explicit complexity penalty would *(required: the automatic-penalty-via-integration argument, not just "it fits the data well")* | — |
| E4 | explain | short-answer | 1.97 | Why does a GP's predictive variance shrink to (close to) zero exactly at observed training points, when the noise variance hyperparameter is small? | conditioning the joint Gaussian on an observed value with little assumed noise leaves almost no residual uncertainty about the function's value there — the model has essentially been told what the answer is at that exact input *(required: connects the near-zero variance to conditioning on a low-noise observation)* | — |
| E5 | explain | short-answer | 2.02 | Why do sparse GP approximations (using a small set of "inducing points") trade some accuracy for a large speed gain? | by summarizing the full dataset's information through a much smaller set of inducing points, the expensive n×n matrix operations are replaced with operations on a much smaller matrix — reducing cost roughly from O(n³) to O(nm²) for m ≪ n inducing points, at the cost of losing some information not captured by that summary *(required: names the inducing-point mechanism and the resulting complexity change)* | — |
| T2 | transfer | short-answer | 2.37 | How does a GP with a linear kernel relate directly back to ordinary Bayesian linear regression? | a GP prior with a linear kernel k(x,x')=x·x' is mathematically equivalent to placing a Gaussian prior directly on the coefficients of a linear regression model — the GP framework, with this specific kernel choice, exactly recovers Bayesian linear regression as a special case *(required: names the linear-kernel-equals-Bayesian-linear-regression equivalence)* | — |
| T3 | transfer | short-answer | 2.42 | Why does the kernel's central role in a GP connect this concept directly to the earlier `kernel` and `mercers-theorem` concepts from the SVM cluster? | the same positive-semi-definiteness requirement (Mercer's condition) that legitimizes a kernel for the kernel trick in SVMs is exactly what's required for a function to be a valid GP covariance function — both uses of "kernel" rely on the identical mathematical validity condition, not a coincidental naming overlap *(required: names Mercer's condition as the shared requirement)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.32…2.42.*

---

## GP Classification (`gp-classification`)
*Prereq: GP Regression, Logistic Regression · ancestors 56 · b₀ = 1.52*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.52 | Describe the key challenge extending GP regression to classification, and its standard solution. | a GP's raw output is a continuous Gaussian value, but classification needs a probability in [0,1]; passing the output through a squashing/link function (the sigmoid, as in logistic regression) maps it to a valid probability | — |
| R2 | recall | mcq | 0.82 | Once passed through a sigmoid link, exact Bayesian inference becomes: | generally intractable in closed form, requiring approximation methods (e.g. the Laplace approximation) | claims it stays "exactly as easy/closed-form as GP regression" — the nonlinearity breaks the closed-form Gaussian-conditioning property entirely → `gp-classification` |
| A1 | apply | short-answer | 1.32 | Why does GP regression have a closed-form solution while GP classification doesn't? | conditioning one Gaussian on another stays Gaussian, giving GP regression its clean closed form; passing the underlying Gaussian through a nonlinear sigmoid destroys that Gaussian structure entirely, so the classification posterior is no longer Gaussian and has no simple closed form *(required: names the sigmoid's nonlinearity as specifically what breaks the closed form)* | — |
| E1 | explain | short-answer | 2.02 | What role does the sigmoid link play, and how does it connect GP classification to logistic regression directly? | both use the sigmoid to convert an unbounded real-valued score — a linear combination in logistic regression, a GP function value here — into a valid probability in [0,1]; the same function serving the same conversion purpose in both methods *(required: names the shared purpose explicitly, not just "both use sigmoid")* | — |
| T1 | transfer | short-answer | 2.52 | Describe GP classification as a generalization of logistic regression. | GP classification is a nonparametric, infinite-dimensional generalization of logistic regression — rather than assuming the underlying score is a simple linear function of the inputs, it allows the score to be any function drawn from a flexible Gaussian process prior, at the cost of needing approximate rather than exact inference *(required: names both the generalization and its approximation-inference cost)* | — |
| R3 | recall | short-answer | 0.57 | What is the "latent function" f in GP classification? | the underlying, unobserved real-valued GP-distributed function whose value at x is squashed through the sigmoid to produce a class probability | — |
| R4 | recall | mcq | 0.62 | Name a standard approximate-inference method used for GP classification. | the Laplace approximation | picks "the normal equations" — normal equations solve linear least squares exactly; GP classification needs an approximate-inference method because its posterior isn't Gaussian → `gp-classification` |
| R5 | recall | short-answer | 0.67 | Fill in the blank: in GP classification, the observation model (likelihood) for each label is typically ___, not Gaussian. | Bernoulli (a Bernoulli/binomial likelihood over the class label) | — |
| R6 | recall | mcq | 0.72 | The Laplace approximation for GP classification works by: | fitting a Gaussian to the true posterior around its mode (MAP estimate), using a local quadratic (second-order Taylor) approximation | picks "exactly computing the true non-Gaussian posterior in closed form" — the whole reason an approximation is needed is that the true posterior has no closed form → `gp-classification` |
| R7 | recall | short-answer | 0.77 | What does the decision boundary look like in GP classification, compared to logistic regression? | it can be a flexible, nonlinear boundary (since the underlying latent function is a GP draw, not restricted to a linear combination of inputs), unlike logistic regression's linear decision boundary | — |
| R8 | recall | mcq | 0.82 | An alternative to the Laplace approximation for GP classification, generally giving better accuracy at higher cost, is: | Expectation Propagation (EP) | picks "ordinary least squares" — OLS is a linear regression fitting method, unrelated to approximating a classification posterior → `gp-classification` |
| R9 | recall | short-answer | 0.87 | Why is finding the mode of the latent function's posterior (used by the Laplace approximation) itself an iterative optimization problem? | because the posterior is not Gaussian and has no closed-form maximizer, so it is typically found by an iterative method such as Newton's method | — |
| R10 | recall | mcq | 0.92 | True or false: GP classification can be extended to more than two classes using a multi-class link function or a one-vs-rest scheme. | True | picks "False" — multiclass extensions of GP classification exist, via softmax-style multi-class links or one-vs-rest combinations of binary GP classifiers → `gp-classification` |
| R11 | recall | short-answer | 0.97 | What does "squashing" refer to in the context of the sigmoid link function used here? | mapping an unbounded real-valued score into the bounded [0,1] range interpretable as a probability | — |
| R12 | recall | mcq | 1.02 | Compared to GP regression's predictive distribution, GP classification's predictive class probability requires: | an additional integration/approximation step over the non-Gaussian posterior, since the sigmoid destroys the closed-form Gaussian result | picks "no extra computation — the same closed-form formulas apply directly" — the nonlinearity of the sigmoid link is exactly what removes the closed form, requiring further approximation → `gp-classification` |
| R13 | recall | short-answer | 1.07 | Fill in the blank: replacing the sigmoid link with a probit (Gaussian CDF) link gives an alternative form of GP classification, analogous to ___ regression instead of logistic regression. | probit | — |
| R14 | recall | short-answer | 1.12 | What kernel-related concept does GP classification inherit unchanged from GP regression? | the choice of a covariance/kernel function to encode prior beliefs about the smoothness of the latent function | — |
| R15 | recall | mcq | 1.17 | The main added difficulty GP classification introduces relative to GP regression is: | the non-Gaussian likelihood breaks conjugacy, making the posterior over the latent function intractable in closed form | picks "the number of hyperparameters, which is much larger" — GP classification typically uses the same kind of kernel hyperparameters as regression; the added difficulty is about likelihood conjugacy, not parameter count → `gp-classification` |
| R16 | recall | short-answer | 1.22 | What quantity is squashed by the sigmoid function to produce the predicted class probability? | the latent GP function's value f(x) at the input point of interest | — |
| A2 | apply | short-answer | 1.27 | A dataset has a genuinely nonlinear decision boundary (e.g. a circular boundary separating two classes). Why might GP classification outperform plain logistic regression here? | GP classification's latent function can be any smooth function drawn from the GP prior, so it can represent a curved/circular boundary directly, while logistic regression is restricted to a single linear decision boundary and would need explicit feature engineering to capture the same shape | — |
| A3 | apply | mcq | 1.32 | A practitioner needs fast, exact posterior inference for a classification task and is willing to sacrifice some model flexibility. Which is the more appropriate choice? | plain logistic regression, since it has an exactly tractable posterior under a Gaussian prior on the coefficients via the Laplace approximation applied to a much lower-dimensional (finite) parameter | picks "GP classification, since it is always more accurate" — GP classification's added flexibility comes with more expensive, approximate inference, so it is not always the better choice when speed and exactness matter → `gp-classification` |
| A4 | apply | short-answer | 1.37 | Why does GP classification's Laplace approximation become less accurate when the true posterior over the latent function is strongly skewed or multi-modal? | the Laplace approximation fits a single Gaussian centered at the mode using local curvature information; a skewed or multi-modal true posterior is poorly represented by any single symmetric Gaussian, so the approximation's quality degrades | — |
| A5 | apply | short-answer | 1.42 | A GP classifier's kernel has a very short length-scale. What effect would you expect on its decision boundary? | a highly flexible, possibly overfit boundary that wiggles to closely track individual training points, analogous to an overly flexible GP regression fit | — |
| A6 | apply | short-answer | 1.47 | Why is computing the predictive class probability at a new test point in GP classification described as requiring an "integration" step, rather than simple evaluation? | the predicted probability must average the sigmoid of f(x*) over the (approximate) posterior distribution of f(x*), not just evaluate the sigmoid at a single point estimate — that averaging is an integral over the latent function's uncertainty | — |
| E2 | explain | short-answer | 2.07 | Why does the non-conjugacy between a Bernoulli likelihood and a Gaussian prior specifically cause the intractability in GP classification? | conjugacy is what keeps the posterior in the same distributional family as the prior after seeing data (as happens in GP regression with Gaussian likelihood and Gaussian prior); a Bernoulli likelihood times a Gaussian prior does not integrate to another Gaussian, so the exact posterior has no closed form and must be approximated *(required: names conjugacy specifically as what GP regression has and classification lacks)* | — |
| E3 | explain | short-answer | 2.12 | Explain why the Laplace approximation's quality tends to improve as the amount of training data grows. | with more data, the likelihood increasingly dominates the posterior and concentrates it more tightly around its mode, making the posterior shape closer to the local quadratic (Gaussian) approximation the Laplace method assumes — the approximation is asymptotically justified even though it's approximate for finite data *(required: connects more data to a more concentrated, more Gaussian-like posterior)* | — |
| E4 | explain | short-answer | 2.17 | Why does choosing between a sigmoid (logistic) link and a probit (Gaussian CDF) link rarely make a large practical difference in GP classification? | both are smooth, monotonic S-shaped functions mapping the real line to [0,1] with very similar shapes over most of their range, differing mainly in the thickness of their tails, so predictions from the two links are usually numerically very close in practice *(required: names the similar-shape, similar-predictions point, not just "they're both valid links")* | — |
| E5 | explain | short-answer | 2.22 | Why is a MAP estimate of the latent function alone not sufficient for GP classification's predictions, unlike some other MAP-based methods? | GP classification's whole appeal is calibrated *uncertainty*, so collapsing the latent posterior to a single mode (MAP) point and ignoring its spread would throw away exactly the information a GP is meant to provide — the Laplace approximation therefore keeps a covariance around the mode, not just the mode itself *(required: connects to the uncertainty-quantification purpose of using a GP at all)* | — |
| T2 | transfer | short-answer | 2.57 | How does the intractability introduced by the sigmoid link here connect to the general theme of "approximate inference" seen with variational methods and MCMC elsewhere in the graph? | GP classification is one specific instance of a broader, recurring pattern — a Bayesian model whose posterior lacks a closed form because of a nonlinear or non-conjugate likelihood, which is exactly the situation `variational-inference-elbo` and MCMC methods are built to handle generally; the Laplace approximation here is one particular tool in that same family *(required: places Laplace approximation within the broader approximate-inference family)* | — |
| T3 | transfer | short-answer | 2.62 | Why does the progression from GP regression to GP classification mirror the earlier progression from linear regression to logistic regression? | in both cases, moving from a continuous to a categorical outcome required passing a real-valued score through a sigmoid link, which broke a previously clean closed-form solution (OLS's normal equations; GP regression's Gaussian conditioning) and forced the introduction of iterative or approximate methods — the same structural shift happens at both the parametric and nonparametric level *(required: draws the explicit parallel between both regression-to-classification transitions)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.52…2.62. This is the final concept of the entire machine-learning domain.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| kernel's role in a GP prior confused with an unrelated randomness source | `gp-regression` |
| GP classification assumed to retain regression's exact closed form | `gp-classification` |

**Cluster total: 60 items across 2 concepts (30 each).** All numeric/structural claims verified,
including the linear-kernel-equals-Bayesian-linear-regression equivalence in `gp-regression`'s T2 and
the Mercer's-condition link shared with `kernel-pca` in `gp-regression`'s T3.

---

# Machine Learning: complete

**50 / 50 concepts done.** Total items across all 9 clusters: **~255**.
