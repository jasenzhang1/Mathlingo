# Multivariate Cluster 2 — Quadratic Forms & the Regression Payoff

Multivariate MGF, Quadratic Forms in Random Vectors, Cochran's Theorem, Distribution of β̂
(4 concepts, all added in this sweep). Same table format as
[mp-01-multivariate-probability.md](mp-01-multivariate-probability.md).

The first cluster stopped at *describing* a random vector. This one is about **scalars built out of
one**, which is what every statistic in classical inference actually is, and it is written as a
single argument in four steps:

1. `multivariate-mgf` turns the multivariate normal's closure properties into one substitution —
   M_{AX+b}(t) = e^{tᵀb}M_X(Aᵀt) — instead of a change-of-variables argument on a k-dimensional
   density.
2. `quadratic-forms-random-vectors` is the observation that the sample variance, the residual sum of
   squares, the Mahalanobis distance and every Wald statistic are all XᵀAX for different A, with one
   expectation formula and one chi-square criterion between them.
3. `cochrans-theorem` says when the pieces of such a decomposition are *independent* chi-squares,
   and reduces that question to whether a column of integers adds up.
4. `distribution-of-beta-hat` spends all three at once. Every number a regression prints is a
   function of β̂ (an affine map of a normal vector) and RSS (a quadratic form in the same vector),
   and Cochran is what makes those two independent — which is the only reason a t-statistic has a
   t-distribution.

Items lean on the earlier banks rather than restating them: `multivariate-normal`'s projection
definition is what E1 of `multivariate-mgf` differentiates, `trace`'s cyclic property is the whole
proof of E[XᵀAX], `rank`'s dimension counting is where degrees of freedom come from, and
`ols-properties`' T1 ("β̂ is approximately Normal by the CLT") is deliberately answered from the
other direction here — under normal errors it is *exactly* Normal, and the CLT is the fallback, not
the justification.

---

## Multivariate MGF (`multivariate-mgf`)
*Prereq: MGF, MGF Properties, Multivariate Normal, Covariance Matrix, Mutual Independence · ancestors 39 · b₀ = 1.34*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.34 | Define the moment generating function of a random vector X in ℝᵏ. | M_X(t) = E[exp(tᵀX)] for t in a neighbourhood of the origin; the output is a scalar even though the argument is a vector; equivalently it is the univariate MGF of the scalar projection tᵀX evaluated at 1 | — |
| R2 | recall | mcq | 0.64 | The MGF of X ~ N_k(μ, Σ) is: | exp(tᵀμ + ½tᵀΣt) | drops the ½ and answers exp(tᵀμ + tᵀΣt) — the factor is inherited from the univariate exp(sm + ½s²v), not an extra convention → `multivariate-mgf` |
| A1 | apply | numeric | 1.14 | X ~ N₂(μ, Σ) with μ = (1,2)ᵀ and Σ = [[4,2],[2,3]]. Using M_Y(s) = M_X(sa) with a = (1,1)ᵀ, give Var(X₁ + X₂). `[verified]` | aᵀΣa = 4 + 2 + 2 + 3 = 11 | answers 7 by adding only the diagonal, ignoring the two covariance entries the quadratic form aᵀΣa collects → `covariance-matrix` |
| A2 | apply | short-answer | 1.44 | A vector in ℝ² has M(t) = exp(t₁ + 2t₂ + 2t₁² + 2t₁t₂ + 1.5t₂²). Name its distribution and give μ and Σ. `[verified: log M matches tᵀμ + ½tᵀΣt exactly]` | the log is a quadratic with nothing above second order, which is the signature of a normal vector, so X ~ N₂(μ, Σ); the linear part gives μ = (1,2)ᵀ; matching ½σ₁₁ = 2 and ½σ₂₂ = 1.5 gives variances 4 and 3, while the cross term gives σ₁₂ = 2 directly, since the ½ and the two off-diagonal entries cancel *(required: reading σ₁₂ off the cross term without an extra factor of 2)* | halves the cross-term coefficient as well, reporting σ₁₂ = 1 → `multivariate-mgf` |
| E1 | explain | derivation | 1.84 | Derive M_X(t) = exp(tᵀμ + ½tᵀΣt) for X ~ N_k(μ, Σ) without integrating the density. | by the projection definition of the multivariate normal, tᵀX is univariate N(tᵀμ, tᵀΣt); the univariate MGF is E[exp(sY)] = exp(sm + ½s²v), and M_X(t) is that evaluated at s = 1 with m = tᵀμ and v = tᵀΣt; uniqueness of MGFs turns the computation into a characterisation, so any vector with this MGF is multivariate normal *(required: the reduction to the univariate MGF of tᵀX, not an integration)* | — |
| E2 | explain | derivation | 2.04 | Prove M_{AX+b}(t) = exp(tᵀb)·M_X(Aᵀt), and use it to show AX + b is N(Aμ + b, AΣAᵀ) when X is multivariate normal. | tᵀ(AX + b) = (Aᵀt)ᵀX + tᵀb, so the constant leaves the expectation as exp(tᵀb) and the rest is M_X at Aᵀt — the transpose appears because the direction of the projection is pulled back through A; substituting the multivariate normal MGF gives exp(tᵀb + (Aᵀt)ᵀμ + ½(Aᵀt)ᵀΣ(Aᵀt)) = exp(tᵀ(Aμ + b) + ½tᵀ(AΣAᵀ)t), which is the N(Aμ + b, AΣAᵀ) MGF *(required: the Aᵀ substitution, not At)* | writes M_X(At), losing the transpose and with it the AΣAᵀ that every covariance calculation depends on → `multivariate-mgf` |
| T1 | transfer | short-answer | 2.34 | Why does the MGF argument show uncorrelated jointly normal blocks are independent, when zero covariance proves nothing in general? | with Σ₁₂ = 0 the exponent's quadratic form splits as sᵀΣ₁₁s + uᵀΣ₂₂u with no cross terms left, so the joint MGF factors into the two marginal MGFs, which is equivalent to independence; the argument works because the multivariate normal's exponent contains nothing above second order, so killing the second-order cross term kills all the dependence there is, whereas a general distribution has higher-order coupling that zero covariance leaves untouched — exactly `covariance`'s Cov(X, X²) = 0 counterexample *(required: the "nothing above second order" step, not just the factorisation)* | — |
| R3 | recall | short-answer | 0.04 | Fill in the blank: M_X(t) = E[exp(___)]. | tᵀX | — |
| R4 | recall | numeric | 0.09 | What is M_X(0), the MGF evaluated at the zero vector? `[verified: 1]` | 1 — E[exp(0ᵀX)] = E[1] = 1 | — |
| R5 | recall | mcq | 0.14 | True or false: the MGF of a random vector uniquely determines its joint distribution, when it exists in a neighbourhood of the origin. | true | answers "false," missing the uniqueness property the whole cluster leans on to identify distributions from their MGF → `multivariate-mgf` |
| R6 | recall | short-answer | 0.19 | How do you recover the marginal MGF of X₁ alone from the joint MGF M_X(t₁,…,t_k)? | set every other argument to 0: M_{X₁}(t₁) = M_X(t₁, 0, …, 0) | — |
| R7 | recall | mcq | 0.24 | ∂M_X(t)/∂t_i, evaluated at t=0, equals: | E[X_i] | claims it equals Var(X_i), confusing the first partial derivative (a mean) with a second moment quantity → `multivariate-mgf` |
| R8 | recall | short-answer | 0.29 | Fill in the blank: partitioned components of a vector are independent iff their joint MGF factors as the product of their ___. | marginal MGFs | — |
| R9 | recall | mcq | 0.39 | M_X(t) is, by definition, a function that is: | scalar-valued, even though its argument t is a vector | claims M_X(t) is vector-valued, matching the dimension of t → `multivariate-mgf` |
| R10 | recall | short-answer | 0.44 | What does it mean for the MGF to "exist in a neighbourhood of the origin"? | M_X(t) must be finite for every t in some open ball around t=0, not merely finite at t=0 itself | — |
| R11 | recall | mcq | 0.49 | The mixed partial derivative ∂²M_X(t)/∂t₁∂t₂, evaluated at t=0, gives: | E[X₁X₂], a cross moment | claims it gives Cov(X₁,X₂) directly, skipping the −μ₁μ₂ correction that separates a raw cross moment from a covariance → `multivariate-mgf` |
| R12 | recall | mcq | 0.54 | True or false: the MGF of a sum of independent random vectors is the product of their individual MGFs. | true | answers "false," missing the same factorization property that licenses the independence test in R8 → `multivariate-mgf` |
| R13 | recall | short-answer | 0.59 | Why is M_X(t) said to reduce to "the univariate MGF of the scalar projection tᵀX"? | because M_X(t)=E[exp(tᵀX)] is exactly the univariate MGF of the scalar random variable tᵀX, evaluated at argument 1 | — |
| R14 | recall | mcq | 0.69 | The multivariate normal's MGF, exp(tᵀμ + ½tᵀΣt), is defined for: | every finite t — it exists everywhere, not just near the origin | claims it's defined only for t in a small neighbourhood of 0, missing that the MVN's MGF is exceptionally well-behaved and exists for all t → `multivariate-mgf` |
| R15 | recall | short-answer | 0.74 | What role does uniqueness of the MGF play in showing a vector is multivariate normal? | if a candidate MGF matches the known form exp(tᵀμ + ½tᵀΣt), uniqueness lets you conclude the vector *is* multivariate normal without deriving its density directly | — |
| R16 | recall | mcq | 0.79 | Which of these is NOT guaranteed to exist for every random vector? | the MGF — it can fail to exist for heavy-tailed distributions | claims the MGF always exists for every distribution, the property that actually belongs to the characteristic function instead → `multivariate-mgf` |
| A3 | apply | numeric | 1.19 | X ~ N₂(μ, Σ) with μ=(2,−1)ᵀ and Σ=[[5,1],[1,2]]. Using a=(1,−1)ᵀ, compute Var(X₁−X₂) via aᵀΣa. `[verified: 5]` | Σa = (4,−1)ᵀ, so aᵀΣa = 4+1 = 5 | — |
| A4 | apply | short-answer | 1.29 | A vector has M(t) = exp(2t₁−t₂+3t₁²+t₁t₂+2t₂²). Give μ and Σ. `[verified: matches tᵀμ+½tᵀΣt with σ₁₁=6, σ₁₂=1, σ₂₂=4]` | the linear part gives μ=(2,−1)ᵀ; matching ½σ₁₁=3 gives σ₁₁=6, ½σ₂₂=2 gives σ₂₂=4, and the cross term σ₁₂t₁t₂=t₁t₂ gives σ₁₂=1, so Σ=[[6,1],[1,4]] *(required: the explicit ½-coefficient matching for the diagonal entries)* | — |
| A5 | apply | numeric | 1.39 | X ~ N₂(μ, Σ) with Σ=[[3,0],[0,5]] (independent components). Using a=(1,1)ᵀ, compute Var(X₁+X₂). `[verified: 8]` | aᵀΣa = 3+0+0+5 = 8 | — |
| A6 | apply | short-answer | 1.54 | For independent random vectors X and Y, state M_{X+Y}(t) in terms of M_X and M_Y. | M_{X+Y}(t) = M_X(t)·M_Y(t) | — |
| E3 | explain | short-answer | 1.89 | Why does the MGF's uniqueness property make it a more efficient independence test than checking the joint density directly? | checking independence via densities requires confirming f(x,y)=f_X(x)f_Y(y) at every point (x,y), an infinite-dimensional check; checking whether the joint MGF factors into the product of marginal MGFs is often a single algebraic check on the exponent, with uniqueness guaranteeing that passing this test is equivalent to genuine independence *(required: contrasts the density-check and MGF-factorization routes explicitly)* | — |
| E4 | explain | derivation | 1.94 | Derive Var(X_i) from the multivariate MGF by differentiating twice with respect to t_i at t=0. | ∂²M_X(t)/∂t_i²\|₀ = E[X_i²]; combined with ∂M_X(t)/∂t_i\|₀=E[X_i] (R7), Var(X_i)=E[X_i²]−(E[X_i])² follows exactly as in the univariate case, since fixing every other t_j=0 reduces M_X to the univariate MGF of X_i alone *(required: the explicit reduction to the univariate MGF of X_i)* | — |
| E5 | explain | short-answer | 1.99 | Why does M_X(t) always satisfy M_X(0)=1, and what would it mean if a candidate function failed this? | M_X(0)=E[exp(0ᵀX)]=E[1]=1 always, since the zero vector's dot product with anything is 0; a candidate function failing M(0)=1 cannot be the MGF of any genuine distribution, since it would imply the total probability doesn't sum or integrate to 1 *(required: the explicit M(0)=E[1]=1 computation)* | — |
| T2 | transfer | short-answer | 2.39 | Why is the MGF approach to proving "AX+b is normal" more efficient than a change-of-variables argument on the density? | the MGF approach reduces to one substitution — evaluating M_X at Aᵀt and multiplying by exp(tᵀb) — whereas a density-based proof needs the Jacobian of the affine map and a k-dimensional integral manipulation; this is exactly why this cluster's opening argument (E2) leans on the MGF rather than the density *(required: names the single-substitution advantage over the Jacobian route)* | — |
| T3 | transfer | short-answer | 2.44 | How does the MGF factorization criterion for independence generalize `mutual-independence`'s pairwise-versus-joint distinction? | pairwise independence between every pair of variables does not imply full mutual independence in general; the MGF factorization test — the *joint* MGF equalling the product of *all* marginal MGFs simultaneously — is precisely the joint (not pairwise) condition that genuinely certifies mutual independence, echoing why `mutual-independence` insists on a joint rather than pairwise check *(required: names why the test must be joint, not pairwise)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.04…2.44.*

---

## Quadratic Forms in Random Vectors (`quadratic-forms-random-vectors`)
*Prereq: Multivariate Normal, Covariance Matrix, Trace, Rank, Chi Square Distribution · ancestors 45 · b₀ = 1.41*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.41 | State E[XᵀAX] for a random vector with mean μ and covariance Σ. | E[XᵀAX] = tr(AΣ) + μᵀAμ, and it holds for any distribution with those two moments — normality is nowhere in it | — |
| R2 | recall | mcq | 0.71 | For X ~ N_k(0, I) and A symmetric, XᵀAX is chi-square exactly when: | A is idempotent, and then the degrees of freedom are rank(A), which for an idempotent matrix equals tr(A) | answers "whenever A is positive definite" — positive definiteness makes the form non-negative but says nothing about which distribution it has; idempotence is the condition → `quadratic-forms-random-vectors` |
| A1 | apply | numeric | 1.21 | X has mean μ = (1,2)ᵀ and covariance Σ = [[4,2],[2,3]]. Compute E of the squared norm of X. `[verified]` | tr(Σ) + μᵀμ = 7 + 5 = 12 | answers 5 by evaluating the form at the mean, the multivariate version of writing E[X²] = (E[X])² → `quadratic-forms-random-vectors` |
| A2 | apply | numeric | 1.51 | Same X. With A = [[1,−1],[−1,1]], compute E[(X₁ − X₂)²]. `[verified]` | tr(AΣ) = 2 + 1 = 3 and μᵀAμ = (1 − 2)² = 1, so E[(X₁ − X₂)²] = 4 | — |
| E1 | explain | derivation | 1.91 | Prove E[XᵀAX] = tr(AΣ) + μᵀAμ. | XᵀAX is 1 × 1 so it equals its own trace, and the trace is cyclic, giving tr(AXXᵀ); the trace is linear so expectation passes inside, leaving tr(A E[XXᵀ]) with E[XXᵀ] = Σ + μμᵀ; expanding gives tr(AΣ) + tr(Aμμᵀ) = tr(AΣ) + μᵀAμ, the last step being the cyclic property once more on a scalar *(required: the trace trick, not a coordinate-by-coordinate expansion)* | — |
| E2 | explain | derivation | 2.11 | For X ~ N_n(μ1, σ²I), show that XᵀCX/σ² ~ χ²_{n−1} where C = I − (1/n)11ᵀ is the centring matrix. `[verified: C idempotent, tr(C) = n − 1, Monte Carlo mean 4.00 and variance 8.00 at n = 5]` | C is symmetric and C² = C, since 1ᵀ1 = n makes the cross terms collapse, so C is an orthogonal projection with tr(C) = n − 1 = rank(C); an idempotent quadratic form in a standard normal vector is χ² on rank(C) degrees of freedom; the mean is annihilated because C1 = 0, so no noncentrality term survives and the distribution does not depend on μ at all *(required: both the idempotence check and the C1 = 0 step)* | — |
| T1 | transfer | short-answer | 2.41 | Why are degrees of freedom a dimension rather than a penalty for having estimated a parameter? | the df of an idempotent quadratic form is rank(A), the dimension of the subspace A projects onto; centring projects onto the space orthogonal to the vector of ones, which has dimension n − 1, and fitting p regression coefficients leaves a residual space of dimension n − p — nothing is being "subtracted for estimating something", a subspace is being removed and its dimension is the count *(required: naming rank(A) as the df and reading n − 1 as a dimension)* | treats n − 1 as an arbitrary small-sample correction, which leaves the n − p in regression and the (r−1)(c−1) in a contingency table as three unrelated rules → `quadratic-forms-random-vectors` |
| R3 | recall | short-answer | 0.05 | Fill in the blank: for X ~ N_k(0, I) and A symmetric idempotent, XᵀAX ~ χ²_r with r = ___. | rank(A), which equals tr(A) for an idempotent matrix | — |
| R4 | recall | mcq | 0.11 | An idempotent matrix A satisfies: | A² = A | claims idempotence means A⁻¹=A, confusing it with an involution rather than A²=A → `quadratic-forms-random-vectors` |
| R5 | recall | mcq | 0.16 | True or false: for an idempotent matrix, rank(A) = tr(A). | true | answers "false," missing the identity E4 derives that licenses reading off degrees of freedom from the trace → `quadratic-forms-random-vectors` |
| R6 | recall | short-answer | 0.21 | What are the only possible eigenvalues of an idempotent matrix? | 0 and 1 | — |
| R7 | recall | mcq | 0.26 | The Mahalanobis distance (x−μ)ᵀΣ⁻¹(x−μ) is an example of: | a quadratic form, with A = Σ⁻¹ | calls it "a linear form in x," missing that x appears twice (quadratically), not once → `quadratic-forms-random-vectors` |
| R8 | recall | short-answer | 0.31 | Fill in the blank: E[XᵀAX] = tr(AΣ) + ___. | μᵀAμ | — |
| R9 | recall | mcq | 0.36 | If X has mean 0, E[XᵀAX] simplifies to: | tr(AΣ) | keeps a μᵀAμ term even though μ=0 makes it vanish identically → `quadratic-forms-random-vectors` |
| R10 | recall | short-answer | 0.46 | Name two classical statistics that are themselves examples of quadratic forms XᵀAX. | e.g. the sample variance and the residual sum of squares (RSS); or a Wald statistic | — |
| R11 | recall | mcq | 0.51 | The matrix A in XᵀAX is conventionally taken to be: | symmetric, without loss of generality, since only the symmetric part of A contributes to the form | claims A must be diagonal for XᵀAX to be well defined, an unnecessarily strong requirement → `quadratic-forms-random-vectors` |
| R12 | recall | mcq | 0.56 | True or false: XᵀAX is automatically nonnegative for every matrix A. | false — only when A is positive semi-definite | answers "true," missing that A must be PSD for the quadratic form to be guaranteed nonnegative → `quadratic-forms-random-vectors` |
| R13 | recall | short-answer | 0.61 | Beyond idempotence, what additional condition on A does the χ² criterion (R3) require? | A must also be symmetric | — |
| R14 | recall | mcq | 0.66 | For A positive definite but not idempotent, and X ~ N_k(0, I), XᵀAX is: | nonnegative, but not generally chi-square distributed | claims positive definiteness alone is enough for a chi-square conclusion, conflating it with idempotence (the actual criterion from R2) → `quadratic-forms-random-vectors` |
| R15 | recall | numeric | 0.76 | In the centring matrix C = I − (1/n)11ᵀ, what is rank(C)? `[verified: n−1]` | n − 1 | — |
| R16 | recall | mcq | 0.81 | Which additional property, beyond symmetry, is required for tr(A) to equal rank(A)? | A must be idempotent | claims symmetry alone is sufficient, omitting idempotence, the property that actually forces eigenvalues into {0,1} → `quadratic-forms-random-vectors` |
| A3 | apply | numeric | 1.26 | X has mean μ=(0,1)ᵀ and Σ=[[2,0],[0,3]]. Compute E[XᵀX]. `[verified: 6]` | tr(Σ) + μᵀμ = 5 + 1 = 6 | — |
| A4 | apply | numeric | 1.31 | X has mean 0 and Σ=[[4,1],[1,2]]. With A=[[1,0],[0,0]] (selecting X₁²), compute E[X₁²]. `[verified: 4]` | tr(AΣ) = 4 (A picks out Σ₁₁), and μᵀAμ=0 since μ=0, so E[X₁²]=4=Var(X₁) | — |
| A5 | apply | short-answer | 1.36 | Is A=[[0.5,0.5],[0.5,0.5]] idempotent? Give rank(A), and the resulting chi-square df if X ~ N₂(0, I). `[verified: A²=A, trace=rank=1]` | yes, A²=A; rank(A)=tr(A)=1, so XᵀAX ~ χ²₁ | — |
| A6 | apply | short-answer | 1.46 | Is XᵀAX chi-square distributed for X ~ N₂(0, I) and A=[[1,0],[0,2]]? `[verified: A²=[[1,0],[0,4]]≠A]` | no — A is not idempotent (A²≠A), so XᵀAX is a weighted sum of independent χ²₁ terms, 1·χ²₁ + 2·χ²₁, not itself chi-square *(required: names the non-idempotence check)* | reports it as χ²₂ simply because A is 2×2 and diagonal, ignoring the idempotence requirement → `quadratic-forms-random-vectors` |
| E3 | explain | short-answer | 1.96 | Why does an idempotent matrix have only eigenvalues 0 or 1? | if Av=λv then A²v=λ²v, but idempotence gives A²v=Av=λv, so λ²=λ, which forces λ∈{0,1} *(required: the explicit λ²=λ derivation)* | — |
| E4 | explain | derivation | 2.01 | Using E3's eigenvalue fact, show rank(A)=tr(A) for a symmetric idempotent A. | trace is the sum of eigenvalues (with multiplicity); since every eigenvalue is 0 or 1, that sum equals the count of eigenvalues equal to 1 — and because A is symmetric (hence diagonalizable with an orthonormal eigenbasis), that count is exactly the rank *(required: connects the eigenvalue count to rank via symmetric diagonalizability)* | — |
| E5 | explain | short-answer | 2.06 | Why does the Mahalanobis-distance quadratic form use Σ⁻¹ rather than Σ itself? | Σ⁻¹ rescales each direction inversely to its spread, so directions of small variance (where a given deviation is "surprising") are weighted heavily and directions of large variance are weighted lightly, producing a genuinely standardized notion of distance — using Σ instead would weight things backwards *(required: the inverse-rescaling argument)* | — |
| T2 | transfer | short-answer | 2.46 | Sample variance is, up to scaling, the quadratic form XᵀCX with C the centring matrix. Why does knowing C is idempotent immediately give S²'s sampling distribution, with no fresh derivation? | since C is symmetric idempotent with rank n−1, E2's result XᵀCX/σ² ~ χ²_{n−1} applies directly with A=C, giving (n−1)S²/σ² ~ χ²_{n−1} — reusing the quadratic-forms machinery rather than deriving S²'s distribution from scratch *(required: names C as an instance of the general idempotent-quadratic-form result, not a separate fact)* | — |
| T3 | transfer | short-answer | 2.51 | A Wald statistic has the form (θ̂−θ₀)ᵀ[Var(θ̂)]⁻¹(θ̂−θ₀). Why is recognizing this as a quadratic form more useful than memorizing the formula? | recognizing it as XᵀAX with A=[Var(θ̂)]⁻¹ connects it directly to E[XᵀAX]=tr(AΣ)+μᵀAμ and the idempotency-based χ² criterion, so results about degrees of freedom (as rank) and about exact-versus-asymptotic chi-square behavior when Var(θ̂) is itself estimated transfer automatically, instead of needing separate re-derivation for every new named test statistic *(required: names the transfer of the rank-as-df and idempotency results, not just "it's a quadratic form")* | — |

*Coverage: 16/6/5/3 — 30 items, 0.05…2.51.*

---

## Cochran's Theorem (`cochrans-theorem`)
*Prereq: Quadratic Forms in Random Vectors, Mutual Independence, Sample Variance, t-Distribution · ancestors 51 · b₀ = 1.48*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.48 | State Cochran's theorem. | for X ~ N_n(0, σ²I), if the squared norm of X decomposes as a sum of quadratic forms XᵀA_iX with ranks r_i, then the ranks summing to n is equivalent to the pieces being independent, each χ²_{r_i} after dividing by σ² | — |
| R2 | recall | mcq | 0.78 | The condition that makes the pieces independent chi-squares is: | that the ranks of the quadratic forms sum to n, the ambient dimension | answers "that the pieces be uncorrelated" — uncorrelatedness is a consequence, and outside the normal family it would not give independence anyway → `cochrans-theorem` |
| A1 | apply | numeric | 1.28 | Group A is (4,6,8) and group B is (10,12,14). Build the one-way decomposition and give the F-statistic. `[verified: total 70 = between 54 + within 16, df 1 and 4]` | grand mean 9, group means 6 and 12, so between-group SS is 27 + 27 = 54 on 1 df and within-group SS is 8 + 8 = 16 on 4 df, giving F = 54/(16/4) = 13.5 | — |
| A2 | apply | short-answer | 1.58 | For X₁,…,Xₙ iid N(μ, σ²), write the decomposition Cochran is applied to and check its rank condition. | standardise to Z = (X − μ1)/σ ~ N_n(0, I) and split the squared norm as ZᵀPZ + ZᵀCZ with P = (1/n)11ᵀ and C = I − P; rank(P) = 1 and rank(C) = n − 1, which sum to n, so the two pieces are independent with the first χ²₁ and the second χ²_{n−1} *(required: the explicit rank count 1 + (n − 1) = n)* | — |
| E1 | explain | derivation | 1.98 | Use the decomposition to prove X̄ and S² are independent, and assemble the t-statistic from the pieces. | the two pieces are functions of X̄ and of S² separately, so Cochran's independence transfers to them; the first gives √n(X̄ − μ)/σ ~ N(0,1) and the second gives (n−1)S²/σ² ~ χ²_{n−1}; dividing the standard normal by the square root of the independent chi-square over its df cancels σ and leaves √n(X̄ − μ)/S ~ t_{n−1} — the independence is what licenses the ratio, since the t is defined only for independent numerator and denominator *(required: naming the independence as what the t definition needs)* | — |
| E2 | explain | short-answer | 2.18 | Why does the theorem need the covariance to be σ²I specifically? | the proof rotates to an orthonormal basis adapted to the subspaces, which is legitimate only because N(0, σ²I) is rotation-invariant — QᵀX has covariance QᵀσIQ = σ²I, the same distribution; with a general Σ that step fails, the pieces become weighted mixtures of chi-squares rather than chi-squares, and the nominal degrees of freedom are wrong, which is exactly what Welch and Satterthwaite corrections repair *(required: rotation-invariance as the property being used)* | — |
| T1 | transfer | short-answer | 2.48 | An ANOVA table's degrees-of-freedom column sums to n. What is that really asserting, and what breaks in an unbalanced design with sequential sums of squares? | it asserts Cochran's rank condition, so the column summing correctly is the certificate that the sums of squares are independent chi-squares and the F-ratios are genuine F's; in an unbalanced design the subspaces for different factors are not orthogonal, so sequential sums of squares depend on the order the terms enter and the pieces are not independent — the ranks may still add up while the orthogonality that gave them meaning does not hold *(required: connecting non-orthogonal subspaces to the order-dependence of Type I sums of squares)* | reads the df column as bookkeeping that always works out, missing that it is a claim about orthogonal subspaces → `cochrans-theorem` |
| R3 | recall | short-answer | 0.1 | Fill in the blank: Cochran's theorem requires X ~ N_n(0, σ²___). | I — the identity matrix (a common variance, uncorrelated components) | — |
| R4 | recall | mcq | 0.15 | The key condition in Cochran's theorem is that the ranks of the quadratic-form pieces: | sum to n, the ambient dimension | claims the condition is that the pieces be uncorrelated, restating R2's already-identified misconception → `cochrans-theorem` |
| R5 | recall | mcq | 0.2 | True or false: Cochran's theorem concludes the pieces are independent AND each is chi-square, after dividing by σ². | true | answers "false," missing that the theorem delivers both independence and the chi-square distribution together → `cochrans-theorem` |
| R6 | recall | short-answer | 0.25 | What single algebraic fact does Cochran's theorem reduce independence-of-chi-squares questions to? | whether a column of integers (the ranks of the pieces) sums to n | — |
| R7 | recall | mcq | 0.3 | Cochran's theorem is central to proving the independence of which two sample statistics? | X̄ and S² | names X̄ and the sample range instead of X̄ and S², the pair E1 actually establishes → `cochrans-theorem` |
| R8 | recall | short-answer | 0.35 | Fill in the blank: the F-statistic in one-way ANOVA is the ratio of the ___ mean square to the ___ mean square. | between-group; within-group | — |
| R9 | recall | mcq | 0.4 | Why does Cochran's theorem require covariance σ²I specifically, rather than a general Σ? | the proof rotates to an orthonormal basis, a step that preserves the distribution only because N(0, σ²I) is rotation-invariant | claims σ²I is required merely "for simpler arithmetic," missing the rotation-invariance argument E2 makes precise → `cochrans-theorem` |
| R10 | recall | short-answer | 0.55 | What happens to the nominal degrees of freedom in an ANOVA-style decomposition if the true covariance is not σ²I? | they become incorrect — the pieces are no longer exact chi-squares, motivating corrections like Welch or Satterthwaite | — |
| R11 | recall | mcq | 0.6 | In the one-way ANOVA decomposition, the centred sum of squares splits into which two pieces? | the between-group sum of squares and the within-group sum of squares | names "the mean and the variance" as the two pieces, confusing summary statistics with the sum-of-squares decomposition itself → `cochrans-theorem` |
| R12 | recall | mcq | 0.65 | True or false: Cochran's theorem is a special case of the idempotent-quadratic-form machinery from `quadratic-forms-random-vectors`. | true | answers "false," missing that Cochran's theorem builds directly on the earlier chi-square criterion for idempotent quadratic forms → `cochrans-theorem` |
| R13 | recall | numeric | 0.7 | In a one-way ANOVA with g groups, what degrees of freedom are assigned to the between-group piece? `[verified: g−1]` | g − 1 | — |
| R14 | recall | mcq | 0.85 | In Cochran's decomposition ‖X‖²=Σᵢ XᵀAᵢX, what must each Aᵢ individually satisfy, beyond the rank-sum condition? | each Aᵢ must be symmetric and idempotent | claims each Aᵢ need only be positive semi-definite, omitting the idempotence that is actually required for each piece to be chi-square at all → `cochrans-theorem` |
| R15 | recall | short-answer | 0.9 | Why is Cochran's rank-sum condition described as "checking whether a column of integers adds up"? | each quadratic form's degrees of freedom is an integer (its rank), and the condition reduces to verifying these integers — listed in an ANOVA table — sum to n, an arithmetic check rather than a distributional one | — |
| R16 | recall | mcq | 0.95 | What breaks when subspaces in an unbalanced ANOVA are not orthogonal? | sequential sums of squares become order-dependent, and the pieces are no longer guaranteed independent | claims nothing breaks as long as the degrees of freedom still sum to n, missing that the rank-sum condition alone doesn't guarantee orthogonality → `cochrans-theorem` |
| A3 | apply | numeric | 1.33 | Group A is (5,7,9) and Group B is (2,4,6). Build the one-way decomposition and give the F-statistic. `[verified: grand mean 5.5, between SS 13.5, within SS 16.0, F=3.375]` | between SS=13.5 on 1 df, within SS=16.0 on 4 df, giving F = (13.5/1)/(16/4) = 3.375 | — |
| A4 | apply | numeric | 1.38 | A one-way ANOVA has g=4 groups and n=20 total observations. Give the between- and within-group degrees of freedom, and confirm they sum to n−1. `[verified: 3+16=19=n−1]` | between: g−1=3; within: n−g=16; 3+16=19=n−1 | — |
| A5 | apply | short-answer | 1.43 | A decomposition splits ‖X‖² into three pieces with ranks 2, 3, and 5, and the total dimension is n=10. Does Cochran's condition hold, and what follows if so? | yes — 2+3+5=10=n, so Cochran's condition holds; the three pieces are mutually independent, each χ² on its respective degrees of freedom (2, 3, and 5) after dividing by σ² | — |
| A6 | apply | numeric | 1.63 | Group A is (10,12), Group B is (14,16), Group C is (18,20). Compute the ANOVA F-statistic. `[verified: grand mean 15.0, between SS 64.0 (df 2), within SS 6.0 (df 3), F=16.0]` | F = (64/2)/(6/3) = 32/2 = 16 | — |
| E3 | explain | short-answer | 2.03 | Why does Cochran's theorem require each piece's Aᵢ to be individually symmetric and idempotent, rather than just requiring the ranks to sum to n? | the rank-sum condition alone doesn't guarantee each piece is even a valid chi-square-producing quadratic form; each Aᵢ must be symmetric idempotent (per `quadratic-forms-random-vectors`) for XᵀAᵢX/σ² to be chi-square at all — the rank-sum condition then additionally certifies the pieces are mutually independent, not merely individually chi-square *(required: distinguishes the per-piece chi-square requirement from the joint independence requirement)* | — |
| E4 | explain | derivation | 2.08 | Verify Cochran's condition for the one-way ANOVA decomposition of the *uncentred* ‖X‖² with g groups and n observations: show the grand-mean, between-, and within-group ranks sum to n. `[verified: 1+(g−1)+(n−g)=n]` | the grand-mean piece has rank 1, the between-group piece has rank g−1, and the within-group piece has rank n−g; summing gives 1+(g−1)+(n−g)=n exactly *(required: includes the grand-mean piece, not just between/within, to reach n rather than n−1)* | — |
| E5 | explain | short-answer | 2.13 | Why does an unbalanced design threaten Cochran's independence conclusion even when the individual ranks still happen to sum to n? | Cochran's proof requires the projection matrices Aᵢ to correspond to *orthogonal* subspaces, not merely subspaces whose dimensions add up; in an unbalanced design the factor subspaces are not orthogonal, so even when the ranks numerically sum to n the corresponding quadratic forms may fail to be mutually independent — the rank-sum condition is necessary but its sufficiency relies on orthogonality actually holding *(required: distinguishes the numeric rank-sum check from the orthogonality it presupposes)* | — |
| T2 | transfer | short-answer | 2.53 | Two-way ANOVA with interaction splits variation into main effects, interaction, and error. Using Cochran's theorem, what must be true of these subspaces for the resulting F-tests to be valid? | each source of variation — each main effect, the interaction, and the error — must correspond to mutually orthogonal subspaces whose ranks sum to n; only then does Cochran's theorem guarantee the corresponding sums of squares are independent chi-squares, which is what licenses treating each mean-square ratio as a genuine F-statistic *(required: names orthogonality and the rank-sum condition together, not just "the df add up")* | — |
| T3 | transfer | short-answer | 2.58 | Why is Cochran's theorem described as reducing a distributional independence question to "whether a column of integers adds up," and what does that buy practitioners building ANOVA tables? | given orthogonal design subspaces, a practitioner never needs to re-derive independence for a new experimental design from scratch — they only need to confirm the degrees-of-freedom column sums to n (or n−1 after removing the grand mean), turning what would otherwise be a nontrivial probability argument into routine bookkeeping *(required: names that the theorem replaces a fresh probability argument with an arithmetic check)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.1…2.58.*

---

## Distribution of β̂ (`distribution-of-beta-hat`)
*Prereq: Cochran's Theorem, Multivariate MGF, Linear Regression (Probabilistic Version), Geometric Interpretation of OLS, F-Distribution · ancestors 69 · b₀ = 1.62*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.62 | Under y = Xβ + ε with ε ~ N(0, σ²I) and X of full column rank, state the exact distribution of β̂. | β̂ ~ N_p(β, σ²(XᵀX)⁻¹) — multivariate normal, unbiased, with a covariance that depends on the design and σ² but never on y | — |
| R2 | recall | mcq | 0.92 | The exactness of that distribution in finite samples rests on: | the errors being Normal, together with β̂ being an exactly linear function of y | credits the central limit theorem — the CLT is the fallback when the errors are not Normal and delivers only an asymptotic statement, so it cannot be what makes a finite-sample result exact → `distribution-of-beta-hat` |
| A1 | apply | numeric | 1.42 | Four observations at x = 1,2,3,4 with an intercept, and σ² = 1. Compute Var(β̂₁). `[verified: (XᵀX)⁻¹ = [[1.5,−0.5],[−0.5,0.2]]]` | XᵀX = [[4,10],[10,30]] with determinant 20, so the inverse has lower-right entry 4/20, so Var(β̂₁) = σ²/S_xx with S_xx = 5, i.e. 1/5 = 0.2 | — |
| A2 | apply | numeric | 1.72 | Same design with y = (3,4,6,8). Compute the t-statistic for the slope. `[verified: β̂ = (1, 1.7), RSS = 0.30, s² = 0.15, SE = 0.1732, t = 9.8150]` | β̂₁ = 8.5/5 = 1.7 with residuals 0.3, −0.4, −0.1, 0.2, so RSS = 0.30 and s² = 0.30/(4 − 2) = 0.15, giving SE = √(0.15 × 0.2) = 0.1732 and t ≈ 9.82 | divides RSS by n instead of n − p, understating the standard error because the df is the rank of I − H, not the sample size → `quadratic-forms-random-vectors` |
| E1 | explain | derivation | 2.12 | Derive β̂ ~ N_p(β, σ²(XᵀX)⁻¹) from the affine-map rule for multivariate normals. | β̂ = Ay with A = (XᵀX)⁻¹Xᵀ a fixed matrix, so β̂ is an affine image of y ~ N(Xβ, σ²I) and is therefore multivariate normal; its mean is AXβ = β and its covariance is A(σ²I)Aᵀ = σ²(XᵀX)⁻¹XᵀX(XᵀX)⁻¹ = σ²(XᵀX)⁻¹, where the collapse depends on the error covariance being a multiple of the identity *(required: identifying β̂ as exactly linear in y, and using σ²I in the covariance collapse)* | — |
| E2 | explain | derivation | 2.32 | Show that RSS/σ² ~ χ²_{n−p} and that it is independent of β̂, then assemble the t-statistic. | residuals are e = (I − H)y = (I − H)ε since (I − H)X = 0, so RSS/σ² is a quadratic form in a standard normal vector with I − H idempotent of rank n − p, hence χ²_{n−p} and E[s²] = σ²; β̂ − β = (XᵀX)⁻¹Xᵀε is a linear form whose matrix satisfies (XᵀX)⁻¹Xᵀ(I − H) = 0, so by Cochran the fit and the residuals live in orthogonal subspaces and are independent; dividing the standard normal (β̂ⱼ − βⱼ)/(σ√[(XᵀX)⁻¹]ⱼⱼ) by the root of the independent χ²_{n−p}/(n−p) cancels σ and gives t_{n−p} *(required: the orthogonality (XᵀX)⁻¹Xᵀ(I − H) = 0 as the source of the independence)* | — |
| T1 | transfer | short-answer | 2.52 | A joint F-test rejects while no individual t-statistic is significant. What does the geometry of β̂'s distribution say is going on? | the F-test uses the full covariance σ²(XᵀX)⁻¹ and asks whether β̂ lies outside a confidence ellipsoid, while each t looks only at one coordinate's marginal; with strongly correlated predictors the ellipsoid is long and thin along a diagonal, so the origin can sit far outside it while lying inside every one-dimensional projection — collinearity inflates the diagonal of (XᵀX)⁻¹ without inflating the precision of the linear combination the data actually pin down *(required: the ellipsoid-versus-marginal-projection framing, not just "collinearity inflates variances")* | reads the disagreement as an error in one of the tests, rather than as a marginal and a joint statement about the same normal vector → `distribution-of-beta-hat` |
| T2 | transfer | short-answer | 2.62 | Which reported regression numbers survive dropping normality, and which survive dropping homoskedasticity? | dropping normality leaves β̂ unbiased and BLUE by Gauss-Markov and asymptotically normal by the CLT, so t and F become approximations rather than exact — this is the honest reading of `ols-properties`' CLT argument, which is the fallback rather than the justification; dropping homoskedasticity breaks the covariance formula itself, since Cov(β̂) becomes (XᵀX)⁻¹XᵀΣX(XᵀX)⁻¹ and RSS is no longer chi-square, so every printed standard error is wrong rather than merely approximate, and sandwich estimators or weighted least squares are needed *(required: the distinction that non-normality costs exactness while heteroskedasticity costs correctness)* | — |
| R3 | recall | short-answer | 0.2 | Fill in the blank: β̂ = (XᵀX)⁻¹___. | Xᵀy | — |
| R4 | recall | mcq | 0.25 | Cov(β̂) equals: | σ²(XᵀX)⁻¹ | claims Cov(β̂) = σ²I, dropping the design-dependent (XᵀX)⁻¹ factor entirely → `distribution-of-beta-hat` |
| R5 | recall | mcq | 0.3 | True or false: β̂ is an exactly unbiased estimator of β in finite samples, assuming the model is correctly specified. | true | answers "false," treating unbiasedness as only an asymptotic property when it holds exactly here → `distribution-of-beta-hat` |
| R6 | recall | short-answer | 0.35 | What is the hat matrix H, and what does it do to y? | H = X(XᵀX)⁻¹Xᵀ; it projects y onto the fitted values, Hy = ŷ | — |
| R7 | recall | mcq | 0.4 | H is: | symmetric and idempotent — an orthogonal projection matrix | claims H is diagonal, missing that it is a full projection matrix rather than a simple scaling → `distribution-of-beta-hat` |
| R8 | recall | short-answer | 0.45 | Fill in the blank: tr(H) = ___. | p, the number of estimated regression coefficients | — |
| R9 | recall | mcq | 0.5 | rank(I−H) equals: | n−p, the residual degrees of freedom | claims rank(I−H) equals n, missing that fitting p coefficients removes p dimensions from the residual space → `distribution-of-beta-hat` |
| R10 | recall | short-answer | 0.7 | What is s², the standard estimator of σ²? | s² = RSS/(n−p) | — |
| R11 | recall | mcq | 0.75 | The standard error SE(β̂ⱼ) is: | s·√[(XᵀX)⁻¹]ⱼⱼ | omits the s factor, reporting SE(β̂ⱼ) = √[(XᵀX)⁻¹]ⱼⱼ as if σ² were known exactly rather than estimated → `distribution-of-beta-hat` |
| R12 | recall | mcq | 0.8 | True or false: the t-statistic for testing H₀:βⱼ=0 is (β̂ⱼ−0)/SE(β̂ⱼ). | true | answers "false," missing the standard construction of a t-statistic from an estimate and its standard error → `distribution-of-beta-hat` |
| R13 | recall | short-answer | 0.85 | Under what specific assumption on the errors is β̂'s distribution *exactly* normal, not just asymptotically? | ε ~ N(0, σ²I) — normally distributed errors | — |
| R14 | recall | mcq | 1.0 | The residual vector e=(I−H)y equals: | (I−H)ε, since (I−H)X=0 | claims e=(I−H)y equals (I−H)β̂ instead, confusing the residual identity with an unrelated expression → `distribution-of-beta-hat` |
| R15 | recall | short-answer | 1.05 | What does (I−H)X=0 mean geometrically? | the residuals are orthogonal to every column of X — fitted values and residuals live in orthogonal subspaces | — |
| R16 | recall | mcq | 1.1 | The t-statistic (β̂ⱼ−βⱼ)/SE(β̂ⱼ) follows a t-distribution with how many degrees of freedom? | n−p | claims it has n degrees of freedom, ignoring that p coefficients were estimated → `distribution-of-beta-hat` |
| A3 | apply | numeric | 1.47 | Using A1's (XᵀX)⁻¹=[[1.5,−0.5],[−0.5,0.2]] and σ²=1, compute Cov(β̂₀, β̂₁). `[verified: −0.5]` | σ² × [(XᵀX)⁻¹]₀₁ = 1 × (−0.5) = −0.5 | — |
| A4 | apply | numeric | 1.52 | For A1's design (n=4, p=2), compute the residual degrees of freedom n−p. `[verified: 2]` | n − p = 4 − 2 = 2 | — |
| A5 | apply | numeric | 1.57 | For A2's worked example (s²=0.15, n−p=2), compute RSS. `[verified: 0.30]` | RSS = s² × (n−p) = 0.15 × 2 = 0.30 | — |
| A6 | apply | short-answer | 1.62 | For A2's worked example, t≈9.815. State the degrees of freedom of that t-statistic. | n−p = 4−2 = 2 | — |
| E3 | explain | short-answer | 2.17 | Why is s²=RSS/(n−p) unbiased for σ², while RSS/n is not? | E[RSS]=σ²(n−p) exactly, since RSS/σ² ~ χ²_{n−p} has mean n−p; dividing by n−p exactly cancels that factor, giving E[s²]=σ², whereas dividing by n instead leaves a systematic downward bias of factor (n−p)/n *(required: the explicit E[RSS]=σ²(n−p) computation)* | — |
| E4 | explain | derivation | 2.22 | Show H=X(XᵀX)⁻¹Xᵀ is idempotent. | H² = X(XᵀX)⁻¹XᵀX(XᵀX)⁻¹Xᵀ = X(XᵀX)⁻¹Xᵀ = H, since XᵀX(XᵀX)⁻¹=I cancels the two middle factors *(required: the explicit cancellation of XᵀX(XᵀX)⁻¹)* | — |
| E5 | explain | short-answer | 2.27 | Why does tr(H)=p, using H's eigenvalues? | H is symmetric idempotent, so (per `quadratic-forms-random-vectors`'s eigenvalue result) its eigenvalues are all 0 or 1 and tr(H)=rank(H); H's range is exactly the column space of X, which has dimension p when X has full column rank, so tr(H)=p *(required: connects tr(H) to rank(H) via the 0/1-eigenvalue fact, then to the column space's dimension)* | — |
| T3 | transfer | short-answer | 2.7 | A regression is run twice — once with raw predictors, once after standardizing every predictor to mean 0, variance 1. How does this affect β̂'s distribution and the resulting t-statistics? | standardizing is an invertible linear reparameterization of X, so by the same affine-closure argument E1 uses, β̂ under the new parameterization is still exactly multivariate normal, with a rescaled covariance (XᵀX)⁻¹ reflecting the new column scaling; each individual t-statistic is nonetheless invariant to this rescaling, since β̂ⱼ and its SE scale by the same factor and the ratio cancels it — hypothesis tests on individual coefficients are unaffected even though the coefficients' raw values and units change *(required: names both the covariance rescaling and the cancellation that leaves t invariant)* | — |

*Coverage: 16/6/5/3 — 30 items, 0.2…2.7.*

---

## Cluster misconception index

| Misconception | Fires on | Blame |
|---|---|---|
| Drops the ½ from the multivariate normal MGF exponent | `multivariate-mgf` R2 | `multivariate-mgf` |
| Halves the cross term when reading Σ off a quadratic exponent | `multivariate-mgf` A2 | `multivariate-mgf` |
| Adds only the diagonal of Σ for the variance of a sum | `multivariate-mgf` A1 | `covariance-matrix` |
| Substitutes At rather than Aᵀt in the affine MGF rule | `multivariate-mgf` E2 | `multivariate-mgf` |
| Confuses positive definiteness with idempotence as the chi-square criterion | `quadratic-forms-random-vectors` R2 | `quadratic-forms-random-vectors` |
| Evaluates the quadratic form at the mean, dropping tr(AΣ) | `quadratic-forms-random-vectors` A1 | `quadratic-forms-random-vectors` |
| Reads degrees of freedom as a penalty rather than a subspace dimension | `quadratic-forms-random-vectors` T1 | `quadratic-forms-random-vectors` |
| Takes uncorrelatedness as Cochran's condition | `cochrans-theorem` R2 | `cochrans-theorem` |
| Treats the ANOVA df column as bookkeeping rather than an orthogonality claim | `cochrans-theorem` T1 | `cochrans-theorem` |
| Credits the CLT for a finite-sample exact distribution | `distribution-of-beta-hat` R2 | `distribution-of-beta-hat` |
| Divides RSS by n rather than by n − p | `distribution-of-beta-hat` A2 | `quadratic-forms-random-vectors` |
| Reads an F/t disagreement as an error rather than a joint-versus-marginal statement | `distribution-of-beta-hat` T1 | `distribution-of-beta-hat` |
| Confuses idempotence (A²=A) with an involution (A⁻¹=A) | `quadratic-forms-random-vectors` R4 | `quadratic-forms-random-vectors` |
| Treats positive definiteness as sufficient for a chi-square conclusion, in place of idempotence | `quadratic-forms-random-vectors` R14 | `quadratic-forms-random-vectors` |
| Reports XᵀAX as chi-square whenever A is diagonal, without checking idempotence | `quadratic-forms-random-vectors` A6 | `quadratic-forms-random-vectors` |
| Treats σ²I as an arithmetic convenience rather than the rotation-invariance requirement Cochran's proof needs | `cochrans-theorem` R9 | `cochrans-theorem` |
| Omits the grand-mean piece when checking Cochran's rank-sum condition for ANOVA | `cochrans-theorem` E4 | `cochrans-theorem` |
| Drops the (XᵀX)⁻¹ design factor, treating Cov(β̂) as σ²I | `distribution-of-beta-hat` R4 | `distribution-of-beta-hat` |
| Uses n rather than n−p as the t-statistic's degrees of freedom | `distribution-of-beta-hat` R16 | `distribution-of-beta-hat` |

**Cluster total: 120 items across 4 concepts, difficulty 0.04…2.7.**

Numeric claims were verified against an independent computation (NumPy) rather than by hand: the
E[XᵀAX] = tr(AΣ) + μᵀAμ values 12 and 4 against a two-million-draw Monte Carlo (11.99 and 4.00,
with the variance formula 2tr(AΣAΣ) + 4μᵀAΣAμ matching at 162.0 and 30.1); the centring matrix's
idempotence, rank and the χ²₄ mean and variance (3.995 and 8.00) at n = 5; the ANOVA split
70 = 54 + 16 with F = 13.5; and the whole worked regression — (XᵀX)⁻¹ = [[1.5, −0.5], [−0.5, 0.2]],
β̂ = (1, 1.7), RSS = 0.30, s² = 0.15, SE(β̂₁) = 0.17321, t = 9.8150, tr(H) = 2 and
rank(I − H) = 2.
