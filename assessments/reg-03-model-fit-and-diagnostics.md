# Regression Cluster 3 — Model Fit & Diagnostics

OLS Properties, SSR/SSE/SST, R², ANOVA, Effect of Adding Another Variable, Variance Inflation Factor
(6 concepts). Same format as [foundations-of-probability.md](foundations-of-probability.md).

`ssr-sse-sst`'s E1 derives its identity from the *same* orthogonality fact `normal-equations`'s T1
established; `anova`'s E1 closes the loop back to `f-distribution`'s original definition as a ratio of
independent chi-squares.

---

## OLS Properties (`ols-properties`)
*Prereq: OLS Assumptions, Linear Regression (Probabilistic Version) · ancestors 31 · b₀ = 1.23*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.23 | List the key properties of OLS estimators under standard assumptions. | unbiased; consistent; under normality, β̂ is itself Normally distributed; and BLUE per Gauss-Markov | — |
| R2 | recall | mcq | 0.53 | Var(β̂)=σ²(XᵀX)⁻¹ depends on: | σ² and the design matrix X's structure — how spread out/collinear the predictors are | claims it depends "only on n" — ignoring both σ² and X's structure entirely → `ols-properties` |
| A1 | apply | short-answer | 1.03 | Why does spreading out X values more (larger Var(X)) decrease Var(β̂₁) in simple regression? | Var(β̂₁)=σ²/Σ(xᵢ−x̄)²; a larger spread in x directly increases the denominator, shrinking the variance — a practical design implication: spreading treatment levels in an experiment gives more precise coefficient estimates *(required: the explicit denominator argument)* | — |
| E1 | explain | short-answer | 1.73 | Why does β̂ being Normally distributed follow directly from the errors being Normal? | β̂=(XᵀX)⁻¹Xᵀy is a linear function of y; linear combinations of Normal random variables are themselves Normal — the same closure property `multivariate-normal` established for the Normal family under linear operations *(required: the explicit linear-function-of-y argument, tied to that closure property)* | — |
| T1 | transfer | short-answer | 2.23 | Why is β̂ still approximately Normal for large samples even without assuming Normal errors? | β̂ is essentially a weighted sum of the yᵢ's; CLT applies to sums of random variables regardless of their individual distribution shape, giving β̂ approximate normality asymptotically even when the errors aren't exactly Normal *(required: the direct CLT connection, naming β̂ as a sum/weighted average)* | — |
| R3 | recall | short-answer | 0.03 | Fill in the blank: β̂ is unbiased means E[β̂] = ___. | β, the true parameter vector | — |
| R4 | recall | short-answer | 0.08 | What does "consistent" mean for an estimator as n→∞? | the estimator converges in probability to the true parameter value as the sample size grows without bound | — |
| R5 | recall | short-answer | 0.13 | What does the acronym BLUE stand for? | Best Linear Unbiased Estimator | — |
| R6 | recall | mcq | 0.18 | True/false: OLS is BLUE even without Normal errors, given the other Gauss-Markov assumptions. | True — BLUE-ness relies only on linearity, exogeneity, homoskedasticity, and no perfect multicollinearity; Normal errors are not required | claims "Normal errors are required for OLS to be BLUE" — Normality is needed only for exact distributional results, not for the Gauss-Markov BLUE property → `ols-properties` |
| R7 | recall | short-answer | 0.28 | Fill in the blank: Var(β̂) = ___. | σ²(XᵀX)⁻¹ | — |
| R8 | recall | mcq | 0.33 | Which of these is NOT one of the standard properties of β̂ listed under Gauss-Markov plus normality? | it minimizes the sum of absolute residuals | picks a property OLS does not have — OLS minimizes squared residuals, not absolute residuals (that is least absolute deviations regression) → `ols-properties` |
| R9 | recall | mcq | 0.38 | True/false: consistency implies unbiasedness. | False — a consistent estimator can be biased in finite samples, as long as that bias vanishes as n→∞ | claims "consistency and unbiasedness are the same property" — they are related but distinct: one is an asymptotic guarantee, the other a finite-sample one → `ols-properties` |
| R10 | recall | short-answer | 0.43 | What is required, beyond the Gauss-Markov assumptions, for β̂ to be exactly Normally distributed rather than just BLUE? | Normally distributed errors (or a large enough n for the CLT to make it approximately Normal) | — |
| R11 | recall | short-answer | 0.48 | Fill in the blank: as n→∞, β̂ converges in probability to ___. | β, the true parameter vector | — |
| R12 | recall | mcq | 0.58 | Unbiasedness means: | E[β̂] equals the true parameter β exactly, for any sample size | claims it means "β̂ equals β in every individual sample" — unbiasedness is a statement about the estimator's average over repeated samples, not about any single sample's exact value → `ols-properties` |
| R13 | recall | short-answer | 0.63 | What does the "efficiency" property in BLUE refer to? | having the minimum variance among all linear unbiased estimators | — |
| R14 | recall | mcq | 0.68 | True/false: Var(β̂) depends on σ². | True — Var(β̂)=σ²(XᵀX)⁻¹ scales directly and linearly with σ² | claims "Var(β̂) depends only on the design matrix X, not on σ²" — σ² is an explicit multiplicative factor in the formula → `ols-properties` |
| R15 | recall | mcq | 0.78 | Which factor does NOT appear in the formula Var(β̂)=σ²(XᵀX)⁻¹? | the sample mean of Y, ȳ | picks a quantity absent from the formula — Var(β̂) depends only on σ² and X's structure, never on ȳ → `ols-properties` |
| R16 | recall | short-answer | 0.88 | Fill in the blank: OLS achieves minimum variance among all ___ estimators, per Gauss-Markov. | linear unbiased | — |
| A2 | apply | numeric | 0.83 | σ²=4, and the relevant diagonal entry of (XᵀX)⁻¹ for β̂₁ is 0.5. Compute Var(β̂₁). `[verified: 2]` | Var(β̂₁) = σ² × 0.5 = 4 × 0.5 = 2 | — |
| A3 | apply | short-answer | 0.93 | Why does a larger sample size n generally shrink Var(β̂)? | more observations generally make the entries of XᵀX larger (more accumulated information about X), which makes (XᵀX)⁻¹ smaller, directly shrinking Var(β̂)=σ²(XᵀX)⁻¹ *(required: the explicit XᵀX-grows-so-its-inverse-shrinks mechanism)* | — |
| A4 | apply | derivation | 1.13 | Derive E[β̂]=β from exogeneity E[ε\|X]=0. | β̂=(XᵀX)⁻¹Xᵀy=(XᵀX)⁻¹Xᵀ(Xβ+ε)=β+(XᵀX)⁻¹Xᵀε; taking expectations conditional on X, E[β̂\|X]=β+(XᵀX)⁻¹XᵀE[ε\|X]=β+0=β *(required: the explicit substitution and the E[ε\|X]=0 step)* | — |
| A5 | apply | numeric | 1.23 | Using A2's result Var(β̂₁)=2, what is the standard error SE(β̂₁)? `[verified: 1.4142]` | SE(β̂₁)=√Var(β̂₁)=√2≈1.4142 | — |
| A6 | apply | short-answer | 1.33 | Why does consistency matter even for an estimator that is already unbiased in finite samples? | unbiasedness alone says nothing about how tightly β̂ clusters around β for any given n — it could have huge variance; consistency additionally guarantees that variance (and any bias) shrinks toward zero as n grows, so estimates actually become reliably precise with more data *(required: distinguishes the finite-sample guarantee from the asymptotic-precision guarantee)* | — |
| E2 | explain | short-answer | 1.43 | Why doesn't unbiasedness alone guarantee a good estimator? | an estimator can be unbiased yet have enormous variance, making any single estimate wildly unreliable even though it is "correct on average" over many hypothetical samples; a useful estimator needs both low bias and low variance, which is exactly why BLUE's "minimum variance among unbiased estimators" clause matters *(required: names that variance, not just bias, determines usefulness)* | — |
| E3 | explain | short-answer | 1.53 | Why can't Var(β̂)=σ²(XᵀX)⁻¹ be computed directly without an estimate of σ²? | σ² is itself an unknown population parameter (the true error variance), not something observed directly; in practice it is estimated by s²=SSE/(n−p−1) and substituted in, giving an *estimated* variance rather than the exact, unknowable true one *(required: names that σ² is unobserved and must be estimated)* | — |
| E4 | explain | short-answer | 1.63 | Why can an estimator be consistent yet biased for any finite sample size? | consistency is purely an asymptotic (n→∞) statement about where the estimator's distribution eventually concentrates; nothing about that limiting behavior prevents the estimator from having systematic bias at small or moderate n, as long as that bias shrinks to zero as n grows *(required: names that consistency constrains only the limit, not any finite n)* | — |
| E5 | explain | short-answer | 1.83 | Why does BLUE's "Best" specifically mean minimum variance, rather than some other notion of a good estimator? | Gauss-Markov's proof technique compares candidate linear unbiased estimators purely by their variance — under quadratic (squared-error) loss, the estimator with the smallest variance also has the smallest expected squared error, so "best" is precisely defined relative to that specific loss criterion, not an arbitrary preference *(required: names the squared-error-loss connection to variance minimization)* | — |
| T2 | transfer | short-answer | 2.03 | How is the sample mean's unbiasedness and consistency a special case of OLS's β̂ properties? | regressing Y on an intercept-only design matrix (X = a column of 1's) makes β̂₀ exactly the sample mean ȳ; ȳ being unbiased and consistent for the population mean is then just the p=0-predictor special case of β̂'s general unbiasedness and consistency *(required: names the intercept-only regression as the special case)* | — |
| T3 | transfer | short-answer | 2.13 | How does Var(β̂) shrinking as n grows reflect the same intuition as the Law of Large Numbers? | LLN says an average of more independent observations concentrates more tightly around the true mean as n grows; Var(β̂)=σ²(XᵀX)⁻¹ shrinking with more data is the identical phenomenon in a regression setting — more information accumulated in XᵀX plays the same concentrating role that more terms play in an LLN average *(required: the explicit LLN-concentration parallel)* | —

*Coverage: 16/6/5/3 — 30 items, 0.03…2.23.*

---

## SSR, SSE, SST (`ssr-sse-sst`)
*Prereq: Simple Linear Regression · ancestors 20 · b₀ = 1.02*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.02 | Define SST, SSR, SSE, and their identity. | SST=Σ(yᵢ−ȳ)² (total); SSR=Σ(ŷᵢ−ȳ)² (explained); SSE=Σ(yᵢ−ŷᵢ)² (unexplained); SST=SSR+SSE | — |
| R2 | recall | mcq | 0.32 | The identity SST=SSR+SSE holds: | exactly, always, for OLS regression, as a direct consequence of the normal equations' orthogonality | claims it holds "only approximately, for large samples" — it's an exact algebraic identity, not an asymptotic approximation → `ssr-sse-sst` |
| A1 | apply | numeric | 0.82 | SST=100, SSE=30. Find SSR. `[verified: 70]` | SSR=100−30=70 | — |
| E1 | explain | derivation | 1.52 | Prove SST=SSR+SSE using residual orthogonality. | expand SST=Σ[(yᵢ−ŷᵢ)+(ŷᵢ−ȳ)]² = SSE + 2Σ(yᵢ−ŷᵢ)(ŷᵢ−ȳ) + SSR; the cross term vanishes exactly because residuals are orthogonal to ŷ (and hence to ŷᵢ−ȳ, since ȳ lies in C(X) when an intercept is included) — the same orthogonality `normal-equations`'s T1 established *(required: the cross-term vanishing, with the orthogonality justification)* | — |
| T1 | transfer | short-answer | 2.02 | What structural parallel connects this decomposition to `law-of-total-variance`? | both split one total variability measure into an "explained" and "unexplained" piece — here via orthogonality, there via expectation identities — the same kind of decomposition move achieved by different mathematical mechanisms *(required: names both mechanisms and the shared decomposition structure)* | — |
| R3 | recall | short-answer | -0.08 | Fill in the blank: SST stands for ___. | Total Sum of Squares | — |
| R4 | recall | short-answer | -0.03 | Fill in the blank: SSE stands for ___. | Sum of Squared Errors (or residual sum of squares) | — |
| R5 | recall | short-answer | 0.07 | Fill in the blank: SSR stands for ___. | Sum of Squares due to Regression (the explained sum of squares) | — |
| R6 | recall | short-answer | 0.12 | What does SSE measure, intuitively? | the total squared distance between the observed values and the model's fitted values — the variability the model fails to explain | — |
| R7 | recall | mcq | 0.17 | Which of SST, SSR, SSE is also called the "residual sum of squares"? | SSE | picks "SSR" — SSR is the *explained* sum of squares, the opposite of the residual/unexplained one → `ssr-sse-sst` |
| R8 | recall | mcq | 0.22 | True/false: SSR is also sometimes called the "explained sum of squares." | True — SSR measures the variability in Y that the fitted model accounts for | claims "SSR is called the explained sum of squares only informally, and this is not standard terminology" — it is standard, widely-used terminology → `ssr-sse-sst` |
| R9 | recall | short-answer | 0.27 | Fill in the blank: SSE = Σ ___. | (yᵢ − ŷᵢ)², summed over all observations | — |
| R10 | recall | short-answer | 0.37 | Fill in the blank: SST = Σ ___. | (yᵢ − ȳ)², summed over all observations | — |
| R11 | recall | mcq | 0.42 | As a model fits the data better, what happens to SSE? | SSE decreases, approaching (but not necessarily reaching) zero | claims SSE "increases as the fit improves" — the opposite of what a better fit means → `ssr-sse-sst` |
| R12 | recall | mcq | 0.47 | True/false: SST changes depending on which regression model is fit to the data. | False — SST=Σ(yᵢ−ȳ)² depends only on the observed y values and their mean, never on the fitted model | claims "SST depends on the choice of predictors used in the model" — SST is computed from y alone, before any model is fit → `ssr-sse-sst` |
| R13 | recall | short-answer | 0.57 | What is the minimum possible value of SSE? | 0 — achieved only when every fitted value ŷᵢ exactly equals the observed yᵢ | — |
| R14 | recall | short-answer | 0.62 | Fill in the blank: SSR = Σ ___. | (ŷᵢ − ȳ)², summed over all observations | — |
| R15 | recall | mcq | 0.67 | Which of SST, SSR, SSE is entirely determined by the observed y values alone, independent of the fitted model? | SST | picks "SSE" — SSE depends directly on ŷ, which depends on the fitted model → `ssr-sse-sst` |
| R16 | recall | short-answer | 0.72 | State in words what SSR measures. | how much of the total variability in Y is captured by the model's fitted values, relative to the overall mean ȳ | — |
| A2 | apply | numeric | 0.77 | SST=200, SSR=150. Find SSE. `[verified: 50]` | SSE = SST − SSR = 200 − 150 = 50 | — |
| A3 | apply | numeric | 0.87 | SSE=45, SSR=155. Find SST. `[verified: 200]` | SST = SSE + SSR = 45 + 155 = 200 | — |
| A4 | apply | short-answer | 0.97 | If SSE=0 for a fitted model, what does that imply about the fit? | every fitted value ŷᵢ exactly equals the corresponding observed yᵢ — a perfect fit to the training data, with no residual error whatsoever | — |
| A5 | apply | numeric | 1.07 | SST=80, SSE=20. Find SSR. `[verified: 60]` | SSR = SST − SSE = 80 − 20 = 60 | — |
| A6 | apply | short-answer | 1.17 | Why is SST fixed across different candidate models fit to the same dataset? | SST=Σ(yᵢ−ȳ)² is computed entirely from the observed y values and their sample mean, neither of which changes when a different set of predictors or a different model form is tried — only SSR and SSE, which depend on ŷ, shift as the model changes *(required: names that SST depends only on y, not on X or the model)* | — |
| E2 | explain | short-answer | 1.32 | Why are SSE and SSR both always ≥ 0? | both are sums of squared real-number terms — (yᵢ−ŷᵢ)² and (ŷᵢ−ȳ)² respectively — and a square of a real number can never be negative, so each sum of such terms must be non-negative *(required: names the sum-of-squares-is-non-negative fact)* | — |
| E3 | explain | short-answer | 1.42 | Why does the exact identity SST=SSR+SSE require the model to include an intercept? | the cross-term in the expansion of SST vanishes only because the residuals e are orthogonal to (ŷ−ȳ1), which in turn requires ȳ1 to lie in C(X) — true precisely when X's columns include a column of 1's (the intercept); without an intercept, ȳ need not lie in C(X) and the identity can fail *(required: names the intercept's role in the orthogonality argument)* | — |
| E4 | explain | short-answer | 1.62 | Why is SSE's raw value alone not comparable across two different datasets? | SSE's magnitude depends on the scale and total variability of y itself — a dataset with larger SST will typically have larger SSE even for an equally good model, so SSE must be compared relative to SST (as R² does) rather than as a raw number across datasets *(required: names the need to normalize by SST for cross-dataset comparison)* | — |
| E5 | explain | short-answer | 1.72 | Why can SSR never exceed SST? | SST=SSR+SSE and SSE≥0, so SSR=SST−SSE≤SST always; geometrically, ŷ's distance from ȳ1 cannot exceed y's distance from ȳ1 once orthogonality forces e to only add non-negative squared length *(required: the algebraic SSR≤SST argument from SSE≥0)* | — |
| T2 | transfer | short-answer | 2.12 | How does this decomposition directly supply the numerator and denominator that `anova`'s F-statistic is built from? | ANOVA's F-statistic is (SSR/p)/(SSE/(n−p−1)) — exactly this cluster's SSR and SSE, each divided by their respective degrees of freedom, so ANOVA's entire test statistic is a direct reuse of this decomposition rather than a separately-computed quantity *(required: names SSR and SSE as literally ANOVA's numerator/denominator building blocks)* | — |
| T3 | transfer | short-answer | 2.22 | How does this decomposition directly give rise to `r-squared`'s formula? | R²=SSR/SST=1−SSE/SST is built entirely from this cluster's three quantities; the identity SST=SSR+SSE is exactly what guarantees R² is bounded between 0 and 1, since SSR≤SST (per E5) and SSE≥0 *(required: names both R² formulas as direct ratios of SSR/SSE to SST)* | —

*Coverage: 16/6/5/3 — 30 items, -0.08…2.22.*

---

## R² (`r-squared`)
*Prereq: SSR/SSE/SST · ancestors 21 · b₀ = 1.05*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.05 | Define R². | R² = SSR/SST = 1 − SSE/SST — the proportion of total variability in Y explained by the model | — |
| R2 | recall | mcq | 0.35 | For simple linear regression, R² equals exactly: | the square of the Pearson correlation coefficient, r² | picks "the slope β̂₁" — a different quantity that isn't generally equal to R² → `r-squared` |
| A1 | apply | numeric | 0.85 | r=0.8 in a simple regression. Find R². `[verified: 0.64, and independently confirmed R²=r² on real regression data]` | 0.8²=0.64 | — |
| E1 | explain | short-answer | 1.55 | Why does R² always increase (or stay the same) when adding any predictor, even an irrelevant one? | more predictors give OLS more flexibility to reduce SSE, never less, so R² can never decrease — exactly why R² alone is a poor model-selection criterion across models with different predictor counts, motivating adjusted R² and AIC/BIC *(required: the "more flexibility, SSE never increases" mechanism)* | — |
| T1 | transfer | short-answer | 2.05 | Why doesn't a high R² guarantee good predictions on new data? | a model with many predictors can achieve high training R² (per E1's monotonic increase) while generalizing poorly — exactly `overfitting-underfitting`'s phenomenon from the ML sweep; R² on training data is analogous to training accuracy, not test accuracy *(required: the explicit training/test parallel)* | — |
| R3 | recall | short-answer | -0.05 | Fill in the blank: R² = ___ (give both equivalent formulas). | R² = SSR/SST = 1 − SSE/SST | — |
| R4 | recall | short-answer | 0.0 | What is the range of possible R² values for an OLS model with an intercept, fit on its training data? | 0 to 1, inclusive | — |
| R5 | recall | mcq | 0.1 | True/false: R²=0 means the model explains none of the variability in Y beyond simply predicting the mean ȳ for everyone. | True — R²=0 corresponds to SSR=0, meaning the fitted values give no improvement over always predicting ȳ | claims "R²=0 means the model's predictions are exactly wrong (perfectly anti-correlated)" — that describes a very negative correlation, not R²=0, which means no explanatory power either way → `r-squared` |
| R6 | recall | short-answer | 0.15 | What does R²=1 indicate? | a perfect fit — every fitted value ŷᵢ exactly equals the observed yᵢ, so SSE=0 | — |
| R7 | recall | short-answer | 0.2 | Fill in the blank: R² is often described as the proportion of variance in Y "explained" by ___. | the model (its predictors/fitted values) | — |
| R8 | recall | mcq | 0.25 | For simple linear regression, R² is also known as: | the square of the Pearson correlation coefficient, r² | picks "the coefficient of variation" — a different, unrelated statistic → `r-squared` |
| R9 | recall | mcq | 0.3 | True/false: R² can be negative. | True — on data the model wasn't fit to (e.g. a test set), or for a model without an intercept, R² can go below 0 if the model fits worse than simply predicting ȳ | claims "R² is mathematically bounded below by 0 in all cases" — that bound holds only for training data of a model with an intercept, not in general → `r-squared` |
| R10 | recall | short-answer | 0.4 | What does the quantity "1 − R²" represent? | the proportion of the total variability in Y that remains unexplained by the model (SSE/SST) | — |
| R11 | recall | short-answer | 0.45 | Fill in the blank: r² equals R² only in the setting of ___. | simple linear regression (a single predictor) | — |
| R12 | recall | mcq | 0.5 | Which quantity does NOT appear in either standard formula for R²? | the sample size n | picks a quantity absent from R²=SSR/SST=1−SSE/SST — n itself doesn't appear directly in either formula → `r-squared` |
| R13 | recall | short-answer | 0.6 | State a common criticism of using raw R² to compare models with different numbers of predictors. | R² never decreases (and typically increases) when more predictors are added, even irrelevant ones, so it unfairly favors larger models regardless of whether they generalize better | — |
| R14 | recall | mcq | 0.65 | True/false: a high R² guarantees the model is causally correct. | False — R² only measures how well the model fits the observed data's variability; it says nothing about whether the relationship is causal versus merely correlational or confounded | claims "a sufficiently high R² is evidence the predictors cause changes in Y" — high fit quality does not establish causation → `r-squared` |
| R15 | recall | short-answer | 0.7 | Fill in the blank: an R² close to ___ indicates a poor fit, and close to ___ indicates a strong fit. | 0 indicates a poor fit; 1 indicates a strong fit | — |
| R16 | recall | short-answer | 0.75 | What statistic is commonly reported alongside R² to correct for the number of predictors used? | adjusted R² | — |
| A2 | apply | numeric | 0.8 | SSR=72, SST=90. Find R². `[verified: 0.8]` | R² = SSR/SST = 72/90 = 0.8 | — |
| A3 | apply | numeric | 0.9 | r=−0.6 in a simple regression. Find R². `[verified: 0.36]` | R² = r² = (−0.6)² = 0.36 | — |
| A4 | apply | short-answer | 1.0 | A model has R²=0.95 on its training data but R²=0.40 when evaluated on a held-out test set. What does this indicate? | severe overfitting — the model captured patterns specific to the training data (including noise) that don't generalize, exactly the training-vs-test-accuracy gap `r-squared`'s own T1 already warns about *(required: names overfitting as the explanation)* | — |
| A5 | apply | numeric | 1.1 | SSE=25, SST=100. Find R². `[verified: 0.75]` | R² = 1 − SSE/SST = 1 − 25/100 = 0.75 | — |
| A6 | apply | short-answer | 1.2 | Why can R² be negative when evaluated on a dataset different from the one used to fit the model? | R²'s bound of 0 relies on SSR≥0, which in turn relies on the fitted line being at least as good as predicting ȳ — a guarantee only proven for the *training* data the model was optimized on; on new data, a poorly-generalizing model's SSE can exceed the new data's own SST, pushing R² below 0 *(required: names that the ≥0 guarantee is training-data-specific)* | — |
| E2 | explain | short-answer | 1.35 | Why is R² bounded above by 1 on the training data the model was fit to? | R²=1−SSE/SST, and SSE≥0 always (E2 of `ssr-sse-sst`), so 1−SSE/SST≤1 with equality only when SSE=0 — a perfect fit *(required: the explicit SSE≥0-implies-R²≤1 argument)* | — |
| E3 | explain | short-answer | 1.45 | Why is R² alone insufficient to judge whether a model is "good," beyond concerns about overfitting? | R² says nothing about whether the model's assumptions hold (linearity, homoskedasticity), whether individual predictors are statistically significant, or whether the relationship is causal — a high R² can coexist with a badly misspecified or spuriously-fit model *(required: names at least one concern beyond overfitting)* | — |
| E4 | explain | short-answer | 1.65 | Why does R²'s formula make it invariant to the units Y is measured in, unlike SSE alone? | R²=1−SSE/SST is a ratio of two quantities that scale identically with Y's units (both SSE and SST are sums of squared Y-differences), so any unit-conversion factor appears in both numerator and denominator and cancels out, leaving a unitless number between 0 and 1 *(required: names the cancellation of the shared scaling factor)* | — |
| E5 | explain | short-answer | 1.75 | Why is R² not a measure of whether the correct functional form was used? | R² only measures how much of Y's variance the *fitted* model accounts for relative to predicting the mean — a linear model badly misspecified for a curved true relationship can still achieve a high R² if the linear approximation happens to track the data reasonably well over the observed range, without that meaning linearity is actually correct *(required: names that R² measures fit, not correctness of form)* | — |
| T2 | transfer | short-answer | 2.15 | How is R² conceptually analogous to the "proportion of variance explained" by the top components in PCA? | both express a ratio of "explained" variability to total variability — R²=SSR/SST here, and in PCA the fraction of total variance captured by the top k eigenvalues relative to the sum of all eigenvalues; both quantify how much of a fixed total variability budget a reduced representation (fitted values, or a low-dimensional projection) accounts for *(required: the explicit ratio-of-explained-to-total-variance parallel)* | — |
| T3 | transfer | short-answer | 2.25 | How does adjusted R²'s penalty for extra predictors foreshadow `aic-bic`'s penalized model comparison? | both correct the same underlying problem — that a raw fit statistic (R², or raw likelihood) can only improve by adding parameters — by explicitly subtracting a complexity penalty tied to the number of parameters, so a genuinely better-fitting bigger model must clear a bar, not just add any nonzero improvement *(required: names the shared subtract-a-complexity-penalty structure)* | —

*Coverage: 16/6/5/3 — 30 items, -0.05…2.25.*

---

## ANOVA (`anova`)
*Prereq: SSR/SSE/SST, F-Distribution, Hypothesis Test · ancestors 42 · b₀ = 1.38*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.38 | State the ANOVA F-test's hypotheses and statistic. | H₀: β₁=⋯=βₚ=0 (no predictor helps) vs. H₁: at least one is nonzero; F=(SSR/p)/(SSE/(n−p−1)), following an F-distribution under H₀ | — |
| R2 | recall | mcq | 0.68 | The F-statistic's numerator and denominator are each: | a "mean square" — a variance-like quantity (sum of squares divided by its degrees of freedom) | treats them as "unrelated quantities" that happen to be divided — missing why an F-distribution is the correct reference at all → `anova` |
| A1 | apply | short-answer | 1.18 | Why would we expect F≈1 under H₀, and why is a large F evidence against it? | under H₀, both SSR/p and SSE/(n−p−1) are just estimating the same noise variance, so their ratio should hover near 1; a large F means the "explained" mean square substantially exceeds the "noise" mean square, evidence the predictors genuinely carry signal *(required: the "both estimate the same noise variance under H₀" argument)* | — |
| E1 | explain | short-answer | 1.88 | Connect ANOVA's F-statistic directly to `f-distribution`'s original definition. | SSR/σ² and SSE/σ² each follow (scaled) chi-square distributions under H₀ and normality, via a Cochran's-theorem-style decomposition of SST into independent pieces; their ratio, scaled by degrees of freedom, is exactly the ratio-of-independent-chi-squares construction that defines the F-distribution *(required: names the independent chi-square decomposition explicitly, not just "F comes from chi-squares")* | — |
| T1 | transfer | short-answer | 2.38 | Why run the overall ANOVA F-test before individual coefficient t-tests? | running many individual t-tests, one per predictor, at α=0.05 each inflates the overall false-positive rate across the whole model — the same multiple-comparisons problem from `hypothesis-test`'s earlier T1; a single overall F-test asks one question about the whole predictor set at once, avoiding that inflation *(required: the explicit multiple-comparisons connection)* | — |
| R3 | recall | short-answer | 0.13 | Fill in the blank: MSR (mean square regression) = ___. | SSR/p | — |
| R4 | recall | short-answer | 0.18 | Fill in the blank: MSE (mean square error) = ___. | SSE/(n−p−1) | — |
| R5 | recall | short-answer | 0.23 | State the degrees of freedom for the numerator and denominator of the ANOVA F-statistic. | numerator: p; denominator: n−p−1 | — |
| R6 | recall | mcq | 0.28 | True/false: under H₀, E[F]≈1. | True — under H₀, both MSR and MSE are estimating the same noise variance, so their ratio hovers near 1 | claims "under H₀, F should be close to 0" — F near 1, not 0, is what's expected when both mean squares estimate the same variance → `anova` |
| R7 | recall | short-answer | 0.33 | What does a very small p-value for the ANOVA F-test indicate? | strong evidence against H₀ — at least one predictor's true coefficient is likely nonzero, i.e. the predictors collectively help explain Y | — |
| R8 | recall | mcq | 0.43 | Which columns does a standard ANOVA table typically report? | Source, degrees of freedom, sum of squares, mean square, and F-statistic | picks "Source, sample size, and R² only" — omitting the actual sum-of-squares/mean-square/F structure the table is built from → `anova` |
| R9 | recall | short-answer | 0.48 | Fill in the blank: F = ___ / ___, in terms of mean squares. | MSR / MSE | — |
| R10 | recall | mcq | 0.53 | True/false: rejecting H₀ in the overall F-test tells you exactly which predictor(s) matter. | False — it only says at least one predictor's coefficient is likely nonzero, not which one(s); individual t-tests (or a partial F-test) are needed to identify which | claims "a significant F-test means every individual predictor is significant" — the F-test is a single omnibus test, silent on which specific predictor(s) drive the result → `anova` |
| R11 | recall | short-answer | 0.58 | State the null hypothesis of the ANOVA F-test in multiple regression. | H₀: β₁=β₂=⋯=βₚ=0 (none of the predictors help) | — |
| R12 | recall | short-answer | 0.63 | State the alternative hypothesis of the ANOVA F-test. | H₁: at least one βⱼ ≠ 0 | — |
| R13 | recall | mcq | 0.73 | Which distribution does the ANOVA F-statistic follow under H₀, assuming Normal errors? | an F-distribution with p and n−p−1 degrees of freedom | picks "a standard Normal distribution" — the ratio of two independent chi-square-derived quantities follows an F-distribution, not a Normal one → `anova` |
| R14 | recall | short-answer | 0.78 | Fill in the blank: MSR has ___ degrees of freedom. | p | — |
| R15 | recall | short-answer | 0.83 | Fill in the blank: MSE has ___ degrees of freedom. | n−p−1 | — |
| R16 | recall | mcq | 0.93 | True/false: a large F-statistic is evidence against H₀. | True — a large F means MSR substantially exceeds MSE, suggesting the predictors explain more variability than noise alone would produce | claims "a large F-statistic supports H₀" — the opposite; a large F is evidence the predictors carry real signal, against the null of no effect → `anova` |
| A2 | apply | numeric | 0.88 | SSR=120, p=3, SSE=180, n=50. Compute the ANOVA F-statistic. `[verified: df=(3,46), MSR=40, MSE≈3.913, F≈10.22]` | MSR=SSR/p=120/3=40; MSE=SSE/(n−p−1)=180/46≈3.913; F=MSR/MSE≈10.22 | — |
| A3 | apply | short-answer | 0.98 | Interpret a p-value of 0.0001 for the overall F-test. | extremely strong evidence against H₀: if none of the predictors truly mattered, data this extreme (or more extreme) would occur only about 1 in 10,000 times by chance, so we conclude at least one predictor genuinely explains variation in Y *(required: the correct "assuming H₀ true" conditional framing)* | — |
| A4 | apply | numeric | 1.08 | p=4, n=30, SSR=200, SSE=100. Compute the ANOVA F-statistic. `[verified: df=(4,25), MSR=50, MSE=4, F=12.5]` | MSR=200/4=50; MSE=100/(30−4−1)=100/25=4; F=50/4=12.5 | — |
| A5 | apply | short-answer | 1.28 | Why would an insignificant overall F-test alongside individually significant t-tests be a suspicious/rare combination? | the F-test is generally at least as powerful as catching a signal that even one individual t-test detects, since the F-test's numerator directly aggregates the explained variation those significant predictors contribute; seeing individual significance without overall significance suggests something is off (e.g. multiple-comparisons luck, or a coding/computation error) rather than a typical, expected outcome *(required: names why this combination is atypical, not merely "possible")* | — |
| A6 | apply | short-answer | 1.38 | Describe what changes in the ANOVA table if p increases while n stays fixed. | the numerator degrees of freedom (p) increase and the denominator degrees of freedom (n−p−1) decrease; MSE's divisor shrinks, generally increasing MSE (all else equal) and making it harder to achieve a large F, reflecting the smaller reference sample available to estimate noise variance with more parameters fit *(required: names both df changes and their directional effect on MSE)* | — |
| E2 | explain | short-answer | 1.48 | Why does MSE serve as an (asymptotically) unbiased estimator of σ² regardless of whether H₀ is true? | MSE=SSE/(n−p−1) estimates the residual variance directly from the fitted model's leftover error, which reflects the true noise level σ² whether or not the predictors actually help — MSR, by contrast, is only an unbiased estimator of σ² specifically under H₀, since under H₁ it also picks up the predictors' genuine explanatory signal *(required: contrasts MSE's H₀-independence with MSR's H₀-dependence)* | — |
| E3 | explain | short-answer | 1.58 | Why is a single overall F-test preferred over simply eyeballing whether R² "looks high enough" to judge if predictors matter? | R² has no built-in notion of statistical significance or sampling variability — even pure noise predictors typically produce some nonzero R² by chance in a finite sample; the F-test formally accounts for how much R² (equivalently, SSR) would be expected purely from sampling noise given n and p, giving a principled significance threshold R² alone lacks *(required: names R²'s lack of a built-in significance framework)* | — |
| E4 | explain | short-answer | 1.68 | Why does the F-distribution, rather than a t- or chi-square distribution directly, arise naturally as ANOVA's reference distribution? | F is defined as the ratio of two independent chi-square-distributed quantities, each divided by its own degrees of freedom; SSR/σ² and SSE/σ² are exactly such independent chi-squares under H₀ and normality (via Cochran's theorem), so their scaled ratio is by construction an F-distributed quantity, not merely similar to one *(required: names the ratio-of-independent-chi-squares construction)* | — |
| E5 | explain | short-answer | 1.98 | Why can the overall F-test be significant even when none of the individual t-tests are significant? | with several correlated predictors, each one's *unique* contribution (what the t-test measures) can be small and non-significant even while their *combined* contribution (what the F-test measures) is substantial — the classic multicollinearity symptom `effect-of-adding-another-variable`'s T1 and `vif` describe, where shared explanatory power is hard to attribute to any one predictor alone *(required: names the shared/collinear-predictors explanation)* | — |
| T2 | transfer | short-answer | 2.18 | How is the ANOVA F-test a special case of comparing a full model against a nested null (intercept-only) model? | the ANOVA F-test compares the full model (all p predictors) against the reduced model with only an intercept — exactly the general nested-model comparison `effect-of-adding-another-variable`'s partial F-test performs for an arbitrary subset of predictors; ANOVA's F-test is that same partial-F machinery applied to the specific case where the reduced model drops every predictor at once *(required: names it as the all-predictors-dropped special case of the general partial F-test)* | — |
| T3 | transfer | short-answer | 2.28 | How does the p in ANOVA's degrees-of-freedom bookkeeping connect to `geometric-interpretation-of-ols`'s trace(H)=p result? | both p's are the same quantity: the number of fitted parameters, which `geometric-interpretation-of-ols` showed equals trace(H), the model's "effective degrees of freedom used"; ANOVA's df split (p and n−p−1) is exactly that same effective-parameter count partitioning the total n degrees of freedom between what the model used and what remains for estimating noise *(required: names trace(H)=p as the same p ANOVA's numerator df uses)* | —

*Coverage: 16/6/5/3 — 30 items, 0.13…2.38.*

---

## Effect of Adding Another Variable (`effect-of-adding-another-variable`)
*Prereq: Multiple Linear Regression, R² · ancestors 28 · b₀ = 1.18*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.18 | List the key effects of adding a new predictor to a regression. | R² always increases or stays the same; existing coefficients can change; the new variable's own significance depends on whether it explains variation not already captured by the others | — |
| R2 | recall | mcq | 0.48 | Adjusted R² differs from ordinary R² because it: | penalizes adding predictors that don't meaningfully improve fit, and can actually decrease when a useless predictor is added | claims it's "always larger than R²" — the opposite is typical when a weak predictor is added → `effect-of-adding-another-variable` |
| A1 | apply | short-answer | 0.98 | Why can adjusted R² decrease from a useless predictor while ordinary R² can only increase? | the adjustment formula explicitly penalizes the number of predictors used, so a tiny R² gain from a useless variable can be outweighed by the penalty for the extra parameter — resolving `r-squared`'s earlier cliffhanger about R² being a poor comparison tool *(required: the explicit penalty-outweighs-gain mechanism)* | — |
| E1 | explain | short-answer | 1.68 | Describe the partial F-test for adding a group of predictors. | it compares the increase in SSR (or decrease in SSE) from adding the new predictors against what chance alone would produce — a direct generalization of the individual-coefficient t-test to testing several coefficients simultaneously *(required: names it as a generalization of the single-coefficient t-test)* | — |
| T1 | transfer | short-answer | 2.18 | Why should individually-insignificant predictors alongside a highly significant overall F-test raise suspicion of multicollinearity? | when predictors are highly correlated with each other, their individual contributions become hard to disentangle, inflating individual standard errors even though they collectively explain substantial variation in Y — foreshadowing `vif` directly as the diagnostic for exactly this pattern *(required: names the disentangling difficulty as the specific mechanism)* | — |
| R3 | recall | short-answer | 0.03 | Fill in the blank: adjusted R² = ___. | 1 − (1−R²)·(n−1)/(n−p−1) | — |
| R4 | recall | mcq | 0.08 | True/false: adjusted R² can be negative. | True — unlike ordinary R², adjusted R² has no formal floor at 0 and can dip below it for a poorly-fitting model with many parameters relative to n | claims "adjusted R² is bounded between 0 and 1, just like ordinary R²" — the penalty term can push it below 0 → `effect-of-adding-another-variable` |
| R5 | recall | short-answer | 0.13 | What two models does the partial F-test compare? | a "full" model including a group of new predictors versus a "reduced" model without them | — |
| R6 | recall | short-answer | 0.23 | Fill in the blank: adding a predictor to an OLS model can never ___ SSE. | increase — SSE can only decrease or stay exactly the same | — |
| R7 | recall | mcq | 0.28 | Which of these commonly changes when a new correlated predictor is added to a model? | the estimated coefficients of the existing predictors | claims "the coefficients of existing predictors are mathematically guaranteed to stay fixed" — correlated predictors reshuffle each other's estimated effects, per `multiple-linear-regression`'s E1 → `effect-of-adding-another-variable` |
| R8 | recall | mcq | 0.33 | True/false: adjusted R² always increases when more predictors are added. | False — adjusted R² can decrease if a newly added predictor's contribution doesn't outweigh its complexity penalty | claims "adjusted R² behaves exactly like ordinary R², always increasing or staying flat" — that's precisely the property adjusted R² was designed not to have → `effect-of-adding-another-variable` |
| R9 | recall | short-answer | 0.38 | What does adjusted R²'s formula penalize, conceptually, that ordinary R² does not? | the number of predictors used (model complexity) — it explicitly discounts R² by a factor that grows as p increases relative to n | — |
| R10 | recall | short-answer | 0.43 | Fill in the blank: the partial F-test's null hypothesis is that the added predictors' coefficients all equal ___. | 0 | — |
| R11 | recall | mcq | 0.53 | Which quantity is guaranteed to never decrease when any predictor is added to an OLS model? | SSR (and correspondingly, ordinary R²) | picks "adjusted R²" — adjusted R² is specifically designed to be able to decrease, unlike SSR/ordinary R² → `effect-of-adding-another-variable` |
| R12 | recall | mcq | 0.58 | True/false: an added predictor's own OLS coefficient can come out to be exactly zero in the fitted model. | False, essentially — with continuous data and OLS's closed-form solution, the estimated coefficient is a continuous quantity that is almost never exactly 0, even for a truly irrelevant predictor | claims "OLS naturally sets irrelevant predictors' coefficients to exactly zero" — that's LASSO's distinctive behavior, not ordinary OLS's → `effect-of-adding-another-variable` |
| R13 | recall | short-answer | 0.63 | What does it mean if the partial F-test for a newly added group of predictors is not significant? | the added predictors, as a group, don't explain significantly more variability than would be expected by chance alone — there's insufficient evidence they collectively improve the model | — |
| R14 | recall | short-answer | 0.73 | Fill in the blank: SSR always ___ when a predictor is added to an OLS model. | increases or stays exactly the same (never decreases) | — |
| R15 | recall | mcq | 0.78 | The partial F-test generalizes which single-predictor test to testing multiple predictors simultaneously? | the individual coefficient t-test | picks "the overall ANOVA F-test" — the ANOVA F-test is itself a special case of the partial F-test (per `anova`'s T2), not the thing the partial F-test generalizes → `effect-of-adding-another-variable` |
| R16 | recall | mcq | 0.88 | True/false: adjusted R² is always less than or equal to ordinary R² (for p≥1). | True — the penalty factor (n−1)/(n−p−1) is ≥1 for p≥1, so subtracting a larger scaled (1−R²) term can only pull adjusted R² down, never above R² | claims "adjusted R² is always strictly greater than ordinary R²" — the adjustment is a penalty, pulling the value down or leaving it equal, never up → `effect-of-adding-another-variable` |
| A2 | apply | numeric | 0.83 | R²=0.75, n=50, p=4. Compute adjusted R². `[verified: 0.7278]` | adj R² = 1 − (1−0.75)(49)/(45) = 1 − 0.25×1.0889 ≈ 0.7278 | — |
| A3 | apply | numeric | 0.93 | R²=0.6, n=30, p=2. Compute adjusted R². `[verified: 0.5704]` | adj R² = 1 − (1−0.6)(29)/(27) = 1 − 0.4×1.0741 ≈ 0.5704 | — |
| A4 | apply | short-answer | 1.03 | Why can adjusted R² decrease even though raw R² increases whenever a predictor is added? | raw R²'s increase from the new predictor's (possibly tiny) SSR contribution can be smaller than the increase adjusted R²'s penalty term applies for using one more degree of freedom, so the net effect on adjusted R² can be negative even while raw R² ticks up *(required: names that the penalty can outweigh a small genuine gain)* | — |
| A5 | apply | short-answer | 1.13 | Two nearly identical (highly correlated) predictors are both added to a model. What typically happens to their individual significance and VIF? | both predictors' individual t-test significance tends to drop (their standard errors inflate, since each has little *unique* information left once the other is included), while their VIF values rise sharply, per `vif`'s definition of variance inflation from shared collinearity *(required: names both the significance drop and the VIF rise)* | — |
| A6 | apply | short-answer | 1.23 | When is it appropriate to use the partial F-test rather than a single t-test? | when testing whether a *group* of several predictors jointly contributes significantly, rather than testing one predictor's contribution alone — a single t-test only addresses one coefficient at a time and doesn't account for the predictors' potential joint or collinear effects *(required: names the group-vs-single-predictor distinction)* | — |
| E2 | explain | short-answer | 1.38 | Why does adding any predictor — even one that's pure noise — never mathematically decrease SSR? | OLS minimizes SSE by choosing the best coefficients within whatever set of predictors is available; adding a new predictor only expands the set of possible linear combinations to search over, so the optimal SSE found with the extra predictor can be no worse (and possibly better) than without it — the new coefficient can always be set to exactly 0 to recover the old fit as a special case *(required: names the "can always fall back to coefficient 0" argument)* | — |
| E3 | explain | short-answer | 1.48 | Why does adjusted R²'s penalty grow relatively larger when n is close to p? | the penalty factor (n−1)/(n−p−1) blows up as p approaches n−1, since the denominator shrinks toward 0 or even becomes small/negative — with little data relative to the number of parameters, each additional parameter costs proportionally more of the shrinking pool of "spare" degrees of freedom *(required: names the denominator-shrinking-toward-zero mechanism)* | — |
| E4 | explain | short-answer | 1.58 | Why is the partial F-test more appropriate than checking each new predictor's individual t-test when adding several predictors at once? | individual t-tests, run one at a time, inflate the overall false-positive rate across the group (the same multiple-comparisons issue `anova`'s T1 raises) and can also miss a joint effect that's spread thinly across several correlated new predictors; the partial F-test asks one combined question about the whole group at once, avoiding both problems *(required: names both the multiple-comparisons and the spread-thin-joint-effect issues)* | — |
| E5 | explain | short-answer | 1.78 | Why doesn't adjusted R² fully solve the "too many predictors" problem the way AIC/BIC attempt to? | adjusted R²'s penalty is fixed and somewhat ad hoc — it doesn't have the same theoretical grounding (KL-divergence minimization for AIC, Bayesian model posterior for BIC) that connects the choice of penalty size to a specific inferential goal, and it isn't directly comparable across models with different likelihoods or non-nested structures the way AIC/BIC are designed to be *(required: names that adjusted R²'s penalty lacks the theoretical grounding AIC/BIC provide)* | — |
| T2 | transfer | short-answer | 2.08 | How does adding predictors trade lower bias for higher prediction variance, connecting to `bias-variance-tradeoff`? | more predictors let the model fit the training data's patterns more closely (lower bias, since SSR can only rise), but each additional estimated coefficient adds its own sampling variability to predictions on new data — exactly the bias-variance tradeoff `bias-variance-tradeoff` describes, with adjusted R²'s penalty acting as one crude attempt to account for the variance side of that tradeoff *(required: names both directions of the tradeoff explicitly)* | — |
| T3 | transfer | short-answer | 2.28 | How is the ANOVA F-test's nested-model comparison (from `anova`'s T2) itself a special case of the partial F-test described here? | the partial F-test compares a full model against a reduced model missing some subset of predictors; `anova`'s F-test is exactly that comparison for the specific case where the "subset" is *every* predictor at once (reduced model = intercept only) — the general partial-F machinery introduced here is what ANOVA's F-test is built from, not a separate technique *(required: names ANOVA's F-test as the drop-everything special case of this general test)* | —

*Coverage: 16/6/5/3 — 30 items, 0.03…2.28.*

---

## Variance Inflation Factor (`vif`)
*Prereq: Effect of Adding Another Variable · ancestors 29 · b₀ = 1.20*

| # | Lvl | Fmt | b | Item | Key / rubric | Misconception → blame |
|---|---|---|---|---|---|---|
| R1 | recall | short-answer | 0.2 | Define VIF for predictor Xⱼ. | VIF_j = 1/(1−R²_j), where R²_j is from regressing Xⱼ on all other predictors | — |
| R2 | recall | mcq | 0.5 | A VIF of 1 (the minimum) means: | Xⱼ is completely uncorrelated with the other predictors (R²_j=0), so its coefficient variance isn't inflated at all | claims VIF=1 means Xⱼ "is perfectly correlated with the other predictors" — the opposite extreme → `vif` |
| A1 | apply | numeric | 1.0 | R²_j=0.75. Compute VIF_j. `[verified: 4]` | 1/(1−0.75)=4 | — |
| E1 | explain | short-answer | 1.7 | Why is VIF called a "variance inflation" factor specifically? | Var(β̂ⱼ) = [σ²/((n−1)Var(Xⱼ))]·VIF_j — VIF_j is exactly the multiplicative factor by which Xⱼ's coefficient variance is inflated compared to what it would be if Xⱼ were uncorrelated with the other predictors, directly cashing in `ols-properties`' R2 about Var(β̂) depending on collinearity structure *(required: the explicit multiplicative-factor formula, not just "it measures inflation")* | — |
| T1 | transfer | short-answer | 2.2 | Connect the VIF>5 or VIF>10 rule of thumb to `effect-of-adding-another-variable`'s earlier scenario. | a high VIF means a predictor's coefficient estimate is highly unstable precisely because its unique effect is hard to disentangle from other correlated predictors — exactly the symptom of individually-insignificant coefficients alongside a significant overall F-test described there *(required: the explicit connection to that earlier scenario)* | — |
| R3 | recall | short-answer | 0.05 | Fill in the blank: VIF_j = ___. | 1/(1 − R²_j), where R²_j is from regressing Xⱼ on all the other predictors | — |
| R4 | recall | short-answer | 0.1 | What is "tolerance" in relation to VIF? | tolerance = 1/VIF_j = 1 − R²_j — the complementary quantity, measuring the fraction of Xⱼ's variance NOT explained by the other predictors | — |
| R5 | recall | mcq | 0.15 | True/false: VIF can be less than 1. | False — since 0≤R²_j<1, 1/(1−R²_j)≥1 always; VIF is bounded below by 1, never smaller | claims "VIF ranges from 0 to infinity" — its formula guarantees a minimum of exactly 1, not 0 → `vif` |
| R6 | recall | short-answer | 0.25 | In the VIF formula, what is R²_j computed from — regress what on what? | regressing Xⱼ (the predictor of interest) on all the *other* predictors in the model (not on Y) | — |
| R7 | recall | mcq | 0.3 | What is a common rule-of-thumb VIF threshold flagged as concerning multicollinearity? | VIF above 5 or 10 | picks "VIF above 1" — VIF=1 is the *minimum*, indicating no collinearity at all, not a concerning threshold → `vif` |
| R8 | recall | short-answer | 0.35 | Fill in the blank: Tolerance = 1/___. | VIF (equivalently, tolerance = 1 − R²_j) | — |
| R9 | recall | short-answer | 0.45 | What does a VIF of 10 imply about R²_j? | R²_j = 0.9 — 90% of Xⱼ's variance is explained by the other predictors | — |
| R10 | recall | mcq | 0.5 | True/false: VIF is defined per-predictor, not as a single number for the whole model. | True — each predictor Xⱼ gets its own VIF_j, since each measures how well that specific predictor is explained by the others | claims "VIF is a single overall statistic summarizing the whole model's collinearity at once" — VIF is computed separately for each predictor → `vif` |
| R11 | recall | mcq | 0.55 | As R²_j → 1, what happens to VIF_j? | VIF_j → ∞, growing without bound | claims "VIF_j → 0" — the formula 1/(1−R²_j) blows up, not vanishes, as R²_j approaches 1 → `vif` |
| R12 | recall | short-answer | 0.65 | What does VIF diagnose? | the degree of multicollinearity — how much a predictor's coefficient variance is inflated due to its linear relationship with the other predictors | — |
| R13 | recall | short-answer | 0.7 | What does the acronym VIF stand for? | Variance Inflation Factor | — |
| R14 | recall | mcq | 0.75 | True/false: a high VIF for Xⱼ implies Xⱼ has no relationship with Y. | False — VIF says nothing about Xⱼ's relationship with Y; it only measures how well Xⱼ is explained by the *other predictors*, entirely separate from Y | claims "high VIF means the predictor is irrelevant to the outcome" — VIF is purely about redundancy among predictors, not about predictive relevance to Y → `vif` |
| R15 | recall | short-answer | 0.8 | What is the minimum possible value of VIF, and when is it attained? | 1, attained when R²_j=0 — Xⱼ is completely uncorrelated with the other predictors | — |
| R16 | recall | mcq | 0.9 | If Xⱼ is perfectly explained by the other predictors (R²_j=1), what happens to VIF_j (and to the model)? | VIF_j is undefined/infinite, reflecting perfect multicollinearity — XᵀX becomes singular and β̂ cannot be uniquely computed at all | claims "the model still fits fine, just with a very large but finite VIF_j" — R²_j=1 is the exact-singularity boundary case, not merely a large finite VIF → `vif` |
| A2 | apply | numeric | 0.85 | R²_j=0.9. Compute VIF_j. `[verified: 10]` | VIF_j = 1/(1−0.9) = 1/0.1 = 10 | — |
| A3 | apply | numeric | 0.95 | R²_j=0.5. Compute VIF_j. `[verified: 2]` | VIF_j = 1/(1−0.5) = 1/0.5 = 2 | — |
| A4 | apply | numeric | 1.05 | VIF_j=5. Find R²_j. `[verified: 0.8]` | VIF_j=1/(1−R²_j) ⟹ 1−R²_j=1/5=0.2 ⟹ R²_j=0.8 | — |
| A5 | apply | short-answer | 1.15 | Describe the auxiliary regression procedure used to compute VIF_j. | regress Xⱼ (treated as the response) on all the remaining predictors X₁,...,Xⱼ₋₁,Xⱼ₊₁,...,Xₚ; take the R² from that regression (R²_j), then plug it into VIF_j=1/(1−R²_j) *(required: names Xⱼ as the response in the auxiliary regression, not Y)* | — |
| A6 | apply | short-answer | 1.25 | Why does removing one of two highly collinear predictors reduce both their VIFs? | if X₁ and X₂ were each mostly explained by the other (their mutual correlation was driving both R²_j values up), removing either one eliminates the specific source of that mutual explanatory overlap, so the *remaining* predictor's R²_j (against whatever other predictors are left) typically drops sharply, lowering its VIF *(required: names that the mutual dependence, not an independent third factor, was the cause)* | — |
| E2 | explain | short-answer | 1.35 | Why is VIF defined via R²_j from an auxiliary regression, rather than via simple pairwise correlations between predictors? | pairwise correlations only catch collinearity between two predictors at a time; a predictor can be nearly perfectly predictable from a *combination* of several others while having only moderate pairwise correlation with any single one — the auxiliary regression's R²_j captures this multivariate redundancy that no single pairwise correlation could reveal *(required: names the multivariate-vs-pairwise distinction)* | — |
| E3 | explain | short-answer | 1.45 | Why does the common VIF>10 threshold correspond exactly to R²_j>0.9? | plugging R²_j=0.9 into VIF_j=1/(1−R²_j) gives exactly 10, so the VIF>10 rule of thumb is algebraically identical to flagging any predictor whose variance is more than 90% explained by the others — the threshold isn't arbitrary once translated back through the formula *(required: the explicit algebraic translation between the two thresholds)* | — |
| E4 | explain | short-answer | 1.65 | Why is a high VIF not itself evidence that the model is "wrong," only that its coefficient estimates are imprecise? | high VIF doesn't violate exogeneity or bias β̂ (per `ols-properties`); it only inflates Var(β̂ⱼ), per the exact multiplicative-factor formula `vif`'s own E1 establishes — the fitted model can still produce unbiased predictions and a valid overall fit even while individual collinear coefficients are too noisy to interpret reliably *(required: distinguishes imprecision from bias/misspecification)* | — |
| E5 | explain | short-answer | 1.85 | Why can simply dropping a predictor with high VIF be a poor fix if that predictor is theoretically important? | dropping a predictor that genuinely belongs in the model (per domain theory) risks omitted-variable bias if it's correlated with the outcome through a path not captured by the remaining predictors — trading a *precision* problem (high VIF, wide standard errors) for a potentially worse *bias* problem, which is a strictly worse trade *(required: names the omitted-variable-bias risk of dropping a real predictor)* | — |
| T2 | transfer | short-answer | 2.1 | How does VIF fit alongside residual plots as one of several diagnostic tools for checking `ols-assumptions`? | just as residual-vs-fitted plots diagnose violations of linearity and homoskedasticity, VIF specifically diagnoses violations (or near-violations) of the no-perfect-multicollinearity assumption — both are post-fit diagnostic checks that translate an abstract Gauss-Markov assumption into a concrete, computable number or picture a practitioner can act on *(required: names VIF's specific role as the multicollinearity-checking counterpart to residual plots)* | — |
| T3 | transfer | short-answer | 2.3 | How does the VIF>10 rule of thumb motivate turning to ridge regression as a fix, foreshadowing the regularization cluster? | ridge regression's added λI term directly counters the near-singular XᵀX that drives VIF sky-high, shrinking the wildly unstable coefficient estimates multicollinearity produces — exactly the problem VIF flags, addressed not by dropping predictors but by accepting a small bias in exchange for dramatically reduced variance, the bias-variance trade `regularization`'s A1 later formalizes explicitly *(required: names ridge's λI mechanism as directly targeting what makes VIF large)* | —

*Coverage: 16/6/5/3 — 30 items, 0.05…2.3.*

---

## Cluster misconception index

| Tag | Blame |
|---|---|
| Var(β̂) treated as independent of the design matrix's structure | `ols-properties` |
| SST=SSR+SSE treated as approximate rather than exact | `ssr-sse-sst` |
| R² confused with the regression slope | `r-squared` |
| adjusted R² assumed to always exceed ordinary R² | `effect-of-adding-another-variable` |
| VIF=1 misread as maximal rather than minimal collinearity | `vif` |

**Cluster total: 180 items across 6 concepts (30 each).** All numeric claims verified, including
R²=r² confirmed independently on actual regression data (0.81 both ways), not just the algebraic
identity, plus the added recall/apply arithmetic (SST/SSR/SSE splits, ANOVA F-statistics, adjusted R²,
and VIF↔R²_j conversions) checked by direct computation.
